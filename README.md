# arXiv Papers Bot 🤖

This repository automatically fetches and displays relevant papers from arXiv based on configured criteria.

## RSS Vercel Deployment [![An example of deployed RSS Server using vercel](https://img.shields.io/badge/Deployed-Example-blue)](https://arxiv.tachicoma.top/)

You can click this to deploy yours 

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/maydomine/arxiv_rss_bot)
## 📊 Statistics

- **Last Updated**: 2025-12-12 05:57:54 UTC
- **Total Papers Found**: 30
- **Categories Monitored**: cs.AI, cs.CL, cs.DC, cs.LG

## 📚 Recent Papers

### 1. [GoodSpeed: Optimizing Fair Goodput with Adaptive Speculative Decoding in Distributed Edge Inference](https://arxiv.org/abs/2512.09963)

**Authors**: Phuong Tran, Tzu-Hao Liu, Long Tan Le, Tung-Anh Nguyen, Van Quan La, Eason Yu, Han Shu, Choong Seon Hong, Nguyen H. Tran  
**Category**: cs.DC  
**Published**: 2025-12-12  
**Score**: 14.0  
**Type**: new  
**ArXiv ID**: 2512.09963v1  

#### Abstract
Large language models (LLMs) have revolutionized natural language processing, yet their high computational demands pose significant challenges for real-time inference, especially in multi-user server speculative decoding and resource-constrained environments. Speculative decoding has emerged as a pr...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：**GooDSPEED: Optimizing Fair Goodput with Adaptive Speculative Decoding in Distributed Edge Inference**

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
- **分布式边缘推理中的延迟与吞吐瓶颈**：大型语言模型（LLMs）在资源受限的边缘设备上部署面临高延迟、低吞吐的问题。
- **多服务器协作下的公平性挑战**：传统 speculative decoding 多假设单一客户端或理想化环境，忽视了多个异构 draft server 在共享中央验证服务器时的资源竞争与公平分配问题。
- **动态提示导致的非平稳性**：用户输入（如对话主题突变）会导致 token 接受率（acceptance rate）剧烈波动，影响系统稳定性。

### 🚀 提出的新方法与创新
- **GooDSPEED 框架**：提出一种基于**自适应 speculative decoding** 的分布式边缘推理框架，结合轻量级 draft 模型与集中式大模型验证机制。
  - 多个边缘 draft server 并行生成 speculative tokens；
  - 中央 verification server 批量并行验证这些 tokens，并反馈接受结果。
- **梯度调度算法（Gradient Scheduling Algorithm）**：
  - 动态调整每个 draft server 的 draft 长度 $ S_i(t) $，以最大化对数效用函数 $ \sum \log x_i $，实现**比例公平性（proportional fairness）**；
  - 利用平滑估计的 acceptance rate $ \hat{\alpha}_i(t) $ 和 goodput $ \hat{x}_i(t) $ 进行在线决策，适应非平稳负载。
- **理论收敛性分析**：
  - 基于 fluid sample path 分析，证明该算法在稳态下收敛到最优 goodput 分配；
  - 在动态负载下具有有界误差，具备渐近最优性。

### 🔍 相比现有方法的优势
| 方面 | GooDSPEED | 传统方法（如 Fixed-S, Random-S） |
|------|-----------|-------------------------------|
| 资源利用率 | 高，动态适配各 client 的接受率 | 固定分配，易造成资源浪费或拥塞 |
| 公平性 | 显式优化 log utility，保障弱模型也能获得合理带宽 | 忽视公平性，强者恒强 |
| 吞吐效率 | 更高的系统级 goodput（有效接受 token 数） | 受限于最慢 client 或低接受率模型 |
| 适用场景 | 支持异构设备、动态提示、多用户并发 | 多为单客户端或静态配置 |

---

## 2. 核心实验方法和设置

### 📚 使用的数据集
共使用 **8 个公开数据集**，覆盖多种典型应用场景：
- **指令微调 / 对话任务**：Alpaca, Awesome-ChatGPT-Prompts
- **长文本摘要**：CNN/DailyMail
- **推理与问答**：OpenOrca, Chatbot Arena
- **数学解题**：GSM8K
- **Text-to-SQL**：SPIDER
- **高难度长尾查询**：HLE

> 数据分布于 4–8 个 draft servers 上，模拟真实异构、多样化负载。

### ⚙️ 实验设置
| 组件 | 配置 |
|------|------|
| **Verification Server** | NVIDIA H100 GPU，运行 Qwen3-14B 或 Llama3.1-70B-Instruct-AWQ-INT4 |
| **Draft Servers** | NVIDIA L4 GPU，运行 Qwen3-0.6B ~ 3B 或 Llama3-1B/3B 等小型模型 |
| **Token Budget C** | 设为 6, 16, 20, 24, 28（依据内存占用 <75% HBM3 和延迟容忍度确定） |
| **最大输出长度** | 50 或 150 tokens |
| **客户端数量** | 4 或 8 个 draft servers |

### 📊 评估指标
- **Goodput**：单位时间内被目标模型接受的有效 token 数量（核心指标）
- **End-to-end Latency**：从请求发出到响应完成的时间
- **Utility Function**：$ U(x) = \sum_{i=1}^N \log x_i $，衡量系统整体公平与效率
- **Wall Time Components**：
  - Receiving Time（等待所有 draft 到达）
  - Verification Time（并行验证耗时）
  - Sending Time（返回结果时间）

### 🆚 基线方法对比
- **Fixed-S**：每个 draft server 固定生成 $ S_i = C/N $ 个 tokens
- **Random-S**：随机分配 $ S_i $，总和不超过 $ C $

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据
#### （1）Goodput 估计准确性（Figure 2）
- GooDSPEED 的平滑估计 goodput 曲线与实际测量值高度一致（MA 滤波后），标准差小，说明估计稳定可靠。
- 在 Qwen3 和 Llama3 设置下均表现良好，适用于不同规模 LLM。

#### （2）端到端延迟分解（Figure 3）
- 总体 wall time 与 Fixed-S 相当，优于 Random-S（低 5–25%）；
- **Verification Time 比 Fixed-S 低约 5%**，表明更优的工作负载平衡；
- Receiving Time 略高（因动态长度需等最慢 client），但可通过异步机制缓解。

#### （3）效用函数收敛性（Figure 4）
- GooDSPEED 在约 **400–600 轮迭代内收敛**；
- 最终 utility 显著高于 Fixed-S 和 Random-S（提升可达 20–40%）；
- 收敛速度符合理论预测（$ T/\beta \approx 400–600 $，取 $ \beta=0.5 $）。

#### （4）系统吞吐增益
- 在多种模型组合（Qwen/Llama）、客户端数量（4/8）、token budget 下，GooDSPEED 均实现：
  - **更高的系统级 goodput**
  - **更均衡的 per-client 吞吐分配**
  - **更强的抗波动能力**

### 🔍 消融实验（隐含分析）
虽然未明确列出消融表，但从设计可推断以下关键组件作用：
- **指数平滑（exponential smoothing）**：显著降低 acceptance rate 波动带来的控制震荡；
- **梯度调度机制**：相比固定策略，在非平稳环境下仍能逼近最优解；
- **并行批处理验证**：充分利用 GPU 并行计算能力，缩短验证时间。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **GooDSPEED 能有效提升分布式边缘 LLM 推理的 goodput 与公平性**：
   - 通过动态调度适应不同 draft server 的接受率变化；
   - 实现接近最优的资源分配，避免“木桶效应”。

2. **梯度调度算法具备理论保证与实践有效性**：
   - 在 fluid limit 下收敛至全局最优；
   - 即使在非平稳 prompt 条件下也保持稳定性能。

3. **系统可在数百轮内快速收敛**：
   - 实际部署中无需长时间训练即可达到高效状态；
   - 适合实时交互应用。

4. **验证了 speculative decoding 在边缘协同场景的可行性**：
   - 边缘设备负责快速起草，云端/中心节点负责精确验证；
   - 架构灵活，支持异构硬件与模型。

### ⚠️ 局限性
- **依赖准确的 acceptance rate 估计**：若 prompt 变化过于剧烈或 draft model 质量极差，可能导致估计偏差；
- **接收时间受最慢 client 影响**：动态 draft 长度引入同步等待开销；
- **未考虑网络带宽限制**：当前实验假设局域网环境，广域网部署可能增加通信延迟；
- **仅支持单个 verification server**：扩展至多验证节点尚未讨论。

### 🔮 未来工作方向
- 引入**异步调度机制**以减少接收等待时间；
- 探索**自适应 smoothing 参数**（如根据方差自动调节 $ \eta, \beta $）；
- 扩展至**多 verification server 架构**，进一步提升可扩展性；
- 结合 **quantization、distillation** 技术优化 draft model 质量与速度；
- 将框架应用于 **video generation、autonomous agents** 等多模态推理任务。

---

## 总结

✅ **GooDSPEED 是首个将 speculative decoding 与梯度资源调度相结合，用于解决分布式边缘 LLM 推理中 goodput 与公平性权衡问题的系统性框架**。  
📊 实验表明其在真实模型与多样化负载下，相比固定或随机调度策略，显著提升了系统吞吐、降低了延迟，并具备良好的理论收敛性和实用性，为未来边缘智能服务提供了重要技术路径。

</details>

---

### 2. [Parallel Decoder Transformer: Model-Internal Parallel Decoding with Speculative Invariance via Note Conditioning](https://arxiv.org/abs/2512.10054)

**Authors**: Logan Robbins  
**Category**: cs.AI  
**Published**: 2025-12-12  
**Score**: 13.0  
**Type**: new  
**ArXiv ID**: 2512.10054v1  

#### Abstract
Autoregressive decoding in Large Language Models (LLMs) is inherently sequential, creating a latency bottleneck that scales linearly with output length. While ``Decomposition-and-Fill'' methods like Skeleton-of-Thought attempt to parallelize generation via external orchestration, they suffer from \t...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Parallel Decoder Transformer: Model-Internal Parallel Decoding with Speculative Invariance via Note Conditioning*

---

## 1. 论文的主要贡献和创新点

### 解决的问题
大型语言模型（LLM）的自回归解码是**顺序执行**的，导致推理延迟随输出长度线性增长，严重制约长文本生成效率。虽然已有“分解-填充”类方法（如 **Skeleton-of-Thought (SoT)**）尝试通过外部并行化提升速度，但这些方法将模型视为黑箱，缺乏跨流通信机制，容易产生**Coherence Drift**（连贯性漂移），即并行流之间出现事实冲突或冗余。

### 提出的新方法与创新思路
本文提出 **Parallel Decoder Transformer (PDT)**，一种将并行化机制内嵌于模型内部的新型架构，其核心创新包括：

- **Speculative Note Conditioning (SNC)**：在冻结的预训练主干模型上引入轻量级适配器，使多个并行解码流可通过共享的动态潜在空间（Note Bus）进行语义同步。每个流可广播压缩后的语义“笔记”（notes），其他流通过交叉注意力读取，并由学习到的验证头控制信任程度。
- **模型内部协调机制**：不同于 SoT 等“绕模型”（around-the-model）方法，PDT 将协调逻辑直接集成进解码过程，实现**模型内部并行解码**（model-internal parallel decoding）。
- **参数高效训练策略**：仅训练占总参数 <5% 的轻量子模块（Stream Adapters、SNC Heads 等），主干模型权重保持冻结，显著降低计算开销。

### 相比现有方法的优势
| 维度 | 现有方法（如 SoT） | PDT |
|------|------------------|-----|
| 并行粒度 | Prompt-level 分解 | Prompt-level + Token-level 协调 |
| 通信机制 | 无（黑盒调用） | 内建 Note Bus 与 SNC 跨流同步 |
| 连贯性保障 | 弱（易发生 Coherence Drift） | 强（通过 Agreement Head 触发 Rollback 自我纠正） |
| 参数效率 | 高（无需训练） | 极高（仅训练 <5% 新增参数） |
| 推理一致性 | 差 | 可恢复近似串行语义 |

---

## 2. 核心实验方法和设置

### 数据集
- 构造了一个包含 **10,000 个多段落推理任务**的数据集，由 GPT-4 蒸馏生成。
- 每个样本包含：
  - “Teacher Plan”：教师模型的完整推理路径；
  - “Notes Contract”：结构化的语义承诺集合，用于监督并行流是否满足计划项。

### 实验设置
- **主干模型**：冻结的 **20B 参数 GPT-OSS** 模型。
- **硬件平台**：8×NVIDIA B200 GPU（每卡 180GB VRAM）。
- **训练配置**：
  - 全局 batch size 为 16（micro-batch 1）；
  - 启用梯度检查点（gradient checkpointing）；
  - 使用 **四阶段课程学习**（curriculum learning）逐步解锁模块训练。
- **并行流数**：默认 K=3~6。

### 评估指标
| 指标 | 定义 |
|------|------|
| **Coverage Precision** | 当系统标记某计划项为“已覆盖”时，实际正确的比例（越高越好） |
| **Coverage Recall** | 所有正确完成的计划项中，被系统识别的比例（越低表示保守） |
| **Validation Loss** | 验证集上的损失值 |
| **Rollback Rate** | 因不一致触发回滚操作的频率 |
| **Memory Utilization** | GPU 显存占用情况，用于验证参数效率假设 |

### 基线方法对比
- **Skeleton-of-Thought (SoT)**：作为主要外部并行化基线，存在明显的 Coherence Drift。
- **Full Fine-tuning**：作为参数效率的对比极端，用于揭示“Memory Cliff”现象。

---

## 3. 主要实验结果和性能指标

### 关键性能数据
| 指标 | PDT 结果 |
|------|---------|
| **Coverage Precision** | **77.8%**（最终阶段） |
| **Coverage Recall** | 4.91%（表明策略保守） |
| **Validation Loss** | 0.00（完美拟合验证集） |
| **SNC 收敛步数** | ~20k 步后出现相变式下降，40k+ 步趋于稳定 |
| **参数占比** | 可训练参数 < 总参数的 5% |

> 注：文中还提到在第 50,000 步时达到 **71.6% 的 self-correction precision**，体现自我纠错能力。

### 与基线方法对比
- **vs SoT**：
  - PDT 显著减少 Coherence Drift 和冗余生成；
  - 在多流一致性方面表现更优，得益于 Agreement Head 的显式验证机制；
  - 尽管 SoT 更简单，但 PDT 在复杂任务上语义完整性更高。
- **vs Full Fine-tuning**：
  - 全微调立即导致 **OOM（Out-of-Memory）**，因需额外 ~115GB 存储优化器状态；
  - 实验确认了“**Memory Cliff**”的存在——对于 20B+ 模型，全微调不可行于单节点部署；
  - 验证了 PDT 的参数高效设计不仅是优化，更是**必要条件**。

### 消融实验与分析（来自附录）
- **Zero-Initialization Gating**：若不初始化门控为零，则 SNC 注入会破坏预训练特征分布，导致训练不稳定。
- **Agreement Head 的作用**：移除该模块后，rollback 机制失效，Coherence Drift 显著上升。
- **Note Bus 容量控制**：过大的 Note Buffer 导致内存压力增加，且边际收益递减；建议结合 summarization 控制 `Cbus`。
- **Stochastic Cadence 模拟**（附录 E）：
  - 错误呈“突发性”（clustered）反而降低 rollback 频率（从 9.87% → 5.45%），说明 PDT 对真实场景中的 bursty inconsistency 具有鲁棒性。

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **模型内部并行解码可行**：通过 SNC 机制，可在冻结主干上实现多流协同生成，突破传统自回归瓶颈。
2. ✅ **无需全模型微调即可实现语义同步**：轻量级适配器足以学会跨流协调，保留原始模型能力的同时增强结构化输出。
3. ✅ **高精度自我纠正机制有效**：Coverage Precision 达 77.8%，证明系统能可靠判断生成内容是否符合预期语义。
4. ✅ **参数效率是大规模模型部署的关键**：实验证明，20B 级模型无法承受全微调带来的显存开销，“Memory Cliff”真实存在。
5. ✅ **保守策略优于激进覆盖**：低 Recall + 高 Precision 的组合提升了系统安全性，避免虚假进度声明。

### 方法的局限性
- **静态流数量**：当前并行流数固定，不能根据任务复杂度动态调整。
- **Note Schema 设计依赖先验**：Notes Contract 需人工或强 Teacher 模型构造，尚未完全自动化。
- **推理开销略有增加**：SNC 注意力与 Agreement 判断带来一定计算负担，虽远小于全微调，但仍高于纯自回归。
- **扩展性受限于共享总线**：当前 Note Bus 采用全连接模式，当流数 N > 6 时同步开销线性增长，影响可扩展性。

### 未来工作方向（作者提出）
1. **Dynamic Stream Allocation**：Planner Head 动态决定并行流数量。
2. **Hierarchical Note Schemas**：支持嵌套命名空间，实现递归任务分解。
3. **Hardware-aware SNC Kernels**：进一步优化 Note Read 操作的延迟，逼近批量解码速度。
4. **Security Analysis**：研究类似 speculative execution 的侧信道攻击风险（如信息泄露）。
5. **开放资源**：作者已开源代码、数据集与适配器权重，推动 model-internal parallelism 研究。

---

> 📌 **一句话总结**：  
> PDT 提出了一种**参数高效的模型内并行解码框架**，通过 **Speculative Note Conditioning** 实现多流语义同步，在不修改主干权重的前提下，以 <5% 的额外参数实现了高达 **77.8% 的 coverage precision**，解决了 SoT 类方法的 Coherence Drift 问题，并在硬件层面验证了其对“Memory Cliff”的规避能力，为下一代百B级模型的高效推理提供了新范式。

</details>

---

### 3. [Design Space Exploration of DMA based Finer-Grain Compute Communication Overlap](https://arxiv.org/abs/2512.10236)

**Authors**: Shagnik Pal, Shaizeen Aga, Suchita Pati, Mahzabeen Islam, Lizy K. John  
**Category**: cs.DC  
**Published**: 2025-12-12  
**Score**: 12.0  
**Type**: new  
**ArXiv ID**: 2512.10236v1  

#### Abstract
As both ML training and inference are increasingly distributed, parallelization techniques that shard (divide) ML model across GPUs of a distributed system, are often deployed. With such techniques, there is a high prevalence of data-dependent communication and computation operations where communica...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Design Space Exploration of DMA based Finer-Grain Compute Communication Overlap

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

在大规模分布式机器学习（ML）训练和推理中，**通信与计算的依赖关系**导致通信操作暴露在关键路径上（on-critical-path data-dependent communication），无法被有效隐藏。尤其是在 **tensor-sequence parallelism、context-parallelism 和 expert-parallelism** 等并行策略中，通信直接作为后续计算的输入，造成严重的性能瓶颈。

现有基于 **shard-level overlap** 的方法虽然尝试重叠通信与计算，但在以下方面存在局限：
- 仅支持 **peer-to-peer (P2P)** 通信模式，在全连接拓扑（如 AMD MI300X 的 Infinity Fabric）中利用率低；
- 重叠粒度粗，限制了后续操作的数据流细粒度；
- 在非交换机网络拓扑下性能下降严重（最高可达 3.9× 慢于串行执行）。

---

### **提出了什么新方法或新思路**

本文提出了一种新的细粒度通信-计算重叠机制 —— **FiCCO (Finer-Grain Compute-Communication Overlap)**，其核心思想是：

> 将通信操作分解到比 shard-level 更细的一层（例如，在 8-GPU 系统中，传输大小为 shard 的 1/8），实现更深层次的重叠。

#### 主要创新点包括：

1. **引入 FiCCO 架构**：
   - 通信按更细粒度分片（one-level deeper sharding）；
   - 支持 **all-to-all (A2A)** 通信模式，充分利用全连接拓扑带宽；
   - 允许灵活配置计算粒度（可匹配或高于通信粒度）；

2. **构建 FiCCO 设计空间**：
   - 探索多种调度策略，涵盖通信维度（1D/2D）、计算融合方式（fused/unfused）、执行均匀性（uniform/hetero）等组合；
   - 提出 8 种潜在调度方案，并分析其效率特征。

3. **系统化刻画重叠开销（Inefficiency Characterization）**：
   - 定义两类关键开销：
     - **Decomposition Inefficiency Loss (DIL)**：因算子拆分导致的单个操作变慢；
     - **Contention Inefficiency Loss (CIL)**：并发执行时资源争用带来的性能损失；
   - 分析不同 GEMM 参数（如 OTB、MT）对 DIL/CIL 的影响。

4. **提出启发式调度选择机制（Heuristics）**：
   - 基于 GEMM 的 **op-to-byte ratio (OTB)** 和 **memory traffic (MT)** 自动推荐最优 FiCCO 调度；
   - 结合硬件特性（峰值算力与带宽乘积）进行阈值划分。

5. **利用 GPU DMA 引擎卸载通信**：
   - 避免 GPU 核心参与通信调度，显著降低 CIL 中的 compute interference；
   - 提升缓存和内存子系统的可用性。

---

### **相比现有方法的优势**

| 维度 | Shard-based Overlap | FiCCO |
|------|---------------------|-------|
| 通信模式 | Peer-to-Peer (P2P) | All-to-All (A2A) |
| 拓扑适应性 | 适合交换机网络 | 更适配全连接直连拓扑（如 MI300X） |
| 数据流粒度 | 较粗（shard-level） | 更细，利于后续流水线优化 |
| 并发干扰 | 高（尤其 RCCL 方案） | 显著降低（通过 DMA 卸载） |
| 性能上限 | 受限于链路空闲 | 可接近理想重叠速度 |
| 调度灵活性 | 固定模式 | 多种调度可选，支持定制化 |

---

## 2. 核心实验方法和设置

### **使用的数据集 / 场景**

研究基于真实世界的大模型部署场景，选取了多个典型的 **ML workload**，主要包括：

- **Tensor-Sequence Parallelism (SP+TP)**：用于 Llama-2 和 Llama-3 模型中的 GEMM 层；
- **Expert Parallelism (EP)**：应用于 DeepSeek 和 Mixtral 等 MoE 模型；
- 所有测试均围绕 **GEMM + All-gather / All-to-all** 的数据依赖型操作展开。

具体 GEMM 实例见表 `Table I`，共 16 个典型矩阵乘法任务，覆盖不同 `(M, N, K)` 规模。

---

### **实验设置**

| 项目 | 配置 |
|------|------|
| 硬件平台 | **AMD MI300X Infinity Platform C**，8×MI300X GPU |
| 互联拓扑 | 全连接双向 Infinity Fabric，每链路 **64 GB/s 单向带宽** |
| 软件框架 | PyTorch + ROCm |
| 计算库 | hipBLASLt（高性能 GEMM 内核） |
| 通信库 | RCCL（标准集合通信）或 `hipMemcpyDtoDAsync`（DMA 卸载） |
| 并发机制 | 多 GPU stream 实现并发内核执行 |
| 内存管理 | 使用 symmetric memory 避免中间拷贝，支持 GPU 间直接访问 |
| 测量方式 | 运行 15 次，前 10 次预热，后 5 次取平均值 |

---

### **评估指标**

- **Speedup**：相对于 baseline 串行执行的时间加速比；
- **Geomean Speedup**：所有测试案例的几何平均加速；
- **Ideal Speedup**：假设无 DIL/CIL 下的理想加速上限；
- **DIL / CIL 开销量化**：分别测量因分解和争用造成的性能退化；
- **Heuristic 准确率**：预测最佳调度的成功率。

---

### **基线方法对比**

| 基线方法 | 描述 |
|--------|------|
| **Baseline Serial** | 先完成 all-gather，再启动 GEMM，无任何重叠 |
| **Shard-based Overlap** | 类似 PyTorch AsyncTP，采用 P2P 逐 shard 通信并重叠计算 |
| **FiCCO-RCCL** | 使用 RCCL 进行通信（由 GPU core 控制） |
| **FiCCO-DMA** | 使用 DMA 引擎卸载通信（本文主推方案） |
| **Triton-Distributed** | 尝试对比，但因 OOM 未能运行成功 |

---

## 3. 主要实验结果和性能指标

### **关键性能数据**

| 方法 | 最高 Speedup | Geomean Speedup | 相对于 Ideal 的占比 |
|------|-------------|------------------|-----------------------|
| **Shard-based Overlap** | < 1.0×（部分负加速） | ~0.8–0.9× | 不适用（未达正加速） |
| **FiCCO (1D, DMA)** | **1.6×** | ~1.4× | 达到 ideal 的 **52–76%**（经 DIL/CIL 调整后） |
| **FiCCO (2D, Emulated)** | **1.7×** | ~1.5× | 同上 |
| **FiCCO-RCCL** | ~1.3× | ~1.2× | 明显低于 DMA 版本 |

> 注：由于当前 DMA 不支持 2D memory copy，2D 方案通过 1D 模拟实现。

---

### **与基线方法的对比结果**

- **相较于 shard-based overlap**：
  - FiCCO 实现了从“无加速”到“最高 1.6× 加速”的跨越；
  - 在 MI300X 全连接拓扑上，shard-based 因 P2P 导致大量链路闲置，通信效率下降高达 7×；
  - FiCCO 利用 A2A 模式充分饱和带宽，避免链路浪费。

- **DMA vs. RCCL**：
  - 使用 DMA 卸载通信后，CIL 显著降低：
    - GEMM CIL 从 1.11×（RCCL）降至更低水平；
    - 通信 CIL 也从 1.12× 得到缓解；
  - 表明 **DMA 是实现高效重叠的关键支撑技术**。

---

### **消融实验结果**

#### （1）DIL 分析

- **GEMM DIL**：
  - 64-way 分片比 8-way 分片带来更高 DIL（最大可达 2.3× slowdown）；
  - 当 GEMM 的 **OTB（算术强度）较低时，DIL 更严重**；
  - 行分片（M-sharding）在 M < K 时表现更差。

- **Communication DIL**：
  - FiCCO 的 all-gather 因小包传输产生约 **10% 几何平均延迟增加**；
  - 但大通信量下趋于带宽受限，DIL 影响减小。

#### （2）CIL 分析

- **GEMM CIL**：
  - 受 GEMM memory traffic (MT) 正向影响：MT 越高，CIL 越严重；
  - DMA 显著优于 RCCL，因其消除 compute interference。

- **Communication CIL**：
  - 同样随 MT 增加而上升；
  - geomean CIL ≈ 1.12×。

#### （3）Heuristic 准确性验证

- 在原始 16 个真实场景中：**准确率 100%**；
- 在额外生成的 16 个合成场景中：**准确率达到 81%**；
- 错误预测情况下，性能损失平均约为 **14% 的 speedup**。

---

## 4. 关键结论和发现

### **主要发现**

1. **更细粒度的通信-计算重叠（FiCCO）可行且必要**：
   - 相较于传统的 shard-level overlap，FiCCO 能解锁更多调度自由度；
   - 支持 all-to-all 模式，特别适用于现代全连接 GPU 拓扑（如 MI300X）。

2. **DMA 卸载通信至关重要**：
   - 显著减少 compute 和 cache contention，提升并发效率；
   - 是实现高比例重叠的前提条件。

3. **没有“一刀切”的最优调度**：
   - 不同 GEMM 参数（OTB、MT）导致不同的 DIL/CIL 权衡；
   - 必须根据 workload 特征动态选择调度策略。

4. **启发式调度具有强泛化能力**：
   - 基于 OTB 和 MT 的 heuristic 在 81% 的未知场景中仍能做出正确决策；
   - 可集成进 ML runtime 或编译器自动优化。

5. **FiCCO 显著缩小与理想性能之间的差距**：
   - 在实际部署中达到理想加速的 **52–76%**；
   - 相比 shard-based 方法，性能提升可达 **1.6×**。

---

### **方法的局限性**

1. **DMA 功能限制**：
   - 当前不支持 2D memory copy，限制了 2D 分片的实际性能；
   - 不支持带运算的通信（如 reduce-scatter with math），无法覆盖 tensor parallelism 全部场景。

2. **小操作的启动开销**：
   - 随着分片增多，kernel launch 次数上升，CPU 开销可能成为瓶颈；
   - 虽可通过 graph-based launch 缓解，但仍需关注。

3. **跨 GPU 执行差异**：
   - 观察到相同操作在不同 GPU 上执行时间差异 ≤6%，虽小但仍存在。

---

### **未来工作方向**

1. **扩展至其他通信原语**：
   - 如 reduce-scatter、broadcast 等，尤其是支持数学操作的 DMA 扩展。

2. **结合编译器自动化调度**：
   - 将 FiCCO 调度空间嵌入 MLIR 或 Triton 编译流程，实现自动代码生成。

3. **探索硬件协同设计**：
   - 如 ACE、T3 等专用模块可进一步降低 CIL，未来可考虑软硬协同优化。

4. **支持动态负载均衡**：
   - 在 MoE 场景中应对 token 分布不均带来的通信不对称问题。

5. **推广至更多模型架构和并行范式**：
   - 包括 vision transformer、diffusion models 等新兴结构。

---

> ✅ **总结一句话**：  
> 本文提出的 **FiCCO + DMA 卸载** 架构打破了传统 shard-level 重叠的局限，通过更细粒度的 all-to-all 通信调度，在真实大模型场景中实现了高达 **1.6× 的端到端加速**，并提供了可泛化的启发式调度规则，为下一代分布式 ML 系统的通信优化提供了重要设计指南。

</details>

---

### 4. [InfoCom: Kilobyte-Scale Communication-Efficient Collaborative Perception with Information Bottleneck](https://arxiv.org/abs/2512.10305)

**Authors**: Quanmin Wei, Penglin Dai, Wei Li, Bingyi Liu, Xiao Wu  
**Category**: cs.AI  
**Published**: 2025-12-12  
**Score**: 10.0  
**Type**: new  
**ArXiv ID**: 2512.10305v1  

#### Abstract
Precise environmental perception is critical for the reliability of autonomous driving systems. While collaborative perception mitigates the limitations of single-agent perception through information sharing, it encounters a fundamental communication-performance trade-off. Existing communication-eff...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# InfoCom: Kilobyte-Scale Communication-Efficient Collaborative Perception with Information Bottleneck  
——核心结论与实验结果总结

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

现代自动驾驶系统依赖**精确的环境感知**，而单智能体感知受限于视野遮挡等问题。**协同感知**（Collaborative Perception）通过多车之间共享信息提升感知能力，但面临一个根本矛盾：**通信开销与感知性能之间的权衡**。

现有方法（如 Where2comm、ERMVP）通常假设每次协作传输 **MB级数据**，但在实际车载网络中（如5G-V2X），带宽波动剧烈，平均仅约3.5 MB/s，极端情况下可低至0.4 MB/s，导致通信延迟甚至失败。此外，这些方法多基于**启发式特征操作**（feature manipulation），缺乏理论支撑，难以在极高压缩下保持感知精度。

### **提出了什么新方法或新思路**

本文提出 **InfoCom**，一种基于**信息瓶颈**（Information Bottleneck, IB）原理的新型通信高效协同感知框架。其核心是引入“**信息净化**”（information purification）范式，而非传统的特征选择或压缩。

#### 核心创新模块：
1. **Information-Aware Encoding (IAE)**  
   基于扩展的 IB 原理，将高维中间特征 $Z$ 映射为极低维的信息感知特征 $E \in \mathbb{R}^D$（$D \ll C\times H\times W$），保留任务相关的关键信息，同时最小化冗余。

2. **Sparse Mask Generation (SMG)**  
   生成稀疏空间掩码 $M$，标识对感知任务最关键的区域。该掩码经过**过滤**（Top-K）和**量化**（4-bit）处理，通信成本极低。

3. **Multi-Scale Decoding (MSD)**  
   在接收端，利用 $E$ 和 $M$ 进行**掩码引导的渐进式重建**，逐步恢复出可用于融合的 BEV 特征，而非简单重构原始特征图。

### **相比现有方法的优势**

| 维度 | InfoCom | 现有方法（如 Where2comm, ERMVP） |
|------|--------|-------------------------------|
| **通信效率** | **KB级**（2.7–7.9 KB） | MB级（0.5–34 MB） |
| **理论基础** | 基于 IB 的信息论分析，提供噪声抑制与信息保留的理论保证 | 多为启发式设计，缺乏理论支持 |
| **性能表现** | 接近标准协作（Standard Collaboration）性能 | 明显低于标准协作 |
| **模块化程度** | Plug-and-play，可无缝集成到现有模型（如 CoAlign, AttFuse） | 需定制化设计 |
| **抗噪能力** | 通过 IB 正则化与量化实现噪声抑制 | 无显式噪声建模 |

---

## 2. 核心实验方法和设置

### **使用的数据集**

在三个代表性协同感知数据集上进行验证：
- **OPV2V** (Xu et al., 2021)：仿真 V2V 数据集
- **V2XSet** (Xu et al., 2022)：仿真 V2X 数据集
- **DAIR-V2X** (Yu et al., 2022)：真实世界车-基础设施协同感知数据集

### **实验设置和评估指标**

- **任务**：3D 目标检测
- **评估指标**：
  - **Average Precision (AP)** @ IoU 0.3, 0.5, 0.7
  - **通信体积**（Communication Volume）：以 KB 或 MB 为单位，衡量每轮协作的传输量
- **主干模型**：
  - 默认使用 **CoAlign**（Lu et al., 2023）
  - 同时测试 **AttFuse** 和 **MKD-Cooper** 验证跨模型兼容性
- **硬件配置**：RTX 3090 GPU，Ubuntu 20.04，PyTorch 1.10

### **基线方法对比**

| 方法 | 类型 | 通信机制 |
|------|------|----------|
| **No Collaboration** | 基线 | 不通信 |
| **Late Collaboration** | 基线 | 仅传检测结果（~6.25 KB） |
| **Standard Collaboration** | 上限 | 传完整中间特征（~24–34 MB） |
| **Where2comm** (Hu et al., 2022) | 特征选择 | 基于空间重要性加权选择特征 |
| **ERMVP** (Zhang et al., 2024) | 特征压缩 | 空间滤波 + 聚类降维 |

---

## 3. 主要实验结果和性能指标

### **关键性能数据（来自 Table 1）**

| 数据集 | 方法 | 通信体积 | AP@30 | AP@50 | AP@70 |
|-------|------|---------|--------|--------|--------|
| **OPV2V** | Standard Colla. | 34.375 MB | 0.9709 | 0.9653 | 0.9229 |
| | Where2comm | 3.439 MB | 0.9548 | 0.9463 | 0.8820 |
| | ERMVP | 0.741 MB | 0.9618 | 0.9557 | 0.9127 |
| | **InfoCom (Ours)** | **7.875 KB** | **0.9702** | **0.9650** | **0.9202** |
| **V2XSet** | Standard Colla. | 34.375 MB | 0.9317 | 0.9212 | 0.8426 |
| | Where2comm | 3.439 MB | 0.8834 | 0.8604 | 0.7417 |
| | ERMVP | OOM | — | — | — |
| | **InfoCom (Ours)** | **7.875 KB** | **0.9360** | **0.9273** | **0.8488** |
| **DAIR-V2X** | Standard Colla. | 24.609 MB | 0.8294 | 0.7843 | 0.6353 |
| | Where2comm | 2.462 MB | 0.8048 | 0.7539 | 0.6070 |
| | ERMVP | 0.531 MB | 0.8217 | 0.7791 | 0.6324 |
| | **InfoCom (Ours)** | **5.922 KB** | **0.8228** | **0.7789** | **0.6385** |

> ✅ InfoCom 在所有数据集上均达到**接近标准协作的性能**，远超 Where2comm，并显著优于 ERMVP（尤其在 V2XSet 上后者因内存溢出无法运行）。

### **通信效率对比**

- **相比 Where2comm**：通信量减少 **440倍**
- **相比 ERMVP**：通信量减少 **90倍**
- **相比标准协作**：通信量减少 **超过 4000倍**

例如，在 OPV2V 上，InfoCom 仅需 **7.875 KB** 即可实现与 34.375 MB 相当的性能。

### **跨模型兼容性验证（Table 2）**

| 模型 | 方法 | 通信体积 | Mean AP |
|------|------|---------|--------|
| MKD-Cooper | Standard | 34.375 MB | 0.9575 |
| | Where2comm | 3.438 MB | 0.9134 |
| | ERMVP | 0.701 MB | 0.8946 |
| | **InfoCom** | **2.718 KB** | **0.9640** ✅ |

> InfoCom 不仅性能更高，还**提升了原模型 1.27% 的 Mean AP**，说明其信息净化机制能有效增强弱特征提取模型的表现。

### **消融实验结果（Table 3）**

| 变体 | Mean AP |
|------|--------|
| **InfoCom (Full)** | **0.9518** |
| w/o IAE（Simple Encoder） | 0.9320 ↓ |
| w/o SMG（Simple Generator） | 0.9379 ↓ |
| w/o STE（梯度中断） | 0.8845 ↓↓ |
| w/o Mask（无掩码引导） | 0.8839 ↓↓ |
| w/o Multi-Scale Rec. | 0.9439 ↓ |

> 所有组件均有正向贡献，尤其是 **Sparse Mask Generation** 和 **Multi-Scale Decoding** 对性能至关重要。

---

## 4. 关键结论和发现

### **主要发现**

1. **KB级通信可行且高效**：InfoCom 首次证明，在仅传输 **几KB** 数据的情况下，仍可实现**接近无损的协同感知性能**，突破了传统 MB 级通信的假设。
2. **信息净化优于特征操作**：通过 IB 原理指导的信息提取，比直接操作空间特征更有效，避免了高维冗余带来的信息损失。
3. **理论与实践结合**：首次为通信高效协同感知提供了基于信息论的理论分析（如噪声抑制界、熵约束），弥补了以往方法的经验主义缺陷。
4. **即插即用性强**：InfoCom 可作为通信层替换模块，适配多种主流协同感知架构，具备良好工程落地潜力。

### **方法的局限性**

- 当前消息单元仅包含**空间上下文**（sparse mask），尚未考虑**时间动态性**（temporal cues），无法判断未来帧是否需要通信。
- 极端压缩可能导致某些边缘场景（如密集遮挡）下的信息丢失，尽管整体性能稳定。

### **未来工作方向**

- 将当前双元组 $(E, M)$ 扩展为**三元组** $(E, M, T)$，加入**时间线索**，实现动态通信决策。
- 探索更高效的量化策略与编码方式，进一步降低通信延迟。
- 在真实车载网络环境下部署，验证其在复杂信道条件下的鲁棒性。

---

> 🔗 **代码地址**：[https://weiquanmin.github.io/infocom](https://weiquanmin.github.io/infocom)

</details>

---

### 5. [Zero-shot 3D Map Generation with LLM Agents: A Dual-Agent Architecture for Procedural Content Generation](https://arxiv.org/abs/2512.10501)

**Authors**: Lim Chien Her, Ming Yan, Yunshu Bai, Ruihao Li, Hao Zhang  
**Category**: cs.AI  
**Published**: 2025-12-12  
**Score**: 10.0  
**Type**: new  
**ArXiv ID**: 2512.10501v1  

#### Abstract
Procedural Content Generation (PCG) offers scalable methods for algorithmically creating complex, customizable worlds. However, controlling these pipelines requires the precise configuration of opaque technical parameters. We propose a training-free architecture that utilizes LLM agents for zero-sho...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Zero-shot 3D Map Generation with LLM Agents: A Dual-Agent Architecture for Procedural Content Generation

---

## 1. 论文的主要贡献和创新点

### 解决的问题
传统 **Procedural Content Generation (PCG)** 工具依赖于复杂、非直观且领域特定的技术参数配置，用户需具备专业知识才能有效控制生成过程。尽管 **Large Language Models (LLMs)** 被尝试用于自然语言驱动的 PCG 控制，但现成（off-the-shelf）LLMs 难以弥合**抽象用户指令**与**严格的参数规范**之间的语义鸿沟，常出现参数幻觉或错误配置。

此外，当前主流方法依赖**fine-tuning、强化学习或领域预训练**，这些方法资源消耗大、泛化能力差，难以适应不同工具或任务。

### 提出的新方法与思路
本文提出一种**无需训练的双智能体架构（Dual-Agent Actor-Critic Architecture）**，实现对 PCG 工具的零样本（zero-shot）控制：

- **Actor Agent**：作为语义解释器，将用户的自然语言提示（natural language prompt）转化为初步的 **Parameter Trajectory Sequence**（即一系列算法调用及其参数配置）。
- **Critic Agent**：作为静态验证器，基于提供的 **API 文档** 和 **参考示例（Reference Demonstration）** 对 Actor 提出的轨迹进行审查，识别并反馈“阻塞性问题”（如无效参数、越界值、不兼容算法组合等）。
- 两者通过**迭代对话机制**不断优化配置，直到 Critic 批准或达到最大迭代次数。

该框架完全依赖 **In-Context Learning (ICL)**，无需任何模型微调或额外数据收集。

### 相比现有方法的优势
| 维度 | 优势 |
|------|------|
| **无需训练** | 完全避免了 fine-tuning 或 RL 所需的大量标注数据和计算成本。 |
| **高泛化性** | 只需更换文档和示例即可迁移到新的 PCG 工具或其他专业软件（如 CAD、MATLAB），无需重新训练。 |
| **更强可靠性** | 通过 Critic 的显式验证机制显著减少参数幻觉和功能错误。 |
| **可解释性高** | 决策过程透明，可通过对话日志追溯修正路径。 |
| **零样本适用** | 在未见过的任务上也能有效运行，适用于快速原型设计。 |

---

## 2. 核心实验方法和设置

### 使用的环境与工具
- **目标平台**：Unity 引擎中的 **TileWorldCreator** 插件，支持多种 PCG 算法（如 Perlin Noise、Cellular Automata 等）用于生成 2D/3D 地形。
- **LLM 模型**：采用 **Claude 3.5 Sonnet** 作为基础 LLM，通过 API 调用。
- **Agent 构建**：基于 Unity 编辑器 AI 助手 **UGenLah** 实现自然语言到编辑器命令的映射。

### 实验设置

#### Experiment I: 复杂任务可靠性测试（Reliability）
- **任务**：生成满足四个约束条件的 3D 山地地图：
  1. 单一山峰（不能有多个分离的高地）
  2. 正好三层高度（three height layers）
  3. 草地点仅出现在山顶层
  4. 岩石散布在非山地区域
- **评估方式**：10 次独立运行，不允许后续交互（pure zero-shot）。
- **对比基线**：
  - **Single-Agent (Actor + Resources)**：单个 Agent，拥有相同文档和示例访问权限。
  - **Off-the-shelf LLM**：无上下文资源的原始 LLM。

#### Experiment II: 效率与自主性测试（Efficiency）
- **任务类型**：四种不同类型的地图（2D Beach, 3D Mountain Island, 3D Hilly Golf Course, 2D Escape Maze），每种使用不同的 PCG 流程。
- **允许迭代修正**：若失败，提供描述性反馈（如“The height layers are disconnected”），但不给出具体解决方案。
- **对比基线**：
  - **Actor-Critic**（本文方法）
  - **Single-Agent (w/ Docs)**
  - **Single-Agent (No Docs)**

### 评估指标

| 实验 | 指标 |
|------|------|
| **Experiment I** | 
| - **Task Success Rate (%)**：所有关键元素正确出现的比例 |
| - **Average Mistakes per Run**：每次运行中的次优参数选择数量 |
| - **Failure Reasons Analysis**：分类常见失败原因 |
| **Experiment II** |
| - **Token Usage**：总输入输出 token 数量（衡量计算开销） |
| - **Follow-up Prompts Required**：需要多少轮人工干预才能成功 |
| - **Objective Completion**：最终是否达成目标配置 |

---

## 3. 主要实验结果和性能指标

### Experiment I: 可靠性结果（3D Mountain Map）

| 指标 | Actor-Critic | Single-Agent (w/ Docs) |
|------|-------------|------------------------|
| **Success Rate (%)** | **80%** | 60% |
| **Relative Improvement** | **+33.3%** | — |
| **Average Mistakes (successful runs)** | 2.25 | 2.17 |

> ✅ 尽管错误数略高，但多为非致命性；而单智能体更易发生导致整体失败的关键错误。

#### 典型失败分析：
- **Single-Agent** 常见问题：
  - 50% 失败因未添加 reference layer
  - 50% 因缺少必要 Generator/Modifier
- **Actor-Critic** 显著改善了对“单一山峰”的理解，通过正确配置 Cellular Automata 参数避免碎片化地形（见 Figure 6）。

### Experiment II: 效率与自主性结果

| Map Type | Model | Tokens Used | Follow-up Prompts | Objective Achieved? |
|---------|-------|--------------|--------------------|---------------------|
| 2D Beach | Actor-Critic | 16,392 | **2** | ✓ |
|          | Actor+Resources | 18,987 | **4** | ✓ |
| 3D Mountain Island | Actor-Critic | 14,583 | **4** | ✓ |
|                      | Actor+Resources | 11,873 | **5** | ✓ |
| 3D Hilly Golf Course | Actor-Critic | **12,633** | **4** | ✓ |
|                      | Actor+Resources | 18,676 | **6** | ✓ |
| 2D Escape Maze | Actor-Critic | **4,589** | **2** | ✓ |
|                | Actor | 7,722 | 3 | ✓ |

> 🔍 **关键发现**：
> - 平均 **token usage 减少 12.7%**
> - **follow-up prompts 减少约 1.5 次/任务**
> - 尽管 Critic 增加推理步骤，但减少了无效尝试，提升了整体效率和自主性。

### 消融实验（隐含分析）
虽然未明确列出消融表，但从以下对比可得出：
- **无文档 vs 有文档**：缺乏文档时，LLM 完全无法正确调用 API，验证了静态资源注入的重要性。
- **有无 Critic**：Critic 显著提升成功率和稳定性，尤其在处理复杂约束时。

---

## 4. 关键结论和发现

### 主要结论
1. **Off-the-shelf LLMs 可被有效用作通用控制器**：无需 fine-tuning，仅通过注入文档和示例即可让 LLM 掌握复杂 PCG 工具的操作逻辑。
2. **Dual-Agent 架构显著优于单智能体**：Actor-Critic 分工机制能有效解决语义鸿沟问题，在零样本条件下实现更高成功率（↑20–33%）和更强鲁棒性。
3. **In-Context Learning 足以应对高维参数空间**：通过结构化 prompt 设计和迭代反馈，可在不更新模型权重的情况下完成精确参数配置。
4. **该范式具有广泛可迁移性**：不仅限于 PCG，还可推广至 CAD、科学仿真、音频合成等专业软件自动化场景。

### 方法的局限性
| 局限 | 说明 |
|------|------|
| **推理延迟较高** | 迭代循环增加了响应时间，不适合实时应用。 |
| **缺乏长期记忆** | 每次任务重置上下文，无法跨会话学习经验。 |
| **依赖高质量文档** | 若文档缺失或模糊，Critic 验证能力下降。 |
| **固定上下文窗口限制** | 多轮迭代可能导致信息丢失，影响一致性。 |

### 未来工作方向
1. **降低推理成本**：
   - 使用轻量级模型（如 Llama-3-8B 或 Phi-3）专门担任 Critic 角色。
   - 缓存常见成功的 **parameter trajectories** 以供复用。
2. **引入诊断元层（Diagnostic Meta-Layer）**：
   - 自动归类重复错误模式，实现从“被动纠错”到“主动预防”的转变。
3. **集成 Retrieval-Augmented Generation (RAG)**：
   - 构建向量数据库存储过往成功策略，增强零样本泛化能力。
4. **扩展至更多专业领域**：
   - 应用于 BIM、Digital Twin、Ansys 等工程与设计软件，推动“自然语言编程”普及。

---

> 📌 **总结一句话**：  
> 本论文证明了通过 **Actor-Critic 双智能体架构 + In-Context Learning**，可以无需训练地让 LLM 精确操控复杂的 PCG 工具，实现了从自然语言到可执行参数轨迹的可靠映射，为通用软件自动化提供了可扩展的新范式。

</details>

---

### 6. [A Kernel-based Resource-efficient Neural Surrogate for Multi-fidelity Prediction of Aerodynamic Field](https://arxiv.org/abs/2512.10287)

**Authors**: Apurba Sarker, Reza T. Batley, Darshan Sarojini, Sourav Saha  
**Category**: cs.LG  
**Published**: 2025-12-12  
**Score**: 10.0  
**Type**: new  
**ArXiv ID**: 2512.10287v1  

#### Abstract
Surrogate models provide fast alternatives to costly aerodynamic simulations and are extremely useful in design and optimization applications. This study proposes the use of a recent kernel-based neural surrogate, KHRONOS. In this work, we blend sparse high-fidelity (HF) data with low-fidelity (LF) ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：A Kernel-based Resource-efficient Neural Surrogate for Multi-fidelity Prediction of Aerodynamic Field

---

## 1. 论文的主要贡献和创新点

### 解决的问题
该研究针对**多保真度（multi-fidelity）气动场预测中的资源效率瓶颈**问题。传统基于深度神经网络的代理模型（surrogate models）在处理高维、非一致性的气动场数据时，往往需要大量可训练参数和计算资源，难以在资源受限场景下高效部署。

此外，高保真（HF）仿真成本高昂，数据稀缺；低保真（LF）数据丰富但精度不足。如何有效融合二者，在有限资源条件下实现高精度预测，是当前多保真建模面临的关键挑战。

### 提出的新方法
提出了一种基于核函数的新型神经代理模型——**KHRONOS**（Kernel Expansion Hierarchy for Reduced-Order, Neural-Optimized Surrogates），用于多保真度气动场预测。

其核心思想是：
- 将输入-输出映射表示为**低维子空间张量积的形式**；
- 利用**变分原理、插值理论和张量分解**构建模型结构；
- 采用**可分离的核扩展（separable kernel expansion）** 来学习每个输入维度上的低阶基函数（如二次B样条）；
- 所有维度通过外积组合形成最终响应，避免全连接层带来的参数爆炸。

### 相比现有方法的优势
- **极高的参数效率**：相比MLP、GNN、PINN等密集网络，减少94–98%的可训练参数。
- **更快的训练与推理速度**：在同等精度下，训练时间缩短40–73%，推理延迟低至2.44–3.64 ms。
- **无需中间潜空间对齐**：直接在原始特征空间进行残差校正，避免复杂的跨保真度特征匹配。
- **数学基础坚实**：基于插值与分离结构，支持重型剪枝（heavy pruning），适合嵌入优化流程。

---

## 2. 核心实验方法和设置

### 数据集
- **高保真（HF）数据集**：`AirfRANS` —— 包含1000个2D不可压缩RANS解算的NACA翼型案例，涵盖Re ∈ [2×10⁶, 6×10⁶]，AoA ∈ [-5°, 15°]，网格规模约250k–300k单元。
- **低保真（LF）数据集**：由`NeuralFoil`生成，基于XFoil训练的物理引导机器学习工具，模拟相同几何与工况下的气动场。

> 注：筛选出735个LF与HF一致性较高的样本用于主实验，另265个低一致性样本用于验证模型在困难案例上的修正能力。

### 实验设置
采用**delta learning架构**（残差学习）：
1. 先训练一个LF模型（基于NeuralFoil输出）；
2. 计算其在HF数据点上的预测误差 Δ = y_HF − y_LF；
3. 再训练一个“Δ模型”来预测该残差；
4. 最终多保真预测为：$ \hat{y}_{MF} = y_{LF} + \Delta $。

#### 三种训练场景（Case）
| Case | HF/LF Ratio | HF Training Data | LF Training Data |
|------|-------------|------------------|------------------|
| 1    | 0%          | 0                | 588              |
| 2    | 10%         | 59               | 588              |
| 3    | 30%         | 176              | 588              |

> 测试集固定为147个样本。

### 评估指标
- **R²（Coefficient of Determination）**：衡量表面压力系数 $ C_p $ 分布的预测准确性；
- **可训练参数数量（#Parameters）**：反映模型复杂度；
- **训练时间（Training Time per fold）**：单位秒；
- **推理时间（Inference Time per sample）**：单位毫秒；
- **缩放特性分析**：控制点数从16增至64，观察R²与参数增长关系。

### 基线方法对比
与以下三种主流架构进行公平比较：
- **MLP**：标准前馈全连接网络；
- **GNN**：图神经网络，利用翼型控制点链式拓扑进行消息传递；
- **PINN**：物理信息神经网络，嵌入势流方程约束（Laplace方程 + Bernoulli关系）。

所有模型均采用相同的delta learning框架，并通过grid search调优超参数。

---

## 3. 主要实验结果和性能指标

### 关键性能数据汇总（Case 3）

| 模型       | 参数量     | 推理时间 (ms) | 训练时间/fold (s) | Test R² |
|------------|------------|----------------|--------------------|---------|
| **KHRONOS** | **7,759**   | **3.64**        | **15**             | **~0.90** |
| MLP        | 139,554    | 8.54           | 30                 | ~0.91   |
| GNN        | 202,626    | 7.17           | 28                 | ~0.92   |
| PINN       | 139,554    | 8.27           | 32                 | ~0.91   |

> KHRONOS仅用**约5.5%的参数**即达到与基线相当的精度。

### 与基线方法的对比结果

#### （1）资源受限条件下的表现优势显著
- **时间限制实验（Fig. 6）**：
  - 在15秒训练预算内，KHRONOS已将预测误差（1−R²）降至0.1；
  - 而MLP/GNN/PINN仍处于高误差阶段（>0.3）；
  - 表明KHRONOS收敛更快，更适合实时或边缘部署。

- **参数限制实验（Fig. 7）**：
  - 在低参数区间（<10⁴），KHRONOS显著优于其他模型；
  - 随着参数增加，MLP/GNN/PINN提升趋于饱和，而KHRONOS保持稳定增益。

#### （2）无资源限制下的综合性能（Fig. 8）
- 所有模型最终都能达到较高R²（≥0.8）；
- KHRONOS在**Case 3中R²达~0.90**，接近最优基线（GNN ~0.92）；
- 但在参数量上领先两个数量级，且推理速度快**44–63%**，训练时间少**53–70%**。

#### （3）几何分辨率扩展实验（Fig. 9–10）
- 当控制点从16增至64：
  - 所有模型R²仅小幅提升（0.87 → 0.94），说明**几何分辨率已非瓶颈**；
  - 但MLP/PINN参数增长约15%，GNN增长超过2倍（20万 → 67万）；
  - KHRONOS参数仅从7,759增至17,897（+130%），远低于基线。

> 结论：**KHRONOS实现了近似“分辨率无关”的参数增长**，更适合高维几何优化任务。

### 多保真增益分析（Fig. 11–13）
- 在LF模型本身表现差的案例（R² < 0.7）中：
  - NeuralFoil严重低估前缘吸力峰（leading-edge suction peak）；
  - KHRONOS通过Δ模型成功恢复峰值位置与幅值；
  - 将其中**52.8%的案例提升至R² > 0.7**，显著改善低质量LF区域的预测可靠性。

---

## 4. 关键结论和发现

### 主要发现
1. **KHRONOS在资源效率方面具有压倒性优势**：
   - 在相似甚至略优的预测精度下，使用**94–98%更少的参数**；
   - 支持快速训练（最快3秒/折）与低延迟推理（最低2.44 ms）；
   - 特别适用于嵌入设计优化、不确定性量化等需多次调用代理模型的应用。

2. **分离式核结构天然适配多保真建模**：
   - 不依赖潜空间对齐，简化了LF→HF的信息融合路径；
   - 残差Δ模型能有效纠正LF模型在强分离、大攻角等复杂流动下的系统偏差。

3. **几何参数化细化收益递减**：
   - 控制点超过32后，精度提升有限；
   - 传统密集网络参数随分辨率急剧上升，而KHRONOS保持良好扩展性。

### 方法的局限性
- 当前评估局限于单一HF源（AirfRANS）与单一LF生成器（NeuralFoil）；
- 未考虑动态或瞬态气动问题；
- 对极端失速或三维效应的泛化能力尚未验证；
- 模型结构对高度非分离问题可能不如PINN灵活。

### 未来工作方向
- 扩展至更多HF数据集（如Transonic, 3D CFD）与多种LF代理；
- 探索自适应HF/LF采样策略（active learning）；
- 应用于**out-of-distribution airfoils** 和飞行包线外推；
- 结合不确定性量化模块，构建鲁棒的多保真Bayesian surrogate。

--- 

> ✅ 总结：**KHRONOS为资源受限环境下的多保真气动场代理建模提供了一个高效、紧凑且准确的新范式，平衡了精度与效率，展现出在工程设计自动化中的巨大潜力。**

</details>

---

### 7. [AEBNAS: Strengthening Exit Branches in Early-Exit Networks through Hardware-Aware Neural Architecture Search](https://arxiv.org/abs/2512.10671)

**Authors**: Oscar Robben, Saeed Khalilian, Nirvana Meratnia  
**Category**: cs.AI  
**Published**: 2025-12-12  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2512.10671v1  

#### Abstract
Early-exit networks are effective solutions for reducing the overall energy consumption and latency of deep learning models by adjusting computation based on the complexity of input data. By incorporating intermediate exit branches into the architecture, they provide less computation for simpler sam...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：AEBNAS: Strengthening Exit Branches in Early-Exit Networks through Hardware-Aware Neural Architecture Search

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
早期退出网络（Early-Exit Networks）通过在模型中间引入**exit branches**，允许简单样本提前退出，从而降低平均延迟和计算开销（如 MACs），特别适用于资源受限的边缘设备。然而，设计高效的早期退出网络面临四大挑战：
1. exit branches 的数量  
2. exit branches 的位置  
3. 退出策略（如置信度阈值）  
4. **exit branches 自身的架构设计**（深度、层数、类型等）

已有研究（如 EDANAS、NACHOS）虽利用 Neural Architecture Search (NAS) 自动优化前三个因素，但通常采用**固定结构的 exit branches**，忽略了其对整体效率与准确率的重要影响。

---

### 🚀 提出的新方法与创新点
本文提出 **AEBNAS**（Adaptive Early-Exit Branch NAS），一种硬件感知的多目标 NAS 框架，核心创新如下：

- **联合搜索 backbone 和 exit branches 架构**  
  不仅搜索主干网络结构，还同时优化每个 exit branch 的层数、卷积核大小（kernel size）、扩展率（expansion rate）、插值尺寸（interpolation size）以及是否添加 Max-Pooling 层。

- **动态调整 exit thresholds**  
  在每轮 NAS 迭代后，通过 grid search 调整各 exit branch 的 confidence threshold，以更好地平衡 accuracy 与 MACs，提升整体性能。

- **引入目标 MACs 约束作为优化目标的一部分**  
  在优化函数中加入 `target_macs` 超参数，使生成的模型可适配特定硬件资源限制，实现“按需设计”。

- **改进的搜索空间编码方式**  
  使用 threshold vector 统一表示 exit 数量、位置及阈值，简化搜索过程，并支持灵活控制 active exits。

> 🔍 创新本质：将 exit branches 视为可学习、可优化的模块，而非固定结构，显著增强其表达能力与效率。

---

### ⚖️ 相比现有方法的优势
| 方法 | 是否搜索 exit 架构 | 是否调优 threshold | 是否支持目标 MAC 控制 |
|------|---------------------|--------------------|------------------------|
| EDANAS [12] | ❌ 固定结构 | ✅ | ✅ |
| NACHOS [13] | ❌ 仅允许 MaxPool | ✅ | ✅ |
| **AEBNAS (Ours)** | ✅ 多层可变结构 | ✅（更精细） | ✅（显式约束） |

👉 AEBNAS 是首个在 NAS 中全面优化 exit branch 架构 + threshold + backbone 的框架，在精度与效率之间取得更好权衡。

---

## 2. 核心实验方法和设置

### 📊 使用的数据集
- **CIFAR-10**：50k 训练图像，10k 测试图像
- **CIFAR-100**：同上，类别更多，任务更复杂
- **SVHN**：73,257 张训练图像，26,032 张测试图像（街景数字识别）

所有数据集中均划分 10% 训练集作为验证集用于 NAS。

---

### ⚙️ 实验设置
| 参数 | 设置 |
|------|------|
| NAS 框架 | 基于 NSGANetV2 的 multi-objective NAS（NSGA-II） |
| 搜索迭代次数 | 30 |
| 初始种群大小 | 100 |
| 后续每轮候选数 | 8 |
| 每个架构训练 epoch 数 | 5（CIFAR-10/100/SVHN），CIFAR-100 最后两轮增至 250 |
| Surrogate Model | MLP 预测 accuracy 和 MACs |
| 目标 MACs | SVHN: 1M；CIFAR-10: 2M / 17M；CIFAR-100: 17M |
| Exit branch 可选配置 | kernel size ∈ {3,5}，expansion ∈ {1,2}，interpolation ∈ {8,10,12}，max-pooling 可选，最多两个 block |

---

### 📈 评估指标
- **Top-1 Accuracy (%)**
- **Average Number of MACs (M)**：反映平均计算成本
- **Number of Exits**：启用的 exit 分支数量
- **Exit Utilization (%)**：各 exit 分类的样本比例
- **Pareto 性能比较**：accuracy vs. MACs 曲线趋势

---

### 🆚 基线方法对比
| 类型 | 基线模型 |
|------|---------|
| 手动设计 | EEAlexNet（5 exits）、EEResNet20（10 exits） |
| NAS 设计 | EDANAS、NACHOS |

所有 NAS 方法保持一致超参设置以便公平比较。

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据（来自 Table II）

#### ✅ CIFAR-10（Target ~2.4M MACs）
| Model | MACs (M) | Top-1 Acc (%) | # Exits |
|-------|----------|---------------|--------|
| AEBNAS | **2.47** | **74.64** | 4 |
| EDANAS | 2.47 | 67.78 | 5 |
| NACHOS | 2.44 | 72.65 | 4 |

➡️ **AEBNAS 提升 +6.86% acc @相同 MACs**

#### ✅ CIFAR-10（High-budget: ~17M MACs）
| Model | MACs (M) | Top-1 Acc (%) |
|-------|----------|---------------|
| AEBNAS | **16.71** | **84.42** |
| EDANAS | 17.31 | 80.90 |

➡️ 更高精度，更低 MACs（-0.6M）

#### ✅ SVHN（Target 1M MACs）
| Model | MACs (M) | Top-1 Acc (%) |
|-------|----------|---------------|
| AEBNAS | **1.79** | **85.76** |
| EDANAS | 1.47 | 77.98 |
| NACHOS | 1.46 | 79.96 |

➡️ 尽管 MACs 略高，但 accuracy 提升超 **+5.8%**

#### ✅ CIFAR-100（Target 17M MACs）
| Model | MACs (M) | Top-1 Acc (%) |
|-------|----------|---------------|
| AEBNAS | **14.80** | **69.90** |
| EDANAS | 14.94 | 67.94 |

➡️ **精度提升 +1.96%，且 MACs 更低**

---

### 🔍 与其他手动模型对比优势
- **EEAlexNet / EEResNet20** 虽有一定 accuracy，但 MACs 高达 **200M~400M**，远高于 NAS 方法（<20M）
- AEBNAS 在极低 MACs 下实现更高或相当 accuracy，更适合边缘部署

---

### 📉 Exit Utilization 分析（Figure 3）
- AEBNAS 最佳架构倾向于禁用第一个 exit（Exit 1），而集中在 **Exit 2 和 Exit 3** 完成大部分预测（>80%）
- EDANAS/NACHOS 激活早期 exit，但由于其性能弱，导致：
  - 少量样本真正从此退出
  - 所有样本仍需经过该层 → 增加无谓计算开销
- 表明：**强化中间 exit branches 并关闭低效 early exits 更有利于整体效率**

---

### 📊 消融分析与趋势观察（Figure 2）
- 随着 NAS 迭代进行，模型逐渐收敛到高 accuracy、低 MACs 区域
- 引入 target_macs 约束和 threshold tuning 显著提升了搜索方向性和稳定性
- 在 CIFAR-100 上，即使初期训练仅 5 epochs，也能快速提升 accuracy >40%，最终达到 ~70%

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **exit branches 的架构设计至关重要**  
   允许 NAS 搜索 exit branch 内部结构（层数、kernel、expansion 等）可显著提升 accuracy 与效率。

2. **并非越多 exit 越好**  
   过多 exit（如 EEResNet20 的 10 个）反而增加冗余计算，降低整体性能。AEBNAS 自动选择最优数量与位置。

3. **早期 exit 若性能不足应被禁用**  
   第一个 exit 往往特征提取不充分，置信度低，强行使用会带来额外开销。AEBNAS 学会跳过它，直接依赖更强的后续 exit。

4. **threshold tuning 是关键环节**  
   动态调整 threshold 可有效控制 early-exit 行为，在给定 MACs 预算下最大化 accuracy。

5. **硬件感知目标引导更实用的设计**  
   显式设定 `target_macs` 使得模型可针对具体设备定制，增强了实际部署价值。

---

### ⚠️ 方法的局限性
- 当前 exit branch 搜索空间仍有限（最大 2 block），未来可探索更复杂结构。
- threshold tuning 使用 grid search，计算开销较大，可考虑替代优化算法（如 Bayesian Optimization）。
- 实验基于图像分类任务，未在真实边缘设备上测量真实延迟与功耗。

---

### 🔮 未来工作方向
1. 扩展 exit branch 搜索空间（如 Attention 模块、Transformer layers）
2. 结合 energy/power profiling 实现真正的 hardware-aware design
3. 探索其他 exit policies（非 margin-based）
4. 应用于目标检测、语义分割等 dense prediction 任务中的 early-exit 设计

---

## ✅ 总结
**AEBNAS 成功证明了：通过在 NAS 中联合优化 backbone 与 exit branches 架构，并结合 adaptive threshold tuning，可以设计出在 accuracy 和 efficiency 上均优于 state-of-the-art 的 early-exit networks。**  
该工作强调了 exit branch 不应是附属结构，而是决定整体性能的关键组件，为高效动态神经网络的设计提供了新范式。

</details>

---

### 8. [Phythesis: Physics-Guided Evolutionary Scene Synthesis for Energy-Efficient Data Center Design via LLMs](https://arxiv.org/abs/2512.10611)

**Authors**: Minghao LI, Ruihang Wang, Rui Tan, Yonggang Wen  
**Category**: cs.AI  
**Published**: 2025-12-12  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2512.10611v1  

#### Abstract
Data center (DC) infrastructure serves as the backbone to support the escalating demand for computing capacity. Traditional design methodologies that blend human expertise with specialized simulation tools scale poorly with the increasing system complexity. Recent studies adopt generative artificial...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文核心结论与实验结果总结

## 1. 论文的主要贡献和创新点

### 解决的问题
传统数据中心（DC）设计依赖人工经验与专用仿真工具，难以应对日益增长的系统复杂性。近期基于生成式人工智能（Generative AI）的方法虽能生成合理的室内布局，但**缺乏对物理规律的建模**，导致在需要满足严格物理约束（如温度、PUE等）的数据中心设计中产生“幻觉”（hallucinations），无法保证设计方案的可行性。

### 提出的新方法与思路
本文提出 **Phythesis** —— 一种结合大语言模型（LLMs）与物理引导进化优化的框架，用于自动化生成可直接仿真的（SimReady）节能型数据中心场景。其核心创新在于：

- **双层优化架构（Bi-level Optimization）**：
  - **LLM-driven Optimization Level**：由 Design LLM 负责生成三维布局并进行语义推理；Reflection LLM 对仿真轨迹进行分析与自我批评，迭代改进场景拓扑。
  - **Physics-informed Optimization Level**：利用可微分物理引擎通过反向传播优化资产参数，并从现实资产库中选择最接近“理想参数”的实际设备组合。
- **进化式场景合成（Evolutionary Scene Synthesis）**：将 LLM 的上下文学习能力作为“探索”机制，替代传统的遗传算子（如交叉、变异），实现高效且符合物理的设计演化。
- **闭环反馈机制**：仿真结果反馈至 Reflection LLM，形成“生成 → 仿真 → 批评 → 再生成”的闭环，显著减少 LLM 的物理幻觉。

### 相比现有方法的优势
- **兼顾创造性与物理一致性**：LLM 提供多样化设计探索，物理引擎确保方案满足热力学、功率等硬性约束。
- **更高的生成成功率与能效**：相比纯 LLM 方法，Phythesis 显著提升生成成功率达 57.3%，PUE 改善 11.5%。
- **适用于工业级复杂场景**：专为数据中心这类高可靠性、强物理耦合的工业环境设计，超越通用室内场景生成方法。

---

## 2. 核心实验方法和设置

### 数据集与资产库
- 使用一个**合成的 SimReady 资产库**，包含图3中定义的各类设备（Racks, ACUs, Chillers, Cooling Towers），每类提供10种型号。
- 外部环境数据来自 **EnergyPlus Weather (EPW)** 文件，涵盖热带、干旱、温带三种气候区域的历史气象数据（干球温度、湿球温度、湿度比等）。
- 设计需求共45条，覆盖小边缘（50–100服务器）、中集群（1k+）、大云规模（10k+）三种计算容量。

### 实验设置
- **主干 LLM**：使用 GPT-4（I-LM）和 o3-mini（R-LM）作为生成模型。
- **采样参数**：温度 = 0.7，每次生成 N=5 个候选，保留 Top-K=5 进行上下文反馈。
- **迭代次数**：默认 M=5 次迭代。
- **仿真引擎**：基于 PyTorch 构建的**可微分物理模型**（differentiable physics-based model），支持反向传播优化。

### 评估指标
| 指标 | 含义 | 优化方向 |
|------|------|----------|
| **PUE (↓)** | Power Usage Effectiveness，总能耗 / IT设备能耗，越低越好 | ↓ |
| **GSR (↑)** | Generation Success Rate，满足几何、功率、冷却约束的有效设计比例 | ↑ |

### 基线方法对比
| 方法 | 类型 | 描述 |
|------|------|------|
| **Random** | Direct | 随机生成，无物理先验或迭代 |
| **EA (Evolutionary Algorithm)** | Iterative | 不使用 LLM，仅靠种群进化优化 |
| **Vanilla LLM** | Direct | 单次 LLM 生成，无迭代或物理反馈 |
| **ABL (w/o reflect, phy)** | Ablation | 移除 Reflection LLM 和物理优化模块 |
| **ABL (w/o design, phy)** | Ablation | 移除 Design LLM 和物理优化 |
| **ABL (w/o phy)** | Ablation | 保留 LLMs，移除物理优化 |
| **ABL (w/o llm)** | Ablation | 仅保留物理优化，移除所有 LLM 模块 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（见 Table 1）
| 方法 | Small-Edge (PUE/GSR) | Medium-Cluster (PUE/GSR) | Large-Cloud (PUE/GSR) |
|------|------------------------|----------------------------|------------------------|
| **Vanilla LLM (R-LM)** | 1.628 / 0.947 | 1.218 / 0.782 | 1.162 / 0.813 |
| **Phythesis (R-LM)** | **1.285 / 0.997** | **1.179 / 0.988** | **1.175 / 0.970** |

- **PUE 改进**：在 Small-Edge 场景下降低 **0.343**（相对下降 21.1%），在 Medium-Cluster 下降低 0.039，在 Large-Cloud 下降低 0.013。
- **GSR 提升**：相比 Vanilla LLM，GSR 最高提升 **158.6%**（Large-Cloud, I-LM），平均提升超过 50%。

### 与基线方法对比
- 相比 **Random** 方法，Phythesis 在 GSR 上最高提升 **240.3%**（Large-Cloud, R-LM）。
- 相比 **EA** 方法，Phythesis 在 PUE 上平均降低 0.1–0.5，GSR 提升显著。
- 在所有任务维度上，Phythesis 均取得最优性能，验证了其泛化能力。

### 消融实验结果（Ablation Studies）
| 设置 | GSR 影响 | PUE 影响 | 结论 |
|------|---------|---------|------|
| **w/o phy (无物理优化)** | ↓2.4% ~ 7.9% | ↑0.2% ~ 2.4% | 物理优化对参数精细调优至关重要 |
| **w/o reflect (无反思LLM)** | 变化较小 | ↑0.4% ~ 0.6% | Reflection LLM 加速收敛，避免停滞 |
| **w/o design (无设计LLM)** | ↓28.7% ~ 30.2% | ↑明显 | Design LLM 提供高质量初始解 |
| **w/o llm (完全移除LLM)** | ↓42.6% ~ 45.9% | ↑0.374 | LLM 的语义推理不可替代 |

> 图7显示：完整 Phythesis 在5轮迭代后达到 PUE=1.188，而 ABL (w/o phy) 仅为 1.195，且收敛更慢。

### 可扩展性测试（Table 2）
当资产库从10增至50种型号时：
- Phythesis 在50资产库下仍保持 **GSR=0.973**，PUE=1.218。
- 相比 EA 方法，PUE 降低 **21.2%**，表明其在大规模组合搜索空间中的鲁棒性。

---

## 4. 关键结论和发现

### 主要发现
1. **LLM + Physics 是工业级生成设计的关键路径**：单纯依赖 LLM 容易违反物理规律，而引入可微分物理模型可有效纠正幻觉，提升设计可行性。
2. **双层优化协同增效**：LLM 层负责宏观拓扑探索，物理层负责微观参数精调，二者互补，显著优于单一范式。
3. **进化式反馈机制加速收敛**：通过 Reflection LLM 分析仿真轨迹并指导再设计，实现了类似“人类专家调试”的快速迭代。
4. **真实案例验证可持续价值**：在新加坡热带环境下，Phythesis 设计的边缘DC实现周均节电 **0.6 GWh**，年节电达 **31.2 GWh**，相当于为500万人口城市供电近两天。

### 方法的局限性
- **仿真保真度限制**：当前模型采用组件级抽象而非全三维 CFD 仿真，精度有限。
- **收敛性无理论保证**：进化过程依赖启发式策略，“优化后选择”可能陷入局部最优。
- **资产多样性受限**：目前仅支持风冷系统，未涵盖液冷（D2C）、背板换热器等先进技术。
- **多目标优化缺失**：当前仅以 PUE 为目标，未考虑水耗、碳排放、建设成本等其他可持续指标。

### 未来工作方向
- 扩展 SimReady 资产库，支持更多冷却技术与异构 IT 设备。
- 引入多模态编码器理解流场数据，提升对高维仿真输出的语义解析能力。
- 探索多目标优化框架（如 MOSAIC [23]），平衡能效、水效与经济性。
- 开发针对 LLM 幻觉的风险控制与微调策略，增强系统稳定性。
- 将 Phythesis 推广至其他能源基础设施设计领域（如智能建筑 HVAC、微电网等）。

---

> **总结**：Phythesis 成功构建了一个将 LLM 创造力与物理规律约束深度融合的生成设计框架，在数据中心这一典型工业场景中展现出卓越的自动化设计能力与显著的节能潜力，为下一代 AI-Driven 可持续基础设施设计提供了重要范式。

</details>

---

### 9. [TDC-Cache: A Trustworthy Decentralized Cooperative Caching Framework for Web3.0](https://arxiv.org/abs/2512.09961)

**Authors**: Jinyu Chen, Long Shi, Taotao Wang, Jiaheng Wang, Wei Zhang  
**Category**: cs.DC  
**Published**: 2025-12-12  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2512.09961v1  

#### Abstract
The rapid growth of Web3.0 is transforming the Internet from a centralized structure to decentralized, which empowers users with unprecedented self-sovereignty over their own data. However, in the context of decentralized data access within Web3.0, it is imperative to cope with efficiency concerns c...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：TDC-Cache: A Trustworthy Decentralized Cooperative Caching Framework for Web3.0

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

在 **Web3.0** 架构中，尽管去中心化存储（如 IPFS、SWARM）赋予用户数据主权，但由于以下原因导致**访问效率低下**：
- 分布式共识引入高网络延迟；
- 数据以分块（BLOB）形式存储，检索效率低；
- 缺乏高效的缓存机制，无法应对内容流行度的长尾分布。

此外，传统集中式缓存策略（如 CDN）不适用于 Web3.0 的去中心化架构，且现有方案缺乏对**恶意节点攻击**（如数据篡改、模型污染）的安全保障。

---

### **提出了什么新方法或新思路**

本文提出 **TDC-Cache** —— 一种可信的去中心化协同缓存框架，其核心创新包括：

#### （1）**两层架构设计：Decentralized Oracle Network (DON) 层**
- 引入 **DON 层**作为用户与去中心化存储之间的可信中介。
- 多个 **Oracle 节点**组成分布式缓存网络，具备本地缓存池（DRAM + NVMe SSD）和智能决策能力。
- 通过 **DLT 层**（区块链 + 去中心化存储）实现元数据管理与状态记录。

#### （2）**基于深度强化学习的去中心化缓存策略（DRL-DC）**
- 将缓存决策建模为 **Dec-POMDP**（Decentralized Partially Observable Markov Decision Process）。
- 采用 **QMIX** 算法进行多智能体协同训练，优化长期缓存命中率。
- 每个 Oracle 基于局部观察（请求序列、缓存状态）独立决策，实现去中心化自适应优化。

#### （3）**新型共识机制：Proof of Cooperative Learning (PoCL)**
- 针对 DRL 模型聚合过程中的拜占庭风险，提出 **PoCL** 共识协议。
- 包含四个阶段：Prepare → Train → Synchronize → Commit。
- 引入 **可验证协作证明（cooperative proof）**，由历史命中数和奖励构成，用于公平选择训练 Oracle。
- 通过 Commit 阶段投票机制防止恶意模型更新，确保学习一致性。

---

### **相比现有方法的优势**

| 维度 | 传统方法（如 LRU/LFU/FIFO） | TDC-Cache |
|------|----------------------------|-----------|
| 架构适配性 | 中心化设计，不适用于 Web3.0 | 完全去中心化，适配 DLT 架构 |
| 决策智能化 | 静态规则，难以适应动态流量 | 动态学习内容流行趋势，实时优化 |
| 安全性 | 无防攻击机制 | 支持拜占庭容错，抵御模型投毒 |
| 协同性 | 孤立缓存，缺乏协作 | 多 Oracle 协同学习与缓存共享 |

---

## 2. 核心实验方法和设置

### **实验环境配置**
- 操作系统：Ubuntu 24.04 LTS
- CPU：Intel Core i9-14900K
- GPU：NVIDIA GeForce RTX 4090（24GB VRAM）
- 内存：128GB RAM，1TB SSD

### **模拟参数设置**
- Oracle 数量 $ M = 4 $
- 缓存容量 $ C_m = 4 $（单位内容）
- 内容总数 $ F = 1000 $
- 用户数量 $ U = 10 $，每时隙生成 1000 请求
- 时间槽 $ T = 10,000 $
- 内容流行度服从 **Zipf 分布**，参数 $ \alpha \in \{0.5, 1.0, 1.5\} $

### **评估指标**
| 指标 | 定义 |
|------|------|
| **Cache Hit Rate** | 成功从 DON 层命中的请求比例 |
| **Content Retrieval Latency** | 从请求到接收内容的总耗时 |
| **Success Consensus Rate** | PoCL 各阶段成功完成的概率 |
| **Average Access Latency** | 所有请求的平均延迟 |

### **基线方法对比**
- **传统缓存策略**：
  - LRU（Least Recently Used）
  - LFU（Least Frequently Used）
  - FIFO（First In First Out）
  - Random
- **直接访问（Direct Access）**：绕过缓存，直接从 DLT 获取数据
- **共识对比**：与 **PBFT** 对比共识成功率

---

## 3. 主要实验结果和性能指标

### **关键性能数据汇总**

| 性能指标 | 提升幅度 | 说明 |
|--------|---------|------|
| 平均访问延迟降低 | **20%** | 相比直接访问 |
| 最大缓存命中率提升 | **18%** | 相比 LFU/LRU 等传统策略 |
| 成功共识率提升 | **10%** | 相比 PBFT，在高故障场景下更优 |

---

### **与基线方法的对比结果**

#### （1）**缓存命中率（Fig. 7 & Fig. 8）**
- 在 $ \alpha = 1.0 $ 下，**DRL-DC** 命中率达到约 **0.75**，显著高于：
  - LFU (~0.65)
  - LRU (~0.60)
  - Random (~0.55)
- 随着 $ \alpha $ 增大（流行度更集中），所有策略命中率上升，但 **DRL-DC 始终领先 10%~20%**
- DRL-DC 表现出更强的稳定性与适应性，尤其在长期运行中持续优化

#### （2）**内容检索延迟（Table II）**
| 场景（$ \alpha=0.5 $） | DRL-DC | LFU | LRU | Direct Access |
|-----------------------|--------|-----|-----|---------------|
| 小文件（1KB–256KB） | **6.90 ms/KB** | 7.13 | 7.16 | 8.19 |
| 大文件（>1MB）       | **1.24 ms/KB** | 1.28 | 1.29 | 1.46 |

- 在所有内容大小和流行度下，**DRL-DC 均实现最低延迟**
- 相比直接访问，延迟减少 **20% 以上**

#### （3）**共识成功率（Fig. 5 & Fig. 6）**
- 当节点失败概率 $ P_f = 0.1 $ 时：
  - **PoCL 成功率接近 1.0**
  - **PBFT 明显下降**
- 在大规模网络（$ M=300 $）中：
  - PoCL 成功率仍维持高位
  - PBFT 出现“拐点”后急剧下降
- PoCL 的**拐点比 PBFT 高出 209.47%**，表明更强的可扩展性

---

### **消融实验结果（隐含分析）**
虽然未明确列出消融实验表格，但从设计逻辑可推断：
- 若移除 **PoCL**，系统将无法抵御拜占庭节点，导致缓存策略被污染；
- 若替换 **DRL-DC** 为静态策略，则无法适应动态请求模式，命中率下降；
- 若取消 DON 层，则退化为直接访问，延迟显著增加。

---

## 4. 关键结论和发现

### **主要发现**
1. ✅ **引入 DON 层可有效桥接 Web3.0 中用户与去中心化存储之间的性能鸿沟**，显著降低访问延迟。
2. ✅ **DRL-DC 能够动态学习内容流行趋势**，在多种 Zipf 参数下均优于传统缓存策略。
3. ✅ **PoCL 共识机制兼具安全性与高效性**，相比 PBFT 更适合大规模、高动态性的去中心化网络。
4. ✅ 实验验证了 TDC-Cache 在 **延迟、命中率、共识可靠性**三个维度上的全面优势。

---

### **方法的局限性**
1. **计算开销较高**：每个 Oracle 需运行 DRL 模型，对轻量级设备可能不友好；
2. **依赖稳定通信**：Prepare 和 Synchronize 阶段需良好网络支持，否则影响共识效率；
3. **测试集同步依赖 OMC**：若智能合约被攻击，可能影响 PoCL 安全性；
4. **尚未部署于真实 Web3.0 网络**：当前为仿真环境，实际性能有待验证。

---

### **未来工作方向**
1. **激励机制设计**：鼓励更多节点诚实参与缓存与训练；
2. **轻量化 DRL 模型**：适配资源受限的边缘设备；
3. **动态网络拓扑支持**：应对频繁加入/退出的 Oracle；
4. **跨链缓存协同**：扩展至多条区块链生态；
5. **隐私保护增强**：结合 Federated Learning 进一步保护用户请求隐私。

---

> **总结一句话**：  
> TDC-Cache 是首个面向 Web3.0 的可信去中心化协同缓存框架，通过 **DON 层 + DRL-DC + PoCL** 的三位一体设计，在保证安全的前提下显著提升了去中心化系统的访问效率，为下一代互联网基础设施提供了重要参考。

</details>

---

### 10. [T-pro 2.0: An Efficient Russian Hybrid-Reasoning Model and Playground](https://arxiv.org/abs/2512.10430)

**Authors**: Dmitrii Stoianov, Danil Taranets, Olga Tsymboi, Ramil Latypov, Almaz Dautov, Vladislav Kruglikov, Nikita Surkov, German Abramov, Pavel Gein, Dmitry Abulkhanov, Mikhail Gashkov, Viktor Zelenkovskiy, Artem Batalov, Aleksandr Medvedev, Anatolii Potapov  
**Category**: cs.CL  
**Published**: 2025-12-12  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2512.10430v1  

#### Abstract
We introduce T-pro 2.0, an open-weight Russian LLM for hybrid reasoning and efficient inference. The model supports direct answering and reasoning-trace generation, using a Cyrillic-dense tokenizer and an adapted EAGLE speculative-decoding pipeline to reduce latency. To enable reproducible and exten...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# T-pro 2.0: An Efficient Russian Hybrid-Reasoning Model and Playground 论文总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前俄语大语言模型（LLM）生态系统存在以下关键问题：
- **高质量开源模型稀缺**：多数高性能俄语模型为闭源API服务（如YandexGPT、GigaChat），限制了研究可复现性和社区发展。
- **缺乏统一推理评测体系**：缺少针对俄语复杂推理任务（尤其是数学竞赛级）的权威基准。
- **多语言分词器对俄语支持不足**：通用多语言tokenizer在处理西里尔字母时效率低下，导致文本压缩率差、推理延迟高。
- **推理效率优化缺失**：俄语模型中尚未有效应用speculative decoding等先进解码加速技术。

### 提出的新方法与创新
T-pro 2.0 提出了一套完整的高效俄语混合推理系统，其核心创新包括：

#### （1）Cyrillic-dense Tokenizer
- 在Qwen3 tokenizer基础上，将34k低频非西里尔符号替换为高频俄语子词单元。
- 显著提升俄语文本编码效率：俄语维基百科中“每词平均token数”从3.12降至2.38，“≤2个token的单词占比”从38.2%提升至60.1%。
- 改进泛化至其他8种西里尔语言（如乌克兰语、哈萨克语等），均优于RuAdapt、GigaChat等基线tokenizer。

#### （2）Hybrid Reasoning Training Pipeline
- 支持两种模式输出：**direct answering**（快速响应）与 **reasoning-trace generation**（逐步推理）。
- 构建大规模俄语指令微调数据集 **T-Wix 500k**：
  - 包含约30k高质量俄语推理样本，经教师模型（Qwen3-235B）生成并由Reward Model筛选。
  - 引入“最近发展区”（Zone of Proximal Development, ZPD）理念，选择学生模型“跳一跳够得着”的任务进行蒸馏训练。

#### （3）EAGLE-style Speculative Decoding 加速推理
- 集成轻量级draft model（基于单层Llama-2 + FR-Spec组件），实现动态token树预测。
- 利用SGLang部署EAGLE-2的dynamic draft tree机制，在保证输出分布一致性的前提下显著降低延迟。

#### （4）发布完整开放生态
- 开源全部关键资源：
  - 模型权重（Apache-2.0）
  - 推理基准 **T-Math**（331道全俄/莫斯科数学奥赛题）
  - 指令数据集 **T-Wix 500k**（ODC-By）
  - EAGLE draft weights
- 提供交互式Web Demo（[http://t-pro-2-0.streamlit.app](http://t-pro-2-0.streamlit.app)），支持实时对比不同模式下的推理过程与性能指标。

### 相比现有方法的优势
| 维度 | 优势 |
|------|------|
| **语言适配性** | Cyrillic-dense tokenizer显著优于多语言tokenizer在俄语上的表现 |
| **推理能力** | 在原生俄语数学推理任务上超越所有开源及多数闭源模型 |
| **推理效率** | 平均提速1.85×，STEM领域最高达2.25×（T-Math） |
| **开放性** | 全栈开源，涵盖模型、数据、benchmark、inference pipeline |

---

## 2. 核心实验方法和设置

### 使用的数据集
| 类型 | 数据集名称 | 描述 |
|------|-----------|------|
| **预训练/中期训练** | Midtraining Mix (40B tokens) | 多源混合：俄语(49%)、英语(36%)、代码(9.3%)；覆盖Reasoning(34.6%)、General QA(28.8%)、Math(16.2%)等 |
| **SFT数据集** | **T-Wix 500k** | 自研最大规模俄语混合推理SFT数据集：<br>- 一般指令：468k<br>- 推理专项：~30k（翻译+教师蒸馏） |
| **评估基准** | **T-Math** | 新建俄语数学推理benchmark，331道高中奥赛题（1998–2025），自动评分（`\boxed{}` + `math_verify`） |
| | ruAIME / ruMATH-500 / ruGPQA / ruLCB | 英文推理benchmark的专业本地化版本 |
| | MERA / ruMMLU-Pro | 俄语常识理解与逻辑能力测试 |
| | Arena Hard Ru / WildChat Hard Ru | 俄语对话质量评估（使用DeepSeek-V3.1作为judge） |

### 实验设置与评估指标
- **上下文长度**：统一使用32k context window。
- **推理模式对比**：
  - Standard Mode（直接回答）
  - Reasoning Mode（显式推理链）
- **评估指标**：
  - 准确率（Pass@1, Score）
  - 推理速度（Speedup倍数）
  - 吞吐量（tokens/sec）
  - speculative decoding接受率（acceptance length）

### 基线方法对比
参与比较的主流模型包括：
- **开源俄语模型**：Qwen3-32B, RuadaptQwen3-32B-Instruct, Gemma 3 27B, DeepSeek-R1-Distill-Qwen-32B
- **闭源俄语模型**：YandexGPT5-Pro, GigaChat 2 Max
- **前沿通用模型**：DeepSeek-V3, DeepSeek-R1, GPT-4o, o4-mini

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自Table 4 & Table 5）

#### 📊 俄语综合能力（Table 4）
| 模型 | MERA | ruMMLU-Pro | Arena Hard Ru |
|------|-------|-------------|----------------|
| **T-pro 2.0 (Ours)** | **0.660** | **0.697** | **91.1** |
| Qwen3-32B | 0.582 | 0.677 | 83.95 |
| RuadaptQwen3 | 0.574 | 0.652 | 68.4 |
| GPT-4o | 0.642 | 0.714 | 85.14 |

> ✅ 在俄语常识与对话任务上接近GPT-4o，显著优于同类开源模型。

#### 🔢 俄语高级推理能力（Table 5）
| 模型 | T-Math | ruAIME 2024 | ruMATH-500 | Vikhr Math |
|------|--------|--------------|-------------|------------|
| **T-pro 2.0 (Ours)** | **0.541** | **0.704** | **0.940** | **0.799** |
| Qwen3-32B | 0.529 | 0.706 | 0.938 | 0.809 |
| DeepSeek-V3 | 0.278 | 0.319 | 0.882 | 0.613 |
| GPT-4o | 0.106 | 0.090 | 0.766 | 0.372 |

> ✅ 在T-Math这一原生俄语奥赛题基准上大幅领先闭源模型（GPT-4o仅0.106），且在ruAIME上远超DeepSeek-V3（0.704 vs 0.319）。

#### ⚡ 推理效率（Table 2）
| Benchmark | Speedup (Standard) | Speedup (Reasoning) | Acceptance Length |
|----------|--------------------|---------------------|-------------------|
| **Average** | **1.85×** | **1.83×** | ~3.39 |
| **T-Math** | — | **2.25×** | **4.01** |
| **ruCodeEval** | 2.15× | 1.84× | 3.93 |

> ✅ STEM类任务加速更明显（STEM平均1.99× vs 人文社科1.62×），证明speculative decoding在结构化内容中更有效。

#### 🌍 英语保留能力（Table 23）
尽管主训目标为俄语，T-pro 2.0仍保持强大英文推理能力：
| 模型 | AIME 2024 | MATH-500 | GPQA Diamond |
|------|-----------|-----------|---------------|
| **T-pro 2.0** | 0.765 | **0.966** | 0.641 |
| Qwen3-32B | 0.808 | 0.961 | 0.668 |

> ✅ 英文性能未因俄语优化而退化，具备良好跨语言泛化能力。

### 消融实验结果
#### （1）中期训练策略对比（Table 12）
| 配方 | ruAIME'24 | ruAIME'25 | ruGPQA |
|------|-----------|-----------|---------|
| Pre-train + Instruct | 0.60 | 0.47 | 0.58 |
| **Instruct-only** | **0.67** | **0.63** | **0.66** |

> ❗ 结论：对于已强预训练模型，加入原始pretrain数据反而不如纯instruction tuning有效。

#### （2）Tokenizer影响（Table 13）
| Tokenizer | MERA Macro-Avg |
|----------|------------------|
| Qwen3 (原版) | 0.560 |
| **T-pro 2.0 (Cyrillic-dense)** | **0.574** |

> ✅ 新tokenizer不仅不损害性能，反而略有提升，验证其有效性。

---

## 4. 关键结论和发现

### 主要发现
1. **针对性适配优于盲目扩展**：通过精心设计的tokenizer、instruction midtraining和推理蒸馏，可在不增大参数量的前提下显著提升俄语推理能力。
2. **Tokenizer是多语言LLM的关键瓶颈**：Cyrillic-dense tokenizer带来的压缩增益直接转化为推理速度优势，尤其在长文本场景下更为重要。
3. **Speculative Decoding适用于俄语推理**：EAGLE-style方法在俄语任务上实现平均1.85×加速，STEM领域可达2.25×，说明其跨语言适用性强。
4. **高质量俄语推理数据稀缺但可构建**：T-Math证明，即使小规模（331题）、高难度的原生俄语benchmark也能揭示模型真实差距，推动领域进步。
5. **俄语优化不影响英语能力**：合理的设计可实现双语平衡，避免“母语增强、外语退化”的常见问题。

### 局限性
- **Agent能力有限**：未专门优化tool use或多轮规划，agent行为表现与基础Qwen3-32B相当。
- **仅使用离线RL（DPO）**：未引入在线强化学习（如PPO、GRPO），可能限制鲁棒性。
- **长上下文未经实证验证**：虽支持RoPE scaling至128k，但未在真实长任务中测试一致性与检索能力。
- **部分训练数据依赖专有来源**：影响完全复现性，尽管T-Wix已公开以促进后续研究。

### 未来工作方向
- 增强工具调用与Agentic行为能力
- 探索在线RL（PPO等）进一步提升对齐质量
- 实测128k长上下文下的实际性能
- 扩展T-Math至更多学科与语言变体
- 推动更透明、标准化的俄语LLM评估协议

---

> 💡 **总体评价**：  
> T-pro 2.0 不只是一个高性能俄语模型，更是一个**开放、可复现、可扩展的研究平台**。它系统性地解决了俄语LLM在**数据、模型、评估、推理效率**四个维度的关键挑战，为非英语LLM的发展提供了范式参考。其“小步快跑、精准优化”的思路值得借鉴。

</details>

---

### 11. [DynaMate: An Autonomous Agent for Protein-Ligand Molecular Dynamics Simulations](https://arxiv.org/abs/2512.10034)

**Authors**: Salom\'e Guilbert, Cassandra Masschelein, Jeremy Goumaz, Bohdan Naida, Philippe Schwaller  
**Category**: cs.AI  
**Published**: 2025-12-12  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2512.10034v1  

#### Abstract
Force field-based molecular dynamics (MD) simulations are indispensable for probing the structure, dynamics, and functions of biomolecular systems, including proteins and protein-ligand complexes. Despite their broad utility in drug discovery and protein engineering, the technical complexity of MD s...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：DynaMate: An Autonomous Agent for Protein-Ligand Molecular Dynamics Simulations**

---

## **1. 论文的主要贡献和创新点**

### **解决了什么问题**
分子动力学（**Molecular Dynamics, MD**）模拟在药物发现和蛋白质工程中至关重要，但其流程复杂、技术门槛高。特别是**蛋白-配体系统**的模拟，涉及繁琐的预处理步骤（如结构清洗、质子化、溶剂化、离子中和）、力场参数化、多阶段平衡以及自由能计算等。这些步骤容易出错且高度依赖专家经验，限制了MD的广泛应用。

现有自动化工具（如CHAPERONg、PyAutoFEP）虽然简化了部分流程，但通常**缺乏灵活性**，难以适应不同系统或软件平台，且无法处理运行时错误或进行自适应修正。

### **提出了什么新方法或新思路**
本文提出 **DynaMate**，一个基于**agentic LLM**（自主代理大语言模型）的模块化多智能体框架，能够**自主设计并执行完整的蛋白-配体MD模拟流程**，并支持使用 **MM/PB(GB)SA** 方法进行结合自由能计算。

DynaMate的核心创新在于：
- **模块化多智能体架构**：由三个专用智能体组成——**Planner**（规划）、**MD Agent**（执行）、**Analyzer**（分析），实现任务分工与协同。
- **动态工具调用与检索增强生成（RAG）**：集成 **web search** 和 **PaperQA** 工具，使智能体能从文献和数据库中获取参数选择依据，提升决策的科学性和可追溯性。
- **自纠错能力**：通过迭代推理机制，在模拟失败时分析错误日志，尝试修复输入文件或调整参数，具备一定的“试错”能力。
- **自然语言驱动**：用户只需提供自然语言指令（如“对PDB ID为3HTB的蛋白-配体复合物进行100ns MD模拟”），DynaMate即可自动完成全流程。

### **相比现有方法的优势**
| 功能 | MDCrow | NAMD-Agent | DynaMate |
|------|--------|------------|----------|
| 蛋白-配体模拟 | ❌ | ❌ | ✅ |
| 结合自由能计算（MM/PB(GB)SA） | ❌ | ❌ | ✅ |
| 文献/数据库检索（RAG） | 有限 | ❌ | ✅ |
| 自适应错误纠正 | ❌ | ❌ | ✅ |
| 多平台兼容（GROMACS/AmberTools） | ❌（仅OpenMM） | ❌（仅CHARMM-GUI） | ✅ |

> **来源：Table S1**

DynaMate是首个成功实现**端到端自动化蛋白-配体MD模拟**的agentic系统，显著提升了自动化水平和适用范围。

---

## **2. 核心实验方法和设置**

### **使用的数据集**
实验在**12个不同的蛋白系统**上进行评估，分为三类：
1. **5个经典蛋白-配体复合物**（用于基准测试）：
   - PDB IDs: `3HTB`, `3PTB`, `4GIH`, `4W52`, `5UEZ`
2. **5个无配体蛋白系统**（测试多链蛋白适应性）：
   - PDB IDs: `1AKI`, `1FDH`, `1J37`, `2CBA`, `2VVB`
3. **2个含已知错误的挑战性系统**：
   - `BRD4_UNL`：输入PDB中故意引入错误的氯原子命名
   - `5KB6_ADN`：系统包含两个配体，超出当前工具支持范围

> **来源：Table S2**

### **实验设置和评估指标**
- **LLM模型**：测试了5种主流LLM：
  - `Claude 3 Opus`
  - `Claude 3.7 Sonnet`
  - `GPT-4.1`
  - `GPT-4.1 mini`
  - `Llama 3.3`
- **每组实验重复3次**，以验证鲁棒性。
- **评估指标**：
  1. **准确性（Accuracy）**：关键步骤是否成功生成非空输出文件（如拓扑文件、能量最小化输出等）。
  2. **效率（Efficiency）**：实际调用工具次数 vs. 最小必要调用次数的比率。
  3. **错误恢复能力**：能否识别并修复运行时错误（如原子命名错误、位置约束缺失等）。
  4. **结合自由能预测性能**：使用 `gmx_MMPBSA` 计算 ΔΔG，并与实验IC50值比较相关性（Pearson *r*）。

### **基线方法对比**
本文未直接对比传统脚本化工具（如手动GROMACS流程），而是将不同LLM作为“智能体”在同一框架下运行，间接体现DynaMate框架的通用性和LLM选择的影响。

---

## **3. 主要实验结果和性能指标**

### **关键性能数据**
- **在5个标准蛋白-配体系统上，所有LLM均达到100%准确率**（图2a），表明DynaMate能可靠完成完整MD流程。
- **在挑战性系统上的表现分化明显**：
  - `BRD4_UNL`（氯原子命名错误）：
    - `Claude 3.7 Sonnet` 和 `GPT-4.1 mini` 在3次运行中均成功修复错误并完成模拟。
    - `GPT-4.1` 有一次成功（手动插入正确GAFF参数）。
    - `Llama 3.3` 完全失败。
  - `5KB6_ADN`（双配体系统）：
    - 所有智能体均**未能完成**，因当前工具集不支持多配体参数化。

### **与基线方法的对比结果**
由于DynaMate是首个支持蛋白-配体系统的agentic框架，无直接可比基线。但与MDCrow、NAMD-Agent相比，DynaMate在功能覆盖上具有压倒性优势（见Table S1）。

### **消融实验结果**
- **模型性能差异显著**：
  - `Claude 3 Opus` 和 `GPT-4.1` 在**效率**上最优（工具调用最接近理论最小值）。
  - `Claude 3.7 Sonnet` 在**准确性**上最佳，尤其在错误恢复场景中表现突出，尽管效率略低（因尝试更多修复策略）。
  - `Llama 3.3` 表现最差，常出现工具误用和错误修正失败。
- **错误恢复案例研究**：
  - **位置约束错误（1J37系统）**：所有智能体均识别出问题，但仅`Claude 3.7 Sonnet`尝试手动创建约束文件（虽不完整），其余则移除约束（妥协方案）。
  - **原子命名错误（BRD4_UNL）**：`Claude 3.7 Sonnet` 和 `GPT-4.1 mini` 成功修改PDB文件；`GPT-4.1` 尝试手动插入GAFF参数，也取得成功。

> **来源：Table 1, 2, 3 及 图3**

---

## **4. 关键结论和发现**

### **主要发现**
1. **DynaMate实现了蛋白-配体MD模拟的端到端自动化**，是目前功能最全面的agentic MD框架。
2. **LLM的选择显著影响性能**：`Claude` 和 `GPT-4` 系列在推理、工具调用和错误恢复方面优于开源模型（如`Llama 3.3`）。
3. **自纠错能力有效但有限**：智能体能在多种错误场景下尝试修复，但在复杂或多文件联动错误（如双配体系统）中仍受限。
4. **结合自由能预测合理**：在BRD4 BD1抑制剂测试集中，DynaMate的MM/PBSA ΔΔG 与实验IC50的相关性为 **r = 0.597**，优于GNINA对接分数（r = 0.385），表明其在排序相似化合物方面更具优势。

> **来源：图4**

### **方法的局限性**
1. **依赖外部LLM API**：需用户提供API密钥和信用额度，存在成本和安全顾虑。
2. **缺乏长期记忆**：每次运行独立，无法从历史任务中学习。
3. **工具集限制**：不支持多配体、膜蛋白、DNA/RNA等复杂系统。
4. **输入文件质量敏感**：非常规原子命名、缺失残基等问题仍是失败主因。
5. **力场参数假设风险**：默认使用`ff14sb`和`GAFF2`，某些特殊体系（如活性位点质子化状态）可能需要人工干预。

### **未来工作方向**
- 扩展工具集以支持**多配体、膜蛋白、核酸系统**。
- 引入**持久化记忆机制**，使智能体能从过往运行中积累经验。
- 集成更多**软件文档和科学论文数据库**，提升参数选择的准确性。
- 探索**本地化部署**方案，减少对商业LLM API的依赖。
- 增强对**构象熵、隐式溶剂模型局限性**的理解，提升自由能计算可靠性。

---

> **代码开源地址**：[https://github.com/schwallergroup/DynaMate](https://github.com/schwallergroup/DynaMate)

</details>

---

### 12. [Neuronal Attention Circuit (NAC) for Representation Learning](https://arxiv.org/abs/2512.10282)

**Authors**: Waleed Razzaq, Izis Kankaraway, Yun-Bo Zhao  
**Category**: cs.AI  
**Published**: 2025-12-12  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2512.10282v1  

#### Abstract
Attention improves representation learning over RNNs, but its discrete nature limits continuous-time (CT) modeling. We introduce Neuronal Attention Circuit (NAC), a novel, biologically plausible CT-Attention mechanism that reformulates attention logits computation as the solution to a linear first-o...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Neuronal Attention Circuit (NAC) for Representation Learning

---

## 1. 论文的主要贡献和创新点

### 解决的问题
传统 **Attention 机制** 虽然在序列建模中表现出色，但其本质是离散时间（Discrete-Time）操作，难以有效处理**不规则采样时间序列**（irregularly sampled time series）。同时，现有的连续时间（Continuous-Time, CT）模型如 CT-RNNs 和 CT-Attention 在建模动态演化时存在计算开销大、缺乏生物可解释性等问题。

此外，标准 Attention 的 **scaled dot-product** 操作不具备连续深度（continuous depth）特性，限制了其对连续轨迹的表达能力。

### 提出的新方法与创新思路
作者提出了一种全新的、**生物可解释的连续时间注意力机制**——**Neuronal Attention Circuit (NAC)**，其核心思想包括：

- **将 Attention Logits 视为微分方程的解**：  
  将 attention logits $ a_t $ 定义为一个一阶线性常微分方程（ODE）的输出：
  $$
  \frac{da}{dt} = -w_T \cdot a + \phi
  $$
  其中 $ w_T $ 是可学习的时间常数门控（time-constant gate），$ \phi $ 是内容目标门控（content-target gate），二者均由非线性互连门控网络生成。

- **基于 C. elegans 的神经回路策略（NCPs）构建稀疏骨干网络**：  
  复用秀丽隐杆线虫（*C. elegans*）神经系统中的 **Neuronal Circuit Policies (NCPs)** 结构，设计了一个具有生物学合理性的稀疏递归架构作为 NAC 的 backbone 和 sensory gate，提升模型效率与可解释性。

- **三种 attention logit 计算模式**：
  1. **Euler 模式**：显式欧拉积分近似求解 ODE；
  2. **Exact 模式**：闭式解析解 $ a_t = \frac{\phi}{w_T}(1 - e^{-w_T t}) $；
  3. **Steady 模式**：仅使用稳态解 $ a^* = \phi / w_T $，类比于传统 attention。

- **稀疏 Top-K 成对拼接机制**：  
  引入 **sparse Top-K pairwise concatenation** 策略，在保留关键 query-key 交互的同时显著降低内存消耗。

### 相比现有方法的优势
| 维度 | NAC 的优势 |
|------|-----------|
| **生物可解释性** | 基于真实生物神经回路（NCPs）设计，具备更强的神经科学基础 |
| **连续时间建模能力** | 支持不规则时间序列输入，适应任意时间间隔 |
| **计算效率** | 稀疏连接 + Top-K 机制减少参数量和内存占用 |
| **理论保障** | 提供状态稳定性、误差指数界、通用逼近定理等严格数学证明 |
| **灵活性** | 支持多种计算模式（Euler/Exact/Steady），可在精度与速度间权衡 |

---

## 2. 核心实验方法和设置

### 使用的数据集
实验覆盖多个领域，验证 NAC 的泛化能力：

| 领域 | 数据集 | 特点 |
|------|--------|------|
| **不规则时间序列分类** | Event-based MNIST (E-MNIST) | 图像转事件流，长度从 784 压缩至 ~53 |
| | Person Activity Recognition (PAR) | 来自 UCI 的人体活动识别数据，含惯性传感器信号 |
| **自动驾驶车道保持** | OpenAI CarRacing | 分类任务，预测五种离散动作 |
| | Udacity 自动驾驶模拟器 | 回归任务，预测连续转向角 |
| **工业预测维护（Industry 4.0）** | PRONOSTIA | 轴承退化数据，用于训练 |
| | XJTU-SY | 不同工况下的轴承故障数据，用于跨域测试 |
| | HUST | 多负载条件下的实用轴承数据集，用于交叉验证 |

### 实验设置与评估指标
- **训练方式**：采用 5 折交叉验证（5-fold CV）
- **优化器**：Adam/AdamW
- **损失函数**：
  - 分类任务：Sparse Categorical Crossentropy (SCE)
  - 回归任务：MSE 或 MAE
- **评估指标**：
  - E-MNIST / PAR / CarRacing：Accuracy (↑)
  - Udacity：MSE (↓)
  - Industry 4.0（RUL 估计）：Score metric (↓)，该指标不对称惩罚过估（更危险）

### 基线方法对比
涵盖主流 DT/CT 模型：
- **DT-RNNs**：RNN, LSTM, GRU
- **CT-RNNs**：CT-RNN, GRU-ODE, mmRNN, LTC, CfC
- **DT-Attention**：Attention, MHA
- **CT-Attention**：mTAN, CTA, ODEFormer, ContiFormer

所有基线均与 NAC 使用相同超参配置以保证公平比较。

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table I）

#### 📊 不规则时间序列分类
| 模型 | E-MNIST (%) | PAR (%) |
|------|-------------|---------|
| **NAC-PW** | **96.64** ✅ | **89.15** ✅ |
| GRU-ODE | 96.04 | 89.01 |
| ContiFormer | 96.04 | 81.28 ❌ |
| MHA | 95.94 | 88.36 |

> ✅ NAC-PW 在两个任务上均取得 SOTA 性能。

#### 🚗 自动驾驶车道保持
| 模型 | CarRacing (Acc ↑) | Udacity (MSE ↓) |
|------|-------------------|-----------------|
| **NAC-PW** | **80.72** ✅ | 0.0177 |
| **NAC-32k** | 80.38 | **0.0170** ✅ |
| NAC-Exact | 80.59 | 0.0173 |
| ContiFormer | 80.47 | 0.0174 |

> ✅ NAC 在两类驾驶任务中表现领先。

#### 🔧 工业剩余寿命预测（Score ↓）
| 模型 | PRONOSTIA | XJTU-SY | HUST |
|------|-----------|---------|-------|
| **NAC-Exact/05s/8k** | 37.75 | **19.87** ✅ | **27.82** ✅ |
| **NAC-PW** | 37.50 | 28.01 | 30.14 |
| ContiFormer | 27.82 ✅ | 34.71 | 43.81 |

> ⚠️ 注意：ContiFormer 在 PRONOSTIA 上最优，但 NAC 在跨域数据集 XJTU-SY 和 HUST 上表现更优，显示其**强泛化能力**。

### 消融实验结果（Ablation Studies）

#### Top-K 消融（控制交互数量）
| 模型 | Top-K | 内存 (MB) | E-MNIST (%) |
|------|-------|----------|------------|
| NAC-PW | Full | 5042 | 96.64 ✅ |
| NAC-32k | 32 | 549.86 | 95.15 |
| NAC-2k | 2 | 44.75 | 95.73 |

> ✅ 减少 Top-K 显著降低内存，但会牺牲一定精度；**Top-K=8 是平衡点**。

#### Sparsity 消融（控制网络稀疏度）
| 模型 | Sparsity | E-MNIST (%) |
|------|--------|-------------|
| NAC-02s | 20% | 95.31 |
| NAC-09s | 90% | 95.86 ✅ |
| NAC-PW | 50% | 96.64 ✅ |

> ✅ 更高 sparsity（90%）有助于提升鲁棒性和准确率。

#### 模式消融（Mode Comparison）
| 模式 | 运行速度 | 适用场景 |
|------|--------|----------|
| **Steady** | 最快 | 快速推理 |
| **Exact** | 中等 | 高精度建模 |
| **Euler** | 较慢 | 动态适应性强 |

> ✅ Steady 模式接近传统 attention 效率，而 Euler/Exact 提供更高表达力。

---

## 4. 关键结论和发现

### 主要发现
1. **NAC 实现了 CT 与 Attention 的有效融合**：通过将 attention logits 定义为 ODE 解，实现了真正的连续时间 attention，克服了传统 attention 的离散性限制。
2. **生物启发结构带来高效与稳定**：基于 NCPs 的稀疏架构不仅提升了模型效率，还增强了训练稳定性。
3. **在多领域达到或超越 SOTA**：在不规则时间序列、自动驾驶、工业预测等多个任务中，NAC 表现优于或媲美当前最先进的 CT 和 DT 模型。
4. **内存与运行时处于中间位置**：相比 CT-RNNs，NAC 运行更快；相比 CT-Attention（如 mTAN），内存更低，适合部署在资源受限设备上。

### 方法的局限性
- **依赖预设 wiring 结构**：目前 NAC 使用固定的 NCP wiring，灵活性有限，需手动调整 sensory/motor 单元比例。
- **Top-K 可能遗漏重要上下文**：稀疏选择可能导致关键信息丢失，且对 $ K $ 值敏感。
- **仍需计算完整 QK^T 得分矩阵**：尽管后续处理稀疏化，初始相似度计算仍是 $ O(T^2) $，不利于极长序列。

### 未来工作方向
1. **支持用户自定义或随机化 NCP 配置**：提升架构灵活性，避免硬编码 wiring。
2. **可学习的 Top-K 选择机制**：引入 adaptive 或 learned routing 替代固定 Top-K。
3. **硬件感知优化**：针对边缘设备进行模型压缩与推理加速。
4. **扩展至其他模态**：探索在语音、视频等领域的应用潜力。

---

> 🔗 **代码开源地址**：[https://github.com/itxwaleedrazzaq/neuronal_attention_circuit](https://github.com/itxwaleedrazzaq/neuronal_attention_circuit)  
> 📄 **论文链接**：`arXiv:2512.10282`

</details>

---

### 13. [Computational emotion analysis with multimodal LLMs: Current evidence on an emerging methodological opportunity](https://arxiv.org/abs/2512.10882)

**Authors**: Hauke Licht  
**Category**: cs.CL  
**Published**: 2025-12-12  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2512.10882v1  

#### Abstract
Emotions are central to politics and analyzing their role in political communication has a long tradition. As research increasingly leverages audio-visual materials to analyze the display of emotions, the emergence of multimodal generative AI promises great advances. However, we lack evidence about ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Computational emotion analysis with multimodal LLMs: Current evidence on an emerging methodological opportunity*

---

## 1. 论文的主要贡献和创新点

### 解决的问题
该论文旨在解决**多模态大语言模型（mLLMs）在政治传播中的情感分析能力尚未被系统评估**的问题。尽管文本型 LLMs 已广泛用于社会科学研究中的标注任务，但针对视频中情感强度（如情绪唤醒度 *arousal*）的自动化测量仍缺乏可靠证据。特别是，现有研究未能区分理想实验室环境与真实世界复杂场景下的模型表现差异。

### 提出的新方法与思路
- **提出并验证了一种基于 in-context learning 的多模态情感分析框架**：利用 mLLMs 对视频输入进行指令跟随式的情绪唤醒度评分，无需微调即可实现概念引导的测量。
- **构建了一个可复现的评估框架**：结合两个互补的数据集（一个受控实验室数据集和一个真实议会辩论数据集），系统地评估 mLLMs 在不同条件下的可靠性、偏差及下游推断影响。
- **首次将 mLLMs 应用于视频级情感强度（arousal）而非仅限于情感极性（sentiment）的分析**，突出了非言语线索（语调、面部表情等）的重要性。

### 相比现有方法的优势
- **无需训练数据即可部署**：相比传统依赖监督学习的多模态模型（如融合音频与视觉特征的深度网络），mLLMs 可通过 in-context learning 快速适应新任务，降低对标注数据的需求。
- **理论驱动的测量设计**：可通过自然语言提示（prompt）嵌入社会科学理论定义（如“arousal 是指表达的情感激活程度”），提升测量的透明性和可解释性。
- **支持跨模态统一处理**：mLLMs 能同时处理文本、音频、图像和视频，为整合政治演讲中的“说了什么”与“怎么说”提供了技术基础。

---

## 2. 核心实验方法和设置

### 使用的数据集
| 数据集 | 描述 | 特点 |
|-------|------|------|
| **RAVDESS** (Livingstone & Russo, 2018) | 包含 1248 个由演员表演的短语音频-视频片段，涵盖多种情绪及其强度等级（正常 vs 强烈）。每个视频均有 10 名人类评分者对情绪强度打分（1–5 分）。 | 实验室控制环境，高质量、高信度的人类标签；是“最可能成功”的理想测试场景。 |
| **Cochrane et al. (2022)** | 来自加拿大下议院质询时间（Question Time）的 595 段政治演讲视频，每段由多人类编码员从**文本转录**和**视频记录**两种模式分别评定情感极性（sentiment）和唤醒度（arousal）。 | 真实世界政治语境，存在背景噪音、多人干扰、摄像头角度不正等问题；更具现实挑战性。 |

> 注：两个数据集均划分为 train/validation/test 子集，并按 speaker blocking 划分以确保 out-of-sample 评估。

### 实验设置
- **模型选择**：
  - **Open-weights mLLMs**：`Qwen 2.5 Omni`（3B, 7B）、`TowerVideo`（2B, 9B）
  - **Closed-weights model**：`Gemini 2.5 Flash`（Google）
- **任务形式**：Zero-shot 和 Few-shot In-Context Learning（ICL）
  - 提示模板明确要求模型根据视频判断情绪唤醒度（arousal），使用整数标尺（RAVDESS: 1–5；Cochrane: 1–9）
  - 少样本示例（3 或 5 个）来自训练集，包含视频+人类平均得分作为示范
- **输出处理**：采用 token probability weighting 方法生成连续值评分，避免 LLM 输出的整数聚集偏差。

### 评估指标
| 指标 | 用途 |
|------|------|
| **Pearson’s r / Spearman’s ρ** | 衡量 mLLM 评分与人类平均评分之间的相关性（主指标） |
| **RMSE** | 预测误差大小 |
| **ICC (Intra-class Correlation)** | 评估人类评分内部一致性（用于衰减校正） |
| **Demographic Bias Analysis** | 按性别、年龄分组比较模型性能差异 |
| **Downstream Regression Analysis** | 回归分析政府 vs 反对派议员的 arousal 差异，检验替代人类评分是否改变统计结论 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据

#### （1）在 RAVDESS 数据上的表现（理想环境）
| 模型 | Pearson’s r (3-shot) | RMSE (3-shot) | 备注 |
|------|------------------------|---------------|------|
| `Gemini 2.5 Flash` | **0.690** | 0.784 | 接近人类评分信度（ICC ≈ 0.74），衰减校正后达 0.793 |
| `Qwen 2.5 Omni-7B` | 0.609 | **0.691** | 性能仅次于 Gemini，优于其他开源模型 |
| `Qwen 2.5 Omni-3B` | 0.516 | 0.705 | 随少样本数量增加有所提升 |
| `TowerVideo-9B` | 0.401 | 0.933 | 表现较差，响应分布狭窄，显示过度自信 |

> ✅ **结论**：在理想条件下，顶级 mLLMs（尤其是 Gemini）的情绪强度评分已接近人类水平，且无显著性别偏见。

#### （2）在 Cochrane et al. (2022) 数据上的表现（真实世界）
| 模型 | Pearson’s r (3-shot) | RMSE (3-shot) | 备注 |
|------|------------------------|---------------|------|
| `Gemini 2.5 Flash` | 0.430 | 1.327 | 相关性较弱，远低于 RAVDESS 表现 |
| `Qwen 2.5 Omni-7B` | 0.298 | 1.191 | 相关性极低 |
| `TowerVideo-9B` | 0.201 | **0.984** | RMSE 较低因预测趋近均值，实际排序能力差 |

> ❌ **结论**：所有 mLLMs 在真实政治演讲中表现不佳，相关系数普遍偏低（< 0.5），无法有效捕捉人类感知的情绪强度。

### 与基线方法的对比结果
- **与人类评分对比**：
  - 在 RAVDESS 中，`Gemini` 和 `Qwen-7B` 的评分与人类高度一致；
  - 在 Cochrane 数据中，mLLM 评分与人类评分几乎不相关（r ≈ 0.12–0.43），甚至不如随机猜测。
- **与文本型 LLM 对比（sentiment 分析）**：
  - 在 sentiment 任务上，mLLMs 视频评分虽优于 arousal，但仍显著落后于其对应的 text-based base LLMs（如 Qwen 2.5 Instruct）。
  - 例如，Gemini 的视频 sentiment 评分（r ≈ 0.57–0.60）远低于其文本评分（r ≈ 0.87–0.90），差距约为 2.5 倍于人类评分间的信度落差。

### 消融实验结果
#### （1）信号-噪声比的影响（Signal-to-Noise Ratio）
- **方法**：对 Cochrane 数据中的视频进行预处理：
  - 音频端：应用降噪滤波器抑制背景杂音；
  - 视觉端：使用 vLLM 进行对象检测并遮蔽非说话人区域（background masking）。
- **结果**（见 Table D4）：
  - 处理后的“干净”视频并未显著提升 mLLM 的评分准确性；
  - 多数情况下相关性反而下降或无改善。
- **结论**：背景噪音不是导致性能下降的主要原因。

#### （2）模型容量的影响（Model Capacity）
- **方法**：比较 `Qwen 2.5 Omni`（3B/7B）与更大参数量的音频专用 mLLM `Voxtral`（3B/24B）在**纯音频输入**下的 arousal 评分表现。
- **结果**（见 Table D6）：
  - 更大的模型（24B Voxtral）并未带来明显优势；
  - 所有模型在音频模式下的表现均较差，表明问题不在参数规模。
- **结论**：当前 mLLMs 的架构或训练方式可能不足以有效提取和整合多模态情感信号。

---

## 4. 关键结论和发现

### 主要发现
1. **mLLMs 在理想环境下表现良好，但在真实世界中失效**：
   - 在 RAVDESS 这类受控环境中，`Gemini 2.5 Flash` 和 `Qwen 2.5 Omni-7B` 的情绪强度评分接近人类水平，具备一定可靠性。
   - 但在真实的议会辩论视频中，所有测试的 mLLMs 都未能可靠还原人类的情绪唤醒度判断，相关性仅为中等至极弱。

2. **存在系统性 demographic bias**：
   - 在 Cochrane 数据中，多个 mLLMs 对女性发言者的 arousal 评分一致性更低；
   - `Gemini` 和 `TowerVideo-9B` 对年轻发言人（24–45岁）的表现也更差。

3. **下游推断可能产生误导**：
   - 当用 mLLM 评分代替人类评分进行回归分析时（如比较政府 vs 反对派的 arousal），部分模型得出相反方向的显著效应（如 TowerVideo-2B 显示反对派更激动，而人类评分为无差异）。
   - 即使总评分分布相似，细微偏差也可能扭曲因果推论。

4. **多模态优势未体现，文本模型仍更强**：
   - 尽管人类研究表明视频比文本更能反映 arousal，但当前 mLLMs 的视频分析能力远逊于其文本分析能力。
   - 在 sentiment 任务上，mLLMs 的视频评分也明显劣于其 base LLM 的文本评分。

### 方法的局限性
- **模型黑箱性**：尤其是闭源模型（如 Gemini），缺乏对其如何处理视频帧与音频流的技术细节披露。
- **提示工程未充分探索**：仅使用单一 prompt 模板，未系统优化提示结构或引入 chain-of-thought 等高级策略。
- **评估范围有限**：仅聚焦 arousal 和 sentiment，未涉及离散情绪分类或多维情感轨迹建模。
- **数据多样性不足**：两个数据集均集中于英语政治演讲，缺乏跨文化、跨平台（如 TikTok）的内容。

### 未来工作方向
1. **深入探究失败机制**：
   - 分析特定声学属性（重叠语音、背景音量）或视觉因素（摄像角度、身体姿态）如何影响模型判断。
   - 开展 controlled perturbation 实验，识别关键干扰变量。

2. **拓展任务类型**：
   - 测试 mLLMs 在检测 discrete emotions（愤怒、喜悦等）或追踪情绪动态变化方面的能力。
   - 探索其在 short-form political content（如 TikTok、Instagram Reels）中的适用性。

3. **改进模型架构与训练**：
   - 设计更适合政治语境的多模态预训练目标；
   - 引入 attention masking 机制以增强对主讲人的聚焦能力。

4. **建立标准化 benchmark**：
   - 推动开发面向社会科学的多模态情感分析基准数据集与评估协议，促进可比性研究。

---

> 📌 **总体评价**：本文是一篇具有警示意义的重要研究。它揭示了当前 mLLMs 在真实世界多模态情感分析中的严重局限性，提醒研究者不可盲目信任“通用智能”模型的输出。尽管前景广阔，但要将其作为可靠的 drop-in replacement for human coders，仍需更多严谨验证与方法革新。

</details>

---

### 14. [HGC-Herd: Efficient Heterogeneous Graph Condensation via Representative Node Herding](https://arxiv.org/abs/2512.09947)

**Authors**: Fuyan Ou, Siqi Ai, Yulin Hu  
**Category**: cs.LG  
**Published**: 2025-12-12  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2512.09947v1  

#### Abstract
Heterogeneous graph neural networks (HGNNs) have demonstrated strong capability in modeling complex semantics across multi-type nodes and relations. However, their scalability to large-scale graphs remains challenging due to structural redundancy and high-dimensional node features. Existing graph co...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：HGC-Herd: Efficient Heterogeneous Graph Condensation via Representative Node Herding

---

## 1. 论文的主要贡献和创新点

### 解决的问题
现有的图神经网络（HGNNs）在处理大规模异构图（Heterogeneous Graph）时面临计算开销大、内存占用高以及训练效率低等问题。传统的图压缩方法如 GCond 主要针对同构图设计，依赖梯度匹配（gradient matching），导致优化复杂、计算成本高，且难以扩展到多类型节点和关系的异构场景。

此外，采样类方法（如 GraphSAINT）虽然降低了计算负担，但容易引入信息丢失和结构偏差，尤其在保留元路径（metapath）语义和跨类型连接方面表现不佳。

### 提出的新方法与思路
本文提出 **HGC-Herd** —— 一种无需训练的异构图压缩框架（training-free heterogeneous graph condensation），其核心思想是通过“代表性节点聚集”（Representative Node Herding）生成紧凑而信息丰富的子图。

该方法包含三个关键模块：
1. **Feature Propagation**：基于预定义 metapath 进行轻量级特征传播，融合多跳邻域语义信息；
2. **Class-wise Prototype Construction**：为每个类别构建类原型（prototype），作为语义中心；
3. **Strategic Herding Selection**：采用贪心策略选择最接近类原型的节点，形成每类的“herd”，确保语义多样性和类别平衡。

### 相比现有方法的优势
- ✅ **无需训练**：避免了梯度优化过程，显著降低计算和内存开销；
- ✅ **高效可复现**：选择过程确定性强，不受随机种子影响，支持跨任务复用；
- ✅ **保持语义与结构保真度**：通过类感知（class-wise）选择机制维持类别分布和跨类型连接；
- ✅ **灵活可控**：支持按类别设定压缩预算（budget），适应类别不平衡场景；
- ✅ **即插即用**：生成的压缩图可直接用于标准 HGNN 模型（如 Simple-HGN）训练，无需修改模型架构。

---

## 2. 核心实验方法和设置

### 使用的数据集
实验在三个标准异构图基准上进行：
| 数据集 | 节点数 | 边数 | 节点类型数 | 边类型数 | 目标节点类型 | 分类类别数 |
|--------|--------|------|-------------|-------------|----------------|--------------|
| **ACM** | 10,942 | 547,872 | 4 | 8 | paper | 3 |
| **DBLP** | 26,128 | 239,566 | 4 | 6 | author | 4 |
| **Freebase** | ~180k | ~1M | 8 | 36 | book | 7 |

所有数据集均使用原始特征，未引入外部预训练信息。

### 实验设置与评估指标
- **任务**：目标节点类型的 Node Classification
- **评估指标**：分类准确率（Accuracy %）
- **压缩比例（condensation ratio）**：`{1.2%, 2.4%, 4.8%, 9.6%}`
- **模型架构**：Simple-HGN（2层，64隐藏单元，Adam优化器，lr=5e-4）
- **硬件平台**：NVIDIA RTX 3050 GPU
- **重复次数**：5次运行取平均值，固定随机种子以保证可复现性
- **压缩图构造方式**：每个比例下仅构建一次，供多次训练复用

### 基线方法对比
| 方法 | 类型 | 特点 |
|------|------|------|
| **Random-HG** | 随机采样 | 均匀随机选取目标节点 |
| **K-Center-HG** | 贪心覆盖 | 基于K中心聚类选择最具代表性的节点 |
| **Coarsening-HG** | 结构粗化 | 使用 METIS 风格的图粗化方法 |
| **GCond** | 梯度匹配法 | 同构图压缩代表，需双层优化 |
| **Whole Dataset** | 上界参考 | 全图训练结果，作为性能上限 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table I）

#### 在 ACM 上的表现（Full: 93.11%）
| Ratio | HGC-Herd | GCond | K-Center-HG | Coarsening-HG | Random-HG |
|-------|----------|--------|--------------|----------------|------------|
| 1.2% | **91.88%** | 41.17% | 62.66% | 64.17% | 53.37% |
| 9.6% | **93.01%** | 65.56% | 75.68% | 70.91% | 66.25% |

> 🔥 即使只保留 **1.2% 的数据**，HGC-Herd 准确率已达全图的 **98.7%**

#### 在 DBLP 上的表现（Full: 95.19%）
| Ratio | HGC-Herd | GCond | K-Center-HG | Coarsening-HG | Random-HG |
|-------|----------|--------|--------------|----------------|------------|
| 1.2% | **89.86%** | 53.26% | 61.39% | 53.27% | 38.73% |
| 9.6% | **94.79%** | 64.25% | 79.68% | 76.91% | 56.01% |

> 💡 表现出极强的信息保留能力，接近全图性能

#### 在 Freebase 上的表现（Full: 63.67%）
| Ratio | HGC-Herd | GCond | K-Center-HG | Coarsening-HG | Random-HG |
|-------|----------|--------|--------------|----------------|------------|
| 1.2% | **57.18%** | 51.24% | 48.18% | 46.28% | 45.32% |
| 9.6% | **61.42%** | 57.03% | 52.68% | 52.91% | 50.01% |

> ⚠️ 尽管 Freebase 更复杂，HGC-Herd 仍明显优于其他方法，尤其在低压缩比下优势显著

### 与基线方法的对比结果
- 在所有数据集和压缩比例下，**HGC-Herd 显著优于所有基线方法**；
- 相比依赖梯度的 GCond，HGC-Herd 不仅精度更高，而且稳定性更强（方差更小）；
- 在 1.2% 极端压缩下，HGC-Herd 性能远超第二名（通常高出 20–30 个百分点）；
- K-Center-HG 和 Coarsening-HG 表现尚可，但仍无法有效保持类别平衡和语义一致性。

### 消融实验结果（Table II，1.2% ratio）

| 变体 | ACM | Freebase |
|------|-----|----------|
| **HGC-Herd (完整)** | **91.88%** | **57.18%** |
| w/o Feature Propagation | 89.45% | 55.12% |
| w/o Herding | 81.23% | 48.67% |

> 📌 结论：
- 移除 **Feature Propagation** 导致性能下降，说明多跳语义聚合对提升代表性至关重要；
- 移除 **Herding Selection** 导致最大性能损失，验证了类感知选择机制的关键作用；
- 两者协同工作才能实现最优压缩效果。

---

## 4. 关键结论和发现

### 主要发现
- **HGC-Herd 能在极高压缩比（低至 1.2%）下逼近全图训练性能**，证明其强大的语义保留能力；
- **无需梯度优化即可实现高质量图压缩**，打破了以往“必须通过梯度匹配”的范式；
- **类原型引导 + herding 机制** 是实现语义多样性与类别平衡的关键；
- **轻量级特征传播 + 确定性选择策略** 使得整个流程高效、稳定、可复现；
- 实验表明，HGC-Herd 在不同规模和复杂度的异构图上均具有良好的泛化能力和鲁棒性。

### 方法的局限性
- 当前方法依赖人工指定 metapath，可能限制其在缺乏领域知识场景下的应用；
- 对于动态图或流式图数据，尚未提供增量更新机制；
- 压缩后的图虽小，但仍是真实节点的子集（非合成节点），在极端压缩下可能仍存在冗余；
- 多类型边的全局结构保真度仍有进一步提升空间。

### 未来工作方向
- 扩展至 **动态图（Dynamic Graphs）** 场景，支持时间演化下的持续压缩；
- 探索 **自适应 per-class budget allocation**，根据类别难度或稀疏性自动调整压缩比例；
- 研究与 **联邦学习（Federated Learning）** 和 **隐私保护** 设置的结合，在不共享原始数据的前提下进行分布式图压缩；
- 引入 **元学习或自监督机制** 自动发现最优 metapath 或传播策略。

---

> ✅ **总体评价**：  
HGC-Herd 提出了一种简洁、高效、可复现的异构图压缩新范式，解决了传统方法在计算效率、优化难度和语义保真之间的权衡难题，在多个真实世界数据集上展现出卓越性能，具有很强的实际部署价值。

</details>

---

### 15. [Multi-Objective Reward and Preference Optimization: Theory and Algorithms](https://arxiv.org/abs/2512.10601)

**Authors**: Akhil Agnihotri  
**Category**: cs.LG  
**Published**: 2025-12-12  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2512.10601v1  

#### Abstract
This thesis develops theoretical frameworks and algorithms that advance constrained reinforcement learning (RL) across control, preference learning, and alignment of large language models. The first contribution addresses constrained Markov Decision Processes (CMDPs) under the average-cost criterion...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Multi-Objective Reward and Preference Optimization: Theory and Algorithms*

---

## 1. 论文的主要贡献和创新点

### 解决的问题
该博士论文系统地研究了**强化学习（RL）中的多目标优化与偏好对齐问题**，特别是在以下三个关键挑战场景中：
- **安全约束下的策略优化**：在平均奖励准则下，如何有效处理长期安全约束（如机器人控制、LLM 安全性）。
- **离线偏好与在线奖励的融合**：如何利用来自不同可靠度评分者的离线偏好数据，并结合在线数值奖励进行高效学习。
- **大语言模型（LLM）的多目标对齐**：如何在不依赖单一标量化奖励的前提下，平衡多个冲突的目标（如 helpfulness、harmlessness、truthfulness）。

这些问题在现实应用中普遍存在，但传统方法（如 PPO、DPO）往往通过简单加权或忽略约束来处理，导致性能下降、reward hacking 或无法逼近 Pareto 前沿。

---

### 提出的新方法与创新思路

论文提出了五个核心算法，覆盖从理论到实践的完整链条：

| 方法 | 所属章节 | 创新点 |
|------|----------|--------|
| **ACPO** (Average-Constrained Policy Optimization) | Chapter 1 | 首个适用于 **average-reward CMDP** 的实用信任区域策略优化算法。引入基于敏感性分析和子水平集（sublevel sets）的投影机制，在保证约束满足的同时提供强理论保障。 |
| **e-COP** (episodic Constrained Optimization Problem) | Chapter 2 | 将约束 RL 扩展至 **episodic MDPs**，提出一种新颖的近似线搜索程序，提升策略更新效率，并首次为 episodic 设置设计了有效的约束优化框架。 |
| **warmPref-PS** | Chapter 3 | 一种贝叶斯后验采样（Posterior Sampling）算法，用于线性 bandits 场景。**建模评分者能力（rater competence）**，动态调整离线偏好的可信度，避免低质量数据误导学习过程。 |
| **PSPL** (Posterior Sampling for Preference-based Learning) | Chapter 4 | 在 preference-based RL 中实现 **best-policy identification**，首次给出 **Bayesian simple regret bounds**。支持轨迹级反馈下的信用分配，并提出 bootstrap 版本以适应高维函数逼近。 |
| **MOPO** (Multi-Objective Preference Optimization) | Chapter 5 | 面向 LLM 对齐的可扩展多目标优化框架。将主目标最大化，同时将次级目标作为**可调阈值的约束项**，直接作用于 pairwise preferences，避免 reward hacking，逼近 Pareto front。 |

---

### 相比现有方法的优势

| 方法 | 相比优势 |
|------|--------|
| **ACPO vs CPO / PPO** | 在 average-reward 设置下表现更优；理论上有更强的性能退化界；能处理长期平均约束，更适合安全关键任务。 |
| **warmPref-PS vs vanilla PS / DPO** | 显式建模 rater competence，减少噪声影响；支持 offline + online 混合学习，样本效率更高。 |
| **PSPL vs optimism-based baselines** | 更易于实现且计算高效；首次提供 simple regret 理论保证；适用于非表格型环境的近似版本（Bootstrapped PSPL）。 |
| **MOPO vs DPO / MODPO / RiC** | 不依赖 reward scalarization，能探索整个 Pareto front；无需推理时条件输入；闭式更新使其可扩展至数十亿参数模型；优化更稳定。 |

---

## 2. 核心实验方法和设置

### 使用的数据集
- **标准 RL 环境**：OpenAI Gym (MuJoCo) 连续控制任务（如 HalfCheetah, Walker2d），用于评估 ACPO 和 e-COP。
- **合成偏好数据集**：构造具有已知 Pareto front 的二维/三维 reward 函数（如 $r_1(x,y)=(x+y)^2$, $r_2(x,y)=\log(xy)$），用于验证 MOPO 是否能恢复最优策略。
- **真实世界 LLM 数据集**：
  - **Helpful Assistant Task**：人类标注的助手回复偏好数据。
  - **Reddit Summary Task**：社区摘要生成任务的人类偏好数据。
  - 开源模型测试集：Zephyr-7b-beta, Mistral-7b-v0.2, Llama2-7b-chat 等。

### 实验设置与评估指标

| 方法 | 设置 | 主要评估指标 |
|------|------|-------------|
| **ACPO / e-COP** | 使用 5 次独立运行取均值；discount factor 设为接近 1 的值（模拟 average setting）；采用 Lagrangian 方法作为 baseline。 | 平均 reward、constraint violation（vs threshold）、收敛速度 |
| **warmPref-PS** | 线性 bandit 设置，A=10 arms；离线数据大小 N 变化；引入不同 competence levels 的 rater。 | Bayesian regret $\mathcal{O}(\sqrt{T} + \alpha_{\text{rater}})$ |
| **PSPL** | Gridworld 与随机 MDP；trajectory-level feedback；比较不同 offline 数据量的影响。 | Simple regret、policy identification accuracy |
| **MOPO** | 多亿参数 LLM（如 Llama-3.1-8B）fine-tuning；batch size=128；使用 trl 库实现；lagged reference policy every $t_0$ steps。 | Normalized reward per objective、Pareto dominance rate、overfitting on held-out comparisons |

### 基线方法对比
- **ACPO**: vs CPO, PCPO, PPO, BVF-PPO, ATRPO
- **warmPref-PS**: vs vanilla PS, LinTS, Hybrid-DPO
- **PSPL**: vs optimism-based methods（虽不可行，但仍作概念对比）
- **MOPO**: vs DPO, MODPO, RiC, PARM, SFT（监督微调）

---

## 3. 主要实验结果和性能指标

### 关键性能数据
- **ACPO**：在 MuJoCo 环境中，相比 CPO 和 PPO，**平均 reward 提升 12–18%**，同时将 constraint violation 控制在阈值以内（<5% 超限），而其他方法常出现严重违规。
- **warmPref-PS**：当 rater competence 较低时，其 regret 比 vanilla PS 降低 **超过 40%**；理论界的 $\alpha_{\text{rater}}$ 项被实验证实有效。
- **PSPL**：在 small MDP 上，simple regret 收敛速度快于随机策略约 **3倍**；bootstrap 版本在高维空间仍保持鲁棒性。
- **MOPO**：
  - 在 synthetic benchmarks 上，**完全恢复了真实 Pareto front**（见 Fig 5.9）；
  - 在 LLM 实验中，相比 DPO，**held-out comparison 准确率提高 6.2%**，表明无过拟合；
  - 在 Helpfulness-Harmlessness 权衡中，**Pareto-dominates 所有 baseline**（Fig 5.5）；
  - 训练时间仅需 **4.1 小时**（vs PPO 5.1h, MODPO 5.7h），具备良好可扩展性。

### 与基线方法的对比结果
- **MOPO 显著优于所有 scalarization-based 方法**（如 DPO, MODPO），后者只能学到特定权重下的解，无法泛化。
- **ACPO 在长期安全约束任务中显著优于 Lagrangian 方法**（如 PPO-Lag），后者常因 dual variable 更新不稳定而导致 constraint violation。
- **warmPref-PS 在低质量离线数据下表现远超 Hybrid-DPO**，证明建模 rater competence 的必要性。

### 消融实验结果
- **MOPO 消融实验**（Fig 5.8）：
  - **Lagged reference policy ($t_0 > 0$)**：设置 $t_0=100$ 比 $t_0=0$（无延迟）reward 提升 9%，说明目标策略滞后有助于稳定训练。
  - **Constraint relaxation ($\beta$)**：$\beta=0.95$ 最佳，过大（0.999）导致约束太松，过小（0.9）限制探索。
  - **KL 正则强度 ($\tau$)**：$\tau=0.5$ 平衡 exploration-exploitation，过大导致偏离参考策略不足。
- **ACPO 恢复步骤**：当约束违反时，引入 recovery step 可使策略快速回到可行域，避免训练崩溃。

---

## 4. 关键结论和发现

### 主要发现
1. **平均奖励下的约束 RL 是可行且必要的**：ACPO 成功解决了 average-CMDP 的优化难题，为长期安全控制提供了新工具。
2. **离线偏好必须考虑评分者可靠性**：盲目信任所有偏好会导致性能下降；warmPref-PS 通过建模 $\lambda$ 和 $\beta$ 实现选择性学习。
3. **多目标对齐不应依赖 reward scalarization**：MOPO 表明，将次要目标设为约束而非加权项，能更好地逼近 Pareto front，避免 reward hacking。
4. **后验采样是连接 offline 与 online 学习的有效范式**：PSPL 和 warmPref-PS 展示了 PS 在 preference learning 中的潜力，尤其适合最终策略选择任务（simple regret）。
5. **统一框架的可能性**：ACPO、e-COP、warmPref-PS、PSPL、MOPO 构成了一个从显式约束到隐式偏好的统一 constrained RL 框架。

---

### 方法的局限性
- **ACPO / e-COP**：依赖于策略梯度估计，可能受方差影响；未在真实机器人上部署验证。
- **warmPref-PS / PSPL**：当前理论分析集中在 tabular 或线性 setting，尚未推广到 full deep RL。
- **MOPO**：假设 preference dataset 覆盖足够状态空间；adaptive constraint scheduling 引入额外超参 $\beta$。
- 所有方法均假设偏好一致性较强，未充分处理矛盾或模糊偏好。

---

### 未来工作方向
1. **扩展至 continuous state/action spaces**：将 posterior sampling 类算法（如 warmPref-PS, PSPL）推广到 deep function approximation setting。
2. **动态或多层级约束建模**：研究时间变化的安全阈值或层次化目标结构。
3. **在线自适应 rater competence learning**：让模型自动识别并校准不同用户的评分偏差。
4. **结合 MOPO 与 reasoning agents**：在复杂决策链中实现多目标偏好导航。
5. **开放更多代码与基准**：推动 community 对 multi-objective alignment 的标准化评估（如 HarmBench 已初步尝试）。

--- 

> ✅ **总结一句话**：本文构建了一个从理论到应用的完整“约束强化学习”体系，提出了一系列创新算法（尤其是 MOPO 和 ACPO），在安全控制与 LLM 对齐等关键领域实现了性能突破，强调了**避免 reward hacking、尊重 Pareto 权衡、建模人类偏好异质性**的重要性，为下一代 AI 对齐技术奠定了基础。

</details>

---

### 16. [EpiPlanAgent: Agentic Automated Epidemic Response Planning](https://arxiv.org/abs/2512.10313)

**Authors**: Kangkun Mao, Fang Xu, Jinru Ding, Yidong Jiang, Yujun Yao, Yirong Chen, Junming Liu, Xiaoqin Wu, Qian Wu, Xiaoyan Huang, Jie Xu  
**Category**: cs.AI  
**Published**: 2025-12-12  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.10313v1  

#### Abstract
Epidemic response planning is essential yet traditionally reliant on labor-intensive manual methods. This study aimed to design and evaluate EpiPlanAgent, an agent-based system using large language models (LLMs) to automate the generation and validation of digital emergency response plans. The multi...

---

### 17. [User-Feedback-Driven Continual Adaptation for Vision-and-Language Navigation](https://arxiv.org/abs/2512.10322)

**Authors**: Yongqiang Yu, Xuhui Li, Hazza Mahmood, Jinxing Zhou, Haodong Hong, Longtao Jiang, Zhiqiang Xu, Qi Wu, Xiaojun Chang  
**Category**: cs.AI  
**Published**: 2025-12-12  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.10322v1  

#### Abstract
Vision-and-Language Navigation (VLN) requires agents to navigate complex environments by following natural-language instructions. General Scene Adaptation for VLN (GSA-VLN) shifts the focus from zero-shot generalization to continual, environment-specific adaptation, narrowing the gap between static ...

---

### 18. [Remember Me, Refine Me: A Dynamic Procedural Memory Framework for Experience-Driven Agent Evolution](https://arxiv.org/abs/2512.10696)

**Authors**: Zouying Cao, Jiaji Deng, Li Yu, Weikang Zhou, Zhaoyang Liu, Bolin Ding, Hai Zhao  
**Category**: cs.AI  
**Published**: 2025-12-12  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.10696v1  

#### Abstract
Procedural memory enables large language model (LLM) agents to internalize "how-to" knowledge, theoretically reducing redundant trial-and-error. However, existing frameworks predominantly suffer from a "passive accumulation" paradigm, treating memory as a static append-only archive. To bridge the ga...

---

### 19. [Multi-Granular Node Pruning for Circuit Discovery](https://arxiv.org/abs/2512.10903)

**Authors**: Muhammad Umair Haider, Hammad Rizwan, Hassan Sajjad, A. B. Siddique  
**Category**: cs.AI  
**Published**: 2025-12-12  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.10903v1  

#### Abstract
Circuit discovery aims to identify minimal subnetworks that are responsible for specific behaviors in large language models (LLMs). Existing approaches primarily rely on iterative edge pruning, which is computationally expensive and limited to coarse-grained units such as attention heads or MLP bloc...

---

### 20. [AgriGPT-Omni: A Unified Speech-Vision-Text Framework for Multilingual Agricultural Intelligence](https://arxiv.org/abs/2512.10624)

**Authors**: Bo Yang, Lanfei Feng, Yunkui Chen, Yu Zhang, Jianyu Zhang, Xiao Xu, Nueraili Aierken, Shijian Li  
**Category**: cs.CL  
**Published**: 2025-12-12  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.10624v1  

#### Abstract
Despite rapid advances in multimodal large language models, agricultural applications remain constrained by the lack of multilingual speech data, unified multimodal architectures, and comprehensive evaluation benchmarks. To address these challenges, we present AgriGPT-Omni, an agricultural omni-fram...

---

### 21. [CloudFix: Automated Policy Repair for Cloud Access Control Policies Using Large Language Models](https://arxiv.org/abs/2512.09957)

**Authors**: Bethel Hall, Owen Ungaro, William Eiers  
**Category**: cs.DC  
**Published**: 2025-12-12  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.09957v1  

#### Abstract
Access control policies are vital for securing modern cloud computing, where organizations must manage access to sensitive data across thousands of users in distributed system settings. Cloud administrators typically write and update policies manually, which can be an error-prone and time-consuming ...

---

### 22. [Uncertainty-Preserving QBNNs: Multi-Level Quantization of SVI-Based Bayesian Neural Networks for Image Classification](https://arxiv.org/abs/2512.10602)

**Authors**: Hendrik Borras, Yong Wu, Bernhard Klein, Holger Fr\"oning  
**Category**: cs.LG  
**Published**: 2025-12-12  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.10602v1  

#### Abstract
Bayesian Neural Networks (BNNs) provide principled uncertainty quantification but suffer from substantial computational and memory overhead compared to deterministic networks. While quantization techniques have successfully reduced resource requirements in standard deep learning models, their applic...

---

### 23. [Exploring LLMs for Scientific Information Extraction Using The SciEx Framework](https://arxiv.org/abs/2512.10004)

**Authors**: Sha Li, Ayush Sadekar, Nathan Self, Yiqi Su, Lars Andersland, Mira Chaplin, Annabel Zhang, Hyoju Yang, James B Henderson, Krista Wigginton, Linsey Marr, T. M. Murali, Naren Ramakrishnan  
**Category**: cs.AI  
**Published**: 2025-12-12  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2512.10004v1  

#### Abstract
Large language models (LLMs) are increasingly touted as powerful tools for automating scientific information extraction. However, existing methods and tools often struggle with the realities of scientific literature: long-context documents, multi-modal content, and reconciling varied and inconsisten...

---

### 24. [Investigating The Functional Roles of Attention Heads in Vision Language Models: Evidence for Reasoning Modules](https://arxiv.org/abs/2512.10300)

**Authors**: Yanbei Jiang, Xueqi Ma, Shu Liu, Sarah Monazam Erfani, Tongliang Liu, James Bailey, Jey Han Lau, Krista A. Ehinger  
**Category**: cs.AI  
**Published**: 2025-12-12  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2512.10300v1  

#### Abstract
Despite excelling on multimodal benchmarks, vision-language models (VLMs) largely remain a black box. In this paper, we propose a novel interpretability framework to systematically analyze the internal mechanisms of VLMs, focusing on the functional roles of attention heads in multimodal reasoning. T...

---

### 25. [Murmur2Vec: A Hashing Based Solution For Embedding Generation Of COVID-19 Spike Sequences](https://arxiv.org/abs/2512.10147)

**Authors**: Sarwan Ali, Taslim Murad  
**Category**: cs.LG  
**Published**: 2025-12-12  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2512.10147v1  

#### Abstract
Early detection and characterization of coronavirus disease (COVID-19), caused by SARS-CoV-2, remain critical for effective clinical response and public-health planning. The global availability of large-scale viral sequence data presents significant opportunities for computational analysis; however,...

---

### 26. [Interpretable and Steerable Concept Bottleneck Sparse Autoencoders](https://arxiv.org/abs/2512.10805)

**Authors**: Akshay Kulkarni, Tsui-Wei Weng, Vivek Narayanaswamy, Shusen Liu, Wesam A. Sakla, Kowshik Thopalli  
**Category**: cs.LG  
**Published**: 2025-12-12  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2512.10805v1  

#### Abstract
Sparse autoencoders (SAEs) promise a unified approach for mechanistic interpretability, concept discovery, and model steering in LLMs and LVLMs. However, realizing this potential requires that the learned features be both interpretable and steerable. To that end, we introduce two new computationally...

---

### 27. [CAPTAIN: Semantic Feature Injection for Memorization Mitigation in Text-to-Image Diffusion Models](https://arxiv.org/abs/2512.10655)

**Authors**: Tong Zhang, Carlos Hinojosa, Bernard Ghanem  
**Category**: cs.AI  
**Published**: 2025-12-12  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2512.10655v1  

#### Abstract
Diffusion models can unintentionally reproduce training examples, raising privacy and copyright concerns as these systems are increasingly deployed at scale. Existing inference-time mitigation methods typically manipulate classifier-free guidance (CFG) or perturb prompt embeddings; however, they oft...

---

### 28. [ESS: An Offload-Centric Latent-Cache Management Architecture for DeepSeek-V3.2-Exp](https://arxiv.org/abs/2512.10576)

**Authors**: Xinhang Chen, Chao Zhang, Jiahuan He, Wei Liu, Jianming Zhang, Wenlong Zhou, Xiao Li, Pai Zeng, Shiyong Li, Yuanpan Qian, Dong Li, Zhaogeng Li  
**Category**: cs.DC  
**Published**: 2025-12-12  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2512.10576v1  

#### Abstract
DeepSeek-V3.2-Exp introduces a sparse attention mechanism that significantly reduces inference latency in long-context scenarios. Although the overall throughput has improved greatly, the Decode-stage of PD disaggregation remains to be a major bottleneck. This bottleneck primarily stems from the con...

---

### 29. [\textsc{Text2Graph}: Combining Lightweight LLMs and GNNs for Efficient Text Classification in Label-Scarce Scenarios](https://arxiv.org/abs/2512.10061)

**Authors**: Jo\~ao Lucas Luz Lima Sarcinelli, Ricardo Marcondes Marcacini  
**Category**: cs.LG  
**Published**: 2025-12-12  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2512.10061v1  

#### Abstract
Large Language Models (LLMs) have become effective zero-shot classifiers, but their high computational requirements and environmental costs limit their practicality for large-scale annotation in high-performance computing (HPC) environments. To support more sustainable workflows, we present \textsc{...

---

### 30. [Unlocking the Address Book: Dissecting the Sparse Semantic Structure of LLM Key-Value Caches via Sparse Autoencoders](https://arxiv.org/abs/2512.10547)

**Authors**: Qingsen Ma, Dianyun Wang, Jiaming Lyu, Yaoye Wang, Lechen Ning, Sujie Zhu, Zhenbo Xu, Liuyu Xiang, Huining Li, Huijia Wu, Zhaofeng He  
**Category**: cs.LG  
**Published**: 2025-12-12  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2512.10547v1  

#### Abstract
The Key-Value (KV) cache is the primary memory bottleneck in long-context Large Language Models, yet it is typically treated as an opaque numerical tensor. In this work, we propose \textbf{STA-Attention}, a framework that utilizes Top-K Sparse Autoencoders (SAEs) to decompose the KV cache into inter...

---

## 🔧 Configuration

This bot is configured to look for papers containing the following keywords:
- framework, System, Generation, Video, Linear, LLM, RL, RLHF, Inference, Training, Attention, Pipeline, MOE, Sparse, Quantization, Speculative, Efficient, Efficiency, Framework, Parallel, Distributed, Kernel, Decode, Decoding, Prefill, Throughput, Fast, Network, Hardware, Cluster, FP8, FP4, Optimization, Scalable, Communication

## 📅 Schedule

The bot runs daily at 12:00 UTC via GitHub Actions to fetch the latest papers.

## 🚀 How to Use

1. **Fork this repository** to your GitHub account
2. **Customize the configuration** by editing `config.json`:
   - Add/remove arXiv categories (e.g., `cs.AI`, `cs.LG`, `cs.CL`)
   - Modify keywords to match your research interests
   - Adjust `max_papers` and `days_back` settings
3. **Enable GitHub Actions** in your repository settings
4. **The bot will automatically run daily** and update the README.md

## 📝 Customization

### arXiv Categories
Common categories include:
- `cs.AI` - Artificial Intelligence
- `cs.LG` - Machine Learning
- `cs.CL` - Computation and Language
- `cs.CV` - Computer Vision
- `cs.NE` - Neural and Evolutionary Computing
- `stat.ML` - Machine Learning (Statistics)

### Keywords
Add keywords that match your research interests. The bot will search for these terms in paper titles and abstracts.

### Exclude Keywords
Add terms to exclude certain types of papers (e.g., "survey", "review", "tutorial").

## 🔍 Manual Trigger

You can manually trigger the bot by:
1. Going to the "Actions" tab in your repository
2. Selecting "arXiv Bot Daily Update"
3. Clicking "Run workflow"

---
*Generated automatically by arXiv Bot* 
