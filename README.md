# arXiv Papers Bot 🤖

This repository automatically fetches and displays relevant papers from arXiv based on configured criteria.

## RSS Vercel Deployment [![An example of deployed RSS Server using vercel](https://img.shields.io/badge/Deployed-Example-blue)](https://arxiv.tachicoma.top/)

You can click this to deploy yours 

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/maydomine/arxiv_rss_bot)
## 📊 Statistics

- **Last Updated**: 2025-12-09 05:54:21 UTC
- **Total Papers Found**: 30
- **Categories Monitored**: cs.AI, cs.CL, cs.DC, cs.LG

## 📚 Recent Papers

### 1. [Stable-MoE: Lyapunov-based Token Routing for Distributed Mixture-of-Experts Training over Edge Networks](https://arxiv.org/abs/2512.06784)

**Authors**: Long Shi, Bingyan Ou, Kang Wei, Weihao Zhu, Zhe Wang, Zhiyong Chen  
**Category**: cs.DC  
**Published**: 2025-12-09  
**Score**: 13.0  
**Type**: new  
**ArXiv ID**: 2512.06784v1  

#### Abstract
The sparse activation mechanism of mixture of experts (MoE) model empowers edge intelligence with enhanced training efficiency and reduced computational resource consumption. However, traditional token routing in distributed MoE training faces significant challenges in resource-constrained edge netw...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：Stable-MoE: Lyapunov-based Token Routing for Distributed Mixture-of-Experts Training over Edge Networks**

---

## **1. 论文的主要贡献和创新点**

### **解决了什么问题**
在资源受限、计算能力异构且存在随机token到达的边缘网络中，传统的 **distributed MoE**（Mixture-of-Experts）训练面临以下挑战：
- **Token backlog**：由于边缘设备计算能力不均，部分服务器负载过重，导致token积压。
- **资源利用率低**：传统路由策略（如top-K）仅基于token特征进行专家选择，忽略设备的实时计算和能量状态，造成资源浪费或瓶颈。
- **系统吞吐量下降与性能退化**：缺乏对token队列和能量队列的长期稳定性控制，影响训练效率和模型精度。

### **提出了什么新方法或新思路**
作者提出了一种名为 **Stable-MoE** 的新型 **Lyapunov-based token routing framework**，其核心思想是：
- 将分布式MoE中的token路由与计算频率分配建模为一个**联合优化问题**，目标是在最大化系统吞吐量的同时保持**gating consistency**（路由一致性）。
- 引入 **Lyapunov optimization理论**，将原本难以求解的长期随机优化问题转化为每个时隙可在线决策的子问题，无需预知未来的系统状态。
- 设计虚拟队列机制（token queue 和 energy queue），通过最小化 **Lyapunov drift-plus-penalty函数** 来保证系统的长期稳定性和资源高效利用。

### **相比现有方法的优势**
- **动态适应性强**：能够实时响应异构边缘设备的计算能力和能量约束，避免过载。
- **无需先验信息**：在线决策，适用于实际部署中不可预测的token到达模式。
- **兼顾性能与稳定性**：不仅提升系统吞吐量，还确保token和能量队列的长期稳定，防止崩溃或资源耗尽。
- **通用性强**：框架可扩展至其他资源受限的分布式AI训练场景。

---

## **2. 核心实验方法和设置**

### **使用的数据集**
- **SVHN**（Street View House Numbers）：包含32×32彩色数字图像，用于图像分类任务。
- **CIFAR-100**：同样为32×32彩色图像，共100类，更具挑战性。

### **实验设置**
- **边缘网络配置**：
  - $ J = 10 $ 个异构边缘服务器（edge servers），每台部署一个expert。
  - 时间槽（time slot）长度 $ T = 1 $ 秒。
  - 平均token到达率 $ \lambda = 390 $ tokens/slot（服从泊松分布）。
  - CPU频率上限 $ f_{\text{max}} = 3 $ GHz，计算复杂度 $ c = 10^7 $ cycles/token。
  - 能量预算 $ E^{\text{max}} \in [3J, 15J] $，平均可用能量 $ E^{\text{avg}} \in [1.5J, 9.5J] $。
  - 切换电容系数 $ \kappa = 2 \times 10^{-27} $。
- **模型结构**：
  - gating network：前馈神经网络。
  - expert network：卷积层，用于图像分类。
- **Top-K设置**：$ K = 3 $，即每个token被路由到3个专家。

### **评估指标**
- **系统吞吐量**（System Throughput）：单位时间内完成处理的token数量。
- **测试准确率**（Test Accuracy）：模型在测试集上的分类准确率。
- **队列稳定性**：token queue 和 energy queue 的 backlog 变化趋势。
- **gating consistency**：优化后的路由与原始gating score的一致性程度。

### **基线方法对比**
1. **Strategy A（Random Routing）**：随机均匀地将token分配给所有expert。
2. **Strategy B（Traditional Top-K Routing）**：根据gating score选择top-3专家，不考虑资源状态。
3. **Strategy C（Queue-aware Routing）**：优先路由到token队列最短的expert。
4. **Strategy D（Energy-aware Routing）**：优先路由到energy queue backlog最小的expert。

---

## **3. 主要实验结果和性能指标**

### **关键性能数据**
- 在 **SVHN** 数据集上，Stable-MoE 达到约 **400,000 tokens** 的累计吞吐量（1000轮后），显著高于所有基线。
- 在 **CIFAR-100** 上也表现出持续增长的高吞吐优势。
- 测试准确率方面：
  - **SVHN**：Stable-MoE 快速收敛至 **80%** 准确率，远超基线。
  - **CIFAR-100**：同样取得最高准确率，优于所有对比方法。

### **与基线方法的对比结果**
| 指标 | Stable-MoE vs. Baselines |
|------|--------------------------|
| **系统吞吐量增益** | 相比最优基线（Strategy D）提升至少 **40%** |
| **测试准确率增益** | 提升约 **5%** 或以上 |
| **队列稳定性** | token和energy队列快速稳定，无持续增长现象 |

> 图3显示，Stable-MoE的吞吐量曲线始终领先且差距随时间扩大；图4表明其准确率收敛更快、更稳定。

### **消融实验结果（文中未明确提供）**
- 文中未单独列出消融实验（ablation study），但从设计逻辑可知：
  - 若移除Lyapunov drift项，则队列可能不稳定；
  - 若移除gating consistency正则项，则可能偏离原始MoE语义，影响模型表现；
  - 参数 $ V $ 控制吞吐量与稳定性的权衡，实验中通过调参实现最佳平衡。

---

## **4. 关键结论和发现**

### **主要发现**
1. **Stable-MoE有效解决了边缘网络中MoE训练的token backlog问题**，实现了高吞吐、低延迟的稳定训练。
2. **基于Lyapunov的在线优化框架能够在未知未来状态的情况下做出近优决策**，适合真实边缘环境。
3. **综合考虑token队列和energy队列的双虚拟队列机制** 显著提升了资源利用率和系统鲁棒性。
4. **在SVHN和CIFAR-100上的实验验证了该方法在吞吐量和准确率上的双重优势**，证明其在实际应用中的潜力。

### **方法的局限性**
- **依赖混合整数规划求解器**（如branch-and-bound）解决每时隙的优化问题，可能带来较高计算开销，尤其是在大规模expert系统中。
- 当前假设为**one-to-one expert-server映射**，未考虑多expert共享同一设备的情况。
- 实验基于模拟环境，尚未在真实无线边缘网络中部署验证通信延迟、带宽波动等影响。

### **未来工作方向**
- 探索轻量化求解策略（如启发式算法或神经求解器）以降低在线决策延迟。
- 扩展至支持 **multi-token batching** 和 **dynamic expert scaling** 的场景。
- 结合无线信道质量（channel state）进一步优化端到端延迟。
- 将框架推广至联邦学习或其他分布式AI架构中。

--- 

> ✅ **总结一句话**：  
> **Stable-MoE通过Lyapunov优化实现了资源感知的动态token路由，在保障边缘设备队列稳定的前提下，显著提升了distributed MoE训练的吞吐量和模型准确性，为边缘智能的大规模部署提供了新思路。**

</details>

---

### 2. [Each Prompt Matters: Scaling Reinforcement Learning Without Wasting Rollouts on Hundred-Billion-Scale MoE](https://arxiv.org/abs/2512.07710)

**Authors**: Anxiang Zeng, Haibo Zhang, Hailing Zhang, Kaixiang Mo, Liang Yao, Ling Hu, Long Zhang, Shuman Liu, Shuyi Xie, Yanshi Li, Yizhang Chen, Yuepeng Sheng, Yuwei Huang, Zhaochen Xu, Zhiqiang Zhou, Ziqin Liew  
**Category**: cs.AI  
**Published**: 2025-12-09  
**Score**: 12.5  
**Type**: new  
**ArXiv ID**: 2512.07710v1  

#### Abstract
We present CompassMax-V3-Thinking, a hundred-billion-scale MoE reasoning model trained with a new RL framework built on one principle: each prompt must matter. Scaling RL to this size exposes critical inefficiencies-zero-variance prompts that waste rollouts, unstable importance sampling over long ho...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Each Prompt Matters: Scaling Reinforcement Learning Without Wasting Rollouts on Hundred-Billion-Scale MoE*

---

## 1. 论文的主要贡献和创新点

### 解决的问题
该论文针对在**百亿参数规模的 Mixture-of-Experts (MoE)** 模型上进行长链推理（LongCoT）强化学习（Reinforcement Learning, RL）时面临的多个系统性和算法性挑战，主要包括：

- **Zero-Variance Prompts (ZVP)**：大量提示（prompts）在训练中产生相同奖励，导致无有效梯度信号，浪费 rollout 资源。
- **Importance Sampling 不稳定**：在 token 和 sequence 层面的重要性采样在长序列下易崩溃。
- **Train-Infer Mismatch**：训练与推理阶段 MoE 路由器（router）行为不一致，引发数值误差和训练不稳定。
- **Advantage Inversion**：标准 Bradley-Terry (BT) 奖励模型在均值附近出现优势反转（advantage flipping），误导优化方向。
- **系统瓶颈**：rollout 阶段延迟高、GPU 利用率低，限制端到端训练吞吐。

### 提出的新方法与创新点

作者提出一个**统一的 RL 框架**，以“每个 prompt 都应提供有效学习信号”为核心原则，集成多项算法与系统级创新：

#### （1）Multi-Stage Zero-Variance Elimination（多阶段零方差消除）
- **目标**：识别并处理导致组内奖励方差为零的 prompts。
- **方法**：
  - 扩展探索空间（增大 sampling size $N$）；
  - 引入长度惩罚、重复惩罚等重塑奖励；
  - 使用 RL-ZVP 技术注入随机性以稳定优势估计。
- **优势**：减少约 17% 的 ZVP，显著提升收敛速度与稳定性。

#### （2）ESPO: Entropy Importance Sampling Policy Optimization
- **目标**：解决 GRPO 中 token-level 重要性采样不均衡问题。
- **方法**：
  - 将序列按熵值分组，在 token-group 级别进行策略优化；
  - 引入 entropy-adaptive clipping 动态调整裁剪阈值；
  - 改进目标函数以平衡探索与利用。
- **优势**：相比 GRPO 的均匀处理，ESPO 更好地保留高熵区域的学习信号，提升长序列下的学习稳定性。

#### （3）Router Replay 与 Advantage Stabilization
- **Router Replay**：
  - 在 vLLM rollout 阶段记录 MoE 路由决策；
  - 在 Megatron 训练阶段重放这些路由选择，对齐训练与推理行为。
  - **效果**：log-prob 差异从 $10^{-3}$ 降至 $10^{-4}$，有效防止 RL 崩溃。
- **Generative Reward Model (GenRM)**：
  - 使用具备 CoT 推理能力的 GenRM 替代传统 ORM；
  - 引入三类判断（better/tie/worse），避免 near-mean 优势反转。
  - **效果**：与 GPT-4 判断一致性达 84.3%，远超基线 ORM 的 74.1%。

#### （4）High-Throughput Rollout System
- **FP8-Quantized Rollout**：使用 FP8 精度加速推理，降低 rollout 延迟。
- **Length-Aware Scheduling**：基于预测生成长度动态负载均衡，减少 worker 同步等待。
- **Overlapped Reward Computation**：rollout 完成就立即计算 reward，实现流水线并行。
- **Multi-Detokenization Parallelism**：并行化 detokenization 过程，缓解 CPU 瓶颈。
- **总体收益**：端到端训练时间减少近 20%，系统吞吐提升 1.66×。

---

## 2. 核心实验方法和设置

### 数据集
- **内部构建多领域数据集**：
  - **E-commerce**：覆盖东南亚七国语言（en, tw, id, my, pt, th, vi），任务包括商品推荐、售后问题、属性提取、标题优化等。
  - **Multilingual General Tasks**：涵盖知识理解、创意生成、数学、代码、安全等。
- **公开基准测试集**：
  - **ARC 能力评估套件**：包括 MMLU-Redux、GPQA-Diamond、AIME/HMMT（数学）、HumanEval/MBPP（代码）、BFCL（Agent 任务）、IFeval（指令遵循）等。

### 实验设置
- **模型架构**：CompassMax-V3-Thinking，百亿参数 MoE 架构。
- **训练流程三阶段**：
  1. **Cold-Start SFT**：基于 DeepSeek-R1 蒸馏 Long-CoT 数据；
  2. **Model Merge**：融合通用 Long-CoT 模型与电商专用模型（采用 TIES 方法）；
  3. **Large-Scale RL**：两阶段 RL 微调（先 code/math/IF，后扩展至 e-commerce/tool-use/general QA）。
- **评估方式**：
  - 内部任务使用自动化 verifier 或 GPT-4.1 作为裁判；
  - 公开基准使用标准 metric（如 Pass@1, EM, Acc）。

### 基线方法对比
- **模型基线**：
  - CompassMax-V3
  - GPT-5-Thinking(medium)
  - DeepSeek-R1
  - Qwen3-235B-A22B(think)
  - Gemini-2.5-pro
  - GLM-4.5
- **算法基线**：
  - GRPO（Group Relative Policy Optimization）
  - ORM（Ordinary Reward Model）
  - BF16/FP16 推理精度方案

---

## 3. 主要实验结果和性能指标

### 关键性能数据

#### （1）内部电商基准（Table 2）
| 指标 | CompassMax-V3-Thinking |
|------|------------------------|
| **Ecom QA 平均得分** | 92.17 |
| **After-Sales Issue** | 98.48 |
| **Product Recommendation** | **94.58** |
| **Overall Macro Average** | **85.79** |

> 显著优于 CompassMax-V3（74.65）及其他闭源模型，尤其在生产关键任务上表现稳健。

#### （2）多语言评估（Table 3）
| 指标 | 结果 |
|------|------|
| **SEA 7语言平均分** | **86.41** |
| 英文（en） | 88.07 |
| 印尼语（id） | 87.89 |
| 泰语（th） | 86.04 |
| 葡萄牙语（pt） | 86.84 |

> 表现出高度跨市场稳定性，缩小了与 GPT-4o/GPT-5 的差距，且在低资源语言上保持一致性。

#### （3）通用能力评估（Table 4）
| 领域 | CompassMax-V3-Thinking |
|------|-------------------------|
| Knowledge & Comprehension | 62.07 |
| Creative Generation | **77.75** |
| Reasoning | **74.54** |
| Code | **76.83** |
| Safety | **88.86** |
| **Overall Average** | **76.01** |

> 相比原始 CompassMax-V3（64.49）提升超过 11 个百分点，尤其在安全性与代码生成方面领先。

#### （4）开源 ARC 基准（Table 5）
| Benchmark | CompassMax-V3-Thinking |
|----------|------------------------|
| **HumanEval (Pass@1)** | **98.17** |
| MBPP (Pass@1) | 73.54 |
| AIME24 (Pass@1) | 83.30 |
| AIME25 (Pass@1) | 80.00 |
| HMMT (Pass@1) | 46.70 |
| IFeval (prompt strict) | 85.40 |
| GPQA-Diamond (EM) | 68.69 |
| BFCL_AST_NON_LIVE | 83.73 |

> 在代码生成接近完美（HumanEval 98.17），数学与推理显著优于多数基线，agent 多轮交互仍有提升空间（BFCL_MULTI_TURN_LIVE: 19.50）。

### 与基线对比结果
- 在所有内部评测中均超越 GPT-5-Thinking(medium)、DeepSeek-R1、Qwen3-235B-A22B。
- 在 HumanEval 上超过 DeepSeek-R1（96.95 → 98.17），在 Safety 上超越 GPT-5（88.57 vs 88.86）。
- 多语言平均分（86.41）接近 GPT-5-Thinking(medium) 的 86.64，优于其他开源模型。

### 消融实验（Figure 3）
- **Zero-Variance Rate vs N**：随着 sampling size $N$ 增大，ZVP 率下降，Pass@n 上升；
- **Advantage per Compute**：最优 $N$ 可使单位算力获取的优势最大化；
- **系统优化叠加效果（Table 1）**：
  - +FP8 rollout → 1.52× 加速
  - +Length-based load balancing → 1.66× 整体提速

---

## 4. 关键结论和发现

### 主要发现
1. **“每个 prompt 都重要”是可实现的设计原则**：通过 ZVP 消除机制，可以显著减少无效 rollout，提高训练效率。
2. **ESPO 比 GRPO 更适合长序列 MoE 模型**：token-group 级别的 entropy-aware 重要性采样提升了学习稳定性。
3. **Router Replay 是稳定 MoE RL 的关键**：训练与推理路由对齐能从根本上缓解 train-infer mismatch。
4. **GenRM + 三类标签设计可防止 advantage inversion**：结构化推理奖励模型更可靠，尤其在质量相近样本中。
5. **系统级优化不可忽视**：FP8、length-aware scheduling、reward overlap 共同贡献近 70% 的端到端加速。

### 方法的局限性
- **多轮 Agent 性能仍不足**：在 BFCL_MULTI_TURN_LIVE 上仅得 19.50，表明复杂工具链规划能力有待加强。
- **依赖高质量参考答案**：GenRM 在无 reference 场景下的泛化能力未充分验证。
- **硬件依赖较强**：FP8 rollout 依赖 H100 等高端 GPU，可能限制部署灵活性。

### 未来工作方向
- 扩展 GenRM 至 fully online preference learning；
- 探索 asynchronous RL 与更细粒度的 entropy grouping；
- 引入 memory-augmented routing 以进一步提升 MoE 稳定性；
- 构建更大规模的 multilingual agent benchmark。

---

> ✅ **总结一句话**：  
> 本文通过算法与系统的协同创新，首次实现了**百亿参数 MoE 模型上的高效、稳定 LongCoT 强化学习**，提出“每个 prompt 都重要”的理念，并通过 ESPO、Router Replay、GenRM 和 FP8 rollout 等技术组合，在电商、多语言、代码、数学等多个维度达到 SOTA 表现，为大规模推理模型训练提供了可复用的工程与理论框架。

</details>

---

### 3. [Native Parallel Reasoner: Reasoning in Parallelism via Self-Distilled Reinforcement Learning](https://arxiv.org/abs/2512.07461)

**Authors**: Tong Wu, Yang Liu, Jun Bai, Zixia Jia, Shuyi Zhang, Ziyong Lin, Yanting Wang, Song-Chun Zhu, Zilong Zheng  
**Category**: cs.CL  
**Published**: 2025-12-09  
**Score**: 11.5  
**Type**: new  
**ArXiv ID**: 2512.07461v1  

#### Abstract
We introduce Native Parallel Reasoner (NPR), a teacher-free framework that enables Large Language Models (LLMs) to self-evolve genuine parallel reasoning capabilities. NPR transforms the model from sequential emulation to native parallel cognition through three key innovations: 1) a self-distilled p...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# Native Parallel Reasoner: Reasoning in Parallelism via Self-Distilled Reinforcement Learning — 核心总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前主流的 **Large Language Models (LLMs)** 在复杂推理任务中仍依赖于**顺序链式思维（Chain-of-Thought, CoT）**，存在以下三大瓶颈：
1. **算法与架构不兼容**：现有推理引擎（如 SGLang）和强化学习（RL）算法难以支持真正的并行分支生成与聚合。
2. **低效的手工并行化**：已有方法（如 Multiverse）依赖独立采样进行任务分解，无法共享 Key-Value (KV) 缓存，导致冗余计算和线性延迟增长（O(N)），效率低下。
3. **依赖监督蒸馏**：多数并行推理框架（如 Multiverse）依赖更强教师模型生成的轨迹数据，限制了学生模型探索新颖、内在的并行策略的能力，形成“智能天花板”。

### 提出的新方法与思路
作者提出 **Native Parallel Reasoner (NPR)**，一个无需外部教师、完全自演化的并行推理框架，通过三个阶段实现从顺序模拟到原生并行认知的转变：

#### 创新点一：自蒸馏渐进训练范式（Self-Distilled Progressive Training）
- **Stage 1（格式引导 RL）**：使用 **DAPO** 算法对基础模型（如 Qwen3-4B-Instruct）进行强化学习，仅通过格式奖励（是否符合 `<plan>/<step>/<takeaway>` 结构）和答案正确性奖励，诱导模型自发生成结构化并行输出，得到 **NPR-ZERO**。
- **Stage 2（拒绝采样 + 并行预热）**：对 NPR-ZERO 进行多次采样，通过 **rejection sampling** 筛选出格式正确且答案正确的高质量轨迹，构建自蒸馏数据集；随后在这些数据上进行 **Parallel SFT**，引入严格的并行注意力掩码（Parallel Attention Mask）和位置编码（Positional Encoding），使模型行为真正转变为原生并行执行。
- **Stage 3（原生并行 RL）**：基于 NPR-BETA 模型，使用新提出的 **PAPO（Parallel-Aware Policy Optimization）** 算法，在真实并行图结构中优化分支策略，让模型通过试错学习自适应的任务分解能力。

#### 创新点二：PAPO 强化学习算法
- 针对并行解码特性设计，直接在并行执行图中优化策略。
- 改进标准 PPO：
  - 使用 **batch-level advantage normalization** 替代 group-level，避免因格式过滤导致方差坍缩。
  - **保留特殊标记（如 `<step>`）上的梯度**，防止结构崩溃。
  - 移除重要性采样（importance sampling），采用严格 on-policy 更新以提升稳定性。

#### 创新点三：NPR Engine（工程级增强）
为支持大规模稳定并行 RL 训练，重构了底层推理引擎（基于 SGLang），解决了多个关键问题：
- **KV-cache 内存泄漏**：引入预算感知的显式回收机制。
- **token 数量误算**：改进全局 token 账本，考虑分支因子。
- **非法并行结构导致未定义状态**：增加轻量级前置验证器。
- **局部重复问题**：在 `<step>` 块内施加轻微重复惩罚。

---

### 相比现有方法的优势
| 维度 | 现有方法（如 Multiverse） | NPR |
|------|--------------------------|-----|
| **训练方式** | 依赖强教师模型蒸馏 | 完全自蒸馏，无需外部监督 |
| **并行真实性** | 存在 ~30% AR fallback（退化为顺序生成） | **100% genuine parallelism** |
| **KV 共享** | 不充分或未利用 | 支持高效 KV-cache 复用 |
| **推理速度** | 加速有限（1.3×–2.4×） | 最高 **4.6× wall-clock speedup** |
| **泛化能力** | 受限于教师模型模式 | 可演化出新型并行策略 |

---

## 2. 核心实验方法和设置

### 使用的数据集
- **训练数据**：基于 **ORZ 数据集**（57k 问答对），从中抽取固定子集 **8k 示例**用于所有训练阶段。
- **评估基准**（共 8 个）：
  - 数学竞赛类：**AIME25**, **AIME24**, **HMMT25**, **AMC23**
  - 综合推理类：**OlympiadBench**, **Minerva-Math**, **ZebraLogic**, **MATH500**

### 实验设置与评估指标
- **模型基础**：基于 **Qwen3-4B-Instruct-2507** 和 **Qwen3-4B (Non-Thinking)**。
- **评估指标**：
  - **avg@k**：k 次采样中正确答案的比例期望值。
  - **best@8**：8 次采样中最优结果的准确率，衡量 test-time scalability。
  - **Tokens Per Second (TPS)** 与 **Speedup Ratio**：评估推理效率。
  - **Parallel Reasoning Trigger Rate**：触发并行推理的样本占比。

### 基线方法对比
| 类别 | 对比方法 |
|------|--------|
| **顺序推理器** | Qwen2.5-32B-Instruct, Qwen3-4B-Instruct, SR-BETA/SR |
| **并行推理器** | Multiverse-32B, Multiverse-4B（复现） |
| **变体对比** | NPR-BETA（仅 SFT）、SR（顺序 RL）等 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（见 Table 2）

| 模型 | AIME25 | AIME24 | HMMT25 | AVG |
|------|--------|--------|--------|-----|
| Qwen3-4B-Instruct | 47.4 | 60.0 | 31.0 | 63.7 |
| Multiverse-4B | 42.9 | 46.7 | 20.8 | 50.1 |
| **NPR (Qwen3-4B-Instruct)** | **50.4** | **63.3** | **30.8** | **65.0** |
| Multiverse-32B | 45.8 | 53.8 | 20.8 | 52.5 |

> ✅ NPR 在 **AIME25 上达到 50.4%**，超越 Multiverse-32B（45.8）和 Multiverse-4B（42.9），即使后者使用更强教师模型蒸馏。

当训练起点为非思考模式的 Qwen3-4B 时：
- NPR 平均性能增益超过 **24.5%**。

---

### 与基线方法的对比结果

#### ✅ 自蒸馏数据更优
- NPR 使用自蒸馏数据（orz-8k）相比 Multiverse 使用教师蒸馏数据（s1.1-8k），平均得分提升 **+8.9 pts**。
- 在 ZebraLogic 上提升高达 **+15.9 pts**，说明自演化的并行策略更具多样性与准确性。

#### ✅ 并行 SFT 优于顺序 SFT
- NPR-BETA（Parallel SFT）相比 SR-BETA（Sequential SFT）在 AIME25 上提升 **+5.8 pts**（42.9 vs 37.1）。
- 表明并行监督能缓解顺序先验偏差，促进灵活任务分解。

#### ✅ 并行 RL 显著增强性能
- NPR（Parallel RL）相比 SR（Sequential RL）平均提升 **+3.0 pts**。
- 在 AIME24 上从 57.1 → 63.3（+6.2），HMMT25 上 26.3 → 30.8（+4.5），显示 PAPO 能有效放大高奖励路径。

---

### 推理加速效果（Table 3）
| 方法 | AIME25 Speedup | HMMT25 Speedup | AMC23 Speedup |
|------|---------------|---------------|--------------|
| Multiverse | 2.4× | 2.1× | 1.7× |
| **NPR** | **4.6×** | **4.1×** | **2.9×** |

> ⚡️ NPR 在难题上加速更显著，表明其在需要深度探索的任务中优势更大。

---

### 并行性分析（Table 4）
| 模型 | AIME25 | ZebraLogic | MATH500 | 平均 |
|------|--------|------------|---------|------|
| Multiverse-32B | 65.0% | 45.8% | 76.0% | ~63% |
| **NPR-Inst.** | **100.0%** | **100.0%** | **100.0%** | **100%** |

> 🎯 NPR 实现了 **100% genuine parallel reasoning**，无任何 AR fallback，而 Multiverse 存在严重伪并行行为。

---

### Test-Time Scalability（Table 5）
- 在 Non-Thinking 模型上，NPR 将 AIME25 的 **best@8** 从 36.7（原始）→ 70.0（SFT）→ **76.7（NPR）**，进一步提升 6.7 pts。
- 表明 NPR 显著增强了采样多样性与最优解覆盖率。

---

## 4. 关键结论和发现

### 主要发现
1. **自蒸馏可替代教师模型**：无需依赖外部强模型，LLM 可通过自我演化掌握高效的并行推理能力。
2. **原生并行优于模拟并行**：通过严格的并行注意力结构和位置编码，可实现真正意义上的并发执行，而非顺序模拟。
3. **PAPO + NPR Engine 是关键支撑**：专用 RL 算法与稳定引擎共同保障了大规模并行 RL 的可行性与有效性。
4. **并行带来双重收益**：不仅提升 **accuracy**（最高 +24.5%），还实现 **4.6× 推理加速**，打破“精度 vs 效率” trade-off。
5. **100% genuine parallelism 可达**：NPR 彻底消除 AR fallback，建立新的并行推理标准。

---

### 方法的局限性
- 当前主要在 **Qwen3-4B** 上验证，尚未扩展至更大规模模型（如 70B+）。
- 初步尝试在 Qwen2.5 系列失败，说明对基础模型的指令遵循与推理能力有一定要求。
- 并行结构目前依赖预定义模板（`<plan>/<step>/<takeaway>`），灵活性受限。

---

### 未来工作方向
- 扩展至更多模型家族（Llama、DeepSeek 等）。
- 探索动态、可学习的并行结构，而非固定模板。
- 将 NPR 应用于长文本生成、代码合成、多模态推理等更广泛场景。
- 构建开放的并行推理生态系统（已开源代码、模型、网站）。

---

## 总结
**NPR** 是首个实现 **完全自演化、100% 原生并行推理** 的框架，通过 **自蒸馏 + 并行 SFT + PAPO RL** 三阶段训练，结合工程级 **NPR Engine** 支撑，实现了推理能力与效率的双重突破。它不仅超越了依赖教师蒸馏的 Multiverse 等 SOTA 方法，更为未来 agentic AI 的规模化、高效化推理提供了新范式。

> 🔗 开源地址：
> - Code: [https://github.com/bigai-nlco/Native-Parallel-Reasoner](https://github.com/bigai-nlco/Native-Parallel-Reasoner)
> - Model: [https://huggingface.co/bigai-NPR](https://huggingface.co/bigai-NPR)
> - Website: [https://bigai-nlco.github.io/Native-Parallel-Reasoner](https://bigai-nlco.github.io/Native-Parallel-Reasoner)

</details>

---

### 4. [Communication-Efficient Serving for Video Diffusion Models with Latent Parallelism](https://arxiv.org/abs/2512.07350)

**Authors**: Zhiyuan Wu, Shuai Wang, Li Chen, Kaihui Gao, Dan Li, Yanyu Ren, Qiming Zhang, Yong Wang  
**Category**: cs.DC  
**Published**: 2025-12-09  
**Score**: 11.0  
**Type**: new  
**ArXiv ID**: 2512.07350v1  

#### Abstract
Video diffusion models (VDMs) perform attention computation over the 3D spatio-temporal domain. Compared to large language models (LLMs) processing 1D sequences, their memory consumption scales cubically, necessitating parallel serving across multiple GPUs. Traditional parallelism strategies partiti...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Communication-Efficient Serving for Video Diffusion Models with Latent Parallelism

---

## 1. 论文的主要贡献和创新点

### 解决的问题
视频扩散模型（**Video Diffusion Models, VDMs**）在生成高质量、长时序一致的视频方面取得了显著进展（如 Sora、Veo、WAN）。然而，VDMs 在推理过程中需要对三维时空域（时间、高度、宽度）进行注意力计算，导致其 **GPU 内存消耗呈立方级增长**，远超处理一维序列的大型语言模型（LLMs）。这使得单卡部署几乎不可行，必须依赖多 GPU 并行服务。

传统的并行策略（如 **Naive Model Parallelism (NMP)**、**Pipeline Parallelism (PP)** 和 **Tensor Parallelism (TP)**）虽然能分摊内存压力，但在 VDM 推理中会引入严重的通信瓶颈——因为它们需要在每个去噪步（diffusion timestep）频繁传输高维中间激活张量（activation tensors），尤其是在带宽受限的 PCIe 互联环境中，通信开销成为系统性能的瓶颈。

### 提出的新方法：Latent Parallelism (LP)
本文提出了 **Latent Parallelism (LP)**，这是首个专为 VDM 服务设计的并行化策略。其核心思想是：

> **将并行化的对象从“模型”转移到“请求的潜在空间（latent tensor）”本身**。

具体而言，LP 将整个视频的全局潜在空间（latent space）动态地划分为多个子区域（sub-latents），这些子区域被分配到不同的 GPU 上并行执行去噪操作，最后再合并重构为完整的潜在表示。

#### 创新机制
1. **动态旋转分区（Dynamic Rotating Partition）**  
   在不同的去噪时间步，交替沿时间、高度、宽度三个维度进行分区。这种旋转确保了每个局部区域在多个时间步后都能获得完整的时空上下文，从而维持全局一致性。

2. **基于视觉块对齐的重叠分区（Patch-Aligned Overlapping Partition）**  
   分区边界与 VDM 内部的视觉块（visual patches）对齐，并引入可控的重叠区域，防止因切割破坏特征完整性而导致的边界伪影。

3. **位置感知的潜在重构（Position-Aware Latent Reconstruction）**  
   在合并阶段，采用基于位置的自适应加权平均，使重叠区域的预测更加平滑，消除拼接痕迹。

### 相比现有方法的优势
- **通信效率极高**：仅需在 GPU 间传输紧凑的潜在张量（latent tensors），而非庞大的中间激活张量，通信开销降低高达 **97%**。
- **非侵入式插件范式**：LP 可无缝集成到现有的并行策略（如 DP、TP、PP）之上，形成混合并行框架。
- **保持生成质量**：通过精心设计的分区与重构机制，生成视频的质量与集中式（Centralized）生成相当。
- **可扩展性强**：适用于不同长度的视频和不同规模的 GPU 集群。

---

## 2. 核心实验方法和设置

### 数据集
在三个广泛使用的视频生成基准上进行了评估：
- **EvalCrafter** [17]
- **T2V-CompBench** [25]
- **VBench** [12]

由于数据集规模较大，作者使用固定随机种子选取代表性子集以保证可复现性。

### 实验设置
- **硬件平台**：4 台 NVIDIA RTX A6000 GPU，通过 PCIe 总线互联，配备 80 核 CPU 和 280GB RAM。
- **模型配置**：采用 **WAN2.1-1.3B** 作为基础 VDM，包含 30 个 DiT Blocks、T5 文本编码器和预训练 VAE 解码器。
- **默认参数**：
  - 视频帧率：16 FPS
  - 分辨率：480p
  - 去噪步数：60 步
  - 并行 GPU 数：4

### 评估指标
1. **通信开销（Communication Overhead）**：以 MB 为单位测量总网络流量。
2. **生成质量（Generation Quality）**：使用 **VBench** 框架评估以下五个子指标：
   - Subject Consistency (SC)
   - Background Consistency (BC)
   - Temporal Flickering (TF)
   - Motion Smoothness (MS)
   - Imaging Quality (IQ)

### 基线方法对比
- **NMP**（Naive Model Parallelism）：连续划分 DiT Blocks。
- **PP**（Pipeline Parallelism）：利用 CFG 的条件/无条件通路构造微批次流水线。
- **HP**（Hybrid Parallelism）：结合 FSDP 与 xDiT 的算子级并行框架。
- **Centralized**：单卡集中式生成（理想参考）。
- **VideoCrafter**：作为外部模型性能参考。

---

## 3. 主要实验结果和性能指标

### 关键性能数据

#### 通信开销对比（Table 1）
| 方法 | 49帧（3秒）总开销 | 81帧（5秒）总开销 |
|------|------------------|------------------|
| NMP | 57,950.17 MB (~57.9 GB) | 93,050.17 MB (~93.1 GB) |
| PP | 57,590.16 MB (~57.6 GB) | 92,690.16 MB (~92.7 GB) |
| HP | 4,758.08 MB (~4.8 GB) | 7,686.12 MB (~7.7 GB) |
| **LP (r=0.5)** | **1,354.34 MB (~1.4 GB)** | **2,191.29 MB (~2.2 GB)** |
| **LP (r=1.0)** | **1,811.88 MB (~1.8 GB)** | **2,912.81 MB (~2.9 GB)** |

> ✅ **LP 相比 NMP/PP 减少约 97% 通信开销，相比 HP 减少约 72%**。

#### 端到端延迟（Table 2）
| 方法 | 推理延迟（秒） |
|------|---------------|
| NMP | 239.33 s |
| LP (r=1.0) | 220.69 s |
| **LP (r=0.5)** | **195.27 s** |

> ✅ 即使有更大重叠（r=1.0），LP 仍比 NMP 更快，表明通信瓶颈的有效缓解。

### 与基线方法的对比结果

- **生成质量**（Figure 4 & Table 3）：
  - LP（r=0.5 和 r=1.0）在所有 VBench 子指标上的表现与 **Centralized、NMP、PP、HP** 处于同一高性能层级。
  - 所有 LP 配置的平均质量得分与 Centralized 的差异不超过 **0.6%**。
  - 显著优于 **VideoCrafter**，尤其在 IQ、SC 和 MS 指标上。

- **可视化对比**（Figure 5, 11–13）：
  - LP 生成的视频在起始、中间、结束帧均与 Centralized 结果视觉上难以区分。
  - 成功保留面部细节、纹理清晰度、色彩准确性，未出现边界伪影或空间不连续性。

### 消融实验结果

| 实验方向 | 发现 |
|--------|------|
| **重叠比例 r**（Fig 6–7） | - 通信开销随 r 增大而增加（r=0.1→1.0，开销翻倍）<br>- 但质量提升趋于饱和，**r=0.5 已达最优权衡点** |
| **GPU 数量**（Fig 8） | - LP 在 2–8 GPU 上均保持高质量<br>- 性能稳定，优于 VideoCrafter，验证可扩展性 |
| **视频时长**（Fig 9） | - HP 开销随视频变长急剧上升（3s→10s，5GB→15GB）<br>- LP 仅增加约 4GB，表现出更强的长视频适应能力 |
| **分区策略消融**（Fig 10） | - 固定时间维度分区（w/o LP）性能全面落后<br>- 验证了动态旋转、块对齐、位置感知重构三者协同作用的重要性 |

---

## 4. 关键结论和发现

### 主要发现
1. **通信瓶颈的根本原因在于高维激活传输**，而 LP 通过在潜在空间进行并行化，从根本上规避了这一问题。
2. **Latent Parallelism 是首个专为 VDM 设计的并行策略**，实现了通信效率与生成质量的双重保障。
3. **理论分析证明 LP 具备“2-完备性”（2-Completeness）**：任意位置在两个去噪步内即可获取完整时空上下文，确保全局一致性。
4. **LP 是非侵入式插件**，可与现有并行策略组合，构建层次化混合并行架构（Hierarchical Hybrid Parallelism），进一步提升大规模集群下的可扩展性。

### 方法的局限性
- 当前实现假设所有 GPU 具有相同算力，未考虑异构设备场景。
- 动态旋转分区可能带来轻微负载不均衡，尤其在非均匀分辨率输入下。
- 虽然通信大幅减少，但潜在空间的分割与重构本身引入额外计算开销，在极小模型或短视频中优势可能减弱。

### 未来工作方向
- 将 LP 扩展至支持 **Mixture-of-Experts (MoE)** 架构的 VDM。
- 探索与 **runtime-adaptive caching**（如 MagCache、TeaCache）等加速技术的联合优化。
- 在真实生产环境中部署 LP，研究其在动态批处理（dynamic batching）、流式生成等场景下的表现。
- 进一步优化分区策略，实现更智能的自适应分割（adaptive partitioning）。

---

> **总结**：本文提出的 **Latent Parallelism (LP)** 为 VDM 的高效服务提供了一种全新的范式。它不仅解决了传统并行策略带来的严重通信瓶颈，还通过精巧的设计保持了生成质量，是迈向实用化、低成本、高吞吐视频生成服务的关键一步。

</details>

---

### 5. [Bandwidth-Aware Network Topology Optimization for Decentralized Learning](https://arxiv.org/abs/2512.07536)

**Authors**: Yipeng Shen, Zehan Zhu, Yan Huang, Changzhi Yan, Cheng Zhuo, Jinming Xu  
**Category**: cs.DC  
**Published**: 2025-12-09  
**Score**: 11.0  
**Type**: new  
**ArXiv ID**: 2512.07536v1  

#### Abstract
Network topology is critical for efficient parameter synchronization in distributed learning over networks. However, most existing studies do not account for bandwidth limitations in network topology design. In this paper, we propose a bandwidth-aware network topology optimization framework to maxim...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Bandwidth-Aware Network Topology Optimization for Decentralized Learning

---

## 1. 论文的主要贡献和创新点

### 解决的问题
在**Decentralized Learning**（去中心化学习）中，网络拓扑结构对参数同步效率至关重要。然而，大多数现有研究在设计网络拓扑时**忽略了带宽限制**，导致在实际异构网络环境中性能下降。本文旨在解决以下问题：
- 如何在**考虑节点间带宽差异**的前提下，优化网络拓扑以最大化共识速度（consensus speed）。
- 如何在通信开销（每轮迭代时间）与收敛速度之间取得更好平衡。

### 提出的新方法与思路
作者提出了一个**带宽感知的网络拓扑优化框架（Bandwidth-Aware Network Topology Optimization）**，命名为 **BA-Topo**，其核心创新包括：

- **将拓扑设计建模为带约束的优化问题**：
  - 以最小化**渐近收敛因子** $ r_{\text{asym}}(W) = \max\{|\lambda_2(W)|, |\lambda_n(W)|\} $ 为目标，即最大化共识速度。
  - 引入**边基数约束**（cardinality constraint），控制拓扑稀疏性。
  - 显式建模**同构与异构带宽场景下的物理资源限制**（如节点端口、服务器内链路、交换机端口等）。

- **提出最大带宽分配策略（Maximum Bandwidth Allocation Strategy）**：
  - 在异构带宽场景下，通过算法动态分配每条逻辑边所占用的物理资源，以**最大化最小边带宽**，从而减少通信瓶颈。

- **问题重构为混合整数半定规划（Mixed-Integer SDP）**：
  - 利用拉普拉斯矩阵变换和引理（Lemma 1）将非凸特征值优化转化为可解的**线性矩阵不等式（LMI）** 形式。
  - 采用基于 **ADMM** 的求解框架，并结合 **Conjugate Gradient Method**（具体为 Bi-CGSTAB）高效求解大规模线性系统，提升可扩展性。

### 相比现有方法的优势
| 方面 | 现有方法局限 | BA-Topo 改进 |
|------|---------------|-------------|
| **带宽建模** | 多假设同构带宽，难以应用于真实异构环境 | 统一支持同构与多种异构带宽场景（节点级、服务器内、跨服务器） |
| **拓扑设计方式** | 多为启发式设计（如 ring, exponential）或简化权重设定（如等权） | 联合优化拓扑结构与边权重，在完整解空间中搜索最优解 |
| **优化目标** | 忽视硬件限制，仅关注理论收敛率 | 同时优化共识速度与通信效率，实现更优的实际训练加速 |
| **可扩展性** | 部分方法计算复杂度高，难扩展至百节点规模 | 使用预条件共轭梯度法（ILU + Bi-CGSTAB），支持数百节点规模 |

---

## 2. 核心实验方法和设置

### 使用的数据集
- **CIFAR-10** 和 **CIFAR-100**：用于验证 BA-Topo 在真实分布式深度学习任务中的有效性。
- 模型：**ResNet-18**
- 分布式训练算法：**DSGD**（Decentralized SGD）

### 实验设置
- **硬件平台**：2 × Intel Xeon Gold 6226R CPU + 8 × NVIDIA RTX 2080 Ti GPU
- **通信后端**：PyTorch 2.0.0 + Gloo
- **本地批量大小**：32（每个节点）
- **超参数**：学习率 0.05，动量 0.9，weight decay 0.0001，训练 100 epochs
- **通信与计算时间测量**：
  - 单次前向传播时间 $ t_{\text{comp}} = 15.21 \, \text{ms} $
  - 参数通信时间 $ t_{\text{comm}} = 5.01 \, \text{ms} $（带宽 9.76 GB/s）
  - 迭代时间按最小边带宽进行归一化：  
    $$
    t_{\text{iter}} = \frac{b_{\text{avail}}}{b_{\min}} \times t_{\text{comm}}, \quad t_{\text{epoch}} = \left(\frac{b_{\text{avail}}}{b_{\min}} \times t_{\text{comm}} + t_{\text{comp}}\right) \times C_{\text{iter}}
    $$

### 评估指标
- **共识速度**（Consensus Speed）：
  - 渐近收敛因子 $ r_{\text{asym}}(W) $
  - 达成共识误差 $ \|x_k - \bar{x}\| < 10^{-4} $ 所需时间
- **训练效率**：
  - 达到目标测试准确率所需的时间（如 CIFAR-10 上 84%，CIFAR-100 上 62%）
  - 相对于基线的**加速比**（speedup）

### 基线方法对比
- Ring
- 2D Grid
- 2D Torus
- Exponential Topology
- Equitopo（及其静态变体 U-EquiStatic）

---

## 3. 主要实验结果和性能指标

### 关键性能数据

#### （1）共识速度表现（Consensus Speed）
- 在 **n=16 节点、同构带宽** 场景下，当边数 $ r=32 $ 时，**BA-Topo 具有最快的共识速度**，显著优于 exponential 和 U-EquiStatic。
- 在 **节点级异构带宽**（8个高带宽节点 + 8个低带宽节点）场景下，BA-Topo 在 $ r=32 $ 和 $ r=48 $ 时均优于所有基线。
- 在 **服务器内链路异构** 场景中，exponential 拓扑因将过多边映射到 SYS 链路（导致最小带宽仅为 0.976 GB/s），性能严重下降；而 BA-Topo 自动避免此类瓶颈。
- 在 **BCube 架构下的跨服务器端口异构** 场景中，BA-Topo 在相同时间内达成更低共识误差。

#### （2）训练效率提升（DSGD 性能）
| 场景 | 数据集 | BA-Topo 最佳配置 | 达标时间（秒） | 相对最快基线加速比 |
|------|--------|------------------|----------------|--------------------|
| 同构带宽 | CIFAR-10 | $ r=32 $ | 110.1 | **1.22×** |
| 同构带宽 | CIFAR-100 | $ r=32 $ | 127.6 | **1.11×** |
| 节点级异构 | CIFAR-10 | $ r=32 $ | 181.6 | **1.58×** |
| 节点级异构 | CIFAR-100 | $ r=48 $ | 194.7 | **1.55×** |
| 服务器内异构 | CIFAR-10 | $ r=8 $ | 266.8 | **1.53×** |
| 服务器内异构 | CIFAR-100 | $ r=8 $ | 261.8 | **1.34×** |
| 跨服务器异构 | CIFAR-10 | $ r=48 $ | 86.3 | **1.56×** |
| 跨服务器异构 | CIFAR-100 | $ r=48 $ | 117.3 | **1.21×** |

> 注：以上数据来自 Table II，展示了 BA-Topo 在不同场景下均显著优于现有拓扑。

#### （3）可扩展性分析（Scalability）
- 表格 I 显示，在节点数从 4 到 128 变化时，BA-Topo 的**渐近收敛因子始终低于 exponential 和 U-EquiStatic**。
- 在 n=128 时，BA-Topo 收敛时间为 1127ms，远低于 exponential 的 1157ms 和 U-EquiStatic 的 1242ms。
- 结果表明 BA-Topo 在大规模场景下仍保持优越的共识性能。

#### （4）消融实验与初始化策略
- 使用 **Simulated Annealing** 初始化拓扑，以获得较小的平均最短路径长度（ASPL），有助于避免陷入局部最优。
- 实验表明该 warm-start 策略有效提升了最终拓扑质量。

---

## 4. 关键结论和发现

### 主要发现
1. **带宽异构性严重影响传统拓扑性能**：exponential 等高性能拓扑在异构环境下可能因集中使用低容量链路而导致通信瓶颈。
2. **联合优化拓扑结构与边权重可显著提升共识速度**：BA-Topo 在完整解空间中搜索，避免了人为设定权重带来的次优性。
3. **BA-Topo 在多种现实场景下均实现显著加速**：
   - 在同构场景下提速 **1.11× ~ 1.22×**
   - 在异构场景下提速可达 **1.58×**
4. **提出的 ADMM + Bi-CGSTAB 框架具备良好可扩展性**：支持百节点级别拓扑优化，适用于实际部署。

### 方法的局限性
- 当前框架针对**静态带宽环境**设计，无法应对动态变化的网络条件（如突发拥塞、链路故障）。
- 拓扑优化过程本身需要一定计算开销，虽离线运行，但在频繁变更集群配置时可能带来额外负担。
- 实验主要基于单服务器或多服务器小规模集群，未在超大规模数据中心（如千节点以上）验证。

### 未来工作方向
- 研究**动态带宽感知的时变拓扑优化方案**，适应实时网络波动。
- 探索轻量化在线拓扑调整机制，降低重新优化成本。
- 将该框架扩展至 **Federated Learning** 或 **data-heterogeneous** 场景，进一步提升实用性。

---

> ✅ **总结一句话**：  
> 本文提出了一种**带宽感知的去中心化学习网络拓扑优化框架 BA-Topo**，通过将拓扑设计建模为 Mixed-Integer SDP 并结合 ADMM 与共轭梯度法求解，在多种异构带宽场景下实现了比现有拓扑更快的共识速度和更高的训练效率，最高提速达 **1.58×**。

</details>

---

### 6. [Think-While-Generating: On-the-Fly Reasoning for Personalized Long-Form Generation](https://arxiv.org/abs/2512.06690)

**Authors**: Chengbing Wang, Yang Zhang, Wenjie Wang, Xiaoyan Zhao, Fuli Feng, Xiangnan He, Tat-Seng Chua  
**Category**: cs.CL  
**Published**: 2025-12-09  
**Score**: 10.5  
**Type**: new  
**ArXiv ID**: 2512.06690v1  

#### Abstract
Preference alignment has enabled large language models (LLMs) to better reflect human expectations, but current methods mostly optimize for population-level preferences, overlooking individual users. Personalization is essential, yet early approaches-such as prompt customization or fine-tuning-strug...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# Think-While-Generating: On-the-Fly Reasoning for Personalized Long-Form Generation 论文总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前的 **Preference Alignment** 技术主要针对群体偏好进行优化，忽视了个体用户的独特需求。在长文本生成任务中，传统的“think-then-generate”范式存在以下问题：
- **静态推理（static one-shot reasoning）** 需要在生成前一次性捕捉所有信息，难以适应内容演进过程中的动态变化；
- 推理与生成之间存在强依赖关系，导致训练和推理效率低下。

这些问题限制了模型对隐式用户偏好的建模能力，影响个性化生成的质量。

---

### 提出的新方法：FlyThinker
作者提出 **FlyThinker**，一种高效的 “**think-while-generating**” 框架，用于个性化长文本生成。

#### 核心创新点：
1. **并行化的“边思考边生成”范式（Think-While-Generating）**
   - 不同于传统“先思考后生成”，FlyThinker 在每个 token 生成过程中动态插入 latent reasoning 步骤，实现细粒度的上下文感知推理。
   - 更符合人类写作时逐步深化理解的过程。

2. **双模型架构设计（Separate Reasoner & Generator）**
   - 引入一个独立的 **Reasoner** 模型，专门负责生成 token-level 的 latent reasoning；
   - **Generator** 则融合这些 reasoning 信号来指导输出。
   - 两者可并行运行，显著提升效率。

3. **保持训练并行性（Preserved Training Parallelism）**
   - Reasoner 的推理仅依赖于已生成的 response tokens，而不依赖自身先前的 reasoning 输出；
   - 这使得所有 reasoning tokens 可以通过一次前向传播批量生成（类似 teacher-forcing），从而保留了标准 LLM 训练的并行优势。

4. **高效推理机制**
   - 在推理阶段，Generator 生成当前 token 的同时，Reasoner 并行准备下一个 step 的 reasoning；
   - 实现近乎零延迟的推理开销，接近非推理增强模型（如 SFT）的延迟水平。

---

### 相比现有方法的优势
| 方法 | 是否支持动态推理 | 训练效率 | 推理效率 | 个性化效果 |
|------|------------------|----------|----------|------------|
| Prompt/Fine-tuning | ❌ | ✅ | ✅ | ⚠️（难捕获隐式偏好） |
| Think-then-generate (e.g., CoT, Coconut) | ❌（静态推理） | ❌（自回归推理慢） | ❌（需额外生成推理链） | ✅ |
| **FlyThinker** | ✅（token-level 动态推理） | ✅（并行训练） | ✅（近似 SFT 延迟） | ✅✅ |

---

## 2. 核心实验方法和设置

### 数据集
在 **LONGLAMP benchmark** 的三个用户划分（user-based split）任务上进行评估：
- **Product Review**：撰写产品评论
- **Abstract Generation**：生成学术摘要
- **Topic Writing**：主题性长文写作  
这些任务要求模型基于用户历史行为推断其风格与偏好，并生成一致的长文本。

主干模型：`Qwen2.5-3B-Instruct`，并在附录中验证了 `Qwen2.5-7B-Instruct` 和 `Gemma-7B-it` 上的有效性。

---

### 实验设置
- 所有实验在 4×NVIDIA A100 GPU 上完成；
- 使用 HuggingFace Transformers 和 Verl 框架实现；
- 采用标准的 next-token prediction loss 进行端到端联合优化。

---

### 评估指标
使用多个自动评价指标衡量生成质量：
- **ROUGE-1 / ROUGE-L**：评估 n-gram 匹配和最长公共子序列
- **BLEU**：衡量词汇精确匹配
- **METEOR**：考虑同义词和词形变化
- **BERTScore**：基于语义相似性的评分

此外还进行了：
- **LLM-based evaluation（GPT-4o）**：人工打分模拟
- **Position-sensitive evaluation**：分析不同生成位置的表现
- **Difficulty-aware evaluation**：按难度分层测试性能

---

### 基线方法对比
分为两类：

#### （1）无需微调的方法（Tuning-free）
- **Non-pers**：无个性化
- **RAG**：检索增强生成
- **CoS**：Context Steering

#### （2）需要微调的方法（Tuning-based）
- **SFT**：监督微调（强基线）
- **LLM-TRSR**：基于摘要的训练
- **NextQuill**：因果偏好建模
- **CoT**：Chain-of-Thought 推理
- **Coconut**：latent reasoning（think-then-generate 范式）

---

## 3. 主要实验结果和性能指标

### 关键性能数据（见 Table 1）

| 方法 | Product Review (BLEU) | Abstract Gen. (BLEU) | Topic Writing (BLEU) |
|------|------------------------|------------------------|------------------------|
| SFT | 3.91 | 5.82 | 3.89 |
| CoT | 3.37 | 5.85 | 3.00 |
| Coconut | 3.32 | 5.24 | 3.07 |
| **FlyThinker** | **4.36** (+11.5%) | **6.34** (+8.8%) | **4.06** (+4.4%) |

> FlyThinker 在所有任务上均优于最强基线 SFT 和其他推理方法，在 BLEU 上平均提升约 10%，且在 ROUGE 系列指标上也全面领先。

---

### 与基线方法的对比结果
- **Obs 1**: FlyThinker 在所有任务上 consistently 超过 SFT 和其他 tuning-based 方法；
- **Obs 2**: 在 Abstract Generation 上表现尤为突出，说明其在长序列连贯性和相关性维护方面更强；
- **Obs 3**: 显著优于 Coconut 等 latent reasoning 方法，证明 **interleaved reasoning 更有效**。

---

### 消融实验结果（Ablation Study）

#### （1）Reasoner 规模的影响（Figure 5 & Table 9）
| Reasoner Size | 性能趋势 |
|---------------|---------|
| 3B → 1.5B | 几乎无损，ROUGE/BLEU 维持高位 |
| 1.5B → 0.5B | 明显下降，尤其在 ROUGE-L 和 BLEU 上 |

👉 结论：适度缩小 Reasoner（如 1.5B）可在不牺牲性能的前提下大幅提升效率。

#### （2）融合系数 λ 的敏感性分析（Figure 6 & Table 10）
- 最佳范围：**λ ∈ [0.5, 2.0]**
- λ 过小 → reasoning 信号弱化
- λ 过大 → 生成不稳定
- 在合理范围内性能稳定，表明方法鲁棒性强

#### （3）Latent Reasoning 注入位置（Table 2）
| 配置 | 性能 |
|------|------|
| Input-only | 多样性高但对齐差（ROUGE↓） |
| Output-only | 缺乏上下文 grounding，性能弱 |
| **Global enhancement（输入+输出）** | ✅ 最优，兼顾对齐与表达力 |

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **“think-while-generating” 范式优于 “think-then-generate”**
   - 动态、逐 token 的 reasoning 更适合处理长文本中不断演变的内容和用户意图。
   
2. ✅ **FlyThinker 显著缓解“context drift”问题**
   - 如 Figure 4 所示，在生成后期（[200,300] token 区间），FlyThinker 仍能维持高质量个性化输出，而 SFT、CoT 等方法明显退化。

3. ✅ **训练与推理效率俱佳**
   - 训练时间接近 SFT，远快于 CoT/Coconut；
   - 推理延迟几乎与 SFT 相当，具备实际部署潜力。

4. ✅ **Reasoner 可轻量化**
   - 使用 1.5B 或更小的 Reasoner 即可获得良好性能，适合资源受限场景。

5. ✅ **跨架构泛化能力强**
   - 在 Qwen 和 Gemma 等不同 backbone 上均取得一致增益。

---

### 方法的局限性
- **增加内存占用**：由于引入额外的 Reasoner 模型，总参数量和显存消耗上升；
- **依赖高质量历史数据**：若用户历史交互稀疏或噪声多，可能影响 Reasoner 对偏好的准确建模；
- **latent reasoning 可解释性有限**：虽可通过 t-SNE 可视化发现不同用户有不同的 reasoning 轨迹（Appendix H），但具体语义仍不够透明。

---

### 未来工作方向
1. **探索更轻量的 Reasoner 架构**，例如 MoE 或蒸馏方案；
2. **结合 RLHF 或 DPO** 进一步优化偏好对齐目标；
3. **扩展至多模态个性化生成** 场景（如图文生成）；
4. **提升 latent reasoning 的可解释性与可控性**，便于人工干预与调试。

---

> **总结一句话**：FlyThinker 通过创新的 **parallel think-while-generating** 架构，在保证训练与推理效率的同时，实现了更精准、更持久的个性化长文本生成，是迈向真正用户中心化 LLM 的重要一步。

</details>

---

### 7. [Revolutionizing Mixed Precision Quantization: Towards Training-free Automatic Proxy Discovery via Large Language Models](https://arxiv.org/abs/2512.07419)

**Authors**: Haidong Kang, Jun Du, Lihong Lin  
**Category**: cs.LG  
**Published**: 2025-12-09  
**Score**: 10.0  
**Type**: new  
**ArXiv ID**: 2512.07419v1  

#### Abstract
Mixed-Precision Quantization (MPQ) liberates the Deep Neural Networks (DNNs) from the Out-Of-Memory (OOM) bottleneck, which garnered increasing research attention. However, conventional methods either searched from costly differentiable optimization, which is neither efficient nor flexible, or learn...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Revolutionizing Mixed Precision Quantization: Towards Training-free Automatic Proxy Discovery via Large Language Models

---

## 1. 论文的主要贡献和创新点

### 解决的问题
传统的 **Mixed-Precision Quantization (MPQ)** 方法存在两大瓶颈：
- **依赖专家设计的代理（proxy）规则**：如 HAWQ 使用 Hessian 分析、OMPQ 手动选择权重/激活统计量，这些需要大量领域知识和试错成本；
- **高计算开销或强校准数据依赖**：可微搜索方法（如 EdMIPS）训练昂贵；训练免费方法仍需数千样本和多次迭代（如 HAWQ-V2 需 8,192 校准样本 + 50 轮优化）。

该论文提出：**能否在无需人工干预和模型训练的前提下，自动发现高质量的 MPQ 代理？**

### 提出的新方法与新思路
作者提出了 **TAP（Training-free Automatic Proxy）** 框架，其核心是：
- 利用 **Large Language Models (LLMs)** 自动生成适用于 MPQ 的零成本量化代理（proxy），完全摆脱手工规则；
- 引入基于 **Direct Preference Optimization (DPO)** 的轻量级强化学习机制，通过反馈信号优化 LLM 的提示（prompt），实现“推理-评估-改进”闭环。

#### TAP 的三大组件：
1. **Proxy Candidate Generator**：由 LLM 构建，生成包含自然语言逻辑、可执行代码和位宽分配策略的候选代理。
2. **Fitness Evaluator**：基于 Spearman 相关系数衡量代理预测敏感度与真实量化误差的一致性，并结合 Top-1 准确率进行综合评分。
3. **DPO Evolution Scheduler**：利用偏好对（preferred vs. dispreferred proxy）更新 LLM 的 prompt 生成策略，形成正向反馈循环。

### 相比现有方法的优势
| 维度 | 传统方法（如 HAWQ, OMPQ） | TAP |
|------|--------------------------|-----|
| 是否需要专家参与 | 是（依赖手工规则） | 否（全自动） |
| 是否需要训练 | 否（但需调优） | 否 |
| 校准数据量 | 大（64~8192 样本） | 极小（仅 16 样本） |
| 收敛速度 | 慢（2500+ 更新 / 50 轮） | 快（5 轮内收敛） |
| 泛化能力 | 弱（任务特定） | 强（跨架构、跨数据集迁移） |
| 搜索效率 | 中到高（GPU 小时级） | 极高（μs 级推理） |

> ✅ **TAP 实现了真正意义上的“zero-cost + zero-human”代理发现范式转变。**

---

## 2. 核心实验方法和设置

### 使用的数据集
- **主基准**：ImageNet-1k（用于量化性能评估）
- **代理搜索数据集**：CIFAR-10（用于 TAP-C 的 proxy 发现）
- **其他验证数据集**：CIFAR-10, PASCAL VOC 2007, MS COCO 2017
- **测试模型覆盖广泛架构**：
  - CNNs: ResNet-18/50, MobileNetV2
  - Transformers: ViT-B, DeiT-B, Swin-B

### 实验设置与评估指标
| 类别 | 设置说明 |
|------|--------|
| **硬件平台** | NVIDIA 3090 GPU（24GB） |
| **量化方式** | Mixed-Precision Quantization（W/A 不同位宽） |
| **目标压缩率** | 参数压缩率达 ~80%-90% |
| **校准样本数** | TAP-C 仅用 16 样本；对比方法用 64~8192 |
| **评估指标** | 
| - Top-1 Accuracy (%) | 主要精度指标 |
| - #Params (M) | 模型参数量 |
| - Cost. (GPU hours) | 搜索耗时（体现效率） |
| - Comp. (%) | 压缩比率 |
| - BOPs (G) | 运算量 |
| - Runtime (s) | 推理延迟（表7） |

### 基线方法对比
涵盖多种主流 MPQ 方法：
- **Fixed-precision**: PACT, LSQ
- **RL-based**: HAQ
- **Sensitivity-based**: HAWQ, HAWQ-V3
- **Differentiable**: DNAS, EdMIPS, FracBits-SAT, GMPQ
- **Training-free**: OMPQ, EMQ
- **Transformer-specific PTQ**: FQ-ViT, APQ-ViT, OMSE 等

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 3–6）

#### 在 ResNet-18 上的表现（ImageNet）
| 方法 | Top-1 (%) | Bit (W/A) | Cost. (GPU h) |
|------|-----------|-----------|---------------|
| Full-precision | 73.09 | 32/32 | — |
| OMPQ | 72.08 | */5 | 0.45 |
| EMQ | 72.28 | */6 | 0.51 |
| **TAP-C** | **72.93** | ***/8** | **9.17×10⁻⁶** |

> ⬆️ **超越所有 baseline，且搜索时间快约 50,000 倍！**

#### 在 ResNet-50 上的表现
| 方法 | Top-1 (%) | Bit (W/A) | Cost. (GPU h) |
|------|-----------|-----------|---------------|
| Full-precision | 77.72 | — | — |
| EMQ | 76.70 | */6 | 0.52 |
| **TAP-C** | **76.81** | ***/8** | **8.75×10⁻⁶** |

> ✅ 达到 SOTA 精度，同时几乎为“瞬时搜索”。

#### Post-Training Quantization 结果（ResNet-18 & MobileNetV2）
| 方法 | Model | Top-1 (%) | Data (#samples) |
|------|-------|------------|------------------|
| OMPQ | ResNet-18 | 69.41 | 64 |
| EMQ | ResNet-18 | 69.92 | 64 |
| **TAP-C** | **ResNet-18** | **70.26** | **16** |
| OMPQ | MobileNetV2 | 69.62 | 32 |
| EMQ | MobileNetV2 | 70.75 | 64 |
| **TAP-C** | **MobileNetV2** | **71.81** | **16** |

> 🔥 **以更少校准数据实现更高精度，展现极强数据效率。**

#### Transformer 架构上的泛化能力（ViT-B / DeiT-B / Swin-B）
| 方法 | Comp. Ratio | ViT-B | DeiT-B | Swin-B |
|------|-------------|-------|--------|--------|
| APQ-ViT | 81.25% | 82.21 | 80.42 | 84.18 |
| OMSE | 75% | 73.39 | 79.57 | 48.55 |
| **TAP-C** | **82%** | **83.56** | **81.24** | **83.79** |

> 🚀 **首次证明 LLM-driven proxy 可无缝迁移到复杂 Transformer 架构，在高压缩下保持领先精度。**

### 消融实验结果

#### (1) 超参数 α 的影响（Table 8）
- α 控制 `sensitivity score` 与 `quantization accuracy` 的权衡。
- 实验表明：TAP 在不同 α 下表现稳定（如 ResNet-18 上波动 <1.5%），说明其鲁棒性强。

#### (2) 不同 LLM 的影响（Table 9）
| LLM | Average Top-1 (%) |
|------|--------------------|
| Deepseek-chat | 71.44 |
| Qwen3-max | 71.35 |
| Grok 3 | 71.01 |

> ✅ 性能高度一致，表明 TAP 对底层 LLM 的推理风格具有强适应性和稳定性。

#### (3) 效率分析（Table 7）
| 步骤 | 平均耗时 (s) |
|------|--------------|
| Proxy Generation | 0.0133 |
| Bit Allocation | 0.0645 |
| **Total** | **< 0.1 s** |

> 💡 完整量化流程可在 **100ms 内完成**，适合边缘部署场景。

---

## 4. 关键结论和发现

### 主要发现
1. **LLMs 可有效替代人类专家进行 MPQ 代理设计**：TAP 首次实现了无需人工干预的自动化 proxy 发现。
2. **DPO 显著提升 LLM 推理质量**：通过构建“任务感知”的反馈机制，使 LLM 逐步生成更优的 Chain-of-Thought 式推理路径。
3. **TAP 具备卓越的泛化能力**：
   - 跨网络架构（CNN → Transformer）
   - 跨数据集（CIFAR-10 → ImageNet）
   - 跨任务类型（QAT → PTQ）
4. **极致高效**：搜索成本降至 μs 级，远低于现有任何可微或进化方法。

### 方法的局限性（见 Appendix E）
- 当前 TAP 依赖于预定义的输入输出契约（IO contract），尚不能处理极端非结构化任务；
- 对非常小的 LLM（<7B）效果下降明显，提示需要一定规模的语言模型支持；
- 暂未探索动态输入长度或复杂约束下的多目标优化。

### 未来工作方向
- 扩展至更多硬件后端（如 NPU、MCU）定制化 proxy 发现；
- 探索多模态 LLM 在视觉-硬件联合建模中的潜力；
- 将 TAP 框架推广到其他神经网络压缩任务（如剪枝、蒸馏）；
- 开发开源工具链，推动 LLM-driven 设计算法生态发展。

---

> ✅ **总结一句话**：  
> **TAP 开创了 LLM-driven 自动化模型压缩新范式——用大模型“思考”如何量化模型，在无需训练、无需专家的情况下，实现高效、精准、通用的混合精度量化。**

</details>

---

### 8. [UniDiff: A Unified Diffusion Framework for Multimodal Time Series Forecasting](https://arxiv.org/abs/2512.07184)

**Authors**: Da Zhang, Bingyu Li, Zhuyuan Zhao, Junyu Gao, Feiping Nie, Xuelong Li  
**Category**: cs.LG  
**Published**: 2025-12-09  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2512.07184v1  

#### Abstract
As multimodal data proliferates across diverse real-world applications, leveraging heterogeneous information such as texts and timestamps for accurate time series forecasting (TSF) has become a critical challenge. While diffusion models demonstrate exceptional performance in generation tasks, their ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：UniDiff: A Unified Diffusion Framework for Multimodal Time Series Forecasting

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

当前的时间序列预测（TSF）模型大多局限于处理单一模态的数值序列，难以有效利用现实应用中广泛存在的**多模态异构信息**（如文本描述、时间戳等）。尽管扩散模型（Diffusion Models）在生成任务中表现出色，但在多模态TSF中的应用仍处于初级阶段，普遍存在以下问题：

- 多模态融合策略简单（如特征拼接），无法捕捉动态跨模态交互；
- 条件控制机制（如Classifier-Free Guidance, CFG）仅支持单模态引导，缺乏对多源条件的独立调控能力；
- 忽视外部事件（如新闻、报告）对时间序列突变的影响。

### **提出了什么新方法或新思路**

本文提出 **UniDiff** —— 一个统一的扩散框架，用于**多模态时间序列预测**，其核心创新包括：

#### ✅ 统一并行融合模块（Unified Parallel Fusion Module）
- 将时间序列划分为patch，并通过轻量级MLP嵌入为`Z`；
- 时间戳（Timestamps）和文本（Text）分别编码为`t`和`d`；
- 在每一层融合模块中，采用**单一交叉注意力机制**（cross-attention）实现三者的一体化融合：
  - Query来自时间序列嵌入 `Z`
  - Key 和 Value 来自 `[t; d]` 的拼接
- 实现了**一步完成、并行动态加权**的多模态融合，避免了串行处理带来的信息瓶颈。

#### ✅ 面向多源条件的解耦式Classifier-Free Guidance（Decoupled CFG）
- 提出一种新型CFG机制，允许在推理时**独立调节文本和时间戳的引导强度**（`wt` 和 `wd`）；
- 训练时随机丢弃文本或时间戳条件（使用null token），使模型能学习到部分无条件下的预测能力；
- 推理公式如下：
  $$
  \hat{y}_k^{\text{guided}} = y_k + w_t(y_k - y_{k,\text{no-T}}) + w_d(y_k - y_{k,\text{no-D}})
  $$
- 显著提升了模型鲁棒性和可控性。

#### ✅ 完整的多模态扩散架构设计
- 包含三个核心组件：
  1. **Modality-specific Encoders**：分别处理数值序列、时间戳、文本；
  2. **Unified Fusion Module**：实现高效跨模态交互；
  3. **Prediction Head**：通过可学习权重融合各模态预测输出。

### **相比现有方法的优势**

| 方面 | 传统方法局限 | UniDiff优势 |
|------|--------------|------------|
| 融合方式 | 拼接、串行融合（rigid pipeline） | 并行、自适应融合（adaptive weighting） |
| 扩散条件控制 | 单一CFG权重，无法区分模态重要性 | 解耦式CFG，独立控制每种模态影响 |
| 数值保真度 | LLM-centric方法将数值转为文本，损失精度 | 保留原始数值结构，不进行格式转换 |
| 不确定性建模 | 多数为确定性预测 | 基于扩散模型，天然支持概率预测 |

---

## 2. 核心实验方法和设置

### **使用的数据集**

所有实验基于 **Time-MMD** 数据集，这是目前最全面的多领域多模态时间序列基准，涵盖8个真实世界领域：

| Dataset | Domain | Frequency | Length | Dim |
|--------|--------|----------|--------|-----|
| Agriculture | 农业 | Monthly | 496 | 1 |
| Climate | 气候 | Monthly | 496 | 5 |
| Economy | 经济 | Monthly | 423 | 3 |
| Energy | 能源 | Weekly | 1,479 | 9 |
| Environment | 环境 | Daily | 11,102 | 4 |
| Health | 健康 | Weekly | 1,389 | 11 |
| Social Good | 社会公益 | Monthly | 900 | 1 |
| Traffic | 交通 | Monthly | 531 | 1 |

> 文本信息来源于历史时间段内的相关报告、新闻等。

### **实验设置与评估指标**

- **任务类型**：多步预测（forecasting horizon: 6, 8, 10, 12, 24, 36, 48, 96）
- **预处理**：
  - Lookback窗口根据频率设定；
  - 数值归一化（zero-mean, unit-variance）；
  - 文本输入为前36个区间的报告拼接。
- **模型参数**：
  - Patch大小：P=16，Stride=8；
  - 使用冻结的BERT-base作为Text Encoder；
  - Fusion Layers: L=6，d_model=512，8-head attention；
  - Diffusion Steps: K=200，噪声调度为quadratic；
  - 推理使用DDIM采样器；
  - 默认CFG权重：`wt=0.5`, `wd=0.8`；
  - 训练时drop概率：`puncond_T = puncond_D = 0.1`

- **评估指标**：
  - **MSE**: Mean Squared Error
  - **MAE**: Mean Absolute Error
  - 所有结果取三次运行平均值。

### **基线方法对比**

共比较四大类SOTA模型：

| 类别 | 方法 | 简要说明 |
|------|------|---------|
| **LLM/VLM-based** | CALF, Time-VLM, GPT4MTS | 利用大语言/视觉语言模型增强TSF |
| **Transformer Foundation Models** | Sundial, ROSE, MOIRAI | 专为TS设计的基础模型 |
| **CV-inspired Methods** | VisionTS | 将TS视为图像进行修复预测 |
| **Generative/Diffusion Models** | MCD-TSF, CSDI | 基于扩散的概率预测模型 |

---

## 3. 主要实验结果和性能指标

### **关键性能数据（Table II摘要）**

在**8个真实数据集上平均表现**：

| Model | Avg MSE ↓ | Avg MAE ↓ |
|-------|-----------|-----------|
| **UniDiff (Ours)** | **0.213** | **0.294** |
| MCD-TSF (diffusion baseline) | 0.222 | 0.322 |
| Sundial | 0.373 | 0.392 |
| ROSE | 0.290 | 0.336 |
| GPT4MTS | 0.225 | 0.298 |
| CSDI (unimodal diffusion) | 2.096 | 1.048 |

> ✅ UniDiff在绝大多数数据集上取得**SOTA性能**，尤其在Agriculture、Energy、Traffic等领域领先显著。

### **与基线方法的对比结果**

- 相比最强的多模态扩散模型 **MCD-TSF**：
  - MSE降低约 **4%**，MAE降低约 **8.6%**
  - 表明**统一并行融合 + 解耦CFG**优于简单的条件拼接
- 相比LLM-based方法（如GPT4MTS）：
  - 总体更稳定，在非经济类数据上优势明显
  - 验证了“**不破坏数值结构**”的设计合理性
- 相比纯数值扩散模型 **CSDI**：
  - MSE下降超过 **90%**
  - 强调了**多模态信息整合的重要性**

### **消融实验结果（Table III）**

| 变体 | Avg MSE | Avg MAE | 分析 |
|------|--------|--------|------|
| **Full Model (UniDiff)** | **0.213** | **0.294** | 基准 |
| w/o Text | 0.347 | 0.394 | 性能大幅下降，说明**文本语义至关重要** |
| w/o Timestamp | 0.289 | 0.335 | 下降明显，体现**时间结构的价值** |
| w/o Both (Unimodal) | 2.096 | 1.048 | 错误飙升，证明**多模态必要性** |
| Sequential Fusion | 0.222 | 0.322 | 差于并行融合，验证**并行设计优越性** |
| Simple Fusion (concat) | 0.290 | 0.336 | 远差于cross-attention，说明**高级融合机制关键** |
| Coupled CFG | 0.238 | 0.312 | 差于解耦CFG，表明**独立控制更优** |

> 🔍 **定量分析结论**：每个组件都对最终性能有显著贡献，尤其是多模态输入和解耦CFG。

---

## 4. 关键结论和发现

### **主要发现**

1. **多模态信息显著提升预测准确性**  
   特别是当突发事件发生时（如流感爆发），文本提供的上下文信息能够帮助模型提前感知异常。

2. **并行融合机制优于串行或拼接方式**  
   cross-attention能动态分配不同模态的注意力权重，适应不同的预测场景（event-driven vs pattern-driven）。

3. **解耦式CFG提供更强的控制力与鲁棒性**  
   允许在推理时灵活调整文本/时间戳的影响程度，防止噪声文本误导预测。

4. **UniDiff兼具高精度与高效率**  
   如图5所示，UniDiff在保持最低误差的同时，计算复杂度（MACs）、内存占用和推理速度均优于多数SOTA模型。

5. **时间戳权重 λ 分析显示其积极作用**  
   当 `λ ∈ [0.8, 1.0]` 时性能最佳，过高反而轻微下降，说明需平衡各模态贡献。

### **方法的局限性**

- 当前仅整合了**文本 + 时间戳 + 数值序列**三种模态，尚未扩展至图像、音频等其他形式；
- 文本编码依赖预训练BERT，可能受限于领域适配性；
- 模型训练需要大量多模态对齐数据，在某些领域获取成本较高。

### **未来工作方向**

- 扩展至更多模态（如图像、语音）；
- 探索更高效的轻量化版本以适用于边缘设备；
- 结合因果推理机制，提升对外部干预的解释能力；
- 应用于实时决策系统（如金融交易、智能交通）。

---

## 总结

✅ **UniDiff 是首个将扩散模型与多模态时间序列预测深度结合的统一框架**，通过**并行交叉注意力融合**和**解耦式CFG机制**，实现了对文本、时间戳和数值序列的高效协同建模。实验证明其在多个真实世界数据集上达到SOTA水平，且具备良好的可解释性与实用性，为下一代智能预测系统提供了强有力的范式支持。

</details>

---

### 9. [GatedFWA: Linear Flash Windowed Attention with Gated Associative Memory](https://arxiv.org/abs/2512.07782)

**Authors**: Jiaxu Liu, Yuhe Bai, Christos-Savvas Bouganis  
**Category**: cs.LG  
**Published**: 2025-12-09  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2512.07782v1  

#### Abstract
Modern autoregressive models rely on attention, yet the Softmax full attention in Transformers scales quadratically with sequence length. Sliding Window Attention (SWA) achieves linear-time encoding/decoding by constraining the attention pattern, but under an \textit{Associative Memory} interpretati...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：GatedFWA: Linear Flash Windowed Attention with Gated Associative Memory

---

## 1. 论文的主要贡献和创新点

### 解决的问题
现代自回归模型（如Transformer）依赖于 **Softmax full attention**，其计算复杂度随序列长度呈**二次方增长**（$O(N^2)$），严重限制了长序列建模的效率。为缓解此问题，**Sliding Window Attention (SWA)** 被提出以实现线性时间解码（$O(Nw)$），但作者通过**关联记忆（Associative Memory）视角**分析发现：

- **Softmax Attention**：由于归一化机制，每步的记忆更新被不断缩小，导致**梯度消失（gradient vanishing）**。
- **SWA**：采用差分式更新（difference-style update），其优化目标在数学上是**无界的（unbounded）**，容易引发**梯度不稳定（gradient instability）** 和记忆爆炸。

因此，论文旨在设计一种既能保持 SWA 的线性效率，又能稳定记忆更新、控制梯度流的新注意力机制。

---

### 提出的新方法：GatedFWA
作者提出了 **GatedFWA**（Gated Flash Windowed Attention），一种基于**门控关联记忆**的线性注意力机制，核心思想如下：

- **门控衰减机制（Memory-Gated Decay）**：
  - 为每个 token 和 attention head 引入一个轻量级的非负门控（non-negative gate）。
  - 该门控沿序列累积形成一个**衰减偏置（decay bias）**，并注入到 attention logits 中。
  - 在关联记忆框架下，这相当于对记忆状态施加了一个**可学习的收缩操作（learnable contraction）**，即 $M_t \leftarrow \exp(-\alpha) M_{t-1} + \cdots$，从而软性地遗忘无关历史。

- **硬件对齐设计（Hardware-Aligned Implementation）**：
  - **融合预处理（Fused One-Pass Preprocessing）**：通过一次扫描完成门控计算及其前缀和，避免中间张量存储，提升 I/O 效率。
  - **FlashAttention 兼容内核**：将门控偏置在滑动窗口掩码下注入 FlashAttention 流水线，保持数值稳定性与高吞吐。

---

### 相比现有方法的优势
| 特性 | Softmax | SWA | GatedFWA |
|------|--------|-----|---------|
| 时间复杂度 | $O(N^2)$ | $O(Nw)$ | $O(Nw)$ ✅ |
| 内存复杂度 | $O(N^2)$ | $O(Nd + Nw)$ | $O(Nd + Nw)$ ✅ |
| 梯度稳定性 | ❌ 梯度消失 | ❌ 梯度不稳定 | ✅ 可控梯度流 |
| 长程依赖保留 | ⚠️ 归一化削弱 | ⚠️ 差分放大噪声 | ✅ 门控选择性保留 |
| 硬件友好性 | ✅（FA优化后） | ✅ | ✅（兼容FA） |

此外，GatedFWA 可作为 **NSA（Native Sparse Attention）等压缩/选择方法中的局部模块**无缝替换 SWA，进一步增强全局上下文利用能力。

---

## 2. 核心实验方法和设置

### 数据集
- **语言建模基准**：
  - **WikiText103**：标准语言建模数据集。
  - **OpenWebText**：大规模开放网页文本，用于训练与评估。
- **下游任务评估**：
  - 包括 **PiQA, HellaSwag, WinoGrande, ARC-e/c, COPA, OpenBookQA, SciQA, BoolQ** 等常识推理与问答任务。
- **召回密集型任务**：
  - **MQAR（Multi-Query Associative Recall）**：测试模型对键值对记忆与检索的能力。

---

### 实验设置与评估指标
- **模型规模**：120M 和 360M 参数量。
- **上下文长度**：1024 到 4096。
- **评估指标**：
  - **语言建模**：验证集损失（Validation Loss ↓）
  - **下游任务**：准确率（Accuracy ↑），部分任务使用归一化准确率（acc_norm）
  - **效率对比**：前向/反向传播时间、吞吐量（tokens/sec）、预处理开销
- **实现细节**：
  - 所有模型均基于 LLaMA 架构修改。
  - 使用 Triton 实现高效内核，支持 FlashAttention 风格融合。
  - 对比方法包括：RetNet, Mamba, RWKV, GLA, Transformer (LLaMA), SWA, SWA+NSA 等。

---

### 基线方法对比
| 基线方法 | 类型 | 是否线性 |
|--------|------|----------|
| Softmax (FA) | 全局注意力 | 否 |
| SWA | 局部滑窗 | 是 |
| SWA + NSA | 滑窗 + 压缩选择 | 是 |
| Mamba / GLA / RWKV | SSM 或线性注意力 | 是 |
| RetNet | 循环增强注意力 | 是 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据

#### （1）语言建模损失（Tab. 1 & Fig. 5）
在 OpenWebText 上训练，GatedFWA 显著优于各类基线：

| 架构 | #Param (M) | N=1024 Val Loss | N=4096 Val Loss |
|------|------------|------------------|------------------|
| Transformer (LLaMA) | 124.4 | 3.247 | 3.273 |
| + SWA | 124.4 | 3.248 | 3.274 |
| + SWA + NSA | 125.4 | 3.240 | 3.248 |
| **+ GatedFWA** | **125.1** | **3.237** | **3.255** |
| **+ GatedFWA + NSA** | **126.1** | **3.215** | **3.230** |

> ✅ 在长上下文（N=4096）下，GatedFWA + NSA 将验证损失从 3.273 降至 **2.842**（360M 模型），显著领先。

#### （2）下游任务表现（Tab. 2）
在多个常识推理任务上，GatedFWA 模型平均准确率提升明显：

| 架构 | 平均得分 ↑ |
|------|-----------|
| Transformer (LLaMA) | 50.41 |
| + SWA | 50.28 |
| + SWA + NSA | 51.03 |
| **+ GatedFWA** | **50.98** |
| **+ GatedFWA + NSA** | **51.65** |

> ✅ 相较于基础 Transformer，GatedFWA+NSA 实现 **2.45% 的相对提升**。

#### （3）MQAR 回调率（Fig. 8）
在不同序列长度（128–512）和维度（64–512）下的多查询回忆任务中：
- GatedFWA 在所有配置下均优于 SWA 和多种 SSM（如 Mamba, RWKV）。
- 即使在小模型（d=64）和长序列（N=512）下仍保持高 recall，表明其更强的记忆保持能力。

#### （4）效率与吞吐量（Fig. 10）
- **前向/反向速度**：GatedFWA 与 SWA 接近，在 N > 64K 时比 FA 快约 **30×**。
- **预处理开销**：在 N=64K 下，GatedFWA 的门控预处理仅需 **0.3ms**，远低于 PyTorch 基线（2.9ms）。
- **吞吐量**：与 SWA+NSA 相当，且比 FA 高出 **3.1–5.4×**。

#### （5）消融实验（Fig. 9）
- 固定 amplitude $\beta=1$ vs. 可学习 $\beta$：
  - 可学习 $\beta$ 在 GatedFWA 和 GatedFWA+NSA 上均带来更优收敛与更低损失。
  - 表明 amplitude 控制门控切换锐度，有助于适应不同 token 的注意力跨度。

---

## 4. 关键结论和发现

### 主要发现
1. **从关联记忆视角看，Softmax 和 SWA 分别存在梯度消失与不稳定的本质缺陷**。
2. **GatedFWA 通过引入可学习门控衰减，实现了对记忆更新的“软擦除”**，既防止了 SWA 的无界增长，又避免了 Softmax 的过度抑制。
3. **门控机制使得梯度路径可控**：模型可学习在重要位置保留梯度，在无关位置切断，增强了信用分配（credit assignment）。
4. **GatedFWA 完全兼容 FlashAttention 和 NSA 等稀疏架构**，可在不增加复杂度的前提下提升性能。
5. **实验表明 GatedFWA 在语言建模、常识推理、长程回忆等任务上全面超越主流线性与二次注意力方法**。

---

### 方法的局限性
- **电路复杂度限制在 TC⁰**：尽管引入了数据依赖门控，但由于其更新仍是**对角矩阵乘法**（diagonal gating），无法解决需要非交换操作的 NC¹ 完全问题（如 S₅ 置换追踪）。
- **表达力受限于写入-only 记忆机制**：缺乏真正的读-写交互（read-write memory），难以模拟复杂的状态转移。

---

### 未来工作方向
- 探索结合 **Delta Rule** 等读写记忆机制，提升至 NC¹ 复杂度类，支持更复杂的顺序依赖建模。
- 设计支持非对角、非交换状态转移的高效内核，突破当前并行化限制。
- 将 GatedFWA 扩展至视觉-语言、事件流等多模态自回归场景。

---

> **总结**：GatedFWA 是一种兼具**理论洞察**（从关联记忆解释梯度问题）与**工程实践价值**（FlashAttention 兼容、低开销）的线性注意力新范式，在保持 SWA 效率的同时显著提升了训练稳定性与长程建模能力，是当前高效 LLM 架构中的有力候选方案。

</details>

---

### 10. [Utilizing Multi-Agent Reinforcement Learning with Encoder-Decoder Architecture Agents to Identify Optimal Resection Location in Glioblastoma Multiforme Patients](https://arxiv.org/abs/2512.06990)

**Authors**: Krishna Arun, Moinak Bhattachrya, Paras Goel  
**Category**: cs.AI  
**Published**: 2025-12-09  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2512.06990v1  

#### Abstract
Currently, there is a noticeable lack of AI in the medical field to support doctors in treating heterogenous brain tumors such as Glioblastoma Multiforme (GBM), the deadliest human cancer in the world with a five-year survival rate of just 5.1%. This project develops an AI system offering the only e...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文核心结论与实验结果总结

## 1. 论文的主要贡献和创新点

### 解决的问题
该研究旨在解决**胶质母细胞瘤（Glioblastoma Multiforme, GBM）诊断与治疗规划中缺乏高效、端到端AI系统支持**的临床痛点。具体包括：
- **诊断阶段**：传统大型模型（如ViT-L/16）参数量巨大（>3亿），计算成本高昂（约$102,420），难以在资源受限地区部署。
- **治疗预测阶段**：现有物理驱动模型（如PDE、Lattice Boltzmann）需数十至数百小时模拟肿瘤生长，且无法建模手术、放疗、化疗等干预措施的影响，临床实用性低。
- **个性化治疗规划缺失**：缺乏能够联合优化手术切除位置并动态反馈调整的智能系统。

### 提出的新方法与思路
提出名为 **Brainstorm** 的首个**端到端多智能体强化学习（MARL）框架**，涵盖诊断与治疗两大阶段：

#### （1）诊断阶段：基于序列决策的轻量化分类链
- 构建由 **5个小型CNN/SVM模型组成的级联分类流水线**，逐步缩小疾病范围：
  1. Mass Detection CNN → 是否存在异常质量
  2. Tumor Confirmation CNN → 是否为肿瘤
  3. nnU-Net Segmentation → 分割水肿、增强肿瘤、核心区域
  4. SVM Malignancy Classifier → 输入30项定量radiomic特征判断良恶性
  5. Multi-class CNN → 最终分类为GBM、Meningioma、Schwannoma、Neurocytoma或其他
- 创新性地采用“分而治之”策略，避免单一巨型模型。

#### （2）治疗规划阶段：生成式MARL闭环优化系统
- 引入三个图像到图像（image-to-image）生成模型依次模拟治疗过程：
  1. **Resection Model**：基于Diffusion Model预测术后MRI
  2. **Radiotherapy Model**：提出一种新型**Spatio-Temporal Vision Transformer**架构，预测多时间点放疗后肿瘤演变
  3. **Chemotherapy Model**：另一个Diffusion Model生成化疗后的MRI
- 使用一个**CNN生存率计算器**评估生成的post-treatment MRI对应的预估生存天数
- 若预测生存率未达到医生设定目标±15%，则通过**Proximal Policy Optimization (PPO)** 反馈循环迭代搜索更优的切除方案

#### （3）数据增强创新
- 在预处理中引入**贴近真实医疗场景的数据增强技术**，如：
  - Random bias field（模拟磁场不均）
  - Elastic deformation（模拟术中脑移位）
  - Gaussian noise（模拟老旧设备低分辨率成像）
- 显著提升模型在非理想影像条件下的泛化能力。

### 相比现有方法的优势
| 维度 | Brainstorm优势 |
|------|----------------|
| **计算效率** | 诊断模型总参数仅~20.9M，较ViT-L/16减少22.28倍计算成本 |
| **推理速度** | 单次治疗预测仅需9.7秒，远快于PDE类模型（31–225小时） |
| **临床实用性** | 支持完整治疗周期建模（手术+放疗+化疗），而非无干预自然进展 |
| **可及性** | 轻量化设计使其可在农村/资源匮乏医院部署 |

---

## 2. 核心实验方法和设置

### 数据集
从The Cancer Imaging Archive (TCIA) 整合了来自7个权威数据库的共 **6,560例患者T1CE MRI扫描及定量radiomic特征**，主要包括：
- **BraTS dataset**（多中心脑瘤分割挑战赛数据）
- **ReMIND dataset**（肿瘤进展预测专用数据集）
- **Lumiere dataset**（融合多模态成像）

所有MRI标准化为 **64×64×32体素分辨率**，并进行强度归一化与高斯去噪。

### 实验设置
- **训练平台**：Google Colab T4/A100 GPU
- **训练方式**：各模块独立训练，超参数针对任务定制（见Table 1）
- **诊断阶段损失函数**：
  - CNN分类器：Cross Entropy
  - SVM：Hinge Loss
  - nnU-Net分割：Dice + Cross Entropy混合损失
- **治疗阶段损失函数**：全部使用MSE（Mean Squared Error）用于图像重建任务
- **优化器**：
  - CNN分类器 & 生成模型：Adam
  - nnU-Net：SGD（利于细粒度分割任务）

### 评估指标
| 阶段 | 指标 |
|------|------|
| **诊断准确性** | Confusion Matrix（TP/TN/FP/FN）、Accuracy |
| **分割性能** | Dice Score、IoU（Intersection over Union） |
| **生成图像质量** | SSIM（Structural Similarity Index Measure） |
| **系统级比较** | 计算时间、训练成本、五年人生存率提升估算 |

### 基线方法对比
- **诊断模型对比**：
  - ViT-L/16（303M参数）
  - ViT-B/16（87M）
  - R50-ViT-L/16（23.5M）
- **分割模型对比**：
  - BraTS 2023冠军模型（GAN-based）
  - BraTS 2020优胜模型（U-Net-based）
- **肿瘤演化预测模型对比**：
  - Glioma Solver（PDE-based）
  - Lattice Boltzmann方法
  - 其他PDE框架

---

## 3. 主要实验结果和性能指标

### 关键性能数据

#### （1）诊断性能（Table 2 & 结果部分）
| 模型 | 准确率 | 特异性 | 敏感性 |
|------|--------|--------|--------|
| Mass Detection CNN | 94.4% | 96.7% | 92.1% |
| Tumor Confirmation CNN | 91.4% | 90.2% | 92.6% |
| Final Diagnosis CNN | **99.4%** | **99.4%** | **99.4%** |
| Malignancy SVM | 97.5% | 98% | 97% |

> 注：最终多分类准确率达99.4%，显著优于单一大模型。

#### （2）分割性能（Table 3）
| 模型 | Edema Dice | Enhancing Tumor Dice | Tumor Core Dice |
|------|-----------|-----------------------|-----------------|
| **Brainstorm (ours)** | **0.9095** | **0.8795** | **0.8653** |
| BraTS 2023 (GAN) | 0.9005 | 0.8673 | 0.8509 |
| BraTS 2020 (U-Net) | 0.8895 | 0.8506 | 0.8203 |

> 所有子区域分割表现均超越当前最优模型。

#### （3）治疗生成模型SSIM得分
| 模型 | SSIM |
|------|------|
| Resection Diffusion Model | **0.89** |
| Radiotherapy ViT Model | **0.81**（中位数，IQR ±0.03） |
| Chemotherapy Diffusion Model | **0.82** |

> 表明生成MRI与真实图像具有高度结构相似性。

#### （4）系统效率对比（Table 4）
| 模型 | 平均每例耗时 | 模拟时间 |
|------|---------------|----------|
| **Brainstorm** | **9.7秒** | **9.7秒** |
| Glioma Solver | 31小时 | 110.5秒 |
| PDE Frameworks | 83小时 | 300秒 |
| Lattice Boltzmann | 225小时 | 850秒 |

> 推理速度快**上万倍以上**，具备实时应用潜力。

#### （5）成本与精度权衡（Table 5）
| 模型 | 参数量 | 成本 | 准确率 |
|------|--------|------|--------|
| **Brainstorm** | **20.9M** | **~$4,600** | **95.28%** |
| ViT-L/16 | 303.3M | ~$102,420 | 97.08% |
| ViT-B/16 | 87.5M | ~$26,800 | 97.89% |

> Brainstorm以不到4%的成本实现接近顶级模型的性能。

### 与基线方法对比结果
- **计算成本降低22.28×**
- **肿瘤进展推断时间缩短113小时**
- **DICE分数平均提升2.9%**
- **适用于非理想影像环境，在低质量MRI上仍保持鲁棒性**

### 消融实验结果（文中隐含分析）
虽然未明确列出消融表，但以下分析体现了关键组件作用：
- **序列决策框架 vs 单一模型**：大幅降低成本而不牺牲精度
- **真实世界风格增强**：提升了模型对低质量MRI的适应能力，DICE提升2.9%
- **Transformer用于放疗建模**：相比RNN能更好捕捉时空依赖关系，提高长期预测稳定性

---

## 4. 关键结论和发现

### 主要发现
1. **轻量化级联诊断框架可行且高效**：通过多个小模型串联实现高精度诊断，显著降低计算负担，适合边缘部署。
2. **Spatio-Temporal Vision Transformer可有效建模放疗响应**：其自注意力机制优于RNN，能捕捉复杂非线性时空变化。
3. **Diffusion Models在医学图像生成中优于GANs**：尤其在存在噪声和类不平衡情况下更稳定，避免模式崩溃。
4. **MARL闭环系统能自动探索最优切除策略**：结合PPO反馈机制，可根据医生目标动态调整治疗计划。
5. **真实场景增强显著提升泛化能力**：使模型更适合全球多样化医疗环境。

### 方法局限性
- **依赖高质量标注数据**：尤其是术后MRI配对样本用于训练resection diffusion model可能稀缺。
- **放射组学特征需人工输入**：目前仍需医生手动提取30项radiomic特征供SVM使用，尚未完全自动化。
- **未在真实手术环境中验证**：目前为离线评估，尚无前瞻性临床试验验证其实际疗效。
- **个体生物学差异建模有限**：尽管考虑异质性，但仍基于群体统计规律，个性化程度有待深化。

### 未来工作方向
- **扩展至其他局部侵袭性癌症**：如乳腺癌、胰腺癌等同样需要综合治疗的实体瘤。
- **实现全自动化radiomic特征提取**：将SVM前的人工环节替换为端到端CNN特征学习。
- **开展多中心前瞻性临床试验**：验证系统在真实诊疗流程中的有效性与安全性。
- **集成更多治疗模态**：如免疫疗法、靶向药物等新兴手段的建模。
- **开发交互式可视化界面**：便于神经外科医生直观理解AI建议并参与决策。

---

> **社会影响展望（Table 6）**  
> 研究团队咨询多位临床医师后预测：
> - 五年人生存率有望从 **5.1% 提升至 6.0%**（+0.9%）
> - 每年可额外挽救约 **2,250条生命**
> - 全球患者覆盖比例从28.7%提升至34.9%，显著改善医疗公平性

综上所述，**Brainstorm** 是一个兼具**高性能、低成本、强泛化性和临床实用性**的端到端AI系统，为GBM精准治疗提供了全新范式，具有重大转化前景。

</details>

---

### 11. [PMA-Diffusion: A Physics-guided Mask-Aware Diffusion Framework for TSE from Sparse Observations](https://arxiv.org/abs/2512.06183)

**Authors**: Lindong Liu, Zhixiong Jin, Seongjin Choi  
**Category**: cs.LG  
**Published**: 2025-12-09  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2512.06183v1  

#### Abstract
High-resolution highway traffic state information is essential for Intelligent Transportation Systems, but typical traffic data acquired from loop detectors and probe vehicles are often too sparse and noisy to capture the detailed dynamics of traffic flow. We propose PMA-Diffusion, a physics-guided ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：PMA-Diffusion: A Physics-guided Mask-Aware Diffusion Framework for TSE from Sparse Observations

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

交通状态估计（Traffic State Estimation, TSE）面临三大挑战：
1. **训练依赖完整观测**：大多数深度生成模型（如VAE、Diffusion）需要完整的高分辨率速度场进行训练，但在现实中，loop detectors 和 probe vehicles 提供的数据高度稀疏（通常仅5%-25%可见），难以获取全观测数据。
2. **缺乏不确定性建模**：多数方法只输出单一估计值，无法提供完整的后验分布，限制了在决策系统中的可靠性。
3. **物理约束与传感器配置耦合过紧**：现有 Physics-Informed Deep Learning (PIDL) 方法将观测掩码（mask）直接编码进损失函数，导致模型对部署时的传感器布局变化不鲁棒。

### **提出了什么新方法或新思路**

本文提出 **PMA-Diffusion**，一个**物理引导的掩码感知扩散框架**，用于从稀疏观测中重建高速公路速度场。其核心思想是将TSE视为一个**贝叶斯逆问题**，并解耦两个阶段：

- **Mask-Aware Diffusion Prior Training**：直接在稀疏观测上训练扩散先验（diffusion prior），无需完整数据。
  - **Single-Mask Strategy**：按真实传感器覆盖模式进行掩码。
  - **Double-Mask Strategy**：额外引入辅助掩码，即使“始终可见”的位置也偶尔被遮蔽，增强模型对永久缺失区域的泛化能力。

- **Physics-Guided Posterior Sampling**：在推理阶段，通过交替执行以下步骤采样后验分布 $p(V|Y)$：
  1. **Reverse Diffusion Update**：基于学习到的先验去噪。
  2. **Observation Projection**：将观测值精确注入对应位置，保证数据保真度。
  3. **Physics-Guided Projection**：使用自适应各向异性平滑（Adaptive Anisotropic Smoothing, AAS）算子，在未观测区域施加交通波动力学约束（如激波传播方向）。

### **相比现有方法的优势**

| 方面 | 优势 |
|------|------|
| **训练数据要求** | 不需要完整观测数据，可直接用稀疏数据训练，更贴近现实场景。 |
| **灵活性与鲁棒性** | 掩码在推理阶段处理，而非固化在训练损失中，支持任意传感器配置。 |
| **物理一致性** | 物理约束以模块化投影形式加入，不影响先验训练，且可灵活替换或扩展其他物理模块（如守恒律）。 |
| **不确定性量化** | 扩散模型天然支持生成多个合理样本，可用于不确定性分析。 |

---

## 2. 核心实验方法和设置

### **使用的数据集**

- **I-24 MOTION Dataset**：由田纳西州交通部提供的真实高速公路监控数据。
  - 覆盖约4.2英里，276个固定摄像头，提供近乎完整的车辆轨迹。
  - 数据被转换为 **200×200 的欧拉网格**（空间步长 Δx=100 ft，时间步长 Δt=1 s），包含四条车道的速度场。
  - 划分比例：70%训练、10%验证、20%测试。

### **实验设置**

#### **模拟稀疏观测**
- **Loop Detectors**：固定行掩码，分别模拟 5%、15%、25% 的空间覆盖率。
- **Probe Vehicles**：从真实轨迹中随机采样，模拟不同密度（λ = 0, 5, 15, 25 辆车/分钟）。
- 总体可见率（visibility ratio）低至 **5%**。

#### **评估指标（均在未观测像素上计算）**
1. **Masked-MSE (2×2)**：对重建与真实速度场进行 2×2 平均池化后计算MSE，衡量低频结构准确性。
2. **Sobel-MSE**：使用Sobel算子提取梯度图后计算MSE，评估激波、停止-启动波等高频特征的恢复质量。
3. **LPIPS**：基于预训练AlexNet的感知相似性度量，反映视觉质量和纹理一致性。

#### **基线方法对比**
1. **AAS-only**：仅使用物理驱动的自适应各向异性平滑，无扩散模型。
2. **RePaint**：经典扩散图像修复方法，交替反向扩散与观测重置。
3. **Full-obs Model**：在完整速度场上训练的标准DDPM，作为性能上限参考。

#### **消融实验设计**
- 对比三种训练策略：
  - **Full-obs**：完整数据训练。
  - **Single-mask**：真实掩码训练。
  - **Double-mask**：双层掩码训练。
- 固定采样器为 PMA-Diffusion，评估不同先验的影响。

---

## 3. 主要实验结果和性能指标

### **关键性能数据（见 Table 2 & Table 3）**

| 设置 | 方法 | Masked-MSE ↓ | Sobel-MSE ↓ | LPIPS ↓ |
|------|------|--------------|-------------|---------|
| Row=5%, λ=0 | AAS-only | 0.1463 | 0.0009 | 0.2662 |
| | RePaint (M1) | 0.0172 | 0.0008 | 0.1617 |
| | **PMA-Diffusion (M1)** | **0.0167** | **0.0008** | **0.1768** |
| | RePaint (M3) | 0.2962 | 0.2066 | 0.0008 |
| | **PMA-Diffusion (M3)** | **0.0289** | **0.0008** | **0.1833** |

> 注：M1=Full-obs, M3=Double-mask

### **与基线方法的对比结果**

- 在最极端稀疏条件下（5%固定检测器 + 无probe车）：
  - **PMA-Diffusion (Double-mask)** 的 Masked-MSE 仅为 **0.0289**，显著优于 RePaint（0.2962）和 AAS-only（0.1463）。
  - RePaint 在 Single-mask 或 Double-mask 先验下易出现严重外推错误（如M2下Masked-MSE高达7.1959），而 PMA-Diffusion 通过物理投影有效稳定重建过程。

- 即使在仅有 **5% 可见率**的情况下，PMA-Diffusion 的性能接近在完整数据上训练的 Full-obs 模型。

- 随着观测密度增加（如Row=25%, λ=25），PMA-Diffusion 几乎达到 Full-obs 模型的性能水平。

### **消融实验结果**

- **Double-mask vs Single-mask**：
  - Double-mask 显著提升在稀疏区域的泛化能力，尤其当某些位置长期不可见时。
  - 例如在 Row=15%, λ=15 下，Double-mask 的 Masked-MSE 达到 0.0014，接近 Full-obs 的 0.0016。

- **物理投影的作用**：
  - 在所有配置下，添加 AAS 投影均能进一步降低误差，尤其是在稀疏条件下效果显著。
  - 物理投影有效抑制了扩散过程中可能出现的非物理解（如反向传播的激波）。

- **分辨率敏感性测试（64×64 网格）**：
  - 在更低分辨率下趋势一致，且数值更稳定。
  - 表明该方法具有良好的尺度适应性和计算效率。

---

## 4. 关键结论和发现

### **主要发现**

1. ✅ **Mask-Aware Training 是可行且有效的**：  
   即使没有完整观测数据，也能通过 Single-mask 和 Double-mask 策略成功训练出高质量的 diffusion prior，大幅缩小与 Full-obs 模型之间的差距。

2. ✅ **Physics-Guided Sampling 极大提升了重建质量**：  
   将物理约束作为推理阶段的投影操作，既能保持数据保真度，又能引导未观测区域符合交通动力学规律，避免“幻觉”结构。

3. ✅ **PMA-Diffusion 在极稀疏条件下仍表现优异**：  
   即使只有 **5% 可见率**，其性能仍显著优于传统方法和纯扩散修复方法，接近全监督模型的表现。

4. ✅ **模块化设计带来高灵活性**：  
   物理投影器（如 AAS）可独立于先验训练，未来可轻松替换为更复杂的物理模型（如守恒律、FD一致性等）。

### **方法的局限性**

1. **假设简化**：
   - 忽略 GPS 延迟（Assumption 1）和传感器偏置（Assumption 2），实际应用中可能需显式建模。
   - 观测噪声假设为高斯白噪声，未考虑异方差或非高斯特性。

2. **物理建模有限**：
   - 当前仅使用 AAS 作为代理物理模块，未显式建模密度、流量或守恒方程。
   - 无法处理 ramp flow、lane change 等复杂边界条件。

3. **计算成本较高**：
   - 扩散模型需数百步迭代采样，实时性不如传统滤波方法。

### **未来工作方向**

1. **扩展多模态建模**：
   - 分别建模 loop detector 和 probe vehicle 的噪声特性与偏差，提升异构数据融合能力。

2. **引入更强物理约束**：
   - 设计基于守恒律（Conservation Law）、基本图（Fundamental Diagram）的投影模块，并集成到采样循环中。

3. **提升推理效率**：
   - 探索蒸馏、加速采样（如DDIM、Progressive Distillation）等技术，实现近实时交通状态估计。

4. **网络级扩展**：
   - 将方法推广至城市路网，结合 Network Macroscopic Fundamental Diagram (NMFD) 进行全局一致性约束。

---

> **总结一句话**：  
> PMA-Diffusion 成功实现了**无需完整标签的扩散先验学习**与**物理引导的后验采样**相结合，在极稀疏观测下实现了高质量、物理一致的交通状态重建，为智能交通系统的感知层提供了可靠的新范式。

</details>

---

### 12. [Less is More: Non-uniform Road Segments are Efficient for Bus Arrival Prediction](https://arxiv.org/abs/2512.07200)

**Authors**: Zhen Huang, Jiaxin Deng, Jiayu Xu, Junbiao Pang, Haitao Yu  
**Category**: cs.LG  
**Published**: 2025-12-09  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2512.07200v1  

#### Abstract
In bus arrival time prediction, the process of organizing road infrastructure network data into homogeneous entities is known as segmentation. Segmenting a road network is widely recognized as the first and most critical step in developing an arrival time prediction system, particularly for auto-reg...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Less is More: Non-uniform Road Segments are Efficient for Bus Arrival Prediction*

---

## 1. 论文的主要贡献和创新点

### 解决的问题
传统公交到达时间预测（ETA）系统通常采用**均匀道路分段策略**（uniform road segmentation），即通过固定间隔插值或以站点为单位划分道路。这种做法忽略了不同路段在物理特性上的差异（如交叉口、学校区域、斑马线等），导致：
- 引入大量冗余且信息量低的特征；
- 在自回归模型中加剧**误差累积**（error accumulation）；
- 降低预测效率与准确性。

该论文指出，并非所有道路分段对预测同等重要，因此提出应**动态选择最具代表性的非均匀道路段**。

---

### 提出的新方法与新思路
作者提出了一种基于**强化学习**（Reinforcement Learning, RL）的框架——**Deep Progressive Reinforcement Learning (DPRL)**，用于自适应地学习最优的非均匀道路分段。其核心思想是将“道路段选择”建模为一个**序列决策过程**。

#### 方法架构：
- **两阶段解耦设计**：
  1. **Road Segment Selection Network (RSS-Net)**：使用 RL 动态选择关键道路段；
  2. **Linear Regression Model (LRM)**：在选出的稀疏路段上进行 ETA 预测。

- **奖励函数设计**：联合优化预测精度与特征稀疏性，定义如下三种奖励机制并比较效果：
  - **Benchmark-Calibrated Reward (BCR)**
  - **Inverse Error Reward (IER)**
  - **Arrival Time difference Reward (ATR)** —— 最终表现最佳

- **渐进训练机制**：通过迭代更新选中的路段位置，逐步逼近最优配置。

---

### 相比现有方法的优势
| 维度 | 优势 |
|------|------|
| **效率提升** | 显著减少输入特征数量（仅保留约 2/3 路段），提高计算效率 |
| **预测性能** | 即使使用简单的线性模型（LRM），也优于复杂的深度模型（如 LSTM） |
| **可解释性增强** | RSS-Net 可识别出真正影响预测的关键路段（如信号灯、学校入口） |
| **避免误差传播** | 非自回归结构 + 稀疏化减少了误差累积风险 |

> ✅ 核心理念：“**Less is More**”——更少但更有代表性的道路段反而能带来更高预测精度。

---

## 2. 核心实验方法和设置

### 数据集
- 来源于文献 [8] 的真实公交 GPS 数据；
- 北京市 6 条公交线路（No. 14, 60, 74, 110, 421, 563）连续 23 天的运营数据；
- 前 22 天用于训练，最后 1 天作为测试集；
- 所有轨迹按每 100 米进行初始均匀插值生成道路段。

---

### 实验设置
- **平台**：PyTorch 1.8.0 + CUDA 11.1，NVIDIA RTX 3080 GPU；
- **超参数**：
  - Batch size: 16
  - Optimizer: SGD（momentum=0.9, weight decay=0.0005）
  - 初始学习率：1e-3，每 20 轮衰减一次
- **动作迭代次数**：设为 2（经验证最优）
- **选段比例**：保留总插值点的 2/3

---

### 评估指标
- **Mean Absolute Error (MAE)**（单位：分钟）：
  $$
  \text{MAE} = \frac{1}{NV}\sum_{i=1}^{N}\sum_{v=1}^{V} |t_{\text{pred}} - t_{\text{true}}|
  $$

---

### 对比的基线方法
| 方法 | 类型 |
|------|------|
| LRM [11] | 线性回归模型 |
| KNN [31] | 基于历史相似轨迹搜索 |
| KR [11] | 核回归 |
| AMM [33] | 加法混合模型（非线性） |
| SPB [32] | 支持向量机 + 探针车数据 |
| KFP [34] | 卡尔曼滤波 |
| MLP [35] | 多层感知机 |
| LSTM [8] | 序列深度模型（SOTA之一） |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（MAE，单位：分钟）

| 方法 | 14 | 60 | 110 | 421 | 74 | 563 |
|------|----|----|-----|-----|----|-----|
| **Our (Proposed)** | **0.99** | **1.11** | **1.23** | **1.15** | **1.26** | **0.93** |
| LSTM [8] | 1.55 | 1.50 | 1.70 | 1.28 | 1.38 | 1.19 |
| LRM [11] | 1.58 | 1.58 | 1.72 | 1.30 | 1.52 | 1.26 |

> ✅ 所有路线均取得**最低 MAE**，显著优于所有基线。

#### 性能提升示例：
- 在 **Route 60** 上比 LSTM 提升 **0.39 min**
- 在 **Route 110** 上比 LSTM 提升 **0.47 min**
- 在 **Route 563** 上比 LSTM 提升 **0.26 min**

> 💡 特别值得注意的是：**一个简单的线性模型 + 智能选段 > 复杂非线性模型（LSTM）**

---

### 消融实验结果

#### （1）不同分段选择策略对比（Table II）

| 方法 | 描述 | 平均 MAE |
|------|------|--------|
| **ALL** | 使用全部插值点 | 1.51 |
| **RS**（Random Selection） | 随机选取 2/3 路段 | 1.91 |
| **RL**（Ours） | RL 自适应选择 | **1.11** |

> ❗说明：**并非越多越好**，随机删减反而更差；只有智能选择才能提升性能。

#### （2）奖励函数比较（Table III）

| 奖励策略 | Route 110 | Route 563 |
|---------|----------|----------|
| BCR | 1.79 | 1.19 |
| IER | 1.67 | 1.12 |
| **ATR**（Ours） | **1.23** | **0.93** |

> ✅ **ATR** 效果最好，因其提供稳定密集的反馈信号。

#### （3）是否使用二元掩码（Binary Mask）（Table IV）

| 方法 | 110 | 563 |
|------|-----|-----|
| RSS-Net w/o S (无掩码) | 1.98 | 1.11 |
| **RSS-Net w/ S**（带掩码） | **1.23** | **0.93** |

> ✅ 二元掩码极大提升了模型感知已选节点的能力。

#### （4）动作迭代次数敏感性分析（Table V）
| 迭代次数 | MAE（平均） |
|--------|------------|
| 2 | **1.11** |
| 4 | 1.13 |
| 6 | 1.16 |
| 8 | 1.18 |

> ✅ 少数几次精细调整优于多次大幅跳跃。

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **非均匀道路分段显著优于均匀分段**：物理意义明确的关键路段（如红绿灯、学校门口）比主干道中间的普通路段更具预测价值。
2. ✅ **“Less is More” 成立**：通过 RL 精准筛选关键路段后，即使使用简单线性模型也能超越复杂深度模型。
3. ✅ **特征质量 > 特征数量**：盲目增加插值点会引入噪声并加剧误差传播，而高质量稀疏表示更利于泛化。
4. ✅ **RSS-Net 具备强泛化能力**：在长短距离、城市功能区各异的多条路线上均保持稳定领先。

---

### 方法的局限性
- 当前模型依赖预定义的候选插值点集合，尚未实现完全端到端的段落生成；
- 未融合实时外部因素（如天气、突发事件）；
- 线性预测器限制了对长程非线性依赖的建模能力，尤其在长途路线中提升空间受限。

---

### 未来工作方向
1. 将 RSS-Net 扩展至**高速公路 ETA 预测**场景；
2. 引入**实时上下文信息**（traffic flow, weather）构建 test-time adaptation 框架；
3. 探索“**prediction as generation**”范式，结合生成式 AI 进行交通网络建模；
4. 推广至其他交通预测任务（如网约车、共享单车到达时间预测）。

---

## 总结
该论文提出了一个新颖且高效的公交到达时间预测框架，通过**强化学习驱动的道路段选择机制**，实现了“用更少、得更多”的目标。实验证明，**合理的特征选择比复杂的模型结构更重要**，为智能交通系统中的数据表示与模型设计提供了新的视角。

> 🔗 开源地址：[https://github.com/pangjunbiao/Less-is-More](https://github.com/pangjunbiao/Less-is-More)

</details>

---

### 13. [Parallel Algorithms for Combined Regularized Support Vector Machines: Application in Music Genre Classification](https://arxiv.org/abs/2512.07463)

**Authors**: Rongmei Liang, Zizheng Liu, Xiaofei Wu, Jingwen Tu  
**Category**: cs.LG  
**Published**: 2025-12-09  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2512.07463v1  

#### Abstract
In the era of rapid development of artificial intelligence, its applications span across diverse fields, relying heavily on effective data processing and model optimization. Combined Regularized Support Vector Machines (CR-SVMs) can effectively handle the structural information among data features, ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文核心结论与实验结果总结

## 1. 论文的主要贡献和创新点

### 解决的问题
本文针对**分布式存储的大规模数据**中，**Combined Regularized Support Vector Machines (CR-SVMs)** 缺乏高效并行算法的问题。传统的SVM模型在处理高维、结构化数据（如音乐信号）时，难以有效利用特征间的组结构（group structure）和时间连续性等先验信息，且现有算法在分布式环境下效率低下。

### 提出的新方法与新思路
1. **提出统一优化框架**：基于共识结构（consensus structure），构建了一个适用于多种损失函数（loss functions）和组合正则项（combined regularization terms）的通用优化框架。该框架可扩展至非凸正则项（如SCAD、MCP），具有强可扩展性。
2. **设计分布式并行ADMM算法**：开发了一种基于**分布式并行交替方向乘子法（distributed parallel ADMM）** 的算法，用于高效求解分布式存储下的CR-SVMs。
3. **引入Gaussian back-substitution保证收敛**：由于SVM中的截距项 $ \beta_0 $ 导致无法转化为两块ADMM，本文采用三块ADMM，并通过**Gaussian back-substitution**技术确保算法收敛，实现改进的次线性收敛率。
4. **提出SGL-SVM模型**：首次系统地提出了**稀疏组套索支持向量机（Sparse Group Lasso SVM, SGL-SVM）**，能够在组级别和个体特征级别同时实现稀疏性，特别适用于具有自然分组结构的数据（如音乐特征）。

### 相比现有方法的优势
- **通用性强**：提出的算法框架可兼容54种不同的CR-SVM模型（6种损失函数 × 9种正则组合），无需为每种模型单独设计算法。
- **计算复杂度独立于正则项与损失函数**：理论分析表明，算法的计算复杂度不受具体正则项或损失函数影响，体现了高度的普适性。
- **适用于非凸正则化**：能够有效处理SCAD、MCP等非凸正则项，减少参数估计偏差。
- **收敛性保障**：通过Gaussian back-substitution解决了多块ADMM的收敛难题，提升了算法稳定性。

---

## 2. 核心实验方法和设置

### 使用的数据集
1. **合成数据（Synthetic Data）**：
   - 数据分布：来自两个多元正态分布 $ N(\mu_+, \Sigma) $ 和 $ N(\mu_-, \Sigma) $
   - 特征维度 $ p = 500 $ 或 $ 1000 $
   - 样本数量 $ n = 200,000 $ 或 $ 500,000 $
   - 引入20%标签噪声以测试鲁棒性

2. **真实数据集：FMA（Free Music Archiv）**
   - 来源：GitHub公开音乐分析数据集（https://github.com/mdeff/fma）
   - 子集：`fma_small`，共8个流派，每类1000个音频样本，总计8000条
   - 特征总数：1036维，分为7个物理意义明确的组：
     - 时间域特征（16）
     - 频谱特征（32）
     - MFCC特征（160）
     - Chroma特征（96）
     - Tonnetz特征（48）
     - Echonest特征（518）
     - 其他特征（166）

### 实验设置与评估指标
- **并行环境**：将数据划分为 $ K=2,4,\dots,20 $ 个子块，模拟分布式训练
- **评估指标**：
  - **分类准确率（CAR / Accuracy）**：训练集与测试集上的准确率
  - **计算时间（CT）**
  - **迭代次数（NI）**
  - **选中真实特征数（NTSF）**
  - **稀疏性（Sparsity）**：零系数比例
- **参数选择**：使用SVM信息准则（SVMIC）自动选择正则参数 $ \lambda_1, \lambda_2 $

### 基线方法对比
- **QPADM-slack**（Guan et al., 2020）
- **QPADM-slack(GB)**（Wu et al., 2025b）
- **M-QPADM-slack(GB)**（Wu et al., 2025b）

所有基线均结合SCAD-SVM进行比较，而本文方法使用SGL-SVM。

---

## 3. 主要实验结果和性能指标

### 关键性能数据（见Table 7 & Table 8）

#### 在合成数据上（$ (n,p)=(500000,1000) $）：
| 方法 | CAR (%) | CT (s) | NI | NTSF |
|------|---------|--------|----|-------|
| **PADMM (本文)** | **97.2** | **1.942** | **40.1** | **10.8** |
| QPADM-slack | 96.5 | 2.010 | 44.7 | 11.1 |
| QPADM-slack(GB) | 96.8 | 1.980 | 44.5 | 11.0 |

- **PADMM在准确性、速度、迭代次数上全面优于基线**
- 所有方法随着 $ K $ 增加，CAR略有下降，CT降低，NI上升 —— 这是共识结构带来的固有 trade-off

#### 在FMA真实数据上（平均100次运行）：
| $ K $ | 方法 | Sparsity (%) | Train Acc (%) | Test Acc (%) |
|-------|------|--------------|---------------|--------------|
| 2 | **PADMM** | **90.38** | **99.32** | **97.27** |
|     | QPADM-slack(GB) | 85.99 | 95.14 | 92.96 |
|     | QPADM-slack | 83.64 | 93.38 | 92.00 |
| 20 | **PADMM** | **89.57** | **98.34** | **96.27** |
|     | QPADM-slack(GB) | 76.26 | 88.64 | 85.35 |
|     | QPADM-slack | 71.46 | 87.67 | 85.18 |

- **PADMM在所有 $ K $ 下均保持最高准确率与稀疏性**
- 即使在 $ K=20 $ 时，PADMM仍维持约96.3%测试准确率，显著优于其他方法（~85%）

### 与基线方法的对比结果
- **分类性能更优**：PADMM在训练和测试集上均取得更高准确率，说明其更强的泛化能力。
- **模型更稀疏**：Sparsity高出约5–18个百分点，有助于提升模型可解释性和压缩存储。
- **收敛更快**：所需迭代次数更少，尤其在大规模数据下优势明显。
- **通信开销更低**：通过局部计算 $ c_k $ 并聚合，减少了中心节点的通信负担。

### 消融实验与特征重要性分析（无显式消融表，但有深入分析）
- **特征组重要性排序**（基于SGL-SVM系数绝对值之和）：
  1. **MFCC特征组**（最重要）
  2. **频谱特征组**
  3. **Chroma特征组**
  4. 其余四组（Tonnetz, Echonest, 时间域, 其他）贡献极小，可忽略

> **关键发现**：当机器内存有限时，仅收集上述前三组特征即可实现高效的音乐分类，节省存储空间并保持高性能。

---

## 4. 关键结论和发现

### 主要发现
1. **SGL-SVM能有效捕捉音乐特征的双重稀疏性**：既能在组层面筛选重要特征类别（如MFCC），也能在组内识别关键预测变量，增强模型可解释性。
2. **所提并行ADMM算法具备高可靠性、稳定性和可扩展性**：在合成与真实数据上均表现出色，尤其适合处理大规模、高维、结构化的音乐数据。
3. **算法复杂度不依赖于正则项与损失函数形式**：验证了所提框架的高度通用性。
4. **MFCC是音乐流派分类中最关键的特征组**，其次是频谱和Chroma特征。

### 方法的局限性
- 当前算法主要面向**凸损失函数**（如hinge loss），对非凸损失函数的支持尚未涵盖。
- 随着子机器数量 $ K $ 增加，由于共识约束的存在，分类精度会缓慢下降，存在性能天花板。
- 实验受限于硬件内存，最多仅支持约20台并行机器，更大规模并行效果未充分验证。

### 未来工作方向
1. 将框架扩展至**非凸损失函数**（如bounded concave loss）场景。
2. 探索在**噪声或缺失数据**条件下的鲁棒性与收敛性质。
3. 结合**随机梯度下降（SGD）或加速技术**（如Nesterov momentum）进一步提升收敛速度。
4. 应用于**多标签任务**（multi-label tasks），如音乐自动打标（music auto-tagging）。
5. 推广到其他具有结构化特征的领域，如生物信息学、遥感图像分析等。

> **代码开源**：作者已将完整R代码发布于 GitHub：[https://github.com/xfwu1016/PADMM-for-Svms](https://github.com/xfwu1016/PADMM-for-Svms)，便于复现与应用。

</details>

---

### 14. [Cognitive Control Architecture (CCA): A Lifecycle Supervision Framework for Robustly Aligned AI Agents](https://arxiv.org/abs/2512.06716)

**Authors**: Zhibo Liang, Tianze Hu, Zaiye Chen, Mingjie Tang  
**Category**: cs.AI  
**Published**: 2025-12-09  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2512.06716v1  

#### Abstract
Autonomous Large Language Model (LLM) agents exhibit significant vulnerability to Indirect Prompt Injection (IPI) attacks. These attacks hijack agent behavior by polluting external information sources, exploiting fundamental trade-offs between security and functionality in existing defense mechanism...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Cognitive Control Architecture (CCA): A Lifecycle Supervision Framework for Robustly Aligned AI Agents

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

该论文针对 **Large Language Model (LLM) agents** 在自主执行任务时面临的严重安全威胁——**间接提示注入攻击（Indirect Prompt Injection, IPI）**。这类攻击通过污染外部信息源（如网页、文件、邮件等），诱导 agent 执行恶意操作（如数据泄露、未经授权的工具调用），从而偏离用户原始意图。

现有防御机制存在根本性缺陷：
- **Runtime checks** 只能检测单步行为，无法识别长程恶意计划；
- **Architectural defenses** 开销大或限制功能；
- **Training-time alignment** 难以泛化到新型或优化型攻击；
- 多数方法是**碎片化的（fragmented）**，缺乏对整个任务生命周期的端到端完整性保障。

因此，当前方法在 **安全性、功能性、效率** 之间被迫做出多维妥协。

---

### **提出了什么新方法或新思路**

作者提出了一种全新的全周期认知监督框架 —— **Cognitive Control Architecture (CCA)**，其核心思想是：

> “无论多么隐蔽的 IPI 攻击，只要追求恶意目标，最终都会在行为轨迹上表现出可检测的偏差。”

基于此洞察，CCA 构建了一个双层协同防御系统：

#### ✅ Pillar I: **Intent Graph**（主动控制流与数据流完整性）
- 在任务开始前，根据用户目标生成一个结构化的 **Directed Acyclic Graph (DAG)**，称为 *Intent Graph*。
- 图中每个节点代表一个合法的工具调用（tool call），边表示执行顺序。
- 运行时对每一步进行双重验证：
  - **Control-Flow Check**：动作是否符合预定义的拓扑序列？
  - **Data-Flow Integrity Check**：参数来源是否合法？仅允许来自用户输入或上游可信节点输出，禁止从可能被污染的上下文中提取。

这实现了**低开销、实时的结构性防护**，过滤掉明显的规划偏差。

#### ✅ Pillar II: **Tiered Adjudicator**（分级裁决器，用于高保真异常判定）
- 当动作偏离 Intent Graph 时触发。
- 引入多维度评分机制 —— **Intent Alignment Score (Salign)**，综合四个子分数加权计算：
  1. **Semantic Alignment (Ssem)**：语义相关性（与用户目标的相似度）
  2. **Causal Contribution (Scausal)**：逻辑必要性（该动作是否真正有助于达成目标？由 LLM 判断）
  3. **Source Provenance (Sprov)**：来源可信度（触发该动作的信息源是否可靠？动态更新信任分）
  4. **Inherent Action Risk Score (Srisk)**：固有风险值（预先设定高危操作的风险等级，如 `transfer_money` = 1.0）

最终决策基于总分决定：批准、阻断或询问用户。

此外，CCA 支持 **动态图更新（dynamic graph update）**：当裁决器确认某次“偏离”实为合理调整后，会将新路径加入 Intent Graph，实现安全边界下的灵活适应。

---

### **相比现有方法的优势**

| 维度 | CCA 的优势 |
|------|-----------|
| **安全性** | 显著降低攻击成功率（ASR），尤其对抗复杂条件性攻击（如 Important Messages） |
| **功能性** | 在无攻击场景下保持接近原始 agent 的任务完成率（BU），远优于其他防御方法 |
| **效率** | 分层设计避免频繁调用昂贵的 LLM 推理，token 消耗仅为 MELON 的 ~1/3.3 |
| **系统性** | 首次实现从“事后校验”到“全生命周期认知监督”的范式转变 |

---

## 2. 核心实验方法和设置

### **使用的数据集**

- **AgentDojo**：一个动态、有状态的 benchmark，包含 97 个多轮任务，涵盖 email、banking、workspace、travel 等多个领域。
- 使用确定性代码评估（deterministic code evaluation），适合测试全局监督能力。

### **实验设置和评估指标**

#### ✅ 模型配置
- **主 agent 模型**：DeepSeek-V3.1 和 Kimi K2
- **Adjudicator 模型**：固定为 DeepSeek-V3.1
- 温度设为 0.0 保证可复现性

#### ✅ 攻击类型（共四种 IPI 攻击）
1. **Direct**：直接嵌入恶意指令
2. **Ignore Prev**：要求忽略先前指令
3. **System Msg**：伪装成系统级提示
4. **Important Messages**：伪装成用户发出的重要消息（最隐蔽、最难防御）

#### ✅ 基线方法对比
| 类型 | 方法 |
|------|------|
| Prompt Engineering | Repeat Prompt, Spotlight |
| 外部分类器 | DeBERTa classifier |
| SOTA Runtime Verification | MELON |
| 无防御 | No Defense |

#### ✅ 评估指标
| 指标 | 含义 |
|------|------|
| **Attack Success Rate (ASR, ↓)** | 攻击者目标达成的比例 |
| **Benign Utility (BU, ↑)** | 无攻击时任务成功完成率 |
| **Utility Under Attack (UA, ↑)** | 存在攻击时仍正确完成任务且未执行恶意操作的比例 |
| **API 开销 / Token 使用量** | 衡量运行效率 |

---

## 3. 主要实验结果和性能指标

### **关键性能数据（见 Table 1）**

| 方法 | 平均 ASR (%) | UA (%) | BU (%) |
|------|-------------|--------|--------|
| No Defense | 11.99 | 81.67 | 89.69 |
| DeBERTa | 2.27 | 42.97 | 53.61 |
| MELON (SOTA) | **0.16** | 43.76 | 72.16 |
| **CCA (Ours)** | **0.34** | **86.43** | **87.63** |

> 🔍 **解读**：
> - CCA 将平均 ASR 从 11.99% 降至 **0.34%**，攻击缓解率达 **>97%**
> - 在最具挑战性的 **Important Messages** 攻击中，CCA ASR = **0.84%**，显著优于 Spotlight (42.78%) 和 MELON (0.63%)
> - CCA 的 UA 达到 **86.43%**，几乎是 MELON (43.76%) 的两倍，说明其在防御同时极大保留了功能性

---

### **与基线方法的对比结果**

- **安全性方面**：
  - CCA 与 MELON 同属顶级防御水平（ASR < 1%）
  - 但在最难的 Important Messages 攻击中，CCA 表现更稳定

- **功能性方面**：
  - CCA 的 BU 仅下降 2.06%，而 MELON 下降 19.6%，DeBERTa 下降 40.2%
  - CCA 成功解决了“安全 vs 功能”的经典 trade-off

- **效率方面**（Figure 4b）：
  - CCA 平均每任务消耗约 **3857 tokens**
  - MELON 消耗高达 **12772 tokens**（约为 CCA 的 3.3 倍）
  - 效率提升主要得益于 Layer 1 的轻量级 Intent Graph 验证，仅在必要时才启用 Layer 2 的 LLM 裁决

---

### **消融实验结果（Ablation Study）**

#### ✅ 模型无关性测试（Table 2）
使用 Kimi K2 替换主模型：
- ASR 从 29.50% → **0.53%**
- UA 从 74.18% → **82.51%**（甚至高于无防御！）
→ 证明 CCA 具备良好的 **model-agnostic robustness**

#### ✅ 组件重要性分析（Table 3 & Figure 3）

| 配置 | ASR (%) | UA (%) |
|------|--------|--------|
| Full CCA | **0.53** | **82.51** |
| w/o S_causal | 4.95 | 66.49 |
| w/o S_risk | 1.58 | 81.98 |
| w/o S_prov | 1.48 | 83.46 |
| w/o S_sem | 0.74 | 83.56 |

> 🔍 **发现**：
> - 移除 **S_causal** 导致性能崩溃，说明因果推理是核心支柱
> - 移除其他组件虽对平均 UA 影响较小，但会导致更多低分异常案例（outliers），影响鲁棒性
> - 所有组件共同作用使 Salign 分布更平滑、更具判别力，避免“信息悬崖”（information cliff）

#### ✅ 动态图更新消融（Table 8）
- 关闭动态更新后，UA 从 86.47% → 84.19%
- 安全性不变（ASR ≈ 0.7%）
→ 证实动态图更新对维持上下文连贯性和任务成功率至关重要

---

## 4. 关键结论和发现

### **主要发现**

1. **IPI 攻击的本质是行为轨迹的可检测偏差**，可通过控制流 + 数据流联合监控有效捕捉。
2. **Intent Graph + Tiered Adjudicator 的双层架构** 实现了安全性、功能性、效率三者的统一，打破了传统 trade-off。
3. **Causal Contribution 分析是裁决机制的核心**，仅靠语义匹配不足以应对高级伪装攻击。
4. **动态图更新机制增强了系统的灵活性与实用性**，允许 agent 在安全前提下适应环境变化。
5. CCA 在多种 LLM 架构（包括开源与 API 模型）上均表现优异，具备良好泛化能力。

---

### **方法的局限性**

1. **任务需可分解为明确的 Intent Graph**：对于高度开放、探索性强的任务（如自由创作、科研探索），难以提前构建完整计划。
2. **Inherent Action Risk Score (Srisk) 是静态的**：未考虑参数依赖的风险变化（例如 `send_email` 发给谁？金额多大？）。
3. **Source Provenance Score 在短任务中简化为静态值**：未能充分发挥其动态学习潜力。
4. **依赖高质量的 Intent Graph 生成能力**：若初始规划错误，可能导致误判。

---

### **未来工作方向**

1. **动态图生成与演化机制**：支持在运行中自动扩展和修正 Intent Graph，适用于开放式任务。
2. **上下文感知的风险评分模型**：开发参数敏感的动态 Srisk，结合 argument 内容评估真实危害。
3. **强化学习驱动的信任更新机制**：让 Sprov 能在长期交互中持续优化。
4. **集成更多模态输入**（如图像、语音）下的 CCA 扩展。
5. 探索 CCA 在 multi-agent 协作场景中的应用。

---

> 📌 **总结一句话**：  
> CCA 提出了一种系统性的全生命周期监督框架，首次实现了 LLM agent 在面对复杂 IPI 攻击时“既安全又不失效、既高效又不脆弱”的理想平衡，为构建可信自主智能体提供了坚实架构蓝图。

</details>

---

### 15. [Enhancing Agentic RL with Progressive Reward Shaping and Value-based Sampling Policy Optimization](https://arxiv.org/abs/2512.07478)

**Authors**: Zhuoran Zhuang, Ye Chen, Jianghao Su, Chao Luo, Luhui Liu, Xia Zeng  
**Category**: cs.CL  
**Published**: 2025-12-09  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2512.07478v1  

#### Abstract
Large Language Models (LLMs) empowered with Tool-Integrated Reasoning (TIR) can iteratively plan, call external tools, and integrate returned information to solve complex, long-horizon reasoning tasks. Agentic Reinforcement Learning (Agentic RL) optimizes such models over full tool-interaction traje...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Enhancing Agentic RL with Progressive Reward Shaping and Value-based Sampling Policy Optimization

---

## 1. 论文的主要贡献和创新点

### 解决的问题
本文针对 **Agentic Reinforcement Learning (Agentic RL)** 在 **Tool-Integrated Reasoning (TIR)** 场景中面临的两大挑战：
1. **稀疏且非指导性奖励**（Sparse and non-instructive rewards）：传统基于最终答案正确性的二元（0-1）奖励信号无法为中间步骤提供有效反馈，导致学习效率低、收敛慢。
2. **GRPO 中的梯度退化问题**（Gradient degradation in GRPO）：当一个 rollout 组内所有样本获得相同奖励时，其优势函数（advantage）为零，导致梯度更新失效，训练不稳定且样本利用率低。

---

### 提出的新方法
作者提出两种互补的技术来解决上述问题：

#### ✅ **Progressive Reward Shaping (PRS)**
- **思想来源**：受课程学习（curriculum learning）启发，设计分阶段、渐进式的奖励机制。
- **核心机制**：
  - 首先鼓励模型掌握基础能力（如生成可解析的工具调用格式）；
  - 再逐步优化更复杂的任务目标（如事实准确性、答案质量）。
- **具体实现**：
  - **短答案 QA**：引入 **length-aware BLEU**，避免对简短但正确的答案进行不公平惩罚。
  - **长答案 QA**：结合 **LLM-as-a-Judge** 评分，防止模型通过“奖励黑客”（reward hacking）生成看似合理但错误的内容。

#### ✅ **Value-based Sampling Policy Optimization (VSPO)**
- **改进对象**：在 GRPO 基础上提出的增强型策略优化算法。
- **核心机制**：
  1. **Value-based Sampling**：识别并替换掉低价值（零优势）样本，优先采样具有高“学习价值”的 prompt。学习价值由任务难度（与最优表现差距）和策略不确定性（reward variance）共同决定。
  2. **Value Smoothing Clipping**：对重复采样的高价值样本进行优势值平滑裁剪，防止其主导梯度更新，提升训练稳定性。

---

### 相比现有方法的优势
| 方法 | 优势 |
|------|------|
| **PRS vs Binary Reward** | 提供密集、阶段性反馈，显著加快收敛速度，减少无效探索 |
| **VSPO vs PPO / GRPO / CISPO** | 更高的样本效率、更强的训练稳定性、更快的收敛速度和更高的最终性能 |
| **PRS + VSPO 联合使用** | 显著提升泛化能力和跨领域适应性，在多种 QA 任务上均取得 SOTA 表现 |

---

## 2. 核心实验方法和设置

### 数据集
#### 🔹 **Long-Form QA**
- 来源于生产系统的专有数据，包含三类查询：
  - `Qsimple`：单个文本问题
  - `Qmultim`：图文混合查询（含图像 URL）
  - `Qmultiq`：包含多个子问题的复杂查询
- 参考答案由人工基于内部知识库标注。
- SFT 数据通过 **Qwen3-235B-A22B** 的知识蒸馏生成，并经过规则过滤和 LLM-as-a-Judge 清洗。

#### 🔹 **Short-Form QA**
- 使用 7 个公开基准数据集：
  - 通用问答：NQ, TriviaQA, PopQA
  - 多跳问答：HotpotQA, 2WikiMultiHopQA, Musique, Bamboogle
- 训练集：合并 NQ 和 HotpotQA 的训练集
- 测试集：全部 7 个数据集的测试/验证集，用于评估域内与域外性能

---

### 实验设置
- **模型**：
  - 短答案任务：Qwen2.5-7B-Instruct
  - 长答案任务：Qwen3-14B
- **训练框架**：Trinity-RFT
- **超参数**：
  - Rollout 数量：5
  - 学习率：1e-6
  - 温度 & top_p：均为 1.0
  - KL 系数 β：0.001，clip ratio ε：0.2
- **硬件**：单节点 8×H100 GPU，启用梯度检查点和 FSDP CPU 卸载以节省显存

---

### 评估指标
| 任务类型 | 评估方式 |
|--------|---------|
| **Long-Form QA** | 使用 **Qwen3-235B-A22B-Instruct-2507** 作为 LLM Judge，判断回复是否与参考答案语义一致（Match/Mismatch） |
| **Short-Form QA** | 使用 **Exact Match (EM)** 指标，衡量预测答案与标准答案完全匹配的比例 |

---

### 基线方法对比
| 类别 | 对比方法 |
|------|----------|
| **Policy Optimization** | PPO, GRPO, CISPO, SFT-only |
| **Reward Design** | Rule-based 0-1 reward（基于 EM 或 LLM 判定） |
| **强参考模型** | Qwen3-235B-A22B（仅用于 Long-Form QA 性能上限参考） |

---

## 3. 主要实验结果和性能指标

### 🔹 Long-Form QA 结果（见 Table 1）
| 方法 | Qsimple | Qmultiq | Qmultim | 平均提升（vs untrained） |
|------|--------|--------|--------|---------------------|
| untrained | 0.6025 | 0.400 | 0.500 | — |
| SFT-only | 0.700 | 0.575 | 0.400 | — |
| GRPO | 0.700 | 0.575 | 0.475 | — |
| PPO | 0.6625 | 0.700 | 0.550 | — |
| CISPO | 0.6875 | 0.4875 | 0.400 | — |
| **VSPO** | **0.7125** | **0.725** | **0.550** | **+18.26%, +81.24%, +10%** |

✅ **VSPO 在所有任务上均优于其他 RL 方法**，尤其在多子问题（Qmultiq）上表现突出。

---

### 🔹 Short-Form QA 结果（见 Table 2）
| 方法 | Avg Score |
|------|---------|
| Direct Inference | 0.181 |
| SFT | 0.207 |
| PPO+EM | 0.385 |
| GRPO+EM | 0.396 |
| VSPO+EM | 0.397 |
| PPO+PRS | 0.396 |
| GRPO+PRS | 0.410 |
| **VSPO+PRS** | **0.419** |

📌 **关键发现**：
- 所有使用 **PRS** 的方法都优于对应 EM 奖励版本 → **PRS 有效**
- 所有使用 **VSPO** 的方法表现稳定且领先 → **VSPO 更高效稳定**
- **VSPO+PRS 组合达到最高平均得分 0.419**

---

### 🔹 消融实验结果（见 Table 4）
| 配置 | Qsimple | Qmultiq | Qmultim |
|------|--------|--------|--------|
| w/o sample, w/o clip（≈GRPO） | 0.700 | 0.575 | 0.475 |
| random sample, w/o clip | 0.460 | 0.160 | 0.100 |
| value-based sample, w/o clip | 0.450 | 0.175 | 0.300 |
| random sample, w/clip | 0.500 | 0.300 | 0.025 |
| **value-based sample, w/clip（VSPO）** | **0.7125** | **0.725** | **0.550** |

✅ **结论**：
- 缺少 **value smoothing clipping** 会导致严重训练不稳定（KL divergence 异常升高）
- **random sampling** 即使加 clipping 也无法带来性能增益
- **只有 value-based sampling + clipping 的组合才能实现最佳性能**

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **PRS 显著改善学习过程**：
   - 分阶段奖励引导模型先学会“怎么做”，再追求“做得好”。
   - 在短答案任务中，length-aware BLEU 避免了对简洁正确答案的误罚。
   - 在长答案任务中，LLM-as-a-Judge 抑制了 reward hacking，提升了事实一致性。

2. **VSPO 解决了 GRPO 的梯度退化问题**：
   - 通过动态替换低价值样本，确保每个 batch 都包含有效梯度信号。
   - value-based sampling 聚焦于“最难且最不确定”的任务，最大化学习收益。
   - value smoothing clipping 保证了重复采样的稳定性，避免过拟合高价值 prompt。

3. **PRS + VSPO 具有协同效应**：
   - 密集奖励 + 高效采样 = 更快收敛 + 更高性能 + 更强泛化能力。
   - 在短形式和长形式 QA 上均超越 PPO、GRPO、CISPO 和 SFT-only。

---

### ⚠️ 局限性
1. **依赖高质量参考答案和 LLM Judge**：LLM-as-a-Judge 的可靠性会影响 PRS 的有效性，可能存在主观偏差。
2. **计算开销增加**：VSPO 需要额外计算任务价值分数，虽然仍优于 DAPO 等动态采样方法，但在大规模部署时需权衡成本。
3. **当前验证集中在 QA 任务**：尽管作者声称方法具有一般性，但仍需在更多 TIR 场景（如规划、决策、代码生成）中进一步验证。

---

### 🔮 未来工作方向
1. 将 PRS 和 VSPO 应用于更广泛的 **Agentic RAG** 和 **multi-agent systems**。
2. 探索自动构建 curriculum 的机制，实现 reward stage 的自适应调整。
3. 设计轻量化版本的 VSPO，降低在线训练中的计算负担。
4. 结合 offline RL 或 preference modeling 进一步提升样本效率。

---

## 总结
该论文系统地解决了 Agentic RL 在 TIR 场景下的两个核心瓶颈——**稀疏奖励**和**梯度退化**，提出了 **PRS** 和 **VSPO** 两项关键技术。实验证明，二者联合使用可在多个 QA 基准上实现稳定且显著的性能提升，为构建高效、鲁棒的 LLM-based agent 提供了一套可推广的方法论框架。

</details>

---

### 16. [Bridging Code Graphs and Large Language Models for Better Code Understanding](https://arxiv.org/abs/2512.07666)

**Authors**: Zeqi Chen, Zhaoyang Chu, Yi Gui, Feng Guo, Yao Wan, Chuan Shi  
**Category**: cs.CL  
**Published**: 2025-12-09  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2512.07666v1  

#### Abstract
Large Language Models (LLMs) have demonstrated remarkable performance in code intelligence tasks such as code generation, summarization, and translation. However, their reliance on linearized token sequences limits their ability to understand the structural semantics of programs. While prior studies...

---

### 17. [gp2Scale: A Class of Compactly-Supported Non-Stationary Kernels and Distributed Computing for Exact Gaussian Processes on 10 Million Data Points](https://arxiv.org/abs/2512.06143)

**Authors**: Marcus M. Noack, Mark D. Risser, Hengrui Luo, Vardaan Tekriwal, Ronald J. Pandolfi  
**Category**: cs.LG  
**Published**: 2025-12-09  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2512.06143v1  

#### Abstract
Despite a large corpus of recent work on scaling up Gaussian processes, a stubborn trade-off between computational speed, prediction and uncertainty quantification accuracy, and customizability persists. This is because the vast majority of existing methodologies exploit various levels of approximat...

---

### 18. [Optimizing LLMs Using Quantization for Mobile Execution](https://arxiv.org/abs/2512.06490)

**Authors**: Agatsya Yadav, Renta Chintala Bhargavi  
**Category**: cs.LG  
**Published**: 2025-12-09  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2512.06490v1  

#### Abstract
Large Language Models (LLMs) offer powerful capabilities, but their significant size and computational requirements hinder deployment on resource-constrained mobile devices. This paper investigates Post-Training Quantization (PTQ) for compressing LLMs for mobile execution. We apply 4-bit PTQ using t...

---

### 19. [KV-CAR: KV Cache Compression using Autoencoders and KV Reuse in Large Language Models](https://arxiv.org/abs/2512.06727)

**Authors**: Sourjya Roy, Shrihari Sridharan, Surya Selvam, Anand Raghunathan  
**Category**: cs.LG  
**Published**: 2025-12-09  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2512.06727v1  

#### Abstract
As Large Language Models (LLMs) scale in size and context length, the memory requirements of the key value (KV) cache have emerged as a major bottleneck during autoregressive decoding. The KV cache grows with sequence length and embedding dimension, often exceeding the memory footprint of the model ...

---

### 20. [LLM-Driven Composite Neural Architecture Search for Multi-Source RL State Encoding](https://arxiv.org/abs/2512.06982)

**Authors**: Yu Yu, Qian Xie, Nairen Cao, Li Jin  
**Category**: cs.LG  
**Published**: 2025-12-09  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2512.06982v1  

#### Abstract
Designing state encoders for reinforcement learning (RL) with multiple information sources -- such as sensor measurements, time-series signals, image observations, and textual instructions -- remains underexplored and often requires manual design. We formalize this challenge as a problem of composit...

---

### 21. [PlantBiMoE: A Bidirectional Foundation Model with SparseMoE for Plant Genomes](https://arxiv.org/abs/2512.07113)

**Authors**: Kepeng Lin, Qizhe Zhang, Rui Wang, Xuehai Hu, Wei Xu  
**Category**: cs.LG  
**Published**: 2025-12-09  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2512.07113v1  

#### Abstract
Understanding the underlying linguistic rules of plant genomes remains a fundamental challenge in computational biology. Recent advances including AgroNT and PDLLMs have made notable progress although, they suffer from excessive parameter size and limited ability to model the bidirectional nature of...

---

### 22. [PINE: Pipeline for Important Node Exploration in Attributed Networks](https://arxiv.org/abs/2512.07244)

**Authors**: Elizaveta Kovtun, Maksim Makarenko, Natalia Semenova, Alexey Zaytsev, Semen Budennyy  
**Category**: cs.LG  
**Published**: 2025-12-09  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2512.07244v1  

#### Abstract
A graph with semantically attributed nodes are a common data structure in a wide range of domains. It could be interlinked web data or citation networks of scientific publications. The essential problem for such a data type is to determine nodes that carry greater importance than all the others, a t...

---

### 23. [LightSearcher: Efficient DeepSearch via Experiential Memory](https://arxiv.org/abs/2512.06653)

**Authors**: Hengzhi Lan, Yue Yu, Li Qian, Li Peng, Jie Wu, Wei Liu, Jian Luan, Ting Bai  
**Category**: cs.AI  
**Published**: 2025-12-09  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2512.06653v1  

#### Abstract
DeepSearch paradigms have become a core enabler for deep reasoning models, allowing them to invoke external search tools to access up-to-date, domain-specific knowledge beyond parametric boundaries, thereby enhancing the depth and factual reliability of reasoning. Building upon this foundation, rece...

---

### 24. [BitStopper: An Efficient Transformer Attention Accelerator via Stage-fusion and Early Termination](https://arxiv.org/abs/2512.06457)

**Authors**: Huizheng Wang, Hongbin Wang, Shaojun Wei, Yang Hu, Shouyi Yin  
**Category**: cs.LG  
**Published**: 2025-12-09  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2512.06457v1  

#### Abstract
Attention-based large language models (LLMs) have transformed modern AI applications, but the quadratic cost of self-attention imposes significant compute and memory overhead. Dynamic sparsity (DS) attention mitigates this, yet its hardware efficiency is limited by the added prediction stage and the...

---

### 25. [ARCANE: A Multi-Agent Framework for Interpretable and Configurable Alignment](https://arxiv.org/abs/2512.06196)

**Authors**: Charlie Masters, Marta Grze\'skiewicz, Stefano V. Albrecht  
**Category**: cs.AI  
**Published**: 2025-12-09  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.06196v1  

#### Abstract
As agents based on large language models are increasingly deployed to long-horizon tasks, maintaining their alignment with stakeholder preferences becomes critical. Effective alignment in such settings requires reward models that are interpretable so that stakeholders can understand and audit model ...

---

### 26. [GENIUS: An Agentic AI Framework for Autonomous Design and Execution of Simulation Protocols](https://arxiv.org/abs/2512.06404)

**Authors**: Mohammad Soleymanibrojeni, Roland Aydin, Diego Guedes-Sobrinho, Alexandre C. Dias, Maur\'icio J. Piotrowski, Wolfgang Wenzel, Celso Ricardo Caldeira R\^ego  
**Category**: cs.AI  
**Published**: 2025-12-09  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.06404v1  

#### Abstract
Predictive atomistic simulations have propelled materials discovery, yet routine setup and debugging still demand computer specialists. This know-how gap limits Integrated Computational Materials Engineering (ICME), where state-of-the-art codes exist but remain cumbersome for non-experts. We address...

---

### 27. [DoVer: Intervention-Driven Auto Debugging for LLM Multi-Agent Systems](https://arxiv.org/abs/2512.06749)

**Authors**: Ming Ma, Jue Zhang, Fangkai Yang, Yu Kang, Qingwei Lin, Saravan Rajmohan, Dongmei Zhang  
**Category**: cs.AI  
**Published**: 2025-12-09  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.06749v1  

#### Abstract
Large language model (LLM)-based multi-agent systems are challenging to debug because failures often arise from long, branching interaction traces. The prevailing practice is to leverage LLMs for log-based failure localization, attributing errors to a specific agent and step. However, this paradigm ...

---

### 28. [JT-DA: Enhancing Data Analysis with Tool-Integrated Table Reasoning Large Language Models](https://arxiv.org/abs/2512.06859)

**Authors**: Ce Chi, Xing Wang, Zhendong Wang, Xiaofan Liu, Ce Li, Zhiyan Song, Chen Zhao, Kexin Yang, Boshen Shi, Jingjing Yang, Chao Deng, Junlan Feng  
**Category**: cs.AI  
**Published**: 2025-12-09  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.06859v1  

#### Abstract
In this work, we present JT-DA-8B (JiuTian Data Analyst 8B), a specialized large language model designed for complex table reasoning tasks across diverse real-world scenarios. To address the lack of high-quality supervision in tabular reasoning scenarios, we construct a comprehensive and diverse tra...

---

### 29. [ClinNoteAgents: An LLM Multi-Agent System for Predicting and Interpreting Heart Failure 30-Day Readmission from Clinical Notes](https://arxiv.org/abs/2512.07081)

**Authors**: Rongjia Zhou, Chengzhuo Li, Carl Yang, Jiaying Lu  
**Category**: cs.AI  
**Published**: 2025-12-09  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.07081v1  

#### Abstract
Heart failure (HF) is one of the leading causes of rehospitalization among older adults in the United States. Although clinical notes contain rich, detailed patient information and make up a large portion of electronic health records (EHRs), they remain underutilized for HF readmission risk analysis...

---

### 30. [ProSocialAlign: Preference Conditioned Test Time Alignment in Language Models](https://arxiv.org/abs/2512.06515)

**Authors**: Somnath Banerjee, Sayan Layek, Sayantan Adak, Mykola Pechenizkiy, Animesh Mukherjee, Rima Hazra  
**Category**: cs.CL  
**Published**: 2025-12-09  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.06515v1  

#### Abstract
Current language model safety paradigms often fall short in emotionally charged or high-stakes settings, where refusal-only approaches may alienate users and naive compliance can amplify risk. We propose ProSocialAlign, a test-time, parameter-efficient framework that steers generation toward safe, e...

---

## 🔧 Configuration

This bot is configured to look for papers containing the following keywords:
- framework, System, Generation, Video, Linear, LLM, RL, RLHF, Inference, Training, Attention, Pipeline, MOE, Sparse, Quantization, Speculative, Efficient, Efficiency, Framework, Parallel, Distributed, Kernel, Decode, Decoding, Prefill, Throughput, Fast, Network, Hardware, Cluster, FP8, FP4, Optimization, Scalable, Communication

## 📅 Schedule

The bot runs daily at 12:00 UTC via GitHub Actions to fetch the latest papers.

## 🚀 How to Use

1. **Fork this repository** to your GitHub account
2. **Customize the configuration** by editing `config.json`:
   - Add/remove arXiv categories (e.g., `cs.AI`, `cs.LG`, `cs.CL`)
   - Modify keywords to match your research interests
   - Adjust `max_papers` and `days_back` settings
3. **Enable GitHub Actions** in your repository settings
4. **The bot will automatically run daily** and update the README.md

## 📝 Customization

### arXiv Categories
Common categories include:
- `cs.AI` - Artificial Intelligence
- `cs.LG` - Machine Learning
- `cs.CL` - Computation and Language
- `cs.CV` - Computer Vision
- `cs.NE` - Neural and Evolutionary Computing
- `stat.ML` - Machine Learning (Statistics)

### Keywords
Add keywords that match your research interests. The bot will search for these terms in paper titles and abstracts.

### Exclude Keywords
Add terms to exclude certain types of papers (e.g., "survey", "review", "tutorial").

## 🔍 Manual Trigger

You can manually trigger the bot by:
1. Going to the "Actions" tab in your repository
2. Selecting "arXiv Bot Daily Update"
3. Clicking "Run workflow"

---
*Generated automatically by arXiv Bot* 
