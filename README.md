# arXiv Papers Bot 🤖

This repository automatically fetches and displays relevant papers from arXiv based on configured criteria.

## RSS Vercel Deployment [![An example of deployed RSS Server using vercel](https://img.shields.io/badge/Deployed-Example-blue)](https://arxiv.tachicoma.top/)

You can click this to deploy yours 

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/maydomine/arxiv_rss_bot)
## 📊 Statistics

- **Last Updated**: 2025-12-15 05:57:29 UTC
- **Total Papers Found**: 30
- **Categories Monitored**: cs.AI, cs.CL, cs.DC, cs.LG

## 📚 Recent Papers

### 1. [xGR: Efficient Generative Recommendation Serving at Scale](https://arxiv.org/abs/2512.11529)

**Authors**: Qingxiao Sun, Tongxuan Liu, Shen Zhang, Siyu Wu, Peijun Yang, Haotian Liang, Menxin Li, Xiaolong Ma, Zhiwei Liang, Ziyi Ren, Minchao Zhang, Xinyu Liu, Ke Zhang, Depei Qian, Hailong Yang  
**Category**: cs.LG  
**Published**: 2025-12-15  
**Score**: 10.5  
**Type**: new  
**ArXiv ID**: 2512.11529v1  

#### Abstract
Recommendation system delivers substantial economic benefits by providing personalized predictions. Generative recommendation (GR) integrates LLMs to enhance the understanding of long user-item sequences. Despite employing attention-based architectures, GR's workload differs markedly from that of LL...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：xGR: Efficient Generative Recommendation Serving at Scale

---

## 1. 论文的主要贡献和创新点

### 解决的问题
本文针对**生成式推荐系统（Generative Recommendation, GR）在高并发场景下的低延迟服务挑战**，识别出三个核心瓶颈：
1. **长提示、短输出导致的内存与计算冗余**：GR输入为用户长期行为序列（数百至数千token），输出仅为少数几个token，传统注意力机制在beam search中重复加载共享KV缓存，造成严重内存带宽压力。
2. **大规模item空间中的beam search效率低下**：大beam width（如512）和Top-K筛选带来巨大的排序开销和无效item生成问题。
3. **系统级流水线调度不充分**：host-device间缺乏重叠，多请求、多阶段并行度不足，尤其对小模型而言，host-side调度开销占比显著。

### 提出的新方法与创新思路
作者提出 **xGR** ——一个面向生成式推荐的端到端高效推理服务系统，包含三大核心组件：

#### （1）xAttention：优化注意力计算
- **KV Cache分离管理**：将KV缓存分为**共享部分**（prompt）和**非共享部分**（decode阶段生成的token），避免beam之间重复加载相同前缀。
- **分阶段计算分配（Staged Computation Allocation）**：
  - 将注意力计算划分为 **共享阶段、非共享阶段、合并阶段**，分别在不同Core Group（CG）上并行执行。
  - 引入 **OnlineSoftmax** 实现跨tile的概率归一化。
  - 使用轻量级决策树回归器动态预测最优CG资源划分策略，实现负载均衡。

#### （2）xBeam：提升beam search效率
- **有效路径约束（Valid Path Constraint）**：
  - 利用预构建的合法item词表生成**item mask**，通过mask过滤非法token组合，防止“幻觉”item生成。
  - 采用**稀疏+密集混合存储**策略：早期dense预生成，后期sparse原地更新，降低mask生成开销。
- **早停排序机制（Early Sorting Termination）**：
  - 维护大小为`beam width`的全局最小堆，遍历beam时若当前log_prob小于堆顶则提前终止该beam搜索，大幅减少排序时间。
- **数据结构复用（Data Structure Reuse）**：
  - 复用被淘汰beam的数据结构空间，避免频繁内存分配/释放，降低GC压力。

#### （3）xSchedule：系统级调度优化
- **三级流水线架构**：Scheduler → Engine → Worker，支持跨批、跨请求、跨阶段的多层次并行。
- **Host-Device重叠**：
  - Host侧filter mask生成与Device侧forward计算重叠；
  - H2D传输mask与self-attention计算重叠。
- **图提交优化（Kernel Graph Dispatch）**：将一系列kernel操作打包成静态图一次性提交，减少CPU干预和launch开销。
- **多流并行（Multi-stream Parallelism）**：多个stream并发处理不同batch，充分利用加速器并行能力，摊销host调度开销。

### 相比现有方法的优势
| 维度 | 现有方法（如vLLM/xLLM） | xGR |
|------|------------------------|-----|
| KV管理 | PagedAttention，忽略共享前缀，重复加载 | 分离KV缓存，物理共享前缀 |
| 内存效率 | 存在大量block copy和碎片 | 避免copy，token粒度管理 |
| 排序开销 | 全局排序所有候选 | 早停机制显著降低排序量 |
| 过滤机制 | 动态生成mask或全量检查，高延迟 | 设备驻留mask融合，开销极低 |
| 调度粒度 | 单一流水线，host-device耦合紧 | 多层次重叠+多stream并行 |

---

## 2. 核心实验方法和设置

### 数据集
- **Amazon Review**：公开推荐任务基准数据集。
- **JD Trace**：来自京东生产环境的真实流量trace，具有动态请求模式。

### 实验设置
- **硬件平台**：
  - **Ascend集群**：64节点，每节点16 Ascend NPU（64GB），通过HCCS互联。
  - **GPU集群**：8节点，每节点8 NVIDIA H800 GPU（80GB），通过NVLink互联。
- **模型**：
  - **Qwen3**（0.6B ~ 4B参数）
  - **OneRec**（0.1B ~ 3B参数）
- **评估指标**：
  - **平均延迟（Average Latency）**
  - **P99延迟（P99 Latency）**
  - **吞吐量（Throughput / RPS）**
  - **峰值内存占用（Peak Memory Usage）**
  - **Kernel级性能分析**（延迟、算力利用率、访存开销）

### 基线方法对比
- **vLLM**：基于PagedAttention的主流LLM推理引擎，在Ascend上使用vllm-ascend移植版本。
- **xLLM**：工业级推理框架，专为Ascend优化，也使用PagedAttention机制。

---

## 3. 主要实验结果和性能指标

### 关键性能数据
- 在严格P99延迟 ≤ 200ms约束下，**xGR相比state-of-the-art基线实现至少3.49×的吞吐提升**。
- 生产部署已持续运行3个月，服务数亿用户，峰值RPS达数万，P99延迟稳定控制在200ms以内。

### 与基线方法对比结果

#### （1）端到端性能（Figure 13 & 14）
- 所有测试配置下，xGR均显著优于vLLM和xLLM。
- 随着RPS增加，基线延迟急剧上升并很快突破SLO；而xGR保持平缓增长。
- 在大beam width（如512）场景下，性能差距进一步拉大，xGR仍能维持稳定低延迟。

#### （2）内存效率（Figure 15 & 16）
- **固定输入长度1k，BW从128增至512**：
  - xGR内存基本稳定在~10GB；
  - xLLM从~15GB飙升至**46.3GB**（超线性增长）。
- **固定BW=256，输入长度从1k到3k**：
  - xGR峰值仅**12.0GB**；
  - xLLM始终维持约**30GB**，受序列长度影响大。

#### （3）Kernel效率（Figure 17）
- **延迟**：xAttention相较PagedAttention降低约**6.6×**。
- **计算吞吐**：提升达**7×**。
- **访存开销**：
  - PagedAttention内存管道忙时高达**93.4%**（严重memory-bound）；
  - xAttention降至**52%**，成功转为compute-bound，硬件利用率更高。

#### （4）消融实验（Figure 18）
使用OneRec-0.1B进行调度模块消融：
- **Baseline（仅xAttention+xBeam）**：随RPS上升延迟快速恶化。
- **+Valid Item Filtering**：几乎无额外开销，验证设备内过滤有效性。
- **+Kernel Graph Dispatch**：显著降低kernel launch开销，延迟下降明显。
- **+Multi-stream Execution**：实现host-device完全重叠，最终性能最佳。

#### （5）GPU平台可移植性验证（Figure 19）
- 在NVIDIA H800 GPU集群上，xGR同样显著优于vLLM。
- 表明即使拥有高带宽硬件，**单纯硬件升级无法解决GR特有挑战**，必须依赖系统级重构。

---

## 4. 关键结论和发现

### 主要发现
1. **生成式推荐的工作负载特性与通用LLM存在本质差异**：长prompt、短输出、大beam width、高并发、严SLO，决定了不能直接套用LLM推理系统。
2. **共享前缀的KV缓存复用是性能关键**：xAttention通过分离KV管理和分阶段计算，有效消除冗余访存，是性能提升的核心驱动力。
3. **算法-系统协同设计至关重要**：xBeam结合valid path constraint与early termination，在保证推荐质量的同时极大降低搜索开销。
4. **系统级重叠与并行不可忽视**：xSchedule通过multi-stream + kernel graph dispatch + host-device overlap，充分挖掘硬件潜力，尤其对小规模GR模型意义重大。
5. **xGR具备良好可移植性**：可在Ascend NPU和NVIDIA GPU上高效运行，得益于统一抽象和细粒度优化。

### 方法的局限性
- 当前假设**decode步数固定**（如3步生成一个item ID），可能限制更灵活的变长生成场景。
- **item mask需预先构建**，依赖业务侧提供合法item集合，对冷启动或动态新增item的支持需额外机制。
- 对**超大规模模型（>10B）** 的扩展性尚未充分验证。

### 未来工作方向
- 支持**动态长度生成**的beam search机制。
- 结合**缓存机制**（如IC-Cache）进一步加速重复用户请求。
- 探索**训练-推理联合优化**，例如结构化剪枝以减少搜索空间。
- 扩展至**多模态生成式推荐**场景（文本+图像+视频）。

--- 

> ✅ **总结一句话**：  
> xGR首次系统性重构了生成式推荐的服务范式，通过**operator-level（xAttention）、algorithm-level（xBeam）、system-level（xSchedule）** 三层协同优化，在真实生产环境中实现了**3.49×以上吞吐提升**，同时满足高并发下的严格低延迟要求，为下一代推荐系统基础设施提供了重要参考。

</details>

---

### 2. [A Multi-Criteria Automated MLOps Pipeline for Cost-Effective Cloud-Based Classifier Retraining in Response to Data Distribution Shifts](https://arxiv.org/abs/2512.11541)

**Authors**: Emmanuel K. Katalay, David O. Dimandja, Jordan F. Masakuna  
**Category**: cs.LG  
**Published**: 2025-12-15  
**Score**: 10.0  
**Type**: new  
**ArXiv ID**: 2512.11541v1  

#### Abstract
The performance of machine learning (ML) models often deteriorates when the underlying data distribution changes over time, a phenomenon known as data distribution drift. When this happens, ML models need to be retrained and redeployed. ML Operations (MLOps) is often manual, i.e., humans trigger the...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：A Multi-Criteria Automated MLOps Pipeline for Cost-Effective Cloud-Based Classifier Retraining in Response to Data Distribution Shifts

---

## 1. 论文的主要贡献和创新点

### 解决的问题
该论文针对**机器学习模型在生产环境中因数据分布漂移（data distribution drift）导致性能下降**的问题，提出了一种自动化解决方案。传统做法中，模型重训练通常依赖人工触发或固定周期执行，容易造成资源浪费或响应不及时。

### 提出的新方法/新思路
作者设计并实现了一个**多标准自动化的 MLOps Pipeline（Auto-MLOps）**，其核心思想是：
- 利用多种统计指标联合检测数据分布变化；
- 只有当检测到显著漂移时才触发模型重训练；
- 在保证模型性能的同时最小化云计算成本。

该 Pipeline 集成了 Continuous Integration / Continuous Deployment (CI/CD) 架构，并引入加权组合的漂移评分机制（Drift Score, DS），实现科学、可审计的重训练决策。

### 相比现有方法的优势
| 方面 | 优势 |
|------|------|
| **自动化程度** | 完全自动化的 CI/CD 流程，减少人工干预 |
| **成本效率** | 避免不必要的重训练，显著降低云资源消耗 |
| **检测精度** | 多指标融合（KS、KL、PSI、MMD、Accuracy/F1 变化）提升漂移识别可靠性 |
| **可解释性** | 提供“为何需要重训练”的诊断依据，增强运维透明度 |
| **与 CARA 的关系** | 是对 Cost-Aware Retraining Algorithm (CARA) 的扩展，增加了严格的漂移检测机制 |

---

## 2. 核心实验方法和设置

### 使用的数据集
实验基于多个基准异常检测数据集，涵盖网络、金融、医疗等领域，具体如下：

| 数据集 | 样本数 | 属性数 | 异常比例 (%) |
|--------|--------|--------|--------------|
| CICIOT [19] | 416,985 | 41 | 45% |
| CREDIT24 | 234,333 | 30 | 0.2% |
| ECG [10] | 4,998 | 140 | 58% |
| IDSS221 | 430,256 | 95 | 43% |
| KITSUNE18 | 210,171 | 116 | 23% |
| MVTec3 | 5,354 | 1,048,576 | 58% |
| Visa [26] | 10,821 | 95 | 11% |

> 注：MVTec3 为高维图像数据，其余多为结构化或时间序列数据。

### 实验设置
- **模型架构**：采用 Auto-Encoder (AE) 进行异常检测任务
- **超参数配置**：
  - Weight decay: 1e-6
  - Batch size: 64
  - Epochs: 100
  - Early stopping patience: 5
  - Learning rate: 1e-3
  - Optimizer: Adam
- **漂移模拟**：通过逐步引入分布变化来模拟真实场景中的 drift progression

### 评估指标
- **Accuracy**：衡量模型预测正确率
- **F1-score**：平衡 precision 与 recall，适用于类别不平衡场景
- **Cloud cost**：估算每次重训练所消耗的云资源开销
- **Retraining frequency**：单位时间内触发重训练的次数
- **Drift Score (DS)**：综合多个统计量的加权得分，用于判断是否重训练

### 基线方法对比
共比较四种策略：
1. **STATIC**：从不重训练（无任何更新）
2. **FIXED**：按固定周期重训练（如每周一次），无视实际漂移
3. **NAIVE**：一旦检测到漂移即重训练，无成本优化
4. **Auto-MLOps**（本文方法）：仅在 DS > 阈值 T 时触发重训练，兼顾性能与成本

---

## 3. 主要实验结果和性能指标

### 关键性能数据（见 Table 2）

| 模型 | Accuracy (mean ± std) | Cloud Cost (mean ± std) | Retraining Frequency (mean ± std) |
|------|------------------------|--------------------------|------------------------------------|
| STATIC | 0.69 ± 0.20 | 54.8 ± 5.6 | 0 ± 0 |
| FIXED | 0.75 ± 0.02 | 160.6 ± 10.5 | 3 ± 0 |
| NAIVE | 0.75 ± 0.03 | 130.5 ± 14.9 | 4.3 ± 1.9 |
| **Auto-MLOps** | **0.75 ± 0.03** | **108.1 ± 14.3** | **1.4 ± 1.2** |

### 与基线方法的对比结果
- **性能方面**：
  - Auto-MLOps 与 FIXED 和 NAIVE 达到相同的最高准确率水平（~0.75），显著优于 STATIC（0.69）
  - 表明该方法能有效维持模型性能
- **成本方面**：
  - 成本比 FIXED 低约 **32.5%**（160.6 → 108.1）
  - 比 NAIVE 低约 **17.2%**（130.5 → 108.1）
- **重训练频率**：
  - 平均仅 **1.4 次**，远低于 NAIVE 的 4.3 次，说明避免了冗余训练

### 图形分析支持
- **图2（Accuracy vs Drift Severity）**：随着漂移加剧，STATIC 性能持续下降，而 Auto-MLOps 能稳定保持高精度。
- **图3（Time-series Comparison）**：Auto-MLOps 在保持 accuracy 接近 0.75 的同时，cloud cost 上升最缓慢，体现出优异的成本效益。

> ❗未报告消融实验（ablation study），无法验证各统计指标（KS、KL、PSI 等）的具体贡献权重。

---

## 4. 关键结论和发现

### 主要发现
1. **多标准漂移检测机制显著提升 MLOps 效率**  
   结合 PSI、KL Divergence、KS Test、MMD 和性能变化的加权 Drift Score，能够更精准地识别真正影响模型表现的分布变化。

2. **自动化决策可在不牺牲性能的前提下大幅降低成本**  
   Auto-MLOps 在达到与 FIXED/NAIVE 相同 accuracy 的情况下，将云成本降低了 17%-32%，证明其具备良好的经济价值。

3. **静态或盲目重训练策略不可持续**  
   - STATIC 导致模型快速退化；
   - FIXED 和 NAIVE 存在大量无效计算，资源利用率低下。

4. **适应性重训练是未来 MLOps 发展方向**  
   将 drift detection 与 CI/CD pipeline 深度集成，是构建可持续、可扩展 ML 系统的关键。

### 方法的局限性
- **依赖统计测试的敏感性**：若阈值设置不当，可能导致 false positive（误报漂移）或 false negative（漏检）。
- **缺乏动态权重调整机制**：当前 DS 中各指标权重为经验设定，未随环境自适应调整。
- **未考虑概念漂移（concept drift）的复杂类型**：主要关注输入分布变化，对 label 分布演变建模不足。
- **缺少消融实验**：未能量化不同 drift metric 的相对重要性。

### 未来工作方向
1. 引入 **Reinforcement Learning (RL)** 来学习最优 retraining policy，从历史部署中自动优化触发时机。
2. 集成 **real-time cloud pricing fluctuation** 模型，进一步提升 cost-efficiency。
3. 扩展至更多任务类型（如 regression、NLP），验证通用性。
4. 开发可视化监控面板，辅助 human-in-the-loop 审核与调试。

---

> ✅ **总结一句话**：  
> 本文提出的 Auto-MLOps Pipeline 通过多标准漂移检测 + 自动化 CI/CD 触发机制，在保障分类器性能的同时显著降低云上重训练成本，为工业级 MLOps 提供了一个高效、可靠且经济可行的实践范式。

</details>

---

### 3. [RollMux: Phase-Level Multiplexing for Disaggregated RL Post-Training](https://arxiv.org/abs/2512.11306)

**Authors**: Tianyuan Wu, Lunxi Cao, Yining Wei, Wei Gao, Yuheng Zhao, Dakai An, Shaopan Xiong, Zhiqiang Lv, Ju Huang, Siran Yang, Yinghao Yu, Jiamang Wang, Lin Qu, Wei Wang  
**Category**: cs.DC  
**Published**: 2025-12-15  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2512.11306v1  

#### Abstract
Rollout-training disaggregation is emerging as the standard architecture for Reinforcement Learning (RL) post-training, where memory-bound rollout and compute-bound training are physically disaggregated onto purpose-built clusters to maximize hardware efficiency. However, the strict synchronization ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：ROLLMUX: Phase-Level Multiplexing for Disaggregated RL Post-Training

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

在大规模语言模型（LLM）的强化学习后训练（Reinforcement Learning, RL post-training）中，**rollout-training disaggregation** 已成为标准架构。该架构将内存密集型的 rollout 阶段与计算密集型的 training 阶段分别部署在专用集群上，以提升硬件利用率。

然而，由于 on-policy 算法要求严格的同步性，两个阶段必须串行执行，导致一个集群空闲等待另一个完成，形成严重的 **dependency bubbles（依赖气泡）**，造成资源严重浪费。传统异步方法（如 AReaL、StreamRL）虽能提高利用率，但会引入样本过期（sample staleness），损害模型收敛性和准确性。

---

### **提出了什么新方法或新思路**

论文提出 **ROLLMUX**，一种面向解耦式 RL 后训练的跨集群调度框架，通过 **相位级多路复用（phase-level multiplexing）** 来回收这些“依赖气泡”。

其核心思想是：
> 利用一个任务的空闲时间（如 rollout 完成后等待 training 的时间），去运行另一个任务的活跃阶段（如 training 或 rollout），从而实现多个 RL 任务之间的高效交错执行。

为此，ROLLMUX 引入了以下关键机制：

#### **(1) Co-Execution Group 抽象**
- 将多个 RL 任务组织为 **共执行组（co-execution group）**，共享一组 rollout 和 training 资源池。
- 组内任务通过时间复用方式交替执行 rollout 和 training 阶段。
- 该抽象实现了调度问题的分解，使全局优化变为可管理的子问题。

#### **(2) 两级调度架构**
- **Inter-Group Scheduler（组间调度器）**  
  决定新任务应加入哪个共执行组或是否新建组。采用保守的随机规划策略，在满足 SLO 和内存约束的前提下最小化边际资源成本。
- **Intra-Group Scheduler（组内调度器）**  
  在组内实施 **轮转调度（round-robin）**，证明其在非过载组中是资源利用率最优的策略。

#### **(3) Warm-Start 上下文切换机制**
- 通过限制每组任务数量，确保所有模型状态（weights、optimizer states）始终缓存在主机 DRAM 中。
- 上下文切换时直接从本地内存加载，避免跨网络或磁盘冷启动，**将切换延迟降低两个数量级**（从最高 80 秒降至 <1 秒）。

#### **(4) 长尾迁移（Long-Tail Migration）**
- 动态检测 rollout 阶段中的“长尾请求”（stragglers），将其迁移到少量 GPU 上继续处理。
- 释放其余 GPU 立即启动下一任务的 rollout，实现流水线并行，有效应对生成长度的长尾分布。

#### **(5) 拓扑感知模型同步（Topology-Aware Sync）**
- 改进传统的 flat AllGather 同步方式，采用两阶段同步：
  1. **跨集群分片传输**：仅传输一份完整参数副本（via P2P）
  2. **组内广播**：利用高速 NVLink/InfiniBand 进行内部扩散
- 显著减少跨集群通信开销。

---

### **相比现有方法的优势**

| 方面 | ROLLMUX | 现有方法（如 Solo-D, veRL） |
|------|--------|-----------------------------|
| **资源利用率** | 高，通过相位交错填充依赖气泡 | 低，存在大量空闲周期 |
| **成本效率** | 成本降低 1.84×（vs. Solo-D），1.38×（vs. veRL） | 成本高，尤其在昂贵 GPU 上运行 rollout |
| **SLO 保障** | 100% 达标 | 异步方法难以保证 |
| **上下文切换开销** | 极低（warm-start） | 冷启动耗时高达 80 秒 |
| **算法兼容性** | 支持任意 on-policy RL 算法（PPO、GRPO 等） | 多数需修改为 off-policy 才能提升利用率 |

---

## 2. 核心实验方法和设置

### **使用的数据集与工作负载**

- 基于真实生产环境采集的 **两周轨迹数据**，包含 **200 个异构 RL 任务**。
- 模型规模覆盖 **3B–32B**，最大输出长度 **4K–32K tokens**，平均响应长度 12.1K。
- 数据集涵盖数学推理（DeepMath-103K）、软件工程（SWE-bench）、游戏交互等。
- 微基准测试使用 Qwen 系列模型（7B–32B），定义五类典型任务（见 Table 3）。

---

### **实验设置**

- **硬件平台**：
  - **Rollout Cluster**：328 × NVIDIA H20（低成本、高带宽）
  - **Training Cluster**：328 × NVIDIA H800（高性能、高算力）
  - 两集群通过 **20 Gbps Ethernet** 连接，内部为 400 Gbps InfiniBand。
- **评估指标**：
  - **总资源供给成本（Provisioning Cost）**（$/h）
  - **SLO 达成率**（Slowdown ≤ SLO）
  - **GPU 利用率 / 依赖气泡减少比例**
  - **端到端吞吐量（Throughput per Dollar）**
  - **决策延迟与可扩展性**

---

### **基线方法对比**

| 基线 | 描述 |
|------|------|
| **Solo Disaggregation (Solo-D)** | 标准解耦架构，每个任务独占 rollout 和 training 资源，无时间复用 |
| **veRL [41]** | 单体架构，所有阶段运行在 H800 上，避免跨集群瓶颈但存在硬件错配 |
| **Gavel+ [29]** | 支持异构调度的增强版 Gavel，支持 RL 场景但无法进行相位级交错 |
| **Random / Greedy / Offline Optimal** | 用于模拟研究的对比调度策略 |

---

## 3. 主要实验结果和性能指标

### **关键性能数据**

| 指标 | ROLLMUX 表现 |
|------|-------------|
| **平均资源成本** | **$510/h** |
| **SLO 达成率** | **100%** |
| **rollout 集群依赖气泡减少** | **24.4%** |
| **training 集群依赖气泡减少** | **43.1%** |
| **峰值 H800 使用量** | **152**（vs. 328 in baselines）→ **2.16× 减少** |
| **峰值 H20 使用量** | **216**（vs. 328 in Solo-D）→ **1.52× 减少** |

---

### **与基线方法的对比结果**

| 对比项 | ROLLMUX vs. Solo-D | ROLLMUX vs. veRL |
|--------|--------------------|------------------|
| **成本效率提升** | **1.84×** 更便宜 | **1.38×** 更便宜 |
| **训练 GPU 利用率** | 显著提升（减少 idle time） | 更优，因 rollout 被卸载至 H20 |
| **rollout 性能** | 更高性价比（H20 成本仅为 H800 的 ~1/3） | 同样受益于 disaggregation |
| **SLO 保障能力** | 严格满足 | veRL 可能满足，但成本更高 |

> 图 13 显示，在真实 workload 下，ROLLMUX 的成本曲线远低于 baselines，且 GPU 使用更紧凑。

---

### **消融实验结果**

#### **(1) 长尾迁移（Long-Tail Migration）**
- 在 rollout 存在长尾请求时，启用迁移可将端到端吞吐提升 **1.06× 至 1.28×**。
- 效果随序列长度增加而增强（如 14B-8K 模型收益最大）。

#### **(2) 拓扑感知同步（Topology-Aware Sync）**
- 单节点同步（8H800 → 8H20）：**加速 7.87×–8.33×**
- 多节点同步（16H800 → 16H20）：仍保持 **2.62×–2.75× 加速**
- 因为只传一份参数副本，大幅缓解跨集群带宽瓶颈。

#### **(3) Warm-Start 机制**
- 如图 4 所示，warm-start 切换延迟比 cold-start 降低 **48×**。
- 使得细粒度的时间复用成为可能，否则切换开销将抵消调度增益。

---

## 4. 关键结论和发现

### **主要发现**

1. **相位级多路复用是解耦 RL 架构的关键突破口**  
   通过将调度粒度从“任务级”细化到“相位级”，ROLLMUX 成功利用了原本被浪费的依赖气泡，显著提升了整体资源利用率。

2. **两级调度 + Warm-Start 是实现高效复用的基础**  
   - 组间调度基于成本最小化决策，避免盲目扩容；
   - 组内 round-robin 被理论证明为最优；
   - warm-start 机制解决了上下文切换的性能瓶颈。

3. **动态适应性至关重要**  
   长尾迁移机制使系统能够应对 RL rollout 的高度不确定性，进一步提升实际吞吐。

4. **ROLLMUX 在真实生产场景中表现卓越**  
   在包含 200 个异构任务的真实 trace 上，实现了 **1.84× 成本节省**，同时 **100% 满足 SLO**，验证了其工业级实用性。

---

### **方法的局限性**

1. **受限于节点内存容量**  
   每个 co-execution group 最多只能容纳 2–5 个任务（取决于模型大小），限制了打包灵活性。

2. **对极端异构任务组合敏感**  
   若任务间 phase duration 差异过大，可能导致调度效率下降（尽管 inter-group scheduler 已尽力规避）。

3. **假设 SLO 可预测**  
   虽然采用保守估计（worst-case token length）来保障 SLO，但在极端动态环境下仍可能存在风险。

4. **未考虑跨组干扰或故障传播**  
   当前设计假设组间完全隔离，但大规模部署中网络拥塞等问题可能影响实际性能。

---

### **未来工作方向**

1. **支持更大规模的共执行组**  
   探索更高效的模型状态压缩或分层缓存机制，突破内存限制。

2. **引入预测驱动的调度**  
   结合 LLM 输出长度预测模型，替代保守的最大长度假设，进一步优化资源分配。

3. **扩展至 off-policy 或混合策略任务**  
   当前聚焦 on-policy，未来可探索对异步任务的支持，实现统一调度框架。

4. **集成 intra-job 优化技术**  
   如 speculative decoding、parameter relocation 等，与 ROLLMUX 形成协同优化。

5. **支持 MoE（Mixture-of-Experts）模型调度**  
   解耦专家并行（Expert Parallelism）与 phase-level multiplexing 的结合是一个重要方向。

---

> ✅ **总结一句话**：  
> **ROLLMUX 通过相位级调度、warm-start 快速切换和拓扑感知同步，在不解耦 on-policy 正确性的前提下，实现了高达 1.84× 的成本节约，是解耦式 RL 训练系统的一次重大突破。**

</details>

---

### 4. [A-LAMP: Agentic LLM-Based Framework for Automated MDP Modeling and Policy Generation](https://arxiv.org/abs/2512.11270)

**Authors**: Hong Je-Gal, Chan-Bin Yi, Hyun-Suk Lee  
**Category**: cs.AI  
**Published**: 2025-12-15  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2512.11270v1  

#### Abstract
Applying reinforcement learning (RL) to real-world tasks requires converting informal descriptions into a formal Markov decision process (MDP), implementing an executable environment, and training a policy agent. Automating this process is challenging due to modeling errors, fragile code, and misali...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# A-LAMP: Agentic LLM-Based Framework for Automated MDP Modeling and Policy Generation —— 核心总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
传统强化学习（RL）在现实任务中的应用面临三大挑战：
- **MDP建模困难**：将非正式的自然语言任务描述转化为形式化的马尔可夫决策过程（MDP）需要专家进行状态、动作、奖励等要素的设计，过程繁琐且易出错。
- **环境实现脆弱**：从MDP到可执行环境的编码过程容易因小错误导致训练失败或策略偏离目标。
- **缺乏灵活性与可扩展性**：任务需求变更时（如优化目标由“最大化性能”变为“最小化能耗”），需重新手动设计MDP并重训策略，难以快速响应。

这些问题严重阻碍了RL在工业自动化、网络资源调度、供应链管理等领域的广泛应用。

---

### 提出的新方法与思路
作者提出 **A-LAMP**（Agentic LLM-Based Framework for Automated MDP Modeling and Policy Generation），一个基于多智能体大模型（multi-agent LLM）的模块化框架，用于**全自动地从自由形式的自然语言描述生成MDP模型和可训练的RL策略**。

其核心思想是：
- 将整个政策生成流程分解为多个阶段，并由专门的LLM代理（Agent）负责每个子任务；
- 各代理之间按依赖顺序协作，确保语义一致性；
- 所有中间输出均为人类可读的形式（如JSON、LaTeX公式、代码），保证透明性和可验证性。

#### 主要组件包括：
| Agent | 功能 |
|-------|------|
| Parameter Agent | 提取任务中已知参数（如用户数、带宽） |
| Objective Agent | 明确优化目标（如“最大化系统吞吐量”） |
| Variable Agent | 识别决策变量（如“被调度的用户”）和系统变量 |
| Constraint Agent | 形式化约束条件（如“每时隙只能调度一个用户”） |
| Modeling Agent | 构建数学化的MDP目标函数与约束 |
| SAR Agent | 定义状态空间（State）、动作空间（Action）、奖励函数（Reward） |
| Env Agent & Coding Agent | 生成可执行的Gym风格环境代码与DQN训练循环 |
| Code Executor | 运行代码并反馈错误以支持自动调试 |

此外，引入了一个**Error Correction Module**，允许代理自我检查输出置信度，必要时请求人工澄清，提升鲁棒性。

---

### 相比现有方法的优势
| 维度 | A-LAMP | 单一LLM（如GPT-4o） |
|------|--------|------------------|
| **建模可靠性** | 分步推理降低认知负担，提高准确性 | 需一次性完成复杂推理，易遗漏细节 |
| **可解释性** | 每个阶段输出结构化、可审查 | 黑箱式端到端生成，难追溯错误来源 |
| **适应性** | 支持通过更新描述自动重构MDP与策略 | 修改后常需重新提示，效果不稳定 |
| **对小模型友好** | 轻量版Light A-LAMP可在Gemma3-27B上运行，接近GPT-4o表现 | 小模型难以独立完成全流程 |
| **成功率** | 显著高于单模型基线，尤其在定制环境任务上 | 编码与训练失败率高 |

> ✅ **核心优势总结**：A-LAMP通过**结构化分工 + 可验证中间表示 + 错误修正机制**，实现了更可靠、可解释、可复用的端到端RL自动化。

---

## 2. 核心实验方法和设置

### 使用的数据集（Benchmark Tasks）
共测试5个任务，涵盖经典控制与领域特定优化：

| 任务 | 类型 | 是否需自定义环境 |
|------|------|----------------|
| Cart-pole | 经典控制 | 否（已有Gym环境） |
| Mountain-car | 经典控制 | 否 |
| Wireless | 无线通信资源分配 | 是（Shannon容量公式建模） |
| Drone-delivery (Drone-del.) | 网格路径规划 | 是 |
| Inventory-management (Inv.-mgmt.) | 零售库存优化 | 是 |

所有任务仅提供**自由格式的自然语言描述**作为输入，无其他辅助信息。

---

### 实验设置
- **主干算法**：统一采用 **Deep Q-Network (DQN)** 作为RL训练算法（适用于离散动作空间）。
- **运行次数**：每种方法在每个任务上运行20次，统计成功率。
- **评估指标**：
  1. **Modeling Success Rate**：提取的状态、动作、奖励是否逻辑正确且完整。
  2. **Coding Success Rate**：生成代码能否在标准Python环境中无语法错误执行。
  3. **Policy Generation Success Rate**：RL训练是否收敛至满足任务目标的最优策略（最关键指标）。

---

### 基线方法对比
| 方法 | 描述 |
|------|------|
| **A-LAMP (GPT-4o)** | 完整框架 + 最强LLM |
| **Light A-LAMP (Gemma3-27B)** | 轻量版本，使用较小LLM |
| **GPT-4o (Single Model)** | 单一LLM直接生成完整训练代码 |
| **Gemma3-27B (Single Model)** | 同上，但使用小模型 |
| **A-LAMP w/o EC** | 移除Error Correction模块的消融版本 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 1）

| Task | A-LAMP | A-LAMP w/o EC | Light A-LAMP | GPT-4o | Gemma3-27B |
|------|--------|---------------|--------------|--------|------------|
| **Cart-pole** | 1.00 / 0.95 / **0.95** | – | 1.00 / 0.85 / **0.45** | 1.00 / 0.75 / **0.45** | 1.00 / 0.60 / **0.35** |
| **Mountain-car** | 1.00 / 1.00 / **0.75** | – | 0.95 / 0.70 / **0.55** | 1.00 / 1.00 / **0.40** | 1.00 / 0.35 / **0.30** |
| **Wireless** | 1.00 / 1.00 / **0.45** | 0.90 / 0.80 / **0.40** | 0.95 / 0.60 / **0.15** | 0.80 / 0.90 / **0.20** | 0.55 / 0.65 / **0.05** |
| **Drone-del.** | 0.80 / 0.95 / **0.45** | 0.65 / 0.75 / **0.30** | 0.55 / 0.50 / **0.15** | 0.35 / 0.55 / **0.10** | 0.40 / 0.05 / **0.00** |
| **Inv.-mgmt.** | 1.00 / 0.55 / **0.30** | 1.00 / 0.40 / **0.20** | 0.85 / 0.25 / **0.05** | 0.65 / 0.05 / **0.05** | 0.60 / 0.00 / **0.00** |

> 注：三元组分别为 Modeling / Coding / **Policy Generation** 成功率

---

### 与基线方法的对比结果
- 在所有任务中，**A-LAMP均取得最高的Policy Generation Success Rate**。
- 在需要自定义环境的任务（Wireless, Drone-del., Inv.-mgmt.）上，A-LAMP的成功率几乎是单一LLM方法的**两倍以上**。
- **Light A-LAMP（基于Gemma3-27B）的表现远超单独使用Gemma3-27B**，甚至接近GPT-4o水平，说明性能增益主要来自**框架设计而非模型规模**。

---

### 消融实验结果（A-LAMP w/o EC）
- 移除Error Correction模块后，性能略有下降，但在大多数任务中仍显著优于单模型方法。
- 在复杂任务（如Drone-delivery）上，启用EC可进一步提升成功率（例如Policy Generation从0.30→0.45），表明**轻量级人机交互能有效增强系统鲁棒性**。

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **结构化多代理分解显著提升建模可靠性**：相比单LLM“端到端幻觉”，分阶段专业化代理能更准确地捕捉任务语义，减少建模偏差。
2. ✅ **语义对齐贯穿全流程**：通过中间可验证输出（JSON、LaTeX、代码），确保从自然语言到可执行策略的全过程保持一致。
3. ✅ **即使轻量模型也能胜任复杂任务**：Light A-LAMP证明该框架可部署于资源受限场景，具备良好泛化能力。
4. ✅ **生成策略具有任务最优性**：在Wireless任务中，A-LAMP生成的DQN策略性能逼近理论最优的贪婪调度器（见Figure 4），验证了其正确性与有效性。

---

### 方法的局限性
- **编码阶段仍较脆弱**：尽管有反馈调试机制，但语法或结构不匹配仍可能导致执行失败（尤其在Inv.-mgmt.任务中Coding成功率仅为55%）。
- **依赖高质量LLM基础能力**：若底层LLM无法理解领域知识（如Shannon公式），则建模可能出错。
- **未处理连续动作空间或高维状态**：当前实验集中在离散动作任务，尚未验证在MuJoCo等复杂控制任务上的表现。

---

### 未来工作方向
1. 引入更细粒度的**编码代理模块**（如专用于环境构建、训练循环生成）以提升代码稳定性。
2. 加入**自适应超参数调优与验证机制**，减少人工干预。
3. 融合**领域先验知识与结构化知识库**，增强对专业术语的理解。
4. 探索支持**连续动作空间（如DDPG、SAC）与POMDP建模**的能力，拓展适用范围。

---

## 总结
A-LAMP提出了一种**模块化、可解释、可验证的多代理LLM框架**，成功实现了从自然语言描述到可执行RL策略的全自动化流程。实验证明其在建模精度、代码质量与策略成功率方面全面超越单一LLM方法，特别是在需要自定义环境的复杂任务中优势明显。该工作为推动RL在真实世界的大规模落地提供了强有力的自动化工具链支持。

</details>

---

### 5. [Motif-2-12.7B-Reasoning: A Practitioner's Guide to RL Training Recipes](https://arxiv.org/abs/2512.11463)

**Authors**: Junghwan Lim, Sungmin Lee, Dongseok Kim, Taehyun Kim, Eunhwan Park, Jeesoo Lee, Jeongdoo Lee, Junhyeok Lee, Wai Ting Cheung, Dahye Choi, Minsu Ha, Jaeheui Her, Jaeyeon Huh, Hanbin Jung, Changjin Kang, Beomgyu Kim, Minjae Kim, Taewhan Kim, Youngrok Kim, Hyukjin Kweon, Haesol Lee, Kungyu Lee, Dongpin Oh, Yeongjae Park, Bokki Ryu, Dongjoo Weon  
**Category**: cs.AI  
**Published**: 2025-12-15  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2512.11463v1  

#### Abstract
We introduce Motif-2-12.7B-Reasoning, a 12.7B parameter language model designed to bridge the gap between open-weight systems and proprietary frontier models in complex reasoning and long-context understanding. Addressing the common challenges of model collapse and training instability in reasoning ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Motif-2-12.7B-Reasoning: A Practitioner's Guide to RL Training Recipes*

---

## 1. 论文的主要贡献和创新点

### 解决的问题
该论文旨在解决当前**开放权重语言模型在复杂推理和长上下文理解能力上显著落后于闭源前沿模型**的问题。具体挑战包括：
- **训练不稳定**：在引入强化学习微调（RLFT）后常出现模型崩溃（model collapse）或性能退化。
- **分布不匹配**（Distribution Mismatch）：使用大模型生成的合成数据进行监督微调（SFT）时，因推理风格与学生模型不一致而导致性能下降。
- **长上下文训练效率低**：处理64K token级别的上下文对内存和计算资源要求极高。
- **RLFT样本效率低且难以稳定优化**。

### 提出的新方法与创新思路
论文提出了一个**系统性、可复现的完整训练配方**（training recipe），涵盖系统、数据与算法三个层面的优化，主要包括以下创新：

#### （1）两阶段、分布对齐的 SFT 课程设计（Two-stage Distribution-Aligned SFT Curriculum）
- **Stage 1（Reasoning Foundation）**：构建高质量多领域数据集（数学、代码、STEM、工具使用），通过难度筛选与执行验证确保数据质量。
- **Stage 2（Deep Reasoning Specialization）**：注入高粒度合成数据（如Chain-of-Thought轨迹），并重新生成推理路径以**对齐目标模型的推理分布**，避免“分布不匹配”问题。

> ✅ 创新点：强调“推理分布对齐”而非单纯追求数据量，提出“质量优于数量”原则。

#### （2）稳定的 RLFT 流水线设计
- **LLM-as-a-data-filtering**：利用预训练检查点对初始问题池进行采样与 pass rate 统计，仅保留中等难度样本（α ≤ p(x) ≤ β），防止梯度消失。
- **Mixed-Policy Trajectory Reuse**：固定一批rollout轨迹用于多个梯度更新步骤，提升训练效率并增强稳定性。
- **Multi-task RLFT**：联合优化数学、编码与指令遵循任务，缓解单任务RL导致的跨任务性能退化（catastrophic forgetting）。
- **取消长度惩罚**（Length Penalty）：鼓励模型生成更长的推理链，提升复杂任务表现。

#### （3）高效的系统级优化
- **Hybrid Parallelism 架构**：结合 DeepSpeed-Ulysses 的 Sequence Parallelism 和多种 DP/TP 配置，支持64K上下文训练。
- **Liger Kernel Loss Function**：将 logits 沿 context dimension 分片计算损失，显著降低 RL 训练中的峰值显存占用。
- **细粒度激活检查点**（Fine-grained Activation Checkpointing）：手动调优每层重计算策略，平衡内存与计算开销。

### 相比现有方法的优势
| 方面 | 传统做法 | Motif-2-12.7B 方法 |
|------|--------|------------------|
| SFT 数据 | 直接使用大模型生成的合成数据 | 强调**分布对齐**，过滤不兼容轨迹 |
| RLFT 稳定性 | 易发生模型崩溃或性能下降 | 通过难度过滤 + 轨迹复用实现稳定收敛 |
| 上下文扩展 | 突然拉长至长上下文 | 渐进式 curriculum（16K → 32K → 64K） |
| 计算效率 | 全量 on-policy rollout 成本高 | Mixed-policy + trajectory reuse 提升样本效率 |
| 系统支持 | 缺乏针对长序列的优化 | Liger Kernel + Hybrid Parallelism 支持高效64K训练 |

---

## 2. 核心实验方法和设置

### 使用的数据集
| 类型 | 数据来源 | 描述 |
|------|---------|------|
| **SFT 阶段** | Nemotron-Post-Training-Dataset, OpenReasoningDataset, Mixture-of-Thoughts, rstar-coder | 多领域推理数据，经严格清洗与验证 |
| **数学推理** | GURU-92K | 包含组合、代数、几何等子领域，经分层抽样平衡分布 |
| **代码生成** | GURU-92K 中的编程题 | 同样经过 pass rate 过滤与难度控制 |
| **指令遵循** | 自建合成数据集（10K样本） | 专为挑战当前能力边界设计 |
| **评估基准** | MMLU-Pro, GPOA Diamond, Humanity's Last Exam, LiveCodeBench, SciCode, AIME 2025, IFBench, AA-LCR, Terminal-Bench Hard, Telecom Bench | 构成综合指标 AAII |

### 实验设置
- **基础模型**：Motif-2-12.7B-Instruct
- **参数规模**：12.7B
- **上下文长度**：最终支持 **64K tokens**
- **硬件平台**：H100 GPU 集群，使用 SkyPilot 进行资源调度
- **并行策略**：Hybrid Parallelism（Sequence Parallelism + DP-shard + DP-replicate）
- **RL算法**：GSPO（Group Sequence Policy Optimization），critic-free 的 PPO 变体
- **训练流程**：
  1. 两阶段 SFT（逐步扩展上下文）
  2. RLFT 前先进行数据难度过滤
  3. 多任务混合训练，轨迹复用 S=3 步

### 评估指标
- 主要指标：**Artificial Analysis Intelligence Index (AAII)** —— 由10项多样化 benchmark 的平均得分构成。
- 其他关键指标：
  - 数学：AIME 2025 Pass@1
  - 编程：LiveCodeBench v5 Pass@1
  - 推理一致性、长上下文连贯性、工具调用成功率等

### 基线方法对比
- **闭源模型**：GPT-5.1（文中称 Motif 超越其表现）
- **开源模型**：各类 30–40B 参数级 reasoning 模型（如 DeepSeek-Math, Qwen-Max 等）
- 对比重点：在远小于对手参数量的情况下，是否能达到甚至超越其推理性能。

---

## 3. 主要实验结果和性能指标

### 关键性能数据
| 模型 | 参数量 | AAII 得分 | 备注 |
|------|--------|----------|------|
| **Motif-2-12.7B-Reasoning** | **12.7B** | **78.4** | 超过 GPT-5.1 |
| GPT-5.1 | ~未知 | 76.2 | 当前最强闭源之一 |
| 其他领先开源模型 | ≥30B | <78.0 | 无一低于12.7B仍达此水平 |

#### 细项性能亮点：
- **LiveCodeBench v5 Pass@1**：
  - Baseline: 51.78
  - + seed-oss-36b 数据: **63.69** (+11.91)
  - + gpt-oss-120b 数据: **33.92** (↓17.86) → 证明分布不匹配的危害
- **AIME 2025**：相比 base model 显著提升，在中等难度问题上优势明显。
- **长上下文测试**：在 32K–64K token 输入下保持推理一致性，优于多数同类模型。

### 与基线方法的对比结果
- 在 **AAII 总分上超过 GPT-5.1**，成为目前唯一一个在该综合指数上领先的 **<13B 参数模型**。
- 性能媲美甚至超越多个 **30–40B 参数的闭源与开源模型**。
- 在数学与编程任务上的增益尤为显著，表明其推理能力真正得到增强，而非泛化偏差。

### 消融实验结果（Ablation Studies）
虽然未提供完整表格，但从文中分析可提炼出以下关键消融发现：

| 组件 | 移除后的后果 | 结论 |
|------|-------------|------|
| 分布对齐的合成数据 | 使用 gpt-oss-120b 数据导致性能大幅下降（↓17.86） | 数据来源必须与学生模型能力匹配 |
| 难度过滤机制（LLM-as-a-filter） | 出现零优势（zero advantage）现象，梯度消失 | 中等难度样本是有效学习的前提 |
| 轨迹复用（Trajectory Reuse） | 训练波动剧烈，收敛困难 | 显著提高训练稳定性与效率 |
| 多任务 RL | 单任务 RL 导致其他任务性能退化 | Multi-task 是防止灾难性遗忘的关键 |
| 取消长度惩罚 | 模型倾向于生成更完整的中间推理步骤 | 有利于复杂推理，不应盲目套用通用文本生成规则 |

---

## 4. 关键结论和发现

### 主要发现
1. **参数规模并非决定推理能力的唯一因素**：通过精心设计的训练流程，12.7B 模型也能达到甚至超越更大模型的表现。
2. **SFT 阶段的数据质量与分布对齐至关重要**：盲目使用“更强教师模型”的输出反而有害。
3. **RLFT 可以稳定且有益地应用于小模型**：前提是采用难度过滤、轨迹复用、多任务正则等工程技巧。
4. **系统优化是支撑长上下文训练的基础**：Liger Kernel 与 Hybrid Parallelism 使得 64K 上下文下的 RLFT 成为可能。
5. **test-time scaling（如思维链延长）与 RLFT 相辅相成**：RL 训练增强了模型探索深层推理的能力。

### 方法的局限性
- **依赖高质量的验证机制**：如代码执行、数学答案校验等，限制了可扩展到更多非形式化任务的能力。
- **数据过滤成本较高**：需要额外运行 rollouts 来估计 pass rate，增加预处理开销。
- **对基础设施要求高**：尽管强调“现实预算”，但仍需 H100 级别集群与高级并行技术支持。
- **未公开所有训练细节**：例如确切的学习率调度、batch size、总训练步数等仍不够透明。

### 未来工作方向
- 将该训练范式迁移到更大或更小规模模型，验证其可扩展性。
- 探索自动化的 curriculum 设计与动态难度调整机制。
- 扩展到更多非结构化推理任务（如社会科学、伦理判断等）。
- 开发更低显存消耗的 RLFT 实现方案，进一步降低训练门槛。
- 推动社区共建“分布对齐”的合成数据标准与共享生态。

---

> 📌 **总结一句话**：  
> *Motif-2-12.7B-Reasoning* 证明了**通过系统性的工程优化与训练设计，小型开放模型也能在复杂推理任务上媲美甚至超越大型闭源模型**，为社区提供了极具实践价值的 RL 训练蓝图。

</details>

---

### 6. [AdaSD: Adaptive Speculative Decoding for Efficient Language Model Inference](https://arxiv.org/abs/2512.11280)

**Authors**: Kuan-Wei Lu, Ding-Yong Hong, Pangfeng Liu  
**Category**: cs.CL  
**Published**: 2025-12-15  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2512.11280v1  

#### Abstract
Large language models (LLMs) have achieved remarkable performance across a wide range of tasks, but their increasing parameter sizes significantly slow down inference. Speculative decoding mitigates this issue by leveraging a smaller draft model to predict candidate tokens, which are then verified b...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# AdaSD: Adaptive Speculative Decoding for Efficient Language Model Inference — 核心总结

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
大型语言模型（LLMs）虽然在多项任务上表现出色，但由于参数量巨大，推理过程变得**内存受限且计算缓慢**。传统的 **Speculative Decoding** 虽能通过小模型（draft model）生成候选 token 并由大模型（target model）并行验证来加速推理，但存在以下问题：
- 需要额外训练 draft model；
- 依赖大量超参数调优（如候选长度、接受阈值）；
- 接受标准过于严格或固定，难以适应不同上下文。

这些问题限制了其通用性和部署效率。

---

### 🚀 提出的新方法：AdaSD（Adaptive Speculative Decoding）
作者提出 **AdaSD**，一种无需超参数调优的自适应推测解码方案，核心创新如下：

#### （1）双自适应阈值机制
- **Generation Threshold（生成阈值）**：基于 draft model 输出 token 的 **Entropy** 动态决定何时停止生成候选序列。
  - 若当前 token 的熵超过历史拒绝 token 的平均熵，则终止生成。
- **Verification Threshold（验证阈值）**：基于 draft 和 target 模型输出分布之间的 **Jensen-Shannon Distance (JS Distance)** 决定是否接受候选 token。
  - 接受条件放宽为“分布相似”而非“token 完全匹配”，提升灵活性。

#### （2）实时动态更新机制
两个阈值均在推理过程中**在线更新**，利用已生成 token 的统计信息（熵与 JS 距离），无需预分析模型或任务特性。

#### （3）完全免训练、免调参
- 不需要对 draft 或 target 模型进行微调；
- 所有决策自动适应输入内容和模型行为，适用于任意现成（off-the-shelf）模型组合。

---

### 🔍 相比现有方法的优势
| 特性 | Vanilla SD | AssistedGen | BiLD/FSD | AdaSD |
|------|------------|-------------|----------|--------|
| 是否需训练 | 否 | 否 | 否 | ❌ 否 |
| 是否需调参 | 是（固定长度/阈值） | 是（ROC 曲线） | 是（固定阈值） | ✅ **否（全自动）** |
| 是否动态调整长度 | 否 | 是 | 否 | ✅ 是 |
| 是否动态调整接受标准 | 否 | 否 | 是（但需预设） | ✅ 是 |
| 兼容性 | 高 | 中 | 中 | 高 |

> AdaSD 在保持高兼容性的同时，实现了更全面的自适应控制。

---

## 2. 核心实验方法和设置

### 📚 数据集
使用三个代表性基准数据集，覆盖多种任务类型：
- **GSM8K**：小学数学应用题（推理密集型）
- **HumanEval**：编程与代码生成（逻辑性强）
- **MMLU**：多学科选择题（短输出、分类任务）

---

### ⚙️ 实验设置
#### 模型配置
测试三组 draft-target 模型对：
1. `Llama 3.1 70B`（target） + `Llama 3.1 8B`（draft）
2. `Llama 3.1 70B` + `Llama 3.2 1B`
3. `Qwen 2.5 72B` + `Qwen 2.5 7B`

所有模型来自同一族以确保共享词汇表（vocabulary alignment）。

#### 基线方法（Baseline）
- **Vanilla Speculative Decoding**：固定候选长度为 5。
- **AssistedGen**（Hugging Face 内置）：基于 DISCO 的无监督变体，动态判断生成停止时机。

#### 评估指标
| 指标 | 描述 |
|------|------|
| **tks/sec** | 解码吞吐量（tokens per second），衡量速度 |
| **Speedup** | 相对于 Vanilla 的加速比 |
| **Accuracy** | 任务正确率（exact match） |
| **JSDist @ threshold** | 验证时使用的 JS Distance 阈值水平 |
| **#cand / #match / AccRate** | 生成数、接受数、接受率 |

#### 实现细节
- 使用 Hugging Face Transformers（v4.55）
- 运行于 4×NVIDIA A6000 GPU
- 采用 sampling 策略（非 greedy）

---

## 3. 主要实验结果和性能指标

### 📊 性能汇总（以 Llama 为例）

| 方法 | GSM8K (speedup) | HumanEval (speedup) | MMLU (speedup) | Accuracy Drop |
|------|------------------|----------------------|----------------|---------------|
| **Vanilla** | 1.00× | 1.00× | 1.00× | — |
| **AssistedGen** | 1.16× | 1.27× | 1.01× | <0.5% |
| **AdaSD** | **1.26×** | **1.32×** | **1.09×** | <2% |

> 在 Llama 模型上，AdaSD 实现最高达 **49% 的加速**（见消融实验），显著优于基线。

---

### 🔬 关键性能数据亮点

#### （1）显著提速
- 在 `Llama 3.1 70B/8B` 上：
  - GSM8K：**1.26× speedup**
  - HumanEval：**1.32× speedup**
- 在 `Llama 3.1 70B/1B` 上：
  - 最高达 **1.49× speedup**（HumanEval）

#### （2）精度损失极小
- 所有任务中 accuracy 下降均 **< 2%**
- 尤其在 HumanEval 上仍保持 >73% 准确率

#### （3）MMLU 加速有限的原因分析
- MMLU 输出极短（通常仅几个 token），推测空间小 → 加速上限低（约 8–10%）
- 符合预期，说明 AdaSD 更适合长序列生成场景

#### （4）Qwen 模型上的表现差异
- AdaSD 在 Qwen 上略逊于 AssistedGen（1.28 vs 1.29 speedup）
- 原因：AdaSD 生成更长候选序列（~14.5 tokens），导致 draft 开销增加
- 但接受率更高（72% vs 83%），表明其验证策略有效

---

### 🔍 消融实验（Ablation Study）

| 变体 | 方法描述 | Speedup (GSM8K) | AccRate |
|------|---------|------------------|--------|
| **Gen-Only** | 仅启用生成阈值（熵控制） | 1.17–1.29× | ↓ 接受率 |
| **Verify-Only** | 仅启用验证阈值（JS 距离） | ≤1.08× | ↑ 准确性 |
| **AdaSD (Full)** | 两者结合 | **1.26–1.41×** | 平衡最佳 |

> 结论：**生成长度控制是主要加速来源**，而验证阈值优化进一步提升了鲁棒性和稳定性。

此外，作者尝试了三种更复杂的验证阈值启发式（Appendix B），但发现简单取“接受与拒绝 JS 距离均值”的策略效果最好。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **熵和 JS Distance 是有效的不确定性度量指标**
   - 被拒 token 显著具有更高的 entropy 和 JS distance（图2、图3）
   - 支持将其作为自适应决策依据

2. **双阈值协同作用最优**
   - 单独使用任一机制已有增益，联合使用实现最大加速

3. **AdaSD 是真正“即插即用”的高效方案**
   - 无需训练、无需调参、无需先验知识
   - 可直接应用于 Hugging Face 生态中的主流模型

4. **适用于长文本生成任务**
   - 在 GSM8K 和 HumanEval 上表现优异
   - 在短输出任务（如 MMLU）中收益较小

---

### ⚠️ 局限性（Limitations）
1. **要求 draft 与 target 模型共享 vocabulary**
   - 否则无法计算 token-level entropy 和 JS distance
   - 当前许多小模型（<1B）使用不同 tokenizer，限制适用范围

2. **缺乏高效的 draft-target 模型配对**
   - 理想比例约为 50–100× 参数差距
   - 但现实中很少有同系列的大/小模型同时满足此比例且共享 vocab

3. **生成过长可能导致开销上升**
   - 如在 Qwen 实验中观察到，较长的候选序列增加了 draft 模型负担

---

### 🔮 未来工作方向
- 引入更多信号（如 attention variance、feature norm）改进 generation 控制
- 设计更智能的 verification threshold 更新策略（如加权移动平均）
- 探索跨 vocabulary 的适配方法（例如通过 embedding 对齐）
- 扩展至 vision-language 或 speech 模型的 speculative inference

---

## ✅ 总结
**AdaSD** 提出了一种新颖、实用且高效的自适应推测解码框架，通过引入基于 **entropy** 和 **JS Distance** 的双动态阈值机制，在不牺牲准确性的前提下显著提升了 LLM 推理速度。其实验充分验证了方法的有效性，并为未来免调参、自适应推理系统提供了重要思路。尽管存在 vocabulary 对齐等现实约束，但在合适场景下，AdaSD 是当前最先进的 speculative decoding 方案之一。

</details>

---

### 7. [Speculative Decoding Speed-of-Light: Optimal Lower Bounds via Branching Random Walks](https://arxiv.org/abs/2512.11718)

**Authors**: Sergey Pankratov, Dan Alistarh  
**Category**: cs.CL  
**Published**: 2025-12-15  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2512.11718v1  

#### Abstract
Speculative generation has emerged as a promising technique to accelerate inference in large language models (LLMs) by leveraging parallelism to verify multiple draft tokens simultaneously. However, the fundamental limits on the achievable speedup remain poorly understood. In this work, we establish...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Speculative Decoding Speed-of-Light: Optimal Lower Bounds via Branching Random Walks*

---

## 1. 论文的主要贡献和创新点

### 解决的问题
本文聚焦于 **Speculative Decoding**（推测解码）技术在大语言模型（LLMs）推理加速中的**理论极限问题**。尽管该技术已被广泛采用并取得显著经验成功（如 EAGLE、Medusa），但其**可实现的最大加速比（speedup）仍缺乏严格的理论分析**。现有研究多基于简化假设（如平均接受率），未能揭示系统容量 $P$ 和模型内在属性（如熵）对性能的根本限制。

### 提出的新方法与新思路
作者提出了一种全新的理论框架，通过将推测生成过程建模为 **Branching Random Walks (BRW)** 来分析最优性能边界：

- **核心洞察**：将 token 的生成树（token tree）视为一个以 log-probability 为权重的 BRW，其中每个节点代表一个可能的 token 序列路径。
- **理论工具引入**：利用概率论中成熟的 BRW 工具（如 **Many-to-One Lemma** 和 **Renewal Theory**）来分析在验证预算 $P$ 下高概率路径的分布。
- **最优策略刻画**：证明最优的确定性推测策略是贪心地选择具有最高接受概率的 $P$ 个 token 路径（即最小化 log-probability 和的子树）。

### 相比现有方法的优势
- **首次“紧”下界**：提供了首个关于任何确定性推测算法的**紧致运行时间下界**（等价于速度上界），而非启发式估计。
- **揭示根本权衡**：明确指出加速比的增长受限于模型输出的**期望熵 $\mu$** 和**二阶 log-矩 $\mu^{(2)}$**，揭示了并行度 $P$ 带来的收益递减规律。
- **通用性强**：所建立的框架不仅适用于基础线性推测，也可用于分析 EAGLE-3 等先进动态树推测系统的效率上限。

---

## 2. 核心实验方法和设置

### 使用的数据集
实验在多个标准基准任务上进行，涵盖不同领域：
- **代码生成**：HumanEval
- **对话能力**：MT-bench
- **数学推理**：GSM8K
- **摘要生成**：CNN/DM
- **问答任务**：Natural Questions (NQ)

### 实验设置和评估指标
- **目标模型**：Llama 3.1 8B Instruct (L3-8B), Llama 3.3 70B Instruct (L3-70B), DeepSeek R1 Distill Llama 8B (DS-8B), Qwen3 8B (Q3-8B)
- **温度设置**：所有运行均使用 temperature = 1.0
- **样本数量**：每数据集 80 个样本用于计算统计量
- **关键参数测量**：
  - $\mu$：目标模型输出分布的**期望熵**
  - $\mu^{(2)}$：**期望二阶 log-矩**（即 $-\log p$ 的方差相关项）
- **评估方式**：
  - 验证理论预测的接受 token 数 $E[X]$ 上界是否与实际系统（如 EAGLE-3）表现一致。
  - 绘制理论边界与 EAGLE-3 在不同 $P$（推测大小）下的性能曲线对比图。

### 基线方法对比
- 主要对比对象是当前最先进的推测解码系统 **EAGLE-3**。
- 将其实测的平均每轮接受 token 数与以下理论边界比较：
  - **Theorem 1 提供的精确上界**（蓝色虚线）
  - **Corollary 1 提供的渐近极限**（橙色虚线，$P \to \infty$）
  - **Lemma 7 提供的知识不完美情况下的下界**（绿色虚线）

---

## 3. 主要实验结果和性能指标

### 关键性能数据
#### 表1：各模型与任务下的 $\mu$ 和 $\mu^{(2)}$ 测量值（部分摘录）

| Model       | Task        | $\mu$   | $\mu^{(2)}$ |
|-------------|-------------|---------|-------------|
| L3-8B       | HumanEval   | 0.279   | 0.777       |
| L3-8B       | MT-bench    | 1.088   | 6.654       |
| L3-70B      | HumanEval   | 0.136   | 0.238       |
| L3-70B      | MT-bench    | 0.179   | 0.383       |
| DS-8B       | HumanEval   | 0.530   | 1.252       |
| Q3-8B       | HumanEval   | 0.178   | 0.313       |

> 观察：更大更准确的模型（如 L3-70B）通常具有更低且更稳定的 $\mu$，意味着更高的推测潜力；Qwen3 在多数任务上表现出较低的 $\mu$，理论上更适合推测。

### 与基线方法的对比结果
- **Figure 1** 显示，在固定 $P=60$ 时，EAGLE-3 的实测性能与 Lemma 7 的下界呈**近似线性关系**，表明理论边界能有效预测实际系统的行为趋势。
- **Figure 2** 展示了随着 $P$ 增加，EAGLE-3 的性能变化：
  - 实际性能（星号标记）接近“潜在上限”（黑色实线，考虑实际接受率但忽略起草开销）。
  - 但仍显著低于理论最大上界（蓝色虚线），说明当前算法尚未达到最优。
  - 不同任务间差距明显，高熵任务（如 MT-bench）提升空间更小。

### 消融实验结果
虽然未进行传统意义上的消融实验，但通过对比不同假设下的边界揭示了关键因素影响：
- **完美知识 vs 不完美知识**：从 Lemma 7 可见，当推测器无法完全掌握目标模型分布时（存在 $q(x)=0$ 但 $p(x)>0$ 的情况），性能下界显著降低，突显了训练高质量 draft model 的重要性。
- **渐近分析**：Theorem 3 和 Corollary 1 表明，当 $P \to \infty$ 时，最优 $E[X] \sim \frac{\log P}{\mu}$，进一步确认了对数增长规律。

---

## 4. 关键结论和发现

### 主要发现
1. **加速比存在根本上限**：任何确定性推测算法的期望每轮接受 token 数满足：
   $$
   E[X] \leq \frac{\mu + \mu^{(2)}}{\mu^2} \log P + O(1)
   $$
   即加速比随并行容量 $P$ **对数增长**，存在严重收益递减。
   
2. **模型不确定性是瓶颈**：加速潜力主要由目标模型的**期望熵 $\mu$** 决定——$\mu$ 越低（输出越确定），推测越高效；反之则难以并行化。

3. **分布变异性有额外影响**：除了平均熵外，**二阶矩 $\mu^{(2)}$** 也影响常数因子，表明 log-prob 分布的波动性会进一步限制性能。

4. **当前系统仍有改进空间**：EAGLE-3 等先进系统虽已接近其设计下的性能极限，但距离理论最优仍有约 **×2 的差距**，主要源于推测误差（speculation error）。

### 方法的局限性
- **i.i.d. 假设不现实**：假设不同前缀下的接受概率独立同分布（Assumption 2），忽略了语言的上下文依赖性。
- **简化时序模型**：假设 verifier 延迟恒定、drafting 开销可忽略，未考虑 KV Cache 增长等实际工程因素。
- **仅限确定性算法**：分析集中于确定性推测策略，未涵盖随机性方法。
- **完美知识假设**：上界推导假设 draft model 完全知晓目标模型分布，现实中不可达。

### 未来工作方向
- 放宽 i.i.d. 假设，扩展至平稳或遍历过程以更好建模语言动态。
- 引入更真实的时序和资源消耗模型（如考虑 drafting 成本）。
- 探索非确定性或自适应推测策略的理论边界。
- 设计更接近理论极限的实用算法，例如优化 cross-entropy 损失以减少 speculation error。

--- 

> ✅ **总结一句话**：本文通过引入 **Branching Random Walks** 框架，首次为 Speculative Decoding 建立了**紧致的理论性能上界**，揭示了加速比受模型熵制约的本质规律，并验证了当前系统与理论极限之间的差距，为未来高效 LLM 推理系统的设计提供了坚实的理论指导。

</details>

---

### 8. [Evaluation Framework for Centralized and Decentralized Aggregation Algorithm in Federated Systems](https://arxiv.org/abs/2512.10987)

**Authors**: Sumit Chongder  
**Category**: cs.DC  
**Published**: 2025-12-15  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2512.10987v1  

#### Abstract
In recent years, the landscape of federated learning has witnessed significant advancements, particularly in decentralized methodologies. This research paper presents a comprehensive comparison of Centralized Hierarchical Federated Learning (HFL) with Decentralized Aggregated Federated Learning (AFL...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Evaluation Framework for Centralized and Decentralized Aggregation Algorithm in Federated Systems

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
该研究聚焦于 **Centralized Hierarchical Federated Learning (HFL)** 所面临的挑战，主要包括：
- **通信瓶颈**：所有客户端需与中央服务器频繁通信，导致延迟高、带宽压力大。
- **隐私风险**：尽管原始数据不上传，但模型更新集中聚合仍可能泄露敏感信息。
- **单点故障**：依赖中心化服务器，系统鲁棒性差。

这些问题限制了 HFL 在大规模、边缘设备分布广泛场景下的可扩展性和安全性。

### 🚀 提出的新方法与思路
论文提出并系统比较了两种去中心化的联邦学习范式：
- **Decentralized Aggregated Federated Learning (AFL)**  
- **Decentralized Continual Federated Learning (CFL)**

其核心创新在于：
- 引入 **完全去中心化的聚合机制**，无需中央服务器参与模型聚合，通过客户端之间的直接通信完成协作训练。
- 对比分析了 HFL、AFL 和 CFL 在真实数据集上的表现，构建了一个全面的评估框架，涵盖精度、效率、通信开销等多个维度。
- 特别强调了 **Decentralized Aggregation** 在提升模型性能和保护隐私方面的潜力。

### 🔍 相比现有方法的优势
| 方法 | 优势 |
|------|------|
| **AFL** | 减少对中心节点的依赖，降低通信延迟；支持局部模型聚合，提高训练效率 |
| **CFL** | 支持持续学习能力，在动态环境中不断适应新任务；表现出最高的准确率和稳定性 |
| 整体框架 | 首次在同一实验条件下对 HFL、AFL、CFL 进行横向对比，提供可复现的基准结果 |

---

## 2. 核心实验方法和设置

### 📊 使用的数据集
- **MNIST**：手写数字图像数据集（10类，28×28灰度图）
- **Fashion-MNIST**：服装品类图像数据集（10类，28×28灰度图）

两个均为标准图像分类 benchmark 数据集，用于验证不同 FL 范式的泛化能力。

### ⚙️ 实验设置
- **数据分布**：采用 IID（Independent and Identically Distributed）分布模拟理想环境。
- **模型架构**：统一使用 CNN 架构，包含三个卷积层（16@3x3, 12@3x3, 10@3x3）、ReLU 激活函数、两个 Max-Pooling 层及全连接输出层。
- **训练轮数**：多轮迭代训练，每轮选择部分客户端参与。
- **实现工具**：
  - Python 3.11.4
  - TensorFlow 2.16.1 / Keras 3.1.1
  - Jupyter Notebook + Anaconda 环境

### 📈 评估指标
| 指标 | 定义 |
|------|------|
| **Accuracy** | 正确预测样本占比 |
| **Precision** | 所有正类预测中真正例的比例 |
| **Recall** | 所有实际正例中被正确识别的比例 |
| **F1 Score** | Precision 与 Recall 的调和平均值 |
| **Build Time (s)** | 模型训练总耗时（从开始到收敛） |
| **Classification Time (s)** | 推理阶段耗时（单次前向传播时间） |

### 🔁 基线方法对比
- **Hierarchical FL (HFL)**：传统中心化分层联邦学习，作为主要 baseline
- **Aggregated FL (AFL)**：去中心化聚合联邦学习
- **Continual FL (CFL)**：支持持续学习的去中心化联邦学习

三者在相同网络结构、优化器（SGD）、学习率等超参数下进行公平比较。

---

## 3. 主要实验结果和性能指标

### 📊 关键性能数据（来自 Table 1 & Table 2）

#### 在 **Fashion-MNIST** 上的表现：

| Environment | Testing Acc | Precision | Recall | F1 Score | Build Time (s) | Class. Time (s) |
|------------|-------------|-----------|--------|----------|----------------|------------------|
| HFL        | 0.41        | 0.41      | 0.33   | 0.40     | 86.11          | 0.57             |
| AFL        | 0.70        | 0.71      | 0.68   | 0.68     | 55.33          | 0.47             |
| CFL        | **0.88**    | **0.88**  | **0.87**| **0.86**| 80.07          | **0.31**         |

#### 在 **MNIST** 上的表现：

| Environment | Testing Acc | Precision | Recall | F1 Score | Build Time (s) | Class. Time (s) |
|------------|-------------|-----------|--------|----------|----------------|------------------|
| HFL        | 0.60        | 0.75      | 0.60   | 0.59     | 88.26          | 0.55             |
| AFL        | 0.72        | 0.76      | 0.72   | 0.72     | **54.02**      | 0.52             |
| CFL        | **0.98**    | **0.98**  | **0.98**| **0.98**| 79.02          | **0.29**         |

> 注：最佳结果加粗显示

### 🔍 与基线方法的对比结果
- **准确性方面**：
  - CFL 显著优于 HFL 和 AFL，尤其在 Fashion-MNIST 上测试准确率高出近 **47个百分点**（0.41 → 0.88）。
  - AFL 相较 HFL 提升明显，说明去中心化聚合本身即可带来性能增益。
- **效率方面**：
  - **AFL 具有最短的 Build Time**，表明其在快速收敛方面具有优势。
  - **CFL 拥有最低的 Classification Time**，适合低延迟推理场景。
- **综合性能**：
  - CFL 在几乎所有指标上均取得最优，尤其是在 Precision、Recall、F1 Score 上接近完美表现（MNIST 上达 0.98）。

### ❌ 是否有消融实验？
论文未明确开展消融实验（ablation study），例如：
- 不同聚合策略的影响（如 FedAvg vs. Gossip）
- 非 IID 数据下的性能变化
- 网络拓扑结构对去中心化通信的影响

因此无法判断各组件的具体贡献度，这是本研究的一个局限。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **去中心化联邦学习显著优于中心化架构**：
   - AFL 和 CFL 在 **accuracy、precision、recall、F1 score** 上全面超越 HFL。
   - 尤其是 **CFL 表现出最强的建模能力和泛化性能**。

2. **Decentralized Aggregation 是关键驱动力**：
   - 分布式聚合机制有效缓解了通信瓶颈，提升了系统的可扩展性和鲁棒性。
   - 客户端间直接交换模型更新增强了隐私保护能力。

3. **CFL 在推理效率上具备显著优势**：
   - 最低的 **classification time** 使其适用于实时应用（如移动端、IoT 设备）。

4. **AFL 平衡了速度与性能**：
   - 虽然精度略低于 CFL，但拥有最快的 **build time**，适合需要快速部署的场景。

### ⚠️ 方法的局限性
- 实验基于 **IID 数据假设**，未考虑现实世界中常见的 non-iid 数据分布问题。
- 缺乏对 **通信开销、能耗、网络拓扑稳定性** 的量化分析。
- 未涉及安全攻击（如拜占庭攻击）或差分隐私机制下的鲁棒性测试。
- 所有实验在仿真环境中完成，缺乏真实分布式网络中的验证。

### 🔮 未来工作方向（作者建议）
1. **探索多样化的数据分布组合**：研究 non-IID、偏斜分布、跨域迁移等复杂场景下的 DFL 性能。
2. **解决异构性与可扩展性问题**：针对设备计算能力、存储、网络带宽差异设计更灵活的调度机制。
3. **增强隐私与安全性保障**：结合 Secure Multi-Party Computation（SMPC）或 Differential Privacy 技术进一步强化去中心化系统的抗攻击能力。
4. **拓展至更多应用场景**：如医疗、自动驾驶、多机器人协同等高隐私需求领域。

---

## ✅ 总结
该论文系统地评估了三种主流联邦学习架构（HFL、AFL、CFL），实验证明 **Decentralized FL 范式在性能、效率和隐私保护方面均优于传统的 Centralized HFL**。其中，**CFL 在整体性能上表现最为突出**，而 **AFL 在训练效率上有明显优势**。研究成果为未来联邦学习系统的设计提供了重要参考，推动了从“中心化聚合”向“去中心化协作”的演进趋势。

</details>

---

### 9. [ECCO: Leveraging Cross-Camera Correlations for Efficient Live Video Continuous Learning](https://arxiv.org/abs/2512.11727)

**Authors**: Yuze He, Ferdi Kossmann, Srinivasan Seshan, Peter Steenkiste  
**Category**: cs.DC  
**Published**: 2025-12-15  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2512.11727v1  

#### Abstract
Recent advances in video analytics address real-time data drift by continuously retraining specialized, lightweight DNN models for individual cameras. However, the current practice of retraining a separate model for each camera suffers from high compute and communication costs, making it unscalable....

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# ECCO: Leveraging Cross-Camera Correlations for Efficient Live Video Continuous Learning — 核心总结

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
当前基于边缘服务器的**实时视频连续学习系统**（live video continuous learning）面临严重的资源效率瓶颈。主流方法（如 Ekya、RECL）采用“独立重训练”（independent retraining），即为每个摄像头单独重训练一个轻量级 DNN 模型以应对数据漂移（data drift）。这种策略导致：
- **计算冗余**：多个摄像头经历相似的数据漂移时，重复训练高度相似的模型。
- **通信开销大**：每个摄像头需持续上传帧数据，占用大量带宽。
- **扩展性差**：随着摄像头数量增加，GPU 和网络资源压力线性增长。

### 🚀 提出的新方法：ECCO 与 Group Retraining
论文提出 **ECCO**，一种全新的高效连续学习框架，其核心思想是 **Group Retraining（分组重训练）**：
> 将经历**相似数据漂移**的摄像头动态聚合成组，并使用它们的**联合数据**共同训练一个共享模型。

#### 创新点包括：
1. **轻量级动态分组算法**  
   - 基于元数据（时间、位置）快速筛选候选组，再通过精度增益评估是否合并，避免高维视频流直接比较。
   - 支持周期性“重新分组”（regrouping），适应动态变化的场景分布。

2. **面向分组重训练的 GPU 分配器**  
   - 提出新的优化目标：`max ∑nⱼ^β·Aⱼ + min Aⱼ`，兼顾整体精度与组间公平性。
   - 设计贪心算法（Algorithm 1）在微窗口（micro-window）粒度上动态分配 GPU 时间，防止小规模组被“饿死”。

3. **资源感知的传输控制器（Transmission Controller）**  
   - 在每台摄像头端实现：
     - **采样配置自适应**：根据分配的 GPU 资源选择最优帧率与分辨率组合。
     - **定制化 GAIMD 拥塞控制**：使各组的带宽分配与其 GPU 配额成正比，实现 compute-aware bandwidth sharing。

---

### 🔍 相比现有方法的优势
| 维度 | 现有方法（Ekya, RECL） | ECCO |
|------|------------------------|------|
| 模型训练方式 | 独立重训练（per-camera） | 分组共享训练（group retraining） |
| GPU 分配目标 | 最大化总精度提升 | 平衡精度与公平性 |
| 带宽利用 | 默认均等分配（AIMD） | GPU-比例加权分配（GAIMD） |
| 数据效率 | 低（重复训练） | 高（聚合数据 + 自然模型复用） |

> **根本优势**：ECCO 从“跨摄像头相关性”中挖掘资源效率红利，在不牺牲甚至提升精度的前提下，显著降低计算与通信成本。

---

## 2. 核心实验方法和设置

### 📚 使用的数据集
| 数据集 | 类型 | 描述 |
|-------|------|------|
| **CityFlow** | 静态交通摄像头 | 40 台相机拍摄城市路口，3+小时同步视频 |
| **MDOT** | 移动无人机摄像头 | 5 架无人机编队飞行，共 155 组视频片段（>259k 帧） |
| **CARLA Simulated Datasets** | 模拟自动驾驶场景 | 使用 CARLA 模拟器生成不同相似度级别的多摄像头视频流，用于可控实验 |

### ⚙️ 实验设置
- **任务类型**：Object Detection 与 Instance Segmentation
- **模型架构**：
  - Student Model: YOLOv11n / YOLOv11n-Seg
  - Teacher Model: YOLOv11x / YOLOv11x-Seg
- **硬件平台**：服务器配备 5× NVIDIA RTX 4090 GPU
- **仿真环境**：Docker 容器模拟摄像头 + NS-3 网络仿真（支持 GAIMD）
- **重训练窗口**：固定长度的时间片，内含多个 micro-windows

### 📊 评估指标
| 指标 | 含义 |
|------|------|
| **mAP**（mean Average Precision） | 检测与分割任务的标准精度指标 |
| **Response Time** | 触发重训练后达到目标 mAP 所需时间 |
| **Scalability** | 在相同资源下支持的最大摄像头数 |
| **Fairness** | 不同组之间的精度差异（尤其是大小组之间） |

### 🆚 基线方法对比
| 基线 | 特点 |
|------|------|
| **Naive Baseline** | 均匀 GPU 分配 + 固定采样 + 均等带宽 |
| **Ekya** | 先进 GPU 调度，但仍为独立重训练 |
| **RECL** | 使用历史模型初始化（model zoo）加速收敛，独立重训练 |

> 所有基线均未利用跨摄像头相关性进行模型共享。

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据（来自 Figure 6 & Section 5.1）
| 指标 | 提升幅度 |
|------|----------|
| **Object Detection mAP** | 提升 **6.7% – 16.6%** |
| **Instance Segmentation mAP** | 提升 **9.3% – 18.1%** |
| **同等精度下支持摄像头数** | 提升 **3.3×** |
| **达到相同精度所需 GPU 数量** | 减少 **1.8× – 2.4×** |
| **达到相同精度所需带宽** | 仅需基线的 **25% – 33%** |
| **低带宽下响应时间** | 缩短 **>5×** |

> 💡 即便在极端条件下（如仅 1 GPU），Group Retraining 性能接近使用 3 GPU 的独立训练（见 Figure 2c）。

---

### 🔬 消融实验结果（Ablation Study）

#### （1）动态分组有效性（Figure 9）
- 当移动摄像头进入隧道导致场景突变时，ECCO 成功检测到其与原组精度下降，将其移出并启动新组。
- 验证了 **regrouping 机制能有效维护组内一致性**。

#### （2）GPU 分配器对比（Figure 10）
| 分配器 | 表现 |
|--------|------|
| **RECL Allocator** | 大组（3 cam）优先获得资源，小组（1 cam）长期饥饿，精度差距达 **23% mAP** |
| **ECCO Allocator** | 实现近似同步精度上升，显著提升公平性 |

> 结论：传统以“总精度最大化”为目标的分配器不适合 group retraining 场景。

#### （3）传输控制器消融（Figure 11）
- **无控制器**：固定采样 + 标准 AIMD → 带宽分配偏离理想比例，精度低。
- **ECCO 控制器**：
  - 在 9 Mbps 共享带宽下，带宽分配逼近 GPU-proportional 目标。
  - 在 3 Mbps 低带宽下，精度高出基线 **4.7%**。
  - 达到峰值精度所需带宽仅为基线的 **1/3**。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **跨摄像头数据漂移存在强时空相关性**  
   - 静态摄像头（如广场监控）、移动设备（如无人机编队）常经历相似环境变化，适合分组处理。

2. **Group Retraining 显著提升资源效率**  
   - 通过共享模型减少冗余训练，降低 GPU 与带宽需求。
   - 在相同资源下可支持 **3.3× 更多摄像头**，或提升精度 **>15%**。

3. **compute-aware 通信设计至关重要**  
   - 仅优化 GPU 或仅优化带宽都不够；必须联合调度。
   - 定制化的 GAIMD 可在异构网络中逼近 GPU-proportional 带宽分配。

4. **自然模型复用增强响应速度**  
   - 后加入组的摄像头可继承已部分更新的模型，起始精度更高（Figure 12）。
   - 数据聚合加快训练收敛，尤其在低带宽下响应时间缩短 **>5×**（Figure 13）。

---

### ⚠️ 局限性
1. **依赖高质量的漂移检测机制**  
   - 若漂移检测不准，可能导致错误分组或延迟响应。
2. **分组收益依赖摄像头相似度**  
   - 如 Figure 8 所示，在低相似度组中，group retraining 收益有限（仅 +1.2% mAP）。
3. **中心化服务器假设**  
   - 所有重训练集中在边缘服务器完成，未考虑完全去中心化部署。

---

### 🔮 未来工作方向
1. **更智能的自动分组策略**  
   - 探索基于 embedding similarity 或 clustering 的轻量级在线分组算法。
2. **联邦学习融合**  
   - 在保护隐私前提下实现分布式 group retraining。
3. **端到端联合优化**  
   - 将分组决策、GPU 分配、带宽控制统一建模为强化学习问题。
4. **扩展至更多视觉任务**  
   - 如 multi-object tracking、action recognition 等。

---

## 总结
ECCO 是首个系统性利用 **cross-camera correlations** 来优化 **continuous learning** 效率的工作。它通过 **group retraining** 范式，实现了计算、通信与响应性的全面优化，在真实与模拟数据集上均展现出显著优于 Ekya、RECL 等先进系统的性能。该工作为大规模实时视频分析系统的可持续演进提供了重要思路。

</details>

---

### 10. [Softmax as Linear Attention in the Large-Prompt Regime: a Measure-based Perspective](https://arxiv.org/abs/2512.11784)

**Authors**: Etienne Boursier, Claire Boyer  
**Category**: cs.LG  
**Published**: 2025-12-15  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2512.11784v1  

#### Abstract
Softmax attention is a central component of transformer architectures, yet its nonlinear structure poses significant challenges for theoretical analysis. We develop a unified, measure-based framework for studying single-layer softmax attention under both finite and infinite prompts. For i.i.d. Gauss...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Softmax as Linear Attention in the Large-Prompt Regime: a Measure-based Perspective*

## 1. 论文的主要贡献和创新点

### 解决的问题
本文旨在解决 **softmax attention** 在理论分析上的困难。尽管 softmax 是 Transformer 架构的核心组件，并在实践中表现出色（尤其在长提示下的泛化能力），但其非线性结构使得从优化和统计角度进行严格分析极具挑战。相比之下，**linear attention** 因其代数简单性而易于分析，但其实际性能通常弱于 softmax。

一个核心问题是：**能否在理论上解释为什么 softmax 在长提示下表现优异？它是否可以被简化模型（如 linear attention）近似？**

### 提出的新方法与新思路
作者提出了一种 **基于测度（measure-based）的统一框架** 来研究单层 softmax attention，该框架能够同时处理有限长度和无限长度的提示（prompt）。

- **核心洞察**：当输入 token 是独立同分布（i.i.d.）的高斯变量时，在提示长度 $L \to \infty$ 的极限情况下，softmax attention 操作会收敛到一个**关于输入 token 测度的线性算子**。
- **新框架**：将有限提示表示为经验测度 $\mu_L = \frac{1}{L}\sum_{i=1}^L \delta_{z_i}$，而无限提示则对应于真实的分布 $\mu$。通过这种形式化，可以系统地比较有限提示模型与其无限提示极限之间的差异。

### 相比现有方法的优势
- **理论严谨性**：提供了 **非渐近的集中不等式（non-asymptotic concentration bounds）**，量化了有限提示下的 softmax attention 输出及其梯度如何快速逼近其无限提示的线性对应物。
- **动态稳定性**：证明了这种集中现象在整个训练轨迹中是稳定的，即使在一般的 in-context learning (ICL) 设置下也成立。
- **可迁移性**：建立了一个“桥梁”，使得为 linear attention 开发的优化分析可以直接迁移到 large-prompt regime 下的 softmax attention。这为分析复杂任务中的 softmax 提供了强有力的工具箱。
- **更广适用性**：相比 Chen et al. (2024) 等仅限于各向同性协变量（isotropic covariates）的分析，本文的方法适用于**各向异性协变量（anisotropic covariates）**，更具普适性。

---

## 2. 核心实验方法和设置

需要强调的是，**本文是一篇纯理论论文，没有进行传统意义上的数值实验或在真实数据集上的性能测试**。所有的“结果”都是数学推导和定理证明。

### 数据集
- 理论分析基于 **合成数据**，特别是假设输入 token 服从 **高斯分布**（如 $\mathcal{N}(0, \Sigma)$）或更一般的 sub-Gaussian 分布。
- 在第5节中，具体考虑了 **in-context linear regression** 任务，其中每个提示对应一个随机生成的线性回归问题 $y = w^\top x + \epsilon$，$w \sim \mathcal{N}(0, I_d)$。

### 实验设置和评估指标
- **设置**：研究单层 Transformer 的 **梯度流（gradient flow）** 动力学。
- **核心指标**：
  - **风险（Risk）** $R(U,V)$：衡量模型预测误差的期望损失。
  - **参数收敛性**：分析注意力参数 $(U(t), V(t))$ 随时间 $t$ 的演化。
  - **集中界（Concentration bounds）**：量化 $\left\|\mathcal{T}_{U,V}[\mu_L] - \mathcal{T}_{U,V}[\mu]\right\|$ 和梯度差别的大小，这些界限依赖于提示长度 $L$。

### 基线方法对比
- **主要对比对象是 linear attention**。作者并未将 softmax 与 linear attention 进行直接的性能数字对比，而是证明了在 $L \to \infty$ 时，两者的动力学行为是**等价的**。
- 引用并对比了 **Zhang et al. (2024)** 对 linear attention 在 in-context learning 中的分析结果，证明本文的框架可以将这些结果无缝迁移到 softmax attention。

---

## 3. 主要实验结果和性能指标

再次说明，以下均为**理论结果**，而非实验数据。

### 关键性能数据（理论界限）
- **输出集中界（Proposition 3.1）**：
  $$
  \mathbb{E}\left[\left\|\mathcal{T}_{U,V}[\mu_L](z) - \mathcal{T}_{U,V}[\mu](z)\right\|\right] \leq c_1 \sigma \sqrt{\frac{\ln(L)}{L^{c_2/\sigma^2}}}
  $$
  这表明随着 $L$ 增大，有限提示的输出以多项式对数速率收敛到其无限提示极限。

- **梯度集中界（Proposition 3.4）**：
  类似的界限也适用于梯度 $\nabla_V \mathcal{T}_{U,V}$ 和 $\nabla_U \mathcal{T}_{U,V}$，保证了优化过程的稳定性。

- **训练动态逼近（Theorem 4.3）**：
  $$
  \lim_{t\to\infty} R(\theta_L(t)) \leq \lim_{t\to\infty} R_\infty(\theta_\infty(t)) + \epsilon
  $$
  其中 $\theta_L(t)$ 是有限提示 $L$ 下的参数，$\theta_\infty(t)$ 是无限提示下的参数。这意味着长提示下的最终风险可以任意接近无限提示下的最优风险。

- **In-context Linear Regression 的贝叶斯最优性（Theorem 5.1）**：
  在 in-context linear regression 任务中，当提示足够长时，通过梯度流训练的 softmax attention 可以达到 **Bayes 最优风险**（即理论最小可能风险，此处为0，因为无标签噪声）。

### 与基线方法的对比结果
- 本文证明，**在无限提示极限下，softmax attention 的行为与 linear attention 完全等价**。
- 因此，Zhang et al. (2024) 为 linear attention 证明的收敛性和最优性结果，可以直接应用于 large-prompt 下的 softmax attention。
- 相比 Chen et al. (2024)，本文虽然未给出精确的收敛速率，但优势在于处理了更一般的 **anisotropic covariates** 场景。

### 消融实验
- 本文没有进行消融实验。其理论框架本身是对不同假设（如 sub-Gaussian 输入、高斯输入）的分析，这些构成了理论的组成部分而非实验验证。

---

## 4. 关键结论和发现

### 主要发现
1. **Large-prompt Softmax ≈ Linear Attention**：在提示很长且输入为高斯或 sub-Gaussian 时，softmax attention 的行为本质上是线性的。其无限提示极限是一个关于输入测度的线性变换。
2. **可分析性转移**：这一发现提供了一个强大的工具，允许研究者利用为 **linear attention** 建立的成熟理论来分析 **softmax attention** 的训练动态和泛化行为。
3. **理论正当化**：为使用 linear attention 作为 softmax 的代理模型进行理论研究提供了**原则性的、广泛适用的依据**，尤其是在长上下文场景下。
4. **收敛保证**：首次为 softmax attention 在 **anisotropic Gaussian covariates** 下的 in-context learning 任务提供了收敛到贝叶斯最优解的理论保证。

### 方法的局限性
- **单层限制**：分析局限于**单层** attention。多层堆叠的 Transformer 的动态更为复杂，尚未被涵盖。
- **理想化假设**：依赖于 i.i.d. 输入 token 的假设。现实世界的数据存在复杂的依赖关系和结构。
- **无限深度 vs 无限宽度**：本文关注“无限提示”（large-width in data），而许多神经网络理论关注“无限宽度”（large-width in parameters）。两者结合的分析更具挑战性。
- **高斯输入**：虽然部分结果推广到 sub-Gaussian，但最有力的结论（如线性化）依赖于高斯性。

### 未来工作方向
- 将该测度框架扩展到**多层 Transformer** 模型。
- 研究更复杂的 in-context learning 任务，例如 **n-gram 学习** 或 **符号推理**。
- 放松 i.i.d. 和高斯性假设，分析更贴近真实数据的分布。
- 探索该理论对模型设计（如初始化、架构）的指导意义。
- 将“无限提示”的思想与“无限宽度”的 mean-field 理论相结合，建立更全面的 Transformer 理论。

</details>

---

### 11. [AgentBalance: Backbone-then-Topology Design for Cost-Effective Multi-Agent Systems under Budget Constraints](https://arxiv.org/abs/2512.11426)

**Authors**: Shuowei Cai, Yansong Ning, Hao Liu  
**Category**: cs.AI  
**Published**: 2025-12-15  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2512.11426v1  

#### Abstract
Large Language Model (LLM)-based multi-agent systems (MAS) are becoming indispensable building blocks for web-scale applications such as web search, social network analytics, and online customer support, where cost-effectiveness is increasingly the primary constraint for large-scale deployment. Whil...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：AgentBalance: Backbone-then-Topology Design for Cost-Effective Multi-Agent Systems under Budget Constraints**

---

## **1. 论文的主要贡献和创新点**

### **解决的问题**
当前基于大语言模型（LLM）的多智能体系统（MAS）在复杂任务中表现出色，但其高昂的 **token-cost** 和 **end-to-end latency** 严重制约了大规模部署。现有研究通常：
- 忽视显式的预算约束（token-cost 和 latency）；
- 采用“先拓扑后骨干”（topology-first）的设计范式；
- 忽略不同 LLM 骨干（backbone）对成本效益的巨大影响。

这导致在实际预算限制下，系统性能不佳或不可持续。

### **提出的新方法与创新思路**
本文提出了 **AGENTBALANCE**，一种全新的 **backbone-then-topology** 设计框架，用于构建在显式 token-cost 和 latency 预算下的高性价比 MAS。其核心思想是：
> **先确定合适的 LLM 骨干，再在此基础上优化通信拓扑**。

该方法包含两个核心模块：

#### （1）**Backbone-Oriented Agent Generation（骨干导向的智能体生成）**
- **LLM 池构建与分析**：基于性能、token-cost、latency 构建帕累托前沿（Pareto frontier），聚类形成多个 LLM 池（pool），每个池代表不同的资源层级。
- **难度感知的池选择（Difficulty-Aware Pool Selection）**：通过轻量级难度估计器预测查询难度，并据此选择最合适的 LLM 池，实现预算分配。
- **查询条件的角色-骨干匹配（Query-Conditioned Role-Backbone Matching）**：根据角色需求和查询内容，为每个 agent 分配最匹配的 backbone，提升成本效益。

#### （2）**Adaptive MAS Topology Generation（自适应拓扑生成）**
- **统一智能体表征学习（Unified Agent Representation Learning）**：融合角色、查询、LLM 类型等信息，生成统一的 agent 表征。
- **智能体门控（Agent Gating）**：动态移除冗余 agent，减少不必要的计算开销。
- **延迟感知的拓扑合成（Latency-Aware Topology Synthesis）**：生成稀疏且低延迟的通信图，并通过 hop-length 控制避免过长推理链。

### **相比现有方法的优势**
- **设计范式革新**：从“topology-first”转向“backbone-then-topology”，更符合预算约束下的优化逻辑。
- **全面建模成本因素**：同时考虑 token-cost、latency 和性能，进行端到端联合优化。
- **高度自适应性**：能根据查询难度动态调整 backbone 和 topology，实现个性化资源配置。
- **可插拔性强**：可作为插件集成到现有 MAS 框架（如 AutoGen、Layered Graph）中，提升其成本效益。
- **归纳能力强**：能泛化到训练时未见过的 LLM，具备良好的实用性和扩展性。

---

## **2. 核心实验方法和设置**

### **使用的数据集**
在三个代表性领域进行评估：
- **MMLU**：通用知识问答
- **MATH**：数学推理
- **HumanEval**：代码生成

### **实验设置**
- **候选 LLM 数量**：共 14 个，来自 Qwen 和 DeepSeek 系列，涵盖不同规模（1.7B ~ 235B）和类型（reasoning / non-reasoning）。
- **LLM 池数量**：4 个，按性能由弱到强排序。
- **训练样本数**：HumanEval 使用 20 样本，其余使用 40 样本，强调小样本高效性。
- **优化方式**：使用策略梯度（policy gradient）进行端到端训练，目标函数包含性能奖励与 token-cost、latency 惩罚项。

### **评估指标**
- **Performance-at-Budget (P@T_k, P@L_k)**：在给定 token-cost 或 latency 预算下的任务性能。
  - MMLU/MATH：accuracy
  - HumanEval：Pass@1
- **AUC_tok / AUC_lat**：性能-预算曲线下的面积，衡量整体成本效益。

### **基线方法对比**
| 基线方法 | 类型 | 特点 |
|--------|------|------|
| **AgentPrune** | 单 backbone | 剪枝冗余消息 |
| **AgentDropout** | 单 backbone | 动态移除 agent |
| **G-Designer** | 单 backbone | 使用 GNN 合成稀疏拓扑 |
| **MasRouter** | 多 backbone | topology-first，支持多 LLM 路由 |

所有基线均基于 Complete Graph 架构构建。

---

## **3. 主要实验结果和性能指标**

### **关键性能数据（见 Table 1）**

| 数据集 | 方法 | P@T1 (Token) | P@T4 (Token) | AUC_tok | P@L1 (Latency) | P@L4 (Latency) | AUC_lat |
|-------|------|-------------|-------------|---------|---------------|---------------|--------|
| **MMLU** | AGENTBALANCE | **71.90** | **88.02** | **1.297** | **71.90** | **88.02** | **250.0** |
| | 最佳基线 (G-Designer) | 61.80 | 86.77 | 1.261 | 59.69 | 86.61 | 243.2 |
| **HumanEval** | AGENTBALANCE | **87.94** | **95.46** | 1.880 | **87.94** | **95.46** | **476.5** |
| | 最佳基线 (G-Designer) | 86.90 | 95.52 | **1.885** | 83.81 | 93.51 | 474.9 |
| **MATH** | AGENTBALANCE | **66.46** | **79.38** | **3.523** | **66.46** | **79.38** | **602.4** |
| | 最佳基线 (G-Designer) | 63.05 | 77.45 | 3.450 | 59.43 | 74.26 | 581.9 |

> ✅ **最高提升达 10%（token-cost 下）和 22%（latency 下）**

### **与基线方法的对比结果**
- 在 **tight budget（P@T1/P@L1）** 下，AGENTBALANCE 显著优于所有基线，说明其在资源受限场景下更具优势。
- **MasRouter** 表现不稳定，尤其在低预算下性能骤降，验证了 topology-first 范式的局限性。
- 单 backbone 方法（如 AgentPrune）因无法灵活选择 backbone，在成本效益上存在天花板。

### **消融实验结果（见 Table 3）**
对以下变体进行了消融：

| 变体 | 性能下降 | 成本/延迟变化 | 结论 |
|------|--------|--------------|------|
| (1) 随机 LLM 池 | ↓ 明显 | ↑ token-cost & latency | LLM 池构造至关重要 |
| (2) 随机池选择 | ↓ 明显 | ↓ token-cost but ↓↓ perf | 难度感知选择有效 |
| (3) 随机角色-骨干匹配 | ↓ 明显 | ↓ cost but ↓↓ perf | 匹配精度决定性价比 |
| (4) 忽略 backbone 信息 | ↓ | ↑ latency | 统一表征学习必要 |
| (5) 移除 agent gating | ↔ perf | ↑↑ latency | gating 有效控制延迟 |
| (6) 密集拓扑 | ↔ perf | ↑↑ latency | 稀疏拓扑更优 |

> 🔍 所有模块均对最终性能-成本-延迟平衡有显著贡献。

---

## **4. 关键结论和发现**

### **主要发现**
1. **骨干选择比拓扑优化更能驱动成本效益边界**（见 Figure 2 左）：
   - Backbone choice 对性能-成本权衡的影响远大于 topology 修改。
2. **最优拓扑依赖于所选 backbone**（见 Figure 2 右）：
   - 更换 backbone 后若不重新设计拓扑，性能会下降（transfer 效果差）。
3. **backbone-then-topology 范式优于 topology-first**：
   - 固定 backbone 再优化 topology 更稳定、更高效。
4. **AGENTBALANCE 具备强归纳能力**（见 Figure 4）：
   - 即使移除或新增未见 LLM，仍能快速适配并保持领先性能。
5. **可作为插件增强现有 MAS**（见 Table 2）：
   - 插入 AutoGen 或 Layered Graph 后，性能显著提升，验证其通用性。

### **方法的局限性**
- **依赖高质量的 LLM profiling**：需准确估计各 LLM 的性能、PTP、latency。
- **初始化开销较大**：LLM 池构建和难度估计器预训练需要额外资源。
- **极端预算下可能欠拟合**：在极低预算下，可用 backbone 有限，灵活性受限。

### **未来工作方向**
- 探索更细粒度的 **cost modeling**，如考虑 prefilled vs decode token 的差异。
- 将框架扩展至 **multi-modal MAS** 场景。
- 研究 **动态预算调整机制**，应对运行时资源波动。
- 开发 **自动化 LLM profiling pipeline**，降低部署门槛。

---

> 📌 **总结**：  
> **AGENTBALANCE** 提出了一种面向预算约束的新型 MAS 构建范式 —— **backbone-then-topology**，通过骨干导向的 agent 生成与自适应拓扑设计，在 token-cost 和 latency 受限条件下实现了显著的性能提升。其实验充分、设计合理，兼具实用性与前瞻性，为大规模 LLM 应用的成本控制提供了重要解决方案。

</details>

---

### 12. [Agentic Operator Generation for ML ASICs](https://arxiv.org/abs/2512.10977)

**Authors**: Alec M. Hammond, Aram Markosyan, Aman Dontula, Simon Mahns, Zacharias Fisches, Dmitrii Pedchenko, Keyur Muzumdar, Natacha Supper, Mark Saroufim, Joe Isaacson, Laura Wang, Warren Hunt, Kaustubh Gondkar, Roman Levenstein, Gabriel Synnaeve, Richard Li, Jacob Kahn, Ajit Mathews  
**Category**: cs.DC  
**Published**: 2025-12-15  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2512.10977v1  

#### Abstract
We present TritorX, an agentic AI system designed to generate functionally correct Triton PyTorch ATen kernels at scale for emerging accelerator platforms. TritorX integrates open-source large language models with a custom linter, JIT compilation, and a PyTorch OpInfo-based test harness. This pipeli...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Agentic Operator Generation for ML ASICs

## 1. 论文的主要贡献和创新点

### 解决的问题
在机器学习专用集成电路（ML ASIC）的研发中，构建一个完整的软件生态系统是关键挑战之一。特别是为新兴加速器平台（如 Meta Training and Inference Accelerator, MTIA）实现对 **PyTorch ATen** 算子的全面支持需要大量工程投入。传统方式依赖人工编写高性能内核，效率低且难以扩展。

本文旨在解决 **如何自动化生成功能正确、覆盖广泛的 PyTorch ATen 内核（kernels）以适配新型 ML ASIC 平台** 这一难题。

---

### 提出的新方法：TritorX
作者提出了 **TritorX** —— 一种基于代理（agentic）的 AI 系统，用于大规模自动生成适用于 MTIA 的 **Triton PyTorch ATen 内核**。

#### 核心创新点：
- **端到端自动化流程**：结合开源大语言模型（LLM）、自定义 linter、JIT 编译器和 PyTorch OpInfo 测试框架，形成闭环反馈系统。
- **有限状态机（FSM）架构**：采用 FSM 控制 LLM 的迭代生成过程，确保执行可控性和可调试性，优于纯推理代理模式。
- **强调覆盖率而非单一性能优化**：不同于以往聚焦少数关键路径算子的方法，TritorX 优先保证整个 ATen 算子集合的功能正确性和通用性（涵盖多种数据类型、形状、参数组合）。
- **防“作弊”机制**：通过定制化 **Triton MTIA Linter** 防止 LLM 调用未实现的 PyTorch 函数或将计算回退到主机（host），强制本地化实现。
- **真实硬件验证**：直接在部署的 MTIA 硬件或 QEMU 模拟器上进行编译、运行和测试，确保生成代码的实际可用性。

---

### 相比现有方法的优势
| 维度 | 传统方法 | TritorX |
|------|--------|--------|
| 开发成本 | 高（需专家手动编码） | 极低（自动化） |
| 覆盖率 | 有限（仅高使用频率算子） | 高（481个算子，84.7%覆盖率） |
| 正确性保障 | 手动测试为主 | 自动化 OpInfo + 生产数据测试 |
| 可移植性 | 差（特定于平台） | 支持跨代芯片模拟 |
| 扩展速度 | 数月级 | “overnight”级别 |

> ✅ **优势总结**：TritorX 将后端内核开发从“手工艺”转变为“可测量的工程流程”，显著提升效率与可靠性。

---

## 2. 核心实验方法和设置

### 数据集与算子来源
- 使用 **PyTorch 的 OpInfo 测试套件**作为目标算子集。
- 初始筛选出 629 个 OpInfo 定义的 ATen 算子。
- 排除不兼容项（如复数运算、随机数生成等），最终保留 **568 个 MTIA 兼容的 ATen 算子**。
- 总计超过 **20,000 个测试用例**，覆盖不同数据类型（`bfloat16`, `float16`, `float32`, `int32`, `int64`）、张量形状和输入参数。

---

### 实验设置
- **目标平台**：Meta 的 MTIA 加速器（真实硅片 + QEMU 模拟器）
- **LLM 模型**：
  - 主生成模型：`Code World Model (CWM)` 和 `GPT-OSS 120B`
  - 反馈摘要模型：`Llama-4-Maverick`
- **上下文长度**：131,072 tokens
- **温度**：1.0；Top-P：0.95（CWM）/ 1.0（GPT-OSS）
- **最大尝试次数**：每算子最多 3 次对话会话（dialog session），每次最多 15 次 LLM 调用。
- **并行规模**：在 **200 台生产级 MTIA 设备**上并发执行。

---

### 评估指标
- **算子覆盖率（Operator Coverage）**：成功通过所有对应 OpInfo 测试的算子比例。
- **端到端模型支持率**：在实际模型（Nano-GPT, DLRM, Meta Models）中能被替代执行的算子占比。
- **消融分析**：移除关键组件（如 linter、摘要模块）后的性能下降情况。
- **成功率判定标准**：生成的 kernel-wrapper 对必须通过 **全部相关 OpInfo 测试** 才视为成功。

---

### 基线方法对比
文中未直接与其他完整系统进行全面对比，但明确指出：
- 不同于 `KERNELBENCH`、`TRITON-BENCH` 等仅关注 GPU 上的性能导向生成任务；
- 区别于 `AUTOTRITON`、`GEAK` 等强化学习或搜索增强方法；
- 强调自身以 **功能正确性与全覆盖为目标**，而非追求极致性能。

其主要参照系为“零样本提示”或“简单提示工程”的失败案例，证明 FSM + 反馈机制的必要性。

---

## 3. 主要实验结果和性能指标

### 关键性能数据
- 成功生成并通过所有测试的 ATen 算子数量：**481 个**
- 在 568 个兼容算子中的总体覆盖率：**84.7%**
- 单次大规模运行可在 **2 小时内完成 95% 的算子生成**
- 累计多次运行后可达更高覆盖率（通过 test-time scaling）

---

### 与基线配置的对比结果
| 配置 | CWM (%) | GPT-OSS (%) |
|------|---------|------------|
| Baseline（完整 TritorX） | 55.3 | 72.0 |
| 移除 Linter | 48.9 ↓ | 68.7 ↓ |
| 移除 Summarization 模块 | 48.2 ↓ | 71.5 ↓ |

> 🔍 结果表明：**Linter 和反馈摘要模块对最终性能有显著正向影响**，尤其在防止“作弊”和缓解上下文溢出方面至关重要。

---

### 按算子类别划分的覆盖率（Table 1）
| 算子类别 | 算子数 | CWM (%) | GPT-OSS (%) |
|--------|-------|--------|----------|
| Shape Manipulation | 75 | 96.0 | 96.0 |
| Elementwise | 161 | 80.1 | 84.6 |
| Linear Algebra | 78 | 71.8 | 79.5 |
| Reduction | 63 | 69.8 | 74.6 |
| Indexing & Selection | 34 | 73.5 | 79.4 |
| Other | 78 | 75.6 | 74.3 |
| **Deep Learning** | **90** | **64.4** | **71.1** |

> ⚠️ 发现：**深度学习类算子最难生成**，可能因其逻辑复杂、依赖多步调度。

---

### 端到端模型启用效果（Table 2）
在四类模型上的算子覆盖情况（使用生产数据输入测试）：

| 模型 | OpInfo 子集（初始） | 加入 MIS 反馈后（Refinement） |
|------|------------------|----------------------------|
| Nano-GPT | 80.0% | **100.0%** |
| DLRM | 80.0% | **90.0%** |
| Meta M1 | 83.8% | **91.9%** |
| Meta M2 | 81.7% | **87.3%** |

> ✅ 表明：已有 OpInfo 验证的内核在真实场景下已有良好泛化能力，进一步微调可大幅提升成功率。

---

### 其他重要结果
- 在下一代 MTIA 的 QEMU 模拟器上运行 TritorX，获得 **73.1% 覆盖率**，提前暴露编译器与硬件缺陷，为设计提供反馈。
- 通过聚合多个独立运行的结果（test-time scaling），仅两次运行即可将 CWM 的覆盖率从 55% 提升至 **64%**。

---

## 4. 关键结论和发现

### 主要发现
1. **TritorX 可实现高质量、高覆盖率的自动内核生成**，在真实 ASIC 上验证可行。
2. **FSM 架构 + 多重反馈机制（linting, compiling, testing, debugging）是成功的关键**，相比纯 prompt 工程更鲁棒。
3. **Linter 是防止“作弊”的核心组件**，有效阻止非法函数调用和非本地化实现。
4. **OpInfo 测试集提供了强大的泛化基础**，多数生成内核无需调整即可用于真实模型。
5. **该方法具备前瞻性**：可用于未来芯片的早期软硬件协同设计反馈。

---

### 局限性
1. **性能未优化**：当前工作只保证功能正确，不保证生成内核达到最优性能。
2. **部分复杂算子仍难处理**：尤其是涉及动态控制流或稀疏操作的 Deep Learning 类算子。
3. **依赖高质量测试集**：若 OpInfo 未能充分覆盖边界条件，可能导致漏检。
4. **LLM 的不确定性**：输出受随机性影响，需多次运行才能稳定提升覆盖率。

---

### 未来工作方向
1. **引入自洽生成机制**：允许 wrapper 调用已实现的其他算子，提升效率与性能。
2. **Prompt 优化与本地化增强**：利用历史成功案例进行上下文学习（in-context learning）。
3. **全代理化 Pipeline**：让 LLM 自主调用工具（如调试器、编译器日志分析器）。
4. **专用模型后训练**：对 LLM 进行 Triton MTIA 特定领域的微调。
5. **Test-Time Scaling 与进化策略**：探索更高效的集成与搜索机制以进一步提高覆盖率。
6. **建立全自动后端维护环境**：应对未来多代硬件共存下的 Kernel Library 自动更新需求。

---

## 总结
TritorX 提供了一个 **实用、可扩展、面向未来的 PyTorch 后端自动化构建蓝图**。它不仅解决了当前 ML ASIC 软件栈建设的瓶颈问题，也为 AI 驱动的编译器与硬件协同设计开辟了新路径。其核心思想——**以覆盖率为目标、以反馈为驱动、以生产环境为验证场**——具有广泛推广价值。

</details>

---

### 13. [Enhanced Pruning for Distributed Closeness Centrality under Multi-Packet Messaging](https://arxiv.org/abs/2512.11512)

**Authors**: Patrick D. Manya, Eugene M. Mbuyi, Gothy T. Ngoie, Jordan F. Masakuna  
**Category**: cs.DC  
**Published**: 2025-12-15  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2512.11512v1  

#### Abstract
Identifying central nodes using closeness centrality is a critical task in analyzing large-scale complex networks, yet its decentralized computation remains challenging due to high communication overhead. Existing distributed approximation techniques, such as pruning, often fail to fully mitigate th...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文核心结论与实验结果总结**

## **1. 论文的主要贡献和创新点**

### **解决了什么问题**
在大规模分布式网络中，**Closeness Centrality**（接近中心性）是识别关键节点的重要指标，但其去中心化计算面临严重的通信开销问题。现有的分布式近似方法（如 **pruning**）虽然能减少部分通信量，但在高吞吐、多包传输（multi-packet messaging）环境下仍存在大量消息交换，导致通信瓶颈。

特别是当采用 **multi-packet messaging** 来传输大块数据时，尽管提升了带宽利用率和抗丢包能力，但也显著增加了总消息数量，加剧了网络拥塞和延迟。

### **提出了什么新方法或新思路**
本文提出了一种**增强型分布式剪枝方法**（Enhanced Pruning），通过以下创新机制应对上述挑战：

- **引入 multi-packet messaging 架构**：将单个消息拆分为多个数据包进行传输，提升大容量数据传输的可靠性与效率。
- **优化剪枝策略以减少通信负载**：改进原始 pruning 方法，使某些节点（如叶子节点）在整个通信阶段不再发送任何消息，从而减少整体消息数。
- **采用滑动窗口协议（Go-Back-N ARQ）**：用于 multi-packet 传输中的可靠交付，在低误码率环境中实现高效重传机制。
- **设计通信过载限制机制**：对可剪枝节点施加通信限制，降低非中心节点的消息负担，同时保持估计精度。

### **相比现有方法的优势**
- 显著减少了**总消息数量**（平均减少 5%-15%），尤其适用于大型网络。
- 在不牺牲估计准确性的前提下，大幅提升了**通信效率**和**运行速度**。
- 能有效缓解 multi-packet messaging 带来的额外通信负担，避免因分包导致的消息爆炸。
- 支持异构网络拓扑和真实世界复杂图结构，具备良好的可扩展性。

---

## **2. 核心实验方法和设置**

### **使用的数据集**
实验在两类网络上进行：

- **随机生成网络**：
  - 共 100 个连通无向图，节点数 $ N \in [100, 2000] $
  - 基于 250×250 网格布局，欧氏距离小于通信范围 $ d=10 $ 的节点相连
  - 图直径分布在 [30, 68] 区间

- **真实世界网络**（共 35 个）：
  - **Facebook artist network**（艺术家社交关系）
  - **Phenomenology collaboration network**（高能物理合作网络）
  - **Gnutella peer-to-peer network**（P2P 文件共享网络）
  - **32 个 Autonomous System graphs**（互联网路由系统图）

### **实验设置和评估指标**

#### **参数配置**
- 最大迭代次数 $ D $ 小于图直径
- multi-packet 分包数 $ m \in \{1, 10, 20, 30, 50\} $
- 使用 **NetworkX** 构建图结构，**Pympler** 测量内存，**Timer** 模块记录时间

#### **评估指标**
| 指标 | 描述 |
|------|------|
| `Average/Max number of messages` | 每节点平均/最大发送消息数 |
| `Running time` | 算法执行时间（秒） |
| `Data loss rate` | 数据包丢失百分比 |
| `Memory usage` | 节点本地内存占用 |
| `Closeness centrality quality` | 近似最中心节点与真实最中心节点之间的最短路径距离 |
| `Statistical significance` | 使用 Wilcoxon signed-rank test 和 effect size 验证差异 |

### **基线方法对比**
- **Baseline**: 原始 pruning 方法（来自 Masakuna et al. [22]）
- **Proposed**: 本文提出的 enhanced pruning + multi-packet messaging 优化方案

---

## **3. 主要实验结果和性能指标**

### **关键性能数据**

#### **通信效率提升**
- 在所有测试图中（共 135 个），所提方法将每节点**平均消息数减少 5%–15%**。
- 对于最大消息数（max messages per node），优化效果更明显，尤其在高 $ m $ 场景下（如 $ m=50 $）。
- 如图 5 和图 6 所示，随着分包数增加，优势更加突出。

#### **运行时间和数据丢失**
- 使用 multi-packet messaging 后：
  - 当 $ m=50 $ 时，**运行时间下降约 30%**，**数据丢失率降低至接近 0%**
  - 单包传输（$ m=1 $）易发生拥塞和丢包，性能较差
- 图 3 显示：随着 $ m $ 增加，系统延迟更低，稳定性更高

#### **内存与本地开销**
- 每节点内存使用略有上升（见图 4），主要由于缓存多个数据包及滑动窗口缓冲区
- 局部处理开销增加约 10%-15%，但被通信节省所抵消，总体性价比高

#### **中心性估计质量**
- 表 II 显示：在不同 $ D $ 设置下，本文方法与原始 pruning 方法选出的“最中心节点”完全一致
- 两者到真实最中心节点的最短路径距离相同，说明**精度未受损**
- 特别是在 $ D \geq 14 $ 时，误差为 0，表明收敛良好

#### **统计显著性检验（表 III）**
| 指标 | p-value | Effect Size |
|------|--------|------------|
| 消息数量 | 0.0625 | 0.586（medium） |
| 运行时间 | 0.0876 | 0.437（medium） |
| 中心性质量 | ~0 | 0 |

> 注：虽然 p-value > 0.01（未达传统显著水平），但 **effect size 达到中等水平**，说明实际性能差异具有意义；而中心性质量的 p≈0，证明准确性高度一致。

---

## **4. 关键结论和发现**

### **主要发现**
1. **multi-packet messaging 是必要的**：在带宽受限或大数据传输场景下，能显著降低丢包率并提升吞吐。
2. **原始 pruning 方法在 multi-packet 下会恶化通信开销**：若不加以优化，分包反而增加总消息数。
3. **本文提出的增强 pruning 成功逆转这一趋势**：通过减少冗余通信（如禁止叶子节点发消息），实现了更高的通信效率。
4. **估计精度得以保留**：即使在极端剪枝条件下，最中心节点的选择与原方法完全一致。
5. **trade-off 可接受**：小幅增加的内存和本地计算代价，远小于通信成本的节约，尤其适合大规模网络。

### **方法的局限性**
- 假设节点不会在算法初期失效（否则邻居无法感知其状态）
- 依赖 FIFO、双向异步通信模型，在部分无线网络中可能难以满足
- 当 $ m $ 过大时，滑动窗口机制可能导致短暂阻塞，需进一步调优

### **未来工作方向**
- 将该方法扩展至其他 centrality 指标（如 Betweenness、Eigenvector）
- 在移动传感器网络、社交网络分析等真实场景中部署验证
- 探索动态网络下的自适应 pruning 策略
- 结合机器学习预测潜在可剪枝节点，进一步提前终止通信

---

> ✅ **总结一句话**：  
> 本文通过融合 **multi-packet messaging** 与 **enhanced pruning**，在不损失 Closeness Centrality 估计精度的前提下，显著降低了分布式计算中的通信开销，为大规模网络的去中心化分析提供了更具可扩展性的解决方案。

</details>

---

### 14. [Refining Graphical Neural Network Predictions Using Flow Matching for Optimal Power Flow with Constraint-Satisfaction Guarantee](https://arxiv.org/abs/2512.11127)

**Authors**: Kshitiz Khanal  
**Category**: cs.LG  
**Published**: 2025-12-15  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2512.11127v1  

#### Abstract
The DC Optimal Power Flow (DC-OPF) problem is fundamental to power system operations, requiring rapid solutions for real-time grid management. While traditional optimization solvers provide optimal solutions, their computational cost becomes prohibitive for large-scale systems requiring frequent rec...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Refining Graphical Neural Network Predictions Using Flow Matching for Optimal Power Flow with Constraint-Satisfaction Guarantee

---

## 1. 论文的主要贡献和创新点

### 解决的问题
传统的 **Optimal Power Flow (OPF)** 求解器（如 SLSQP）虽然能提供最优解，但在大规模系统中计算耗时较长，难以满足现代电网高频次调度需求（如高比例可再生能源接入场景）。而现有的 **Machine Learning (ML)** 方法虽速度快，但常面临两大挑战：
- **Constraint Violation**：神经网络输出不保证满足物理约束（如功率平衡、线路容量限制等）；
- **Optimality Gap**：预测解与真实最优解存在显著成本差距。

本文聚焦于 **DC-OPF** 问题，旨在实现**快速、可行且接近最优**的电力调度预测。

---

### 提出的新方法与创新思路

作者提出了一种**两阶段学习框架**，结合 **Physics-Informed Graph Neural Networks (GNNs)** 和 **Continuous Flow Matching (CFM)**：

#### （1）Stage 1: Physics-Informed GNN
- 利用图结构建模电网拓扑（节点为母线，边为输电线路），通过 **GCN** 层提取特征；
- 设计**多目标物理引导损失函数**，将以下先验知识嵌入训练过程：
  - **经济调度原则**（边际成本相等）
  - **基尔霍夫定律**（功率平衡）
  - **发电机上下限约束**
  - **KKT互补条件**（识别活跃约束）
- 引入**软投影层**（Soft Projection）用于训练期间梯度传播，以及**硬投影算法**（Hard Projection）确保推理时严格可行性。

#### （2）Stage 2: Continuous Flow Matching Refinement
- 使用 **CFM** 对 GNN 输出的初始可行解进行优化精炼；
- 将“从可行解到最优解”的映射视为一个**条件生成过程**，通过学习向量场 $v(p_g(t))$ 来驱动 ODE 积分路径：
  $$
  \frac{dp_g(t)}{dt} = v(p_g(t)),\quad p_g(0) = p_g^{(0)},\quad p_g(1) \approx p_g^*
  $$
- 采用线性插值路径构建概率流，避免传统 CNF 中昂贵的 ODE 反向传播；
- 在 CFM 训练中引入**渐进式课程学习**（curriculum learning），动态调整各损失权重。

---

### 相比现有方法的优势

| 维度 | 优势 |
|------|------|
| **可行性保障** | 通过物理引导损失 + 投影机制，实现 **100% 约束满足率**，优于普通 ML 方法 |
| **接近最优性** | 成本差距极小（<0.1% @ nominal load），远优于仅使用 GNN 的基线 |
| **泛化能力** | 在训练范围外的极端负荷场景下仍保持鲁棒性能（gap < 3%） |
| **训练效率** | CFM 无需反向ODE求解，支持混合精度训练，提升训练速度 |
| **结构合理性** | 分阶段设计：GNN 负责“可行性”，CFM 负责“最优性”，职责分离更高效 |

---

## 2. 核心实验方法和设置

### 数据集
- **IEEE 30-bus system**：标准测试系统，包含 30 个母线、6 台发电机、41 条线路。
- 发电机参数见原文 Table I，具有不同成本曲线和出力限值。

### 数据生成
- 随机采样负载水平在 **70%–100%** 基准负荷之间，共生成 **20,000 个训练样本**；
- 使用 **SLSQP 求解器** 获取每个样本对应的最优调度方案作为标签。

### 实验设置
- **Stage 1 GNN 训练**：
  - 40 epochs，Adam 优化器，lr=1e-5
  - 2 层 GCN（hidden dim=128），MLP 输出调度
- **Stage 2 CFM 训练**：
  - 100 epochs，AdamW，lr=3e-3，余弦退火
  - GNN 参数冻结，仅训练 ResNet 构成的向量场模型
  - 时间嵌入维度：64（32 对 sin/cos）
  - ODE 步数：训练 20 步，推理 30 步

### 评估指标
| 指标 | 定义 |
|------|------|
| **Cost Gap (%)** | $\frac{C(\hat{p}_g) - C(p^*)}{C(p^*)} \times 100\%$ |
| **Feasibility Rate (%)** | 所有约束满足的比例（容忍误差 ε=0.1 MW） |
| **Worst-Case Gap (%)** | 测试集中最大成本差距 |
| **Inference Time** | 单次前向推理时间（未详细报告数值，强调“远快于传统求解器”） |

### 基线方法对比
| 方法 | 描述 |
|------|------|
| **SLSQP Solver** | 数值优化基准（最优解） |
| **GNN Only** | 仅 Stage 1 输出，无 CFM 精炼 |
| **CFM Refined** | 本文完整两阶段方法 |

---

## 3. 主要实验结果和性能指标

### 性能汇总（Table II & III）

| 场景 | 最优成本 ($, mean±std) | GNN 成本 ($) | GNN Gap (%) | **CFM 成本 ($)** | **CFM Gap (%)** | **CFM Feas. (%)** | **Worst Gap (%)** |
|-------|------------------------|---------------|--------------|--------------------|------------------|---------------------|---------------------|
| Very Low (70–75%) | 490.24±10.32 | 528.80 | 7.86 | **490.39** | **0.03** | **100.0** | **0.04** |
| Low (83–88%) | 588.10±10.68 | 644.52 | 9.60 | **588.39** | **0.05** | **100.0** | **0.05** |
| Nominal (95–100%) | 680.75±11.44 | 754.10 | 10.78 | **681.20** | **0.07** | **100.0** | **0.16** |
| High (110–115%) | 823.24±14.68 | 901.26 | 9.48 | **844.59** | **2.59** | **100.0** | **3.60** |
| Very High (125–130%) | 982.93±16.11 | 1053.40 | 7.17 | **1010.88** | **2.84** | **100.0** | **3.15** |

### CFM 精炼带来的改进（Table III）

| 场景 | 成本降低 (%) | Gap 缩减 (p.p.) |
|-------|---------------|------------------|
| Very Low | 7.26 | 7.83 |
| Low | 8.71 | 9.55 |
| Nominal | **9.67** | **10.71** |
| High | 6.29 | 6.89 |
| Very High | 4.04 | 4.33 |

> ✅ **关键观察**：CFM 在训练分布内（nominal load）带来最大提升，即使在极端负载下也能稳定改善经济性。

---

### 消融分析与训练动态
- **非单调收敛行为**（图3）：
  - 初期低估成本（-21% gap），随后被惩罚项纠正至 +15%，最终收敛至 ~3%
  - 表明 **flow matching** 与 **cost-aware loss** 存在博弈，课程学习有效引导平衡
- **三阶段 curriculum learning** 显著提升稳定性与泛化能力
- **硬投影机制** 是实现 100% 可行性的关键组件

---

## 4. 关键结论和发现

### 主要发现
1. **物理先验是泛化的基石**：
   - 经济调度损失（marginal cost equality）、KKT 条件等作为归纳偏置，使模型能在 **70%–130%** 负荷范围内保持良好表现，包括训练范围之外。
   
2. **两阶段架构设计合理**：
   - GNN 负责快速生成**可行初值**，CFM 负责沿物理一致路径向最优解逼近；
   - 分工明确，避免端到端训练中的冲突优化目标。

3. **CFM 适用于优化问题精炼**：
   - 首次将 **Continuous Flow Matching** 应用于 OPF 这类**约束优化任务**；
   - 证明其不仅能用于图像/分子生成，也可用于“从可行到最优”的确定性轨迹学习。

4. **接近传统求解器质量，具备实时应用潜力**：
   - 在典型运行条件下（nominal load），成本差距低至 **0.07%**，且始终可行；
   - 推理为前向传播 + ODE 积分，适合部署于高频调度场景。

---

### 方法的局限性
1. **仅验证于小型系统**（IEEE 30-bus），尚未扩展至千节点级实际电网；
2. **基于 DC-OPF 模型**，忽略电压、无功功率及 AC 功率流非线性，限制在实际 AC 系统中的直接应用；
3. **假设负载分布平稳**，对罕见故障或剧烈分布偏移的鲁棒性有待验证；
4. **缺乏不确定性量化**，无法提供预测置信区间，影响调度决策可信度。

---

### 未来工作方向
1. **扩展至 AC-OPF**：
   - 引入复数电压变量、非线性潮流方程；
   - 加入电压幅值与 reactive power 约束；
   - 结合 AC 版本的 KKT 条件进行物理引导。

2. **规模化与分布式训练**：
   - 采用 hierarchical GNN 或 subgraph sampling 处理大型电网；
   - 支持 mini-batch 训练与多 GPU 并行。

3. **不确定性建模**：
   - 构建 **ensemble CFM** 或 **probabilistic flow models** 提供解分布；
   - 引入 worst-case guarantee 机制增强安全性。

4. **在线自适应学习**：
   - 支持 topology change（线路断开、新增机组）；
   - 利用 transfer learning / continual learning 适应参数漂移。

---

## 总结

该论文提出了一种新颖的 **physics-informed + flow-based refinement** 框架，在保证 **100% 约束可行性**的前提下，实现了接近传统优化器精度的 DC-OPF 快速求解。其核心思想——“**先可行后最优**”的两阶段策略，为机器学习应用于安全关键型基础设施优化提供了范式参考。尽管当前局限于小规模 DC 模型，但其设计理念具有很强的可拓展性，是迈向智能、快速、可靠电网调度的重要一步。

</details>

---

### 15. [Mitigating the Safety Alignment Tax with Null-Space Constrained Policy Optimization](https://arxiv.org/abs/2512.11391)

**Authors**: Yifan Niu, Han Xiao, Dongyi Liu, Nuo Chen, Jia Li  
**Category**: cs.LG  
**Published**: 2025-12-15  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2512.11391v1  

#### Abstract
As Large Language Models (LLMs) are increasingly deployed in real-world applications, it is important to ensure their behaviors align with human values, societal norms, and ethical principles. However, safety alignment under Reinforcement Learning (RL) often suffers from forgetting learned general a...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文核心结论与实验结果总结

## 1. 论文的主要贡献和创新点

### 解决的问题
该论文旨在解决大型语言模型（LLMs）在进行 **safety alignment**（安全对齐）时普遍存在的“**alignment tax**”（对齐税）问题。这一现象指的是，在提升模型安全性的同时，往往会损害其原有的通用能力（如数学推理、代码生成、指令遵循等），导致模型变得过度谨慎甚至拒绝回答无害问题。

### 提出的新方法与新思路
作者提出了 **Null-Space constrained Policy Optimization (NSPO)**，一种新颖的强化学习（RL）框架，用于实现安全对齐，同时有效缓解对齐税。其核心思想是：

- **几何投影机制**：将安全策略梯度（safety policy gradients）**几何地投影到通用任务表征的零空间（null space）中**。
- **解耦更新**：通过这种投影，确保安全优化的方向与模型已习得的通用能力子空间正交，从而在不干扰通用能力的前提下提升安全性。
- **移除KL散度**：NSPO 移除了传统RLHF方法中的KL散度项，因为该惩罚项会与安全目标冲突；而零空间投影本身就能防止策略过度偏离，是一种更优的约束方式。

### 相比现有方法的优势
- **性能卓越**：在多个安全基准上达到SOTA（state-of-the-art）水平，显著优于现有的DPO、SafeRLHF等方法。
- **能力保留**：在大幅提升安全性的同时，几乎不损害模型在数学、代码、指令遵循等通用任务上的性能。
- **数据高效**：仅需使用 **40% 的公共人类标注安全数据**（来自PKU-SafeRLHF）即可取得优异效果，无需混合大量通用任务数据进行训练，大大降低了数据需求。

---

## 2. 核心实验方法和设置

### 使用的数据集
- **训练数据**：
  - **PKU-SafeRLHF**：用于NSPO的安全对齐训练，NSPO仅使用其中的 **40%**（约11K样本）。
- **投影矩阵构造数据**：
  - 从三个领域的混合数据集中随机采样1000个实例构建投影矩阵 $K$，涵盖：
    - **Common sense**（常识）
    - **Math**（数学）
    - **Code**（代码）
- **评估基准**：
  - **安全基准（Safety Benchmarks）**：
    - *Harmful Query*: AdvBench, PKU-SafeRLHF, HarmBench, JailbreakBench, SORRY-Bench
    - *Red-team Query*: HarmfulQA, ALERT
  - **通用能力基准（General Capability Benchmarks）**：
    - *General Knowledge*: MMLU, SuperGPQA
    - *Instruction Following*: AlpacaEval
    - *Code Generation*: LiveCodeBench
    - *Math & Reasoning*: GSM8K, MATH, OlympiadBench

### 实验设置和评估指标
- **基础模型**：`Llama3-8B-Instruct`, `Qwen2.5-7B-Instruct`
- **安全奖励模型**：`Llama-Guard-4-12B`
- **训练框架**：基于 `verl` 框架实现，以 `GRPO` 为基础算法。
- **评估指标**：
  - **安全性能**：**Attack Success Rate (ASR)**，越低越好。
  - **通用能力**：
    - **Accuracy**（MMLU, GSM8K, MATH, OlympiadBench）
    - **Win Rate (WR)**（AlpacaEval）
    - **Pass@1**（LiveCodeBench）

### 基线方法对比
论文对比了多种主流的对齐方法作为基线：
- **DPO 变体**：`DPO-H`, `DPO-S`, `DPO-Mix`
- **RLHF 类方法**：`SafeRLHF`, `PeCAN`, `MoCAN`, `W-DOOR`, `BFPO`

---

## 3. 主要实验结果和性能指标

### 关键性能数据
根据 **Table 1** 和 **Table 2** 的结果，NSPO 在 `Llama3-8B-Instruct` 和 `Qwen2.5-7B-Instruct` 上均表现出色：

#### 安全性能（ASR ↓）
- 在 `HarmBench` 上，NSPO 将 ASR 降至 **0.18%**（Llama3）和 **0.50%**（Qwen2.5），远低于其他方法（如 DPO-H 达到 57.32%）。
- 在 `JailbreakBench` 上，NSPO 表现同样领先，ASR 为 **1.33%**（Llama3）和 **0.67%**（Qwen2.5）。
- 在 `SORRY-Bench` 上，NSPO 将 ASR 从基线的 34.15% 降至 **16.81%**（Llama3），效果显著。

#### 通用能力（Accuracy/WR/Pass@1 ↑）
- **MMLU**：NSPO 得分 **63.64%**（Llama3）和 **71.74%**（Qwen2.5），与基线模型（63.79%, 71.71%）几乎持平，且优于大多数基线方法。
- **GSM8K**：NSPO 得分 **76.42%**（Llama3）和 **81.96%**（Qwen2.5），保持高水平。
- **LiveCodeBench (Pass@1)**：NSPO 得分 **13.43%**（Llama3）和 **24.05%**（Qwen2.5），基本无损。
- 总体来看，NSPO 在通用能力上的性能下降**通常在1%以内**，极少数情况不超过2.67%，而其他方法（如 DPO-H, SafeRLHF）则出现显著下降。

### 与基线方法的对比结果
- **安全性能**：NSPO 在所有7个安全基准上均取得**最佳或次佳**成绩，显著优于所有基线。
- **通用能力**：NSPO 在所有7个通用能力基准上表现稳健，性能损失最小，**仅次于 BFPO**，但 BFPO 需要混合数据，而 NSPO 仅用纯安全数据。
- **综合表现**：NSPO 成功实现了**安全性和通用能力的最优平衡**，解决了传统方法中存在的 trade-off 问题。

### 消融实验结果
- **投影的有效性**（Figure 4）：
  - `GRPO w/o KL`：安全好，但通用能力严重退化。
  - `Standard GRPO`：通用能力恢复，但安全性能大幅下降。
  - `NSPO`：**两者兼顾**，证明零空间投影是更有效的约束机制。
- **投影矩阵 $K$ 的影响**（Figure 5, 6）：
  - **数据来源**：使用混合领域数据（`Mix`）构建 $K$ 能最好地保留通用能力。
  - **样本大小**：增大样本量有助于保留通用能力，但可能轻微牺牲安全性。
  - **特征值阈值**：中等阈值（如 5e-4）能实现安全与能力的最佳权衡。

---

## 4. 关键结论和发现

### 主要发现
1. **零空间投影是缓解对齐税的有效机制**：通过将安全梯度限制在不影响通用能力的子空间内，NSPO 成功实现了安全优化与能力保留的解耦。
2. **NSPO 是数据高效的**：仅需 40% 的安全数据即可达到甚至超越使用全量数据的方法的性能，无需额外的通用任务数据。
3. **理论保证**：论文从理论上证明了 NSPO 既能保留原始模型的核心能力，又能保证安全目标的有效下降方向。
4. **可视化验证**：t-SNE 可视化显示，NSPO 在安全相关表示上有更大变化，而在通用能力表示上变化较小，直观验证了其选择性更新的特性。

### 方法的局限性
- **依赖预定义的通用能力数据**：需要一个独立的通用任务数据集来构建投影矩阵 $K$，这增加了额外的数据准备步骤。
- **计算开销**：虽然作者声称开销小，但 SVD 分解和投影操作仍会引入一定的计算和内存负担，尤其是在大模型上。
- **零空间的近似性**：实际应用中，严格为零的特征值难以获得，需通过设定阈值来近似，这可能影响投影的精确性。

### 未来工作方向
- 探索更高效的零空间计算方法，减少对 SVD 的依赖。
- 研究如何动态更新投影矩阵 $K$，以适应不同阶段的训练。
- 将 NSPO 框架扩展到多目标对齐（multi-objective alignment）场景，处理更多维度的约束。
- 探索在更小模型或不同架构上的泛化能力。

> **代码开源**：该方法的代码已在 GitHub 开源：[https://github.com/ivanniu/NSPO](https://github.com/ivanniu/NSPO)

</details>

---

### 16. [TriFlow: A Progressive Multi-Agent Framework for Intelligent Trip Planning](https://arxiv.org/abs/2512.11271)

**Authors**: Yuxing Chen, Basem Suleiman, Qifan Chen  
**Category**: cs.AI  
**Published**: 2025-12-15  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.11271v1  

#### Abstract
Real-world trip planning requires transforming open-ended user requests into executable itineraries under strict spatial, temporal, and budgetary constraints while aligning with user preferences. Existing LLM-based agents struggle with constraint satisfaction, tool coordination, and efficiency, ofte...

---

### 17. [EmeraldMind: A Knowledge Graph-Augmented Framework for Greenwashing Detection](https://arxiv.org/abs/2512.11506)

**Authors**: Georgios Kaoukis, Ioannis Aris Koufopoulos, Psaroudaki Eleni, Danae Pla Karidi, Evaggelia Pitoura, George Papastefanatos, Panayiotis Tsaparas  
**Category**: cs.AI  
**Published**: 2025-12-15  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.11506v1  

#### Abstract
As AI and web agents become pervasive in decision-making, it is critical to design intelligent systems that not only support sustainability efforts but also guard against misinformation. Greenwashing, i.e., misleading corporate sustainability claims, poses a major challenge to environmental progress...

---

### 18. [FutureWeaver: Planning Test-Time Compute for Multi-Agent Systems with Modularized Collaboration](https://arxiv.org/abs/2512.11213)

**Authors**: Dongwon Jung, Peng Shi, Yi Zhang  
**Category**: cs.AI  
**Published**: 2025-12-15  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2512.11213v1  

#### Abstract
Scaling test-time computation improves large language model performance without additional training. Recent work demonstrates that techniques such as repeated sampling, self-verification, and self-reflection can significantly enhance task success by allocating more inference-time compute. However, a...

---

### 19. [Mistake Notebook Learning: Selective Batch-Wise Context Optimization for In-Context Learning](https://arxiv.org/abs/2512.11485)

**Authors**: Xuanbo Su, Yingfang Zhang, Hao Luo, Xiaoteng Liu, Leo Huang  
**Category**: cs.CL  
**Published**: 2025-12-15  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2512.11485v1  

#### Abstract
Large language models (LLMs) adapt to tasks via gradient fine-tuning (heavy computation, catastrophic forgetting) or In-Context Learning (ICL: low robustness, poor mistake learning). To fix this, we introduce Mistake Notebook Learning (MNL), a training-free framework with a persistent knowledge base...

---

### 20. [Deep Learning--Accelerated Multi-Start Large Neighborhood Search for Real-time Freight Bundling](https://arxiv.org/abs/2512.11187)

**Authors**: Haohui Zhang, Wouter van Heeswijk, Xinyu Hu, Neil Yorke-Smith, Martijn Mes  
**Category**: cs.AI  
**Published**: 2025-12-15  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2512.11187v1  

#### Abstract
Online Freight Exchange Systems (OFEX) play a crucial role in modern freight logistics by facilitating real-time matching between shippers and carrier. However, efficient combinatorial bundling of transporation jobs remains a bottleneck. We model the OFEX combinatorial bundling problem as a multi-co...

---

### 21. [When Actions Teach You to Think: Reasoning-Action Synergy via Reinforcement Learning in Conversational Agents](https://arxiv.org/abs/2512.11277)

**Authors**: Mrinal Rawat, Arkajyoti Chakraborty, Neha Gupta, Roberto Pieraccini  
**Category**: cs.CL  
**Published**: 2025-12-15  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2512.11277v1  

#### Abstract
Supervised fine-tuning (SFT) has emerged as one of the most effective ways to improve the performance of large language models (LLMs) in downstream tasks. However, SFT can have difficulty generalizing when the underlying data distribution changes, even when the new data does not fall completely outs...

---

### 22. [Harnessing Rich Multi-Modal Data for Spatial-Temporal Homophily-Embedded Graph Learning Across Domains and Localities](https://arxiv.org/abs/2512.11178)

**Authors**: Takuya Kurihana, Xiaojian Zhang, Wing Yee Au, Hon Yung Wong  
**Category**: cs.LG  
**Published**: 2025-12-15  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2512.11178v1  

#### Abstract
Modern cities are increasingly reliant on data-driven insights to support decision making in areas such as transportation, public safety and environmental impact. However, city-level data often exists in heterogeneous formats, collected independently by local agencies with diverse objectives and sta...

---

### 23. [Bandwidth-constrained Variational Message Encoding for Cooperative Multi-agent Reinforcement Learning](https://arxiv.org/abs/2512.11179)

**Authors**: Wei Duan, Jie Lu, En Yu, Junyu Xuan  
**Category**: cs.LG  
**Published**: 2025-12-15  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2512.11179v1  

#### Abstract
Graph-based multi-agent reinforcement learning (MARL) enables coordinated behavior under partial observability by modeling agents as nodes and communication links as edges. While recent methods excel at learning sparse coordination graphs-determining who communicates with whom-they do not address wh...

---

### 24. [PIAST: Rapid Prompting with In-context Augmentation for Scarce Training data](https://arxiv.org/abs/2512.11013)

**Authors**: Pawel Batorski, Paul Swoboda  
**Category**: cs.CL  
**Published**: 2025-12-15  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.11013v1  

#### Abstract
LLMs are highly sensitive to prompt design, but handcrafting effective prompts is difficult and often requires intricate crafting of few-shot examples. We propose a fast automatic prompt construction algorithm that augments human instructions by generating a small set of few shot examples. Our metho...

---

### 25. [Extending a Parliamentary Corpus with MPs' Tweets: Automatic Annotation and Evaluation Using MultiParTweet](https://arxiv.org/abs/2512.11567)

**Authors**: Mevl\"ut Bagci, Ali Abusaleh, Daniel Baumartz, Giueseppe Abrami, Maxim Konca, Alexander Mehler  
**Category**: cs.CL  
**Published**: 2025-12-15  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.11567v1  

#### Abstract
Social media serves as a critical medium in modern politics because it both reflects politicians' ideologies and facilitates communication with younger generations. We present MultiParTweet, a multilingual tweet corpus from X that connects politicians' social media discourse with German political co...

---

### 26. [Pace: Physics-Aware Attentive Temporal Convolutional Network for Battery Health Estimation](https://arxiv.org/abs/2512.11332)

**Authors**: Sara Sameer, Wei Zhang, Kannan Dhivya Dharshini, Xin Lou, Yulin Gao, Terence Goh, Qingyu Yan  
**Category**: cs.LG  
**Published**: 2025-12-15  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.11332v1  

#### Abstract
Batteries are critical components in modern energy systems such as electric vehicles and power grid energy storage. Effective battery health management is essential for battery system safety, cost-efficiency, and sustainability. In this paper, we propose Pace, a physics-aware attentive temporal conv...

---

### 27. [Hyperbolic Gaussian Blurring Mean Shift: A Statistical Mode-Seeking Framework for Clustering in Curved Spaces](https://arxiv.org/abs/2512.11448)

**Authors**: Arghya Pratihar, Arnab Seal, Swagatam Das, Inesh Chattopadhyay  
**Category**: cs.LG  
**Published**: 2025-12-15  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.11448v1  

#### Abstract
Clustering is a fundamental unsupervised learning task for uncovering patterns in data. While Gaussian Blurring Mean Shift (GBMS) has proven effective for identifying arbitrarily shaped clusters in Euclidean space, it struggles with datasets exhibiting hierarchical or tree-like structures. In this w...

---

### 28. [Elastic-Net Multiple Kernel Learning: Combining Multiple Data Sources for Prediction](https://arxiv.org/abs/2512.11547)

**Authors**: Janaina Mour\~ao-Miranda, Zakria Hussain, Konstantinos Tsirlis, Christophe Phillips, John Shawe-Taylor  
**Category**: cs.LG  
**Published**: 2025-12-15  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.11547v1  

#### Abstract
Multiple Kernel Learning (MKL) models combine several kernels in supervised and unsupervised settings to integrate multiple data representations or sources, each represented by a different kernel. MKL seeks an optimal linear combination of base kernels that maximizes a generalized performance measur...

---

### 29. [High-Dimensional Surrogate Modeling for Closed-Loop Learning of Neural-Network-Parameterized Model Predictive Control](https://arxiv.org/abs/2512.11705)

**Authors**: Sebastian Hirt, Valentinus Suwanto, Hendrik Alsmeier, Maik Pfefferkorn, Rolf Findeisen  
**Category**: cs.LG  
**Published**: 2025-12-15  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.11705v1  

#### Abstract
Learning controller parameters from closed-loop data has been shown to improve closed-loop performance. Bayesian optimization, a widely used black-box and sample-efficient learning method, constructs a probabilistic surrogate of the closed-loop performance from few experiments and uses it to select ...

---

### 30. [BAID: A Benchmark for Bias Assessment of AI Detectors](https://arxiv.org/abs/2512.11505)

**Authors**: Priyam Basu, Yunfeng Zhang, Vipul Raheja  
**Category**: cs.AI  
**Published**: 2025-12-15  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2512.11505v1  

#### Abstract
AI-generated text detectors have recently gained adoption in educational and professional contexts. Prior research has uncovered isolated cases of bias, particularly against English Language Learners (ELLs) however, there is a lack of systematic evaluation of such systems across broader sociolinguis...

---

## 🔧 Configuration

This bot is configured to look for papers containing the following keywords:
- framework, System, Generation, Video, Linear, LLM, RL, RLHF, Inference, Training, Attention, Pipeline, MOE, Sparse, Quantization, Speculative, Efficient, Efficiency, Framework, Parallel, Distributed, Kernel, Decode, Decoding, Prefill, Throughput, Fast, Network, Hardware, Cluster, FP8, FP4, Optimization, Scalable, Communication

## 📅 Schedule

The bot runs daily at 12:00 UTC via GitHub Actions to fetch the latest papers.

## 🚀 How to Use

1. **Fork this repository** to your GitHub account
2. **Customize the configuration** by editing `config.json`:
   - Add/remove arXiv categories (e.g., `cs.AI`, `cs.LG`, `cs.CL`)
   - Modify keywords to match your research interests
   - Adjust `max_papers` and `days_back` settings
3. **Enable GitHub Actions** in your repository settings
4. **The bot will automatically run daily** and update the README.md

## 📝 Customization

### arXiv Categories
Common categories include:
- `cs.AI` - Artificial Intelligence
- `cs.LG` - Machine Learning
- `cs.CL` - Computation and Language
- `cs.CV` - Computer Vision
- `cs.NE` - Neural and Evolutionary Computing
- `stat.ML` - Machine Learning (Statistics)

### Keywords
Add keywords that match your research interests. The bot will search for these terms in paper titles and abstracts.

### Exclude Keywords
Add terms to exclude certain types of papers (e.g., "survey", "review", "tutorial").

## 🔍 Manual Trigger

You can manually trigger the bot by:
1. Going to the "Actions" tab in your repository
2. Selecting "arXiv Bot Daily Update"
3. Clicking "Run workflow"

---
*Generated automatically by arXiv Bot* 
