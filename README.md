# arXiv Papers Bot 🤖

This repository automatically fetches and displays relevant papers from arXiv based on configured criteria.

## RSS Vercel Deployment [![An example of deployed RSS Server using vercel](https://img.shields.io/badge/Deployed-Example-blue)](https://arxiv.tachicoma.top/)

You can click this to deploy yours 

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/maydomine/arxiv_rss_bot)
## 📊 Statistics

- **Last Updated**: 2025-12-08 05:55:13 UTC
- **Total Papers Found**: 30
- **Categories Monitored**: cs.AI, cs.CL, cs.DC, cs.LG

## 📚 Recent Papers

### 1. [SQ-format: A Unified Sparse-Quantized Hardware-friendly Data Format for LLMs](https://arxiv.org/abs/2512.05409)

**Authors**: Ruixuan Huang, Hao Zeng, Hantao Huang, Jinyuan Shi, Minghui Yu, Ian En-Hsu Yen, Shuai Wang  
**Category**: cs.CL  
**Published**: 2025-12-08  
**Score**: 10.5  
**Type**: new  
**ArXiv ID**: 2512.05409v1  

#### Abstract
Post-training quantization (PTQ) plays a crucial role in the democratization of large language models (LLMs). However, existing low-bit quantization and sparsification techniques are difficult to balance accuracy and efficiency due to the limited hardware support. For example, W4A8 can only achieve ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：SQ-format: A Unified Sparse-Quantized Hardware-friendly Data Format for LLMs

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

当前大语言模型（LLMs）在部署时面临**精度与效率难以兼顾**的问题。尽管 Post-Training Quantization (PTQ) 和 sparsification 技术可以压缩模型，但存在以下瓶颈：

- **硬件支持不足**：现有的低比特量化（如 W4A8）在现代 GPU 上无法发挥理论吞吐优势，因为缺乏对混合精度操作（如 INT4 × INT8）的原生支持，通常会回退到更高精度路径（如 W8A8），导致性能增益消失。
- **静态稀疏格式灵活性差**：例如 NVIDIA 的 2:4 semi-structure sparse 虽然硬件友好，但难以适应 LLM 中权重和激活值中“非均匀信息分布”的特性（outlier 存在），导致精度显著下降。
- **动态压缩开销高**：基于运行时选择重要元素的方法（如 TopK）引入额外计算开销，影响实际推理速度。

这些问题共同造成了 **“算法-硬件鸿沟”（hardware-algorithm gap）**。

---

### **提出了什么新方法或新思路**

作者提出了一种统一的稀疏-量化数据格式 —— **Sparse-Quantized Format (SQ-format)**，其核心思想是：

> 将一个张量划分为**高精度稀疏部分**（保留关键值）和**低精度密集部分**（压缩普通值），并以一种**硬件友好的方式组织存储和计算**。

#### 核心设计特点：
- **统一格式**：同时支持量化（quantization）与稀疏化（sparsification），适用于 weights 或 activations。
- **Bank-based 结构**：将矩阵按固定大小 `bank` 分块，每块内固定稀疏度 `s`，避免负载不均衡和复杂索引。
- **双路径计算**：利用 tensor cores 并行处理高精度（sparse）和低精度（dense）部分，通过专用硬件单元（gather/scatter）协调。
- **静态激活策略**：为解决动态 TopK 开销，提出基于校准集预生成 mask 的 **static SQ-format on activations**，实现零运行时开销。

该格式可看作是对 NVIDIA 2:4 sparse 的泛化扩展（当 `s=0.5`, `h_low=0`, `b=4` 时退化为 2:4 sparse）。

---

### **相比现有方法的优势**

| 维度 | SQ-format 优势 |
|------|----------------|
| **精度-效率权衡** | 实现了 Pareto 改进：在接近 W4A8 精度的同时，达到 W4A4 级别的吞吐。 |
| **硬件兼容性** | 可在现有 GPU 上模拟运行，并为下一代 AI 加速器提供 co-design 蓝图。 |
| **灵活性** | 支持多种 `(h_high/h_low)` 配置（如 INT8/INT4, INT8/INT2）和 sparsity 设置。 |
| **系统开销低** | 静态策略仅需极小额外存储（如 Llama-3-70B 仅 5.94MB）。 |
| **稳定性强** | 在大模型（Llama-3-70B, Qwen-3-30B）上表现更优，尤其生成任务准确率提升明显。 |

---

## 2. 核心实验方法和设置

### **使用的模型与数据集**

#### **模型**
- **Dense LLMs**: Llama-3-8B, Llama-3-70B
- **MoE Model**: Qwen-3-30B-A3B
- **超大规模模型验证**: DeepSeek-R1 (685B)

#### **评估数据集**
- **非生成类任务（zero-shot）**:
  - ARC-easy/challenge, PIQA, Hellaswag, Winogrande, OpenBookQA
- **生成类任务**:
  - GSM8k (8-shot), AGIEval (zero-shot)
- **困惑度（Perplexity）测试**:
  - Wikitext, Lambada

使用 `lm-evaluation-harness` 进行统一评测。

---

### **实验设置与评估指标**

#### **量化配置表示法**
- `W(SQx)Ay`: 权重使用 SQ-format，等效比特数为 `x`，激活使用 INTy
- `WxA(SQy)`: 激活使用 SQ-format，等效比特数为 `y`
- 示例：`B-(8/4)=0.5` 表示 bank size=64, h_high=INT8, h_low=INT4, sparsity=0.5（即 2x sparse）

#### **校准集（Calibration Set）**
- 使用 Wikitext 中随机采样的 32 个文本段，每段 2048 tokens。

#### **搜索空间**
- Bank size: {4, 8, 16, 32, 64, 128}
- Sparsity: {0.5, 0.75, 0.875, 0.9375}（对应 2x 到 16x 稀疏）
- h_high/h_low: 多种组合（如 8/4, 8/3, 8/2, 4/2）

选取最优结果报告，完整网格见附录。

---

### **基线方法对比**

| 类型 | 基线方法 |
|------|--------|
| **PTQ 方法** | GPTQ, SmoothQuant, SpinQuant |
| **稀疏化方法** | SpQR, SparseGPT |
| **基准配置** | W4A8, W4A4, W8A8, BF16 |

---

## 3. 主要实验结果和性能指标

### **关键性能数据汇总（来自 Table 1 & D）**

| 方法 | 模型 | Non-gen Avg (%) | Gen Avg (%) | Perplexity ↓ | Speedup vs W4A8 |
|------|------|------------------|-------------|---------------|------------------|
| **BF16** | Llama-3-70B | 75.6 | 63.36 | 2.92 / 2.58 | 1.00× |
| **W4A8** | Llama-3-70B | 72.6 | 61.68 | 3.48 / 2.61 | 1.00× |
| **W4A4** | Llama-3-70B | 68.9 | 55.71 | 4.58 / 2.98 | **1.92×** |
| **SQ W4A(SQ6)** | Llama-3-70B | **72.8** | **61.5** | **3.31 / 2.65** | **1.71×** |
| **SQ W(SQ6)A4** | Llama-3-70B | 71.5 | 59.73 | 3.79 / 2.66 | — |

> 注：SQ W4A(SQ6) 在保持与 W4A8 接近精度的同时，获得 **1.71× 速度提升**，接近 W4A4 的理论极限（1.92×）。

---

### **与基线方法的对比结果**

#### ✅ **优于主流 PTQ 方法**
- 在 Llama-3-70B 上，`W4A(SQ6)` 的平均 non-gen 准确率比 GPTQ 高约 0.3%，且 perplexity 更低。
- 相比 SmoothQuant 和 SpinQuant，SQ-format 显著更稳定，尤其在生成任务上避免崩溃（如 SmoothQuant 在 W4A4 下 GSM8k 为 0%）。

#### ✅ **优于稀疏化方法**
- SpQR 和 SparseGPT 在 W4A16 或全精度下仍损失较大精度（如 SparseGPT 在 Llama-3-8B 上 ARC-c 从 53.41→31.74）。
- SQ-format 在同等稀疏度下精度远超这些方法。

#### ✅ **实现 Pareto 最优**
- 如 Figure 5 所示，SQ-format 成功填补了 W4A8（高精度低速）与 W4A4（高速低精度）之间的空白，在精度几乎无损的前提下大幅提升吞吐。

---

### **消融实验结果**

#### （1）**静态 vs 动态激活 SQ-format（Table 2）**
- 静态策略性能与动态策略相差 <1%，说明预生成 mask 具备良好泛化能力。
- 静态策略更适合部署，无需运行时 TopK，适合 GPU 实现。

#### （2）**Bank Size 与 Sparsity 影响（Figures 7 & 8）**
- **Bank size 过小（如 4）** 导致精度分区不灵活，低精度部分仍含 outlier，效果差。
- **推荐 bank size ≥ 32**，尤其对于 weights；activations 对 bank size 敏感度较低。
- **Sparsity 越高越好**，但受限于硬件算力平衡（如 W8A8 是 W4A4 的 4 倍，则 sparsity 至少需 0.75 才能掩盖高精度路径延迟）。

#### （3）**不同 h_high/h_low 配置**
- `INT8/INT4` 表现最佳；
- `INT8/INT3` 可作为折中方案；
- `INT8/INT2` 仅在低 sparsity 下可用，否则精度严重下降。

#### （4）**FP 格式验证（Table 8）**
- 在 DeepSeek-R1 上应用 `W(SQ5)A8`（FP8/FP4）：
  - 性能几乎无损（如 GSM8k: 95.83 → 96.21）
  - 展现出强大的可扩展性和跨数据类型适用性。

#### （5）**硬件合成分析（Table 9）**
- 相比标准 INT6 MAC 阵列，SQ-format 单元总面积减少 **35.8%**，证明其物理实现高效。

---

## 4. 关键结论和发现

### **主要发现**

1. ✅ **SQ-format 实现了精度与吞吐的 Pareto 改进**：
   - 在 Llama-3-70B 上实现 **1.71× 速度提升**，同时保持接近 W4A8 的精度。
   - 吞吐逼近 W4A4 极限（~89%），但精度高出 3.9 个百分点。

2. ✅ **静态激活策略可行且高效**：
   - 基于 activation-weight product 贡献预先生成 mask，性能媲美动态方法。
   - 消除 TopK 开销，更适合硬件部署。

3. ✅ **Bank-based 设计优于传统稀疏模式**：
   - 固定 bank 和 sparsity 提升硬件调度效率，避免 unstructured sparse 的负载不均问题。
   - 比 2:4 semi-structure sparse 更灵活，能更好捕捉信息分布。

4. ✅ **具备良好的通用性和扩展性**：
   - 支持 INT 和 FP 格式（已验证 FP8/FP4）。
   - 在不同规模模型（8B~685B）上均有效。

---

### **方法的局限性**

1. **依赖专用硬件才能完全释放潜力**：
   - 当前在 GPU 上为模拟实现，真实加速需定制硬件支持（如 Figure 2a 所示的 gather unit）。
   - 动态策略若无专用 pipeline 单元，仍依赖 CUDA core 计算 mask。

2. **极端低比特（如 INT2）支持有限**：
   - `h_low=INT2` 时即使引入高精度补偿也难维持精度，限制了极致压缩场景。

3. **bank size 选择需权衡灵活性与面积成本**：
   - 大 bank size（如 128）虽更灵活，但增加 MUX 和控制逻辑开销。

---

### **未来工作方向**

1. **推动 SQ-format 成为下一代 AI 加速器的标准指令集扩展**。
2. **探索更多 h_high/h_low 组合与自适应 sparsity 控制机制**。
3. **将 SQ-format 应用于训练阶段（QAT）以进一步提升性能**。
4. **结合 MoE 架构优化专家稀疏路由与参数压缩协同设计**。

---

## 总结

> SQ-format 提出了一种**算法-硬件协同设计的新范式**，通过将稀疏化与量化统一在一个硬件友好的格式中，成功打破了 PTQ 中长期存在的“精度-效率悖论”。它不仅为当前 LLM 部署提供了实用高效的解决方案，也为未来 AI 芯片架构设计指明了方向——**细粒度、混合精度、结构化稀疏将成为主流**。

</details>

---

### 2. [When Forgetting Builds Reliability: LLM Unlearning for Reliable Hardware Code Generation](https://arxiv.org/abs/2512.05341)

**Authors**: Yiwen Liang, Qiufeng Li, Shikai Wang, Weidong Cao  
**Category**: cs.LG  
**Published**: 2025-12-08  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2512.05341v1  

#### Abstract
Large Language Models (LLMs) have shown strong potential in accelerating digital hardware design through automated code generation. Yet, ensuring their reliability remains a critical challenge, as existing LLMs trained on massive heterogeneous datasets often exhibit problematic memorization of propr...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*When Forgetting Builds Reliability: LLM Unlearning for Reliable Hardware Code Generation*

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前用于硬件代码生成的 **Large Language Models (LLMs)** 在训练过程中可能记忆了以下三类有害内容：
- **专有知识产权 (IP)**：导致潜在的法律风险和泄露；
- **被污染的基准测试数据**：影响模型评估的公平性和可信度；
- **不安全的编码模式**：引入功能错误或安全隐患。

这些问题严重威胁了 LLM 在数字硬件设计中的**可靠性与安全性**。传统的“从头重训”方法成本过高（如 LLaMA-3 需百万级 GPU 小时），而通用的机器遗忘（machine unlearning）方法在应用于硬件描述语言（HDL）时会破坏语法正确性，导致生成不可综合（non-synthesizable）的 RTL 代码。

因此，本文旨在解决：**如何高效、可靠地让 LLM “忘记”特定有害知识，同时保持其高质量硬件代码生成能力？**

---

### 提出的新方法与创新思路

作者提出了首个面向硬件代码生成任务的**领域定制化 LLM 遗忘框架**，核心包含两大技术创新：

#### ✅ 创新点一：Syntax-Preserving Unlearning（语法保持型遗忘）
- **动机**：RTL 代码具有严格的语法结构（如 `always @(posedge clk)`、`module/endmodule` 等），若遗忘过程中修改这些关键词或结构，会导致生成代码无法编译。
- **方法**：
  - 引入 **keyword masking**：对 Verilog 关键字（如 `wire`, `reg`, `assign`）进行保护，使其不参与损失计算；
  - 设计 **skip-tag masking**：用 `<SKIP_S>` 和 `<SKIP_E>` 标记需跳过的代码段（如敏感逻辑块），防止其内部 token 被更新。
- **效果**：确保遗忘过程不会破坏 HDL 的语法骨架，维持生成代码的**可综合性与有效性**。

#### ✅ 创新点二：Fine-grained Floor-aware Selective Loss (FiFSL)
- **动机**：传统遗忘方法（如 GA、NPO）对所有样本均匀施加遗忘压力，容易造成过度遗忘或梯度不稳定。
- **方法**：
  - 在 token 级别定义带 margin 的负偏好损失：  
    $$
    \phi_i = \frac{2}{\beta} \text{softplus}(-\beta(L_i - \gamma))
    $$
    其中 $L_i$ 是样本损失，$\gamma$ 为遗忘阈值，控制何时开始施加强遗忘信号；
  - 引入 **hard floor $L_{\min}$**，仅对未充分遗忘的样本（$\phi_i > L_{\min}$）反向传播梯度；
  - 实现 **选择性更新机制**：随着训练推进，难遗忘样本成为优化重点，已遗忘样本被“关闭”，避免无效更新。
- **优势**：提升遗忘效率与稳定性，减少所需训练轮数。

---

### 相比现有方法的优势

| 维度 | 本文方法（Ours） | 基线方法（GA / SimNPO） |
|------|------------------|--------------------------|
| **遗忘规模** | 支持高达 30% 的 forget set（即 3× 更大） | 超过 10% 即出现崩溃 |
| **训练开销** | 通常只需 **1 个 epoch** | 需要 4–7 个 epochs |
| **模型效用保留** | Pass@1 最高保留至 53%，远超基线（<15%） | 功能性能急剧下降 |
| **语法正确性** | 显式保护关键字与结构，保证生成代码可综合 | 忽视领域特性，易破坏语法 |
| **实用性** | 可扩展至主流 LLM 架构（CodeLlama, DeepSeek, Qwen 等） | 缺乏领域适配，在硬件场景表现差 |

> 特别指出：唯一相关工作 SALAD [16] 使用了通用遗忘技术，但缺乏领域适应，导致显著效用退化。

---

## 2. 核心实验方法和设置

### 使用的数据集
- **主训练与微调数据集**：RTLCoder 数据集（开源），包含自然语言设计需求与对应 Verilog RTL 实现之间的指令-响应对。
- **遗忘集构造方式**：由于无公开的“专有/污染” RTL 数据集，作者仿照先前研究做法，从训练集中随机抽取 10%、20%、30% 的样本作为 forget set，模拟 IP 泄露、污染数据等真实场景。
- **验证与测试集**：RTLCoder 的 hold-out 测试集，用于评估模型 utility。

---

### 实验设置
- **模型选择**：
  - CodeLlama-7B-Instruct
  - Llama-3-8B-Instruct（通用模型）
  - DeepSeek-Coder-7B-Instruct
  - Qwen2.5-Coder-7B-Instruct
- **微调配置**：
  - Adam 优化器，学习率 $1\times10^{-5}$，最大序列长度 2048，batch size=2（4×A6000 GPU）
  - 微调 6 个 epochs
- **遗忘阶段设置**：
  - 学习率：$2\times10^{-6}$
  - FiFSL 参数：$\beta=2.5$, $\gamma=0$, $L_{\min}=0.35$
  - 目标：使 MinK++ 接近 0.5（表示接近无记忆状态）

---

### 评估指标

#### 🔹 忘记质量（Forget Quality）
| 指标 | 描述 |
|------|------|
| **PrivLeak ↓** | 衡量模型是否再生出 forget set 中的原始 RTL 片段（越低越好） |
| **MinK++ ↓** | 检测最低 k% 正确 token 的平均 log-likelihood，反映隐性记忆（如命名习惯、模板）残留程度；未训练模型约为 0.5 |

#### 🔹 模型效用（Model Utility）
| 指标 | 描述 |
|------|------|
| **Validation Loss ↓** | 在保留集上的交叉熵损失，衡量预测准确性 |
| **Pass@1 ↑** | 生成代码能否通过编译并满足测试激励的功能正确性（黄金标准） |
| **BLEU / chrF ↑** | 衡量生成代码与参考实现的 n-gram 和字符级相似度，反映风格一致性 |

#### 🔹 泛化能力
- 在简单未见任务上测试 Pass@1，验证遗忘是否损害通用生成能力；
- 在 **VerilogEval [4]** 和 **Rtllm [5]** 基准上评估前后性能变化。

---

### 基线方法对比
- **Gradient Ascent (GA)** [9]：最大化 forget set 上的负对数似然；
- **SimNPO** [12]：改进版 NPO，引入平滑惩罚项以增强稳定性；
- 两者均为当前最先进的通用 LLM 遗忘方法，并被 SALAD [16] 应用于硬件设计场景。

---

## 3. 主要实验结果和性能指标

### 关键性能数据汇总（来自 Table II）

| Model | Forget Set | Method | Epochs | MinK++ ↓ | Pass@1 ↑ | Val Loss ↓ |
|-------|------------|--------|--------|-----------|-----------|-------------|
| Llama-7B | 30% | Ours | **1** | **0.53** | **44%** | **0.32** |
| Llama-7B | 30% | GA | 4 | 0.54 | 3% | 1.00 |
| Llama-7B | 30% | SimNPO | 4 | 0.53 | 1% | 1.00 |
| DeepSeek-7B | 30% | Ours | **1** | **0.45** | **53%** | **0.29** |
| DeepSeek-7B | 30% | GA | 5 | 0.51 | 28% | 1.00 |
| Qwen-7B | 30% | Ours | **1** | **0.49** | **55%** | **0.27** |

> 注：Pass@1 下降不超过 2–5 个百分点，而基线方法下降超过 40 个百分点。

---

### 与基线方法的关键对比结果

- **遗忘能力更强**：
  - 所有模型均能成功遗忘 **最多 30% 的训练数据**，而 GA/SimNPO 在 30% 忘记集下几乎完全失效（Pass@1 接近 0%）；
  - MinK++ 普遍降至 0.5 左右，表明记忆已被有效清除。

- **效用保留更优**：
  - 在 30% 忘记比例下，本文方法的 **Pass@1 平均高出基线 14.3×**；
  - BLEU 和 chrF 指标基本不变，说明代码风格和语法完整性得以维持。

- **训练效率更高**：
  - 本文方法平均仅需 **1 个 epoch** 达到理想遗忘效果；
  - GA 和 SimNPO 需要 **4–7 个 epochs**，且仍无法兼顾遗忘与效用。

- **泛化能力不受损**：
  - 如 Table II 所示，“Generalization Ability”列显示，遗忘后模型在新任务上的 Pass@1 仍接近原始水平；
  - 在 VerilogEval 和 Rtllm 基准上的性能下降极小（Table III），证明下游任务能力未受损。

---

### 消融实验与参数分析（文中提及）
- 对 $\beta$（曲率）、$\gamma$（margin）、$L_{\min}$ 进行搜索，发现 $\beta=2.5, \gamma=0, L_{\min}=0.35$ 效果最佳；
- 移除 syntax-preserving masking 导致生成代码频繁出现语法错误（如缺失 `endmodule`）；
- 移除 FiFSL 的 selective gating 机制会导致收敛变慢、效用下降更快。

---

## 4. 关键结论和发现

### 主要发现
1. **领域定制化遗忘至关重要**：直接将 NLP 遗忘方法迁移至硬件代码生成会导致严重效用损失；必须结合 HDL 的语法约束进行设计。
2. **语法保护 + 精细损失控制 = 高效可靠遗忘**：Syntax-preserving masking 保障了生成代码的可综合性，FiFSL 实现了快速、稳定、精准的知识移除。
3. **大规模遗忘是可行的**：本框架支持遗忘高达 **30% 的训练数据**（约 3× 于传统方法），适用于实际场景中的 IP 清理或污染数据剔除。
4. **单轮训练即可完成遗忘**：相比传统方法节省大量计算资源，具备良好的工程部署潜力。

---

### 方法的局限性
- **依赖人工规则定义关键字集合**：虽然基于 IEEE 标准 [32]-[34]，但仍需手动维护 keyword list 和 skip-tag 规则；
- **forget set 需明确标注**：假设已知哪些数据需要遗忘，尚未解决自动检测“有害记忆”的问题；
- **目前仅限于 Verilog**：未扩展至 VHDL 或 SystemVerilog 高级特性；
- **未考虑多模块依赖关系**：遗忘某个模块时，未处理其调用者的影响。

---

### 未来工作方向
- 开发自动化工具识别需遗忘的内容（如通过 watermark detection 或版权匹配）；
- 将本框架扩展至其他硬件语言（VHDL、SystemVerilog）及 EDA 子任务（testbench 生成、assertion synthesis）；
- 探索在线遗忘机制，支持动态增量式知识擦除；
- 结合形式验证技术，进一步提升生成代码的功能可靠性保证。

---

> ✅ **一句话总结**：  
> 本文提出了一种面向硬件代码生成的领域专用 LLM 遗忘框架，通过 **syntax-preserving masking** 和 **FiFSL 损失函数**，实现了在仅 1 轮训练内遗忘 3× 更大数据集的同时，保持生成代码的语法正确性与功能可靠性，为构建可信、合规的 AI 辅助芯片设计系统提供了实用路径。

</details>

---

### 3. [Uncertainty Quantification for Scientific Machine Learning using Sparse Variational Gaussian Process Kolmogorov-Arnold Networks (SVGP KAN)](https://arxiv.org/abs/2512.05306)

**Authors**: Y. Sungtaek Ju  
**Category**: cs.LG  
**Published**: 2025-12-08  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2512.05306v1  

#### Abstract
Kolmogorov-Arnold Networks have emerged as interpretable alternatives to traditional multi-layer perceptrons. However, standard implementations lack principled uncertainty quantification capabilities essential for many scientific applications. We present a framework integrating sparse variational Ga...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文核心结论与实验结果总结**

## **1. 论文的主要贡献和创新点**

### **解决的问题**
传统基于多层感知机（MLP）的神经网络在科学机器学习中缺乏**原则性的不确定性量化能力**，仅提供点估计而无法区分**aleatoric uncertainty**（由测量噪声等固有随机性引起）和**epistemic uncertainty**（由模型认知不足引起）。尽管贝叶斯神经网络（BNN）和高斯过程（Gaussian Process, GP）可提供不确定性估计，但其计算复杂度高、难以扩展。

此外，标准的 **Kolmogorov-Arnold Network (KAN)** 虽然具有良好的可解释性（通过边上的可学习一元函数），但仍是确定性模型，缺乏不确定性建模能力。

---

### **提出的新方法：SVGP-KAN**
本文提出了 **Sparse Variational Gaussian Process Kolmogorov-Arnold Network (SVGP-KAN)**，将稀疏变分高斯过程（Sparse Variational GP）引入 KAN 架构，构建了一个兼具**可解释性、不确定性量化能力和计算可扩展性**的新型架构。

#### **核心创新点：**
- **函数空间不确定性建模**：不同于仅对权重分布建模的 Bayesian KAN，SVGP-KAN 将每条边上的一元函数 $\phi_{j,i}$ 视为从一个 **Gaussian Process 先验** 中采样而来，实现对函数本身的概率建模。
- **稀疏变分推断（Sparse Variational Inference）**：引入诱导点（inducing points）近似，将原本 $O(N^3)$ 的精确 GP 推断降低至 $O(NM^2)$，其中 $M \ll N$，实现**准线性时间复杂度**，适用于大规模数据。
- **解析矩匹配（Analytic Moment Matching）**：利用 KAN 的加性结构，在输入存在不确定性时，通过闭式表达传播均值与方差，高效实现深度不确定性传播。
- **异方差噪声建模**：通过额外的 GP 对观测噪声的对数方差进行建模，显式分离 aleatoric 与 epistemic 不确定性。
- **灵活的 KL 正则化控制**：通过调节 KL 散度权重 $\lambda$，可在不同任务中平衡拟合能力与不确定性校准——例如，在 OOD 检测中设 $\lambda=0$ 以保留自然方差放大机制。

---

### **相比现有方法的优势**
| 方法 | 可解释性 | 不确定性量化 | 可扩展性 | 分离 aleatoric/epistemic |
|------|----------|----------------|------------|----------------------------|
| MLP | ❌ | ❌ | ✅ | ❌ |
| BNN | ⭕ | ✅ | ❌（训练难） | ⭕ |
| Exact GP-KAN [12] | ✅ | ✅ | ❌ ($O(N^3)$) | ✅ |
| **SVGP-KAN (本文)** | ✅ | ✅ | ✅ ($O(NM^2)$) | ✅ |

> ✅ 表示支持，⭕ 表示部分支持，❌ 表示不支持

---

## **2. 核心实验方法和设置**

### **使用的三个案例研究（无公开真实数据集，均为合成或标准基准）**

#### **Study A: 流体流场重建中的异方差噪声校准**
- **数据生成**：
  - 使用流函数 $\psi$ 生成二维不可压缩流场 $(v_x, v_y)$。
  - 添加空间变化的异方差噪声：$\sigma^2_{\text{noise}}(x,y) = \sigma_{\text{base}} + A_{\text{noise}} \exp(-\frac{(x-x_c)^2 + (y-y_c)^2}{r^2})$
- **网络结构**：三层全连接 GPKANLayer，每层 5 个神经元，每条边使用 20 个 inducing points。
- **评估指标**：
  - RMSE（预测精度）
  - 预测不确定性 vs 实际绝对误差的相关性（Pearson $\rho$）
  - 置信区间覆盖率（±1σ, ±2σ, ±3σ）
  - 标准化预测误差 $(y - \mu)/\sigma$ 是否接近 $N(0,1)$

#### **Study B: 对流-扩散动力学的多步预测中认知不确定性的增长**
- **物理系统**：求解 $\partial_t T = -u \cdot \nabla T + \kappa \nabla^2 T + S(x,t)$
- **训练方式**：只训练单步预测（$T_n \to T_{n+1}$）
- **测试方式**：使用 ensemble rollout 进行 15 步前向预测，初始扰动 1%
- **网络结构**：卷积编码器-解码器 + GPKANLayer bottleneck（24 通道，20 inducing points）
- **评估指标**：
  - Ensemble spread（epistemic uncertainty）
  - 时间维度上的不确定性增长倍数
  - 空间不确定性分布模式

#### **Study C: 卷积自编码器中的分布外检测（Out-of-Distribution Detection）**
- **数据集**：MNIST（仅用 digit "0" 训练）
- **异常样本**："7"
- **网络结构**：Conv Encoder → GPKANLayer Bottleneck (6D latent) → Transposed Conv Decoder
- **关键设计**：$\lambda = 0$（无 KL 正则化），保留 GP 的自然方差放大特性
- **评估指标**：
  - Bottleneck 不确定性（latent dimensions 方差之和）
  - Anomaly Score Ratio（OOD / ID 不确定性比值）
  - ROC-AUC
  - Reconstruction MSE

---

### **基线方法对比**
文中未直接与其他不确定性建模方法（如 MC Dropout、Deep Ensembles、Standard GP）进行全面对比，而是强调：
- 相比 **exact GP-KAN**：本方法显著提升可扩展性（$O(N^3) \to O(NM^2)$）
- 相比 **Bayesian KAN**：本文在函数空间建模更完整，能更好捕捉非参数不确定性
- 强调 SVGP-KAN 在**无需专门训练或阈值调整**的情况下即可实现 OOD 检测

---

## **3. 主要实验结果和性能指标**

### **Study A: 异方差噪声校准**
- **Pearson 相关系数**：$\rho = 0.55$（预测不确定性与实际误差之间），表明成功识别高噪声区域
- **置信区间覆盖率误差 < 1%**（在 ±1σ, ±2σ, ±3σ），说明不确定性高度校准
- **标准化预测误差**：均值 -0.09，标准差 1.01 → 接近理想 $N(0,1)$
- **RMSE**：0.085，显示准确的均值预测

> ✅ 成功实现了 aleatoric uncertainty 的空间分辨建模与校准

---

### **Study B: 多步预测中 epistemic uncertainty 增长**
- **时间维度不确定性增长**：ensemble spread 在 15 步内增长 **2.4 倍**
- **空间不确定性集中于梯度剧烈区域**（如热冷界面），变异系数达 **12.7x**
- 物理敏感性测试确认系统稳定（非混沌），因此不确定性增长源于**模型近似误差累积**

> ✅ 成功捕获了因模型不完美导致的认知不确定性随预测步长增加的现象

---

### **Study C: 分布外检测**
- **Anomaly Score Ratio**：平均 **10~100 倍**（OOD 输入的瓶颈不确定性远高于 ID 输入）
- **ROC-AUC**：**0.8–0.9**，表明良好判别能力
- **Reconstruction MSE**：异常输入高出正常输入 **3–4 倍**
- 视觉上，“7”的重构出现模糊、扭曲，试图投影到“0”的流形上

> ✅ 在 $\lambda=0$ 设计下，SVGP-KAN 自然具备 OOD 检测能力，无需额外监督信号

---

### **消融实验（隐含分析）**
虽然没有明确命名“ablation study”，但以下设计体现了关键变量的影响：
- $\lambda = 0.01$ 用于 Study A/B → 更好校准 in-distribution 预测
- $\lambda = 0$ 用于 Study C → 保留 OOD 方差放大机制
- 使用 **RBF kernel** 和 **analytic moment matching** 实现高效不确定性传播
- Inducing point 数量 $M=20$ 已足够获得良好近似，且计算开销可控

---

## **4. 关键结论和发现**

### **主要发现**
1. **SVGP-KAN 成功融合了 KAN 的可解释性与 GP 的不确定性量化能力**，是首个支持 scalable Bayesian inference 的 KAN 变体。
2. 通过稀疏变分推断和加性结构，实现了 **$O(NM^2)$ 准线性复杂度**，适合科学计算中大规模数据场景。
3. 能有效**分离 aleatoric 与 epistemic uncertainty**：
   - Study A 显示对异方差噪声的学习；
   - Study B 显示模型误差随时间积累；
4. 在 $\lambda=0$ 下，SVGP-KAN 的 bottleneck 层天然具备 **OOD 检测能力**，源于 GP 的方差回归特性。
5. 所有实验均显示高度校准的不确定性估计（coverage error <1%），验证了框架的可靠性。

---

### **局限性**
1. 当前主要使用 **RBF kernel**，假设函数光滑；对于不连续或尖锐过渡现象（如激波），可能需改用 Matérn 或分段核函数。
2. **inducing point 数量 $M$ 是超参**，过多影响效率，过少影响近似质量，尚无自动选择机制。
3. 假设 **Gaussian likelihood**，限制了在分类、计数数据等任务的应用。
4. 当前框架集中在前馈结构，尚未拓展至 RNN、GNN 等动态或图结构模型。
5. **KL 权重 $\lambda$ 需手动设定**，缺乏自动化调优策略。

---

### **未来工作方向**
- 扩展至 **non-Gaussian likelihoods**（如 Poisson、Bernoulli）
- 开发 **adaptive inducing point allocation** 策略（基于局部复杂度）
- 探索 **recurrent SVGP-KAN** 用于时间序列建模
- 构建 **graph-based SVGP-KAN** 用于分子、材料等图结构数据
- 设计 **automatic $\lambda$ tuning** 机制，基于验证集不确定性校准指标动态调整
- 应用于更多科学领域：气候建模、生物成像、量子化学等

---

## **总结**
**SVGP-KAN** 是一种面向科学机器学习的新型不确定性感知架构，它解决了传统 KAN 缺乏不确定性建模的问题，同时克服了标准 GP 方法不可扩展的瓶颈。通过结合 **sparse variational inference**、**analytic moment matching** 和 **additive edge functions**，该框架实现了**可解释性、不确定性分解与计算效率的统一**，在流体力学、传输过程建模和异常检测等多个场景中展现出强大潜力。

> 🔗 **代码开源地址**：[https://github.com/sungjuGit/svgp-kan](https://github.com/sungjuGit/svgp-kan)

</details>

---

### 4. [CureAgent: A Training-Free Executor-Analyst Framework for Clinical Reasoning](https://arxiv.org/abs/2512.05576)

**Authors**: Ting-Ting Xie, Yixin Zhang  
**Category**: cs.AI  
**Published**: 2025-12-08  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2512.05576v1  

#### Abstract
Current clinical agent built on small LLMs, such as TxAgent suffer from a \textit{Context Utilization Failure}, where models successfully retrieve biomedical evidence due to supervised finetuning but fail to ground their diagnosis in that information. In this work, we propose the Executor-Analyst Fr...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：CureAgent: A Training-Free Executor-Analyst Framework for Clinical Reasoning

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前基于小规模 LLM（如 TxAgent）构建的临床智能体存在 **Context Utilization Failure**（上下文利用失败）问题：尽管模型能够通过监督微调成功检索到生物医学证据，但在最终诊断中未能有效利用这些信息，导致推理错误或幻觉（hallucination）。此外，还存在输出格式错误（Output Parsing Error）、指令遵循失败（Instruction Adherence Failure）等问题。

### 提出的新方法与创新思路
作者提出 **Executor-Analyst Framework**，一种无需训练的模块化多智能体架构，其核心思想是将“工具执行”与“临床推理”解耦：

- **Executor（执行者）**：由专门微调的小模型（如 TxAgent）担任，专注于精确调用 ToolUniverse 中的 API 工具进行信息检索。
- **Analyst（分析者）**：由大上下文、强推理能力的基础模型（如 Gemini 2.5）担任，负责对 Executor 返回的原始证据流进行综合、验证、补充搜索，并生成可靠的临床推理链。
- **Stratified Ensemble（分层集成）策略**：不同于传统的全局聚合（Global Pooling），该框架采用“晚期融合”（Late Fusion）机制，在多个并行子管道中保留多样化的检索路径，直到最后阶段才进行投票决策，从而避免早期过滤掉关键但稀有的证据。

### 相比现有方法的优势
- **无需微调（Training-Free）**：完全依赖架构设计而非昂贵的端到端微调，具备高可扩展性和敏捷部署能力。
- **更高的准确性与鲁棒性**：通过角色分离和分层集成显著提升在复杂病例上的表现。
- **更强的信息多样性保持能力**：解决了传统方法中的信息瓶颈问题。
- **兼容性强**：可灵活替换不同版本的 Analyst 或 Executor，适应快速演进的 LLM 生态。

---

## 2. 核心实验方法和设置

### 数据集
- 使用 **CURE-Bench@NeurIPS 2025** 公开竞赛数据集，包含两个测试集：
  - `testset_phase1`（公开排行榜）
  - `testset_phase2`（私有排行榜，作为主评估基准）

该数据集要求模型通过调用一个包含超过 200 个工具的 **ToolUniverse**（涵盖 FDA 标签、OpenTarget 数据库、HPO 等）完成多步临床推理任务。

### 实验设置与评估指标
- 主要评估指标为 **准确率（Accuracy %）**
- 所有实验均在固定计算预算下进行比较（控制总 agent 数量）
- 关键变量包括：
  - Executor 数量 $n_1$
  - Analyst 数量 $n_g$
  - 是否启用 Self-Consistency（SC）
  - 架构拓扑：Global Pooling vs. Stratified Ensemble
  - 是否启用互联网搜索（Search）

### 基线方法对比
| 基线方法 | 描述 |
|--------|------|
| TxAgent [8] | 竞赛官方基线，Llama-3.1-8B 微调模型，专用于治疗推理 |
| Gemini-2.5-flash / pro | 强大的闭源基础模型，用于零样本推理 |
| 单一 Agent + Self-Consistency | 多次采样推理路径后聚合结果 |
| Global Pooling（Config A） | 所有 Executor 输出统一聚合后再送入 Analyst |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（Phase2 准确率）

| 方法 | 准确率 (%) |
|------|-----------|
| TxAgent（单模型） | 69.325 |
| Gemini-2.5-flash（无工具） | 63.104 |
| TxAgent + Self-Consistency ($n=30$) | 73.508 |
| **Executor-Analyst（Config A, $n_1=30, n_g=3$)** | **80.510** |
| **Executor-Analyst（Config B, Stratified Ensemble, $n_1=10, n_g=3$)** | **81.367** |
| **+ Gemini with search** | **83.803** ✅（最终提交成绩） |

> 💡 **State-of-the-art 性能达成：83.803%**

### 与基线方法的对比结果
- 相比原始 TxAgent（69.3%），本框架提升 **+14.5 个百分点**
- 相比最强单一模型 Gemini-3-Pro + search（81.283%），仍高出 **2.5 个百分点**
- 显著优于所有开源模型（见 Table 1），即使参数更大的 gpt-oss-120b 也仅得 39.896%

### 消融实验结果
#### （1）温度调优（Executor Calibration）
| 温度 | Phase1 准确率 |
|-----|--------------|
| 0.6 | 58.950 |
| 0.7 | 59.248 |
| **0.8** | **65.214** ✅ |
| 0.9 | 56.747 |

→ 最佳温度为 0.8，过高会导致不稳定输出。

#### （2）Self-Consistency 对 Executor 的影响
- 随着采样路径数增加（$n$ 从 1 到 60），准确率从 69.3% 提升至 74.2%
- 收益在 $n > 20$ 后趋于饱和

#### （3）架构拓扑对比（Config A vs B）
- 在相同计算资源下（$N_{total} = 30$ Execs, 3 Analysists）：
  - Global Pooling（早融合）：79.311% → 80.510%
  - **Stratified Ensemble（晚融合）**：**81.367%**
- 结论：**Late Fusion 更能保留证据多样性，减少集体幻觉**

---

## 4. 关键结论和发现

### 主要发现
1. **Decoupling 是关键**：将“工具执行”与“语义推理”解耦可显著缓解 Context Utilization Failure，充分发挥各类模型优势。
2. **Topology matters**：Stratified Ensemble 比 Global Pooling 更适合复杂医疗推理任务，因后者易造成信息瓶颈。
3. **Training-free 架构工程潜力巨大**：无需微调即可实现 SOTA 表现，提供了一种低成本、高适应性的 AI 医疗系统构建范式。
4. **Scaling Laws 存在反常现象**：
   - **Context-Performance Paradox**：当推理上下文超过 12k tokens 时，性能反而下降（从 94% → 87.93%），说明噪声积累会压倒注意力机制。
   - **Curse of Dimensionality in Action Spaces**：ToolUniverse 从 200+ 扩展到 600+ 工具后，TxAgent 准确率从 92.0% 下降到 87.5%，表明扁平化工具检索难以扩展。

### 方法的局限性
- 当前框架依赖于高质量的外部工具接口（ToolUniverse），若工具不可靠或响应延迟，会影响整体性能。
- 分层架构带来更高推理延迟和资源消耗（需并行运行多个 agents）。
- 对 Analyst 的依赖较强，若 Analyst 本身不具备良好事实核查能力，则仍可能产生幻觉。

### 未来工作方向
1. **大规模动作空间优化**：
   - 探索 **Hierarchical Tool Indexing**（分层工具索引）
   - 利用 **In-Context Learning (ICL)** 或 **RAG-based documentation retrieval** 实现免训练适配新工具
   - 引入 **Curriculum Learning** 逐步暴露更多工具以增强抗干扰能力

2. **上下文效率改进**：
   - 设计 **confidence-based filtering** 机制（如 DeepConf），提前拒绝无关证据，提高信噪比
   - 开发动态压缩算法减少长上下文带来的噪声负担

3. **推理引擎演化**：
   - 随着 Foundation Model 自身工具使用能力增强（如 Gemini 3 Pro），Executor 可能退化为轻量级 Prompt Engineering 模块
   - 但“执行-推理”解耦的核心理念仍将长期适用

---

> 📌 **总结**：  
> CureAgent 展示了通过**训练免费的架构创新**解决复杂临床推理任务的巨大潜力。它不仅在 CURE-Bench 上取得领先成绩，更重要的是揭示了下一代可信 AI 医疗系统的构建原则：**模块化、多样性保持、免微调适应性、以及人机协同式信息处理设计**。

</details>

---

### 5. [Learning from Self Critique and Refinement for Faithful LLM Summarization](https://arxiv.org/abs/2512.05387)

**Authors**: Ting-Yao Hu, Hema Swetha Koppula, Hadi Pouransari, Cem Koc, Oncel Tuzel, Raviteja Vemulapalli  
**Category**: cs.CL  
**Published**: 2025-12-08  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2512.05387v1  

#### Abstract
Large Language Models (LLMs) often suffer from hallucinations: output content that is not grounded in the input context, when performing long-form text generation tasks such as summarization. Prior works have shown that hallucinations can be reduced by iteratively critiquing and refining previously ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Learning from Self Critique and Refinement for Faithful LLM Summarization

---

## 1. 论文的主要贡献和创新点

### 解决的问题
大型语言模型（LLMs）在执行长文本生成任务（如摘要生成）时普遍存在**幻觉（hallucinations）**问题，即生成的内容无法在输入上下文中找到依据。尽管已有研究通过测试时（test-time）的迭代批判与精炼（critique and refinement）来缓解该问题，但这些方法通常依赖更强的教师模型或多模型协作，导致推理成本高、实用性差。

### 提出的新方法：SCRPO
本文提出 **Self Critique and Refinement-based Preference Optimization (SCRPO)**，一种**自监督训练框架**，其核心思想是：
- 利用同一个LLM自身的**自我批判（self-critique）** 和 **自我精炼（self-refinement）** 能力，在训练阶段构建偏好数据集；
- 将初始摘要与经自身批判后精炼的摘要构建成 `(chosen, rejected)` 偏好对；
- 使用 **Preference Learning**（如 DPO）对该LLM进行微调，从而提升其生成忠实摘要的能力。

### 相比现有方法的优势
| 维度 | 传统方法（Test-Time Refinement） | SCRPO（本文方法） |
|------|-------------------------------|------------------|
| 推理开销 | 高（每次推理需多次调用模型） | **无额外开销**（训练后直接推理） |
| 是否需要教师模型 | 是（或多个专用模型） | 否（仅用单一LLM） |
| 可扩展性 | 差（依赖外部资源） | 强（完全自监督） |
| 性能表现 | 一般 | **更优的忠实性和整体质量** |

此外，SCRPO实现了“**训练时蒸馏推理时策略**”，但效果优于直接在推理时进行精炼。

---

## 2. 核心实验方法和设置

### 使用的数据集
在三个主流摘要基准上进行评估：
- **XSum**：新闻文章单句摘要，强调简洁性和事实性。
- **CNNDM**：新闻文章摘要，内容较长，常用于评估抽象摘要能力。
- **SAMSum**：日常对话摘要，模拟家庭朋友间的聊天记录。

所有实验从官方训练集中采样 10,000 个文档作为无标签训练数据。

### 实验设置
- **基础模型**：`Qwen2.5-7B-Instruct`
- **适配器技术**：LoRA（rank=16, alpha=32）
- **生成方式**：Beam Search（beam size=5）
- **任务要求**：生成单句摘要

### 评估指标
| 类别 | 指标 | 描述 |
|------|------|------|
| **忠实性（Faithfulness）** | `MiniCheck` | 自动化事实核查工具，衡量摘要是否基于原文 |
| | `GPT4-Likert` | 使用 GPT-4 进行人工风格评分，评估忠实性 |
| **整体质量（Quality）** | `GEval` 系列 | 包括 Coherence（连贯性）、Consistency（一致性）、Fluency（流畅性）、Relevance（相关性） |

### 基线方法对比
- **Pretrained LLM**：原始预训练模型
- **MPO (Choi et al., 2024)**：基于不同解码策略构建偏好的自监督方法
- **SCOPE (Duong et al., 2025)**：利用无上下文模型生成不忠实现象以构造偏好数据
- **SCRPO-Inference Time**：在推理阶段应用相同批判与精炼流程（非训练）

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 3）

| 方法 | XSum MiniCheck | XSum GPT4-Likert | CNNDM MiniCheck | CNNDM GPT4-Likert | SAMSum MiniCheck | SAMSum GPT4-Likert |
|------|---------------|------------------|------------------|--------------------|-------------------|---------------------|
| Pretrained LLM | 0.701 | 4.16 | 0.715 | 4.45 | 0.437 | 4.17 |
| MPO | 0.694 | 4.13 | 0.712 | 4.42 | 0.456 | 4.18 |
| SCOPE | 0.713 | 4.14 | 0.721 | 4.43 | 0.440 | 4.17 |
| SCRPO-Inference Time | 0.722 | 4.23 | 0.746 | 4.48 | 0.470 | 4.21 |
| **SCRPO (Ours)** | **0.761** | **4.38** | **0.806** | **4.65** | **0.523** | **4.42** |

> ✅ 所有数据集上，SCRPO 在 **MiniCheck** 和 **GPT4-Likert** 上均显著优于所有基线。

同时，**GEval 指标显示**：SCRPO 在保持甚至提升摘要整体质量的前提下，大幅提高忠实性。

---

### 消融实验结果

#### （1）批判策略比较（Table 1）
| 策略 | XSum MiniCheck | CNNDM MiniCheck | SAMSum MiniCheck |
|------|----------------|------------------|-------------------|
| Binary Feedback | 0.748 | 0.803 | 0.498 |
| **Fine-grained Feedback** | **0.761** | **0.806** | **0.523** |

> ✅ 细粒度反馈（原子事实提取 + NLI验证）优于二值判断，说明精细分析更有效。

#### （2）偏好三元组选择策略（Table 2）
| 策略 | 特点 | 结果 |
|------|------|------|
| Single Beam Search | 仅生成一对初稿与精炼稿 | 忠实性最高但整体质量下降 |
| Random Selection | 随机选取初始与精炼摘要 | 表现中等 |
| **Extreme Selection**（最终采用） | 最差初稿 vs 最佳精炼稿 | **平衡忠实性与质量**，综合最优 |

#### （3）变体对比（Table 3）
| 变体 | 特征 | 结果 |
|------|------|------|
| SCRPO-Critique Only | 仅批判打分，不精炼 | 忠实性提升有限 |
| SCRPO-SFT | 对精炼摘要做 SFT 微调 | 效果接近但不如 Preference Learning |
| **SCRPO (完整版)** | 批判 + 精炼 + Preference Learning | **全面领先**，证明各组件协同作用 |

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **SCRPO 显著提升了 LLM 摘要的忠实性**，在多个数据集上超越当前最先进的自监督方法。
2. ✅ **优于推理时精炼（inference-time refinement）**：尽管后者计算更多，但 SCRPO 在忠实性和效率之间取得更好平衡。
3. ✅ **无需外部监督或教师模型**：完全依靠单一LLM的内在能力完成自我改进，具备高度可部署性。
4. ✅ **具有跨域泛化能力**（见 Table 4）：即使在源域与目标域不同（如新闻 → 对话），SCRPO 仍能带来增益。
5. ✅ **人类评估一致支持自动指标趋势**（Table 5）：在 80 个 XSum 测试样本中，SCRPO 在 **24%** 的情况下被判定为更忠实，仅 **1%** 落败。

### 方法的局限性
- **模型容量门槛**（见 Figure 3）：实验表明，小于 3B 参数的模型（如 0.5B, 1.5B）在 SCRPO 下出现性能退化，说明该方法对模型规模有一定要求。
- **依赖高质量提示工程**：批判与精炼的效果受 prompt 设计影响较大（见 Appendix A.1）。
- **未解决根本幻觉机制**：仍是后处理式修正，而非从建模层面根除幻觉。

### 未来工作方向
- 探索更高效的批判-精炼路径（如减少采样次数 N）；
- 将 SCRPO 扩展到其他条件生成任务（如问答、故事生成）；
- 结合解码时约束（如 context-aware decoding）进一步抑制幻觉；
- 研究如何降低对大模型规模的依赖，使小模型也能受益于自我精炼。

---

> 🔚 **总结一句话**：  
> SCRPO 成功将“推理时自我精炼”蒸馏进模型权重中，实现了**高效、低成本、高保真的摘要生成**，为构建可信 LLM 应用提供了实用且强大的训练范式。

</details>

---

### 6. [Capturing Classic Authorial Style in Long-Form Story Generation with GRPO Fine-Tuning](https://arxiv.org/abs/2512.05747)

**Authors**: Jinlong Liu, Mohammed Bahja, Venelin Kovatchev, Mark Lee  
**Category**: cs.CL  
**Published**: 2025-12-08  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2512.05747v1  

#### Abstract
Recent advances in large language models (LLMs) show impressive performance in open-ended story generation, but fine-grained stylistic control remains limited. Existing methods often rely on shallow cues (e.g., names or topics) to simulate authorial style, without robust evaluation. In this work, we...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Capturing Classic Authorial Style in Long-Form Story Generation with GRPO Fine-Tuning

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
当前大型语言模型（LLMs）在开放性故事生成任务上表现优异，但在**细粒度的作者风格控制**方面仍存在显著不足。现有方法通常依赖浅层线索（如角色名、主题词）来模拟“风格”，缺乏对真正文学声音（literary voice）、叙事节奏和句法特征的建模，且缺乏可靠的评估机制。

本文旨在解决以下核心挑战：
- 如何在不牺牲故事逻辑性和连贯性的前提下，精确引导模型模仿经典作家的写作风格？
- 如何构建一个可量化、可控的风格生成与评估闭环？

---

### 🚀 提出的新方法与创新思路

1. **基于 GRPO 的风格条件化训练框架**
   - 首次将 **Group Relative Policy Optimization (GRPO)** 应用于长篇小说的风格化生成任务。
   - 利用 GRPO 双模型架构（参考模型 + 可训练策略模型），通过组内奖励估计实现更稳定、多样化的探索。

2. **多奖励系统设计**
   - 构建三重奖励函数：
     - **Style Reward**：基于 fine-tuned Sentence Transformer 模型计算与目标作者（Mark Twain）文本的相似度，源自 Authorship Verification (AV) 信号。
     - **Content Reward**：由另一个 LLM（openchat-3.5-0106）根据预定义评分标准打分，衡量叙事质量（连贯性、人物发展等）。
     - **Completeness Reward**：检查输出是否达到约 1500 字并以完整句子结尾，防止截断或未完成结局。
   - 最终奖励为加权组合：`0.6 × Style + 0.3 × Content + 0.1 × Completeness`

3. **动态风格相似性评估指标**
   - 将 AV-based reward model 的输出作为**动态风格对齐度量**，提供可解释、连续的风格分数（而非仅分类判断）。

4. **受控的数据增强策略用于风格标注**
   - 设计了一种基于掩码-重填（masking and refill）的方法构造带标签的风格相似性数据集，使相似度近似等于 `1 - r_mask`，从而获得可解释的软标签。

---

### 🔍 相比现有方法的优势

| 维度 | 传统方法 | 本文方法 |
|------|--------|---------|
| **风格建模深度** | 浅层提示（如“用马克·吐温的语气”） | 基于 AV 的深层语义+句法风格嵌入 |
| **训练方式** | 多数为监督微调（SFT）或 DPO | 使用 GRPO 进行强化学习优化，支持多样化探索 |
| **评估可靠性** | 缺乏客观指标或依赖人工评价 | 引入 AV-based reward model 作为自动化、可复现的风格度量 |
| **长文本控制能力** | 易出现风格漂移或逻辑断裂 | 多奖励机制协同维持风格一致性与叙事完整性 |

---

## 2. 核心实验方法和设置

### 📚 数据集

1. **基础故事生成数据集（SFT Dataset）**
   - 来源：Fan et al. (2018) 的 300K prompt-story 对。
   - 自建子集：从中精选 50 个通用文学性提示（prompt），使用 Qwen2.5-32B 生成 40,000 个约 1500 字的故事。
   - 用途：用于 Supervised Fine-Tuning（SFT），建立基础叙事能力。

2. **风格对齐训练数据集（GRPO Dataset）**
   - 目标作者：Mark Twain，以 *The Adventures of Huckleberry Finn* 为主要参考文本。
   - 构造方式：
     - 将原著切分为 1500 字的段落；
     - 使用 7B 模型（openchat-3.5-0106）作为评估器；
     - 每个原始 prompt 被修改为包含“以马克·吐温风格写作”的指令；
     - 共生成 4,450 个 prompt-reference 对，用于 GRPO 微调。

3. **风格奖励模型训练数据**
   - 正样本（Positive Pairs）：同一作者不同作品间的原段落 vs 掩码后重填段落，得分 = `1 - r_mask`
   - 负样本（Negative Pairs）：不同作者之间的重填段落，得分 = 0
   - 中间样本（Refilled-Refilled Pairs）：同作者不同书中两次重填的结果，得分 = `|r_mask1 - r_mask2|`
   - 总规模：65K 训练样本，7K 验证/测试各一份

---

### ⚙️ 实验设置

- **模型规模**：主实验使用 **8B 参数模型**
- **硬件环境**：4×NVIDIA A100 GPU（80GB），2×Intel Xeon CPU，512GB RAM
- **训练流程**：
  1. SFT 初始化 → 2. Reward Model Fine-tuning → 3. GRPO 微调
- **超参数**：
  - 学习率：初始 5e-6，最终调整至 3e-6
  - β（KL 控制系数）：选择 **0.035** 以平衡稳定性与探索效率
  - 批大小：每 prompt 至少采样 16 个 completion
  - DrGRPO 损失函数（Liu et al., 2025）

---

### 📊 评估指标

| 指标 | 描述 |
|------|------|
| **Style Score** | 基于 fine-tuned AV reward model 输出的相似度分数（0–1） |
| **Content Score** | 由 openchat-3.5-0106 按照四级评分标准给出（1–4 分），归一化到 [0.25, 1] |
| **Completeness Score** | 是否达字数要求 + 是否完整结束，有惩罚因子（缺句则×0.5） |
| **Weighted Average Score** | `0.6×Style + 0.3×Content + 0.1×Completeness` |
| **Human-interpretable Analysis** | 定性分析生成文本的语言特征（如方言使用、第一人称叙述、幽默讽刺语气） |

---

### 🆚 基线方法对比

参与比较的主流 LLMs 包括：
- **GPT-4o**
- **Claude Sonnet 4**
- **Gemini 2.5 Flash**
- **Gemma 3-27B**
- **Qwen 2.5-32B**

所有模型均接收相同 10 个 prompt，并生成一篇故事进行统一评分。

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据（见 Table 3）

| Model | Style (↑) | Content (↑) | Completeness (↑) | Weighted Avg (↑) |
|-------|-----------|-------------|------------------|------------------|
| **FT-Agentic (8B)** | **0.628** | 0.750 | 0.744 | **0.676** |
| GPT-4o | 0.510 | **0.800** | 0.873 | 0.633 |
| Claude Sonnet 4 | 0.450 | 0.750 | 0.999 | 0.595 |
| Qwen 2.5-32B | 0.328 | 0.750 | 0.911 | 0.513 |
| Gemma 3-27B | 0.306 | 0.750 | 0.450 | 0.453 |

> ✅ **核心发现**：尽管我们的 8B 模型在内容质量和完整性上略逊于 GPT-4o 和 Claude，但在 **Style Score 上显著领先（0.628 vs. 0.510）**，且总加权得分最高（0.676），证明该方法在风格捕捉上的有效性。

---

### 🔬 消融实验与关键分析

#### （1）风格奖励模型 fine-tuning 效果（Table 1 vs Table 2）
- **Pretrained Models 表现差**：baseline 模型（如 all-mpnet-base-v2）在区分同作者 vs 异作者时几乎无差异（△≈0.003–0.015），IQR 重叠 >80%
- **Fine-tuned 后大幅提升**：
  - FT-all-distilroberta-v1 达到 △=+0.628，IQR 重叠降至 **0%**
  - 所有 fine-tuned 模型实现完全分布分离，说明任务特定微调至关重要

#### （2）为何选择 FT-all-mpnet-base-v2 作为最终 RM？
- 虽然 FT-all-distilroberta-v1 在判别力上最强，但其奖励空间过于“保守”（reward saturation），导致早期 RL 更新难以获得正反馈。
- FT-all-mpnet-base-v2 提供更平滑的梯度信号，利于稳定训练。

#### （3）β 参数影响（Figure 3）
- β < 0.04 导致 KL divergence 波动剧烈，训练不稳定；
- β = 0.035 结合低学习率（3e-6）和梯度归一化可有效抑制 reward hacking。

---

## 4. 关键结论和发现

### ✅ 主要发现

1. **风格可以被有效建模与优化**  
   通过 AV-based reward model 与 GRPO 框架结合，即使是中等规模（8B）模型也能在风格模仿上超越 GPT-4o 等更大模型。

2. **风格与叙事完整性存在权衡**  
   实验表明，追求高风格对齐可能导致 completeness 下降（本模型 completeness=0.744，低于 Claude 的 0.999），反映全局连贯性仍是挑战。

3. **多奖励机制提升训练稳定性**  
   内容与完整性奖励有效防止模型陷入“只顾风格而忽略情节”的陷阱，确保生成的是完整、可读的故事。

4. **小模型 + 特定训练 > 大模型 + 通用能力**  
   在特定任务（经典作家风格生成）上，经过精心设计的任务专用训练流程，小型模型可超越通用大模型。

---

### ⚠️ 局限性

1. **计算资源限制导致内容奖励模型不可靠**  
   当前 content reward 依赖单一 LLM 打分，存在误判风险，尤其在处理复杂叙事结构时。

2. **训练数据范围狭窄**  
   仅基于 Mark Twain 的一部作品（*Huckleberry Finn*）进行训练，泛化能力受限，无法适应其他文体或作者。

3. **缺乏人类主观评估**  
   所有评估均为自动指标驱动，缺少真实读者对“风格真实性”的感知验证。

4. **叙事完整性仍待改进**  
   部分生成故事虽风格鲜明，但结局仓促或逻辑断裂，显示长期依赖建模仍有困难。

---

### 🔮 未来工作方向

1. **扩展多作者、多体裁数据集**  
   收集更多经典作家作品（如 Jane Austen, Hemingway, Dickens），构建跨风格迁移能力。

2. **引入人类反馈（RLHF 或 RLAIF）**  
   结合人工标注提升风格真实性与叙事质量的评估可信度。

3. **改进内容奖励模型**  
   使用更强的 evaluator model 或集成多个模型投票机制，提高 content scoring 的鲁棒性。

4. **探索全局叙事规划模块**  
   在生成前加入 plot planner 或 outline generator，辅助维持长程一致性。

5. **伦理透明化机制**  
   明确声明生成文本非“复活”作者，避免误导性使用，强调教育与创意辅助定位。

---

> 💡 **总结一句话**：  
> 本文提出了一套基于 **GRPO + AV-style reward modeling** 的新型风格化长篇故事生成框架，在有限模型规模下实现了优于 GPT-4o 的经典作家风格捕捉能力，验证了“agent-style”创作代理的可行性，同时揭示了风格保真与叙事完整的内在张力，为未来可控创意生成研究提供了重要范式。

</details>

---

### 7. [LDLT $\mathcal{L}$-Lipschitz Network: Generalized Deep End-To-End Lipschitz Network Construction](https://arxiv.org/abs/2512.05915)

**Authors**: Marius F. R. Juston, Ramavarapu S. Sreenivas, Dustin Nottage, Ahmet Soylemezoglu  
**Category**: cs.LG  
**Published**: 2025-12-08  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2512.05915v1  

#### Abstract
Deep residual networks (ResNets) have demonstrated outstanding success in computer vision tasks, attributed to their ability to maintain gradient flow through deep architectures. Simultaneously, controlling the Lipschitz constant in neural networks has emerged as an essential area of research to enh...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：LDLT $\mathcal{L}$-Lipschitz Network

## 1. 论文的主要贡献和创新点

### 解决的问题
本文旨在解决深度神经网络（DNNs）在安全敏感领域中的**对抗鲁棒性**和**可认证性**问题。传统深度网络对输入的小扰动非常敏感，容易受到对抗攻击。通过施加 **Lipschitz 约束**可以限制网络输出对输入变化的敏感度，从而提升模型的鲁棒性和可验证性。

然而，现有的 Lipschitz 约束方法存在以下挑战：
- 多数基于 **SDP（Semidefinite Programming）框架**的方法难以扩展到深层残差网络（ResNet），因为其形成的 LMI（Linear Matrix Inequality）具有复杂的循环三对角结构，导致闭式解难以推导。
- 现有方法多为逐层约束（如堆叠 1-Lipschitz 层），缺乏端到端的全局 Lipschitz 控制，可能导致表达能力下降或优化困难。

---

### 提出的新方法与创新思路
作者提出了一种名为 **LDLT $\mathcal{L}$-Lipschitz Network** 的新框架，其核心思想是利用 **LDLT 分解** 来高效求解并参数化满足 Lipschitz 约束的深层网络。

#### 主要创新点包括：

1. **将 ResNet 构建为循环块三对角 LMI**  
   将整个残差网络的 Lipschitz 约束形式化为一个大型线性矩阵不等式系统，并识别出其特有的“伪三对角”结构。

2. **引入 LDLT 分解进行可行性认证与参数化**  
   利用 **LDLT 分解**（实数平方根自由 Cholesky 分解）将原始 LMI 转换为更易处理的形式。该分解允许直接约束对角矩阵 $D$ 的正定性来保证整体 LMI 的半负定性，避免昂贵的特征值计算。

3. **提出端到端的 C-Lipschitz 参数化方法**  
   推导出网络权重（$A, B, \{C_i\}$）的显式参数化公式，确保整个网络满足全局 C-Lipschitz 条件，而不仅仅是每层独立满足。这实现了真正的 **end-to-end Lipschitz 构造**。

4. **结合 Cholesky 分解实现高效计算**  
   进一步使用 **Cholesky 分解** 替代矩阵平方根运算，在保持相同表达能力的前提下，显著降低计算复杂度（从 $O(n^3)$ 到 $O(n)$），适用于大规模网络。

5. **通用架构扩展能力**  
   所提方法不仅适用于 ResNet，还可推广至其他分层非线性系统，如全连接网络（FNN）、U-Net 等，提供统一的 Lipschitz 构造范式。

---

### 相比现有方法的优势
| 特性 | LDLT 方法 | 其他主流方法（如 SLL, AOL, Orthogonal） |
|------|----------|----------------------------------------|
| **理论保障** | ✅ 严格的全局 C-Lipschitz 保证 | ⚠️ 多为逐层近似或松弛 |
| **表达能力** | ✅ 高（支持深层模块化结构） | ❌ 受限于正交/单位谱归一化 |
| **计算效率** | ✅ 使用 Cholesky 加速，避免特征值分解 | ⚠️ 或需迭代投影、幂迭代等耗时操作 |
| **灵活性** | ✅ 支持多种激活函数（ReLU, GELU, SiLU 等） | ⚠️ 某些方法对激活函数有限制 |
| **可扩展性** | ✅ 可扩展至 ConvNet、U-Net 等复杂结构 | ⚠️ 多集中于简单前馈结构 |

---

## 2. 核心实验方法和设置

### 数据集
实验在 **121 个 UCI 机器学习数据集** 上进行，涵盖物理、地质、生物等多个领域。这些数据集大小从 10 到 6300 万样本不等，特征维度从 1 到 320 万，具有广泛的代表性。

子集选择参考了先前关于 SeLU 的研究工作，确保公平比较。

---

### 实验设置
- **网络结构**：所有模型均为 **4 层隐藏层** 的 MLP。
- **宽度启发式**：根据输入规模动态设定宽度 $w = \min(\max(4N, 32), 512)$，并取最接近的 2 的幂次。
- **优化器**：使用 **AdamW**，初始学习率 $1e^{-3}$，weight decay $1e^{-4}$。
- **学习率调度**：当验证准确率停滞超过 8 个 epoch 时减半。
- **训练轮数**：最多 100 个 epoch，早停条件为 30 个 epoch 无改进。
- **损失函数**：交叉熵损失，使用类别权重平衡不平衡数据。
- **最后一层**：添加 **线性归一化头（linear normalization head）** 以方便认证准确率计算。

---

### 评估指标
1. **Clean Accuracy**：标准测试集上的分类准确率。
2. **Certified Robust Accuracy**：在不同 $l_2$ 扰动半径下的**可证明鲁棒准确率**，即预测标签在扰动球内不变的比例。
   - 测试半径：$\epsilon = 36/255, 72/255, 108/255, 255/255$
3. **统计检验**：
   - **平均排名（Average Rank）**
   - **Wilcoxon 符号秩检验**（配对比较）
   - **Nemenyi 检验**（多重比较校正）

---

### 基线方法对比
共对比六种方法：
| 方法 | 类型 |
|------|------|
| **AOL** | Almost-Orthogonal Layers |
| **Orthogonal** | 正交参数化网络 |
| **Sandwich** | Sandwich Layers (Wang & Manchester, 2023) |
| **SLL** | SDP-based Lipschitz Layers (Araujo et al., 2023) |
| **LDLT-L** | LDLT 构造的线性网络变体 |
| **LDLT-R** | LDLT 构造的残差网络变体（本文主推） |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 2）

| Algorithm | Clean Acc (%) | Certified @ 36/255 | @ 72/255 | @ 108/255 | @ 255/255 |
|----------|---------------|---------------------|-----------|------------|------------|
| **AOL** | 62.95 ± 0.2278 | 36.69 | 26.60 | 20.76 | 9.99 |
| **Orthogonal** | 69.69 ± 0.1938 | 59.73 | 50.73 | 43.00 | 19.70 |
| **Sandwich** | **72.15 ± 0.1871** | **63.75** | **55.93** | **48.36** | **24.96** |
| **SLL** | 69.78 ± 0.1998 | 58.85 | 49.75 | 41.46 | 19.18 |
| **LDLT-L** | 72.23 ± 0.1868 | 53.01 | 42.93 | 35.35 | 16.52 |
| **LDLT-R** | 70.22 ± 0.1944 | 61.07 | 52.92 | 44.92 | 21.72 |

> 注：数值为 121 个数据集上的均值 ± 标准差。

---

### 与基线方法的对比结果

#### （1）总体排名表现（Table 3–4）
- 在 **Clean Accuracy** 上：
  - **LDLT-L** 表现最佳（平均排名 2.434），略优于 **Sandwich**（2.566）。
  - **LDLT-R** 排名第三（3.438），优于 SLL 和 Orthogonal。
- 在 **Certified Accuracy**（各扰动级别）上：
  - **Sandwich** 始终排名第一。
  - **LDLT-R** 稳定第二，显著优于 SLL、Orthogonal 和 AOL。
  - **LDLT-L** 在高扰动下表现较差，说明其结构对噪声容忍度较低。

#### （2）相对于 SLL 的优势
- LDLT-R 在所有扰动级别的认证准确率上均 **显著高于 SLL**。
- 平均提升幅度达 **3%–13%**，尤其在中等扰动（72/255 ~ 108/255）区间优势明显。
- Wilcoxon 检验显示 LDLT-R 对 SLL 的胜率高达 66/121（Table 10），且 p-value < 0.001。

#### （3）消融实验分析（隐含在设计中）
虽然未明确列出消融表，但从方法设计可以看出：
- 使用 **LDLT 分解** 而非传统 Cholesky 或 SDP 求解，使深层 ResNet 的 Lipschitz 构造成为可能。
- 引入 **Cholesky 加速** 显著降低了训练开销，使得大模型可行。
- **残差结构（LDLT-R）** 比线性结构（LDLT-L）在认证鲁棒性上更具优势，尤其是在中等扰动水平。

---

## 4. 关键结论和发现

### 主要发现
1. **LDLT 分解是一种有效的 LMI 求解工具**，特别适合处理具有稀疏块结构的深层网络 LMI，能够实现精确且高效的 Lipschitz 约束参数化。
2. **LDLT-R 构造的残差网络在认证鲁棒性方面显著优于 SLL 层**，证明了所提方法的有效性和优越性。
3. 尽管 **Sandwich Layer** 整体性能最优，但 LDLT-R 是唯一能与之抗衡的基于严格 LMI 的构造方法，且具备更强的理论可解释性。
4. **Cholesky 加速策略不会损失表达能力**（Lemma 26），可在不牺牲性能的前提下大幅提升计算效率。
5. 该框架具有良好的**通用性**，可自然扩展到 FNN、ConvNet、U-Net 等多种架构。

---

### 方法的局限性
1. **数值稳定性问题**：在极深网络中，ResNet 结构可能出现数值不稳定，需要更鲁棒的 Cholesky 更新机制。
2. **卷积实现成本高**：当前卷积版本依赖频域变换和复数运算，计算开销较大，尤其在 CIFAR、TinyImageNet 等图像任务上难以扩展。
3. **PyTorch 支持不足**：缺少针对三角矩阵乘法的原生优化内核，需自定义 CUDA 实现才能充分发挥性能潜力。
4. **超参调优复杂**：参数化涉及多个缩放因子（如 $\alpha, \gamma$），需 careful tuning 才能达到最佳效果。

---

### 未来工作方向
1. **集成 Sandwich Layer 思想**：将 Sandwich 的高性能参数化方式融入 LDLT 框架，进一步提升精度。
2. **开发 U-Net 版本**：探索 1-Lipschitz U-Net 在生成模型（如 Diffusion Model）中的应用，构建 Lipschitz 编码空间。
3. **设计新型归一化方案**：基于 LDLT 分解开发更高效的训练加速策略。
4. **拓展至图像基准**：在 CIFAR-10/100、TinyImageNet 上验证方法的大规模适用性。
5. **开源支持增强**：发布完整代码与预训练权重（已部分公开于 GitHub 和 HuggingFace）。

---

> **代码仓库**：https://github.com/Marius-Juston/DeepLipschitzResNet  
> **模型权重**：https://huggingface.co/SuperComputer/LDLT

</details>

---

### 8. [BEAVER: An Efficient Deterministic LLM Verifier](https://arxiv.org/abs/2512.05439)

**Authors**: Tarun Suresh, Nalin Wadhwa, Debangshu Banerjee, Gagandeep Singh  
**Category**: cs.AI  
**Published**: 2025-12-08  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.05439v1  

#### Abstract
As large language models (LLMs) transition from research prototypes to production systems, practitioners often need reliable methods to verify that model outputs satisfy required constraints. While sampling-based estimates provide an intuition of model behavior, they offer no sound guarantees. We pr...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# BEAVER: An Efficient Deterministic LLM Verifier 论文总结

## 1. 论文的主要贡献和创新点

### 解决的问题
随着大型语言模型（LLMs）从研究原型转向生产系统，确保其输出满足特定约束（如正确性、隐私、安全性）变得至关重要。然而，现有的验证方法存在明显不足：
- **Sampling-based 方法**（如 rejection sampling）仅提供统计估计，无法给出**sound guarantees**（即确定性的概率边界）。
- 传统神经网络验证技术（如基于抽象解释或 SMT 求解器的方法）难以扩展到 LLM 的自回归生成过程。

因此，论文旨在解决如何为 LLM 输出提供**确定性、可证明正确的概率边界**这一挑战。

### 提出的新方法与核心思路
作者提出了 **BEAVER**，这是首个实用的、用于计算 LLM 约束满足概率的**确定性、sound 验证框架**。

#### 核心创新点：
- **形式化验证问题**：将 LLM 验证问题定义为在给定提示（prompt）下，计算所有可能输出序列中满足语义约束（semantic constraint）的总概率，并求其 sound 的上下界 `[PLB, PUB]`。
- **利用 Prefix-closed 语义约束**：针对一类重要的“前缀封闭”（prefix-closed）语义约束（即一旦某个前缀违反约束，则其所有后续扩展也必然违反），BEAVER 能够在生成过程中**尽早检测并剪枝**（prune）违反约束的路径，从而大幅减少搜索空间。
- **新颖的数据结构**：
  - **Token Trie**：显式地跟踪所有已探索的、满足约束的前缀序列及其概率。
  - **Frontier**：维护当前待扩展的不完整序列集合，用于高效更新概率边界。
- **分支定界（Branch-and-Bound）算法**：通过迭代选择 Frontier 中的序列进行扩展，逐步收紧概率边界，提供“anytime guarantees”（即在任意时刻终止都能得到 sound 的区间）。

### 相比现有方法的优势
- **Soundness**：相比采样方法，BEAVER 提供的是**可证明正确的概率边界**，而非统计估计。
- **效率与精度**：相比 baseline（rejection sampling），BEAVER 能够实现**6-8倍更紧的概率边界**，并识别出**3-4倍更多的高风险实例**。
- **实用性**：在相同的计算预算下，能够对 LLM 行为进行更精确的风险评估和特征描述。

---

## 2. 核心实验方法和设置

### 使用的数据集与任务
实验在三个关键的验证任务上进行，覆盖了不同类型的语义约束：

1. **GSM-Symbolic**：数学推理任务，验证生成的符号表达式是否语法正确且功能等价于标准答案。语义约束 `Φ_GSM` 是前缀封闭的（语法部分）。
2. **Enron Email Leakage**：隐私泄露任务，评估模型是否会泄露训练数据中的个人邮箱地址。语义约束 `Φ_p` 定义为不生成未知的邮箱地址。
3. **Secure Code Generation**：安全代码生成任务，使用 CyberSecEval 基准测试 Rust 代码补全的安全性。语义约束 `Φ_safe` 检测是否存在不安全编码模式（如硬编码密钥、跳过证书验证），该约束是前缀封闭的。

### 实验设置和评估指标
- **模型**：评估了四种 state-of-the-art 的指令微调模型：Qwen3-4B, Qwen2.5-14B, Qwen3-30B-A3B, Llama3.3-70B。
- **计算预算**：每个任务实例分配 **100 次 forward passes**，确保与基线方法公平比较。
- **早期终止**：如果上下界差距 `< ε = 0.01`，则提前终止。
- **评估指标**：
  - **Bound Tightness (Gap)**：`PUB - PLB`，衡量概率边界的紧密程度。
  - **Risky Distribution Ratio (RDR)**：满足 `PUB < 0.9` 的任务比例，表示模型有非平凡概率（>0.1）违反约束。更高的 RDR 意味着能揭示更多潜在风险。
  - **收敛速度**：达到 `Gap < 0.01` 所需的平均 forward passes 数量。

### 基线方法对比
- **Baseline**：**Rejection Sampling (RS)**。这是作者采用的基线，因为此前没有针对此设定的直接可比工作。
- **对比方式**：在完全相同的计算预算（100次 forward passes）下，比较 BEAVER 与 RS 的 Gap、RDR 和收敛速度。

---

## 3. 主要实验结果和性能指标

### 关键性能数据与对比结果

#### （1）GSM-Symbolic 任务（正确性验证）
- **结果见 Table 2**：
  - BEAVER 将概率 Gap 显著缩小。例如，对于 Qwen3-4B，RS 的 Gap 为 0.092，而 BEAVER 缩小至 **0.013**（约 **7 倍更紧**）。
  - BEAVER 收敛更快。例如，Qwen3-4B 达到 tight bounds 的平均 forward passes 从 RS 的 49.02 降至 BEAVER 的 24.95。
- **意义**：紧致的边界使得模型能力的比较成为可能（如 Llama3.3-70B ≥43.5% 正确率），而 RS 的宽松边界（如 Qwen2.5-14B 的 [0.356, 0.704]）无法提供有意义的信息。

#### （2）Enron Email Leakage 任务（隐私验证）
- **结果见 Table 3**：
  - BEAVER 的 RDR 远高于 RS。例如，对于 Qwen3-4B，RS 的 RDR 为 15/100 (0.15)，而 BEAVER 为 **67/100 (0.67)**。
- **意义**：RS 的宽松上界会给人错误的安全感，而 BEAVER 揭示了模型存在严重的隐私泄露风险。

#### （3）Secure Code Generation 任务（安全代码生成）
- **结果见 Table 4**：
  - BEAVER 识别出的风险实例数量远超 RS。例如，对于 Qwen3-4B，RS 仅识别出 9/204 个风险实例，而 BEAVER 识别出 **68/204** 个。
  - 对于 Qwen3-30B-A3B，RS 仅识别出 2/204 个，而 BEAVER 识别出 **86/204** 个。
- **意义**：在对抗性提示下，BEAVER 能有效揭示模型生成不安全代码的重大风险，而 RS 会产生“虚假的安全感”。

### 消融实验结果
- **序列选择策略对比**（Table 5）：
  - 比较了贪心策略 **Max-p**（总是扩展概率最高的序列）和随机策略 **Sample-u**。
  - 结果显示两者最终的边界紧密度相近，但 Max-p 在收敛速度上略优。
- **温度参数的影响**（Table 6）：
  - 降低 **temperature**（如 T=0.33）会使概率分布更集中，从而使 BEAVER 更快地收敛（Gap 更小，所需 forward passes 更少）。
  - 同时，在安全验证任务中，较低的温度会提高 RDR，因为更容易确定高概率补全是不安全的。

---

## 4. 关键结论和发现

### 主要发现
1. **确定性验证是可行且实用的**：BEAVER 成功证明了为 LLM 提供 sound 的概率边界是可能的，并且在实际计算预算内是高效的。
2. **前缀封闭性是关键**：利用 `prefix-closed` 语义约束进行早期剪枝是 BEAVER 高效性的核心。
3. **紧致边界至关重要**：宽松的边界（如来自 rejection sampling）会掩盖真实风险，导致错误的部署决策。BEAVER 提供的紧致边界能实现精确的风险评估。
4. **BEAVER 显著优于基线**：在相同预算下，BEAVER 实现了 6-8 倍更紧的边界，并发现了 3-4 倍更多的高风险实例。

### 方法的局限性
1. **仅限于 Prefix-closed 约束**：无法处理非前缀封闭的约束（如必须以特定格式结尾的日期）。尽管文中提到可以转换为前缀封闭变体，但这并非对所有约束都适用。
2. **需要白盒访问**：要求访问模型每一步的完整概率分布，无法应用于黑盒 API 模型。
3. **约束检查开销**：对于复杂的语义约束（如调用外部 SMT 求解器），每次扩展时的约束检查成本可能很高，成为性能瓶颈。

### 未来工作方向
- 探索更高效的 **frontier expansion 策略**。
- 将验证扩展到 **prompt 分布** 上，而不仅仅是单个 prompt。
- 开发适用于 **有限模型访问**（如黑盒）场景的验证技术。
- 探索在 **公平性验证、幻觉量化、多轮对话安全** 等领域的应用。

</details>

---

### 9. [Bridging quantum and classical computing for partial differential equations through multifidelity machine learning](https://arxiv.org/abs/2512.05241)

**Authors**: Bruno Jacob, Amanda A. Howard, Panos Stinis  
**Category**: cs.LG  
**Published**: 2025-12-08  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.05241v1  

#### Abstract
Quantum algorithms for partial differential equations (PDEs) face severe practical constraints on near-term hardware: limited qubit counts restrict spatial resolution to coarse grids, while circuit depth limitations prevent accurate long-time integration. These hardware bottlenecks confine quantum P...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Bridging Quantum and Classical Computing for Partial Differential Equations through Multifidelity Machine Learning*

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前量子算法在求解偏微分方程（PDEs）时面临严重的硬件限制：
- **有限的 qubit 数量** 导致空间分辨率极低（粗网格）；
- **电路深度受限** 阻碍长时间积分的精度；
- 因此，量子 PDE 求解器只能产生**低保真度（low-fidelity）** 的近似解，难以满足实际科学计算需求。

尽管量子算法理论上具有加速潜力，但在近期硬件上无法直接达到高精度，限制了其“量子优势”的实现。

---

### 提出的新方法与新思路
作者提出了一种**混合量子-经典多保真度机器学习框架（hybrid quantum-classical multifidelity learning framework）**，其核心思想是：
- 利用**量子求解器**（如 QLBM）生成大量**低保真数据**（coarse grid, short time）；
- 利用**经典求解器**生成少量**高保真参考数据**（fine grid, long time）；
- 构建一个基于 **Kolmogorov-Arnold Networks (KANs)** 的多保真度神经网络架构，学习从低保真到高保真的映射关系，从而“纠正”量子解为高精度预测。

该框架的关键组件包括：
- **低阶网络 $K_{LF}$**：仅训练于量子输出，用于建模低保真动态；
- **线性校正网络 $K_{lin}$** 和 **非线性校正网络 $K_{nl}$**：分别捕捉线性和非线性相关性；
- **可学习混合参数 $\alpha$**：自动平衡线性与非线性校正权重，形式为：
  $$
  q_{MF} = \alpha \cdot K_{nl}(x,t,q_{LF}) + (1-\alpha) \cdot K_{lin}(x,t,q_{LF})
  $$

---

### 相比现有方法的优势
| 方面 | 优势 |
|------|------|
| **资源效率** | 显著减少对昂贵高保真模拟的需求（只需稀疏采样），降低总体计算成本 |
| **泛化能力** | 能够进行**时间外推（temporal extrapolation）**，即在无高保真训练数据的时间段仍能准确预测 |
| **硬件兼容性** | 充分利用当前含噪声、小规模量子设备的能力，无需等待容错量子计算机 |
| **模型可解释性** | KAN 结构通过边上的可学习样条函数提供更强的参数效率和解释性 |
| **系统性误差修正** | 成功补偿量子求解器因粗网格导致的空间平滑和数值耗散 |

---

## 2. 核心实验方法和设置

### 使用的数据集与物理问题
研究在两个典型非线性 PDE 上验证方法有效性：

1. **1D 黏性 Burgers 方程**  
   - 初始条件：高斯脉冲 $ u(x,0) = \exp(-40(x-0.35)^2) $
   - 边界条件：周期边界
   - 物理特性：非线性对流与扩散竞争，形成陡峭梯度

2. **2D 顶盖驱动空腔流（lid-driven cavity flow）**
   - 域：$[0,1]^2$，顶部边界速度 $u=1$，其余壁面静止
   - Reynolds 数：Re = 100
   - 使用**流函数-涡量公式**避免压力求解

---

### 实验设置
| 组件 | 设置说明 |
|------|----------|
| **量子求解器** | 采用 **QLBM-frugal** 方法：<br>- Burgers: D1Q3 lattice, 16-grid points<br>- Navier-Stokes: D2Q5 lattice, 16×16 grid<br>- 使用 block encoding + LCU 实现非酉碰撞算子 |
| **经典求解器** | <br>- Burgers: FD, upwind advection, central diffusion, 256-grid<br>- NS: streamfunction-vorticity FDM, 64×64 grid |
| **训练策略** | 分两阶段：<br>1. 用全部量子数据训练 $K_{LF}$<br>2. 冻结 $K_{LF}$ 后，用稀疏经典数据训练 $K_{lin}, K_{nl}, \alpha$ |
| **外推设置** | 高保真训练数据仅覆盖部分时间区间：<br>- Burgers: $t \leq 0.25$（总至 0.5）<br>- Cavity: $t \leq 2.0$（总至 3.0） |
| **评估指标** | 相对 $L^2$ 误差：<br>$L_2 = \|q_{pred} - q_{HF}\| / \|q_{HF}\|$<br>分为训练区、外推区、全域误差 |

---

### 基线方法对比
- **纯量子低保真解（LF）**：原始 QLBM 输出
- **纯非线性校正（$\alpha=1$ 固定）**
- 不同正则化强度（$\lambda_\alpha$）和样条分辨率（$G$）的消融实验

---

## 3. 主要实验结果和性能指标

### 关键性能数据汇总

#### ✅ **Burgers 方程（实验 B4 最优）**
| 方法 | 全域 $L_2$ 误差 | 外推区误差 | 提升幅度 |
|------|------------------|------------|-----------|
| 原始量子 LF | 0.335 | — | — |
| MF-KAN (B4) | **0.0615** | 0.0914 | ↓ **82%** |

> 在外推区域误差相比未正则化模型下降 **21%**

#### ✅ **2D 空腔流（实验 C3 最优）**
| 方法 | 水平速度 $L_2$ | 垂直速度 $L_2$ | 提升幅度 |
|------|------------------|------------------|-----------|
| 原始量子 LF | 0.285 | 0.412 | — |
| MF-KAN (C3) | **0.079** | **0.167** | ↓ **72% (u), 60% (v)** |

> 外推至 $t=3.0$ 时仍保持高精度，尤其在边界层附近改进显著

---

### 与基线方法的对比结果
- **固定 $\alpha=1$（纯非线性）**：
  - Burgers 上外推失败（$L_2^{extrap}=0.7173 > LF$）
  - 表明**过度依赖非线性校正会导致过拟合**
- **强正则化（$\lambda_\alpha=10^{-4}$）**：
  - 学得较小 $\alpha$（0.53），更依赖线性路径
  - 外推性能最佳 → **线性先验有助于泛化**

---

### 消融实验关键发现

#### 🔹 正则化强度 $\lambda_\alpha$ 影响
- $\lambda_\alpha = 0$：训练误差低但外推差（过拟合）
- $\lambda_\alpha = 10^{-4}$（Burgers）、$10^{-5}$（NS）：最优权衡
- 正则项 $\lambda_\alpha \alpha^n$（$n=4$）有效防止复杂模型滥用

#### 🔹 B-spline 网格分辨率 $G$
- $G=3$：欠表达，训练误差高
- $G=7$：过表达，外推性能恶化（过拟合）
- $G=5$：最佳平衡点 → **适度表达力优于最大容量**

#### 🔹 网络宽度影响（Cavity 测试）
- 宽度过小（5）→ 欠拟合
- 过宽（15）→ 训练时间增加且未提升性能
- 中等宽度（10）结合正则化表现最好

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **多保真度学习可有效桥接量子与经典计算**：即使量子解非常粗糙（16 vs 256 点），也能通过稀疏经典数据纠正至接近高保真精度。
2. ✅ **成功实现时间外推**：模型能在没有高保真训练数据的时间段做出准确预测，拓展了实用场景。
3. ✅ **线性-非线性混合结构至关重要**：自动学习的 $\alpha$ 参数表明，**线性相关性主导泛化行为**，而正则化鼓励简约模型。
4. ✅ **KAN 架构适合此类任务**：边缘可学习样条提供了良好的函数逼近能力和稳定性，尤其适用于时空连续场的建模。

---

### 方法的局限性
| 局限 | 说明 |
|------|------|
| **依赖高质量量子模拟输出** | 当前实验基于 statevector 模拟；真实硬件噪声可能破坏低阶解质量 |
| **需经典高保真数据初始化** | 尽管稀疏，但仍需要一定数量的经典仿真作为锚点 |
| **特定于QLBM结构** | 当前框架针对 QLBM 设计，扩展到其他量子算法需重新适配 |
| **尚未在真实量子硬件运行** | 所有量子电路均通过 Qiskit 模拟执行 |

---

### 未来工作方向
1. **引入 Physics-Informed Learning**：加入残差约束或守恒律，进一步减少对高保真数据的依赖。
2. **不确定性量化（UQ）扩展**：构建概率性多保真模型（如 Bayesian KAN），估计预测置信区间。
3. **真实硬件部署**：在超导或离子阱设备上测试框架鲁棒性。
4. **扩展至更多 PDE 类型**：如反应-扩散系统、可压缩流、多相流等。
5. **结合 Variational Quantum Algorithms**：将 VQA 与本框架融合，处理更强非线性问题。

---

## 总结
本文开创性地提出了一条**通往近期量子实用性（NISQ utility）的可行路径**：不追求单一量子求解器的完美精度，而是将其视为**快速低保真引擎**，再通过**多保真机器学习**升级为高精度预测工具。这种方法不仅提升了量子计算的实际价值，也为未来量子-经典协同算法的设计提供了范式参考。

</details>

---

### 10. [Utility Boundary of Dataset Distillation: Scaling and Configuration-Coverage Laws](https://arxiv.org/abs/2512.05817)

**Authors**: Zhengquan Luo, Zhiqiang Xu  
**Category**: cs.LG  
**Published**: 2025-12-08  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.05817v1  

#### Abstract
Dataset distillation (DD) aims to construct compact synthetic datasets that allow models to achieve comparable performance to full-data training while substantially reducing storage and computation. Despite rapid empirical progress, its theoretical foundations remain limited: existing methods (gradi...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# Utility Boundary of Dataset Distillation: Scaling and Configuration-Coverage Laws 论文总结

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题

本文针对 **Dataset Distillation (DD)** 领域存在的两大核心挑战：

1. **理论碎片化**：现有的 DD 方法（如 Gradient Matching, Distribution Matching, Trajectory Matching）基于异构的代理目标（surrogate objectives）和优化假设，缺乏统一的理论框架来解释其共性原理或提供通用保证。
2. **配置鲁棒性缺失**：当前方法在训练配置（如 optimizer、architecture、augmentation）发生变化时，蒸馏数据的有效性往往显著下降，尚不清楚在何种条件下蒸馏数据能保持与全量数据相当的性能。

### 提出了什么新方法或新思路

作者提出了一种名为 **Configuration-Dynamics-Error Analysis** 的统一理论框架，将主流 DD 方法置于一个共同的泛化误差视角下进行分析，并由此得出两个核心定律：

#### （1）**Scaling Law（缩放律）**

- **内容**：当蒸馏样本数 $k$ 增加时，泛化误差以 $\Delta \leq O(1/\sqrt{k}) + \epsilon_{\text{bound}}$ 的速率下降。
- **意义**：解释了实践中广泛观察到的 **IPC（Images Per Class）饱和现象**——当 $k$ 足够大时，误差被不可约的 $\epsilon_{\text{bound}}$ 所限制，继续增加 $k$ 无法提升性能。此时，降低 $\epsilon_{\text{bound}}$ 比盲目增加 $k$ 更有效。

#### （2）**Coverage Law（覆盖律）**

- **内容**：为了在多样化的配置族 $\mathcal{A}$ 上保持泛化能力，所需的蒸馏样本数 $k$ 必须与配置多样性 $H_{\text{cov}}(\mathcal{A}, r)$ 成**线性关系**，即 $k = \Omega(H_{\text{cov}}(\mathcal{A}, r))$。
- **意义**：首次形式化定义了 **Dataset Distillation 的效用边界（utility boundary）**，揭示了样本效率与配置鲁棒性之间的根本权衡。

此外，该框架还揭示了 GM、DM、TM 三种看似不同的匹配方法本质上是同一目标（最小化训练动态差异）的不同代理选择，它们通过相同的双层机制减少相同的泛化误差。

### 相比现有方法的优势

- **理论统一性**：首次将 GM、DM、TM 统一在一个理论框架下，解释了为何不同方法都能实现数据蒸馏。
- **可解释性强**：提出的两个定律为 DD 的设计提供了明确的指导原则，例如如何平衡样本大小与配置多样性。
- **指导实践**：为构建更高效、更具配置鲁棒性的蒸馏方法提供了理论依据，推动了从经验驱动向理论驱动的设计范式转变。

---

## 2. 核心实验方法和设置

### 使用了哪些数据集

实验在以下四个标准数据集上进行：
- **MNIST**
- **CIFAR-10**
- **CIFAR-100**
- **ImageNette**（ImageNet 的 10 类子集）

### 实验设置和评估指标

#### 实验设置
- **蒸馏方法**：选取四类代表性方法：
  - **Gradient Matching (GM)**：DC、DSA
  - **Distribution Matching (DM)**：DM
  - **Trajectory Matching (TM)**：MTT
  - **Diffusion-based**：MGD3（用于 ImageNette）
- **源配置（Source Configuration）**：所有蒸馏均在 `ConvNet + SGD` 下完成（启用 DSA 时也一致）。
- **目标配置（Target Configuration）**：评估时使用多种组合，包括不同架构（ConvNet, LeNet, ResNet-18, AlexNet）、优化器（SGD, Adam）和增强策略（启用/禁用 DSA）。
- **蒸馏预算**：系统性地改变每类图像数（IPC），范围为 `{2, 4, 6, 8, 12, 18, 28, 51, 100, 200}`。

#### 评估指标
- **主要指标**：**泛化误差（Generalization Error）**
  $$
  \Delta_a = |R(\theta_s^{(a)}) - R(\theta_T^{(a)})|
  $$
  即在相同目标配置 $a$ 下，使用蒸馏数据训练模型与使用真实数据训练模型的测试风险差。
- **验证 Scaling Law**：固定单一配置（如 `ConvNet+SGD`），绘制 $\Delta_a$ 对 $1/\sqrt{k}$ 的曲线，检验是否呈线性关系。
- **验证 Coverage Law**：对配置子集大小 $m$，计算平均误差 $\Delta(k, m)$，并绘制 $\Delta(k, m)$ 对 $\sqrt{\log m}/\sqrt{k}$ 的曲线，检验线性趋势。
- **配置多样性估计**：使用 $\log M$（候选配置数量的对数）作为 $H_{\text{cov}}(\mathcal{A}, r)$ 的代理。

---

## 3. 主要实验结果和性能指标

### 关键性能数据与对比结果

#### （1）Scaling Law 验证（单配置场景）

- 在 **MNIST、CIFAR-10、CIFAR-100 和 ImageNette** 上，所有方法的 $\Delta_a$ 均随 $1/\sqrt{k}$ 近似线性下降。
- **拟合优度 $R^2$ 多数高于 0.85**，强烈支持理论预测。
  - **MNIST**：DC、DSA、DM 的 $R^2$ 达 0.90–0.99，MTT 斜率更陡且方差大，反映其不稳定性。
  - **CIFAR-10**：所有方法 $R^2 = 0.86–0.96$，DC 和 DSA 最稳定。
  - **CIFAR-100**：DC 和 DSA 仍保持高线性（$R^2 = 0.92–0.97$），而 DM ($R^2=0.84$) 和 MTT ($R^2=0.73$) 偏离更多，表明轨迹失配在复杂数据集上被放大。
- **结论**：$1/\sqrt{k}$ 缩放律具有普适性，但不同方法的样本效率和稳定性存在差异。

#### （2）Coverage Law 验证（跨配置场景）

- 绘制 $\Delta(k, m)$ 对 $\sqrt{\log m}/\sqrt{k}$ 的曲线，观察到近似的线性趋势，支持 Coverage Law。
- **斜率反映方法对配置多样性的敏感度**：
  - **MNIST**：MTT 敏感度最高（$\beta_1=0.91$），DC/DSA 较低（$\beta_1 \sim 0.20$），DM 居中（$\beta_1=0.14$）。
  - **CIFAR-10**：敏感度排序为 MTT < DC < DSA < DM，表明 DM 对多样性最稳健但依赖更大的 $k$。
  - **CIFAR-100**：规律依然可见但拟合度降低（$R^2 < 0.74$），因类别增多导致估计噪声增大。
- **结论**：Coverage Law 得到实证支持，不同方法在配置鲁棒性上表现不同。

#### （3）消融实验与发现

- **MTT 不稳定性**：在多个数据集上，MTT 表现出更高的方差和更陡的斜率，验证了其对配置变化的脆弱性。
- **GM 方法（DC/DSA）**：在单一配置下样本效率高，但在跨配置时退化较快。
- **DM 方法**：相对更鲁棒于配置多样性，但需要更多样本才能达到低误差。
- **扩散方法 MGD3**：在 ImageNette 上也表现出 $1/\sqrt{k}$ 缩放，说明该定律可能超越传统匹配范式。

---

## 4. 关键结论和发现

### 论文的主要发现

1. **统一框架成立**：GM、DM、TM 并非独立启发式方法，而是通过双层优化机制减少相同泛化误差的不同代理选择。
2. **Scaling Law 存在**：泛化误差随 $1/\sqrt{k}$ 下降，最终受限于不可约误差 $\epsilon_{\text{bound}}$，解释了 IPC 饱和现象。
3. **Coverage Law 存在**：蒸馏样本数 $k$ 必须与配置多样性 $H_{\text{cov}}$ 线性增长，才能维持跨配置的性能，这定义了 DD 的“效用边界”。
4. **方法特性差异**：GM 高效但不鲁棒，DM 鲁棒但需更多样本，TM 易受配置变化影响。

### 方法的局限性

1. **优化动力学假设**：理论依赖 PL 条件和 Lipschitz 连续性，在大规模自适应优化器（如 AdamW）或非光滑目标下可能失效。
2. **覆盖复杂度估计**：实验中使用 $\log M$ 作为 $H_{\text{cov}}$ 的代理，可能低估实际异质性，尤其当配置间差异显著时。
3. **生态范围有限**：当前框架仅考虑算法级变化（optimizer, architecture），未涵盖数据分布偏移（如跨域迁移）、类别不平衡等语义层面的多样性。

### 未来工作方向

1. **放宽优化假设**：将理论扩展到更弱的条件，如 one-point convexity 或 uniform stability。
2. **改进覆盖估计**：开发基于 $d_A$ 度量空间的聚类方法，自适应分配蒸馏原型。
3. **扩展配置定义**：将数据分布距离（如 Wasserstein 或 MMD）纳入 $d_A$，研究跨域场景下的缩放律。
4. **理论驱动设计**：利用 Coverage Law 设计更高效的蒸馏策略，例如根据配置多样性动态调整 $k$。

--- 

> **总结**：本文通过提出 **Configuration-Dynamics-Error** 框架，首次为 Dataset Distillation 建立了坚实的理论基础，揭示了 **Scaling Law** 和 **Coverage Law** 两大核心规律，不仅统一了解释现有方法，也为未来构建更高效、更鲁棒的蒸馏技术提供了明确的设计蓝图。

</details>

---

### 11. [A Fast Anti-Jamming Cognitive Radar Deployment Algorithm Based on Reinforcement Learning](https://arxiv.org/abs/2512.05753)

**Authors**: Wencheng Cai, Xuchao Gao, Congying Han, Mingqiang Li, Tiande Guo  
**Category**: cs.AI  
**Published**: 2025-12-08  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2512.05753v1  

#### Abstract
The fast deployment of cognitive radar to counter jamming remains a critical challenge in modern warfare, where more efficient deployment leads to quicker detection of targets. Existing methods are primarily based on evolutionary algorithms, which are time-consuming and prone to falling into local o...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文核心结论与实验结果总结

## 1. 论文的主要贡献和创新点

### 解决的问题
本文针对**认知雷达在电子干扰环境下的快速部署问题**（Anti-Jamming Cognitive Radar Deployment），该问题是现代战争中提升目标探测效率的关键挑战。传统方法依赖于**进化算法**（如 GA、PSO），存在以下缺陷：
- **计算耗时长**：需要多轮迭代和种群适应度评估；
- **易陷入局部最优**：搜索策略受限，难以充分探索解空间。

### 提出的新方法与创新思路
作者提出了一种全新的基于**深度强化学习**（Deep Reinforcement Learning, DRL）的框架——**Fast Anti-Jamming Radar Deployment Algorithm (FARDA)**，其核心创新包括：

1. **首次将 DRL 应用于抗干扰雷达部署问题**  
   迄今为止，尚无研究使用 DRL 解决此类组合优化问题。

2. **端到端建模为序列决策问题**  
   将雷达部署建模为一个**马尔可夫决策过程**（MDP），通过智能体逐步选择雷达位置。

3. **设计专用神经网络架构**
   - 构建了一个融合 **DCNN** 和 **LSTM** 的**编码器模块**（Encoder Network）：
     - DCNN 用于提取检测概率热力图（heatmap）的空间特征；
     - LSTM 用于记忆历史部署状态的时间动态变化。

4. **提出新型奖励整形机制 CVDP-EXPR**
   - **Constraint Violation Degree Penalty (CVDP)**：对偏离均匀部署约束的行为施加惩罚，防止训练崩溃；
   - **EXPonential Function Reward (EXPR)**：缓解后期增益递减导致的学习停滞问题。

5. **问题简化策略提升求解效率**
   - **维度降低**：仅在部署区域边界 BR 上部署雷达；
   - **松弛处理**：将离散动作空间松弛为连续边界；
   - **环境简化**：缩小采样密度并裁剪低信息量区域，减少计算开销。

### 相比现有方法的优势
- **速度极快**：部署速度约为进化算法的 **7,000 倍**；
- **覆盖性能更优或相当**：平均覆盖率略高于基线；
- **训练稳定高效**：通过 CVDP-EXPR 避免策略退化；
- **适用于实时战场响应场景**：满足“快速部署即战斗力”的军事需求。

---

## 2. 核心实验方法和设置

### 数据集与环境设置
- **仿真区域 S**：$20\text{km} \times 120\text{km}$，坐标范围 $[3\times10^4, 5\times10^4] \times [0, 1.2\times10^5]$；
- **干扰节点区域 $D_j$**：右上角矩形区域；
- **雷达可部署区域 $D_R$**：左下角矩形区域；
- **离散化粒度**：原始为每 100 米采样一点（共约 $2.4\times10^5$ 点），训练时简化为每 500 米采样，并去除下半部分，减少至约 1/50 的计算量；
- **干扰源数量**：固定 3 个；
- **需部署雷达数**：4 个。

### 实验设置
- **测试数据集**：随机生成 500 组不同的干扰节点组合；
- **训练配置**：
  - 使用 **Nvidia GeForce RTX 3090 GPU** 训练 FARDA；
  - 进化算法运行于 **Intel i9-12900 CPU**；
  - 总训练回合数 $E = 2\times10^5$，每轮最多 4 步（对应 4 个雷达）；
  - 使用 **PPO** 算法训练，**Adam** 优化器。

### 评估指标
| 指标 | 定义 |
|------|------|
| **Coverage (%)** | 被成功探测的区域占比（以阈值 $T=0.5$ 判定） |
| **Time** | 单次部署所需时间（秒或分钟） |
| **Efficiency** | 自定义效率指标：<br>$\frac{\text{coverage}}{\log(1+\text{time})}$ |

### 基线方法对比
- **PSO1D**：粒子群优化，在边界 BR 上部署；
- **GA1D**：遗传算法，在边界 BR 上部署；
> 注：文中已验证在边界部署优于全区域部署，因此所有比较均基于边界策略。

---

## 3. 主要实验结果和性能指标

### 关键性能数据（见 Table 3）
| 方法 | Coverage (%) | Time | Efficiency |
|------|---------------|--------|------------|
| PSO1D | 93.75 | 24.9 min | 0.13 |
| GA1D | 93.79 | 30.7 min | 0.12 |
| **FARDA** | **93.94** | **0.25s** | **4.21** |

#### 对比分析
- **速度优势**：FARDA 比最慢的 GA1D 快约 **7,368 倍**（30.7 min vs 0.25 s）；
- **效率提升**：效率指标从 ~0.12 提升至 **4.21**，提高超过 **35 倍**；
- **覆盖性能**：FARDA 在所有类别数据上均优于基线。

### 不同难度数据集上的表现（Table 4）
| 数据类型 | 数量 | FARDA (%) | GA1D (%) | 提升幅度 |
|---------|-------|-------------|-----------|----------|
| Bad（<90%） | 56 | **89.70** | 89.44 | +0.29% |
| Normal（90~95%） | 288 | **93.10** | 92.94 | +0.17% |
| Good（>95%） | 156 | **97.00** | 96.92 | +0.08% |

> 结果显示：FARDA 在更难的“Bad”和“Normal”案例中提升更为显著，说明其在复杂干扰环境下更具实用性。

### 消融实验结果

#### （1）网络结构消融（Table 5）
| 方法 | Coverage (%) |
|------|----------------|
| FARDA（完整） | **93.94** |
| w/o CNN | 93.89 |
| w/o LSTM | 93.90 |
| w/o Encoder | 91.44 |

> **结论**：Encoder 至关重要；移除任一组件都会导致性能下降，尤其是完全移除 Encoder 导致 **-2.5%** 的大幅回落。

#### （2）奖励机制消融（Table 6）
| 方法 | Coverage (%) |
|------|----------------|
| FARDA（完整） | **93.94** |
| w/o CVDP | 88.80 |
| w/o EXPR | 93.90 |
| w/o CVDP-EXPR | 88.80 |

> **关键发现**：
> - 移除 **CVDP** 导致严重性能崩溃（↓5.14%），可视化显示雷达聚集在同一方向（图5）；
> - CVDP 引入负奖励机制，防止策略发散；
> - EXPR 影响较小，但仍有助于维持学习动力。

---

## 4. 关键结论和发现

### 主要结论
1. **FARDA 是首个将 DRL 成功应用于抗干扰雷达部署的框架**，实现了从“缓慢迭代搜索”到“快速推理决策”的范式转变。
2. 通过**问题重构 + 网络设计 + 奖励整形**三位一体的设计，解决了高维、非连续、大搜索空间带来的优化难题。
3. 实验表明，FARDA 在保持甚至略微提升**覆盖性能**的同时，实现高达 **7,000 倍的速度加速**，具备极强的实战应用潜力。

### 方法的局限性
- 当前模型假设为**静态干扰环境**，未考虑干扰源移动等动态情况；
- 雷达数量固定为 4，尚未扩展至可变规模；
- 所有参数（如功率、波长等）预设不变，未联合优化；
- 模型泛化能力有待在更多地形和部署形状中验证。

### 未来工作方向
1. **拓展至更复杂的静态环境**：
   - 变化的雷达/干扰源数量；
   - 不规则的部署区域形状；
   - 更大的地理尺度。

2. **研究动态环境下的自适应部署**：
   - 干扰节点随时间移动；
   - 雷达位置需平滑调整（不能突变）；
   - 引入在线学习与重规划机制。

3. **改进评估体系**：
   - 设计面向动态场景的新型评价指标；
   - 加入通信成本、能耗等因素进行多目标优化。

4. **探索多智能体协同部署**（Multi-Agent RL）以应对大规模分布式雷达系统。

</details>

---

### 12. [Interleaved Latent Visual Reasoning with Selective Perceptual Modeling](https://arxiv.org/abs/2512.05665)

**Authors**: Shuai Dong, Siyuan Wang, Xingyu Liu, Zhongyu Wei  
**Category**: cs.CL  
**Published**: 2025-12-08  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2512.05665v1  

#### Abstract
Interleaved reasoning paradigms enhance Multimodal Large Language Models (MLLMs) with visual feedback but are hindered by the prohibitive computational cost of repeatedly re-encoding pixel-dense images. A promising alternative, latent visual reasoning, circumvents this bottleneck yet currently force...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Interleaved Latent Visual Reasoning with Selective Perceptual Modeling

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前多模态大语言模型（MLLMs）在进行**交错图像-文本推理**（interleaved image-text reasoning）时面临两个核心瓶颈：
- **计算成本过高**：反复对高分辨率像素图像进行编码导致推理效率低下。
- **感知与动态建模的权衡**：现有**潜在视觉推理**（latent visual reasoning）方法存在两难：
  - 要么通过过度压缩特征牺牲细粒度感知能力（如 Mirage）；
  - 要么采用静态、非交错结构，无法建模动态演化的视觉状态（如 LVR）。

### 提出的新方法：Interleaved Latent Visual Reasoning (ILVR)
ILVR 是一种新型的多模态推理框架，其核心思想是：
- 在**文本生成过程**中**交错插入动态更新的潜在视觉表示**（latent visual representations），作为后续推理步骤的具体、演化线索。
- 这些潜在表示不是一次性生成，而是随着推理步骤逐步更新，实现**动态状态演化**。

### 创新机制
1. **自监督的选择性感知建模**（Selective Perceptual Modeling）：
   - 引入一个**动量教师模型**（Momentum Teacher Model），从“辅助图像”（helper images）中选择最关键的视觉 patch 特征。
   - 教师模型基于当前上下文（文本历史 + 上一步潜在状态）生成查询向量，通过注意力机制筛选出最相关的视觉特征，形成稀疏监督目标。
2. **两阶段训练策略**：
   - **第一阶段**：联合监督，强制学生模型生成的潜在状态与教师选择的特征对齐（`latent alignment loss`）。
   - **第二阶段**：仅保留文本监督，放松潜在对齐约束，使模型内化推理过程，提升灵活性。

### 相比现有方法的优势
| 维度 | 现有方法（如 Mirage / LVR） | ILVR |
|------|----------------------------|------|
| 结构 | 静态、非交错（non-interleaved） | 动态、交错（interleaved） |
| 感知精度 | 过度压缩导致细节丢失 | 自适应选择关键特征，保留细粒度信息 |
| 动态建模 | 无法模拟状态变化 | 支持多步视觉状态演化 |
| 推理效率 | 避免重复像素编码 | 同样避免像素级处理，高效 |

---

## 2. 核心实验方法和设置

### 数据集
- **主评估基准**：
  - **COMT**：包含 Creation, Deletion, Selection, Update 四类任务，共 400 个测试样本。
  - **VSP**：空间规划任务，评估感知与推理双重挑战，400 个测试样本。
- **泛化性评估**（OOD）：
  - **Zebra-CoT**：训练集用于训练，保留 2D 子集（Visual Jigsaw, Visual Search）用于测试。
  - **EMMA BENCH**：科学推理任务（Chemistry, Coding, Math, Physics），400 题。
  - **VisualLogic**：细粒度视觉逻辑推理（Positional, Quantitative, Stylistic），290 题。

### 实验设置
- **基础模型**：Qwen2.5-VL 7B。
- **训练配置**：
  - AdamW 优化器，学习率 1e-5，余弦调度。
  - 潜在 token 数量 $ K = 8 $，EMA 衰减系数 $ \tau = 0.999 $。
  - 训练轮数：COMT/VSP 为 15 轮，Zebra-CoT 为 2 轮。
- **评估指标**：准确率（Accuracy），宏平均（Macro-average）。

### 基线方法对比
| 类型 | 方法 |
|------|------|
| 零样本 | Qwen2.5-VL (Zero-shot) |
| 微调基线 | Qwen2.5-VL (Direct-FT), Qwen2.5-VL (CoT-FT) |
| 潜在推理基线 | Mirage（直接范式，静态压缩） |
| 本文方法 | ILVR（交错范式） |

> 注：LVR 因依赖真实边界框标注，未参与通用任务比较。

---

## 3. 主要实验结果和性能指标

### 主要性能对比（Table 1）
| 方法 | 范式 | COMT (Avg.) | VSP (Acc.) |
|------|------|-------------|-----------|
| Qwen2.5-VL (Direct-FT) | Direct Ans. | 53% | 72% |
| Qwen2.5-VL (CoT-FT) | Text CoT | 55.8% | 47% |
| Mirage | Direct | 56% | 76% |
| **ILVR (Ours)** | **Interleaved** | **60.8%** | **81.5%** |

> ✅ ILVR 在两个主基准上均显著优于所有基线，尤其在 VSP 上超越最强基线 5.5 个百分点。

### 泛化性能（Table 2）
| 方法 | EMMA BENCH | VisualLogic | Zebra-CoT (OOD) | **Overall Avg.** |
|------|------------|--------------|------------------|------------------|
| CoT-FT | 27.8% | 25.9% | 45% | 33.6% |
| Mirage | 27.0% | 26.6% | 47.3% | 34.3% |
| **ILVR (Ours)** | **33.3%** | **29.3%** | **47.8%** | **37.5%** |

> ✅ ILVR 在跨领域任务中表现最优，尤其在科学推理（EMMA）和细粒度逻辑（VisualLogic）上优势明显。

### 消融实验结果（Table 3 & 4）

#### 核心组件消融（Table 3）
| 方法 | 结构 | 机制 | 总体准确率 |
|------|------|------|------------|
| Direct (Pooling) | × | 平均池化 | 31.5% |
| Direct (Adaptive) | × | 自适应选择 | 32.4% |
| **ILVR (Ours)** | √ | 自适应选择 | **34.8%** |

> 🔍 结论：**交错结构**（interleaved structure）比自适应选择更重要，二者结合效果最佳。

#### 超参数分析
- **潜在 token 数量 $ K $**（Fig. 4）：
  - 最优值为 $ K=8 $，总准确率达 34.8%。
  - $ K > 8 $ 后性能下降，表明过长序列引入噪声。
- **对齐损失权重 $ \lambda_{\text{sim}} $**（Table 4）：
  - 最优值为 $ \lambda_{\text{sim}} = 1.0 $。
  - 权重过大（如 10）会过度约束潜在空间，损害推理灵活性。

---

## 4. 关键结论和发现

### 主要发现
1. **交错式潜在推理优于静态压缩**：
   - 动态更新的潜在表示能有效建模复杂任务中的状态演化（如棋局推演、物体操作）。
2. **选择性感知建模至关重要**：
   - 动量教师引导的特征选择机制可避免信息丢失，保留关键视觉细节。
3. **两阶段训练提升泛化能力**：
   - 先严格对齐，再放松约束，有助于模型内化推理模式而非机械模仿。
4. **ILVR 实现了感知与推理的统一**：
   - 在不牺牲推理能力的前提下，实现了细粒度视觉理解与多步动态推理的融合。

### 方法的局限性
- **依赖高质量的 helper images**：需要预构建包含中间状态的图像序列作为监督信号，在真实世界应用中可能难以获取。
- **潜在表示仍为黑箱**：虽然可视化显示注意力聚焦合理区域，但潜在空间的语义解释性有待加强。
- **扩展到更大模型的成本**：目前基于 7B 模型验证，扩展至更大规模（如 72B）的训练开销需进一步评估。

### 未来工作方向
- 构建自动化的 helper image 生成 pipeline，减少人工标注依赖。
- 探索无教师的自蒸馏方式，进一步降低监督需求。
- 将 ILVR 应用于具身智能（embodied AI）、机器人规划等需要长期状态追踪的任务。
- 研究潜在表示的可解释性与可控编辑能力。

---

> **代码开源地址**：[https://github.com/XD111ds/ILVR](https://github.com/XD111ds/ILVR)

</details>

---

### 13. [IDK-S: Incremental Distributional Kernel for Streaming Anomaly Detection](https://arxiv.org/abs/2512.05531)

**Authors**: Yang Xu, Yixiao Ma, Kaifeng Zhang, Zuliang Yang, Kai Ming Ting  
**Category**: cs.LG  
**Published**: 2025-12-08  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2512.05531v1  

#### Abstract
Anomaly detection on data streams presents significant challenges, requiring methods to maintain high detection accuracy among evolving distributions while ensuring real-time efficiency. Here we introduce $\mathcal{IDK}$-$\mathcal{S}$, a novel $\mathbf{I}$ncremental $\mathbf{D}$istributional $\mathb...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：IDK-S: Incremental Distributional Kernel for Streaming Anomaly Detection

---

## 1. 论文的主要贡献和创新点

### 解决的问题
该论文针对**数据流环境下的异常检测**（Streaming Anomaly Detection）中存在的两个核心挑战：
- **概念漂移**（Concept Drift）：数据分布随时间变化，传统静态模型无法适应；
- **计算效率瓶颈**：现有方法若采用全量重训练（retraining-based）策略，计算开销大，难以满足实时性要求。

许多现有流式异常检测器基于较弱的离线基础模型（如 iForest、LOF），其性能上限受限于这些基础模型的能力，尤其在处理密度变化大或局部异常的数据时表现不佳。

---

### 提出的新方法与创新思路
作者提出 **IDK-S**（Incremental Distributional Kernel for Streaming anomaly detection），一种面向数据流的增量式分布核方法，核心创新如下：

#### （1）继承并改进强大的离线检测器 IDK
- 基于 **Isolation Distributional Kernel (IDK)** 构建，该方法利用**数据依赖型核函数**（data-dependent kernel），能更准确捕捉复杂数据分布中的“正常性”。
- IDK 在离线场景下已被证明显著优于 iForest、LOF 和 kNN 等经典方法。

#### （2）设计轻量级增量更新机制
- 避免对整个模型进行昂贵的周期性重训练；
- 引入**增量分区替换机制**（incremental partition-update mechanism）：
  - 将旧窗口中过时样本生成的 hypersphere 分区移除；
  - 用新到达数据中的样本构建新的分区进行替换；
  - 仅更新受影响部分的特征映射和均值向量。

#### （3）理论保障：统计等价性
- 提出 **Proposition 1 (Sampling Distribution Equivalence)**：
  > IDK-S 的采样过程与完全重训练版本的 IDK 具有相同的样本集合概率分布。
- 这意味着其效率提升**不是以牺牲模型统计有效性为代价的近似方法**，而是保持了原始模型的统计性质。

---

### 相比现有方法的优势
| 维度 | IDK-S 的优势 |
|------|--------------|
| **准确性** | 基于更强的 IDK 模型，在多数数据集上达到 SOTA AUC 性能 |
| **效率** | 时间复杂度从 $O(wbt)$ 降至 $O(lwt)$，其中 $l \ll w$，实现数量级加速 |
| **内存友好** | 空间复杂度与窗口大小相关，适用于无限长数据流 |
| **适应性** | 能有效应对渐进式概念漂移（gradual concept drift） |

---

## 2. 核心实验方法和设置

### 使用的数据集
共使用 **13 个基准数据集**，涵盖不同类型：

| 类型 | 数据集 |
|------|--------|
| **大规模静态数据流**（11个） | `Donors`, `Http`, `ForestCover`, `Fraud`, `Mulcross`, `Smtp`, `Shuttle`, `Mammography`, `NYC_taxi_single`, `Annthyroid`, `Satellite` |
| **非平稳/概念漂移数据流**（2个） | `INSECTS`（突变型漂移）、`TwoCluster`（渐进型漂移） |

> 所有静态数据集经过随机打乱后模拟数据流输入；`INSECTS` 包含真实由温度引起的昆虫行为变化。

---

### 实验设置与评估指标

| 设置项 | 配置说明 |
|-------|----------|
| **窗口大小** $w$ | 2048 |
| **更新步长** $l$ | 100 |
| **集成数** $t$ | 100（所有 ensemble 方法统一设置） |
| **IDK-S 参数搜索范围** | $\nu \in [2^1, 2^2, ..., 2^6]$ |
| **运行次数** | 每个方法在每个数据集上执行 20 次取平均 |
| **硬件平台** | AMD 128核 CPU @ 2GHz, 1TB RAM（除 Memstream 外均跑在 CPU 上） |

#### 评估指标
- **检测效果**：ROC-AUC（Area Under Curve）
- **运行效率**：总运行时间（seconds）
- **动态性能分析**：滑动窗口内瞬时 AUC 曲线（用于观察对概念漂移的响应能力）

---

### 基线方法对比
比较了五类主流流式异常检测方法，代表不同技术路线：

| 方法 | 基础模型 | 类型 |
|------|---------|------|
| **SiForest** | Isolation Forest | 流式扩展 |
| **oIFOR** | Isolation Forest | 流式扩展（基于多分辨率直方图） |
| **DILOF** | LOF | 密度基流式方法 |
| **CPOD** | Distance-based | 距离基流式方法 |
| **Memstream** | Autoencoder + kNN | 深度学习流式方法（唯一使用 GPU） |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（见 Table 3）

| 方法 | 平均 AUC | 平均运行时间（秒） |
|------|----------|------------------|
| **IDK-S** | **0.876** ✅ | **27** ✅ |
| SiForest | 0.853 | 432 |
| oIFOR | 0.838 | 218 |
| DILOF | 0.730 | 781 |
| CPOD | 0.727 | 1916 |
| Memstream | 0.841 | 423 |

> ✅ 表示最优值

#### 性能提升总结：
- **AUC 提升**：相比次优方法 SiForest，IDK-S 提高 **2.2%**；
- **速度优势**：
  - 比 SiForest 快 **16倍**；
  - 比 oIFOR 快 **8倍**；
  - 比 CPOD 快 **71倍**；
- 在多个大数据集（如 `Donors`, `Fraud`, `INSECTS`）上提速达 **5x~10x以上**；
- 在小数据集（如 `Satellite`, `Annthyroid`）甚至实现 **超过一个数量级的速度提升**。

---

### 动态适应性表现（Figure 4）
- 在 `INSECTS`（突变漂移）上：
  - 所有方法在漂移点附近出现 AUC 下降；
  - **IDK-S 恢复最快**，表现出良好鲁棒性。
- 在 `TwoCluster`（渐进漂移）上：
  - IDK-S 和 oIFOR 表现稳定；
  - **SiForest 出现明显性能退化**，因其 reservoir sampling 更新机制导致旧数据滞留过久。

---

### 参数敏感性分析（Figure 5）
- IDK-S 最优参数 $\nu$（每轮采样数）集中在 **[2, 8]**；
- 对比：
  - oIFOR：最优 `max_leaf_samples` ∈ [32, 128]
  - SiForest：最优 `reservoir_samples` ∈ [256, 512]
- 结论：**IDK-S 更加样本高效**，少量样本即可捕获数据分布，是其高效性的关键原因之一。

---

### 消融实验结果（Table 4）——IDK 变体对比

| 方法 | 类型 | 平均 AUC | 平均时间（秒） |
|------|------|----------|----------------|
| **IDK-S** | Online | 0.877 | 14 |
| Retraining-based IDK | Online | 0.879 | 352 |
| IDK（offline） | Offline | 0.898 | 8 |

> 注：此处 IDK-S 时间更低是因为未计入初始化成本，体现长期运行优势。

#### 发现：
- IDK-S 与 retraining-based IDK 的 AUC 差距极小（仅差 0.002）；
- 但运行时间减少 **超过 20倍**；
- 验证了增量更新机制在不损失精度的前提下极大提升了效率。

---

## 4. 关键结论和发现

### 主要发现
1. **性能天花板源于基础模型**：
   - 当前流式方法性能受限于其底层离线模型（如 iForest、LOF）；
   - 若想突破性能瓶颈，必须采用更强的基础检测器 —— IDK 正是这样一个更优选择。

2. **IDK-S 实现精度与效率双赢**：
   - 利用 IDK 的数据依赖核特性，获得更高检测精度；
   - 设计增量更新机制，在保持统计等价性的前提下大幅降低计算负担。

3. **IDK-S 是目前最高效的流式异常检测器之一**：
   - 在 13 个基准任务中，综合性能领先；
   - 特别适合高吞吐、低延迟的真实工业场景应用。

---

### 方法的局限性
- **对突变型概念漂移仍有一定滞后**：
  - 虽然恢复较快，但在 `INSECTS` 上仍有短暂性能下降；
  - 当前机制依赖滑动窗口逐步替换，缺乏主动漂移检测与快速重置机制。
- **参数选择影响性能**：
  - 尽管 $\nu$ 较小且易调，但仍需通过交叉验证确定最优值；
  - 自动化超参优化尚未探索。

---

### 未来工作方向
- 探索增强对**突发式概念漂移**的鲁棒性；
- 引入**在线漂移检测模块**，触发更激进的模型调整；
- 扩展至**多模态或图数据流**上的异常检测；
- 结合轻量化深度表示学习，进一步提升高维稀疏数据的表现。

---

## 总结
**IDK-S** 是一项将强大离线检测器 IDK 成功迁移到流式场景的重要工作。它不仅提出了一个高效、可证明等价于全量重训练的增量框架，而且在实验中展现出**前所未有的精度-效率平衡**，为下一代高性能流式异常检测系统提供了新的范式。

</details>

---

### 14. [MCP-AI: Protocol-Driven Intelligence Framework for Autonomous Reasoning in Healthcare](https://arxiv.org/abs/2512.05365)

**Authors**: Zag ElSayed, Craig Erickson, Ernest Pedapati  
**Category**: cs.AI  
**Published**: 2025-12-08  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2512.05365v1  

#### Abstract
Healthcare AI systems have historically faced challenges in merging contextual reasoning, long-term state management, and human-verifiable workflows into a cohesive framework. This paper introduces a completely innovative architecture and concept: combining the Model Context Protocol (MCP) with a sp...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：MCP-AI: Protocol-Driven Intelligence Framework for Autonomous Reasoning in Healthcare

---

## 1. 论文的主要贡献和创新点

### 解决了什么问题
当前医疗AI系统面临三大核心挑战：
- **缺乏上下文推理能力**：传统CDSS依赖静态规则，而LLMs虽能生成合理叙述，但无状态记忆、任务逻辑和持续推理能力。
- **纵向管理缺失**：难以在长期诊疗流程中维持患者状态和决策连续性。
- **人机协作不透明**：缺乏可审计、可追溯的决策路径，限制临床信任与监管合规。

这些问题导致AI在高风险医疗场景中的应用受限，尤其是在慢性病管理、罕见病诊断和跨学科协作中。

### 提出了什么新方法或新思路
本文提出 **MCP-AI** —— 一种基于 **Model Context Protocol (MCP)** 的新型架构，用于实现自主、可解释、协议驱动的临床推理框架。

#### 核心创新点：
- **MCP 文件作为“认知内存”对象**：每个MCP文件封装了患者的临床目标、上下文、推理状态和任务逻辑，形成一个**可重用、可审计、版本控制的记忆单元**。
- **双AI模块协同机制**：
  - **Generative AI Modules**（如LLMs）负责生成诊断假设、护理计划等叙事输出；
  - **Descriptive AI Modules**（如规则引擎、知识图谱）进行指南验证、风险评分和安全性检查。
- **五层模块化架构**：支持从数据输入到人类验证的全流程闭环，包括感知层、MCP引擎、推理模块、任务代理和医生接口。
- **医师在环（Physician-in-the-loop）设计**：确保所有AI建议均需经医生审查、修改或批准，保障临床主导权。

### 相比现有方法的优势
| 维度 | 传统CDSS / Prompt-based LLMs | MCP-AI |
|------|-------------------------------|--------|
| 推理模式 | 静态、一次性响应 | 动态、持续、纵向推理 |
| 上下文保持 | 无持久状态 | MCP文件提供完整历史记录 |
| 可解释性 | 黑箱或简单规则追溯 | 全链路元数据日志（信心值、依据、模块来源） |
| 协作能力 | 孤立系统 | 支持多智能体协作与跨科室交接（Handoff） |
| 合规性 | 难以满足HIPAA/FDA SaMD要求 | 内建审计日志、版本控制、安全约束 |

> ✅ **本质转变**：从“被动决策支持”转向“主动认知协作者”。

---

## 2. 核心实验方法和设置

### 使用了哪些数据集
论文未使用公开标准数据集，而是构建了两个**模拟临床案例**来验证MCP-AI的有效性：

1. **Fragile X Syndrome with Comorbid Depression**
   - 虚构13岁患者，含行为障碍、发育迟缓、学业困难
   - 输入数据：家长访谈、行为观察笔记、EEG波形、教育记录、门诊电子健康档案（EHR）

2. **Type 2 Diabetes and Hypertension 远程协调管理**
   - 虚构58岁患者，血糖波动、用药依从性差
   - 输入数据：可穿戴设备数据、血糖仪日志、HbA1c趋势、社会决定因素（住房不稳定、食物获取困难）

所有数据通过HL7/FHIR接口集成至MCP-AI系统。

### 实验设置和评估指标
#### 实验形式
- **仿真测试（Simulation-based Evaluation）**
- 在受控环境中运行MCP-AI处理上述两个病例，记录其推理过程、任务执行与输出质量

#### 评估维度
| 维度 | 指标说明 |
|------|----------|
| **临床准确性** | 是否符合DSM-V、ICD-10、ADA指南等标准 |
| **可解释性与可追溯性** | MCP文件是否完整记录每一步推理逻辑、置信度、数据源、模块调用 |
| **工作流整合能力** | 是否能自动生成检验申请、随访提醒、转诊建议，并与EHR系统对接 |
| **跨提供者交接能力** | Handoff Agent能否生成结构化摘要，支持无缝过渡 |
| **监管合规性** | 是否满足HIPAA隐私要求、FDA SaMD原则（如变更追踪、版本控制） |

### 基线方法对比
虽然没有直接数值对比实验，但文中明确将MCP-AI与以下基线系统进行定性比较：
- **传统CDSS**（如基于SNOMED CT/ICD-10的规则系统）
- **Prompt-based LLMs**（如GPT-4, Med-PaLM）
- **孤立AI管道（Siloed AI Pipelines）**

强调这些方法在**状态持久性、任务编排、协作透明度**方面的根本缺陷。

---

## 3. 主要实验结果和性能指标

### 关键性能数据（基于案例分析）
#### ✅ Fragile X Syndrome 案例
- 成功识别出**Fragile X综合征+抑郁共病**的可能性
- 自动生成：
  - FMR1基因检测请求
  - 向临床遗传学家自动转诊
  - 行为健康综合评估安排
- Descriptive AI模块成功验证诊断链完整性，标记潜在遗漏项（如家族史缺失）
- MCP文件记录了完整的推理轨迹，包括：
  - 初始假设生成时间戳
  - 各AI模块贡献标识
  - 置信度评分（如Depression可能性：78%）
  - 医生审批后触发后续任务钩子（three-week follow-up EEG）

#### ✅ Diabetes & Hypertension 案例
- 基于传感器和社会决定因素动态调整护理计划
- 输出建议包括：
  - 饮食咨询介入
  - 二甲双胍剂量滴定
  - 考虑引入SGLT2抑制剂（经肾功能排除禁忌）
- Descriptive模块计算患者依从概率（Patient Engagement Score: 62%）
- Handoff Agent生成嵌入式MCP摘要，包含：
  - 最近调整的理由
  - 缺失实验室警报
  - 下一步随访任务清单
- 系统预测未来血糖/血压轨迹，供接班医生参考调整

### 与基线方法的对比结果
| 能力 | Prompt-based LLM | 传统CDSS | MCP-AI |
|------|------------------|-----------|---------|
| 多模态数据融合 | 有限 | 弱 | ✅ 强（EEG + EHR + 社会因素） |
| 长期状态维护 | ❌ 无记忆 | ❌ 固定规则 | ✅ MCP文件持续更新 |
| 跨专业交接 | ❌ 不支持 | ❌ 断裂 | ✅ 结构化推理摘要 |
| 安全验证 | ❌ 无 | ✅ 规则校验 | ✅ 双重验证（生成+描述） |
| 可审计性 | ❌ 黑箱 | ✅ 日志 | ✅ 全链路元数据记录 |

> ⚠️ 注：由于是概念验证研究，尚未报告量化准确率、敏感性/特异性等统计指标。

### 消融实验结果（如有）
论文未开展正式消融实验（ablation study），但通过架构设计说明各组件必要性：
- 若移除Descriptive AI → 失去安全性验证能力
- 若无MCP Engine → 无法协调多Agent协作
- 若缺少Verification Module → 违反“Physician-in-the-loop”原则

---

## 4. 关键结论和发现

### 论文的主要发现
1. **MCP-AI实现了真正的“自主临床推理”**  
   不仅做出推荐，还能保留推理路径、适应环境变化、跨时间与空间延续决策逻辑。

2. **MCP文件是医疗AI的认知基础设施**  
   将AI决策转化为**机器可读+人类可审**的协议对象，解决了当前AI“不可控、不可信、不可管”的痛点。

3. **支持复杂疾病的端到端管理**  
   在罕见病（Fragile X）和慢性病（Diabetes/Hypertension）中均展示出强大的情境理解与任务自动化能力。

4. **天然契合医疗监管要求**  
   支持HL7/FHIR集成、HIPAA合规、FDA SaMD规范，具备临床部署潜力。

5. **推动AI角色从“工具”向“协作者”演进**  
   医生不再是被动接收建议，而是参与、修正、监督整个AI推理过程。

### 方法的局限性
- **尚处仿真阶段**：未在真实临床环境中进行前瞻性试验。
- **依赖高质量结构化输入**：对非结构化文本的理解仍依赖LLM性能。
- **MCP文件复杂性增加运维成本**：需要专门平台管理和解析。
- **尚未验证泛化能力**：仅展示两个案例，需更多疾病领域验证。

### 未来工作方向
作者明确提出以下发展方向：
1. **集成实时生物信号反馈机制**（如EEG、ECG）以增强动态监测能力
2. **扩展任务代理库**：加入康复、预防保健等新模块
3. **发展分布式学习机制**：在保护隐私的前提下实现模型联合更新
4. **启动前瞻性临床试验**：与现有AI/ML诊断工具进行头对头比较
5. **打造标准化MCP扩展生态**：支持影像AI、基因组解读器等第三方接入

---

## 总结

📌 **MCP-AI不是另一个AI诊断模型，而是一个全新的医疗AI操作系统范式**。它通过**MCP协议**将生成式AI与描述性AI统一在一个可审计、可持续、可协作的框架下，真正迈向“可信、可控、可持续”的临床智能时代。该工作为下一代**Explainable Medical AI**奠定了架构基础，具有深远的学术与产业价值。

</details>

---

### 15. [MIND: Multi-rationale INtegrated Discriminative Reasoning Framework for Multi-modal Large Models](https://arxiv.org/abs/2512.05530)

**Authors**: Chuang Yu, Jinmiao Zhao, Mingxuan Zhao, Yunpeng Liu, Xiujun Shu, Yuanhao Feng, Bo Wang, Xiangyu Yue  
**Category**: cs.AI  
**Published**: 2025-12-08  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2512.05530v1  

#### Abstract
Recently, multimodal large language models (MLLMs) have been widely applied to reasoning tasks. However, they suffer from limited multi-rationale semantic modeling, insufficient logical robustness, and are susceptible to misleading interpretations in complex scenarios. Therefore, we propose a Multi-...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：MIND: Multi-rationale INtegrated Discriminative Reasoning Framework for Multi-modal Large Models

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前的 **Multimodal Large Language Models (MLLMs)** 在推理任务中存在以下关键缺陷：
- **单理由监督（Single-rationale supervision）** 导致模型仅学习到表面映射关系，无法捕捉人类推理的多样性与复杂性。
- 缺乏对错误逻辑的识别与自我修正能力，在面对误导性或模糊信息时表现出脆弱的逻辑鲁棒性。
- 推理过程为“被动模仿”，缺乏主动的逻辑判别与反思机制。

### 提出的新方法与创新思路
作者提出了首个 **Multi-rationale INtegrated Discriminative (MIND)** 推理框架，旨在赋予 MLLMs 类似人类的“**Understand → Rethink → Correct**”认知能力，实现从**被动模仿推理**向**主动判别式推理**的范式演进。

#### 核心组件：
1. **Rationale Augmentation and Discrimination (RAD) 范式**
   - 自动高效地生成多样化的**正向理由（positive rationales）** 和语义反转的**负向理由（negative rationales）**。
   - 构建统一且可扩展的多理由训练数据基础，显式建模正确推理与潜在推理陷阱。

2. **Progressive Two-stage Correction Learning (P2CL) 策略**
   - **阶段 I (P2CL-I)**：多理由正向学习，增强语义理解与逻辑建模。
   - **阶段 II (P2CL-II)**：主动逻辑判别与修正，利用正/负理由对进行联合生成训练，提升模型纠错能力。

3. **Multi-rationale Contrastive Alignment (MCA) 优化策略**
   - 在嵌入空间中通过对比学习，聚合正确的硬正例理由，分离错误的硬负例理由。
   - 缓解多理由语义空间中的表示纠缠，增强逻辑一致性与判别敏感性。

### 相比现有方法的优势
- **更丰富的语义建模**：通过多理由训练，捕捉人类推理的多样性。
- **更强的逻辑鲁棒性**：引入负理由暴露推理陷阱，提升模型抗干扰能力。
- **具备自省与纠错能力**：P2CL 实现了“先理解、再反思、后纠正”的闭环推理机制。
- **端到端可训练**：MCA 与 P2CL 协同优化，提升整体推理质量。

---

## 2. 核心实验方法和设置

### 使用的数据集
在三个涵盖不同领域的公开 VQA 数据集上进行了系统实验：
- **ScienceQA**：科学领域问答，21,208 样本。
- **A-OKVQA**：常识推理问答，24,903 样本。
- **M³CoT**：融合科学、常识与数学的多步推理基准，11,328 样本。

基于 RAD 范式构建了扩展版本：
- **ScienceQA-RAD**（×1000）
- **A-OKVQA-RAD**（×1000）
- **M³CoT-RAD**（×500）

### 实验设置
- **模型架构**：基于 T5 的 encoder-decoder 架构。
- **模型规模**：Base (223M) 和 Large (738M)。
- **视觉编码器**：冻结的 BLIP2-flan-t5-xxl。
- **图像描述生成**：Qwen2.5-VL-72B。
- **训练参数**：学习率 8e-5，batch size 8，最大序列长度 512。
- **训练轮数**：ScienceQA-RAD 200 轮，A-OKVQA-RAD 和 M³CoT 400 轮。
- **硬件**：8×NVIDIA H20 96GB GPU。

### 评估指标
- 主要指标：**Accuracy (%)**
- 对比方法包括：
  - 零样本方法（如 GPT-3.5, GPT-4, Gemini）
  - 微调方法（如 LLaVA, LaVIN）
  - 多模态 CoT 方法（如 Multimodal-CoT, MC-CoT, DPMM-CoT, T-SciQ）

---

## 3. 主要实验结果和性能指标

### 关键性能数据（SOTA 表现）

| 数据集       | 方法               | 准确率 (%) |
|--------------|--------------------|------------|
| **ScienceQA** | MIND-base (Ours)   | **92.29**  |
|              | MIND-large (Ours)  | **未报告具体值，优于所有基线** |
| **A-OKVQA**   | MIND-base (Ours)   | **70.6**   |
| **M³CoT**     | MIND-base (Ours)   | **57.38**  |
|              | MIND-large (Ours)  | **61.56**  |

### 与基线方法的对比结果
- **vs. Multimodal-CoT**：
  - ScienceQA 上提升 **6.98%**（85.31% → 92.29%）
  - A-OKVQA 上提升 **20.0%**（50.6% → 70.6%）
  - M³CoT 上提升 **12.53%**（44.85% → 57.38%）

- **vs. 其他 SOTA 方法**：
  - 在 ScienceQA 上超越 GPT-4 (83.99%) 和 Gemini (91.75%)。
  - 在 A-OKVQA 上显著优于 IPVR、GPV-2 等方法。
  - 在 M³CoT 上优于零样本大模型（如 GPT-4V 达 56.95%，MIND-large 达 61.56%）。

### 消融实验结果（Ablation Studies）

| 模型变体                     | ScienceQA 准确率 (%) | 提升 (%) |
|------------------------------|------------------------|----------|
| Baseline                     | 90.29                  | —        |
| + MCA only                   | 90.36                  | +0.07    |
| + P2CL only                  | 92.15                  | +1.86    |
| **Full MIND (P2CL + MCA)**   | **92.29**              | **+2.00**|

> ✅ 结论：P2CL 是性能提升的主因，MCA 提供额外增益，二者协同产生“1+1 > 2”的效果。

其他消融分析还验证了：
- 更高质量的生成模型（如 Qwen3-235B）生成的理由能带来更高性能。
- 理由数量增加（至 ×1000）带来持续但趋于饱和的性能提升。
- 更强的视觉编码器（如 BLIP2-flan-t5-xxl）和图像描述模型（如 Qwen2.5-VL-72B）有助于提升推理一致性。

---

## 4. 关键结论和发现

### 主要发现
1. **多理由训练是提升推理鲁棒性的关键**：通过 RAD 生成多样化正/负理由，使模型学会区分正确与错误逻辑路径。
2. **“理解→反思→纠正”范式有效**：P2CL 策略成功模拟人类认知循环，显著增强了模型的逻辑判别与自我修正能力。
3. **对比对齐优化（MCA）提升语义边界清晰度**：在嵌入空间中拉近正确理由、推开错误理由，强化了模型的判别敏感性。
4. **MIND 实现了跨领域泛化优势**：在科学、常识、数学等不同场景下均达到 SOTA，展现出强大的通用推理能力。

### 方法的局限性
- **依赖外部大模型生成理由**：RAD 范式依赖于现有强大 LLM（如 Qwen、DeepSeek）生成高质量理由，可能引入偏差。
- **计算成本较高**：尽管采用批量生成，大规模多理由数据构建仍需较高算力。
- **负理由构造依赖提示工程**：负理由的质量受 Negative Prompt 设计影响较大，自动化程度有待提高。

### 未来工作方向
- 探索更高效的多理由生成机制，减少对外部模型的依赖。
- 将 MIND 框架应用于更多模态（如音频、视频长序列）和任务（如决策规划、对话系统）。
- 研究如何将多理由判别能力迁移到低资源或少样本场景。
- 进一步探索人类认知机制（如元认知、注意力调控）在 MLLMs 中的建模。

---

> **总结**：  
> MIND 提出了一种全新的多理由集成判别式推理范式，通过 **RAD + P2CL + MCA** 三重机制，显著提升了 MLLMs 的逻辑鲁棒性、解释性与自纠错能力，在多个基准上取得 SOTA 成绩。该工作为推动 MLLMs 从“模仿”走向“真正认知智能”提供了重要思路。

</details>

---

### 16. [Transformer-Enabled Diachronic Analysis of Vedic Sanskrit: Neural Methods for Quantifying Types of Language Change](https://arxiv.org/abs/2512.05364)

**Authors**: Ananth Hariharan, David Mortensen  
**Category**: cs.CL  
**Published**: 2025-12-08  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2512.05364v1  

#### Abstract
This study demonstrates how hybrid neural-symbolic methods can yield significant new insights into the evolution of a morphologically rich, low-resource language. We challenge the naive assumption that linguistic change is simplification by quantitatively analyzing over 2,000 years of Sanskrit, demo...

---

### 17. [Structured Reasoning with Tree-of-Thoughts for Bengali Math Word Problems](https://arxiv.org/abs/2512.05580)

**Authors**: Aurprita Mahmood, Sabrin alam, Neloy kumer Sagor, Md. Abdul Hadi, Md. Sehab Al Islam, Minhajul Islam  
**Category**: cs.CL  
**Published**: 2025-12-08  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2512.05580v1  

#### Abstract
Mathematical Word Problems (MWPs) are among the most challenging tasks in natural language processing because they require both linguistic understanding and multi-step numerical reasoning. While Chain-of-Thought (CoT) prompting has shown promise, its linear structure often propagates errors, limitin...

---

### 18. [Bridging Interpretability and Optimization: Provably Attribution-Weighted Actor-Critic in Reproducing Kernel Hilbert Spaces](https://arxiv.org/abs/2512.05291)

**Authors**: Na Li, Hangguan Shan, Wei Ni, Wenjie Zhang, Xinyu Li  
**Category**: cs.LG  
**Published**: 2025-12-08  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2512.05291v1  

#### Abstract
Actor-critic (AC) methods are a cornerstone of reinforcement learning (RL) but offer limited interpretability. Current explainable RL methods seldom use state attributions to assist training. Rather, they treat all state features equally, thereby neglecting the heterogeneous impacts of individual st...

---

### 19. [Taxonomy-Adaptive Moderation Model with Robust Guardrails for Large Language Models](https://arxiv.org/abs/2512.05339)

**Authors**: Mahesh Kumar Nandwana, Youngwan Lim, Joseph Liu, Alex Yang, Varun Notibala, Nishchaie Khanna  
**Category**: cs.LG  
**Published**: 2025-12-08  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2512.05339v1  

#### Abstract
Large Language Models (LLMs) are typically aligned for safety during the post-training phase; however, they may still generate inappropriate outputs that could potentially pose risks to users. This challenge underscores the need for robust safeguards that operate across both model inputs and outputs...

---

### 20. [Bounded Graph Clustering with Graph Neural Networks](https://arxiv.org/abs/2512.05623)

**Authors**: Kibidi Neocosmos, Diego Baptista, Nicole Ludwig  
**Category**: cs.LG  
**Published**: 2025-12-08  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2512.05623v1  

#### Abstract
In community detection, many methods require the user to specify the number of clusters in advance since an exhaustive search over all possible values is computationally infeasible. While some classical algorithms can infer this number directly from the data, this is typically not the case for graph...

---

### 21. [Semantic Faithfulness and Entropy Production Measures to Tame Your LLM Demons and Manage Hallucinations](https://arxiv.org/abs/2512.05156)

**Authors**: Igor Halperin  
**Category**: cs.AI  
**Published**: 2025-12-08  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.05156v1  

#### Abstract
Evaluating faithfulness of Large Language Models (LLMs) to a given task is a complex challenge. We propose two new unsupervised metrics for faithfulness evaluation using insights from information theory and thermodynamics. Our approach treats an LLM as a bipartite information engine where hidden lay...

---

### 22. [Evolutionary System 2 Reasoning: An Empirical Proof](https://arxiv.org/abs/2512.05760)

**Authors**: Zeyuan Ma, Wenqi Huang, Guo-Huan Song, Hongshu Guo, Sijie Ma, Zhiguang Cao, Yue-Jiao Gong  
**Category**: cs.AI  
**Published**: 2025-12-08  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.05760v1  

#### Abstract
Machine intelligence marks the ultimate dream of making machines' intelligence comparable to human beings. While recent progress in Large Language Models (LLMs) show substantial specific skills for a wide array of downstream tasks, they more or less fall shorts in general intelligence. Following cor...

---

### 23. [Using Large Language Models to Create Personalized Networks From Therapy Sessions](https://arxiv.org/abs/2512.05836)

**Authors**: Clarissa W. Ong, Hiba Arnaout, Kate Sheehan, Estella Fox, Eugen Owtscharow, Iryna Gurevych  
**Category**: cs.AI  
**Published**: 2025-12-08  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.05836v1  

#### Abstract
Recent advances in psychotherapy have focused on treatment personalization, such as by selecting treatment modules based on personalized networks. However, estimating personalized networks typically requires intensive longitudinal data, which is not always feasible. A solution to facilitate scalabil...

---

### 24. [Exposing Pink Slime Journalism: Linguistic Signatures and Robust Detection Against LLM-Generated Threats](https://arxiv.org/abs/2512.05331)

**Authors**: Sadat Shahriar, Navid Ayoobi, Arjun Mukherjee, Mostafa Musharrat, Sai Vishnu Vamsi  
**Category**: cs.CL  
**Published**: 2025-12-08  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.05331v1  

#### Abstract
The local news landscape, a vital source of reliable information for 28 million Americans, faces a growing threat from Pink Slime Journalism, a low-quality, auto-generated articles that mimic legitimate local reporting. Detecting these deceptive articles requires a fine-grained analysis of their lin...

---

### 25. [GRASP: Graph Reasoning Agents for Systems Pharmacology with Human-in-the-Loop](https://arxiv.org/abs/2512.05502)

**Authors**: Omid Bazgir, Vineeth Manthapuri, Ilia Rattsev, Mohammad Jafarnejad  
**Category**: cs.LG  
**Published**: 2025-12-08  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.05502v1  

#### Abstract
Quantitative Systems Pharmacology (QSP) modeling is essential for drug development but it requires significant time investment that limits the throughput of domain experts. We present \textbf{GRASP} -- a multi-agent, graph-reasoning framework with a human-in-the-loop conversational interface -- that...

---

### 26. [BERTO: an Adaptive BERT-based Network Time Series Predictor with Operator Preferences in Natural Language](https://arxiv.org/abs/2512.05721)

**Authors**: Nitin Priyadarshini Shankar, Vaibhav Singh, Sheetal Kalyani, Christian Maciocco  
**Category**: cs.LG  
**Published**: 2025-12-08  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.05721v1  

#### Abstract
We introduce BERTO, a BERT-based framework for traffic prediction and energy optimization in cellular networks. Built on transformer architectures, BERTO delivers high prediction accuracy, while its Balancing Loss Function and prompt-based customization allow operators to adjust the trade-off betwee...

---

### 27. [Towards agent-based-model informed neural networks](https://arxiv.org/abs/2512.05764)

**Authors**: Nino Antulov-Fantulin  
**Category**: cs.LG  
**Published**: 2025-12-08  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.05764v1  

#### Abstract
In this article, we present a framework for designing neural networks that remain consistent with the underlying principles of agent-based models. We begin by highlighting the limitations of standard neural differential equations in modeling complex systems, where physical invariants (like energy) a...

---

### 28. [KQ-SVD: Compressing the KV Cache with Provable Guarantees on Attention Fidelity](https://arxiv.org/abs/2512.05916)

**Authors**: Damien Lesens, Beheshteh T. Rakhshan, Guillaume Rabusseau  
**Category**: cs.LG  
**Published**: 2025-12-08  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.05916v1  

#### Abstract
The Key-Value (KV) cache is central to the efficiency of transformer-based large language models (LLMs), storing previously computed vectors to accelerate inference. Yet, as sequence length and batch size grow, the cache becomes a major memory bottleneck. Prior compression methods typically apply lo...

---

### 29. [A Greek Government Decisions Dataset for Public-Sector Analysis and Insight](https://arxiv.org/abs/2512.05647)

**Authors**: Giorgos Antoniou, Giorgos Filandrianos, Aggelos Vlachos, Giorgos Stamou, Lampros Kollimenos, Konstantinos Skianis, Michalis Vazirgiannis  
**Category**: cs.CL  
**Published**: 2025-12-08  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2512.05647v1  

#### Abstract
We introduce an open, machine-readable corpus of Greek government decisions sourced from the national transparency platform Diavgeia. The resource comprises 1 million decisions, featuring and high-quality raw text extracted from PDFs. It is released with raw extracted text in Markdown format, alongs...

---

### 30. [MedTutor-R1: Socratic Personalized Medical Teaching with Multi-Agent Simulation](https://arxiv.org/abs/2512.05671)

**Authors**: Zhitao He, Haolin Yang, Zeyu Qin, Yi R Fung  
**Category**: cs.CL  
**Published**: 2025-12-08  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2512.05671v1  

#### Abstract
The significant gap between rising demands for clinical training and the scarcity of expert instruction poses a major challenge to medical education. With powerful capabilities in personalized guidance, Large Language Models (LLMs) offer a promising solution to bridge this gap. However, current rese...

---

## 🔧 Configuration

This bot is configured to look for papers containing the following keywords:
- framework, System, Generation, Video, Linear, LLM, RL, RLHF, Inference, Training, Attention, Pipeline, MOE, Sparse, Quantization, Speculative, Efficient, Efficiency, Framework, Parallel, Distributed, Kernel, Decode, Decoding, Prefill, Throughput, Fast, Network, Hardware, Cluster, FP8, FP4, Optimization, Scalable, Communication

## 📅 Schedule

The bot runs daily at 12:00 UTC via GitHub Actions to fetch the latest papers.

## 🚀 How to Use

1. **Fork this repository** to your GitHub account
2. **Customize the configuration** by editing `config.json`:
   - Add/remove arXiv categories (e.g., `cs.AI`, `cs.LG`, `cs.CL`)
   - Modify keywords to match your research interests
   - Adjust `max_papers` and `days_back` settings
3. **Enable GitHub Actions** in your repository settings
4. **The bot will automatically run daily** and update the README.md

## 📝 Customization

### arXiv Categories
Common categories include:
- `cs.AI` - Artificial Intelligence
- `cs.LG` - Machine Learning
- `cs.CL` - Computation and Language
- `cs.CV` - Computer Vision
- `cs.NE` - Neural and Evolutionary Computing
- `stat.ML` - Machine Learning (Statistics)

### Keywords
Add keywords that match your research interests. The bot will search for these terms in paper titles and abstracts.

### Exclude Keywords
Add terms to exclude certain types of papers (e.g., "survey", "review", "tutorial").

## 🔍 Manual Trigger

You can manually trigger the bot by:
1. Going to the "Actions" tab in your repository
2. Selecting "arXiv Bot Daily Update"
3. Clicking "Run workflow"

---
*Generated automatically by arXiv Bot* 
