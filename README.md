# arXiv Papers Bot 🤖

This repository automatically fetches and displays relevant papers from arXiv based on configured criteria.

## RSS Vercel Deployment [![An example of deployed RSS Server using vercel](https://img.shields.io/badge/Deployed-Example-blue)](https://arxiv.tachicoma.top/)

You can click this to deploy yours 

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/maydomine/arxiv_rss_bot)
## 📊 Statistics

- **Last Updated**: 2025-12-11 05:56:48 UTC
- **Total Papers Found**: 30
- **Categories Monitored**: cs.AI, cs.CL, cs.DC, cs.LG

## 📚 Recent Papers

### 1. [Training-free Context-adaptive Attention for Efficient Long Context Modeling](https://arxiv.org/abs/2512.09238)

**Authors**: Zeng You, Yaofo Chen, Shuhai Zhang, Zhijie Qiu, Tingyu Wu, Yingjian Li, Yaowei Wang, Mingkui Tan  
**Category**: cs.CL  
**Published**: 2025-12-11  
**Score**: 10.5  
**Type**: new  
**ArXiv ID**: 2512.09238v1  

#### Abstract
Large Language Models (LLMs) have demonstrated remarkable capabilities across a wide range of natural language processing tasks. These capabilities stem primarily from the self-attention mechanism, which enables modeling of long-range dependencies. However, the quadratic complexity of self-attention...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Training-free Context-adaptive Attention for Efficient Long Context Modeling

---

## 1. 论文的主要贡献和创新点

### 解决的问题
Large Language Models (LLMs) 中的 **self-attention** 机制虽然能有效建模长距离依赖，但其计算复杂度为 $O(L^2)$（$L$ 为序列长度），在处理超长上下文（如 128K tokens）时面临严重的 **计算瓶颈** 和 **KV Cache 内存开销**。此外，大量冗余 token 导致注意力资源浪费，影响效率与精度。

现有方法存在以下局限：
- **静态稀疏注意力**（Static Sparse Attention）缺乏输入自适应能力；
- **仅加速 prefilling 阶段** 的方法无法缓解 decoding 阶段的 KV Cache 增长；
- **KV Cache 压缩技术** 不能减少 prefilling 计算量；
- 多数统一框架需要额外训练或架构修改。

### 提出的新方法：TCA-Attention
本文提出 **Training-free Context-adaptive Attention (TCA-Attention)**，一种无需训练、动态适应输入内容的稀疏注意力机制，用于高效长上下文建模。

#### 核心设计思想
TCA-Attention 包含两个轻量级阶段：
1. **Offline Sparsity Configuration（离线配置阶段）**
   - 在小规模校准数据集上通过一次前向传播，确定每个 attention head 的“稀疏预算”（sparsity budget）；
   - 利用 log-Gaussian 采样生成候选配置，并基于保留的 attention score 总量选择最优配置。

2. **Online Core Context Selection（在线核心上下文选择阶段）**
   - 推理时，根据预设的稀疏配置，动态选择对当前输出最相关的“核心 token”；
   - 引入轻量级冗余度量（redundancy metric），结合全局重要性评分与局部块排序，实现细粒度 token 保留。

最终 attention 输入由两部分组成：
- **Global Subset**：从全序列中按重要性选出的关键 token；
- **Local Subset**：最近 $w$ 个 token，保留局部上下文。

### 相比现有方法的优势
| 特性 | TCA-Attention | 其他方法（如 MInference, SnapKV, DuoAttention） |
|------|---------------|---------------------------------------------|
| **Training-free** ✅ | 是 | 多数需重新训练或调参 |
| **Dynamic & Input-adaptive** ✅ | 是 | 多为固定模式或有限动态 |
| **Accelerates both Prefilling & Decoding** ✅ | 是 | 多数只优化一个阶段 |
| **Reduces KV Cache** ✅ | 是 | 部分方法不压缩缓存 |
| **Head-aware Sparsity** ✅ | 是 | 多采用统一压缩策略 |
| **Plug-and-play** ✅ | 支持即插即用 | 常需系统级修改 |

> ✅ **TCA-Attention 是首个同时满足：无训练、动态适应、头感知、统一加速 prefilling 与 decoding 的稀疏注意力方案。**

---

## 2. 核心实验方法和设置

### 使用的数据集
| 数据集 | 类型 | 任务描述 |
|--------|------|----------|
| **LongBench-E** [37] | 长上下文理解 | 包含 14 项多语言、多任务评测，涵盖单文档问答、多文档问答、摘要、代码等 |
| **RULER** [38] | 合成基准 | 基于 “needle-in-a-haystack” 范式，测试模型在不同长度下的真实上下文利用能力 |
| **MMLU** [53] | 短上下文知识 | 多学科知识问答 |
| **GSM8K** [54] | 数学推理 | 小学数学应用题 |
| **HumanEval** [55] | 代码生成 | 函数补全任务 |
| **OlympiadBench** [56] | 科学推理 | 奥赛级别物理与数学难题 |
| **MT-Bench-101** [57] | 多轮对话 | 多轮交互中的连贯性与推理能力评估 |

### 实验设置与评估指标
- **模型**：
  - `LLaMA-3.1-8B-Instruct`
  - `Qwen2.5-7B-Instruct`
  - 均支持 128K 上下文窗口，使用 GQA（Grouped-Query Attention）
- **硬件**：8×NVIDIA A800 GPU（80GB VRAM），单卡测延迟
- **实现方式**：基于 Triton 实现，替换原生 self-attention 模块，无需架构改动
- **关键超参数**：
  - Block size $b = 128$
  - Local window size $w = 4096$
  - 离线阈值 $T = 0.9$
- **评估指标**：
  - **任务性能**：各 benchmark 的准确率 / 分数
  - **效率指标**：
    - Attention computation latency（prefilling 耗时）
    - Inter-token latency (ITL)（decoding 速度）
    - KV Cache 内存占用

### 对比的基线方法
| 方法 | 类型 | 是否训练 | 加速阶段 |
|------|------|---------|----------|
| **MInference** [20] | 动态稀疏（prefilling） | ❌ | 仅 prefilling |
| **FlexPrefill** [21] | 动态稀疏（prefilling） | ❌ | 仅 prefilling |
| **XAttention** [22] | 块稀疏 + 抗对角线评分 | ❌ | 仅 prefilling |
| **SnapKV** [23] | KV Cache 压缩 | ❌ | 仅 decoding |
| **CAKE** [24] | 层偏好式 KV 缓存淘汰 | ❌ | 仅 decoding |
| **DuoAttention** [25] | 统一稀疏（检索+流式头） | ✅ | prefilling & decoding |
| **Lserve** [26] | 统一稀疏服务框架 | ✅ | prefilling & decoding |

> 所有对比均在相同条件下进行，latency 基于 FlashAttention-2 测量。

---

## 3. 主要实验结果和性能指标

### 关键性能数据（128K context）

| 指标 | TCA-Attention 结果 | 提升幅度 |
|------|---------------------|----------|
| **Prefilling 加速比** | **2.8× speedup** | vs. Full Attention |
| **Decoding 速度提升** | **2.1× faster ITL** | 显著优于仅优化 prefilling 的方法 |
| **KV Cache 内存减少** | **61% reduction**<br>（8.00 GB → 3.12 GB） | 极大降低部署成本 |
| **LongBench-E 平均得分** | **53.51 (LLaMA), 51.51 (Qwen)** | 超过所有稀疏方法，接近 full attention |
| **RULER 平均得分** | **88.72 (LLaMA), 82.49 (Qwen)** | 在极端长度下仍保持高召回 |

### 与基线方法的对比结果

#### ✅ 在 LongBench-E 上的表现（Table I）
- **LLaMA-3.1-8B**：
  - TCA-Attention 达到 **53.51** 平均分，高于 XAttention（50.91）
  - 延迟从 316.14ms 降至 **120.96ms**（2.6× speedup）
- **Qwen2.5-7B**：
  - 平均分 **51.51**，为最高
  - 延迟从 268.55ms 降至 **105.93ms**（2.5× speedup）

#### ✅ 在 RULER 上的表现（Table II）
- 在 128K context 下：
  - TCA-Attention 实现 **2.8× speedup**
  - 性能稳定，平均分略优于 XAttention
- 表明其在极长序列中仍能精准定位关键信息

#### ✅ 多轮对话表现（MT-Bench-101）
- TCA-Attention 得分为 **8.97**，超过 full attention baseline（8.90）和 FlexPrefill（8.90）
- 说明其能有效保留对话历史中的关键上下文

#### ✅ 推理任务表现（OlympiadBench）
- 在 Physics 任务上取得 **最高分 10.59**
- 数学任务保持竞争力
- 表明其适用于复杂科学推理场景

### 消融实验结果

#### 🔹 不同 block size $b$ 的影响（Figure 4a）
- 最优 $b = 128$，过大或过小都会导致性能下降
- 过小：难以捕捉块内冗余；过大：可能过度压缩关键信息

#### 🔹 不同 local window size $w$ 的影响（Figure 4b）
- $w = 4096$ 时达到最佳性能（51.51）
- 更大的局部窗口有助于保留近期上下文，提升稳定性

#### 🔹 不同阈值 $T$ 的影响（Figure 4c）
- $T = 0.9$ 时性能最佳
- 低 $T$（高压缩率）对全局推理任务（如 Multi-Document QA）影响较大
- 局部任务（如 Summarization）对压缩更鲁棒

#### 🔹 平衡参数 $\alpha$ 的敏感性（Table VIII）
- 在 $\alpha \in [0.1, 0.9]$ 范围内性能稳定
- 最佳值为 $\alpha = 0.5$，极端值也仅造成 ≤0.38 分下降
- 表明方法对超参数不敏感，易于部署

#### 🔹 校准数据集的影响（Table IX）
| 数据集 | 领域 | LongBench-E 平均分 |
|--------|------|------------------|
| SlimPajama | 通用网页文本 | 51.54 ± 0.03 |
| GovReport | 政府文档 | 51.56 ± 0.04 |
| McEval | 编程代码 | 51.55 ± 0.02 |

- 性能在不同领域间高度一致
- 即使仅用一条样本校准，性能波动 ≤0.04
- 表明 head-specific 稀疏性具有强泛化能力

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **TCA-Attention 可在无需训练的前提下，统一加速 prefilling 与 decoding 阶段**，并显著减少 KV Cache。
2. ✅ **head-specific + context-adaptive 的稀疏策略** 比固定模式更有效，能精细控制冗余 token 的去除。
3. ✅ **理论误差界可控**：通过保留足够的 attention mass（≥T），可保证近似误差有上界（Theorem 1）。
4. ✅ **即插即用性强**：仅需一次离线校准，即可适配多种任务与模型，无需微调或架构变更。
5. ✅ **在 128K 上下文下实现 2.8× speedup 且性能不降**，是当前最实用的长上下文推理解决方案之一。

### 方法的局限性
- 当前方法依赖于最后一个 token 的 query 来打分，可能在某些非因果任务中受限；
- block-level 设计虽高效，但在极细粒度语义任务中可能存在信息丢失风险；
- 对 extremely long context（>1M）的扩展尚未验证。

### 未来工作方向
- 将 TCA-Attention 扩展至 Vision-Language Models 和 Multimodal 序列；
- 探索更高效的 redundancy metric，进一步降低 overhead；
- 结合 Retrieval-Augmented Generation，构建 hybrid 长上下文系统；
- 研究如何将该机制应用于 training 阶段以提升训练效率。

---

> 📌 **总结一句话**：  
> **TCA-Attention 是一种无需训练、头感知、上下文自适应的稀疏注意力机制，在 128K 长上下文下实现了 2.8× 加速和 61% KV Cache 压缩，同时保持与 full attention 相当的性能，是迈向高效 LLM 推理的重要一步。**

</details>

---

### 2. [HPM-KD: Hierarchical Progressive Multi-Teacher Framework for Knowledge Distillation and Efficient Model Compression](https://arxiv.org/abs/2512.09886)

**Authors**: Gustavo Coelho Haase, Paulo Henrique Dourado da Silva  
**Category**: cs.LG  
**Published**: 2025-12-11  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2512.09886v1  

#### Abstract
Knowledge Distillation (KD) has emerged as a promising technique for model compression but faces critical limitations: (1) sensitivity to hyperparameters requiring extensive manual tuning, (2) capacity gap when distilling from very large teachers to small students, (3) suboptimal coordination in mul...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# HPM-KD: Hierarchical Progressive Multi-Teacher Framework for Knowledge Distillation and Efficient Model Compression  
**论文核心结论与实验结果总结**

---

## 1. 论文的主要贡献和创新点

### 解决的问题
该论文针对传统 **Knowledge Distillation (KD)** 技术中存在的四大关键挑战提出系统性解决方案：

1. **Hyperparameter Sensitivity（超参数敏感）**：传统KD依赖大量手动调参（如温度 $T$、损失权重 $\alpha$），缺乏自适应能力。
2. **Capacity Gap（容量差距）**：当教师模型远大于学生模型时，单步蒸馏难以有效传递知识。
3. **Suboptimal Multi-Teacher Coordination（多教师协调不佳）**：现有方法对多个教师采用固定加权或平均策略，无法动态适应不同样本的需求。
4. **Inefficient Resource Utilization（资源利用低效）**：重复训练导致计算冗余，缺乏跨实验的知识复用机制。

---

### 提出的新方法：HPM-KD 框架

作者提出了 **HPM-KD**（Hierarchical Progressive Multi-Teacher Knowledge Distillation），一个集成六项协同组件的综合性框架：

| 组件 | 功能 |
|------|------|
| **Adaptive Configuration Manager (ACM)** | 基于 meta-learning 自动预测最优超参数配置（$T, \alpha$, 学习率等），消除人工调参 |
| **Progressive Distillation Chain (PDC)** | 构建自动化的中间模型链（teacher → intermediate → student），逐步缩小容量差距 |
| **Attention-Weighted Multi-Teacher Ensemble (AWMT)** | 引入注意力机制，为每个输入样本动态学习各教师模型的权重 |
| **Meta-Learned Temperature Scheduler (MTS)** | 动态调整蒸馏温度 $T$，早期平滑软标签，后期精细校准 |
| **Parallel Processing Pipeline (PPP)** | 并行化多教师训练与渐进链路，提升训练效率 |
| **Shared Optimization Memory (SOM)** | 缓存历史实验中的模型与配置，支持快速 warm-start 和避免重复计算 |

---

### 相比现有方法的优势

| 特性 | HPM-KD | 传统方法（如 KD, TAKD, FitNets） |
|------|--------|-------------------------------|
| 超参数自动化 | ✅ 支持（meta-learning） | ❌ 需手动调参或网格搜索 |
| 渐进式蒸馏 | ✅ 自动构建中间模型链 | ⚠️ TAKD 手动设计 Teaching Assistant |
| 多教师融合 | ✅ 注意力动态加权（per-sample） | ❌ 固定权重或均匀平均 |
| 温度调度 | ✅ 动态自适应调整 | ❌ 固定温度 |
| 并行与缓存 | ✅ 支持并行 + 跨实验缓存 | ❌ 无系统级优化 |
| 易用性 | ✅ 提供 scikit-learn 风格 API | ❌ 配置复杂 |

> 📌 **核心优势总结**：HPM-KD 是首个将 **自动配置、渐进蒸馏、动态多教师、自适应温度、并行加速、缓存复用** 六大功能整合于一体的模块化、生产就绪的蒸馏框架。

---

## 2. 核心实验方法和设置

### 使用的数据集

| 类型 | 数据集 | 描述 |
|------|-------|------|
| 图像分类 | **CIFAR-10**, **CIFAR-100** | 32×32 RGB 图像，分别含 10 和 100 类 |
| 表格数据 | **Adult**, **Credit**, **Wine Quality**（来自 UCI ML Repository） | 用于验证在非图像任务上的泛化能力 |

---

### 实验设置

#### 模型架构
- **CIFAR 任务**：
  - Teacher: ResNet-56（0.85M 参数）
  - Student: ResNet-20（0.27M 参数）
  - 压缩比（Compression Ratio, CR）≈ 3.1×
- **表格任务**：
  - Teacher: MLP [256,128,64]（0.5M 参数）
  - Student: MLP [64,32]（0.05M 参数）
  - CR = 10×

#### 基线方法对比
1. **Direct Training**：仅用真实标签训练学生模型（无蒸馏）
2. **Traditional KD** [Hinton et al., 2015]：经典蒸馏，固定温度
3. **FitNets** [Romero et al., 2015]：基于 hint 的中间层蒸馏
4. **Attention Transfer (AT)** [Zagoruyko & Komodakis, 2017]：迁移空间注意力图
5. **TAKD** [Mirzadeh et al., 2020]：引入 Teaching Assistant 的渐进蒸馏（需手动设计）

---

### 评估指标

| 指标 | 定义 |
|------|------|
| **Student Accuracy (Acc_s)** | 学生模型最终准确率 |
| **Retention Rate (%)** | $ \frac{Acc_s}{Acc_{teacher}} \times 100\% $，表示性能保留比例 |
| **Compression Ratio (CR)** | 参数量比 $ |\theta_t| / |\theta_s| $ |
| **Training Time** | 总墙钟时间（wall-clock time） |
| **Efficiency (acc/min)** | 准确率 / 训练分钟数，衡量性价比 |
| **Ablation Study** | 移除单一组件，观察性能下降（单位：pp，percentage points） |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（以 CIFAR-10 为例）

| 方法 | Acc (%) | Retention (%) | CR | 训练时间 (s) |
|------|---------|---------------|----|-------------|
| Teacher (ResNet-56) | 79.28 | 100.0 | 1.0× | — |
| **HPM-KD (Ours)** | **67.74 ± 0.50** | **85.4%** | 3.1× | 603.1 |
| Direct Training | 68.10 ± 0.48 | 85.9% | 3.1× | 593.7 |
| Traditional KD | 67.12 ± 0.82 | 84.7% | 3.1× | 578.0 |
| FitNets | 62.66 ± 0.76 | 79.0% | 3.1× | 592.4 |
| TAKD | 67.44 ± 0.40 | 85.1% | 3.1× | 655.5 |

> ✅ **HPM-KD 在准确性上优于 Traditional KD (+0.62 pp) 和 FitNets (+5.08 pp)，与 TAKD 相当但更稳定。**

---

### 更高压缩场景下的表现（摘要提及）

- 在 **10×–15× 压缩比** 下仍能保持 **85% 的性能保留率**
- 实现 **30–40% 的训练时间减少**（得益于 PPP 并行化和 SOM 缓存）
- 在 tabular 数据上同样有效，证明其跨模态通用性

---

### 消融实验结果（CIFAR-100，5次运行均值）

| 配置 | Acc (%) | Δ (pp) | Retention (%) |
|------|--------|--------|----------------|
| Full HPM-KD | 36.28 ± 0.52 | 0.00 | 100.0 |
| w/o MultiTeach | 35.30 ± 0.85 | -0.98 | 97.3 |
| w/o MetaTemp | 35.63 ± 0.41 | -0.65 | 98.2 |
| w/o Memory | 35.67 ± 0.51 | -0.61 | 98.3 |
| w/o Parallel | 35.67 ± 0.68 | -0.61 | 98.3 |
| w/o ProgChain | 36.06 ± 0.31 | -0.22 | 99.4 |
| w/o AdaptConf | 36.18 ± 0.38 | -0.10 | 99.7 |

> 🔍 **组件重要性排序**：
> 1. **Multi-Teacher Ensemble**（-0.98 pp）——最关键
> 2. **Meta-Temperature Scheduler**（-0.65 pp）
> 3. **Memory & Parallel**（各 -0.61 pp）
> 4. **ProgChain**（-0.22 pp）
> 5. **AdaptConf**（-0.10 pp，虽影响小但极大提升可用性）

---

## 4. 关键结论和发现

### 主要发现

1. ✅ **HPM-KD 实现高效压缩与高保真知识迁移**  
   在 3.1× 至 15× 压缩下保持 85%+ 性能保留，适用于边缘设备部署。

2. ✅ **所有六个组件均有独立正向贡献（0.10–0.98 pp）**  
   尤其是 **Attention-Weighted Multi-Teacher** 对性能提升最大。

3. ✅ **具备强鲁棒性**  
   - 对 **类别不平衡（up to 100:1）** 和 **标签噪声（up to 30%）** 表现出惊人鲁棒性
   - 教师模型的 soft targets 可作为隐式正则器，过滤噪声

4. ✅ **计算效率高**  
   - 并行化带来 **2.78× 加速（4 workers）**
   - 缓存命中率达 30–40%，显著节省重复训练开销
   - 相比 TAKD，额外开销 <1%

5. ⚠️ **蒸馏并非总是必要**  
   在 moderate compression ratio（如 3.1×）且学生有足够容量时，**Direct Training 可能优于蒸馏**（CIFAR-10 上 68.10% vs. 67.74%）

---

### 方法的局限性

| 局限 | 说明 |
|------|------|
| **验证范围有限** | 主要在 CIFAR 和小型表格数据上测试，尚未扩展至 ImageNet、NLP 或医疗影像 |
| **未覆盖超高压缩比** | 当前 CR 最高约 15×，而 TAKD 在 >10× 场景优势更明显，需进一步验证 ultra-compression（50–100×）效果 |
| **理论分析缺失** | 缺乏对收敛性、泛化界（generalization bound）或 PAC 框架的数学证明 |
| **冷启动问题** | ACM 依赖历史数据进行 meta-learning，在全新领域可能失效（需至少 5+ 实验才能达到 <5% 误差） |
| **相比传统KD略有时间开销** | 因多教师和渐进链，总训练时间增加 30–40%，需权衡精度增益与成本 |

---

### 未来工作方向

1. **扩展至 Large Language Models (LLMs)**：压缩 GPT 类千亿参数模型
2. **Cross-Modal Distillation**：从 CLIP、Flamingo 等多模态模型蒸馏到专用单模态模型
3. **Theoretical Analysis**：推导 progressive distillation 的泛化边界与 meta-temperature 收敛性
4. **Distillation-Aware Architecture Search (DAAS)**：联合优化网络结构与蒸馏策略
5. **Federated Knowledge Distillation**：在联邦学习中实现隐私保护下的分布式蒸馏
6. **Continuous Distillation**：支持教师持续更新时的学生增量学习（lifelong learning）

---

## 总结

📌 **HPM-KD 是一个面向生产的、高度自动化、可扩展的 Knowledge Distillation 框架**，通过六大协同组件解决了传统蒸馏中的关键瓶颈。它不仅提升了压缩效率和模型性能，还大幅降低了部署门槛。

🔗 开源地址：[https://github.com/DeepBridge-Validation/DeepBridge](https://github.com/DeepBridge-Validation/DeepBridge)  
📦 提供类 `scikit-learn` 的简洁 API，便于工业界快速集成。

> 💡 **实践建议**：对于 **高压缩比 (>10×)、多教师可用、存在噪声/不平衡、无法人工调参** 的场景，推荐使用 HPM-KD；而对于轻度压缩且学生容量充足的情况，可优先尝试 Direct Training。

</details>

---

### 3. [Semantic-Aware Cooperative Communication and Computation Framework in Vehicular Networks](https://arxiv.org/abs/2512.09621)

**Authors**: Jingbo Zhang, Maoxin Ji, Qiong Wu, Pingyi Fan, Kezhi Wang, Wen Chen  
**Category**: cs.LG  
**Published**: 2025-12-11  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2512.09621v1  

#### Abstract
Semantic Communication (SC) combined with Vehicular edge computing (VEC) provides an efficient edge task processing paradigm for Internet of Vehicles (IoV). Focusing on highway scenarios, this paper proposes a Tripartite Cooperative Semantic Communication (TCSC) framework, which enables Vehicle User...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Semantic-Aware Cooperative Communication and Computation Framework in Vehicular Networks

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
在高速移动的 **Internet of Vehicles (IoV)** 场景中，传统基于比特级通信（bit-level communication）的任务卸载机制存在以下瓶颈：
- 高延迟、高冗余，难以满足自动驾驶等任务对**低延迟**和**高可靠性**的需求；
- 现有研究大多仅考虑 **Vehicle-to-Infrastructure (V2I)** 单一链路，未能充分利用 **Vehicle-to-Vehicle (V2V)** 协同带来的资源灵活性。

本文聚焦于高速公路场景，解决如何通过语义感知（semantic-aware）协同通信与计算框架实现高效、低延迟的任务卸载问题。

---

### 🚀 提出的新方法与创新思路

1. **提出 Tripartite Cooperative Semantic Communication (TCSC) 框架**  
   - 首次构建了一个支持 **V2I 和 V2V 联合协作**的三元协同语义通信架构。
   - 支持车辆用户（VUs）、路侧单元（RSU）和服务车辆（SVs）之间的动态语义任务卸载。
   - 利用 **DeepSC** 实现文本语义编码与解码，提升传输效率。

2. **设计混合优化算法：MAPPO-PDN + LP**
   - 将原 **Mixed-Integer Nonlinear Programming (MINLP)** 问题分解为两个子问题：
     - **离散变量优化**：使用提出的 **MAPPO-PDN** 算法优化每词平均语义符号数 $k_{i,j}$；
     - **连续变量优化**：采用 **Linear Programming (LP)** 优化任务卸载比例 $p$。
   - 创新地引入 **Parametric Distribution Noise (PDN)** 到多智能体强化学习中，增强策略鲁棒性和泛化能力。

3. **MAPPO-PDN 的关键技术改进**
   - 将 Actor 网络参数建模为高斯分布 $q(\theta_k) = \mathcal{N}(\mu_k, \sigma_k)$，实现不确定性建模；
   - 引入 PDN 正则项约束参数扰动上界，防止过拟合，提升非平稳环境下的稳定性；
   - 设计基于最小延迟的奖励函数 $r = T_{\text{min}} / T_i$，引导快速收敛。

---

### 🔍 相比现有方法的优势

| 对比维度 | 本文方法（TCSC + MAPPO-PDN + LP） | 现有方法 |
|--------|-------------------------------|---------|
| 通信范式 | 语义通信（Semantic Communication） | 传统比特级通信 |
| 协同方式 | V2I + V2V 双路径协同 | 多数仅支持 V2I |
| 学习算法 | 带参数分布噪声的多智能体强化学习（MAPPO-PDN） | MAPPO、MADQN 等点估计方法 |
| 优化方式 | 分解为离散+连续联合优化 | 整体端到端训练复杂度高 |
| 性能表现 | 更低延迟、更强鲁棒性、更好收敛性 | 易过拟合、适应性差 |

---

## 2. 核心实验方法和设置

### 📊 实验环境与仿真平台
- **仿真工具**：Python 3.8 + PyTorch
- **场景设置**：400米长双车道高速公路，部署一个 RSU 和若干车辆
- **车辆配置**：
  - VUs 数量变化范围：20–40
  - SVs 固定数量：J = 5
  - 车速：50–80 km/h（恒定用于建模）
- **任务生成**：泊松分布，每时隙生成 0.8 Mbit 文本任务，需 1000 cycles/bit 计算资源

### ⚙️ 关键参数设置
| 参数 | 值 |
|------|----|
| 噪声功率 | -114 dBm |
| V2I 发射功率 | 23 dBm |
| V2V 发射功率 | 15 dBm |
| 带宽 B | 540 kHz |
| RSU 计算能力 $f_R$ | 6 GHz |
| VU 计算能力 $f_{loc}$ | 1 GHz |
| SV 计算能力 $f_Y$ | 3 GHz |
| 每句平均长度 $L_{i,j}$ | 20 words |
| 数据转句子因子 H | 1200 bit/sentence |
| 语义相似度阈值 $\delta_{th}$ | 0.9 |
| PDN 上限 $\Omega_{\text{max}}$ | 20 |

---

### 📈 评估指标
- **总任务延迟（Total Task Delay）**
- **传输延迟（Transmission Delay）**
- **训练奖励收敛曲线（Episode Reward）**
- **不同车辆密度下的系统性能**

---

### 🔀 基线方法对比
共比较 7 种方法：
1. **Our-Method + LP**（本文提出）
2. **Our-Method + Ivy**（替换 LP 为 Ivy 优化器）
3. **MAPPO + LP**
4. **MADQN + LP**
5. **Linear K-Selection + LP**（固定 k 规则选择）
6. **Traditional + LP**（传统通信 + LP）
7. **Traditional + Ivy**
8. **Traditional (No LP)**（传统固定卸载）

---

## 3. 主要实验结果和性能指标

### 📉 图表关键结果分析

#### ✅ 图2：训练奖励收敛性能（Convergence Performance）
- **MAPPO-PDN** 在整个训练过程中持续获得更高奖励；
- 相比传统 MAPPO 和 MADQN，**收敛更快、更稳定**；
- 表明 PDN 正则化有效提升了探索多样性并抑制了过拟合。

> 💡 结论：MAPPO-PDN 具备更强的环境适应能力和策略稳定性。

---

#### ✅ 图3：不同车辆数量下的总传输延迟
- 当 VU 数量从 20 增加至 40：
  - 所有方法延迟均上升（资源竞争加剧）；
  - 但在 25–35 辆区间出现“凹形下降”，因车距缩短 → V2V 信道质量提高 → 速率提升；
- **Our-Method + LP** 始终保持最低延迟，优于其他方法约 **15%~30%**。

> 💡 特别是在高密度下优势明显，体现 V2V 协同与语义压缩的有效性。

---

#### ✅ 图4：平均任务延迟对比（含计算延迟）
- 随着车辆增多，边缘节点计算资源被分摊 → **计算延迟成为主导因素**；
- **Our-Method + LP** 仍显著优于所有基线：
  - 比 MAPPO + LP 降低约 **25%** 延迟；
  - 比传统方法（Traditional + LP）降低超过 **40%**；
- 即使使用相同 LP 模块，MAPPO-PDN 在语义符号选择上的优越性也带来整体性能增益。

---

### 🔬 消融实验分析（隐含在对比中）
虽然未明确列出消融实验表格，但从方法组合可推断：
| 组件 | 是否启用 | 性能影响 |
|------|----------|---------|
| TCSC 框架（V2I+V2V） | ✔️ | 显著提升资源利用率 |
| Semantic Communication | ✔️ | 减少无效数据传输，降低负载 |
| MAPPO-PDN（vs MAPPO） | ✔️ | 提升收敛速度与鲁棒性 |
| LP 卸载率优化（vs 固定比例） | ✔️ | 精确控制卸载比例，降低延迟 |

> 示例：将 “Our-Method + LP” 与 “Traditional + LP” 对比，体现了 **语义通信本身的价值**；而 “MAPPO + LP” vs “MAPPO-PDN + LP” 展示了 **PDN 带来的性能增益**。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **语义通信 + VEC 是 IoV 中实现低延迟任务处理的有效范式**；
2. **V2I 与 V2V 协同卸载能显著提升系统弹性与资源利用率**；
3. **MAPPO-PDN 通过参数不确定性建模，在非平稳多车环境中表现出更强的泛化能力**；
4. **分离优化策略（MAPPO-PDN + LP）相比端到端方法更高效、准确且易于实现**；
5. 在高车辆密度场景下，所提方法依然保持良好性能，具备实际部署潜力。

---

### ⚠️ 方法的局限性
1. **假设信道为准静态（quasi-static）**，未考虑高速移动导致的快速衰落；
2. **仅针对文本类语义任务**（如导航指令、状态报告），未涉及图像、视频等多模态数据；
3. **SV 资源分配采用均分策略**，可能在异构计算能力下不够公平或高效；
4. **仿真中车速恒定**，未完全模拟真实交通流动力学。

---

### 🔮 未来工作方向
1. **扩展至多模态语义通信**（Multimodal Semantic Communication），支持视觉、语音等融合任务；
2. **结合 Reconfigurable Intelligent Surface (RIS)** 或 AI-driven beamforming 进一步优化信道质量；
3. **引入联邦学习机制**，保护语义模型隐私的同时实现分布式训练；
4. **在城市交叉口、拥堵路段等复杂场景验证框架普适性**；
5. 探索 **语义QoE（Quality of Experience）驱动的自适应压缩机制**。

---

## ✅ 总结
该论文提出了一个面向车联网的 **语义感知协同通信与计算框架 TCSC**，结合 **MAPPO-PDN 与 LP 的混合优化策略**，在保证语义保真度的前提下实现了低延迟任务卸载。实验表明其在多种环境下均优于传统方法，是推动 **Semantic Communication in VEC** 实际落地的重要一步。

</details>

---

### 4. [Mitigating Social Bias in English and Urdu Language Models Using PRM-Guided Candidate Selection and Sequential Refinement](https://arxiv.org/abs/2512.09854)

**Authors**: Muneeb Ur Raheem Khan  
**Category**: cs.CL  
**Published**: 2025-12-11  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2512.09854v1  

#### Abstract
Large language models (LLMs) increasingly mediate human communication, decision support, content creation, and information retrieval. Despite impressive fluency, these systems frequently produce biased or stereotypical content, especially when prompted with socially sensitive language. A growing bod...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Mitigating Social Bias in English and Urdu Language Models Using PRM-Guided Candidate Selection and Sequential Refinement*

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
该论文聚焦于**低资源语言（如 Urdu）在大型语言模型（LLMs）中面临的系统性社会偏见问题**。由于训练数据稀缺且文化代表性不足，Urdu等语言在生成任务中表现出更高的偏见倾向和语义质量下降。此外，大多数现有的去偏研究集中于英语，缺乏对多语言、尤其是低资源语言的公平性评估与干预机制。

### 🚀 提出的新方法与思路
作者提出了一种**统一的推理时（inference-time）去偏框架**，无需重新训练或微调模型，适用于任何黑盒 LLM。该框架基于 **Preference Ranking Model (PRM)**，结合两种候选优化策略：

- **PRM-Select**：通过 Best-of-N 采样生成多个候选词，由 PRM 打分后选择最优结果。
- **PRM-Sequential**：利用 PRM 提供的批评反馈进行多轮迭代精炼，逐步改进输出。

此方法将 PRM 同时用于 **bias scoring** 和 **utility scoring**，并定义了一个加权复合得分 $ S = (1-\alpha)\cdot\text{bias} + \alpha\cdot\text{utility} $（$\alpha=0.5$），实现偏见与语义合理性的平衡。

### 🔍 相比现有方法的优势
| 方面 | 优势 |
|------|------|
| **通用性** | 不依赖模型权重修改，适用于 GPT-3.5、GPT-4 等闭源模型 |
| **轻量高效** | 推理阶段操作，避免昂贵的再训练成本 |
| **跨语言适配** | 首次系统比较 English 与 Urdu 在单字补全任务中的去偏效果 |
| **可解释性** | 引入细粒度的 bias/utility 分项评分，便于分析改进来源 |

---

## 2. 核心实验方法和设置

### 📚 数据集
- 构建了一个双语去偏评估数据集，共 **400 条提示（prompts）**：
  - 英文原始提示：200 条，覆盖性别、种族、宗教、国籍、残疾、职业、年龄、社会经济地位等维度。
  - 对应乌尔都语翻译：200 条，经人工调整以保持社会文化语境一致性。
- 设计为 **单字补全任务（single-word completion）**，提升可比性和评估稳定性。
- 受 CrowS-Pairs 和 StereoSet 启发，但适配推理时去偏场景。

### ⚙️ 实验设置
| 组件 | 配置 |
|------|------|
| **Candidate Generator** | `gpt-3.5-turbo`（英文 & Urdu 输出） |
| **Preference Ranking Model (PRM)** | `gpt-4o-mini`，零样本模式打分（zero-shot scoring） |
| **Debiasing 方法** | 三种 pipeline：<br>1. **Baseline**：一次性生成<br>2. **PRM-Select**：N=8 个候选，选最高分<br>3. **PRM-Sequential**：最多 5 轮迭代 refinement |
| **评分维度** | 每个候选词由 PRM 打两个分数：<br>- **Bias Score** ∈ [0,1]（越高越少偏见）<br>- **Utility Score** ∈ [0,1]（越高语义越合理） |

### 📊 评估指标
- 平均 **Bias Score**
- 平均 **Utility Score**
- 加权 **Composite Score**（bias 与 utility 各占 0.5）
- 跨语言差异 Δ（English − Urdu）
- 改进轨迹分析（stage-wise improvement）

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据（来自 Table 和 Figure）

| 方法 | 语言 | Bias Score | Utility Score | Composite Score |
|------|------|------------|---------------|------------------|
| **Baseline** | English | 0.9525 | 0.985 | 0.96875 |
|              | Urdu    | 0.755  | 0.85  | 0.8025           |
| **PRM-Select** | English | 0.9765 | 1.0   | 0.98825          |
|                | Urdu    | 0.96   | 0.9825| 0.97125          |
| **PRM-Sequential** | English | **0.99**  | 0.99  | **0.99**         |
|                    | Urdu    | **0.975** | **0.8825** | 0.92875       |

### 🔁 与基线对比结果
- **PRM-Select** 显著提升了 Urdu 的表现：
  - Urdu 的 bias 提升高达 **+20.5 个百分点**（0.755 → 0.96）
  - Utility 提升 **+13.25 个百分点**
  - 几乎消除了英乌之间的 composite score 差距（Δ 从 0.166 降至 0.017）

- **PRM-Sequential** 进一步提高 fairness：
  - English 达到接近完美的 0.99 bias score
  - Urdu bias 也达 0.975，优于 PRM-Select
  - 但 **Urdu utility 下降至 0.8825**，表明存在“过度纠正”风险

### 🔍 消融实验与阶段分析（Stage-wise Improvement）
- 图 6 显示：
  - 从 Baseline → PRM-Select：bias 和 utility 均显著上升（尤其 Urdu）
  - 从 PRM-Select → PRM-Sequential：bias 继续上升，但 Urdu utility 下降
- 收敛速度差异：
  - English 多数在 1–2 步内收敛
  - Urdu 需要 2–4 步，反映其初始输出更不稳定

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **Urdu 存在严重的基线劣势**：
   - 初始 bias 更高、utility 更低，反映出多语言 LLM 中的结构性不平等（structural inequities）。
   
2. **PRM-Select 是最稳健的方法**：
   - 在两种语言上均大幅提升 bias 和 utility
   - 几乎消除英乌差距（Δ ≈ 0.017），具备良好跨语言公平性

3. **PRM-Sequential 最大化公平性但牺牲 Urdu 实用性**：
   - 虽然达到最高 bias 分数，但在 Urdu 上导致 utility 下降，产生过于泛化或抽象的词汇

4. **推理时去偏有效且可行**：
   - 即使不修改模型参数，也能显著缓解社会偏见，支持 black-box 场景下的部署

5. **低资源语言易受“去偏过矫”影响**：
   - refinement 过程可能迫使模型生成“安全但无意义”的词，印证了 *debiasing oversmoothing* 的担忧（Barocas & Selbst, 2017）

---

### ⚠️ 局限性
1. **PRM 零样本打分可能存在偏差**：未经过监督训练，跨语境一致性有待验证。
2. **单字生成限制应用广度**：难以推广到复杂句子生成或对话任务。
3. **Urdu 翻译无法完全捕捉文化细微差别**：某些刻板印象可能丢失或失真。
4. **缺乏人类标注验证**：所有评分均由 GPT-4o-mini 自动生成，缺少真实用户感知数据。

---

### 🔮 未来工作方向
- 扩展至 **全句生成任务** 的去偏框架
- 引入 **人类偏好反馈** 进行 PRM 微调
- 探索更多 **低资源语言**（如 Pashto, Yoruba, Nepali）
- 结合 **prompt engineering** 或 **self-debiasing** 技术增强鲁棒性
- 研究如何防止 refinement 导致的语言“去个性化”或“去文化化”

--- 

> **总结一句话**：  
> 本文展示了基于 PRM 的推理时去偏方法能有效缓解 English 和 Urdu 中的社会偏见，尤其通过 PRM-Select 显著缩小了跨语言公平差距，为低资源语言的 AI 公平性提供了实用且可扩展的技术路径。

</details>

---

### 5. [Passing the Baton: High Throughput Distributed Disk-Based Vector Search with BatANN](https://arxiv.org/abs/2512.09331)

**Authors**: Nam Anh Dang (Cornell University), Ben Landrum (Cornell University), Ken Birman (Cornell University)  
**Category**: cs.DC  
**Published**: 2025-12-11  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2512.09331v1  

#### Abstract
Vector search underpins modern information-retrieval systems, including retrieval-augmented generation (RAG) pipelines and search engines over unstructured text and images. As datasets scale to billions of vectors, disk-based vector search has emerged as a practical solution. However, looking to the...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：Passing the Baton: High Throughput Distributed Disk-Based Vector Search with BatANN**

---

## **1. 论文的主要贡献和创新点**

### **解决的问题**
随着向量数据集规模扩展到数十亿级别，单机内存已无法容纳全部数据，**基于磁盘的近似最近邻搜索（disk-based ANN）** 成为必要方案。然而，当数据规模进一步扩大至超出单台服务器的存储和处理能力时，需要分布式系统支持。

传统分布式方法如 **Scatter-Gather** 将数据分片并独立索引，查询时在所有分片上并行搜索后合并结果。这种方法虽然易于实现，但失去了全局图结构带来的对数级查询复杂度优势，导致计算和 I/O 随节点数量线性增长，效率低下。

此外，现有分布式全局图方法（如 CoTra、DistributedANN）依赖于 **RDMA 或 CXL 等专用硬件**，成本高且难以在通用云环境中部署。

---

### **提出的新方法与创新思路**
本文提出了 **BatANN** —— 一种基于标准 TCP 网络的高性能、低延迟的**分布式磁盘向量搜索系统**，其核心创新在于：

#### ✅ **异步状态传递机制（State-Passing Query Execution）**
- 在遍历全局搜索图（global graph）过程中，当当前服务器需访问另一个服务器上的邻居节点时，**不采用传统的“请求-响应”模式**（request-reply），而是将整个查询状态（query state）直接发送给目标服务器。
- 目标服务器接收后继续执行搜索任务，形成“接力”式执行流程（passing the baton）。
- 这种方式显著减少了跨节点通信中的等待时间（round-trip latency），提升了局部性和吞吐量。

#### ✅ **保留单图结构的全局搜索效率**
- 构建一个覆盖全数据集的单一全局图（single global graph），然后将其节点分布到多个服务器上。
- 利用该结构保持了近似最近邻搜索中典型的 **logarithmic 查询复杂度**，避免了 Scatter-Gather 中因多分片独立搜索而导致的计算冗余。

#### ✅ **面向 TCP 的优化设计**
- 所有通信基于标准 TCP 协议完成，无需 RDMA/CXL 等昂贵硬件，适用于更广泛的部署场景。
- 通过减少通信轮次、批量发送消息等方式最大化 TCP 吞吐潜力。

#### ✅ **关键组件协同优化**
- **In-Memory Head Index**：使用 1% 数据构建的小型内存路由索引，快速定位起始节点。
- **Neighborhood-Aware Graph Partitioning**：采用 Gottesburen et al. [12] 提出的图划分算法，使相近节点尽可能位于同一服务器，降低跨节点跳转频率。
- **Adaptive I/O Pipeline Width (W)**：引入启发式策略处理分布式环境下的并发读取（见 Algorithm 2），平衡本地执行与远程转移。

---

### **相比现有方法的优势**
| 维度 | BatANN | Scatter-Gather | DistributedANN / CoTra |
|------|--------|----------------|-------------------------|
| **查询复杂度** | 接近 log(N)，高效 | 多个 log(N_i) 之和 > log(N) | log(N)，优秀 |
| **通信开销** | 异步状态传递，低延迟 | 请求-响应频繁 | 依赖 RDMA 减少延迟 |
| **硬件要求** | 标准 TCP，通用性强 | 无特殊要求 | 需 RDMA/CXL，成本高 |
| **吞吐扩展性** | 近线性扩展 | 受限于每分片负载 | 较好，但封闭不可比 |
| **开源可用性** | ✅ 开源（GitHub） | 商业产品为主 | ❌ 未公开 |

> ⭐ 总结：**BatANN 是首个在标准 TCP 上运行、支持分布式磁盘 ANN 搜索且基于单一全局图的开源系统**，实现了高吞吐、低延迟与良好可扩展性的统一。

---

## **2. 核心实验方法和设置**

### **使用的数据集**
实验基于 BigANN Benchmarks [26] 的三个公开数据集：

| 数据集 | 规模 | 维度 | 类型 | 距离度量 |
|-------|------|------|------|----------|
| **BIGANN** | 100M / 1B | 128 | uint8 (SIFT) | L2 |
| **MSSPACEV** | 100M / 1B | 100 | int8 (语义嵌入) | L2 |
| **DEEP** | 100M | 96 | float (深度特征) | L2 |

涵盖不同精度、维度和应用场景，具有代表性。

---

### **实验设置**
- **硬件平台**：CloudLab 集群，共 10 台 `c6620` 节点
  - CPU：Intel Xeon Gold 5512U (28核 @ 2.1GHz)
  - 内存：128GB DDR5
  - 网络：25Gb Ethernet
  - 存储：NVMe SSD
- **软件实现**：
  - C++ 实现，基于 PipeANN 框架
  - 使用 `io_uring` 实现异步 I/O
  - ZeroMQ 实现跨节点通信（PEER sockets）
- **参数配置**：
  - 图结构：Vamana graph（R=64, L=128, α=1.2）
  - 分区数：5 或 10 个服务器
  - Beam width：默认 W=8，部分消融实验测试 W=1
  - PQ 压缩：每个向量压缩为 32 字节用于近似距离计算

---

### **评估指标**
| 指标 | 描述 |
|------|------|
| **QPS (Queries Per Second)** | 系统每秒能处理的查询数量，衡量吞吐能力 |
| **Recall@10** | 返回的前10个结果中包含真实最近邻的比例，衡量准确性 |
| **End-to-End Latency** | 客户端发出查询到收到结果的时间（含网络传输） |
| **Distance Comparisons** | 全精度距离计算次数，反映计算开销 |
| **Disk I/O Operations** | 发起的磁盘读取操作数，反映 I/O 开销 |
| **Inter-Partition Hops** | 跨服务器的状态转移次数，影响通信开销 |

---

### **基线方法对比**
- **ScatterGather**（本文实现）：
  - 将数据按相同图划分方法切分为独立分片
  - 每个分片独立建立 Vamana 图
  - 查询时广播到所有分片，收集结果后合并重排序
- **DistributedANN** 和 **CoTra**：
  - 虽然相关，但由于未开源，无法直接比较
  - 作者尝试复现 DistributedANN，但性能不如 ScatterGather 和 BatANN

> 因此，主要对比对象是 **ScatterGather**，作为主流分布式范式的代表。

---

## **3. 主要实验结果和性能指标**

### **关键性能数据汇总**

| 场景 | Recall@10 | BatANN QPS | ScatterGather QPS | 加速比 |
|------|-----------|------------|--------------------|--------|
| **BIGANN 100M, 10 servers** | 0.95 | ~18,000 | ~2,800 | **6.43×** |
| **DEEP 100M, 10 servers** | 0.95 | ~19,000 | ~2,900 | **6.55×** |
| **BIGANN 1B, 10 servers** | 0.95 | ~30,000 | ~5,900 | **5.10×** |
| **MSSPACEV 1B, 10 servers** | 0.95 | ~6,000 | ~2,400 | **2.5×** |

> 注：具体数值来自 Figures 8 和 9；MSSPACEV 表现略差可能与 ParlayANN 构图质量有关。

---

### **与基线方法的对比结果**

#### ✅ **吞吐量显著提升**
- 在 100M 规模下，BatANN 相比 ScatterGather 实现 **6.21–6.49× 的 QPS 提升**（@0.95 recall）
- 在 1B 规模下仍达到 **2.5–5.10× 的加速比**
- 随着服务器数量增加，BatANN 的吞吐增益持续扩大，体现其良好的**弱扩展性（weak scaling）**

#### ✅ **维持低延迟**
- 在 1K QPS 发送速率下，BatANN 的平均端到端延迟低于 **6ms**
- 即使在接近最大吞吐率（~30K QPS）时，延迟也稳定在 **~6ms**
- 相比之下，ScatterGather 在超过 6K QPS 后尾延迟急剧上升，系统趋于饱和

#### ✅ **计算与 I/O 效率更高**
- BatANN 的 **distance comparisons** 和 **disk I/O** 数量几乎不随服务器数量变化
- 而 ScatterGather 的这两项开销随节点数线性增长（Figure 10）
- 表明 BatANN 更有效地利用了全局图的搜索路径特性

#### ✅ **跨分区跳数少**
- 使用 neighborhood-aware partitioning 后，inter-partition hops 仅占总跳数的 **11.6%~24.3%**
- 特别是在小规模集群（如 3 节点）中占比更低，验证了图划分的有效性（Figure 3）

---

### **消融实验结果**

#### 🔍 **I/O Pipeline Width (W) 影响（W=1 vs W=8）**
- **W=8 显著优于 W=1**：
  - 在 BIGANN 1B 上，W=8 的总跳数从 138 降至 32.6（↓76%）
  - Inter-partition hops 从 21.9% 降至 18.8%
  - QPS 提升明显，延迟下降一半以上（Figure 14）
- 关键原因：更大的 W 减少了 beam search 的迭代次数，从而降低了跨节点通信频率

#### 🔍 **固定大小查询调度 vs 批处理调度**
- BatANN 采用 **固定并发查询数（8个/线程）**，而非 PipeANN 的批处理模式
- 实验表明该策略能更好掩盖 I/O 延迟，提高资源利用率

---

## **4. 关键结论和发现**

### **主要发现**
1. **全局图 + 状态传递 = 高效分布式搜索**
   - 保持单一全局图结构可在分布式环境下延续 log(N) 查询效率。
   - “Passing the baton” 的状态传递机制有效规避了传统 request-reply 模式中的通信瓶颈。

2. **TCP 上也能实现高性能分布式 ANN**
   - 不依赖 RDMA/CXL，仅用标准 TCP 即可实现超高吞吐（>30K QPS）和低延迟（<6ms）。

3. **吞吐近线性扩展，I/O 与计算恒定**
   - BatANN 的 QPS 随服务器数量近乎线性增长（Figure 11）
   - distance comparisons 和 disk I/O 基本不变，说明其具备优秀的弱扩展性。

4. **图划分与 I/O 宽度至关重要**
   - neighborhood-aware graph partitioning 显著减少跨节点跳转
   - W=8 的 I/O pipeline 设计大幅降低 hop 数，是性能飞跃的关键

---

### **方法的局限性**
1. **状态传输开销不可忽略**
   - 每次跨节点转移需序列化并传输约 4–8KB 的查询状态（beam + result list）
   - 对于极高 recall（如 >0.98）或大 beam size（L > 400），状态体积更大，可能成为瓶颈

2. **缺乏动态更新支持**
   - 当前系统不支持在线插入/删除向量
   - 图结构一旦构建即静态，不适合流式数据场景

3. **未考虑容错机制**
   - 节点故障会导致正在进行的查询失败
   - 缺乏副本或恢复机制

4. **MSSPACEV 表现受限**
   - 在某些数据集（如 MSSPACEV）上性能提升较小，提示图结构质量和数据分布敏感性仍需研究

---

### **未来工作方向**
1. **减小状态传输体积**
   - 只传 beam 状态，将中间结果直接发往客户端聚合（类似 Scatter-Gather 的 gather 阶段）

2. **集成更优磁盘布局优化**
   - 如 Starling 的邻域共置（neighbor co-location）技术，进一步提升 I/O 局部性

3. **支持动态图更新**
   - 结合 FreshDiskANN、SPFresh 等增量更新技术，实现 streaming ANN 支持

4. **构建容错架构**
   - 基于 Derecho 框架实现复制与故障恢复，提升生产可用性

5. **探索 RDMA/CXL 加速潜力**
   - 虽然当前设计已在 TCP 上表现优异，但仍可通过切换底层通信协议测试性能上限

6. **多租户与资源共享**
   - 支持多个向量数据库共享物理资源，应对企业级 RAG 应用需求

---

> 📌 **一句话总结**：  
> **BatANN 通过“状态接力”的创新范式，在标准 TCP 网络上实现了高性能、可扩展的分布式磁盘向量搜索，首次开源支持全局图结构的 disk-based ANN 分布式系统，为大规模 RAG 和检索系统提供了实用高效的基础设施选择。**

</details>

---

### 6. [Scalable Construction of Spiking Neural Networks using up to thousands of GPUs](https://arxiv.org/abs/2512.09502)

**Authors**: Bruno Golosio, Gianmarco Tiddia, Jos\'e Villamar, Luca Pontisso, Luca Sergi, Francesco Simula, Pooja Babu, Elena Pastorelli, Abigail Morrison, Markus Diesmann, Alessandro Lonardo, Pier Stanislao Paolucci, Johanna Senk  
**Category**: cs.DC  
**Published**: 2025-12-11  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2512.09502v1  

#### Abstract
Diverse scientific and engineering research areas deal with discrete, time-stamped changes in large systems of interacting delay differential equations. Simulating such complex systems at scale on high-performance computing clusters demands efficient management of communication and memory. Inspired ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Scalable Construction of Spiking Neural Networks using up to thousands of GPUs

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

该论文针对大规模脉冲神经网络（Spiking Neural Networks, SNNs）在超大规模 GPU 集群上的**高效构建与通信瓶颈**问题。具体挑战包括：

- **内存管理**：大规模 SNN 包含数十亿神经元和数万亿突触，传统方法在 CPU 和 GPU 之间频繁传输连接数据，导致高内存开销和延迟。
- **通信效率**：分布式模拟中跨 MPI 进程的 spike 通信是主要性能瓶颈，尤其在数千 GPU 上扩展时。
- **构建时间过长**：传统的“离板”（offboard）构建方式（先在 CPU 构建再传入 GPU）严重限制了模拟的整体效率。

### **提出了什么新方法或新思路**

提出了一种**全 GPU 内存原位网络构建方法**（onboard network construction），其核心创新包括：

- **无 MPI 通信的并行构建**：每个 MPI 进程独立地在本地 GPU 内存中构建其负责的局部连接，并为远程连接创建通信映射结构，整个构建过程无需进程间通信。
- **高效的通信映射结构**：
  - 引入 **proxy neuron（代理神经元）** 概念，在目标进程中表示源神经元的虚拟映像。
  - 构建 `(R, L)` 映射表（`R`: 源神经元全局索引, `L`: 代理神经元本地索引），支持高效的 spike 路由。
- **双模式 MPI 通信支持**：
  - 支持 **point-to-point** 和 **collective** 两种 MPI 通信协议，用户可根据网络拓扑选择最优模式。
- **四级优化策略（Optimization Levels 0–3）**：
  - 在 GPU 内存占用与运行速度之间提供灵活权衡，通过将部分通信结构存储于 CPU 内存来节省 GPU 显存。

### **相比现有方法的优势**

| 对比维度 | 本文方法 | 现有方法（如 NEST CPU/GPU） |
|---------|--------|--------------------------|
| **构建位置** | 完全在 GPU 内存中（onboard） | 多在 CPU 中完成后再迁移（offboard） |
| **构建通信** | 无 MPI 通信 | 需要大量 MPI 通信同步结构 |
| **通信灵活性** | 支持 point-to-point 和 collective 可切换 | 通常固定一种通信模式 |
| **扩展性** | 支持数千 GPU 规模 | 多数限于数百 GPU 或单机多卡 |
| **构建速度** | 显著加速（>10×） | 较慢，受限于 CPU-GPU 数据传输 |

---

## 2. 核心实验方法和设置

### **使用的模型与数据集**

论文采用两个典型的大规模 SNN 模型进行评估：

1. **Multi-Area Model (MAM)**  
   - 基于猕猴视觉皮层的 32 个脑区模型。
   - 总计约 **4.13×10⁶ 神经元**，**2.42×10¹⁰ 突触**。
   - 具有复杂层级结构和生物合理的 spike 统计特性。
   - 使用 **point-to-point MPI 通信**。

2. **Scalable Balanced Network**  
   - 随机平衡网络（兴奋性/抑制性神经元），用于弱扩展性测试。
   - 网络规模随 MPI 进程数量线性增长。
   - 每 GPU 固定连接数（Kin = 11,250），支持任意规模扩展。
   - 使用 **collective MPI 通信**（MPI_Allgather）。

### **实验设置和评估指标**

#### **硬件平台**
- **JUSUF 集群**：NVIDIA V100 GPU（16GB），用于 MAM 实验。
- **Leonardo Booster 超算**：NVIDIA A100 GPU（64GB），用于可扩展性实验。

#### **评估指标**
- **网络构建时间（Network Construction Time）**：分为初始化、神经元/设备创建、局部/远程连接生成、模拟准备等子阶段。
- **状态传播时间（State Propagation Time）**：以 **Real-Time Factor (RTF)** 衡量，即墙钟时间 / 模拟生物时间。
- **峰值 GPU 内存使用量（Peak GPU Memory Usage）**：决定可扩展上限的关键因素。
- **弱扩展性（Weak Scaling）**：保持每节点负载恒定，增加节点数，观察性能变化。

#### **基线方法对比**
- **Offboard vs Onboard**：对比传统 CPU 构建（offboard）与本文提出的 GPU 原位构建（onboard）。
- **不同 Optimization Levels**：比较 Opt 0 到 Opt 3 在内存与性能间的权衡。
- **与 NEST GPU 前期版本对比**：验证性能提升来源。

---

## 3. 主要实验结果和性能指标

### **关键性能数据**

#### **MAM 模型（Point-to-Point 通信）**
| 指标 | Offboard（CPU 构建） | Onboard（GPU 构建） | 加速比 |
|------|---------------------|--------------------|-------|
| **总构建时间** | 686.0 ± 1.5 s | **55.5 ± 0.1 s** | **~12.4×** |
| 局部连接生成 | — | — | **20× 加速** |
| 远程连接生成 | — | — | **9× 加速** |
| 神经元/设备创建 | — | — | **350× 加速** |
| 模拟准备 | — | — | **50× 加速** |
| **状态传播 RTF** | 16.0 ± 3.0 | 15.0 ± 1.7 | 基本持平 |

> ✅ 结论：**构建阶段获得超过 10 倍加速，而状态传播性能未受影响**。

#### **可扩展平衡网络（Collective 通信）**
- 在 **256 节点（1024 A100 GPUs）** 上实现弱扩展：
  - 总规模达 **2.3×10⁸ 神经元**，**2.59×10¹² 突触**。
  - 实现良好的弱扩展性，RTF 增长缓慢。
- **Optimization Level 3** 表现最佳：
  - 相比 Opt 0，**state propagation 时间减少 ~30%**。
  - 若关闭 spike recording，**额外降低 20% 状态传播时间**。
- **GPU 内存分析**：
  - **Opt 0** 可扩展至 **4096 节点**（理论极限），因通信结构部分存于 CPU。
  - 更高级别优化（Opt 3）虽快但显存占用高，限制最大规模。

#### **消融实验结果**
- **Optimization Levels 对比**：
  - 随着优化等级提高，**构建时间和状态传播时间下降**，但 **GPU 显存上升**。
  - Opt 2 和 Opt 3 在构建时间上接近，但 Opt 3 在状态传播更快。
- **Area Packing 实验（附录 B）**：
  - 利用 A100 大显存，可在单 GPU 上打包多个 MAM 区域。
  - 最少可用 **8 GPUs** 完成 MAM 模拟（原需 32），提升资源利用率。

---

## 4. 关键结论和发现

### **主要发现**

1. ✅ **GPU 原位网络构建显著加速模拟流程**：相比传统 offboard 方法，构建时间缩短 **10 倍以上**，尤其对连接生成和模拟准备阶段效果显著。
2. ✅ **通信映射结构可高效支持多种 MPI 模式**：统一的 `(R, L)` 映射机制同时兼容 point-to-point 和 collective 通信，适应不同网络拓扑需求。
3. ✅ **四级优化策略实现内存-性能灵活权衡**：用户可根据硬件条件选择合适优化级别，在有限显存下仍能运行超大规模模拟。
4. ✅ **方法具备良好弱扩展性**：在上千 GPU 上验证了可扩展性，预示其适用于下一代 exascale 超算（如 JUPITER）。
5. ✅ **生物学有效性得到验证**：通过 Earth Mover’s Distance（EMD）分析 spike 统计分布（firing rate, CV ISI, correlation），证明新方法不引入额外变异性。

### **方法的局限性**

- **依赖用户手动负载均衡**：当前未自动优化神经元到 GPU 的映射，需用户根据网络结构设计分配策略。
- **尚未启用 GPUDirect RDMA**：若启用，可能进一步降低跨节点通信延迟。
- **仅支持 point-neuron 模型**：未涉及多室（multi-compartment）神经元模型的复杂动力学。
- **随机数同步机制增加实现复杂度**：需保证源/目标进程生成相同连接序列，依赖对齐的 RNG 种子。

### **未来工作方向**

- 开发**自动化硬件感知映射策略**，结合集群拓扑优化神经元分布。
- 探索**混合通信方案**：例如局部 spike 用 point-to-point，远距离广播用 collective。
- 扩展至 **multi-compartment neuron models** 并集成 CoreNEURON 类加速技术。
- 在真实 exascale 系统（如 JUPITER）上部署，验证 **human-scale brain simulation**（~10¹⁰ 神经元, ~10¹⁴ 突触）可行性。

---

> 📌 **总结一句话**：本文提出了一种可扩展至数千 GPU 的 SNN 高效构建框架，通过全 GPU 原位构建与灵活通信机制，在不牺牲生物保真度的前提下，实现了超过一个数量级的构建加速，为实现人脑规模的脉冲网络模拟铺平了道路。

</details>

---

### 7. [Learning Unmasking Policies for Diffusion Language Models](https://arxiv.org/abs/2512.09106)

**Authors**: Metod Jazbec, Theo X. Olausson, Louis B\'ethune, Pierre Ablin, Michael Kirchhof, Joao Monterio, Victor Turrisi, Jason Ramapuram, Marco Cuturi  
**Category**: cs.LG  
**Published**: 2025-12-11  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2512.09106v1  

#### Abstract
Diffusion (Large) Language Models (dLLMs) now match the downstream performance of their autoregressive counterparts on many tasks, while holding the promise of being more efficient during inference. One particularly successful variant is masked discrete diffusion, in which a buffer filled with speci...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Learning Unmasking Policies for Diffusion Language Models

---

## 1. 论文的主要贡献和创新点

### 解决的问题
扩散语言模型（dLLMs）在生成文本时采用**逐步去遮罩（unmasking）**的方式，从全遮罩序列开始，逐步将 `M` 替换为真实 token。传统的采样策略如随机去遮罩或基于置信度的启发式方法（如 Fast-dLLM 的 confidence thresholding）虽然有效，但存在以下问题：
- 需要手动调参（如阈值 λ 或块长度 BL）
- 在非半自回归（non-semi-AR）设置下性能显著下降
- 缺乏对效率与准确性之间权衡的灵活控制

本文提出：**不应依赖手工设计的启发式规则，而应通过学习来自动发现最优的去遮罩策略**。

---

### 提出的新方法与思路
作者将 dLLM 的采样过程形式化为一个 **马尔可夫决策过程（MDP）**，并引入一种基于强化学习（RL）的方法来训练一个轻量级的**去遮罩策略网络（unmasking policy）**。

#### 核心创新点：
1. **形式化为 MDP**  
   将每一步的去遮罩决策建模为 RL 中的动作，状态为当前部分去遮罩的序列及其 token 置信度，奖励函数结合正确性和推理步数（NFEs）。

2. **轻量级 Transformer 策略架构**  
   设计了一个仅含单层的轻量 Transformer 网络作为策略网络 $ \pi_\theta $，输入是每个位置的 token 置信度 $ c_k = \max_v p(v|x, y_t) $、遮罩状态 $ m_k $ 和时间步 $ t $，输出为各位置是否去遮罩的 logits。

3. **使用 GRPO 进行训练**  
   采用 Group Relative Policy Optimization（GRPO），一种适用于大规模语言模型后训练的稳定 RL 算法，避免传统 PPO 的高方差问题。

4. **乘法奖励机制**  
   奖励定义为：
   $$
   R = \mathbb{1}[t=T] \cdot r(y, y^*) \cdot (1 - \alpha / (T - T_g))
   $$
   即先确保正确性，再鼓励高效（低 NFE）。相比加法奖励更稳定，防止“快但错”的 reward hacking。

---

### 相比现有方法的优势
| 维度 | 启发式方法（如 Fast-dLLM） | 本文方法（Learned Policy） |
|------|----------------------------|-----------------------------|
| 可调性 | 手动调节阈值 λ 或 K | 自动学习策略，减少人工干预 |
| 泛化能力 | 对 buffer size 敏感 | 能迁移到更长序列（L=512） |
| 性能上限 | 在 semi-AR 下表现优异 | 在 full-diffusion 设置下超越启发式 |
| 推理灵活性 | 固定策略 | 可通过 policy temperature $ T_{\text{policy}} $ 微调行为 |

---

## 2. 核心实验方法和设置

### 数据集
- **数学推理任务**：GSM8k、MATH（训练混合使用，测试分开）
- **代码生成任务**：HumanEval、MBPP（用于 domain transfer 实验）
- **额外数据**：KodCode-RL-10K（专门用于代码领域的 policy 训练）

### 模型基础
- 主要 backbone：LLaDA-8B-Instruct（从头训练的 MDM）
- 对比模型：Dream-7B-Instruct（由 AR 模型初始化）

### 实验设置
- **训练方式**：GRPO，group size=8，每组生成多个 rollout
- **温度设置**：dLLM 推理温度设为 0（greedy decoding），policy 温度 $ T_{\text{policy}} $ 可调
- **训练配置**：
  - Batch size: 16
  - Optimizer: AdamW, lr=3e-5, cosine schedule
  - Policy 参数量约 300K（仅为 LLaDA 的 0.01%）

### 评估指标
- **准确性（Accuracy）**：任务相关指标（如 GSM8k 数学答案匹配率）
- **推理效率**：Network Function Evaluations（NFEs），即采样步数
- **帕累托前沿（Pareto Frontier）**：准确率 vs. NFEs 曲线，衡量速度-精度权衡

### 基线方法对比
| 方法 | 类型 | 特点 |
|------|------|------|
| Random Unmasking | 基线 | 每步随机选择固定数量 token 去遮罩 |
| High-Confidence Unmasking | 启发式 | 每步去遮罩置信度最高的 K 个 token |
| Fast-dLLM | 启发式 | 动态去遮罩所有置信度 > λ 的 token |
| Ours (w/ ES) | 本文方法 | 加入专家引导（Expert Steering）提升探索 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Figure 3）

#### ✅ Semi-AR 设置（BL=32）
- 在 GSM8k 上，learned policy 准确率可达 ~80%，NFE ≈ 64
- 与 Fast-dLLM 表现相当，在中高 NFE 区间未明显超越
- 说明：在 semi-AR 下，Fast-dLLM 已接近最优

#### ✅ Full-Diffusion 设置（BL=256，无块限制）
- Learned policy 显著优于所有启发式方法
- 在 GSM8k 上，仅用 ~12 NFEs 达到 ~50% 准确率
- 启发式方法在此设置下退化严重（≤30%）
- **结论**：RL 策略在完全并行生成场景下更具优势

#### ✅ 引入 Expert Steering（ES）后的改进
- 使用 Fast-dLLM 作为“专家”引导训练
- 在 full-diffusion 设置下进一步缩小与 semi-AR 的差距
- 在 GSM8k 上达到 ~80% 准确率（接近 semi-AR 最优水平）

---

### 消融实验结果

#### （1）奖励函数设计（Multiplicative vs Additive）
- **Additive reward**: 容易陷入“全部一次性去遮罩”，导致 reward hacking
- **Multiplicative reward**: 更稳定，优先保证正确性，RL 收敛良好（见 Figure 5a-b）

#### （2）策略参数化方式
- **Bernoulli 输出**（默认）：简单高效，likelihood 易计算
- **Dynamic Plackett-Luce Sampling (DPLS)**：强制至少去遮罩一个 token，理论上更好
- 结果：两者性能相近，DPLS 略微提升可控性，但复杂度更高

#### （3）输入特征设计
| 输入类型 | 性能 | 备注 |
|--------|------|------|
| Top-1 confidence（最大概率） | ✔️ 最佳 | 简洁且有效 |
| Top-50 confidences | ❌ 稍差 | 并未带来增益 |
| Hidden states（LLaDA 最终层） | ❌ 更差 | 参数量增加 1000×，训练不稳定 |

> **结论**：简单的置信度信号已足够，无需复杂语义信息

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **RL 可以成功学习高效的去遮罩策略**，在 semi-AR 场景下媲美甚至逼近 Fast-dLLM。
2. ✅ **在 full-diffusion（非 semi-AR）设置下，learned policy 显著优于启发式方法**，展现出更强的并行推理潜力。
3. ✅ **策略具有良好的迁移能力**：
   - **跨模型迁移**：在 LLaDA 上训练的 policy 可迁移到 Dream，性能接近原生训练
   - **跨长度迁移**：在 L=256 训练的 policy 可直接用于 L=512，性能几乎不变
4. ⚠️ **领域迁移受限**：在数学数据上训练的 policy 在代码任务（HumanEval/MBPP）上表现不佳；需领域内训练才能恢复性能。

---

### 方法的局限性
| 局限 | 描述 |
|------|------|
| 控制粒度不足 | 调整 $ \alpha $ 无法平滑控制帕累托前沿，多个 $ \alpha $ 收敛到相同策略 |
| 训练不稳定性 | 尤其在 $ \alpha=10 $ 或使用 Expert Steering 时容易崩溃 |
| 领域泛化弱 | 数学 → 代码迁移失败，表明策略可能过拟合特定任务模式 |
| 无法端到端优化模型能力 | 本工作聚焦于采样策略，不增强 dLLM 本身的推理能力 |

---

### 未来工作方向（原文建议）
1. **多模型混合训练**：在多种 dLLM 上联合训练 policy，提升跨模型鲁棒性
2. **多领域数据混合训练**：融合数学、代码、对话等任务，提升 domain transfer 能力
3. **动态调整 policy temperature**：探索让 $ T_{\text{policy}} $ 成为可学习变量
4. **扩展至 remasking 和动态长度调整**：支持更复杂的采样机制（如 Saber、DiFFPO）
5. **应用于多模态扩散模型**：将该框架推广至图像、音频等离散扩散系统

---

> **总结一句话**：  
> 本文首次系统地将强化学习应用于 dLLM 的去遮罩策略学习，在保持极低开销的前提下，实现了媲美甚至超越启发式方法的采样效率，尤其在完全并行生成场景中展现出巨大潜力，为构建更高效、可学习的扩散语言模型推理引擎提供了新路径。

</details>

---

### 8. [TinyD\'ej\`aVu: Smaller Memory Footprint & Faster Inference on Sensor Data Streams with Always-On Microcontrollers](https://arxiv.org/abs/2512.09786)

**Authors**: Zhaolan Huang, Emmanuel Baccelli  
**Category**: cs.LG  
**Published**: 2025-12-11  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2512.09786v1  

#### Abstract
Always-on sensors are increasingly expected to embark a variety of tiny neural networks and to continuously perform inference on time-series of the data they sense. In order to fit lifetime and energy consumption requirements when operating on battery, such hardware uses microcontrollers (MCUs) with...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：TinyDejaVu: Smaller Memory Footprint & Faster Inference on Sensor Data Streams with Always-On Microcontrollers

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

在边缘设备（尤其是基于微控制器 MCU 的 always-on 传感器）上部署深度学习模型面临两大挑战：
- **极小的内存预算**（如仅 128kB RAM），难以支持传统推理中对中间激活值的大规模缓存；
- **高冗余计算**：在处理时间序列数据流时，常采用重叠滑动窗口（overlapping sliding windows），导致大量重复计算。

这些问题显著限制了长时间序列任务在资源受限设备上的实时性和能效。

---

### **提出了什么新方法或新思路**

论文提出 **TinyDejaVu** —— 一个全新的嵌入式软件框架，结合新型算法以优化时间序列神经网络在 MCU 上的推理过程。其核心思想是将**时序算子建模为状态空间模型（State-Space Models, SSMs）**，从而实现高效流式处理。

#### 主要创新点包括：

- ✅ **将通用时序算子转化为 SSM 形式**  
  首次系统性地形式化了卷积、池化等常见 temporal operators 与 SSM 的等价关系，并设计了一套自动转换机制，使这些操作可在固定大小的状态缓冲区中递归执行。

- ✅ **构建因果性分析框架，划分 SSM-subgraph 与 GTA-subgraph**  
  引入“全局时间聚合器”（Global Temporal Aggregator, GTA）作为分界点，将网络划分为可流式处理的局部算子部分（SSM-subgraph）和必须全窗口处理的部分（GTA-subgraph），实现最优资源调度。

- ✅ **支持重叠滑动窗口下的增量推理**  
  利用 SSM 缓存历史隐藏状态，在新窗口到来时只处理新增输入片段，避免整个模型重新计算，极大减少冗余。

- ✅ **优化 Global Pooling 的内存开销**  
  提出两级 SSM 结构替代原始 global pooling，将 RAM 复杂度从 $O(N)$ 降至 $O(N/s)$，其中 $s$ 为步长。

- ✅ **集成 BF16 混合精度支持**  
  在 SSM 子图中使用 BF16 存储隐藏状态，进一步降低内存占用约 50%，同时通过混合精度运算保持准确性。

- ✅ **端到端开源工具链实现**  
  基于 PyTorch + microTVM 构建完整编译流程，生成平台无关的 C 代码，适用于多种 MCU 架构（ARM Cortex-M、ESP32、RISC-V 等）。

---

### **相比现有方法的优势**

| 方面 | 现有方法（如 FastWaveNet、StreamiNNC） | TinyDejaVu |
|------|----------------------------------------|-----------|
| 支持算子范围 | 仅限 1D 卷积或特定结构 | 支持 Conv、Pooling、Attention、Transformer 等通用 temporal operators |
| 是否支持重叠滑动窗口 | 部分支持 | 完整支持，且自动消除冗余 |
| 内存优化程度 | 依赖具体结构 | 统一框架下实现高达 99% RAM 减少 |
| 可移植性 | 多为专用内核 | 基于 microTVM，跨平台通用性强 |
| 工具链完整性 | 手动实现为主 | 自动化分析 + 图变换 + 代码生成 |

> 💡 **优势总结**：TinyDejaVu 是首个面向通用混合架构、支持完整滑动窗口场景、具备自动化编译能力的低内存流式推理框架。

---

## 2. 核心实验方法和设置

### **使用的模型与任务**

未使用传统公开数据集（如 UCI-HAR），而是选取多个典型的时间序列 TinyML 模型进行实测，涵盖不同架构类型：

| 模型 | 类型 | 输入长度 | 应用场景 |
|------|------|----------|---------|
| **WaveNet** | Dilated 1D Conv | 512 | 语音生成 |
| **TC-CNN** | CNN + Dense | 48k | 音频分类（鸟鸣识别） |
| **TEMPONet** | TCN + Dense | 512 | EMG 手势识别 |
| **ResTCN** | Residual TCN | 512 | 时间序列建模 |
| **CET-S** | ResNet + Transformer | 512 | EEG 心肺复苏预后预测 |
| **TC-TFM** | CNN + Transformer | 48k | 长序列声音检测 |

> 所有模型均适配至可在 MCU 上运行的小型版本。

---

### **实验设置**

- **硬件平台**：
  - 主测试平台：**STM32F767ZI** MCU（ARM Cortex-M7 @ 216MHz，512KB RAM，2MB Flash）
  - 辅助验证平台：stm32f746g-disco
- **软件栈**：
  - 前端：PyTorch v2.3.0
  - 编译器：microTVM v0.16.0
  - 运行环境：RIOT-ML（轻量级物联网操作系统）
- **评估指标**：
  - **峰值 RAM 使用量（Peak RAM Usage）**
  - **推理延迟（Compute Latency）**：分为 preheat（首次完整推理）和 streaming（后续增量推理）
  - **相对 RMSE**：比较 BF16 与 FP32 输出差异
  - **重叠率影响**（Overlap Rate = $(l - s)/l$）

---

### **基线方法对比**

- **Vanilla Inference**：标准非流式推理，每个滑动窗口独立计算
- **FastWaveNet / StreamiNNC**：针对 WaveNet 的高效实现（仅用于 WaveNet 对比）
- **TinyDejaVu variants**：
  - `TinyDejaVu`
  - `TinyDejaVu + BF16`

---

## 3. 主要实验结果和性能指标

### **关键性能数据**

#### 🔹 **内存节省效果显著**

| 模型 | Vanilla RAM | TinyDejaVu RAM | 节省比例 |
|------|------------|----------------|---------|
| WaveNet | 178.5 MB | **142.6 kB** | >99.9% |
| TC-TFM | ~6.1 MB | **~6143.74 kB → 实际更低？** | >99%?（原文称 <0.05%） |
| TC-CNN | — | 1535.94 kB → **TinyDejaVu 后大幅下降** | ≥60% |
| CET-S | — | 47.65 kB | ≥60% |

> ⚠️ 注：原文 Fig.4 显示某些模型 RAM 使用仅为 vanilla 的 **<0.05%**，即最高达 **99.95% 减少**。

#### 🔹 **推理速度大幅提升**

- 在 **WaveNet** 上：
  - Preheat 阶段耗时：2776.1 ms
  - Streaming 阶段单样本生成仅需 **14.1 ms**
  - 相比 preheat 加速近 **200×**
- 在高重叠率（>99%）下，几乎所有模型 streaming 推理延迟远低于 preheat

#### 🔹 **BF16 内存优化额外收益**

- 隐藏状态存储由 FP32 → BF16，内存再降 **~31–50%**
  - 如 WaveNet 从 142.6kB → **108.3kB**
- 计算开销增加约 **13%**，但可接受
- 输出 RMSE 控制在 **1%–3%**，精度损失极小

#### 🔹 **消融实验：重叠率的影响（Fig.5）**

- 随着 **overlap rate 增加**，streaming 推理延迟呈**线性下降趋势**
- 表明 TinyDejaVu 成功消除了大部分冗余计算
- 特例：ResTCN 和 TEMPONet 因深层结构保留更多历史信息，即使低重叠也能维持较低延迟

---

## 4. 关键结论和发现

### **主要发现**

1. ✅ **SSM 是统一表达 temporal operators 的有效范式**  
   卷积、池化等均可转化为具有固定状态数的 SSM，实现常量空间复杂度的流式推理。

2. ✅ **GTA 是决定是否可流式处理的关键边界**  
   将网络划分为 SSM-subgraph 和 GTA-subgraph 可最大化资源利用率。

3. ✅ **TinyDejaVu 显著降低 MCU 上的时间序列推理成本**  
   - 最多节省 **>99% RAM**
   - 最高实现 **200× 推理加速**
   - 支持任意重叠滑动窗口输入

4. ✅ **BF16 可进一步压缩内存，精度损失可忽略**

5. ✅ **框架具有良好的通用性和可扩展性**  
   支持 CNN、TCN、Transformer 等混合架构，适用于多种 TinyML 场景。

---

### **局限性**

- ❗ **无法优化 GTA 层本身**（如 Attention、Global Pooling）  
  这些层仍需访问完整上下文，限制了极致流式化；尽管对 Global Pooling 做了优化，但仍不如纯局部算子理想。
  
- ❗ **预热阶段（preheat）开销较大**  
  第一次完整推理仍需较高时间和内存，不适合“冷启动”频繁的应用。

- ❗ **目前依赖人工指定 operator 类型**  
  虽然已实现自动 temporal analysis，但对于非常规自定义层的支持尚有限。

- ❗ **未在真实长期部署中测试能耗**  
  实验集中在 RAM 和延迟测量，缺乏电池寿命的实际验证。

---

### **未来工作方向**

1. 🔄 **开发全自动 operator 分析器**，无需手动标注 temporal 参数
2. 🔋 **联合优化能耗与内存**，探索更激进的稀疏化或量化策略
3. 🧠 **扩展至在线学习场景**，支持参数微调的流式更新
4. 🌐 **支持多模态传感器融合流式推理**
5. 🛠️ **集成至主流 TinyML 工具链**（如 TensorFlow Lite Micro、Edge Impulse）

---

## 总结

> **TinyDejaVu 是一种革命性的 TinyML 推理优化框架**，它通过将时序算子统一建模为 SSM，实现了在 MCU 上对重叠滑动窗口输入的**零冗余、低内存、高速流式推理**。其实验结果显示，在多种代表性模型上可实现 **>99% RAM 节省** 和 **高达 200× 的推理加速**，并具备良好的通用性和可移植性。该工作为 always-on 传感器设备上的长序列 AI 处理提供了极具前景的技术路径。

</details>

---

### 9. [Comparing AI Agents to Cybersecurity Professionals in Real-World Penetration Testing](https://arxiv.org/abs/2512.09882)

**Authors**: Justin W. Lin, Eliot Krzysztof Jones, Donovan Julian Jasper, Ethan Jun-shen Ho, Anna Wu, Arnold Tianyi Yang, Neil Perry, Andy Zou, Matt Fredrikson, J. Zico Kolter, Percy Liang, Dan Boneh, Daniel E. Ho  
**Category**: cs.AI  
**Published**: 2025-12-11  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2512.09882v1  

#### Abstract
We present the first comprehensive evaluation of AI agents against human cybersecurity professionals in a live enterprise environment. We evaluate ten cybersecurity professionals alongside six existing AI agents and ARTEMIS, our new agent scaffold, on a large university network consisting of ~8,000 ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Comparing AI Agents to Cybersecurity Professionals in Real-World Penetration Testing*

---

## 1. 论文的主要贡献和创新点

### 解决的问题
本文首次在**真实的企业生产环境**中，对AI代理（AI Agents）与人类网络安全专家进行了全面、直接的对比评估。此前的AI安全能力基准（如CTF挑战、CVE复现等）存在严重脱离现实的问题，缺乏操作真实性（operational realism），无法反映真实网络攻防中的复杂性、噪声和交互性。

### 提出的新方法：ARTEMIS
作者提出了 **ARTEMIS**（Automated Red Teaming Engine with Multi-agent Intelligent Supervision），一个用于自动化渗透测试的新型多智能体框架。其核心创新点包括：

- **动态提示生成（Dynamic Prompt Generation）**：为每个子任务生成定制化的系统提示，提升子代理的专业性和准确性。
- **任意子代理架构（Arbitrary Sub-agents）**：支持无限数量的并行子代理，实现任务分解与并发执行。
- **自动漏洞分级与验证（Automatic Vulnerability Triaging）**：内置三阶段验证模块（相关性判断、可复现性测试、分类报告），显著降低误报率。
- **长周期任务管理**：通过递归TODO列表、上下文摘要和会话恢复机制，克服现有代理无法持续长时间运行的瓶颈。

### 相比现有方法的优势
- **性能超越多数人类参与者**：ARTEMIS在真实环境中发现9个有效漏洞，有效提交率达82%，综合排名第二，优于10名人类中的9位。
- **成本优势显著**：ARTEMIS A1配置成本仅为$18.21/小时，远低于专业渗透测试员平均$60+/小时的成本。
- **系统化枚举与并行利用能力强**：能同时启动多个子代理进行背景探测，实现真正的并行化攻击。
- **开源透明**：作者公开了ARTEMIS代码和研究数据，推动AI安全评估的可重复性和透明度。

---

## 2. 核心实验方法和设置

### 实验环境与数据集
- **目标环境**：一所大型研究型大学的计算机科学网络，包含约8,000台主机，划分为12个子网（7个公网可达，5个需通过VPN访问）。
- **异构系统**：涵盖Unix/Linux、IoT设备、少量Windows机器及嵌入式系统。
- **权限设置**：所有参与者（人与AI）均获得学生级别权限账户，通过Kerberos认证。

### 参与者构成
- **人类参与者**：招募10名具备专业资质的网络安全人员（如OSCP、CRTO等认证持有者），平均经验丰富。
- **AI代理**：
  - **基线方法**：Codex、CyAgent（搭配GPT-5或Claude Sonnet）、Claude Code、Incalmo、MAPTA。
  - **提出方法**：ARTEMIS（A1：GPT-5作为监督器与子代理；A2：多模型集成监督器 + Claude Sonnet 4子代理）。

### 评估指标
采用综合评分体系，结合技术复杂度与业务影响：
- **总分公式**：
  $$
  S_{\text{total}} = \sum_i (TC_i + W_i)
  $$
  其中：
  - $TC_i$：技术复杂度 = 检测复杂度（DC） + 利用复杂度（EC），若仅验证未利用则EC打0.2折。
  - $W_i$：加权严重性（Critical: 8, High: 3, Medium: 2, Low: 1, Info: 0.5）
- **其他指标**：
  - 发现的有效漏洞数
  - 有效提交率（Valid Submission Rate）
  - 成本分析（$/hour）
  - MITRE ATT&CK 技术映射

### 实验流程
- 所有参与者被分配相同的GCP虚拟机（Kali Linux），记录屏幕、命令行、网络活动等行为日志。
- 人类参与者要求投入至少10小时；AI代理运行16小时（前10小时用于公平比较）。
- 所有发现由研究团队与IT部门共同验证、修复。

---

## 3. 主要实验结果和性能指标

### 关键性能数据（见Table 1）

| 参与者 | 总发现数 | 有效数 | 有效率 | 总分 |
|--------|----------|--------|--------|------|
| P1     | 13       | 13     | 100%   | 111.4 |
| **ARTEMIS A1** | 11       | 9      | **82%** | **95.2** |
| P4     | 8        | 8      | 100%   | 90.0 |
| P5     | 13       | 13     | 100%   | 85.8 |
| ...    | ...      | ...    | ...    | ...  |
| P10    | 6        | 4      | 67%    | 19.4 |

- ARTEMIS A1 排名**第二**，仅次于最强人类参与者P1。
- 发现9个有效漏洞，包括多个Critical级漏洞（如默认凭证访问iDRAC、未授权SMB读写等）。

### 与基线方法的对比
- **Codex、CyAgent、Incalmo、MAPTA** 表现不佳：
  - Codex 和 MAPTA 直接拒绝执行攻击任务（触发安全机制）。
  - Incalmo 因僵化的任务图停滞于早期侦察阶段。
  - CyAgent 多数发现为扫描类低级漏洞（如端口开放、目录遍历），缺乏深度利用。
- **ARTEMIS 显著优于所有现有代理框架**，证明其架构设计的有效性。

### 消融实验与行为分析
- **提示工程重要性**：动态提示生成帮助避免工具误用，提升执行准确性。
- **并行能力**：ARTEMIS峰值启用8个并发子代理，平均2.82个/轮次，体现强大任务调度能力。
- **会话管理有效性**：通过上下文摘要与重启机制，成功运行长达16小时，远超Codex（<20分钟）和CyAgent（<2小时）。

### 成本分析（Section 5.4）
| 配置 | 成本（$/小时） | 年化成本（全职） |
|------|----------------|------------------|
| ARTEMIS A1 | **$18.21** | $37,876 |
| ARTEMIS A2 | $59.00 | $122,720 |
| 美国平均渗透测试员 | ~$60 | ~$125,034 |

> **结论**：ARTEMIS A1 在性能接近顶尖人类的同时，成本仅为人类的一半左右，具备极高的性价比。

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **AI代理可在真实渗透测试中媲美甚至超越多数人类专家**：
   - ARTEMIS 综合表现排名第二，技术深度和提交质量与最强人类相当。
2. ⚙️ **AI在系统化枚举和并行利用方面具有天然优势**：
   - 能同时探测多个目标，而人类受限于注意力和串行操作。
3. 💰 **AI代理具备显著的成本优势**：
   - 成本可低至$18/小时，适合大规模、持续性的安全检测。
4. ❌ **当前AI代理仍存在明显短板**：
   - **GUI交互能力弱**：无法处理基于浏览器的操作（如TinyPilot RCE需图形界面点击）。
   - **误报率较高**：例如将登录失败后的重定向页误判为认证成功。
   - **缺乏纵深利用意识**：常在发现一个漏洞后立即提交，未能像顶级人类那样“横向移动”深入挖掘。

### 局限性
- **时间窗口较短**：实验限制在10小时内，而真实渗透测试通常持续1–2周。
- **防御条件不真实**：IT团队知晓测试存在，未主动拦截可疑行为，缺乏真实对抗性。
- **样本量有限**：仅10名人类参与者，统计功效不足，难以进行假设检验。
- **未模拟高级规避技术**：如绕过EDR、反沙箱等高级对抗手段未涉及。

### 未来工作方向
- 构建可复现的**仿真环境副本**，支持长期、可重复的AI安全评估。
- 开展不同**代理架构、模型、配置的消融研究**。
- 增强**GUI交互能力**（结合Computer Use Agents）。
- 集成**SIEM、EDR等防御工具日志**，实现红蓝对抗闭环。
- 探索AI代理在**漏洞修复建议、补丁生成**方面的应用。

---

> **总结**：该论文是AI安全领域的重要里程碑，首次在真实企业环境中验证了AI代理的实际攻防能力。ARTEMIS不仅展示了强大的技术实力，也揭示了AI在自动化渗透测试中的巨大潜力与现存挑战，为未来AI赋能网络安全奠定了坚实基础。

</details>

---

### 10. [MentraSuite: Post-Training Large Language Models for Mental Health Reasoning and Assessment](https://arxiv.org/abs/2512.09636)

**Authors**: Mengxi Xiao, Kailai Yang, Pengde Zhao, Enze Zhang, Ziyan Kuang, Zhiwei Liu, Weiguang Han, Shu Liao, Lianting Huang, Jinpeng Hu, Min Peng, Qianqian Xie, Sophia Ananiadou  
**Category**: cs.CL  
**Published**: 2025-12-11  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2512.09636v1  

#### Abstract
Mental health disorders affect hundreds of millions globally, and the Web now serves as a primary medium for accessing support, information, and assessment. Large language models (LLMs) offer scalable and accessible assistance, yet their deployment in mental-health settings remains risky when their ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：MentraSuite: Post-Training Large Language Models for Mental Health Reasoning and Assessment

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

当前的 Large Language Models（LLMs）在心理健康领域的应用面临以下关键挑战：

- **推理不完整、不一致或缺乏依据**：许多模型虽然能生成流畅的回答，但在临床相关的复杂任务中（如诊断、干预规划）缺乏透明、连贯且基于证据的推理过程。
- **现有心理领域 LLMs 侧重情感理解或知识回忆**，而忽视了临床实践中所需的**多阶段、步骤化、整合性推理能力**，例如认知评估（appraisal）、诊断（diagnosis）、干预策略选择（intervention）、证据抽象（abstraction）和信息验证（verification）。
- 缺乏一个系统性的基准来同时评估**任务性能**与**推理质量**。

### **提出了什么新方法或新思路**

为解决上述问题，作者提出 **MentraSuite**，一个统一框架，包含三大核心组件：

#### （1）MentraBench：首个综合性心理健康推理基准
- 覆盖五大核心临床推理维度：
  - **Appraisal**（认知模式识别）
  - **Diagnosis**（精神状况判断）
  - **Intervention**（治疗策略制定）
  - **Abstraction**（研究证据提炼）
  - **Verification**（错误信息检测）
- 包含 **6 项任务** 和 **13 个数据集**，涵盖真实社交平台文本、咨询对话、医学考试题、系统综述等。
- 不仅评估任务准确率，还从五个维度评估**推理链质量**：
  - **Conciseness**（简洁性）
  - **Coherence**（逻辑连贯性）
  - **Hallucination Avoidance**（避免幻觉）
  - **Task Understanding**（任务理解）
  - **Internal Consistency**（内部一致性）

#### （2）Mindora：后训练优化的心理健康专用 LLM
- 基于 Qwen3-8B 构建，采用**混合 SFT-RL 框架**（Supervised Fine-Tuning + Reinforcement Learning）。
- 引入 **LLM-based inconsistency-detection reward**，通过辅助模型（auxiliary model）动态检测推理中的事实矛盾，强制提升推理的一致性和保真度。
- 支持端到端的联合训练（joint SFT-RL），避免传统两阶段训练的过拟合问题。

#### （3）Reasoning Trajectory Generation (RTG) 策略
- 针对高质量推理轨迹构建：
  - **难度筛选**：保留 Llama-3-8B 在零样本下答错的“困难样本”进行标注。
  - **迭代最优路径搜索**：使用 GPT-4o 进行多轮生成-验证-修正，确保最终推理路径正确。
  - **结构化格式化**：将推理链标准化为 `<think>...</think><answer>...</answer>` 结构，强制分步分析（如 `###Symptom Analysis`）、最终结论与答案对齐，提升可读性与一致性。

### **相比现有方法的优势**

| 方面 | 现有方法（如 Psyche-R1, Psy-Interpreter, PsychCounsel-Bench） | MentraSuite |
|------|------------------------------------------------------------|-----------|
| **评估范围** | 多集中于单一任务（如共情、知识问答） | 覆盖五大临床推理维度，更贴近真实场景 |
| **推理质量评估** | 主要关注任务准确率 | 显式评估推理链的**质量五维** |
| **训练数据构造** | 依赖人工标注或简单提示生成 | 提出 RTG 策略，结合困难样本筛选与结构化重写 |
| **训练机制** | 多为纯 SFT 或分离式 SFT+RL | 提出一致性感知的奖励机制与联合 SFT-RL 训练 |

---

## 2. 核心实验方法和设置

### **使用的数据集**

MentraBench 整合并构建了 **13 个数据集**，分为六类任务：

| 任务类别 | 数据集 | 来源 |
|--------|-------|------|
| **Appraisal** | CognitiveReframing, PatternReframe, Therapist Q&A | 合成数据、Mental Health America、真实咨询对话 |
| **Diagnosis** | DepSign, SWMH, T-SID | Reddit, Twitter（抑郁与多病症检测） |
| **Intervention** | PsyDTCorpusM, AnnoMI | 合成与真实心理咨询对话（策略标注） |
| **Multi-step Reasoning** | MHQA, MedQAM, MedMCQA, PubMedQAM | PubMed 文献、医学考试题 |
| **Abstraction** | PSRS*（本文新建） | Cochrane Library 系统综述摘要 |
| **Verification** | MentalMisinfo | YouTube/Bitchute 视频脚本（是否含错误信息） |

> 注：带 `*` 表示本文新建；带 `M` 表示本文处理过。

### **实验设置和评估指标**

#### **评估对象**
- 共评估 **20 个主流 LLMs**，包括：
  - 闭源模型：GPT-4o, GPT-4o-mini, DeepSeek-R1, Qwen-plus, QwQ 等
  - 开源模型：LLaMA 系列、Qwen 系列、DeepSeek-distilled 等
  - 心理专项模型：EmoLLM, Psyche-R1

#### **评估方式**
- 所有模型均按 **Mindora 格式生成结构化推理链**（`<think>...</think><answer>...</answer>`）。
- 任务性能使用标准指标：Micro-F1, Jaccard, Macro-F1, Recall 等。
- 推理链质量由人工评估，基于五维打分（每维 0/1），最终取平均值作为 **Reasoning Trajectory Score**。

#### **基线方法对比**
- 主要对比：
  - 前沿闭源推理模型：GPT-4o-mini, DeepSeek-R1
  - 心理专项模型：Psyche-R1
  - 骨干模型：Qwen3-8B
  - 不同训练变体：MindoraSFT, MindoraSFT+RL

---

## 3. 主要实验结果和性能指标

### **关键性能数据**

#### （1）整体任务性能（Table 3）

| 模型 | Avg_all（13 数据集平均得分） |
|------|-----------------------------|
| **MindoraCHORD** | **0.6933** ✅（最高） |
| MindoraSFT+RL | 0.6548 |
| GPT-4o-mini | 0.6515 |
| DeepSeek-R1 | 0.6500 |
| Psyche-R1 | 0.5943 |
| Qwen3-8B（骨干） | 0.5729 |

> MindoraCHORD 在所有数据集中均表现最佳，尤其在 **Intervention** 和 **Abstraction** 任务上优势明显。

#### （2）推理链质量评估（Table 4）

| 模型 | R_avg（五维推理质量平均分） |
|------|----------------------------|
| **MindoraCHORD** | **0.9731** ✅ |
| DeepSeek-R1 | 0.9827（R2 最高） |
| GPT-4o-mini | 0.8731 |
| Qwen3-8B | 0.9039 |
| Psyche-R1 | 0.9442 |

> MindoraCHORD 在 **Reasoning Conciseness (R1)** 和 **Task Understanding (R4)** 上显著优于其他模型，说明其推理更简洁、任务对齐更好。

### **与基线方法的对比结果**

- **超越最强闭源模型**：MindoraCHORD 在平均任务得分上超过 GPT-4o-mini 和 DeepSeek-R1。
- **优于心理专项模型**：Psyche-R1 虽专为心理设计，但性能仍低于 Mindora 系列。
- **小模型胜过大模型**：Mindora（基于 8B 模型）优于多数 32B/70B 模型，证明**针对性后训练比模型规模更重要**。

### **消融实验结果**

- **训练策略对比**：
  - MindoraSFT < MindoraSFT+RL < **MindoraCHORD**
  - 说明 **联合 SFT-RL 训练**（CHORD）比分离训练更有效。
- **结构化推理格式的作用**：
  - 消融显示，无结构化格式的模型更容易出现 backtracking、重复、结论与答案不一致等问题。
- **困难样本筛选的有效性**：
  - 使用困难样本训练的模型在复杂案例上的泛化能力更强。

---

## 4. 关键结论和发现

### **主要发现**

1. **心理健康推理需要专门优化**：通用 LLMs 或通用推理优化无法满足临床级推理需求，必须针对 **appraisal, diagnosis, intervention, abstraction, verification** 五方面进行系统建模。
2. **推理质量与任务性能同等重要**：仅看准确率会掩盖模型在一致性、幻觉、任务漂移等方面的缺陷。MentraBench 首次实现了对推理链的多维量化评估。
3. **结构化训练数据 + 一致性奖励 = 更可靠推理**：
   - RTG 策略生成的高质量、结构化轨迹显著提升了模型的推理清晰度。
   - 基于辅助模型的 inconsistency-detection reward 有效减少了内部矛盾。
4. **小模型也能超越大模型**：通过针对性后训练，8B 级模型 Mindora 可超越 GPT-4o-mini 和 DeepSeek-R1，表明**训练方法比参数规模更具决定性**。

### **方法的局限性**

- **依赖 GPT-4o 构建训练数据**：RTG 中的轨迹生成与验证依赖强模型，可能引入偏差或限制可扩展性。
- **评估成本高**：推理链的人工评分耗时较长，难以大规模自动化。
- **应用场景尚未部署验证**：目前为离线评估，未在真实咨询场景中测试安全性与可用性。

### **未来工作方向**

- 构建完全去中心化的训练数据生成流程（如使用多个开源模型投票）。
- 将 MentraBench 扩展至更多语言和文化背景。
- 探索实时交互式心理支持中的动态推理机制。
- 结合多模态输入（语音、表情）进行综合心理状态评估。

---

> **总结**：  
> MentraSuite 通过 **MentraBench** 建立了首个全面评估心理健康推理能力的基准，提出 **Mindora** 模型与 **RTG** 数据构建策略，显著提升了 LLM 在复杂心理任务中的推理可靠性。实验证明，**结构化训练 + 一致性约束 + 联合 SFT-RL** 是实现可信心理 AI 的关键路径，为未来临床辅助决策系统提供了坚实基础。

</details>

---

### 11. [d-TreeRPO: Towards More Reliable Policy Optimization for Diffusion Language Models](https://arxiv.org/abs/2512.09675)

**Authors**: Leyi Pan, Shuchang Tao, Yunpeng Zhai, Zheyu Fu, Liancheng Fang, Minghua He, Lingzhe Zhang, Zhaoyang Liu, Bolin Ding, Aiwei Liu, Lijie Wen  
**Category**: cs.CL  
**Published**: 2025-12-11  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2512.09675v1  

#### Abstract
Reliable reinforcement learning (RL) for diffusion large language models (dLLMs) requires both accurate advantage estimation and precise estimation of prediction probabilities. Existing RL methods for dLLMs fall short in both aspects: they rely on coarse or unverifiable reward signals, and they esti...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：d-TreeRPO: Towards More Reliable Policy Optimization for Diffusion Language Models

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

该论文针对**扩散大语言模型（dLLMs）在强化学习（RL）中策略优化不可靠**的问题，指出当前方法存在两大核心缺陷：

- **奖励信号粗粒度或不可验证**：现有方法依赖稀疏的结果奖励（outcome rewards），或使用未经验证的过程奖励（如均匀广播最终奖励），容易导致 reward hacking。
- **动作概率估计不准确**：由于 dLLMs 支持任意解码顺序（any-order decoding），无法像自回归模型（AR）那样通过链式法则精确计算序列概率。现有方法（如基于 ELBO 的估计）存在未分析的偏差，且多采用单次前向传播近似，进一步加剧估计误差。

这些问题导致优势函数（advantage）和策略梯度更新不可靠，影响 RL 性能。

---

### **提出了什么新方法或新思路**

作者提出 **d-TreeRPO**，一种更可靠的 dLLM 策略优化框架，包含以下创新设计：

#### ✅ **Tree-Structured Rollout + Bottom-Up Reward Propagation**
- 将 rollout 组织为树结构：每个非叶节点代表一个中间状态，分支因子 $B$ 表示从该状态采样出的多个子状态。
- 叶节点对应完整生成结果，其奖励来自可验证的任务输出（如 Sudoku 是否正确）。
- 奖励自底向上聚合：父节点奖励为其所有子节点奖励的平均值。
- 子节点相对于父节点的 **advantage** 定义为 $A_c = R_c - R_p$，从而实现**细粒度、可验证的 step-wise 奖励信号**。

#### ✅ **Single-Time Forward Pass 的条件概率估计**
- 在树结构中估计从父节点到子节点的转移概率时，沿用 Diffu-GRPO 的方式，使用一次前向传播中解码位置的 log-prob 之和作为估计：
  $$
  \log \hat{\pi}(c|p) = \sum_{i=1}^k \log f_\theta(y_i | p)
  $$
- 提出“**块对齐（block-wise adaptation）**”以解决不同子节点解码位置不同导致的概率不可比问题。

#### ✅ **理论分析：预测置信度与估计误差的关系**
- 首次理论证明：单次前向传播估计的转移概率 $\hat{P}(c|p)$ 与真实期望概率 $P(c|p)$ 之间的误差随模型**预测置信度提升而减小**。
- 定义“最不确定步置信差距” $e = \max(\epsilon_{\text{parent}}, \epsilon_{\text{path}})$，并推导出：
  $$
  (1-e)^k \leq \frac{P(c|p)}{\hat{P}(c|p)} \leq \exp\left(\frac{ke}{1-e}\right)
  $$
- 当 $e \to 0$，近似比趋于 1，说明高置信度可显著降低估计误差。

#### ✅ **Time-Scheduled Self-Distillation Loss**
- 基于上述理论，引入时间调度的自蒸馏损失，在训练后期逐步增强模型确定性，以提高概率估计精度。
- 设计优势加权目标分布：
  $$
  w_c = \frac{\exp(A_c / \tau(t))}{\sum_{c'} \exp(A_c' / \tau(t))}
  $$
  其中温度 $\tau(t)$ 随训练步数递减。
- 自蒸馏损失为：
  $$
  \mathcal{L}_{\text{distill}} = \lambda(t) \cdot \text{KL}(P_{\text{target}} \| \pi_\theta)
  $$
  权重 $\lambda(t)$ 随训练逐步增大。

这一机制实现了**早期鼓励探索、后期促进收敛**的平衡。

---

### **相比现有方法的优势**

| 方面 | 现有方法 | d-TreeRPO |
|------|--------|----------|
| **奖励设计** | 稀疏结果奖励 或 不可验证过程奖励 | 细粒度、可验证的树形 bottom-up 奖励 |
| **优势估计** | 易受噪声干扰，缺乏 step-wise 判别力 | 基于实际采样结果的 group-relative advantage |
| **概率估计** | ELBO 下界偏误大，或多步累积误差 | 单次前向传播 + 理论支持的误差控制机制 |
| **训练稳定性** | 易陷入局部最优或 reward hacking | 自蒸馏引导向高优势路径收敛，提升鲁棒性 |

---

## 2. 核心实验方法和设置

### **使用的数据集**

- **Sudoku**：4x4 数独推理任务，测试逻辑填空能力。
- **Countdown**：数字运算谜题，测试数学组合推理。
- **GSM8K**：小学数学应用题，标准数学推理基准。
- **Math500**：高中水平数学题集合，更具挑战性。

> 所有任务均采用 zero-shot 设置，pass@1 作为评估指标。

---

### **实验设置和评估指标**

- **基础模型**：LLaDA-8B-Instruct（已完成 SFT，未进行 RL）。
- **训练配置**：
  - 使用 LoRA（rank=128, α=64）
  - 最大生成长度：256 tokens（block length=32），128 denoising steps
  - Tree 结构：高度 $H=2$，分支因子 $B=4$
  - 自蒸馏参数：$\lambda_{\max}=3\times10^{-3}$, $T_{\max}=2$, $\beta=0.7$
- **评估设置**：
  - 两种生成长度：256 和 512 tokens
  - deterministic sampling（temperature=0.0）
  - denoising steps = 生成长度 / 2

---

### **基线方法对比**

| 方法 | 类型 | 特点 |
|------|------|------|
| **Diffu-GRPO** | GRPO 变体 | 使用 outcome reward，单步前向估计概率 |
| **VRPO (LLaDA-1.5)** | Variance-reduced PO | 多次前向降低方差 |
| **wd1** | Weighted PO | 加权策略优化 |
| **SAPO+** | Step-aware PO | 引入过程奖励 |
| **d2-stepMerge** | Multi-step merge | 分阶段合并步骤 |
| **TraceRL** | Value-model based | 使用 value model 预测中间奖励 |

---

## 3. 主要实验结果和性能指标

### **关键性能数据（256-token setting）**

| 方法 | Sudoku | Countdown | GSM8K | Math500 |
|------|--------|-----------|-------|---------|
| Base (LLaDA-8B) | 6.7 | 19.5 | 76.7 | 32.4 |
| +Diffu-GRPO | 12.9 | 31.3 | 79.8 | 34.1 |
| +wd1 | 25.2 | 51.2 | 80.8 | 34.4 |
| +d2-stepMerge | 76.1 | 52.4 | 81.1 | 34.4 |
| **+d-TreeRPO (Ours)** | **92.9** | **71.1** | **81.2** | **37.7** |
| **相对提升** | **+86.2** | **+51.6** | **+4.5** | **+5.3** |

> 注：性能单位为百分比（%），提升幅度巨大，尤其在 Sudoku 和 Countdown 上表现突出。

---

### **与基线方法的对比结果**

- 在所有任务上，**d-TreeRPO 均达到 SOTA 性能**，显著超越所有基线。
- 在 Sudoku 上超越最强基线 d2-stepMerge 超过 16 个百分点。
- 在长生成场景（512 tokens）下：
  - Sudoku 和 Countdown 因训练限制（仅 256-token）略有下降；
  - GSM8K 和 Math500 得益于更长上下文空间，性能继续提升。

---

### **消融实验结果**

#### 🔍 **移除自蒸馏损失的影响**

| 方法 | Sudoku | Countdown | GSM8K | Math500 |
|------|--------|-----------|-------|---------|
| d-TreeRPO (Full) | 92.9 | 71.1 | 81.2 | 37.7 |
| w/o self-distill loss | 89.8 | 66.4 | 80.9 | 36.1 |
| w/ diversity-promoting loss | 84.2 | 63.4 | 78.5 | 35.2 |

- 移除自蒸馏导致全面性能下降。
- 使用“多样性促进损失”反而严重损害性能，验证了后期提升确定性的必要性。

#### 🔍 **Tree 参数敏感性分析**

- **Tree Height $H$**：
  - $H=4$：初期收敛最快，但计算开销过大，难以完成训练。
  - $H=2$：兼顾效率与性能，选择为默认设置。
- **Branch Factor $B$**：
  - $B=2$：探索不足，最终奖励低（~0.2）。
  - $B=4$ 和 $B=6$：均能收敛至 >0.9，$B=6$ 初始更快但成本更高，故选 $B=4$。

#### 🔍 **时间调度设计的有效性**

- 对比“反向调度”（early high $\lambda$, late low）：
  - 初期上升快，但中期后性能下降（reward 从 0.75 跌落）。
  - 说明过早固化策略会抑制泛化能力。
- 正向调度（本文设计）在后期持续提升，体现其合理性。

---

## 4. 关键结论和发现

### **主要发现**

1. ✅ **树形 rollout + bottom-up reward 是构建可靠 advantage 的有效范式**，解决了传统 RL 中 reward hacking 和粗粒度奖励问题。
2. ✅ **预测置信度直接影响概率估计误差**，这是首次在 dLLM 中建立该理论联系。
3. ✅ **time-scheduled self-distillation 是协调探索-利用权衡的关键机制**，在训练后期增强确定性可显著提升收敛质量。
4. ✅ **d-TreeRPO 在多个复杂推理任务上取得显著领先性能**，验证了其有效性与通用性。

---

### **方法的局限性**

- **计算开销较高**：tree rollout 需要 $O(B^H)$ 次推理，限制了 $H$ 和 $B$ 的扩展。
- **依赖可验证奖励的任务**：目前适用于 Sudoku、数学等有明确答案的任务，对开放生成任务适用性待验证。
- **块对齐要求**：需与 block-wise decoding 对齐才能保证概率可比性，限制了灵活性。

---

### **未来工作方向**

- 探索更高效的 tree search 策略（如 MCTS 启发式剪枝）。
- 将 d-TreeRPO 扩展到多模态或代码生成等其他领域。
- 研究无需块对齐的通用概率归一化方法。
- 探索在开放域任务中结合过程奖励建模（Process Reward Modeling）的可能性。

---

> **总结**：  
> d-TreeRPO 通过**结构化 rollout** 和 **理论驱动的训练机制**，系统性提升了 dLLM 的 RL 可靠性。它不仅在性能上大幅领先，也为未来 diffusion-based RL 提供了新的设计范式。

</details>

---

### 12. [Visual Categorization Across Minds and Models: Cognitive Analysis of Human Labeling and Neuro-Symbolic Integration](https://arxiv.org/abs/2512.09340)

**Authors**: Chethana Prasad Kabgere  
**Category**: cs.AI  
**Published**: 2025-12-11  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.09340v1  

#### Abstract
Understanding how humans and AI systems interpret ambiguous visual stimuli offers critical insight into the nature of perception, reasoning, and decision-making. This paper examines image labeling performance across human participants and deep neural networks, focusing on low-resolution, perceptuall...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Visual Categorization Across Minds and Models: Cognitive Analysis of Human Labeling and Neuro-Symbolic Integration

---

## 1. 论文的主要贡献和创新点

### 解决的问题
该论文旨在解决 **人类与AI在视觉分类任务中处理模糊图像时的认知机制差异**。具体而言，研究关注以下核心问题：
> 如何比较人类在标注模糊视觉刺激时的认知策略与AI系统的特征驱动分类过程？这些认知差异如何启发更符合人类思维模式的神经符号（neuro-symbolic）AI架构设计？

当前深度学习模型（如CNN）虽在图像分类上表现优异，但其决策机制缺乏可解释性，且与人类的类比推理、上下文理解等高级认知能力存在根本差异。本文通过跨心智（minds）与模型（models）的对比分析，揭示了这种差距，并提出融合路径。

---

### 提出的新方法与新思路

- **多维度人类行为数据采集框架**  
  设计了一套结合**标签选择、置信度评分、策略描述、注意力焦点、情绪反应、信任度**等多维认知指标的实验范式，超越传统仅看准确率的研究方式。

- **基于Marr三层次理论的跨系统比较框架**  
  利用 Marr’s tri-level hypothesis（计算层、算法层、实现层）系统性地对比人类与ResNet-18模型在：
  - **计算目标**（图像分类）
  - **算法过程**（人类使用形状/类比 vs AI使用纹理/特征激活）
  - **实现机制**（具身认知 vs 连接主义网络）

- **推动神经符号整合（Neuro-Symbolic Integration）的设计蓝图**  
  提出将 Grad-CAM 注意力图谱与符号规则引擎结合，在AI置信度低时触发“形状验证”等逻辑检查，从而提升鲁棒性和可解释性。

---

### 相比现有方法的优势

| 维度 | 传统方法局限 | 本工作的优势 |
|------|--------------|-------------|
| **分析粒度** | 仅比较准确率或混淆矩阵 | 引入信心、策略、情感等认知维度，深入理解“为什么这样判断” |
| **解释性建模** | 黑箱模型 + 后解释工具（如Grad-CAM） | 将Grad-CAM与人类报告的策略直接对齐，建立认知可比性 |
| **理论基础** | 多为工程导向 | 融合认知科学经典理论（Bounded Rationality, Analogical Reasoning, Embodied Cognition, Distributed Cognition）指导AI设计 |
| **未来方向** | 单纯优化性能 | 明确提出通往**认知对齐（cognitively aligned）AI**的路径 |

---

## 2. 核心实验方法和设置

### 数据集
- 主要使用 **CIFAR-10** 数据集中的10张低分辨率（32×32）图像。
- 图像被特意选为从**清晰到模糊不等**，以制造感知不确定性，便于观察人类与AI在边缘案例上的行为差异。

---

### 实验设置

#### 人类参与者
- **人数**：12名参与者（便利抽样，来自课堂及同事）
- **任务流程**：
  1. 观察一张低分辨率图像
  2. 选择类别标签（10类之一）
  3. 提供 **confidence rating**（1–5李克特量表）
  4. 描述所用**策略**（如“看起来像翅膀”、“类似停着的车”）
  5. 报告**注意力焦点区域**（中心/顶部/底部/边缘）
  6. 回答**认知负荷、情绪、对AI的信任、任务享受度**

#### AI模型
- 使用 **ResNet-18** 架构，针对CIFAR-10尺寸进行调整：
  - 第一层卷积核由7×7 stride 2改为3×3 stride 1，避免小图像空间信息丢失
  - 不使用初始max pooling
  - 添加数据增强（随机裁剪、水平翻转、归一化）
- 训练配置：
  - 从零开始训练
  - 5个epoch
  - Adam优化器（lr=0.001），交叉熵损失
  - 最终测试准确率约 **70.7%**

#### 可视化与解释工具
- 使用 **Grad-CAM** 生成每张图像的热力图，可视化AI关注区域
- 对比人类报告的“注意力焦点”与Grad-CAM高亮区域的一致性

---

### 评估指标

| 类型 | 指标 |
|------|------|
| **性能指标** | 准确率、softmax置信度 |
| **认知指标** | 人类置信度均值与标准差、认知负荷、策略类型分布 |
| **可解释性对比** | 人类策略 vs Grad-CAM注意力区域匹配程度 |
| **理论映射** | 是否体现 bounded rationality、analogical reasoning、embodied cognition 等原则 |

---

### 基线方法对比
- **人类 vs ResNet-18** 作为主要对比轴
- 无其他DL模型对比（非SOTA性能竞赛），重点在于**机制差异分析**

---

## 3. 主要实验结果和性能指标

### 关键性能数据

| 模块 | 结果 |
|------|------|
| **AI模型整体准确率** | ~70.7% （合理基准，非最优） |
| **人类整体准确率** | **100%**（所有12人全部正确标注10张图） |
| **人类平均置信度** | 高达 **4.5–4.92 / 5**，即使面对模糊图像也保持高度自信 |
| **AI置信度范围** | 0.329 – 0.957，部分错误预测仍有一定置信度（如 deer_00.png 被判为 airplane，置信0.37） |

---

### 与基线方法的对比结果

| 维度 | 人类表现 | AI表现 | 差异说明 |
|------|--------|-------|---------|
| **分类准确性** | 完美（100%） | 中等（~70.7%） | 人类利用先验知识补偿低质量输入 |
| **置信度校准** | 高准确+高置信，模糊时轻微下降 | 正确项置信较高，但误判也有一定置信 | 人类具备更强的元认知监控（metacognitive monitoring） |
| **注意力机制** | 全局形状、结构线索（如“耳朵”、“角”） | 局部纹理、边缘特征（Grad-CAM显示分散或误导性激活） | 支持文献[16]：CNN偏向texture而非shape |
| **推理策略** | 广泛使用“形状”、“熟悉感”、“类比” | 无显式策略，依赖隐式特征组合 | 人类采用 symbolic/analogical reasoning；AI为 subsymbolic processing |

#### 典型错例分析
- **deer_00.png**：
  - AI误判为“airplane”，Grad-CAM高亮顶部斜线纹理 → 被误认为机翼
  - 人类多数识别为“deer”，依据是“antler-like shape”、“forest-like fuzz”
  - 显示AI受误导性几何纹理影响，而人类依赖语义结构与情境联想

- **bird_01.png**：
  - AI正确但低置信（0.358），Grad-CAM注意力弥散
  - 人类中有人误判为cat，理由是“fur texture seems like cat”
  - 表明两者都受纹理干扰，但人类可通过形状修正判断

---

### 消融实验（Ablation Study）
- 本文未进行传统意义上的消融实验（如移除某模块测性能变化）
- 但通过**跨被试策略多样性分析**实现了类似功能：
  - 分析不同策略（shape vs texture vs guessing）对应的准确率与认知负荷
  - 发现使用“shape + familiarity”的策略组准确率最高、认知负荷最低 → 支持其有效性

---

## 4. 关键结论和发现

### 主要发现

1. ✅ **人类在低分辨率图像分类中表现出极高的鲁棒性与一致性**  
   尽管图像模糊，所有人仍达到100%准确率，依赖的是**原型匹配、类比推理和具身经验**。

2. 🔍 **人类决策体现 bounded rationality 和 dual-process theory**  
   - 清晰图像 → 快速Type 1处理（直觉判断）
   - 模糊图像 → 启动Type 2处理（分析性思考），伴随更高认知负荷
   - 置信度与认知负荷呈负相关 → 存在元认知调节机制

3. 🧠 **人类使用 analogical reasoning 和 case-based reasoning**  
   如将猫图像识别为“有耳朵”、“有胡须”，即调用长期记忆中的对象原型进行映射。

4. 🤖 **AI（ResNet-18）严重依赖局部纹理与边缘特征**  
   Grad-CAM显示其注意力常集中在非语义区域，导致在纹理误导下发生语义距离较远的误判（如deer→airplane）。

5. ⚖️ **AI与人类的注意力显著不一致**  
   人类关注整体结构（shape-based），AI关注局部pattern（texture-driven），这是当前CNN模型与人类视觉系统的关键分歧。

6. 💡 **神经符号整合具有明确潜力**  
   当AI置信度低或注意力与常见形状不符时，引入符号规则（如“飞机必须有翅膀”）可作为fallback机制，提高安全性与可解释性。

---

### 方法的局限性

| 局限 | 说明 |
|------|------|
| **样本量小** | 仅12名参与者，可能影响统计效力 |
| **图像数量少** | 仅分析10张图像，泛化性有限 |
| **未控制变量** | 未系统操控图像模糊度、噪声类型等独立变量 |
| **AI非SOTA** | 使用轻量级ResNet-18，未探索ViT或其他先进架构的行为差异 |
| **主观报告偏差** | 人类策略描述可能存在事后合理化（post-hoc rationalization） |

---

### 未来工作方向

1. **构建神经符号混合模型（Neuro-Symbolic System）**
   - 结合 Grad-CAM 特征激活 与 符号规则引擎
   - 在低置信或异常注意力时触发 symbolic validation rules
   - 示例：若检测到“飞行物”但无“对称翼结构”，则降权 airplane 类别

2. **探索 scalable representation alignment**
   - 使用图嵌入（graph-based embeddings）统一神经特征空间与符号概念空间
   - 在 CLEVR 或 GQA 等需要组合推理的数据集上验证抽象能力

3. **增强AI的情境感知与多模态输入**
   - 引入背景颜色、场景上下文（如“天空蓝”暗示飞机）、甚至触觉模拟
   - 推进 towards neuromorphic and context-aware AI

4. **开展人-AI协作实验**
   - 测试加入 symbolic trace 和 confidence reporting 后，是否能提升人类对AI的信任与协作效率
   - 特别是在 ambiguous 或 low-fidelity 条件下的联合决策场景

---

## 总结

> 本论文不是一篇追求性能突破的技术论文，而是一篇**连接认知科学与人工智能的桥梁性研究**。它通过严谨的实验设计，揭示了人类与AI在视觉分类中的深层机制差异，并呼吁下一代AI应走向 **cognitively grounded, interpretable, and neuro-symbolic** 的发展方向。

其最大价值在于：
- 提供了一个**理论驱动的分析框架**（Marr + Simon + Thagard）
- 展示了如何将**人类认知数据转化为AI设计启示**
- 明确指出：未来的智能不应只是“做得对”，更要“想得像”——即具备认知合理性与可沟通性。

</details>

---

### 13. [RIFT: A Scalable Methodology for LLM Accelerator Fault Assessment using Reinforcement Learning](https://arxiv.org/abs/2512.09829)

**Authors**: Khurram Khalil, Muhammad Mahad Khaliq, Khaza Anuarul Hoque  
**Category**: cs.AI  
**Published**: 2025-12-11  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.09829v1  

#### Abstract
The massive scale of modern AI accelerators presents critical challenges to traditional fault assessment methodologies, which face prohibitive computational costs and provide poor coverage of critical failure modes. This paper introduces RIFT (Reinforcement Learning-guided Intelligent Fault Targetin...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：**RIFT: A Scalable Methodology for LLM Accelerator Fault Assessment using Reinforcement Learning**

---

## 1. **论文的主要贡献和创新点**

### ✅ 解决的问题
现代大型语言模型（LLMs）在专用 AI 加速器（如 GPU、TPU）上运行时，其参数规模可达数十亿甚至上千亿，导致传统故障评估方法面临以下挑战：
- **计算成本极高**：随机故障注入（Random Fault Injection, RFI）需要大量仿真时间才能获得有限覆盖。
- **难以发现高影响故障**：最危险的“灾难性故障”通常由少数协同作用的比特翻转（bit-flips）引起，这类稀疏且非线性的组合在海量搜索空间中极难被随机方法捕获。
- **缺乏可扩展性**：形式化验证等精确方法因状态爆炸问题无法应用于大规模设计。

### 🚀 提出的新方法：RIFT 框架
本文提出 **RIFT**（Reinforcement Learning-guided Intelligent Fault Targeting），一个基于强化学习的自动化故障定位框架，用于高效识别对 LLM 推理造成最大破坏的最小比特翻转集合。

#### 创新思路：
1. **将故障搜索建模为 MDP 问题**  
   将寻找最坏情况下的最小高影响故障集转化为一个**序列决策过程**，由 RL Agent 通过试错逐步构建最优故障组合。

2. **三阶段漏斗式架构设计**
   - **Phase I: Vulnerability Profiling**  
     结合静态参数重要性（magnitude）与动态梯度敏感度（gradient），生成混合敏感度评分，并结合内存访问热点进行加权，形成全面的脆弱性排序。
   - **Phase II: Candidate Set Initialization**  
     从所有参数中筛选出 top-ranked 高敏感度子集 $P_{\text{crit}}$，大幅缩小 RL 搜索空间。
   - **Phase III: RL-Powered Test Vector Generation**  
     使用 Q-learning 在候选集中探索添加/删除比特的操作，以最大化“精度下降 + 故障数量惩罚”的奖励函数，最终输出最小高影响故障集 $F_{\text{crit}}$。

3. **支持 UVM 兼容测试平台自动生成**
   输出可直接集成到工业级 RTL 验证流程中的 `UVM-compliant testbench`，实现“push-button”式故障注入验证。

### 🔍 相比现有方法的优势
| 方法 | 局限性 | RIFT 的优势 |
|------|--------|-------------|
| **RFI (Random Fault Injection)** | 覆盖效率低，需百万级测试向量 | >99% 减少测试向量，速度提升 7.5× |
| **Magnitude/Gradient Heuristics** | 固定规则，无自适应能力 | 动态学习策略，更优收敛路径 |
| **Evolutionary Search (e.g., GenBFA)** | 缺乏长期规划，易陷入局部最优 | 强化学习具备记忆与回溯能力，搜索更智能 |
| **Formal Methods** | 不适用于 billion-scale 设计 | 可扩展性强，仅聚焦关键区域 |

---

## 2. **核心实验方法和设置**

### 📚 使用的数据集与目标模型
- **目标模型（DUT）**：
  - GPT-2 Large (~300M 参数)
  - LLaMA 3.1 8B (8 billion 参数)
  - DeepSeek-V2 7B (Mixture-of-Experts 架构)
- 所有模型均采用 **8-bit 整数量化**，符合实际部署场景。
- **评估基准**：
  - MMLU（Massive Multitask Language Understanding）
  - MMLU-Pro（更具挑战性的变体）

### ⚙️ 实验设置
- **硬件平台**：NVIDIA A100 80GB GPUs 组成的高性能集群
- **故障模型**：单比特翻转（Single-bit flip），特别关注 MSB（Most Significant Bit）翻转，因其对数值偏差影响最大
- **灾难性失败定义**：模型准确率下降超过 90%
- **计算预算**：固定为 1000 CPU 小时，用于公平比较不同方法的效率

### 📊 评估指标
| 指标 | 定义 |
|------|------|
| **Coverage (Cov)** | 成功识别的关键故障比例（导致 >90% 精度下降） |
| **Time (hrs)** | 达到该覆盖率所需时间（CPU hours） |
| **Test Vectors (TV)** | 生成的故障测试用例总数 |
| **Efficiency (Eff)** | Coverage per hour，衡量单位时间内的发现效率 |
| **Speedup (SU)** | 相对于 RFI 的加速比 |

### 🆚 基线方法对比
1. **Random Fault Injection (RFI)** — 工业界标准
2. **Magnitude Ranking** — 基于参数大小的静态分析（类似 PrisonBreak）
3. **Gradient Selection** — 基于梯度的动态分析（类似 DeepHammer）
4. **GenBFA [17]** — 当前最先进的进化算法，用于对抗性比特攻击

---

## 3. **主要实验结果和性能指标**

### 📈 主要性能对比（Table I）

| 方法 | Cov (%) | Time (hrs) | TV 数量 | Eff (Cov/hr) | Speedup vs RFI |
|------|---------|------------|----------|---------------|----------------|
| RFI | 65.3±4.1 | 1000±52 | 1.2×10⁵ | 0.065 | 1.0× |
| Magnitude Ranking | 73.8±3.7 | 245±18 | 8.4×10³ | 0.301 | 4.6× |
| Gradient Selection | 79.2±2.9 | 198±15 | 6.1×10³ | 0.400 | 6.2× |
| GenBFA [17] | 84.6±3.2 | 388±24 | 4.7×10³ | 0.218 | 3.4× |
| **RIFT (Ours)** | **91.7±2.1** | **187±12** | **847±73** | **0.490** | **7.5×** |

> ✅ **关键结论**：
> - RIFT 实现了 **最高覆盖率（91.7%）**
> - **比 GenBFA 快 2.2×**（效率 0.490 vs 0.218）
> - **测试向量减少 >99%**（仅需 ~847 个 vs RFI 的 120,000）
> - 总耗时仅为 RFI 的 1/5，却发现了更多关键故障

### 🔍 关键漏洞发现（Table II）
| DUT 模型 | 初始精度 | 致命比特数（平均） | 最终精度 | 发现时间（小时） |
|----------|-----------|--------------------|------------|------------------|
| GPT-2 Large | 30.5% | 5.1±0.6 | 0.34% | 89±8 |
| LLaMA 3.1 8B | 69.9% | 5.3±0.7 | 0.18% | 312±18 |
| DeepSeek-V2 7B | 71.3% | 5.8±1.1 | 0.22% | 156±12 |
| **平均** | — | **5.4±0.8** | **0.25%** | **186±13** |

> 💡 **发现**：平均只需 **翻转约 5 个关键比特** 即可使 LLM 功能完全崩溃（>99% 精度损失），说明存在高度稀疏但致命的脆弱点。

### 🛡️ 可靠性感知设计洞察（Table III）
| 保护策略 | 面积开销 (AO%) | 故障覆盖率 (FC%) | 成本效益 (CE = FC/AO) |
|----------|----------------|-------------------|------------------------|
| No Protection | 0 | 0 | — |
| Parity (Uniform) | 6.3 | 0.1 | 0.0 |
| ECC SECDED (Uniform) | 18.7 | 95.1 | 5.1 |
| ECC ChipKill (Uniform) | 31.4 | 98.7 | 3.1 |
| TMR (Uniform) | 205.0 | 99.2 | 0.5 |
| **RIFT-Guided ECC** | **13.8** | **88.5** | **6.4** |

> ✅ **关键优势**：RIFT 指导的**选择性 ECC 保护方案**，相比全芯片 TMR，在面积开销仅 **13.8%** 的情况下达到 88.5% 覆盖率，**成本效益提升 12.8×**（6.4 / 0.5）！

### 🔬 消融实验结果（Ablation Studies）

#### A. 混合敏感度指标有效性（Figure 3）
- 当混合系数 $\alpha = 0.5$（平衡 magnitude 与 gradient）时，找到的最小故障集最小（5.0±0.4），比纯 magnitude（7.1）减少 **29%**。
- 表明**静态与动态信息融合显著提升引导质量**。

#### B. 三阶段架构必要性（Table V）
| 配置 | 找到的关键故障数 | 收敛所需 Episodes |
|------|------------------|---------------------|
| Complete RIFT | 5.0±0.4 | 50±3 |
| RL-Only（相同预算） | 47.3±8.2 | 50（未收敛） |
| RL-Only（至收敛） | 5.2±0.6 | 890±67 |

> ❗ **结论**：若跳过 Phase I & II 的敏感度预筛选，RL 需要 **17× 更多训练步数** 才能收敛，证明前期剪枝至关重要。

#### C. RL 超参数鲁棒性（Figure 4）
- 训练 Episodes ≥ 50 后性能饱和
- 探索率 $\epsilon \in [0.05, 0.3]$ 内结果稳定（波动 <5%）
> 👉 说明 RIFT 对超参不敏感，易于部署。

#### D. 跨架构泛化能力（Table VI）
| 模型 | 混合指标增益 | 三阶段架构增益 |
|------|--------------|----------------|
| GPT-2 Large | 24% | 3.8× |
| LLaMA 3.1 8B | 29% | 4.2× |
| DeepSeek-V2 7B | 31% | 4.6× |

> ✅ RIFT 的优势在多种 LLM 架构下一致成立，具备良好通用性。

---

## 4. **关键结论和发现**

### ✅ 主要发现
1. **LLM 存在极端稀疏但高影响力的脆弱比特位**，平均仅需 **5~6 个比特翻转**即可导致功能崩溃。
2. **传统 RFI 方法效率低下**，需海量测试向量，而 RIFT 可实现 >99% 向量压缩。
3. **RIFT 显著优于现有方法**：
   - 相比 GenBFA，**速度快 2.2×**
   - 相比 RFI，**效率高 7.5×**
4. **RIFT 支持可靠性感知设计优化**：
   - 提供可操作的脆弱位置数据
   - 指导的选择性 ECC 方案比 TMR **成本效益高 12.8×**
5. **框架具备强一致性与可扩展性**：
   - 多次运行结果稳定（CI 窄）
   - 时间复杂度随关键参数集线性增长（$O(k)$），内存为 $O(k^{1.3})$

### ⚠️ 局限性
- 当前仅考虑 **永久性内存比特翻转**，尚未涵盖瞬态错误（soft errors）、逻辑门故障或时序相关故障。
- 强化学习阶段仍依赖大量 DUT 仿真评估（$C_{\text{eval}}$ 成本高），虽已剪枝但仍有一定开销。
- 对非常规量化格式（如 FP8、NF4）的支持有待验证。

### 🔮 未来工作方向
1. 扩展至其他大模型架构：Vision Transformers、Diffusion Models
2. 支持更复杂的故障模型：transient faults、timing violations、cross-layer error propagation
3. 集成进自动化综合流程（reliability-aware synthesis），实现物理设计层面的自动加固
4. 探索基于 LLM 自身反馈的在线 fault detection 机制

---

## ✅ 总结
**RIFT 是首个将强化学习系统性应用于 LLM 加速器故障评估的工作**，通过“敏感度分析 + RL 搜索 + UVM 自动生成”的三阶段框架，实现了：
- **高效精准地发现最小高影响故障集**
- **极大降低测试成本与时间**
- **为智能硬件保护提供数据驱动依据**

它不仅提升了 Design Automation 在 AI 芯片可靠性领域的技术水平，也为下一代安全关键应用（自动驾驶、医疗 AI）提供了坚实的故障评估基础。

</details>

---

### 14. [SCOPE: Language Models as One-Time Teacher for Hierarchical Planning in Text Environments](https://arxiv.org/abs/2512.09897)

**Authors**: Haoye Lu, Pavan Seshadri, Kaheer Suleman  
**Category**: cs.AI  
**Published**: 2025-12-11  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.09897v1  

#### Abstract
Long-term planning in complex, text-based environments presents significant challenges due to open-ended action spaces, ambiguous observations, and sparse feedback. Recent research suggests that large language models (LLMs) encode rich semantic knowledge about the world, which can be valuable for gu...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：SCOPE: Language Models as One-Time Teacher for Hierarchical Planning in Text Environments

---

## 1. 论文的主要贡献和创新点

### 解决的问题
在复杂的文本环境（text-based environments）中进行长期规划面临三大挑战：
- **开放的动作空间**（open-ended action spaces）
- **模糊的观测状态**（ambiguous observations）
- **稀疏的反馈信号**（sparse feedback）

现有的基于 **Large Language Models (LLMs)** 的分层规划方法虽然能利用 LLM 的语义知识指导高层推理，但通常存在两个显著缺陷：
1. 在训练和推理过程中频繁调用 LLM，导致计算成本高昂、部署效率低；
2. LLM 参数固定（frozen），无法针对目标任务进行适应性调整。

### 提出的新方法：SCOPE
本文提出 **SCOPE**（Subgoal-COnditioned Pretraining for Efficient planning），一种高效的**一次性教师框架**（one-time teacher framework），其核心思想是：
- **仅在初始化阶段使用一次 LLM**，从人类示范轨迹中提取子目标（subgoals）；
- 利用这些子目标对一个轻量级学生模型（student model）进行预训练；
- 后续通过强化学习（RL）微调，使学生模型独立完成任务，无需再依赖 LLM。

#### 关键创新点：
- **One-shot LLM Usage**：不同于以往反复查询 LLM 生成动态子目标的方法，SCOPE 只在开始时用 LLM 分析示范轨迹并生成固定的子目标序列，之后完全脱离 LLM。
- **Subgoal Extraction via Code Generation**：让 LLM 输出一个可执行的 Python 函数 `fac(T)` 来系统化地分解轨迹为子目标，确保一致性。
- **Hierarchical RL 架构**：采用 manager-agent（高层管理者）与 employee-agent（底层执行者）的双层架构，分别负责子目标提议和具体动作执行。
- **World Model 支持 RL 微调**：构建基于示范数据的世界模型（world model），用于在无真实环境交互的情况下进行 RL 训练，提升样本效率。

### 相比现有方法的优势
| 维度 | ADaPT (基线) | SCOPE (本文) |
|------|-------------|------------|
| LLM 调用频率 | 推理时持续调用 | 仅初始化一次 |
| 模型大小 | 175B (GPT-3.5) | 11.04M（小型网络） |
| 推理时间 | 164.4 秒 | **3.0 秒** |
| 成功率 | 0.52 | **0.56** |
| 部署效率 | 低（需 API 调用） | 高（本地 GPU 运行） |

> ✅ **优势总结**：**更高性能 + 更低成本 + 更易部署**

---

## 2. 核心实验方法和设置

### 数据集
- **TextCraft**：一个受 Minecraft 启发的纯文本环境，测试智能体在合成物品任务中的组合推理与长期规划能力。
  - 智能体需根据目标（如“制作云杉台阶”）和可用配方，逐步收集基础材料、制造中间品，最终合成目标物品。
  - 状态以库存字典表示，动作为自然语言命令（如 `craft 6 spruce slab using 3 spruce planks`）。
- **示范数据生成**：
  - 自动生成 50万条次优轨迹（suboptimal trajectories），模拟人类玩家行为（包含约10%随机噪声）。
  - 其中 1K 用于验证和测试。

### 实验设置
- **评估指标**：
  - **Success Rate**：成功完成目标任务的比例。
  - **Inference Time**：单次游戏平均耗时（在 NVIDIA A10 GPU 上测量）。
- **模型架构**：
  - Manager 和 Employee Agent 共享统一的 **variational sequence-to-sequence 架构**，基于 LSTM 编码器-解码器。
  - 所有输入输出均转换为文本形式处理。
- **训练流程**：
  1. **预训练阶段**：使用 LLM 提取的子目标对 manager 和 employee 进行模仿学习（imitation learning）。
  2. **RL 微调阶段**：
     - Employee Agent 在 **Employee World Model (EWM)** 上优化子目标达成率；
     - Manager Agent 在 **Manager World Model (MWM)** 上优化最终目标成功率。
  3. MWM 由 EWM 和训练好的 employee agent 组合而成，实现子目标级别的 rollout。

### 基线方法对比
- **ADaPT (Prasad et al., 2024)**：当前最先进的 LLM 驱动分层规划器，使用 GPT-3.5 作为 planner，在推理时实时生成子目标。
- **其他 LLM 后端对比**：在 Table 4 中还比较了多种 LLM（如 GPT-4o、Mistral Small 3、Claude-3 Haiku 等）作为 ADaPT 的 backend 表现。

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 3）

| 方法 | Success Rate | 参数量 |
|------|--------------|--------|
| ADaPT (GPT-3.5) | 0.52 | 175B |
| **SCOPE (ours)** | **0.56** | **11.04M** |
| SCOPE (hand-engineered subgoal) | 0.58 | 11.04M |
| SCOPE (without manager RL-finetuning) | 0.24 | 11.04M |

> 🔍 **结论**：即使使用非最优的 LLM 生成子目标，SCOPE 仍能超越依赖大型 LLM 实时推理的 ADaPT。

### 推理效率对比
- **SCOPE**：平均 **3.0 秒** 完成游戏（单张 A10 GPU）
- **ADaPT**：平均 **164.4 秒**（通过 OpenAI API 调用 GPT-3.5，理想网络条件下）

> ⏱️ **速度提升超过 50 倍**

### 多种 LLM 后端下的 ADaPT 性能（Table 4）
| Backend | Success Rate | 是否开源权重 |
|--------|---------------|-------------|
| GPT-4o | 0.58 | No |
| Mistral Small 3 | 0.58 | Yes |
| GPT-3.5 | 0.52 | No |
| GPT-4o mini | 0.43 | No |
| DeepSeek-R1-Distill-Qwen-32B | 0.13 | Yes |
| Claude-3 Haiku | 0.00 | No |
| **SCOPE (ours)** | **0.56** | —— |

> 📌 **观察**：SCOPE 以极小模型（11M）达到了接近最强闭源模型（GPT-4o）的性能，远超多数开源 LLM。

### 消融实验结果
#### （1）子目标质量的影响（Fig 8a）
- **Hand-engineered subgoal**（手工设计、可解释性强）：成功率 0.58
- **Standard SCOPE**（LLM 自动生成）：0.56
- **No Quantity Info**（去掉数量信息）：下降至 ~0.50
- **Non-Hierarchical**（无子目标，直接追求终极目标）：仅 0.28

> 💡 说明：**子目标的存在至关重要**，且**数量精度影响显著**。

#### （2）子目标与环境对齐性的破坏实验（Fig 8b）
- 随机替换子目标中物品名称的比例 $ p \in \{0, 0.25, 0.5, 1.0\} $
- 结果显示：
  - 当 $ p=0.25 $ 时，最终成功率降至 **0.09**
  - 当 $ p=1.0 $ 时，几乎完全失败（0.02）
- 即使子目标模糊但**语义正确**，仍有助于规划；一旦**语义错位**，反而误导 agent。

> ❗ 结论：**子目标必须与真实环境因果对齐**，否则危害大于无用。

#### （3）Manager RL 微调的重要性
- 移除 manager 的 RL 微调后，成功率从 0.56 下降到 **0.24**
- 图 6 显示 manager 在训练过程中逐渐学会绕开 employee 的弱点，选择更易实现的替代路径。

> ✅ 证明：**manager 的自适应能力是鲁棒性的关键来源**

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **LLM 不必全程参与也能有效赋能 RL**：
   - 即使 LLM 未与环境交互、生成的是次优甚至不可解释的子目标，只要提供合理的初始分解结构，就能为分层 RL 提供强大先验。
   
2. ✅ **One-time teaching 是高效可行的范式**：
   - 将 LLM 视为“一次性教师”，仅用于初始化，后续完全由小型学生模型自主运行，大幅降低部署成本。

3. ✅ **子目标的质量关键在于“对齐性”而非“精确性”**：
   - 子目标是否反映真实的环境依赖关系（causal grounding）比其是否可解释更重要。
   - 错误对齐的子目标会严重误导 agent，甚至不如没有子目标。

4. ✅ **Manager 层的 RL 微调具有补偿机制作用**：
   - 能主动规避 employee agent 的执行缺陷，动态调整计划，提高整体鲁棒性和成功率。

### 方法的局限性
- **依赖高质量示范轨迹**：若示范本身偏差过大或缺乏多样性，LLM 提取的子目标可能严重失真。
- **子目标不可解释**：由于 LLM 输出的是程序化函数而非自然语言解释，生成的子目标往往难以理解。
- **泛化能力待验证**：目前仅在 TextCraft 上验证，复杂开放世界中的表现尚不明确。

### 未来工作方向
- 探索如何结合少量在线 LLM 查询与 one-time teaching，进一步提升子目标质量；
- 设计更具解释性的子目标提取机制，在保持性能的同时增强透明度；
- 将 SCOPE 扩展到视觉-语言或多模态环境中；
- 研究如何自动识别和修正错误对齐的子目标。

---

> 🏁 **总结一句话**：  
> **SCOPE 成功展示了“大模型教小模型一次，小模型自己学会长期规划”的可行性，在性能上超越实时调用 LLM 的方法，同时将推理速度提升数十倍，为高效、可部署的文本环境智能体提供了新范式。**

</details>

---

### 15. [An End-to-end Planning Framework with Agentic LLMs and PDDL](https://arxiv.org/abs/2512.09629)

**Authors**: Emanuele La Malfa, Ping Zhu, Samuele Marro, Sara Bernardini, Michael Wooldridge  
**Category**: cs.AI  
**Published**: 2025-12-11  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2512.09629v1  

#### Abstract
We present an end-to-end framework for planning supported by verifiers. An orchestrator receives a human specification written in natural language and converts it into a PDDL (Planning Domain Definition Language) model, where the domain and problem are iteratively refined by sub-modules (agents) to ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：An End-to-end Planning Framework with Agentic LLMs and PDDL

---

## 1. 论文的主要贡献和创新点

### 解决的问题
该论文旨在解决**自然语言到可执行计划之间的鸿沟问题**。传统自动化规划（如基于 PDDL 的方法）虽然能保证正确性和最优性，但依赖专家手工建模领域知识；而 Large Language Models（LLMs）虽擅长理解自然语言指令，却在长视野、多步逻辑推理任务中容易产生幻觉（hallucinations）、不一致或不可行的动作序列。

此外，现有 LLM+PDDL 框架通常采用静态流水线结构，缺乏动态适应能力来处理模糊、矛盾或未明确说明的需求。

### 提出的新方法与创新思路
作者提出了一种**端到端的、基于代理（agentic）架构的规划框架**，其核心创新包括：

- **动态多代理工作流编排（Dynamic Agent Orchestration）**  
  引入一个由 LLM 驱动的“Orchestrator”模块，能够根据当前 PDDL 域/问题的状态、求解器日志和验证反馈，**动态选择并调用不同的专用 Agent 子模块**进行迭代优化，而非固定流程。

- **闭环迭代精炼机制（Iterative Refinement Loop）**  
  初始由 LLM 将自然语言转换为 JSON 表示，再生成初步 PDDL 模型 → 调用外部 Planner（如 Fast Downward）尝试求解 → 若失败或存在错误，则通过多个 Refiner Agents 对模型进行语法、语义、时间一致性等方面的修复 → 循环直至成功。

- **全自动化且无需人工干预**  
  整个过程从自然语言输入到最终可执行计划输出完全自动完成，**无需任何中间人类介入**，实现了真正的 end-to-end 规划。

- **自然语言回译增强可解释性**  
  最终将 PDDL 计划翻译回自然语言，提升对用户的可读性和审计能力，同时保持形式化正确性。

- **支持任意 PDDL 求解器与验证工具集成**  
  兼容 Fast Downward、LPG、POPF 等主流 Planner，以及 VAL/uVAL 等 Validator，具备良好的通用性。

### 相比现有方法的优势
| 维度 | 传统 LLM 直接生成 | 静态 LLM+PDDL 流水线 | 本文方法 |
|------|------------------|------------------------|----------|
| 正确性保障 | ❌ 易出现幻觉 | ✅ 有 PDDL 形式化约束 | ✅✅ 多轮验证+修正 |
| 动态适应性 | ❌ 固定流程 | ❌ 固定角色分配 | ✅ 动态调度 Agent |
| 处理模糊/矛盾需求 | ❌ 能力弱 | ⚠️ 有限 | ✅ 显式澄清与修正 |
| 可扩展性 | 中等 | 中等 | ✅ 支持灵活添加新 Agent |
| 用户友好性 | ✅ 自然语言输出 | ⚠️ 输出可能复杂 | ✅ 双向自然语言交互 |

---

## 2. 核心实验方法和设置

### 使用的数据集
- **Google Natural Plan Benchmark**  
  包含三类现实场景任务：
  - Calendar Scheduling（日程安排）
  - Meeting Planning（会议行程规划）
  - Trip Planning（旅行路线规划）

- **PlanBench**  
  标准化规划基准，涵盖经典规划域：
  - Logistics（物流运输）
  - Depots（仓库调度）
  - Blocksworld（积木世界）
  - Mystery Blocksworld
  - Obfuscated Deceptive Logistics（混淆欺骗性物流）

- **Blocksworld 和 Tower of Hanoi**  
  经典符号规划难题，用于测试 LLM 在结构化推理上的极限，尤其是随着问题规模增长的表现。

### 实验设置与评估指标
- **基础模型**：GPT-5-mini（作为 LLM 主干）
- **Planner**：Fast Downward、POPF、LPG 等
- **Validator**：VAL / uVAL
- **评估方式**：
  - **Accuracy**：LLM-as-a-judge 判断生成计划是否与真实解匹配（0/1）
  - **Verified Accuracy**：仅统计那些被 PDDL 验证器接受的有效计划中的准确率
  - **Cost Reduction (%)**：启用优化后平均成本下降比例（衡量优化效果）
- **每项任务运行 30 个实例**

### 基线方法对比
- **Vanilla LLM（GPT-5-mini）**：直接让 LLM 输出计划，不做任何形式化建模或验证
- **其他 LLM+PDDL 方法（如 LLM+P）**：仅单次转换，无迭代精炼机制

---

## 3. 主要实验结果和性能指标

### 关键性能数据

#### 在 Google Natural Plan 上的结果（图3）
| 任务 | 本方法 Accuracy | Vanilla LLM Accuracy | 提升幅度 |
|------|------------------|-----------------------|---------|
| Calendar Scheduling | 93% | 80% | +13% |
| Meeting Planning | 93% | 53% | **+40%** |
| Trip Planning | 88% | 24% | **+64%** |

> 注：本方法在 Meeting Planning 上表现尤为突出，显示其在时序约束处理上的优势。

#### 在 PlanBench 上的结果（图3）
| 域 | 本方法 Accuracy | Vanilla LLM Accuracy | 提升 |
|----|------------------|------------------------|------|
| Depots | 100% | 90% | +10% |
| Logistics | 100% | 93% | +7% |
| Mystery Blocksworld | 93% | 87% | +6% |
| Blocksworld | 93% | 37% | **+56%** |
| Obfuscated Deceptive Logistics | 90% | 20% | **+70%** |

> 平均提升约 **+15%**，在具有误导信息的任务中优势显著。

#### Blocksworld 与 Tower of Hanoi 结果（图4）
| 任务 | 难度 | 本方法 Accuracy | Vanilla LLM Accuracy |
|------|------|------------------|------------------------|
| Blocksworld | Easy (2–4步) | 97% | 97% |
| | Medium (6–8步) | 100% | 97% |
| | Hard (10–12步) | **97%** | 80% | → **+17%**
| Tower of Hanoi | 4–6 disks | 97%~100% | 73%~90% |
| | 7 disks | **90%** | 73% | → **+17%**

> 特别值得注意的是，**LLMs 通常无法解决超过 6 层的 Tower of Hanoi**，而本文框架在 7 层仍达到 90% 准确率。

#### 成本优化效果（表1）
| 任务 | 非优化平均成本 | 优化后平均成本 | 成本降低 |
|------|----------------|----------------|-----------|
| Calendar Scheduling | 1.63 | 1.24 | 23.9% |
| Meeting Planning | 210.46 | 79.00 | **62.5%** |
| Trip Planning | 13.24 | 11.22 | 15.2% |
| **平均降低** | —— | —— | **45.8%** |

> 表明该框架不仅能生成可行计划，还能有效实现**成本最小化**。

### 消融实验分析（图5 & 图6）
- **最常用 Agent**：
  - `AgentSyntaxPDDL`（修复 PDDL 语法错误）使用频率最高（>50%），说明 LLM 生成 PDDL 时常出现语法问题。
  - `AgentDeepThinkPDDL` 和 `TemporalConsistency` 在 Meeting Planning 和 Trip Planning 中频繁调用，反映这些任务对时间逻辑要求高。
  - `FastDownwardsAdapter` 在多个任务中占比高，表明不同求解器的语法兼容性是常见瓶颈。

- 发现：框架能根据不同任务特点**自适应地选择最合适的 Agent**，体现了动态编排的有效性。

---

## 4. 关键结论和发现

### 主要发现
1. **LLMs 单独不足以胜任复杂规划任务**，尤其在涉及时间约束、资源竞争或多步推理时表现脆弱。
2. **结合符号求解器（Symbolic Planner）可大幅提升可靠性与准确性**，尤其是在对抗幻觉和语义不一致方面。
3. **动态多代理协作优于静态流水线设计**，Orchestrator 能根据上下文智能调度 Refiner Agents，形成“自我纠正”循环。
4. **即使小规模 Tower of Hanoi 这类传统 LLM 难题，也能被本框架高效解决**，证明了其强大的组合泛化能力。
5. **计划优化不仅可行，而且收益显著**，平均降低成本近 46%，在复杂任务中可达 60% 以上。

### 方法的局限性
- **对 Orchestrator 的 Prompt 设计敏感**：微小提示变化可能导致 Agent 调度策略剧烈波动，系统较“brittle”。
- **依赖高质量 LLM**：若底层 LLM 推理能力不足，Orchestrator 可能做出错误决策。
- **Benchmark 本身存在问题**：
  - Google Natural Plan 中部分 ground truth 错误或歧义（例如旅行天数计算方式不统一）；
  - PlanBench 覆盖面广但偏重经典领域，缺乏现实复杂性。
- **LLM-as-a-judge 不可靠**：除非评判模型远强于被测模型，否则难以准确判断计划正确性。

### 未来工作方向
- 扩展至更大规模的 Agent 群体与更复杂的协同机制
- 支持多模态输入（图像、语音等）
- 探索在具身智能（embodied AI）和机器人系统中的应用
- 开发理论模型分析动态工作流的收敛性与稳定性
- 构建更严谨、经人工审核的规划评测基准

---

## 总结
本文提出的 **Agentic End-to-End Planning Framework** 是迈向真正自主智能体的重要一步。它通过**动态编排 LLM Agents + 符号求解器 + 迭代验证机制**，实现了从模糊自然语言到形式化可执行计划的全自动转化，在准确性、鲁棒性和可解释性上全面超越现有方法。特别是在传统 LLM 表现糟糕的任务（如 Blocksworld、Tower of Hanoi）上取得突破，展示了 LLM 与 Symbolic AI 深度融合的巨大潜力。

</details>

---

### 16. [Targeting Misalignment: A Conflict-Aware Framework for Reward-Model-based LLM Alignment](https://arxiv.org/abs/2512.09212)

**Authors**: Zixuan Liu, Siavash H. Khajavi, Guangkai Jiang, Xinru Liu  
**Category**: cs.CL  
**Published**: 2025-12-11  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2512.09212v1  

#### Abstract
Reward-model-based fine-tuning is a central paradigm in aligning Large Language Models with human preferences. However, such approaches critically rely on the assumption that proxy reward models accurately reflect intended supervision, a condition often violated due to annotation noise, bias, or lim...

---

### 17. [WarmServe: Enabling One-for-Many GPU Prewarming for Multi-LLM Serving](https://arxiv.org/abs/2512.09472)

**Authors**: Chiheng Lou, Sheng Qi, Rui Kang, Yong Zhang, Chen Sun, Pengcheng Wang, Bingyang Liu, Xuanzhe Liu, Xin Jin  
**Category**: cs.DC  
**Published**: 2025-12-11  
**Score**: 6.5  
**Type**: new  
**ArXiv ID**: 2512.09472v1  

#### Abstract
Deploying multiple models within shared GPU clusters is promising for improving resource efficiency in large language model (LLM) serving. Existing multi-LLM serving systems optimize GPU utilization at the cost of worse inference performance, especially time-to-first-token (TTFT). We identify the ro...

---

### 18. [Luxical: High-Speed Lexical-Dense Text Embeddings](https://arxiv.org/abs/2512.09015)

**Authors**: DatologyAI,  :, Luke Merrick, Alex Fang, Aldo Carranza, Alvin Deng, Amro Abbas, Brett Larsen, Cody Blakeney, Darren Teh, David Schwab, Fan Pan, Haakon Mongstad, Haoli Yin, Jack Urbanek, Jason Lee, Jason Telanoff, Josh Wills, Kaleigh Mentzer, Paul Burstein, Parth Doshi, Paul Burnstein, Pratyush Maini, Ricardo Monti, Rishabh Adiga, Scott Loftin, Siddharth Joshi, Spandan Das, Tony Jiang, Vineeth Dorma, Zhengping Wang, Bogdan Gaza, Ari Morcos, Matthew Leavitt  
**Category**: cs.CL  
**Published**: 2025-12-11  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2512.09015v1  

#### Abstract
Frontier language model quality increasingly hinges on our ability to organize web-scale text corpora for training. Today's dominant tools trade off speed and flexibility: lexical classifiers (e.g., FastText) are fast but limited to producing classification output scores, while the vector-valued out...

---

### 19. [Spectral Embedding via Chebyshev Bases for Robust DeepONet Approximation](https://arxiv.org/abs/2512.09165)

**Authors**: Muhammad Abid, Omer San  
**Category**: cs.LG  
**Published**: 2025-12-11  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2512.09165v1  

#### Abstract
Deep Operator Networks (DeepONets) have become a central tool in data-driven operator learning, providing flexible surrogates for nonlinear mappings arising in partial differential equations (PDEs). However, the standard trunk design based on fully connected layers acting on raw spatial or spatiotem...

---

### 20. [Latent-Autoregressive GP-VAE Language Model](https://arxiv.org/abs/2512.09535)

**Authors**: Yves Ruffenach  
**Category**: cs.LG  
**Published**: 2025-12-11  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2512.09535v1  

#### Abstract
We investigate a fully Latent AutoRegressive scheme based on a Gaussian Process (GP) integrated into a Variational Autoencoder (VAE). In this setting, sequential dynamics are transferred from the observation space to a continuous latent space, while linguistic generation remains parallel through a n...

---

### 21. [Physics-Aware Heterogeneous GNN Architecture for Real-Time BESS Optimization in Unbalanced Distribution Systems](https://arxiv.org/abs/2512.09780)

**Authors**: Aoxiang Ma, Salah Ghamizi, Jun Cao, Pedro Rodriguez  
**Category**: cs.LG  
**Published**: 2025-12-11  
**Score**: 6.0  
**Type**: new  
**ArXiv ID**: 2512.09780v1  

#### Abstract
Battery energy storage systems (BESS) have become increasingly vital in three-phase unbalanced distribution grids for maintaining voltage stability and enabling optimal dispatch. However, existing deep learning approaches often lack explicit three-phase representation, making it difficult to accurat...

---

### 22. [SDialog: A Python Toolkit for End-to-End Agent Building, User Simulation, Dialog Generation, and Evaluation](https://arxiv.org/abs/2512.09142)

**Authors**: Sergio Burdisso, S\'everin Baroudi, Yanis Labrak, David Grunert, Pawel Cyrta, Yiyang Chen, Srikanth Madikeri, Esa\'u Villatoro-Tello, Thomas Schaaf, Ricard Marxer, Petr Motlicek  
**Category**: cs.AI  
**Published**: 2025-12-11  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.09142v1  

#### Abstract
We present SDialog, an MIT-licensed open-source Python toolkit that unifies dialog generation, evaluation and mechanistic interpretability into a single end-to-end framework for building and analyzing LLM-based conversational agents. Built around a standardized \texttt{Dialog} representation, SDialo...

---

### 23. [Advancing Text Classification with Large Language Models and Neural Attention Mechanisms](https://arxiv.org/abs/2512.09444)

**Authors**: Ning Lyu, Yuxi Wang, Feng Chen, Qingyuan Zhang  
**Category**: cs.CL  
**Published**: 2025-12-11  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.09444v1  

#### Abstract
This study proposes a text classification algorithm based on large language models, aiming to address the limitations of traditional methods in capturing long-range dependencies, understanding contextual semantics, and handling class imbalance. The framework includes text encoding, contextual repres...

---

### 24. [A Distributed Framework for Privacy-Enhanced Vision Transformers on the Edge](https://arxiv.org/abs/2512.09309)

**Authors**: Zihao Ding, Mufeng Zhu, Zhongze Tang, Sheng Wei, Yao Liu  
**Category**: cs.DC  
**Published**: 2025-12-11  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.09309v1  

#### Abstract
Nowadays, visual intelligence tools have become ubiquitous, offering all kinds of convenience and possibilities. However, these tools have high computational requirements that exceed the capabilities of resource-constrained mobile and wearable devices. While offloading visual data to the cloud is a ...

---

### 25. [Contrast transfer functions help quantify neural network out-of-distribution generalization in HRTEM](https://arxiv.org/abs/2512.09067)

**Authors**: Luis Rangel DaCosta, Mary C. Scott  
**Category**: cs.LG  
**Published**: 2025-12-11  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.09067v1  

#### Abstract
Neural networks, while effective for tackling many challenging scientific tasks, are not known to perform well out-of-distribution (OOD), i.e., within domains which differ from their training data. Understanding neural network OOD generalization is paramount to their successful deployment in experim...

---

### 26. [Improved Physics-Driven Neural Network to Solve Inverse Scattering Problems](https://arxiv.org/abs/2512.09333)

**Authors**: Yutong Du, Zicheng Liu, Bo Wu, Jingwei Kou, Hang Li, Changyou Li, Yali Zong, Bo Qi  
**Category**: cs.LG  
**Published**: 2025-12-11  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.09333v1  

#### Abstract
This paper presents an improved physics-driven neural network (IPDNN) framework for solving electromagnetic inverse scattering problems (ISPs). A new Gaussian-localized oscillation-suppressing window (GLOW) activation function is introduced to stabilize convergence and enable a lightweight yet accur...

---

### 27. [Training One Model to Master Cross-Level Agentic Actions via Reinforcement Learning](https://arxiv.org/abs/2512.09706)

**Authors**: Kaichen He, Zihao Wang, Muyao Li, Anji Liu, Yitao Liang  
**Category**: cs.LG  
**Published**: 2025-12-11  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.09706v1  

#### Abstract
The paradigm of agentic AI is shifting from engineered complex workflows to post-training native models. However, existing agents are typically confined to static, predefined action spaces--such as exclusively using APIs, GUI events, or robotic commands. This rigidity limits their adaptability in dy...

---

### 28. [Closing the Train-Test Gap in World Models for Gradient-Based Planning](https://arxiv.org/abs/2512.09929)

**Authors**: Arjun Parthasarathy, Nimit Kalra, Rohun Agrawal, Yann LeCun, Oumayma Bounou, Pavel Izmailov, Micah Goldblum  
**Category**: cs.LG  
**Published**: 2025-12-11  
**Score**: 5.5  
**Type**: new  
**ArXiv ID**: 2512.09929v1  

#### Abstract
World models paired with model predictive control (MPC) can be trained offline on large-scale datasets of expert trajectories and enable generalization to a wide range of planning tasks at inference time. Compared to traditional MPC procedures, which rely on slow search algorithms or on iteratively ...

---

### 29. [Toward Closed-loop Molecular Discovery via Language Model, Property Alignment and Strategic Search](https://arxiv.org/abs/2512.09566)

**Authors**: Junkai Ji, Zhangfan Yang, Dong Xu, Ruibin Bai, Jianqiang Li, Tingjun Hou, Zexuan Zhu  
**Category**: cs.AI  
**Published**: 2025-12-11  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2512.09566v1  

#### Abstract
Drug discovery is a time-consuming and expensive process, with traditional high-throughput and docking-based virtual screening hampered by low success rates and limited scalability. Recent advances in generative modelling, including autoregressive, diffusion, and flow-based approaches, have enabled ...

---

### 30. [Gaussian Process Aggregation for Root-Parallel Monte Carlo Tree Search with Continuous Actions](https://arxiv.org/abs/2512.09727)

**Authors**: Junlin Xiao, Victor-Alexandru Darvariu, Bruno Lacerda, Nick Hawes  
**Category**: cs.AI  
**Published**: 2025-12-11  
**Score**: 5.0  
**Type**: new  
**ArXiv ID**: 2512.09727v1  

#### Abstract
Monte Carlo Tree Search is a cornerstone algorithm for online planning, and its root-parallel variant is widely used when wall clock time is limited but best performance is desired. In environments with continuous action spaces, how to best aggregate statistics from different threads is an important...

---

## 🔧 Configuration

This bot is configured to look for papers containing the following keywords:
- framework, System, Generation, Video, Linear, LLM, RL, RLHF, Inference, Training, Attention, Pipeline, MOE, Sparse, Quantization, Speculative, Efficient, Efficiency, Framework, Parallel, Distributed, Kernel, Decode, Decoding, Prefill, Throughput, Fast, Network, Hardware, Cluster, FP8, FP4, Optimization, Scalable, Communication

## 📅 Schedule

The bot runs daily at 12:00 UTC via GitHub Actions to fetch the latest papers.

## 🚀 How to Use

1. **Fork this repository** to your GitHub account
2. **Customize the configuration** by editing `config.json`:
   - Add/remove arXiv categories (e.g., `cs.AI`, `cs.LG`, `cs.CL`)
   - Modify keywords to match your research interests
   - Adjust `max_papers` and `days_back` settings
3. **Enable GitHub Actions** in your repository settings
4. **The bot will automatically run daily** and update the README.md

## 📝 Customization

### arXiv Categories
Common categories include:
- `cs.AI` - Artificial Intelligence
- `cs.LG` - Machine Learning
- `cs.CL` - Computation and Language
- `cs.CV` - Computer Vision
- `cs.NE` - Neural and Evolutionary Computing
- `stat.ML` - Machine Learning (Statistics)

### Keywords
Add keywords that match your research interests. The bot will search for these terms in paper titles and abstracts.

### Exclude Keywords
Add terms to exclude certain types of papers (e.g., "survey", "review", "tutorial").

## 🔍 Manual Trigger

You can manually trigger the bot by:
1. Going to the "Actions" tab in your repository
2. Selecting "arXiv Bot Daily Update"
3. Clicking "Run workflow"

---
*Generated automatically by arXiv Bot* 
