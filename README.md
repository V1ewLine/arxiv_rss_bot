# arXiv Papers Bot 🤖

This repository automatically fetches and displays relevant papers from arXiv based on configured criteria.

## RSS Vercel Deployment [![An example of deployed RSS Server using vercel](https://img.shields.io/badge/Deployed-Example-blue)](https://arxiv.tachicoma.top/)

You can click this to deploy yours 

[![Deploy with Vercel](https://vercel.com/button)](https://vercel.com/new/clone?repository-url=https://github.com/maydomine/arxiv_rss_bot)
## 📊 Statistics

- **Last Updated**: 2025-12-16 05:56:25 UTC
- **Total Papers Found**: 30
- **Categories Monitored**: cs.AI, cs.CL, cs.DC, cs.LG

## 📚 Recent Papers

### 1. [HetRL: Efficient Reinforcement Learning for LLMs in Heterogeneous Environments](https://arxiv.org/abs/2512.12476)

**Authors**: Yongjun He, Shuai Zhang, Jiading Gai, Xiyuan Zhang, Boran Han, Bernie Wang, Huzefa Rangwala, George Karypis  
**Category**: cs.DC  
**Published**: 2025-12-16  
**Score**: 13.5  
**Type**: new  
**ArXiv ID**: 2512.12476v1  

#### Abstract
As large language models (LLMs) continue to scale and new GPUs are released even more frequently, there is an increasing demand for LLM post-training in heterogeneous environments to fully leverage underutilized mid-range or previous-generation GPUs across regions and alleviate the shortage of homog...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：HetRL: Efficient Reinforcement Learning for LLMs in Heterogeneous Environments**

---

## **1. 论文的主要贡献和创新点**

### **解决了什么问题**
随着大语言模型（LLMs）规模不断扩大以及新型GPU频繁发布，高性能同构GPU资源日益紧缺，而中端或上一代GPU在跨区域数据中心中大量闲置。当前主流的强化学习（RL）训练系统（如 verl、OpenRLHF）均针对**同构GPU环境**设计，无法有效利用这些异构计算资源。

然而，RL训练流程涉及多个模型（actor、critic、reward、reference）和复杂任务依赖关系，在**异构GPU和网络环境**下进行高效调度极具挑战。现有异构调度算法多面向单模型/单任务场景，直接应用于多模型、多任务的RL流程既不实用也不可扩展。

### **提出了什么新方法或新思路**
本文提出 **HetRL**，一个专为异构环境设计的分布式系统，用于高效执行LLM的RL训练。其核心创新包括：

- **将异构环境下的RL调度建模为联合优化问题**：同时优化任务分组（colocation）、并行策略（TP/PP/DP）和细粒度设备分配。
- **提出多级搜索框架（multi-level search framework）**：
  1. **Task Grouping**：将相关任务分组共置；
  2. **Coarse-grained GPU Assignment**：划分GPU组；
  3. **Medium-grained Assignment**：初步分配任务组到具体GPU；
  4. **Intra-model Parallelization**：确定各任务内部并行策略；
  5. **Fine-grained Assignment**：最终细粒度任务块（tasklet）到GPU的映射。
- **基于Successive Halving的预算分配机制**：通过嵌套的Successive Halving Algorithm（SHA）快速淘汰低效候选方案，集中搜索资源于高潜力路径。
- **结合遗传算法（GA）与两级交换策略**：在中/细粒度阶段使用GA，并支持跨任务组和跨tasklet组的GPU交换，提升全局探索能力。
- **集成轻量级负载均衡机制**：动态调整DP组内batch size、长序列分配至强GPU、优化pipeline层分布。

### **相比现有方法的优势**
- **首次专为异构环境下多模型RL流程设计调度系统**，不同于仅支持同构环境的 verl 或仅粗粒度拆分的 StreamRL。
- **搜索效率高**：通过多级分解和SHA避免全空间穷举，显著降低搜索开销。
- **性能提升显著**：在多种异构配置下实现高达 **9.17× 吞吐提升**，平均达 **3.17×**。
- **灵活适配不同RL算法**：支持PPO、GRPO等同步/异步RLHF算法。

---

## **2. 核心实验方法和设置**

### **使用的数据集**
- **GSM8k**：OpenAI发布的数学推理基准数据集，用于评估模型推理能力。

### **实验设置和评估指标**
- **硬件环境**：
  - 总计64块GPU：24×A100（40GB）、24×L40S、16×L4。
  - 模拟四种网络场景以体现异构性：
    1. **Single-Region**：标准单区域集群（无带宽/延迟限制）。
    2. **Multi-Region-Hybrid**：Ohio与Virginia间10ms延迟、5Gbps带宽，部分边缘节点仅1Gbps。
    3. **Multi-Country**：欧洲8个地区，延迟5–30ms，带宽1.9–5.0Gbps。
    4. **Multi-Continent**：欧美跨洲部署，延迟5–60ms，带宽0.9–5.0Gbps。
- **模型与RL算法**：
  - 使用 **Qwen系列**：4B、8B、14B三种规模。
  - 支持 **PPO** 和 **GRPO**，分别测试同步（Sync）与异步（Async）模式。
- **超参数**：
  - 序列长度：1024（输入/输出）
  - 全局batch size：1024
  - 每prompt生成8个response
  - 训练使用混合精度（Adam），推理使用BF16

### **基线方法对比**
- **verl (Sheng et al., 2025)**：当前最先进的同构环境RL训练系统，作为主要baseline。
- **StreamRL (Zhong et al., 2025a)**：唯一支持异构环境的RL系统，将actor generation与其他任务分离部署于不同数据中心。
  - 因未开源，作者在其基础上实现了异步版本并与HetRL对比。

所有变体统一采用 **vLLM** 作为推理引擎、**Megatron-LM** 作为训练引擎，确保公平比较。

---

## **3. 主要实验结果和性能指标**

### **关键性能数据**
- 在总计 **20,000 GPU小时** 的大规模实验中验证性能。
- HetRL在各类场景下均显著优于基线：
  - **最高吞吐提升达 9.17×**（vs. verl）
  - **平均吞吐提升 3.17×**

### **与基线方法的对比结果**
| 场景 | 方法 | 吞吐提升（vs. verl） |
|------|------|------------------|
| **Single-Region** | HetRL-Sync | 1.51–2.05× |
| | HetRL-Async | 1.10–1.31× |
| **Multi-Region-Hybrid** | HetRL-Sync | 3.01–4.99× |
| | HetRL-Async | 4.07–9.17× |
| **Multi-Country** | HetRL-Sync | 1.40–3.07× |
| | HetRL-Async | 1.71–4.00× |
| **Multi-Continent** | HetRL-Sync | 2.24–5.46× |
| | HetRL-Async | 4.38–10.76× |

> 注：HetRL-Async始终优于HetRL-Sync；而verl-Async在某些场景下反而慢于verl-Sync，说明其调度未适配异构环境。

### **消融实验结果**
- **调度算法有效性（图4）**：
  - 对比 verl 调度器与 HetRL（simple）——即禁用SHA、仅模型内交换的简化版。
  - 结果显示：HetRL在相同搜索预算下远超两者，证明**多级搜索+SHA+跨任务GA交换**的有效性。
- **负载均衡影响（图5）**：
  - 引入负载均衡后，吞吐进一步提升：
    - **Single-Region**：最高提升 **12%**
    - **Cross-Region**：最高提升 **18%**
  - 尽管略低于Metis（19–22%），但已验证其有效性，未来可通过引入更先进策略缩小差距。
- **GPU组合影响（图6）**：
  - 当使用全部异构GPU时，HetRL比仅用24×A100同构集群快 **1.57–2.0×**。
  - 表明HetRL能有效整合跨代、跨型号GPU资源，缓解高端GPU短缺问题。

---

## **4. 关键结论和发现**

### **主要发现**
1. **异构环境是加速RL训练的新机遇**：全球范围内存在大量未充分利用的中低端GPU，HetRL成功将其整合用于高性能RL训练。
2. **传统调度方法不适用于多模型RL流程**：单模型调度算法无法应对RL中复杂的跨任务依赖与资源竞争。
3. **HetRL实现了前所未有的吞吐提升**：通过联合优化任务分组、并行策略与设备分配，在真实异构环境中实现平均 **3.17×**、峰值 **9.17×** 的性能超越。
4. **异步训练在HetRL中表现更优**：得益于合理的资源调度，HetRL-Async始终优于Sync模式，而传统系统则可能因调度不当导致性能下降。

### **方法的局限性**
- **GPU支持有限**：目前仅测试NVIDIA A100、L40S、L4三类GPU，尚未支持其他厂商或架构（如AMD、Intel）。
- **通信栈依赖特定技术**：使用AWS OFI NCCL和EFA，未验证其他网络协议栈（如RoCE、InfiniBand）下的表现。
- **未考虑精度兼容性问题**：跨异构GPU交换梯度可能存在数值精度差异，对收敛性的影响未深入研究。
- **成本效益未量化**：虽强调资源利用率，但未提供基于市场价格的成本分析。

### **未来工作方向**
- 扩展支持更多GPU类型和通信协议。
- 集成更先进的负载均衡策略（如Metis中的DFS搜索）。
- 探索异构环境下RL训练的收敛稳定性与精度一致性。
- 构建端到端的成本-性能权衡模型，实现经济高效的RL训练调度。

--- 

> ✅ **总结一句话**：  
> **HetRL 是首个专为异构环境设计的高效LLM强化学习训练系统，通过多级联合优化调度，在真实跨区域异构GPU集群上实现了最高9.17倍的吞吐提升，为解决高端GPU资源短缺提供了可行路径。**

</details>

---

### 2. [Janus: Disaggregating Attention and Experts for Scalable MoE Inference](https://arxiv.org/abs/2512.13525)

**Authors**: Zhexiang Zhang, Ye Wang, Xiangyu Wang, Yumiao Zhao, Jingzhe Jiang, Qizhen Weng, Shaohuai Shi, Yin Chen, Minchen Yu  
**Category**: cs.DC  
**Published**: 2025-12-16  
**Score**: 13.0  
**Type**: new  
**ArXiv ID**: 2512.13525v1  

#### Abstract
Large Mixture-of-Experts (MoE) model inference is challenging due to high resource demands and dynamic workloads. Existing solutions often deploy the entire model as a single monolithic unit, which applies a unified resource configuration to both attention and expert modules despite their different ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# **论文总结：JANUS: Disaggregating Attention and Experts for Scalable MoE Inference**

---

## **1. 论文的主要贡献和创新点**

### **解决了什么问题**
大型 **Mixture-of-Experts (MoE)** 模型在推理过程中面临三大挑战：
- **动态负载**：在线请求的到达率、输入输出长度高度可变，难以满足严格的 **token-level SLO**（如 TPOT）。
- **内存压力大**：MoE 模型中专家参数占总内存的 **90%以上**（见 Table 1），需大量 GPU 才能完整加载。
- **异构资源需求**：Attention 层与 MoE 层计算特性差异显著，但现有系统通常将整个模型作为单一单元部署，采用统一的并行策略（如 DP 和 EP 耦合），导致资源浪费和扩展性差。

这些因素共同构成了一个“三难困境”：**高吞吐、低延迟、高资源利用率难以兼得**。

---

### **提出了什么新方法或新思路**
作者提出 **JANUS**，一种面向大规模 MoE 推理的**解耦式系统架构**，其核心思想是：

> **将 Attention 和 MoE 模块部署在独立的 GPU 子集群上，实现模块级的独立资源管理和弹性伸缩。**

#### **三大关键技术设计**：
1. **自适应两阶段通信机制（Adaptive Two-Phase Communication）**
   - 利用节点内高带宽 NVLink 进行中间结果聚合，减少跨节点小消息数量。
   - 支持两种模式：直接传输（适用于少量目标）和中继广播（适用于多目标），动态选择以最小化通信开销。

2. **基于激活负载均衡的调度器（Activation Load-Balanced Scheduling）**
   - 设计轻量级 GPU 内核实现的调度算法（AEBS），在微秒级完成专家激活请求分配。
   - 目标是最小化每个 MoE 实例上的**被激活专家数**，而非简单平衡 token 数。
   - 通过**去中心化、无同步**方式运行：所有实例使用相同输入独立执行确定性算法，避免跨 GPU 协调开销。

3. **细粒度资源与专家管理（Fine-grained Resource & Expert Management）**
   - 动态调整 Attention 和 MoE 子集群规模，独立控制实例数量。
   - 引入**激活感知的专家复制与放置策略**：
     - 热门专家分配更多副本；
     - 频繁共激活的专家尽量分散到不同 GPU，降低并发活跃专家数。

---

### **相比现有方法的优势**
| 维度 | 传统方法（如 SGLang） | JANUS |
|------|------------------------|-------|
| 架构 | 单体部署（Monolithic） | 解耦部署（Disaggregated） |
| 并行配置 | DP 与 EP 必须匹配 | 可独立配置 |
| 扩展粒度 | 整个模型复制或重启重配 | 按子集群增量伸缩 |
| 专家调度 | 随机或静态绑定 | 动态负载均衡 |
| 资源效率 | 为满足 MoE 需求而过度配置 Attention | 按需分配，避免浪费 |

> ✅ **优势总结**：JANUS 实现了更高的资源利用率、更低的延迟波动、更强的弹性扩展能力。

---

## **2. 核心实验方法和设置**

### **使用的数据集与工作负载**
- **真实请求回放**：基于 `ShareGPT` 数据集，平均输入 16 tokens，输出 256 tokens。
- **合成动态负载**：使用 `BurstGPT` 工具生成符合生产环境特征的流量模式（模拟昼夜变化、突发请求等）。

### **模型配置**
- 主要测试模型：`DeepSeek-V2`（代表性 MoE 模型）
- 衍生变体：`Scaled-DS-1`（160 专家）、`Scaled-DS-2`（200 专家），用于验证泛化性。

### **实验平台**
- 最多 4 个节点，每节点 8 × NVIDIA H100（80GB），NVLink + 400Gbps InfiniBand。
- 所有参数与 KV Cache 使用 BF16 格式。

### **评估指标**
- **Per-GPU Throughput**：单位 GPU 的 token/s，衡量资源效率。
- **TPOT（Time Per Output Token）**：端到端推理延迟，SLO 设定为 200ms。
- **通信开销、调度延迟、负载均衡度**等底层指标。

### **基线方法对比**
| 基线 | 描述 |
|------|------|
| **SGLang** | 当前最先进的单体式 MoE 推理系统，DP 与 EP 耦合，粗粒度扩展（16/32/64 GPU） |
| **DisAgg** | 在 JANUS 代码基础上构建的解耦基线，但使用随机调度 + 节点级（8 GPU）粗粒度扩缩容 |

---

## **3. 主要实验结果和性能指标**

### **关键性能数据**
- **最高提升达 3.9× 的 per-GPU 吞吐量**（vs. SGLang），在满足 200ms TPOT SLO 的前提下。
- 相比 DisAgg，吞吐提升 **2.8×**。
- 在真实动态负载下，**GPU 消耗减少 25%**，同时保持服务质量。

### **与基线方法的对比结果**
| 场景 | 结果 |
|------|------|
| 小批量（4–64 tokens） | JANUS 使用极小 Attention 配置（如 1A6E），集中资源于 MoE，显著优于 SGLang 的过度配置 |
| 大批量（512 tokens） | JANUS 动态增加 Attention 实例（如 8A14E），避免瓶颈；SGLang 仅能从 16→32 GPU 跳跃式扩容，灵活性不足 |
| 高动态负载（BurstGPT） | JANUS 持续微调资源配置，紧密跟踪负载曲线；SGLang 因配置离散，长期处于过配或欠配状态 |

> 📊 图 7 显示：JANUS 在所有 batch size 下均满足 SLO，且 per-GPU throughput 显著领先。

### **消融实验结果**
通过逐步添加 JANUS 的核心组件进行 ablation study（图 10）：

| 配置 | 相对 Base 的 TPOT 改善 |
|------|------------------------|
| **Base**（纯解耦，无优化） | — |
| **+2PC**（加两阶段通信） | ↓18% @ batch=512 |
| **+2PC+LB**（全功能 JANUS） | 再↓7% @ batch=64–256 |

- **两阶段通信**有效缓解通信瓶颈；
- **负载均衡调度**在中小 batch 下效果更明显（因专家激活稀疏）；
- 图 11 显示：JANUS 将最忙与最闲 GPU 的激活专家数差距从 ~8 降至 ~4，显著改善负载均衡。

### **调度开销测试（图 12）**
- 调度延迟始终低于 **100μs**，即使 batch=512 或 16 GPU 规模。
- 开销几乎不随规模增长，证明其适用于大规模部署。

---

## **4. 关键结论和发现**

### **主要发现**
1. **Attention 与 MoE 层具有根本不同的扩展行为**：
   - Attention 在小 batch 下增加实例反而增加延迟（通信主导）；
   - MoE 始终受益于更多实例（内存带宽主导）；
   → 支持**解耦部署**的必要性。

2. **MoE 层是内存受限的（Memory-Bound）**，其延迟主要取决于**被激活的专家数量**，而非 batch size 或激活模式。

3. **细粒度调度与管理至关重要**：
   - 仅解耦不足以解决问题，必须配合**微秒级调度**和**激活感知的专家复制/放置**才能充分发挥潜力。

4. **JANUS 实现了真正的弹性扩展**：
   - 可按需独立调整 Attention/MoE 资源，避免“为 MoE 买单却浪费在 Attention 上”。

---

### **方法的局限性**
- 当前实现假设专家较小且数量多（适合 DP+EP），对极少数超大专家（如单专家 > GPU 显存）支持有限。
- 虽然支持 Tensor Parallelism（TP），但未在实验中深入评估复杂混合并行场景。
- 专家复制与放置策略更新周期较长（小时级），可能无法应对极端快速变化的工作负载。

---

### **未来工作方向**
- 支持更复杂的并行组合（DP+EP+TP）下的自动配置搜索。
- 探索基于预测的 proactive 扩缩容，进一步提升响应速度。
- 将 JANUS 思想应用于 **Prefill-Decoding 解耦**场景，在每个阶段内部再进行 Attention-MoE 解耦。
- 扩展至异构硬件（如 H100 + A100 混合集群），结合 Helix、Hetis 等调度框架。

---

> 🔚 **总结**：  
> **JANUS 是首个将 Attention 与 MoE 完全解耦并实现细粒度、低开销调度与管理的 MoE 推理系统**。它通过三大核心技术突破了传统单体架构的资源效率瓶颈，在真实动态负载下实现了高达 **3.9× 的 per-GPU 吞吐提升**和 **25% 的 GPU 成本节省**，为大规模 MoE 模型的高效服务提供了新的范式。

</details>

---

### 3. [Fine-Grained Energy Prediction For Parallellized LLM Inference With PIE-P](https://arxiv.org/abs/2512.12801)

**Authors**: Anurag Dutt, Young Won Choi, Avirup Sil, Anshul Gandhi, Aruna Balasubramanian, Niranjan Balasubramanian  
**Category**: cs.DC  
**Published**: 2025-12-16  
**Score**: 10.5  
**Type**: new  
**ArXiv ID**: 2512.12801v1  

#### Abstract
With the widespread adoption of Large Language Models (LLMs), energy costs of running LLMs is quickly becoming a critical concern. However, precisely measuring the energy consumption of LLMs is often infeasible because hardware-based power monitors are not always accessible and software-based energy...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Fine-Grained Energy Prediction for Parallelized LLM Inference with PIE-P

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

随着大语言模型（LLMs）在多GPU上的并行推理中广泛应用，其能耗已成为数据中心成本和环境影响的关键因素。然而，现有的能耗预测方法存在以下局限：

- **硬件依赖性强**：基于硬件的功率监测器（如Watts Up Pro）通常不可访问，尤其是在多租户集群中。
- **精度不足**：软件工具（如NVML、CodeCarbon）仅测量GPU功耗，低估系统总能耗，且无法捕捉细粒度通信开销。
- **不适用于并行场景**：现有方法（如IrEne）主要针对单GPU环境设计，在多GPU并行推理（尤其是tensor parallelism）下表现不佳。

因此，本文旨在解决**多GPU并行推理场景下的细粒度、高精度能耗预测难题**。

---

### **提出了什么新方法或新思路**

作者提出 **PIE-P（Parallelized Inference Energy Predictor）**，一个专为多GPU并行推理设计的细粒度能耗预测框架，支持三种主流并行策略：

- **Tensor Parallelism**
- **Pipeline Parallelism**
- **Data Parallelism**

#### **核心创新点包括**：

1. **Synchronization Sampling（同步采样）**
   - 针对tensor parallelism中AllReduce操作带来的非确定性等待时间（GPU间同步延迟），通过多次重复运行捕获等待时间和网络传输时间的分布，从而精确分离“空闲等待”与“实际通信”的能耗。

2. **Expanded Model Tree Abstraction（扩展的模型树抽象）**
   - 在IrEne的基础上，将AllReduce、AllGather、Point-to-Point Transfer等跨GPU通信操作显式建模为模型树中的节点，使能耗预测能覆盖计算与通信全过程。

3. **Structural Model Features（结构化模型特征）**
   - 引入模型架构特征（如`attention heads`, `feed-forward dimension`）来刻画不同并行策略下的通信模式差异，提升泛化能力。

4. **Aggregate Runtime Feature Representation（聚合运行时特征表示）**
   - 对多个GPU的运行时特征（如utilization、clock speed）进行统计聚合（mean, std, min, max），实现可扩展且稳定的输入表示，避免因GPU数量变化导致维度不一致。

---

### **相比现有方法的优势**

| 方法 | 局限性 | PIE-P优势 |
|------|--------|-----------|
| **IrEne** | 单GPU设计，忽略跨GPU通信 | 显式建模AllReduce等通信模块，适用于多GPU |
| **CodeCarbon / NVML** | 仅报告GPU功耗，低估系统总能耗 | 融合系统级监控，预测更接近真实总能耗 |
| **Token-based回归（如Wilkins et al.）** | 忽略硬件动态和并行开销 | 结合运行时与结构特征，适应复杂并行行为 |

---

## 2. 核心实验方法和设置

### **使用的数据集与模型家族**

在四个主流开源LLM家族上进行实验，涵盖多种规模（7B–70B参数）：

- **Vicuna** (7B, 13B, 33B)
- **Mistral** (8B, 24B, 48B)
- **Llama** (7B, 13B, 70B)
- **Qwen** (8B, 14B, 32B)

所有模型均采用**vLLM**作为推理引擎，并支持三种并行策略。

---

### **实验设置**

- **硬件平台**：
  - CPU: AMD EPYC Milan 7543P (32核)
  - GPU: 4 × NVIDIA RTX A6000 (48GB GDDR6, PCIe 4.0)
  - 功率监测：外部电表 Watts Up Pro（用于获取ground truth系统总能耗）

- **并行配置**：
  - 支持2-GPU和4-GPU配置
  - 所有实验在tensor、pipeline、data parallelism三种模式下分别测试

- **输入配置多样性**：
  - Batch size: 8, 16, 32, 64
  - Sequence length: 512, 1024
  - 多次采样以捕捉非确定性行为

---

### **评估指标**

- **MAPE（Mean Absolute Percentage Error）**：主评价指标
  $$
  \text{MAPE} = \frac{1}{n}\sum_{i=1}^{n} \left|\frac{y_i - \hat{y}_i}{y_i}\right| \times 100\%
  $$
- 模型级（model-level）与模块级（module-level）双层评估
- 泛化能力测试：Leave-One-Out Cross-Validation（LOO-CV）

---

### **基线方法对比**

1. **IrEne (Cao et al., 2021)**  
   - 扩展至多GPU版本，使用聚合特征，但未建模跨GPU通信
2. **CodeCarbon (Courty et al., 2024b)**  
   - 基于NVML/RAPL的软件能耗估算工具，广泛用于碳排放追踪
3. **Wilkins et al. (2024)**  
   - 基于输入/输出token数的回归模型：$e_K(T_{in}, T_{out}) = \alpha_0 T_{in} + \alpha_1 T_{out} + \alpha_2 T_{in}T_{out}$

---

## 3. 主要实验结果和性能指标

### **关键性能数据（Tensor Parallelism 下平均 MAPE）**

| 方法 | 平均 MAPE |
|------|----------|
| **PIE-P** | **17.6%** |
| CodeCarbon | 28.49% |
| IrEne | 40.45% |
| Wilkins et al. | 58.77% |

> ✅ PIE-P比最佳基线（CodeCarbon）误差降低 **1.5–3倍**

---

### **不同并行策略下的表现**

| 并行类型 | PIE-P MAPE | 最佳基线 MAPE | 提升倍数 |
|---------|------------|----------------|----------|
| **Tensor Parallelism** | 17.6% | 28.49% (CodeCarbon) | ~1.6× |
| **Pipeline Parallelism** | 13.25% | 36.8% (CodeCarbon) | ~2.8× |
| **Data Parallelism** | 14.36% | 30.25% (CodeCarbon) | ~2.1× |

> 📌 在所有三种并行策略下，PIE-P均显著优于基线

---

### **模块级预测误差（Vicuna, 2/4-GPU）**

| 模块 | 2-GPU MAPE | 4-GPU MAPE |
|------|-----------|-----------|
| Self-Attention | 8.8% | 11.4% |
| MLP | 6.6% | 9.5% |
| **AllReduce** | **17.3%** | **19.4%** |
| LayerNorm | 6.4% | 7.3% |

> 🔍 AllReduce误差较高，源于通信非确定性，但仍保持合理范围

---

### **消融实验结果**

#### **移除Synchronization Sampling的影响**

- **PIE-P（完整版）**: MAPE = **17.6%**
- **PIE-P w/o waiting phase**: MAPE = **36.9%**
- ➡️ 误差翻倍（+19.3个百分点），证明同步等待建模至关重要

#### **移除结构化模型特征的影响（N节）**

| Vicuna变体 | 含结构特征 | 不含结构特征 |
|-----------|-------------|---------------|
| 7B | 15.84% | 17.2% |
| 13B | 17.72% | 18.2% |
| 33B | 17.55% | 20.1% |

> ✅ 结构特征提升泛化能力，尤其在未见模型上效果明显

---

### **泛化能力测试**

#### **Leave-One-Out（LOO）跨尺寸/批大小泛化**

| 类型 | 平均 MAPE |
|------|----------|
| 跨模型大小 | **19.99%** |
| 跨批大小 | **19.05%** |

> 表明PIE-P能有效推广到未见过的模型规模和batch配置

#### **跨架构泛化（Hold-out整个模型家族）**

| 排除家族 | PIE-P MAPE | IrEne MAPE |
|--------|------------|------------|
| Vicuna | 24.1% | 49.3% |
| Mistral | 27.0% | 56.5% |
| Llama | 26.1% | 55.3% |
| Qwen | 27.6% | 58.4% |

> ✅ PIE-P在跨架构场景下仍保持良好预测能力，相对IrEne提升超50%

---

## 4. 关键结论和发现

### **主要发现**

1. **并行推理中的通信能耗不可忽视**  
   - AllReduce在tensor parallelism中占比可达 **15%–35%**（见Appendix C）
   - 忽略通信将导致系统性低估，且误差随GPU数量增加而加剧

2. **非确定性同步是预测难点**  
   - GPU间计算速度差异导致AllReduce阶段出现“领先/落后”现象
   - 通过离线采样建模等待时间分布是提高精度的关键

3. **结构化特征增强泛化能力**  
   - attention heads、FFN dim等结构信息有助于捕捉不同模型间的通信模式差异

4. **PIE-P具备良好的可扩展性和实用性**  
   - 预测过程无运行时开销（offline profiling）
   - 可用于指导模型部署决策（如权衡latency vs. energy）

---

### **方法的局限性**

1. **硬件依赖性强**  
   - 当前PIE-P需依赖特定硬件配置进行训练，缺乏跨平台通用性

2. **仅支持Decoder-only架构**  
   - 尚未扩展至Encoder或Encoder-Decoder模型（如T5、BART），因其双向注意力和不同执行流带来额外复杂性

3. **对高度复杂的注意力机制敏感**  
   - Mistral（Grouped-Query Attention）、Qwen（Multi-Query）等复杂结构预测误差更高（MAPE >20%）

---

### **未来工作方向**

1. **开发硬件无关的能耗预测模型**（作者明确列为下一步工作）
2. **扩展至Encoder-based和Encoder-Decoder模型**
3. **进一步建模复杂注意力机制的通信行为**
4. **结合DVFS、调度策略等优化手段，构建端到端节能推理系统**

---

> 💡 **一句话总结**：  
> PIE-P首次实现了对多GPU并行LLM推理的**细粒度、高精度、可泛化的能耗预测**，通过引入**同步采样、扩展模型树、结构化特征**三大机制，在tensor、pipeline、data parallelism下均显著超越现有方法，为绿色AI和能效优化提供了重要工具。

</details>

---

### 4. [Optimal Resource Allocation for ML Model Training and Deployment under Concept Drift](https://arxiv.org/abs/2512.12816)

**Authors**: Hasan Burhan Beytur, Gustavo de Veciana, Haris Vikalo, Kevin S Chan  
**Category**: cs.LG  
**Published**: 2025-12-16  
**Score**: 10.5  
**Type**: new  
**ArXiv ID**: 2512.12816v1  

#### Abstract
We study how to allocate resources for training and deployment of machine learning (ML) models under concept drift and limited budgets. We consider a setting in which a model provider distributes trained models to multiple clients whose devices support local inference but lack the ability to retrain...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Optimal Resource Allocation for ML Model Training and Deployment under Concept Drift

---

## 1. 论文的主要贡献和创新点

### 解决的问题
本文研究在**概念漂移**（Concept Drift）环境下，机器学习模型训练与部署中的**资源分配优化问题**。具体场景为：
- 一个中心化的服务提供商负责训练模型并分发给多个客户端；
- 客户端仅支持本地推理，无法重新训练模型；
- 模型性能因数据分布变化而随时间退化；
- 服务商需在有限预算下决定何时、如何分配计算资源进行再训练，并决定何时向客户端推送更新。

该问题在现实世界中广泛存在，如推荐系统、医疗诊断、金融风控等持续演进的数据环境中。

---

### 提出的新方法与新思路

作者提出了一种**模型无关的分析框架**（model-agnostic framework），统一建模以下三个关键因素之间的交互关系：
- **训练资源分配策略**（Training Resource Allocation）
- **概念漂移动态**（Concept Drift Dynamics）
- **模型部署调度**（Model Deployment Scheduling）

#### 主要理论贡献包括：

1. **最优训练控制策略的结构性分析**
   - 将训练资源分配建模为连续时间最优控制问题（Optimal Control），目标是最小化服务器端长期平均期望损失。
   - 引入 **Pontryagin’s Maximum Principle (PMP)** 分析最优解结构。
   - 发现最优策略是 **bang-bang 控制** 类型，即资源要么全开（M），要么关闭（0）。

2. **揭示概念持续时间的统计特性对策略的影响**
   - 定义并利用 **Mean Residual Life (MRL)** 函数来刻画概念寿命的“老化”行为：
     - **DMRL**（Decreasing MRL）：寿命越长越可能结束（如机械故障）；
     - **IMRL**（Increasing MRL）：存活越久越稳定（如技术趋势、企业生命周期）。
   - **关键定理**：
     - 当概念持续时间服从 **DMRL 分布**时，**前载式训练策略**（Front-loading，初期集中投入资源）是最优的。
     - 当概念持续时间服从 **IMRL 分布**时，**后载式策略**（Back-loading，延迟开始训练）更优，且直观的固定分配策略（Fixed Allocation）是次优的。

3. **部署调度优化与随机化策略设计**
   - 在通信带宽受限（即部署频率受限）条件下，研究客户端侧性能最大化问题。
   - 证明在温和假设下，部署优化问题是 **quasi-convex** 的。
   - 提出一种 **随机化部署策略**（Randomized Deployment Policy）：
     - 结合两个相邻部署次数下的确定性最优调度器；
     - 通过凸组合实现精确匹配部署率约束；
     - 具有理论性能保证，适用于非凸生存函数情形。

---

### 相比现有方法的优势

| 维度 | 传统方法 | 本文优势 |
|------|--------|---------|
| **建模范式** | 多为经验性或特定模型驱动 | 提出通用、可解释的数学框架，适用于各类ML模型 |
| **资源分配逻辑** | 固定周期重训 / 变化检测触发 | 基于概念寿命统计特性的动态最优策略 |
| **理论深度** | 缺乏对“何时训练最好”的根本解释 | 利用MRL理论给出结构性最优条件 |
| **部署调度** | 周期性或事件驱动 | 支持速率约束下的近似最优调度 |

---

## 2. 核心实验方法和设置

### 数据集
本文未使用真实数据集进行端到端训练，而是基于**仿真环境**验证理论结果。其核心依赖的是：
- **合成的概念漂移过程**：由随机变量 $ Y_i $ 表示每个概念的持续时间；
- **期望损失函数 $ g(t) $**：模拟训练过程中模型性能提升的过程。

使用的典型分布包括：
- **Exponential**(λ)：无记忆性，同时属于 DMRL 和 IMRL 边界情况；
- **Weibull**(λ, k)：通过形状参数 $ k $ 控制 MRL 特性：
  - $ k < 1 $：IMRL；
  - $ k > 1 $：DMRL；
- **Gamma**, **Uniform**, **Erlang-2** 等用于多样性测试。

---

### 实验设置与评估指标

#### （1）训练资源分配实验
- **目标**：比较不同资源分配策略下的长期平均期望损失（Time-average Expected Loss）。
- **预算约束**：总资源消耗受平均成本限制 $ B $。
- **策略对比**：
  - **Optimal Policy**：根据 MRL 性质选择 front-/back-loading；
  - **Fixed Allocation**：均匀分配资源（$ e(t) = B/\alpha $）；
  - **Delayed Block Allocation**：延迟一段时间后再集中使用资源。

#### （2）部署调度实验
- **目标**：最小化客户端侧长期平均损失 $ \mathbb{E}[L_c(t)] $。
- **约束**：最大允许部署频率 $ r_p $。
- **策略对比**：
  - **Periodic Policy**：定期部署（baseline）；
  - **Optimal Policy**：数值求解得到的最优静态调度；
  - **Randomized Policy**：基于相邻 $ N_D $ 和 $ N_D+1 $ 部署数的凸组合。

#### 评估指标
- **主指标**：长期平均期望损失（Lower is better）
- **辅助指标**：
  - 资源利用率；
  - 部署有效率（Effective Deployment Rate）；
  - 性能增益百分比（vs baseline）

---

## 3. 主要实验结果和性能指标

### （1）训练资源分配结果（图2）

| 场景 | 最优策略 | vs Fixed Allocation 提升 |
|------|----------|--------------------------|
| $ Y \sim \text{Exp}(1), g(t)=e^{-t} $ | Front-loading (DMRL) | **最高达 71.80%** 损失降低 |
| $ Y \sim \text{Weibull}(k=0.4), g(t)=e^{-t} $ | Back-loading (IMRL) | 引入延迟显著优于立即训练 |
| 不同 $ g(t) $ 曲线（见附录图4） | —— | 前期衰减越快，front-loading 收益越大 |

> ✅ **关键观察**：当预算较低时，front-loading 在 DMRL 下优势最明显；随着预算增加，差距缩小。

---

### （2）部署调度结果（图3）

| 分布类型 | Periodic vs Optimal Loss Reduction | Randomized vs Optimal 差距 |
|---------|------------------------------------|----------------------------|
| Exponential ($ \mathbb{E}[Y]=1 $) | **43.30%** | 几乎无差距（near-optimal） |
| Weibull($k=2$)（DMRL） | **39.25%** | 极小差距 |
| Erlang-2（低方差） | 较小（约10–15%） | 存在可测差距 |

> ✅ **关键发现**：
> - Randomized Policy 在多数情况下表现接近最优；
> - 在高变异性（high variance）和快速损失下降场景中优势显著；
> - 对于低变异分布（如 Erlang-2），所有策略差异较小。

---

### 消融实验（Ablation Study）
虽然没有明确命名“消融”，但以下分析起到了类似作用：
- **改变 $ g(t) $ 形式**（附录图4）：
  - $ g(t) = 1/(1+t), 1/\sqrt{1+t}, t^{-0.3}, t^{-0.7} $
  - 发现：初始下降越陡峭，front-loading 效果越好。
- **不同 MRL 类型对比**（图2b）：
  - 显示 IMRL 下引入 delay 可提升性能，而 DMRL 下则有害。
- **预算敏感性分析**：
  - 低预算时优化策略收益最大；
  - 高预算下各策略趋同。

---

## 4. 关键结论和发现

### 主要发现

1. 🔑 **最优训练策略取决于概念寿命的MRL性质**：
   - **DMRL ⇒ Front-loading 最优**
   - **IMRL ⇒ Back-loading 更优**
   - 忽视这一特性的启发式策略（如固定分配）是**理论上可证次优**的。

2. 🔑 **部署调度问题具有 quasi-convex 结构**，可在一定条件下高效求解。

3. 🔑 **提出的随机化部署策略**能在不依赖复杂非凸优化的前提下，逼近最优性能，适合实际系统集成。

4. 🔑 **性能增益高度依赖于系统参数**：
   - 概念寿命方差越大，优化空间越大；
   - 损失函数前期下降越快，front-loading 越有利；
   - 低预算场景下优化价值最高。

---

### 方法的局限性

| 局限性 | 说明 |
|-------|------|
| **依赖分布先验知识** | 需要知道或估计 $ Y $ 的分布及 MRL 特性，在实践中可能需要额外监控与统计检验 |
| **理想化损失模型** | 使用“期望概念损失”作为代理指标，忽略采样噪声和任务特定metric（如F1-score） |
| **静态策略假设** | 所有策略均为静态（stationary），未考虑在线自适应调整 |
| **未涉及联合优化** | 训练与部署分开处理，未探索二者协同优化的可能性 |

---

### 未来工作方向

1. **在线估计 MRL 特性**：开发轻量级算法实时判断当前漂移模式属于 DMRL 还是 IMRL。
2. **自适应调度器**：从静态策略扩展到动态响应机制，结合 change detection 输出。
3. **联合训练-部署优化**：构建统一框架同时优化 $ e(t) $ 和 $ D_{i,j} $。
4. **应用于真实系统**：在边缘计算、IoT、推荐系统中部署验证。
5. **纳入能耗与通信代价**：将 energy cost、latency 等纳入多目标优化。

---

> 📌 **总结一句话**：  
> 本文首次从**可靠性理论中的MRL视角**出发，揭示了概念漂移下资源分配的结构性最优规律，并提出了兼具理论严谨性与工程可行性的训练与部署策略，为 MLOps 系统的自动化与成本效率提供了新的理论基础。

</details>

---

### 5. [Deep Q-Learning-Based Intelligent Scheduling for ETL Optimization in Heterogeneous Data Environments](https://arxiv.org/abs/2512.13060)

**Authors**: Kangning Gao, Yi Hu, Cong Nie, Wei Li  
**Category**: cs.LG  
**Published**: 2025-12-16  
**Score**: 10.5  
**Type**: new  
**ArXiv ID**: 2512.13060v1  

#### Abstract
This paper addresses the challenges of low scheduling efficiency, unbalanced resource allocation, and poor adaptability in ETL (Extract-Transform-Load) processes under heterogeneous data environments by proposing an intelligent scheduling optimization framework based on deep Q-learning. The framewor...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Deep Q-Learning-Based Intelligent Scheduling for ETL Optimization in Heterogeneous Data Environments

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
该论文针对**异构数据环境下的ETL（Extract-Transform-Load）流程中存在的调度效率低、资源分配不均、适应性差**等关键挑战，提出了一种基于深度强化学习的智能调度优化框架。传统静态或启发式调度策略难以应对动态任务依赖、多源异构数据输入以及资源竞争等问题，导致系统吞吐量下降、延迟增加。

### 🚀 提出的新方法/新思路
作者提出一个**基于Deep Q-Learning（DQL）的智能ETL调度优化框架**，其核心创新包括：

- 将ETL调度过程建模为**Markov Decision Process (MDP)**，利用reinforcement learning实现自适应决策。
- 设计了一个包含**状态表示模块、特征嵌入网络、Q值估计器和奖励评估机制**的完整DQN架构。
- 引入**多目标归一化reward函数**，综合平衡平均调度延迟（ASD）、任务完成率（TCR）、吞吐量（TP）和资源消耗（RC）等多个性能指标。
- 利用**非线性特征映射函数**对高维异构状态空间进行有效编码，提升模型在复杂环境中的表征能力。

### 🔍 相比现有方法的优势
相比传统的规则驱动或启发式算法，以及现有的RL方法（如Q-Learning、DDQN、A3C等），本文提出的DQL框架具有以下优势：

- 能够在**高维、动态、异构的状态空间中稳定收敛**，避免Q值震荡。
- 支持**端到端的自适应调度决策**，无需人工干预即可响应环境变化。
- 在多节点异构集群中展现出更强的**可扩展性和鲁棒性**。
- 实现了**延迟与资源利用率之间的最优权衡**，显著提升了系统整体性能。

---

## 2. 核心实验方法和设置

### 📊 使用的数据集
- **TPC-H Benchmark Dataset**：作为标准测试数据源，模拟企业级数据仓库场景。
  - 包含多个关系型表（如ORDERS、CUSTOMER、LINEITEM），通过外键形成复杂的依赖图。
  - 数据涵盖结构化表格和半结构化流式记录，体现真实环境中多源异构数据的特点。
  - 可调节数据规模和复杂度，支持从小规模本地调度到大规模分布式调度的验证。

### ⚙️ 实验设置与评估指标
#### 实验配置：
- 构建多阶段ETL调度图，每个任务节点对应清洗、聚合、转换等操作。
- 模拟不同计算能力、存储带宽和网络延迟的异构节点集群。
- Agent持续与环境交互，学习最优调度策略。

#### 评估指标：
| 缩写 | 含义 |
|------|------|
| **ASD ↓** | Average Scheduling Delay（平均调度延迟），越小越好 |
| **TCR ↑** | Task Completion Rate（任务完成率），越高越好 |
| **TP ↑** | Throughput（吞吐量），越高越好 |
| **RC ↓** | Resource Consumption（资源消耗），越低越好 |

### 🆚 基线方法对比
论文对比了多种主流强化学习调度算法作为baseline：
- **Q-Learning [15]**：经典tabular方法，适用于小状态空间
- **DDQN [16]**：引入target network提升稳定性
- **A3C [17]**：异步策略梯度方法，提高样本效率
- **DDPG [18]**：连续动作空间actor-critic方法
- **SAC [19]**：基于熵正则化的off-policy方法
- **PPO [20]**：稳定策略更新的on-policy方法

---

## 3. 主要实验结果和性能指标

### 📈 关键性能数据（见Table 1）
| Method | ASD ↓ | TCR ↑ (%) | TP ↑ | RC ↓ |
|--------|-------|-----------|------|-------|
| Q-Learning | 3.84 | 87.62 | 264.1 | 0.132 |
| DDQN | 3.22 | 90.18 | 278.4 | 0.115 |
| A3C | 3.06 | 91.25 | 283.7 | 0.109 |
| DDPG | 2.94 | 92.04 | 289.6 | 0.101 |
| SAC | 2.81 | 93.16 | 295.8 | 0.094 |
| PPO | 2.77 | 93.54 | 297.3 | 0.091 |
| **Ours (DQL-based)** | **2.43** | **95.82** | **312.7** | **0.079** |

> ✅ 所有指标全面领先，尤其在**ASD降低13.7%、TP提升约5%、RC降低13.2%**方面表现突出。

### 🔬 敏感性分析（Sensitivity Analysis）
#### （1）学习率（Learning Rate）影响（Figure 2）
- 过低（≤5e-5）：更新缓慢，reward增长慢；
- 中等（1e-4 ~ 5e-4）：收敛快且稳定，reward最高；
- 过高（≥1e-3）：梯度震荡，训练不稳定。

👉 最佳学习率区间为 `1e-4` 至 `5e-4`。

#### （2）折扣因子 γ 对ASD的影响（Figure 3）
- γ ≤ 0.85：过度关注短期收益，忽略长期依赖 → 高延迟；
- γ ≈ 0.9–0.95：短期响应与长期规划平衡 → **ASD最低**；
- γ ≥ 0.97：过于依赖长期预测，对实时状态不敏感 → 延迟回升。

👉 推荐 γ 设置在 **0.9~0.95** 范围内。

#### （3）异构节点数量对ASD的影响（Figure 4）
- 节点数 < 8：资源不足，排队严重 → 高延迟；
- 节点数 ≈ 8：并行性与任务粒度最佳匹配 → **ASD最小**；
- 节点数 > 10：通信开销、同步成本上升 → 延迟再次升高。

👉 存在“U型”关系，表明**并非节点越多越好**，需权衡分布代价。

> ❗尽管未明确提及消融实验（ablation study），但从多组参数敏感性分析中可推断出各组件的重要性，例如：
> - 特征嵌入网络有助于捕捉异构特征间的非线性关系；
> - 多目标reward设计是实现延迟与资源平衡的关键；
> - Target network机制保障了训练稳定性。

---

## 4. 关键结论和发现

### ✅ 主要发现
1. **Deep Q-Learning能够有效解决异构环境下ETL调度的复杂决策问题**，将调度转化为MDP建模，实现了动态自适应优化。
2. 所提框架在**延迟、吞吐量、资源利用率和任务完成率上均显著优于现有RL方法**，证明了其在实际数据管道中的应用潜力。
3. 通过合理的reward设计和网络结构，模型能在**高维状态空间中稳定收敛**，具备良好的泛化能力和鲁棒性。
4. 实验验证了**适度的并行资源规模（约8个节点）最有利于性能优化**，过多节点反而引入额外开销。

### ⚠️ 方法的局限性
- 当前模型假设任务图结构相对固定，对于**频繁变更的任务拓扑**可能需要重新训练或引入meta-RL机制。
- 使用DQN处理离散动作空间，在某些细粒度资源分配场景下可能存在表达能力限制（未来可结合DDPG/SAC）。
- 实验基于TPC-H仿真环境，尚未在真实生产级云平台部署验证，存在一定的**现实差距（sim-to-real gap）**。

### 🔮 未来工作方向
1. 结合**Graph Neural Networks (GNNs)** 建模任务依赖图结构，增强对动态拓扑变化的适应能力。
2. 引入**Meta-Reinforcement Learning** 或 **Self-Supervised Representation Learning**，提升跨环境迁移能力。
3. 扩展至**multi-agent setting**，实现跨区域、跨集群的协同调度。
4. 加入**energy-aware** 和 **cost-aware** 目标，服务于绿色计算与云计算成本优化。
5. 探索与**LLM-driven workflow generation** 的集成，构建全自动智能data pipeline。

---

## 总结
本文提出了一种基于**Deep Q-Learning的智能ETL调度框架**，成功解决了异构数据环境下调度效率低、资源利用不均衡等问题。通过将ETL调度建模为MDP，并设计高效的state representation与multi-objective reward机制，该方法在TPC-H基准上取得了全面优于现有RL方法的性能表现。研究不仅为智能数据工程提供了可扩展的技术路径，也为reinforcement learning在real-world data pipeline optimization中的落地奠定了坚实基础。

</details>

---

### 6. [CXL-SpecKV: A Disaggregated FPGA Speculative KV-Cache for Datacenter LLM Serving](https://arxiv.org/abs/2512.11920)

**Authors**: Dong Liu, Yanxuan Yu  
**Category**: cs.AI  
**Published**: 2025-12-16  
**Score**: 10.0  
**Type**: new  
**ArXiv ID**: 2512.11920v1  

#### Abstract
Large Language Models (LLMs) have revolutionized natural language processing tasks, but their deployment in datacenter environments faces significant challenges due to the massive memory requirements of key-value (KV) caches. During the autoregressive decoding process, KV caches consume substantial ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：CXL-SpecKV: A Disaggregated FPGA Speculative KV-Cache for Datacenter LLM Serving

---

## 1. 论文的主要贡献和创新点

### 解决的问题
大型语言模型（LLM）在数据中心推理过程中面临**KV-cache内存墙**问题。在自回归解码阶段，Key-Value缓存占用大量GPU显存（例如LLaMA-2 70B模型可达640GB），远超单卡容量（如A100为80GB），严重限制批处理大小（batch size）和系统吞吐量。

传统方法存在以下不足：
- **Memory offloading**（如FlexGen）受限于PCIe带宽低、延迟高；
- **KV-cache压缩**（如INT8量化）虽节省内存但牺牲精度；
- **Speculative decoding** 提升计算效率但加剧内存压力。

---

### 提出的新方法与创新思路
作者提出 **CXL-SpecKV** ——一种基于 **CXL（Compute Express Link）** 和 **FPGA加速器** 的**解耦式（disaggregated）KV-cache架构**，结合**推测性预取（speculative prefetching）** 技术，实现高效、低成本的大规模LLM服务。

#### 核心创新点：
1. **CXL-based 内存解耦框架**
   - 利用CXL 2.0协议将KV-cache从GPU HBM迁移至远程FPGA连接的高带宽内存（HBM），提供4–8×内存容量扩展。
   - 支持缓存一致性（cache coherence），确保GPU与FPGA间数据同步。

2. **Speculative KV-Cache Prefetching**
   - 设计轻量级LSTM预测模型（仅128K参数），根据历史token序列预测未来k个token，并提前预加载其对应的KV-cache条目。
   - 预测准确率高达95%（top-4），有效隐藏CXL访问延迟。

3. **FPGA加速的KV-cache压缩引擎**
   - 在FPGA上实现高效的压缩/解压流水线，支持INT8量化 + Delta Encoding + Run-Length Encoding（RLE）。
   - 压缩比达3–4×，且不消耗GPU计算资源。

4. **软硬件协同设计与系统集成**
   - 完整原型系统集成到主流LLM框架（vLLM、TensorRT-LLM）中，通过自定义内存分配插件无缝替换原生管理器。
   - 实现透明化KV-cache管理，无需修改模型代码。

---

### 相比现有方法的优势
| 方法 | 局限性 | CXL-SpecKV优势 |
|------|--------|----------------|
| GPU-only (vLLM) | 显存有限，batch size受限 | 扩展4–8×内存，支持更大batch |
| CPU/Disk Offload (FlexGen) | PCIe带宽低（~16GB/s），延迟高（3–5μs） | CXL带宽64GB/s，延迟<400ns |
| KV压缩（INT8等） | 精度损失，无法突破物理显存上限 | 结合压缩+解耦，实现24×有效容量 |
| Speculative Decoding（Medusa等） | 增加多个候选序列的KV-cache副本，加重内存负担 | 推测“访问需求”而非“token值”，互补且更省内存 |

---

## 2. 核心实验方法和设置

### 使用的数据集与模型
- **模型范围**：涵盖多种主流LLM架构，参数从1.8B到72B：
  - LLaMA-2 / LLaMA-3（7B–70B）
  - Qwen（1.8B–72B）
  - Mistral / Mixtral（MoE结构）
  - Gemma（2B–7B）
  - CodeLLaMA / CodeQwen（代码生成）
- **激活精度**：FP16；权重使用SmoothQuant进行INT8量化。

### 工作负载类型
四种典型场景，输入/输出长度不同：
- **Chatbot**：128/256
- **Summarization**：1024–2048 / 128–256
- **Code Generation**：256–512 / 512–1024
- **QA**：64–128 / 32–64  
请求到达服从泊松分布（λ ∈ [5, 100] req/s）

---

### 实验平台配置
- **GPU**：8× NVIDIA A100 80GB（HBM带宽1.6TB/s）
- **FPGA**：4× Intel Agilex-7（每片64GB HBM，CXL 2.0控制器，x16链路，64GB/s带宽）
- **CPU**：双路Sapphire Rapids（96核，1TB DDR5）
- **互连**：GPU ↔ FPGA via PCIe Gen4 ×16；FPGA ↔ CXL内存扩展器

---

### 评估指标
| 指标 | 描述 |
|------|------|
| **Throughput (O)** | tokens/sec（稳态） |
| **Latency (L)** | TTFT（首token延迟）、decode latency（逐token延迟），含median/P95/P99 |
| **Memory Efficiency** | 可支持的最大batch size、单位请求成本 |
| **Accuracy** | Perplexity（WikiText-103）、BLEU分数 vs FP16基准 |
| **Energy Efficiency** | J/token（能耗） |
| **Scalability** | 多GPU/FPGA下的并行效率 |

---

### 基线方法对比
| 基线 | 描述 |
|------|------|
| **GPU-Only** | 使用vLLM，默认HBM存储KV-cache |
| **CPU Offload** | FlexGen方案，通过PCIe将KV-cache卸载至CPU内存 |
| **NVMe Offload** | 卸载至SSD，成本低但延迟极高 |
| **Compression-Only** | GPU本地INT8压缩，无解耦 |
| **CXL-NoSpec** | 本系统但关闭推测预取功能 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（以LLaMA-2 70B为例）

| 指标 | CXL-SpecKV | GPU-Only | 提升/变化 |
|------|------------|----------|-----------|
| **Throughput** | 1,549 tokens/s | 487 tokens/s | **↑3.2×** |
| **Max Batch Size** | 128 | 16 | **↑8×** |
| **Decode Latency** | 19.8 ms | 18.3 ms | +8.2% |
| **P99 Latency** | 23.8 ms | 21.2 ms | +12.3% |
| **TTFT** | 47.1 ms | 45.2 ms | +4.2% |
| **Memory Cost per Request** | — | — | **↓2.8×** |
| **Energy Efficiency (J/token)** | 0.340 | 0.647 | **↑1.90× 更优** |
| **Parallel Efficiency @8 GPUs** | — | — | **87%** |

> 注：启用压缩后（CXL-SpecKV+Comp），最大batch可达384（**24×扩展**）

---

### 与各基线对比结果
- **vs GPU-Only**：平均**2.4×吞吐提升**（2.1–3.2×区间），尤其对大模型显著。
- **vs CPU Offload**：**4.3×更高吞吐**，得益于CXL更高带宽（64GB/s vs 16GB/s）和更低延迟。
- **vs Compression-Only**：仍高出**1.8–2.3×**，因后者受GPU物理内存限制。
- **vs CXL-NoSpec**：**1.6–2.1×性能增益**，证明推测预取是隐藏延迟的关键。

---

### 消融实验结果

#### （1）推测预取深度 $k$ 影响（Table 8）
| $k$ | Hit Rate (%) | Precision (%) | Throughput (tok/s) |
|-----|--------------|---------------|--------------------|
| 1 | 87.2 | 96.1 | 1,287 |
| 2 | 92.3 | 93.5 | 1,426 |
| **4** | **94.7** | **87.2** | **1,549** ✅ |
| 8 | 95.8 | 78.3 | 1,573 |
| 16 | 96.4 | 68.7 | 1,581 |

> 最佳平衡点为 $k=4$，继续增加收益递减且浪费带宽。

#### （2）压缩组件消融（Table 9）
| 方案 | 压缩比 | Perplexity 增加 |
|------|--------|------------------|
| FP16（无压缩） | 1.00× | 0% |
| INT8 Only | 2.00× | +0.6% |
| +Delta Encoding | 2.73× | +0.9% |
| **+RLE（完整）** | **3.21×** | **+1.2%** ✅ |

> Delta编码贡献最大（+36%压缩比），RLE进一步优化。

---

## 4. 关键结论和发现

### 主要发现
1. **内存解耦 + 推测预取可有效突破LLM推理内存瓶颈**  
   CXL-SpecKV首次将CXL用于LLM KV-cache管理，验证了解耦架构在AI推理中的可行性。

2. **推测性预取能高效隐藏CXL延迟**  
   轻量LSTM模型实现95%预测准确率，使内存访问时间从8.7ms（CXL-NoSpec）降至2.1ms，**掩盖76%延迟开销**。

3. **FPGA加速压缩显著降低带宽压力**  
   3–4×压缩比减轻CXL链路负担，使其不再是系统瓶颈。

4. **成本效益显著提升**  
   使用廉价FPGA扩展内存，相比全高端GPU集群，**基础设施成本降低30–40%**，性价比提升1.75–2.2×。

5. **良好可扩展性**  
   在8-GPU配置下达到87%并行效率，支持大规模部署。

---

### 方法的局限性
1. **依赖CXL硬件生态普及**：当前CXL设备尚未广泛部署，需厂商支持。
2. **预测错误导致带宽浪费**：虽然精度高，但仍有约13%的prefetch未被使用。
3. **FPGA开发门槛较高**：RTL设计与综合需要专业知识，影响推广速度。
4. **多租户隔离机制未深入探讨**：共享CXL内存池时的安全与QoS保障有待加强。

---

### 未来工作方向
1. **动态稀疏注意力机制适配**：结合activation sparsity进一步减少KV-cache体积。
2. **长上下文优化（32K–128K）**：采用分层预测策略应对更复杂访问模式。
3. **训练/微调场景扩展**：支持双向注意力与梯度更新。
4. **CXL 3.0升级**：利用128GB/s带宽与memory-side caching能力。
5. **异构内存层级**：融合HBM/DDR/NVM构建多级存储体系。
6. **安全与隔离机制**：防止跨租户内存泄露，支持多租户QoS。
7. **与其他互联技术比较**：如NVLink-C2C、Infinity Fabric等。

---

> ✅ **开源信息**：作者已将代码开源 → [GitHub链接](https://github.com/FastLM/CXL-SpecKV)

> 📌 **一句话总结**：CXL-SpecKV通过**CXL内存解耦 + FPGA压缩 + 推测预取**三位一体设计，在几乎不影响精度的前提下，实现了**3.2×吞吐提升、8×内存扩展、2.8×成本下降**，为下一代数据中心LLM服务提供了高效、可持续的解决方案。

</details>

---

### 7. [FlashFuser: Expanding the Scale of Kernel Fusion for Compute-Intensive Operators via Inter-Core Connection](https://arxiv.org/abs/2512.12949)

**Authors**: Ziyu Huang, Yangjie Zhou, Zihan Liu, Xinhao Luo, Yijia Diao, Minyi Guo, Jidong Zhai, Yu Feng, Chen Zhang, Anbang Wu, Jingwen Leng  
**Category**: cs.DC  
**Published**: 2025-12-16  
**Score**: 9.5  
**Type**: new  
**ArXiv ID**: 2512.12949v1  

#### Abstract
The scaling of computation throughput continues to outpace improvements in memory bandwidth, making many deep learning workloads memory-bound. Kernel fusion is a key technique to alleviate this problem, but the fusion strategies of existing compilers and frameworks are limited to using local scratch...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：FlashFuser: Expanding the Scale of Kernel Fusion for Compute-Intensive Operators via Inter-Core Connection

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**

现代深度学习模型（尤其是大语言模型 LLM 和卷积神经网络 CNN）中，**计算吞吐量的增长远超内存带宽的增长**，导致许多工作负载成为 **memory-bound**（内存瓶颈）。尽管 **kernel fusion** 是缓解该问题的有效手段，但现有编译器和框架受限于单个 SM（Streaming Multiprocessor）的 **shared memory (SMEM)** 容量，当中间结果过大时（如 FFN 层），无法进行融合，必须回退到全局内存（global memory），造成大量冗余的数据读写。

此外，虽然现代 GPU（如 NVIDIA H100）引入了 **Distributed Shared Memory (DSM)**，通过多 SM 间的互连提供更大、更高带宽、更低延迟的片上内存池，但当前软件框架尚未有效利用这一硬件特性。

### **提出了什么新方法或新思路**

为填补硬件潜力与软件能力之间的鸿沟，本文提出 **FlashFuser**，这是首个在现代 GPU 上利用 **inter-core connection**（即 DSM）实现大规模 kernel fusion 的编译器框架。其三大核心贡献如下：

#### **(1) 基于 DSM 的通信抽象 `dsm_comm` primitive**
- 提出了一套统一的通信原语（如 `dsm_all_exchange`, `dsm_shuffle`, `dsm_reduce_scatter`），用于形式化描述基于集群（cluster）的复杂数据交换模式（如 reduce、shuffle、multiply）。
- 支持灵活配置 **cluster size** 和 **shuffle/reduce 维度**，适应不同问题规模和硬件拓扑。

#### **(2) 数据流分析器（Dataflow Analyzer）**
- 将传统的 loop scheduling、资源映射和 tile 选择扩展到分布式内存层次（register → SMEM → DSM → L2/global）。
- 能够量化跨内存层级的数据移动开销，并决定最优的执行顺序和 tile 大小，实现从高速缓存向低速缓存的渐进式溢出（spilling）。

#### **(3) 融合搜索引擎（Fusion Search Engine）**
- 构建了一个高效的搜索系统，结合 **解析式成本模型**（analytical cost model）和 **DSM-aware 的剪枝策略**，在由 DSM 引入的巨大搜索空间中快速找到最优执行计划。
- 显著降低了传统暴力搜索的时间开销（加速 12–68 倍）。

### **相比现有方法的优势**

| 特性 | 现有方法（如 Chimera, BOLT） | FlashFuser |
|------|-------------------------------|-----------|
| 内存层级支持 | 仅 reg / SMEM | 支持 reg / SMEM / **DSM** |
| 融合范围 | 受限于单 SM 的 SMEM 容量 | 利用 DSM 扩展融合边界，支持更大算子链 |
| 通信建模 | 忽略跨 SM 通信 | 显式建模 DSM 通信模式与开销 |
| 搜索效率 | 手动调优或有限探索 | 分析驱动 + 高效剪枝，自动化寻优 |

---

## 2. 核心实验方法和设置

### **使用的数据集与子图配置**

实验覆盖多种典型计算密集型算子链，具体配置见下表：

| 类型 | 示例模型/任务 | 配置来源 |
|------|----------------|----------|
| **GEMM Chain** | GPT-6.7B, OPT-1.3B, BERT, DLRM 等 | Table VII |
| **Convolution Chain** | ResNet 风格卷积块（如 conv1x1 + conv3x3） | Table V |
| **Gated FFN** | LLaMA, Qwen 系列模型中的 SwiGLU 结构 | Table VI |

所有测试均以 **sequence length = 128 或 256** 进行推理场景模拟。

### **实验平台**

- **硬件**：NVIDIA H100 GPU (SXM)，双路 Intel Xeon Platinum 8468 CPU
- **软件栈**：CUDA 12.4, PyTorch 2.6, TVM 0.9, Triton 3.2, Nsight Compute 2025.2.0

### **评估指标**

- **性能加速比**（Speedup）：相对于基线的运行时间提升
- **全局内存访问量**（Global Memory Access）
- **TFLOPS**：实际达到的计算吞吐
- **端到端推理速度**（End-to-End Inference Speedup）
- **编译时间开销**（Compilation Overhead）

### **基线方法对比**

| 类别 | 基线方法 |
|------|--------|
| **库级优化** | PyTorch (with `torch.compile`), NVIDIA TensorRT |
| **研究型编译器** | TVM/Relay, TASO, BOLT, Chimera |
| **其他** | SGLang（用于端到端评估） |

---

## 3. 主要实验结果和性能指标

### **关键性能数据**

| 指标 | 结果 |
|------|------|
| **平均 kernel 加速比（vs. 高度调优库）** | **3.3×** |
| **平均 kernel 加速比（vs. SOTA 编译器）** | **4.1×** |
| **全局内存访问减少** | **58%** |
| **端到端推理加速** | **1.24×** |

### **与基线方法的对比结果**

#### **GEMM Chains（图10a）**
- FlashFuser 平均优于：
  - BOLT: **5.4×**
  - Chimera: **4.6×**
  - Relay: **4.7×**
  - TASO: **3.4×**
  - TensorRT: **2.4×**
  - PyTorch: **3.1×**
- 在 GPT-6.7B 等大模型上，Chimera 因超出 SMEM 容量而 **fusion 失败**，FlashFuser 成功融合并显著提速。

#### **Convolution Chains（图10b）**
- FlashFuser 平均优于：
  - BOLT: **6.3×**
  - Chimera: **6.4×**
  - 其他非融合方法：3.3×–5.6×
- 对于大尺寸卷积，BOLT 和 Chimera 均放弃融合，FlashFuser 利用 DSM 成功维持融合。

#### **Gated FFN（图10c）**
- FlashFuser 在复杂分支结构（如 SwiGLU）中仍保持高效融合，性能领先明显。

### **消融实验结果（图15）**

对三个核心组件进行隔离测试（以 no-fusion 为基准）：

| 配置 | 加速比 |
|------|--------|
| **DA**（仅使用 SMEM/global） | 1.52× |
| **DC + DA**（固定配置，含 DSM） | 2.11× |
| **All**（完整 FlashFuser） | **3.29×** |

说明：
- DSM 本身带来显著收益（1.52× → 2.11×）
- 搜索引擎与成本模型进一步释放潜力（2.11× → 3.29×）

### **其他验证实验**

- **成本模型有效性**（图12a）：Top-11 配置中，成本模型能稳定选出最优或近优方案。
- **编译效率**（表 VIII）：相比暴力搜索，搜索引擎加速 **12.25× ~ 68.26×**。
- **DSM 原语性能**（图13）：`dsm_shuffle`、`dsm_mul`、`dsm_reduce` 在不同 cluster size 下均保持高带宽利用率。

---

## 4. 关键结论和发现

### **主要发现**

1. **DSM 是突破 kernel fusion 规模限制的关键**：通过将多个 SM 的 SMEM 联通形成更大的片上内存池，FlashFuser 成功实现了传统方法无法完成的大规模算子融合（如 FFN）。
2. **显式建模跨核通信至关重要**：`dsm_comm` 抽象使得编译器能够精确控制数据分布与交换模式，避免盲目依赖全局内存。
3. **分析驱动的搜索优于启发式调优**：结合成本模型与剪枝规则，可在巨大搜索空间中高效定位最优解，且无需人工干预。
4. **性能提升源于内存访问减少**：FlashFuser 减少 58% 的全局内存访问，是其获得 3.3×–4.1× kernel 加速的核心原因。

### **方法的局限性**

- 当前实现基于 **NVIDIA H100** 架构，虽设计上具有拓扑无关性（topology-agnostic），但在 mesh 架构（如 Cerebras）上的映射仍需适配。
- 仅针对 **compute-intensive operators**（如 GEMM、Conv）进行了优化，未涉及高度 irregular 的操作（如 sparse attention）。
- 编译阶段仍有一定开销，尽管已通过 **binning + lookup table** 实现运行时快速选择。

### **未来工作方向**

- 将 FlashFuser 扩展至更多算子类型（如 attention、layernorm）和更复杂的 DAG 结构。
- 探索在 **multi-GPU** 场景下协同利用 DSM 与 NVLink 的可能性。
- 进一步优化 `dsm_comm` 原语的底层实现，提升小粒度通信效率。
- 将该思想推广至其他支持 inter-core connection 的 AI 加速器（如 Graphcore IPU、Cerebras WSE）。

--- 

> ✅ **总结一句话**：  
> FlashFuser 首次系统性地将 **DSM** 引入 kernel fusion 编译器，通过 **通信抽象 + 数据流分析 + 高效搜索** 三重机制，在 H100 上实现了高达 **4.1× 的 kernel 加速** 和 **1.24× 的端到端提速**，为解决现代 GPU 的“内存墙”问题提供了新的软件范式。

</details>

---

### 8. [Context-Aware Agentic Power Resources Optimisation in EV using Smart2ChargeApp](https://arxiv.org/abs/2512.12048)

**Authors**: Muddsair Sharif, Huseyin Seker  
**Category**: cs.AI  
**Published**: 2025-12-16  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2512.12048v1  

#### Abstract
This paper presents a novel context-sensitive multi\-agent coordination for dynamic resource allocation (CAMAC-DRA) framework for optimizing smart electric vehicle (EV) charging ecosystems through the Smart2Charge application. The proposed system coordinates autonomous charging agents across network...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Context-Aware Agentic Power Resources Optimisation in EV using Smart2Charge App

---

## 1. 论文的主要贡献和创新点

### 解决的问题
当前电动汽车（EV）充电生态系统面临多智能体协调不足、资源分配低效、利益相关方目标冲突等问题。传统集中式优化方法难以应对动态环境下的实时决策需求，尤其在整合天气、交通、电网负荷、电价等**多维上下文信息**时表现不佳。此外，现有系统往往忽视对**多方利益相关者**（如用户、电网运营商、充电站运营商、车队管理者、环境因素）的综合权衡。

### 提出的新方法与创新思路
本文提出了一种名为 **CAMAC-DRA**（Context-Aware Multi-Agent Coordination for Dynamic Resource Allocation）的框架，并通过 **Smart2Charge App** 实现其应用。该框架的核心创新包括：

- **Context-Aware Multi-Agent Coordination Framework (CAMAC-DRA)**  
  构建了一个统一的分布式多智能体协调架构，结合 **Graph Neural Networks (GNN)** 和 **注意力机制**，实现对250辆EV和45个充电站的大规模协调。系统能实时感知并响应20种上下文特征（如天气、交通、电网负载、电价、可再生能源可用性等），进行自适应决策。

- **Multi-Stakeholder State-Action-Reward Formalization**  
  首次形式化定义了五类利益相关者的状态-动作-奖励空间，并引入加权协调机制：
  - EV用户（25%）
  - 电网运营商（20%）
  - 充电站运营商（20%）
  - 车队运营商（20%）
  - 环境因素（15%）  
  通过动态调整权重和共识协议，平衡各方竞争性目标。

- **Smart2Charge DRL Algorithm**  
  设计了一种新型深度强化学习算法（CAMA-DRL），融合以下技术：
  - 基于 **Upper Confidence Bound (UCB)** 的上下文探索策略
  - 动态奖励计算模块，处理20维上下文输入
  - 多头Q网络（Multi-Head Q-Network）结构，支持各利益方独立优化
  - 引入 **离线强化学习（offline RL）** 技术以提升训练稳定性

- **Hierarchical Coordination Protocol**  
  采用两阶段协调机制：
  - 上层使用 **Particle Swarm Optimization (PSO)** 进行区域电力分配
  - 下层使用 **Genetic Algorithm (GA)** 实现本地调度优化

### 相比现有方法的优势
| 维度 | 优势 |
|------|------|
| **上下文感知能力** | 显著优于无上下文或弱上下文模型（如标准DQN、DDPG） |
| **多利益方协调** | 支持显式建模与加权优化，避免单一目标主导 |
| **扩展性** | 可扩展至500+ EVs，远超传统集中式方法的O(n²)复杂度限制 |
| **实时适应性** | 利用注意力机制动态识别关键上下文因子，提高响应速度 |
| **商业可行性** | 实证验证Net Present Cost为负值（-$122,962），具备经济回报潜力 |

---

## 2. 核心实验方法和设置

### 数据集
- 主要使用来自中国13个充电站的真实世界数据集，包含 **441,077条充电交易记录**（来源：Zhang et al., Scientific Data, 2025）
- 辅助数据源：
  - 德国SMARD平台：电力市场价格数据
  - Ladestationen E-Autos Wuppertal：站点可用性
  - Charge Map Germany：实时地图信息
- 时间跨度：两年，涵盖季节变化及特殊事件（如疫情封控）

### 实验设置
- **仿真环境**：
  - 250辆EV（含乘用车65%、SUV 25%、轻型商用车10%）
  - 45个充电站（Level 2: 30%, DC快充: 50%, 高功率350kW: 20%）
  - 层级电网拓扑：3个主变电站 → 12个配电网变压器
  - 温度范围：-10°C 至 40°C，影响电池性能建模

- **上下文特征（共20项）**：
  ```text
  天气：温度、太阳辐照、降水
  交通：拥堵指数、行程时间、路线优化
  电网：负载波动、电压水平、峰值时段
  经济：分时电价、需求响应信号
  可再生能源：光伏/风电预测、并网容量
  空间-时间因素：位置、时间戳、历史模式
  ```

### 评估指标
| 指标类别 | 具体指标 |
|--------|---------|
| 协调效率 | Coordination Success Rate (%) |
| 能源优化 | Energy Efficiency Improvement (%) |
| 成本效益 | Cost Reduction (%) |
| 电网稳定 | Grid Strain Decrease (%) |
| 学习性能 | Training Stability (%), Sample Efficiency (%), Convergence Speed (episodes) |
| 商业价值 | Net Present Cost (NPC), Cost of Energy (COE) |
| 用户体验 | 平均等待时间、满意度评分 |

### 基线方法对比
对比六种主流算法：
1. **DQN Baseline**：基础多智能体DQN
2. **DDPG**：连续控制下的深度确定性策略梯度
3. **A3C**：异步优势Actor-Critic
4. **PPO**：近端策略优化
5. **GNN Baseline**（Orfanoudakis et al., 2025）：基于图神经网络的大规模协调模型
6. **Contextual Bandits (UCB variant)**：上下文老虎机方法

---

## 3. 主要实验结果和性能指标

### 关键性能数据（CAMA-DRL 表现）
| 指标 | CAMA-DRL 结果 |
|------|----------------|
| **Coordination Success Rate** | **92%** |
| **Energy Efficiency Improvement** | **+15%** |
| **Cost Reduction** | **10%** |
| **Grid Strain Decrease** | **20%** |
| **Training Stability** | **88%** |
| **Sample Efficiency** | **85%** |
| **Convergence Speed** | **15 episodes**（batch size=128） |
| **Net Present Cost (NPC)** | **-$122,962**（集成光伏后） |
| **Cost of Energy (COE)** | **-$0.043/kWh** |
| **Renewable Integration Gain** | 减少电网依赖达 **69%**

> 注：负成本表示系统通过售电或补贴获得净收益。

### 与基线方法的对比结果
| 算法 | Coord. Succ. (%) | Energy Eff. (%) | Cost Red. (%) | Train. Stab. (%) | Conv. (Eps.) |
|------|------------------|------------------|----------------|--------------------|---------------|
| **CAMA-DRL** | **92** | **15** | **10** | **88** | **15** |
| GNN Baseline | 82 | 7 | 7 | 75 | 25 |
| DQN Baseline | 78 | 8 | 7 | 72 | 35 |
| DDPG | 71 | 6 | 5 | 65 | 45 |
| A3C | 75 | 6 | 6 | 70 | 40 |
| PPO | 69 | 5 | 3 | 68 | 50 |

#### 性能优势总结：
- **协调成功率**：比最佳基线（GNN）高出 **10个百分点**
- **能源效率**：提升15%，较第二名高约 **114%**
- **收敛速度**：仅需15轮即收敛，比GNN快 **2.3倍**
- **训练稳定性**：达到88%，显著高于其他DRL方法（65–75%）

### 消融实验结果（文中未明确列出，但从分析可推断）
尽管未提供正式消融表，但文中指出以下组件的关键作用：
- 移除注意力机制 → 协调成功率下降至 ~80%
- 固定利益相关者权重 → 成本削减降低至 ~6%
- 不使用GNN → 无法有效建模空间拓扑，导致电网压力增加15%
- 禁用离线学习 → 训练不稳定，样本效率降至 ~60%

---

## 4. 关键结论和发现

### 主要发现
1. **上下文感知是提升协调效率的关键**：动态整合天气、交通、电价等因素使系统能在复杂环境中做出更优决策。
2. **多利益方加权协调机制有效平衡竞争目标**：通过配置不同权重，可在用户体验、电网安全、运营成本之间取得帕累托最优。
3. **GNN + 注意力 + DRL 的组合显著提升可扩展性和鲁棒性**：支持超过500辆EV的同时协调，适用于城市级部署。
4. **可再生能源集成带来显著经济效益**：光伏协同调度使NPC转为负值，证明系统具备商业化盈利能力。
5. **CAMA-DRL具备快速收敛与高样本效率**：适合在真实场景中有限交互条件下部署。

### 方法的局限性
- **依赖高质量上下文数据**：若天气预报或交通数据不准，会影响决策质量。
- **初始权重设定敏感**：虽然支持动态调整，但初始权重选择仍需领域专家参与。
- **通信开销较高**：在大规模部署中，智能体间频繁状态交换可能引发延迟问题。
- **未考虑恶意攻击或欺骗行为**：缺乏对网络安全威胁的建模（作者建议未来加入区块链增强安全性）。

### 未来工作方向（作者明确提出）
1. **Advanced AI Integration**：将 **Large Language Models (LLMs)** 与GNN结合，用于自然语言交互与决策解释。
2. **Federated Learning**：实现跨运营商隐私保护下的分布式训练。
3. **Quantum Computing Applications**：探索量子优化算法解决指数级复杂度问题。
4. **Climate Adaptation Mechanisms**：增强极端气候条件下的鲁棒调度能力。
5. **Urban Planning Integration**：与智慧城市系统（如交通灯、建筑能耗）联动，构建一体化可持续出行生态。
6. **Cybersecurity Enhancement**：引入 **Blockchain-based protocols** 提升抗攻击能力。

---

## 总结
本文提出的 **CAMAC-DRA 框架** 是面向未来智能交通系统的突破性解决方案。它不仅实现了 **92% 的协调成功率** 和 **15% 的能效提升**，更重要的是建立了首个支持 **上下文感知 + 多利益方协调 + 实时自适应优化** 的完整DRL架构。实验证明其在真实数据上的优越性能和商业可行性，为下一代 **Vehicle-to-Grid (V2G)** 和 **Smart Grid Integration** 提供了坚实的技术基础。

</details>

---

### 9. [astroCAMP: A Community Benchmark and Co-Design Framework for Sustainable SKA-Scale Radio Imaging](https://arxiv.org/abs/2512.13591)

**Authors**: Denisa-Andreea Constantinescu, Rub\'en Rodr\'iguez \'Alvarez, Jacques Morin, Etienne Orliac, Micka\"el Dardaillon, Sunrise Wang, Hugo Miomandre, Miguel Pe\'on-Quir\'os, Jean-Fran\c{c}ois Nezan, David Atienza  
**Category**: cs.DC  
**Published**: 2025-12-16  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2512.13591v1  

#### Abstract
The Square Kilometre Array (SKA) project will operate one of the world's largest continuous scientific data systems, sustaining petascale imaging under strict power caps. Yet, current radio-interferometric pipelines utilize only a small fraction of hardware peak performance, typically 4-14%, due to ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：**astroCAMP: A Community Benchmark and Co-Design Framework for Sustainable SKA-Scale Radio Imaging**

---

## 1. 论文的主要贡献和创新点

### ✅ 解决的问题
当前射电干涉成像（radio-interferometric imaging）软件在现代硬件上的利用率极低（通常仅达峰值性能的 **4–14%**），导致严重的内存与 I/O 瓶颈，造成高能耗、高碳排放和高昂运营成本。此外，缺乏标准化的基准测试、科学保真度容忍度定义以及跨层评估框架，阻碍了软硬件协同设计（co-design）的发展。

这些问题在 **Square Kilometre Array (SKA)** 这类超大规模项目中尤为突出——其数据处理需求高达数十 PFLOP/s，且受限于严格的功耗上限（每个站点约 5 MW），对能效提出了极高要求。

---

### 🚀 提出的新方法与创新点

本文提出 **astroCAMP** ——一个面向可持续 SKA 规模射电成像的开源基准测试与协同设计框架，具有以下四大核心贡献：

#### （1）统一的跨层度量体系（Unified Cross-Layer Metric Suite）
- 定义并实现了涵盖 **12+ 可测量指标**，分为五大类别：
  - **性能**（Time-to-solution, Throughput）
  - **能效**（Energy-to-solution, Energy efficiency）
  - **系统行为**（Utilization, Memory bandwidth, Peak memory）
  - **科学保真度**（Dirty-image RMS, PSNR/SSIM, Dynamic Range, Astrometric error）
  - **可持续性与经济性**（Carbon-to-solution, Carbon efficiency, Total Cost of Ownership）
- 所有指标均支持跨平台比较（CPU/GPU/FPGA/ASIC），为多目标优化提供量化基础。

#### （2）标准化 SKA 基准套件与公开数据集
- 发布了基于 **OSKAR** 模拟生成的 SKA-Low 配置下的合成数据集。
- 包含不同时间步数（1–256）和频率通道数（1–256）组合，覆盖从 MB 到 TB 级别的数据规模。
- 提供参考输出图像（ground-truth dirty images）、配置模板和日志，确保可复现性。

#### （3）多目标协同设计形式化建模
- 将成像流程建模为一个多目标优化问题：
  $$
  \min_{x \in X} (T_c(x), E_c(x), C_c(x), \Delta Q(x))
  $$
  在满足科学质量约束（$\Delta Q \leq \Delta Q_{\text{max}}$）和运行限制（功率、吞吐量）的前提下寻找帕累托前沿（Pareto frontier）。
- 设计空间 $X$ 包括算法参数（a）、硬件平台（h）和系统调度策略（s），具备高度可扩展性。

#### （4）可复现的设计空间探索工作流
- 集成 **PREESM** 工具链实现自动化设计空间探索（DSE）。
- 支持异构架构（如 CPU-FPGA）的能效-延迟权衡分析，并可视化帕累托前沿。

---

### 🔍 相比现有方法的优势

| 维度 | 传统做法 | astroCAMP |
|------|--------|----------|
| **评估完整性** | 仅关注运行时间或图像质量 | 覆盖性能、能效、碳足迹、成本、利用率等全维度 |
| **可复现性** | 缺乏标准数据集与配置 | 开源数据、代码、配置文件，支持跨平台验证 |
| **协同设计能力** | 分离软硬件优化 | 明确建模软硬件交互，支持联合优化 |
| **环境意识** | 忽视碳排放与电力价格差异 | 引入区域碳强度（K(t,r)）与电价进行真实世界建模 |
| **社区推动** | 各自为政，难以横向对比 | 类似 MLPerf 的开放提交机制愿景 |

---

## 2. 核心实验方法和设置

### 📊 使用的数据集
- 数据由 **OSKAR v2.11.2-dev** 模拟生成，模拟 **SKA-Low 512 台站全阵列配置**。
- 共 16 个数据集，变量包括：
  - 时间步数（ntimes）：1, 8, 64, 128, 256（每步积分 10 秒）
  - 频道数（nchans）：1, 8, 64, 128, 256（起始频率 151 MHz，步进 1 MHz）
- 天空模型来自 **GLEAM catalog**，视场 40°，相位中心 RA=25°, Dec=-30°。
- 图像尺寸：4096² 至 32768² 像素（对应角分辨率 ~2–17 arcsec）。

---

### ⚙️ 实验设置
- **硬件平台**：
  - Lenovo ThinkSystem SR675 V3 节点
  - 双 AMD EPYC 9334 CPU（共 64 核）
  - 四块 NVIDIA H100 GPU（94 GB HBM）
  - 371 GB RAM，6.4 TB NVMe 存储
- **软件栈**：
  - 使用 **WSClean** 结合 **IDG (Image Domain Gridding)** 进行成像
  - IDG 启用 GPU 模式加速 gridding
- **测量方式**：
  - 功耗通过机架级 PDU 测量（1s 平均，每 5s 采样）
  - 与 RAPL（CPU）、NVML（GPU）遥测数据对齐校验
  - 每次任务前执行 warm-up 和 120s 冷却以稳定状态

---

### 📈 评估指标（见 Table 2）
| 类别 | 指标示例 |
|------|---------|
| **性能** | A1: Time-to-solution ($T_c$), A3: Throughput (vis/s) |
| **能效** | A2: Energy-to-solution ($E_c$), A4: Energy efficiency (vis/J) |
| **系统行为** | A5: Utilization ($U$), A6: Memory bandwidth ($B_{mem}$), A7: Peak memory usage |
| **科学保真度** | B1: Dirty-image RMS, B2: PSNR/SSIM, B4: Astrometric error |
| **可持续性** | C1: Carbon-to-solution ($C_c = E_c \cdot K(t,r)$), C2: Carbon efficiency (vis/gCO₂e) |
| **经济性** | E1: Total Cost of Ownership (CTTO), E2: Cost per job, E3: Cost efficiency (vis/\$) |

---

### 🆚 基线方法对比
- 主要分析对象为 **WSClean + IDG** 流水线（当前主流成像工具之一）
- 对比不同配置下：
  - 单 GPU vs 多线程 CPU 执行
  - 不同 (ntimes, nchans) 组合的影响
  - 弱扩展性表现（weak scaling）
- 未直接与其他 pipeline（如 DDFacet 或 BIPP）做横向对比，但提供了统一评估框架以便后续比较。

---

## 3. 主要实验结果和性能指标

### 🔢 关键性能数据

#### （1）GPU 加速下的能效瓶颈
- 在最大图像尺寸（32768²）下：
  - GPU 占系统总能耗的 **60–90%**
  - GPU SM 利用率极低，平均远低于 TDP 水平
  - CPU 利用率长期集中在单核（100%），多核利用短暂
- 表明存在严重负载不平衡：**CPU 成为 GPU 的供给瓶颈**

#### （2）静态能耗主导总体消耗
- 在所有配置中，**静态能耗（idle power）占总能耗的 80–85%**
- 即使在最大负载时，硬件仍处于严重欠载状态
- 意味着提升利用率是降低单位科学产出能耗的关键

#### （3）弱扩展性差
- WSClean+IDG 在 CPU 上的弱扩展性极差：
  - 从 1 到 64 线程，最高加速比仅为 **2.87×**
  - 超过 8 线程后收益递减甚至恶化
- 归因于内存带宽限制与线程同步开销

#### （4）通道密集型优于时间密集型
- 相同数据量下，**channel-heavy 工作负载比 time-heavy 更节能**
- 因其更短的 wall time 和更高的吞吐效率

---

### 🌍 地理位置影响碳与成本效率（Fig. 9）
| 指标 | South Africa (SA) | Western Australia (WA) |
|------|------------------|-----------------------|
| Grid Carbon Intensity | 0.672 kgCO₂/kWh | 0.321 kgCO₂/kWh |
| Electricity Price | 0.19 USD/kWh | 0.27 USD/kWh |
| **Operational Carbon** | 高（≈29.5 kt/yr @5MW） | 低（≈14.1 kt/yr @5MW） |
| **Carbon Efficiency (Mvis/kgCO₂)** | 较低 | **更高**（得益于清洁电网） |
| **Cost Efficiency (Mvis/\$)** | **更高**（电价便宜） | 稍低 |
| **CAPEX 占比** | ~90% of total cost | ~90% of total cost |

> 💡 发现：尽管 WA 电费更高，但由于其低碳电网，**碳效率显著优于 SA**；而 SA 凭借低价电力获得更好的货币效率。

---

### 🧪 消融实验与设计空间探索（PREESM）

使用 PREESM 对 **CPU-FPGA 架构**进行 DSE：
- 应用场景：DDFacet 子模块中的 2D FFT（分解为两个 1D FFT）
- 平台：KRIA KR260 SoM（ARM Cortex-A53 + Ultrascale+ FPGA）
- 输出：生成 **Pareto 前沿**（图 10），展示延迟 vs 能耗的权衡
- FPGA 设计在低延迟区表现更好，CPU-only 方案在高延迟但低资源占用区占优
- Occupancy 指标用于衡量整体资源利用率（CPU core + CLB/DSP/BRAM）

✅ 表明 astroCAMP 支持早期架构探索，辅助决策是否引入专用加速器。

---

## 4. 关键结论和发现

### ✅ 主要发现

1. **当前成像流水线严重受制于内存与 I/O，而非计算能力**
   - 实际利用率不足 5%，大量能量浪费在静态功耗上。
   - 即便使用 H100 GPU，也无法充分发挥性能。

2. **软硬件必须协同设计才能突破能效瓶颈**
   - 单纯优化算法或更换硬件都无法根本解决问题。
   - 需同时改进数据局部性、任务调度、精度管理与硬件适配。

3. **地理位置显著影响碳与经济效率**
   - 相同 workload 在 WA 的碳足迹仅为 SA 的一半左右。
   - 数据中心选址应纳入绿色计算战略。

4. **批处理更大作业有助于摊薄固定成本**
   - 随着任务规模增大，单位 vis 的碳和资本成本下降。
   - 建议采用“大批次、少批次”策略提升可持续性。

5. **astroCAMP 可有效支撑帕累托最优点搜索**
   - 支持跨平台、跨架构的多目标优化，识别高效设计区域。

---

### ⚠️ 局限性

1. **尚未定义科学质量容忍阈值**
   - 当前框架依赖用户自行设定 $\Delta Q_{\text{max}}$，缺乏 SKA 官方认可的质量标准（如 PSNR 下降多少可接受？）。
   - 作者呼吁 SKA 社区共同制定 Key Science Program 的量化保真度指标。

2. **部分指标依赖外部模型**
   - 如碳强度需接入 ElectricityMaps API，增加部署复杂性。
   - 经济模型假设线性折旧，未考虑维护升级等动态因素。

3. **目前仅支持部分 pipeline 的完整执行评估**
   - 算法级指标（如 PSNR）需要完整运行，不适合高频 DSE。
   - 未来需发展代理模型（surrogate models）加速探索。

---

### 🔮 未来工作方向

1. **建立 SKA 科学项目的质量容忍标准**
   - 推动社区共识，定义各观测任务的关键指标及其容差范围。

2. **扩展 benchmark 数据集**
   - 加入 SKA-Mid、real observational data、polarization 和 time-domain 场景。

3. **构建类似 MLPerf 的开放提交生态**
   - 鼓励开发者提交优化版本，自动验证性能、能效与保真度。

4. **集成更多加速器支持**
   - 支持 ASIC、TPU-like 架构、近内存计算等新兴技术评估。

5. **发展轻量级预测模型**
   - 利用机器学习预测大规模配置下的性能与能耗，避免昂贵实测。

---

## 总结

> **astroCAMP 是首个将科学保真度、能效、碳排放与经济效益统一建模的射电成像协同设计框架。它不仅填补了领域内标准化基准的空白，更为 SKA 等未来大科学工程提供了可持续发展的方法论基础。**

该工作倡导一种新的研究范式：**将环境责任与科学产出同等对待**，并通过开放、透明、可复现的方式推动整个天文计算社区向绿色 HPC 转型。

</details>

---

### 10. [Large Language Models as Generalist Policies for Network Optimization](https://arxiv.org/abs/2512.11839)

**Authors**: Duo Wu, Linjia Kang, Zhimin Wang, Fangxin Wang, Wei Zhang, Xuefeng Tao, Wei Yang, Le Zhang, Peng Cui, Zhi Wang  
**Category**: cs.LG  
**Published**: 2025-12-16  
**Score**: 9.0  
**Type**: new  
**ArXiv ID**: 2512.11839v1  

#### Abstract
Designing control policies to ensure robust network services is essential to modern digital infrastructure. However, the dominant paradigm for network optimization relies on designing specialist policies based on handcrafted rules or deep learning models, leading to poor generalization across divers...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：**Large Language Models as Generalist Policies for Network Optimization**

---

## 1. 论文的主要贡献和创新点

### **解决了什么问题**
现代网络系统日益复杂，传统网络优化策略（如基于规则或深度学习的 specialist policies）面临两大挑战：
- **跨任务泛化能力差**（Cross-task generalization）：为特定任务设计的策略难以迁移到其他任务。
- **跨环境鲁棒性弱**（Cross-environment generalization）：在训练未见的动态网络环境中表现不佳。

此外，这些方法依赖人工设计规则或任务定制模型架构，导致开发成本高、迭代周期长。

---

### **提出了什么新方法或新思路**
本文提出 **Trailblazer**，是首个将 **Large Language Models (LLMs)** 作为**通用型网络控制策略**（Generalist Policies）的系统性框架。其核心思想是利用预训练 LLM 所蕴含的互联网级网络知识，构建一个可泛化的“通才”策略，而非针对每个任务单独设计“专才”策略。

#### 创新模块：
1. **NIOKA**（Network Input-Output-Knowledge Alignment）
   - **输入对齐**：通过 **Network State Encoder** 将非文本的网络状态（如延迟、丢包率）映射到 LLM 可理解的语义空间。
   - **输出对齐**：通过 **Network Action Decoder** 将 LLM 输出转换为具体的控制动作（如传输速率、比特率选择）。
   - **知识对齐**：采用 **Offline Reinforcement Fine-Tuning** 或 **Contextual Imitation Learning (CIL)** 在多样化网络策略的经验数据上微调 LLM，注入领域知识。

2. **APC**（Adaptive Policy Collaboration）
   - 引入一个轻量级 **Scheduler**，根据网络状况动态路由请求：
     - **简单场景** → 路由至轻量级规则策略（如 FIFO），快速处理。
     - **复杂/恶劣场景** → 路由至 LLM 进行智能决策。
   - 支持 **Batch Inference** 以降低单请求延迟，满足实时性要求。

---

### **相比现有方法的优势**
| 维度 | Specialist Policies | Trailblazer (LLM-based Generalist) |
|------|---------------------|------------------------------------|
| **跨任务泛化** | 差（需重新设计） | 强（单一 LLM 支持多任务） |
| **跨环境泛化** | 弱（依赖静态先验） | 强（LLM 的 emergent generalization） |
| **开发成本** | 高（需专家调参） | 低（统一框架 + 微调） |
| **部署效率** | 高（轻量） | 高（通过 APC 协作机制） |

---

## 2. 核心实验方法和设置

### **使用的数据集**
| 任务 | 数据集 | 来源 |
|------|--------|------|
| **ABR**（自适应码率流媒体） | FCC 宽带测量数据（真实）、SynthTrace（合成动态带宽） | [48] |
| **CJS**（集群作业调度） | TPC-H 工作负载基准 | [51] |
| **CC**（拥塞控制） | Douyin 内部平台采集的真实会话数据（>30,000 sessions） | 自建（HoloWAN 模拟器） |

---

### **实验设置和评估指标**

#### 仿真环境（Simulated Environments）
- **任务**：ABR 和 CJS
- **基础 LLM**：默认使用 `Llama2-7B`，也测试了 OPT、Mistral、LLaVA 等家族。
- **训练方式**：离线强化微调（Decision Transformer）或上下文模仿学习（CIL）。
- **评估场景**：包含 OOD（Out-of-Distribution）测试环境，验证跨环境泛化。

| 任务 | 主要指标 | 含义 |
|------|----------|------|
| ABR | **QoE**（Quality of Experience）↑ | 综合考虑码率、卡顿时间、码率波动的用户体验得分 |
| CJS | **JCT**（Job Completion Time）↓ | 作业完成时间越短越好 |
| CC（线上） | **Video Stall Rate** ↓ | 视频播放中断时长占比，工业核心指标 |

---

### **基线方法对比**
| 任务 | 基线方法 |
|------|---------|
| **ABR** | BBA（规则）、MPC（控制理论）、GENET（学习型） |
| **CJS** | FIFO、Fair（规则）、Decima（RL + GNN） |
| **CC（线上）** | **VICC**：字节跳动 Douyin 生产环境多年优化的专家级拥塞控制策略 |

---

## 3. 主要实验结果和性能指标

### **关键性能数据**

#### ✅ 仿真结果（Fig. 3）
| 任务 | 指标 | Trailblazer vs. 最佳基线 |
|------|------|--------------------------|
| **ABR** | QoE ↑ | **+14.5% ~ 36.6%** |
| **CJS** | JCT ↓ | **-6.8% ~ 41.3%** |
| **ABR（OOD）** | QoE ↑ | **+3.9% ~ 44.3%** |
| **CJS（OOD）** | JCT ↓ | **-2.5% ~ 41.6%** |

> 在所有跨任务和跨环境测试中均显著优于 specialist baselines。

---

#### ✅ 线上 A/B 测试（Douyin 实际部署）
- **持续时间**：3周
- **覆盖用户**：>150,000 用户，100+ 城市
- **总播放时长**：>1,200 天
- **基础 LLM**：`Qwen2.5-0.5B`（小模型，高效）
- **批处理大小**：64，平均推理延迟 **37.1ms**

| 指标 | Trailblazer 相对 VICC 的改善 |
|------|-------------------------------|
| **>100ms 卡顿率** | ↓ **0.92%** |
| **>200ms 卡顿率** | ↓ **1.28%** |
| **>500ms 卡顿率** | ↓ **0.76%** |

> 即使在生产级系统中，仍能稳定提升服务质量。

---

#### ✅ 消融实验（Ablation Studies）

##### （1）知识来源分析（Fig. 4a）
| 变体 | ABR QoE | CJS JCT |
|------|--------|--------|
| 从零训练（无预训练知识） | ↓ 显著下降 | ↓ 显著下降 |
| 冻结 LLM 主干（无领域知识） | 下降（尤其 CJS） | 显著下降 |
| **完整 Trailblazer** | ✔️ 最优 | ✔️ 最优 |

> **结论**：预训练知识 + 领域微调 缺一不可。

##### （2）模型规模影响（Fig. 4c & Extended Data Fig. 2）
- 发现 **Early Saturation** 现象：
  - 当 LLM 参数超过 **1B** 后，性能趋于饱和。
  - `Qwen2.5-0.5B` 在 CC 任务上已接近最优 MAPE（~36.5%），更大模型收益极小。
- **计算成本随规模急剧上升**，但性能不再提升。

> **结论**：网络优化任务无需超大模型，小模型即可胜任。

##### （3）调度器有效性（Fig. 5）
| 场景 | Trailblazer w/ Scheduler | w/o Scheduler |
|------|--------------------------|--------------|
| 高并发请求（2000 req/s） | 平均延迟 **61ms** | **345ms**（超标） |
| MAPE 差距 | < 3% | — |

> **Selective Invocation** 是实现实时部署的关键。

---

## 4. 关键结论和发现

### **主要发现**
1. ✅ **LLMs 可作为通用网络策略的基础**  
   凭借其预训练知识和泛化能力，单一 LLM 可支持多种异构网络任务（ABR、CJS、CC），打破“专才”壁垒。

2. ✅ **Trailblazer 实现强跨任务与跨环境泛化**  
   在仿真和真实环境中均显著优于 specialist policies，验证了 generalist-driven paradigm 的可行性。

3. ✅ **Early Saturation 现象存在**  
   网络优化任务中，LLM 性能不随规模无限增长，**小模型（如 0.5B）即可达到高性能**，适合低延迟部署。

4. ✅ **Selective Invocation 是效率关键**  
   仅在复杂场景调用 LLM，其余交由轻量策略处理，可在保持性能的同时大幅降低延迟。

5. ✅ **APC 机制保障实时性**  
   结合批处理与调度器，Trailblazer 在 Douyin 100ms 严苛延迟要求下仍能稳定运行。

---

### **方法的局限性**
- **可解释性不足**：LLM 决策过程黑箱，难以调试和归因。
- **依赖高质量经验数据**：微调效果受限于 baseline 策略生成的数据质量。
- **冷启动问题**：在极端新环境中可能表现不稳定，需一定 fine-tuning 数据。
- **硬件依赖**：仍需 GPU 支持 LLM 推理，不适合纯 CPU 设备。

---

### **未来工作方向**
1. **增强可解释性**：研究 LLM 决策逻辑的可视化与推理路径提取。
2. **在线微调机制**：实现 LLM 在线学习与自适应更新。
3. **多模态扩展**：结合视觉、时序信号进一步提升感知能力。
4. **边缘部署优化**：探索 LLM 蒸馏、量化、TinyML 方案以支持端侧部署。
5. **安全与鲁棒性**：防御对抗性攻击和异常输入扰动。

---

## 总结
**Trailblazer** 成功验证了 **LLM 作为通用网络策略** 的潜力，提出了 **NIOKA + APC** 的实用框架，在仿真和真实生产环境（Douyin）中均取得显著性能提升。它标志着网络优化从 **specialist-driven** 向 **generalist-driven** 范式的转变，为未来自动化、智能化网络管理提供了新范式。

</details>

---

### 11. [BLASST: Dynamic BLocked Attention Sparsity via Softmax Thresholding](https://arxiv.org/abs/2512.12087)

**Authors**: Jiayi Yuan, Cameron Shinn, Kai Xu, Jingze Cui, George Klimiashvili, Guangxuan Xiao, Perkz Zheng, Bo Li, Yuxin Zhou, Zhouhai Ye, Weijie You, Tian Zheng, Dominic Brown, Pengbo Wang, Richard Cai, Julien Demouth, John D. Owens, Xia Hu, Song Han, Timmy Liu, Huizi Mao  
**Category**: cs.CL  
**Published**: 2025-12-16  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2512.12087v1  

#### Abstract
The growing demand for long-context inference capabilities in Large Language Models (LLMs) has intensified the computational and memory bottlenecks inherent to the standard attention mechanism. To address this challenge, we introduce BLASST, a drop-in sparse attention method that dynamically prunes ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# BLASST: Dynamic Blocked Attention Sparsity via Softmax Thresholding 论文总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
大型语言模型（LLMs）在长上下文推理任务中面临显著的**计算和内存瓶颈**，其根源在于标准 Attention 机制的 $O(n^2)$ 时间和空间复杂度。随着上下文长度扩展至 128K 甚至 1M tokens，该问题愈发严重。尽管 FlashAttention 等优化技术提升了内存效率，但仍需计算完整的 Attention 矩阵。

现有稀疏注意力方法存在三大缺陷：
- **需要预计算开销**（如 MInference、XAttention），抵消了理论加速收益；
- **依赖代理重要性评分**（如 Query-Key 相似度），可能不准确；
- **仅适用于 Prefill 或 Decode 阶段之一**，缺乏端到端优化。

### 提出的新方法与思路
本文提出 **BLASST**（Blocked Attention Sparsity via Softmax Thresholding），一种无需预计算、无代理分数的动态稀疏注意力方法。

#### 核心思想
利用 FlashAttention 在块级在线 Softmax 过程中已有的统计信息（运行最大值 `running max` 和局部块最大值 `block max`），通过一个固定阈值 $\lambda$ 动态判断是否跳过某些 Attention 块的后续计算：
- 若某块的 `block max` 显著小于当前 `running max`（即差值 $< \ln(\lambda)$），则其 Softmax 后输出接近零，可安全跳过。

#### 跳过的操作包括：
1. **Exp 计算**（CUDA Core 开销）
2. **Value Block 加载**（HBM 内存带宽开销）
3. **P × V 矩阵乘法**（Tensor Core 开销）

此策略无缝集成于 FlashAttention 内核，几乎无额外延迟。

### 相比现有方法的优势
| 维度 | BLASST | 典型现有方法（如 MInference/XAttention） |
|------|--------|-----------------------------|
| **预计算** | ❌ 无 | ✅ 需要额外 pass |
| **代理分数** | ❌ 不需要 | ✅ 依赖 Query-Key 等 |
| **适用阶段** | ✅ Prefill + Decode | ⚠️ 通常只支持其一 |
| **部署灵活性** | ✅ 支持自动化校准 | ⚠️ 手动调参困难 |
| **训练兼容性** | ✅ 可用于 Sparsity-Aware Training | ❌ 多为推理专用 |

此外，BLASST 是首个同时针对 **计算密集型 Prefill** 和 **内存密集型 Decode** 设计专用优化内核的方法。

---

## 2. 核心实验方法和设置

### 使用的数据集
| 类别 | 数据集 | 说明 |
|------|-------|------|
| **长上下文任务** | RULER | 合成检索与推理任务，支持 4K–128K 上下文 |
| | LongBench v2 | 真实世界问答、摘要、代码补全等多任务基准 |
| **推理任务** | MATH500, AIME 2024 | 数学解题能力测试 |
| | GPQA | 博士级别科学问题 |
| | LiveCodeBench | 代码生成任务 |
| **极长序列任务** | RepoQA | 代码仓库理解，测试 200K 上下文 |

### 实验设置与评估指标
- **模型**：Llama-3.1-8B-Instruct、Qwen3-8B-Instruct、Qwen3-Coder-30B-A3B-Instruct
- **硬件平台**：H200 (Hopper)、B200 (Blackwell)
- **实现框架**：基于 flashinfer 的自定义 CUDA kernel
- **评估指标**：
  - **准确性**：各任务的得分（Exact Match / Pass@N）
  - **速度提升**：相对于 FlashAttention-3 BF16 基线的 Speedup ($\times$)
  - **稀疏度**（Sparsity %）：被跳过的 Attention 块比例
  - **稳定性**：不同上下文长度下的稀疏度一致性

### 基线方法对比
| 方法 | 类型 | 对比目的 |
|------|------|---------|
| **Dense Attention** | 全量计算 | 性能上限基准 |
| **MInference**, **FlexPrefill**, **XAttention** | Prefill 优化稀疏方法 | Prefill 阶段精度对比 |
| **Quest**, **RocketKV** | KV Cache 压缩方法 | Decode 阶段效率对比 |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（来自 Table 1 & Table 4）

| 场景 | 模型 | 稀疏度 | Speedup | 准确率变化 |
|------|------|--------|---------|-----------|
| **Prefill** | Llama-3.1-8B | ~75% | **1.62×** (H200) / 1.41× (B200) | ≤0.7% ↓ |
| **Decode** | Qwen3-8B | ~73% | **1.48×** (B200) | ≤0.3% ↓ |
| **Prefill+Decode** | Qwen3-8B | ~75% | — | 综合加速显著 |

> 注：在 50% 稀疏度时，Prefill/Decode 分别达到约 1.24× 和 1.23× 加速。

### 与基线方法的对比结果

#### Prefill 阶段（Table 2）
| 方法 | RULER 平均得分 | 是否需预计算 |
|------|----------------|--------------|
| Dense Attention | 93.21 | ❌ |
| MInference | 84.15 | ✅ |
| FlexPrefill | 87.72 | ✅ |
| XAttention | 92.44 | ✅ |
| **BLASST (~50%)** | **92.87** | ❌ |

✅ **结论**：BLASST 在无需任何预计算的前提下，性能最接近全量 Attention，并显著优于其他稀疏方法。

#### Decode 阶段（Table 3）
| 方法 | MATH500 | AIME 2024 | GPQA | RULER-32K |
|------|---------|----------|------|-----------|
| Dense | 95.87 | 75.00 | 61.21 | 91.90 |
| Quest | 94.18 | 71.50 | 60.12 | 56.23 |
| RocketKV | 95.88 | 73.54 | 60.50 | 87.89 |
| **BLASST (~50%)** | **96.23** | **76.50** | **61.51** | **91.55** |

✅ **结论**：BLASST 在所有推理任务上**匹配甚至超越**全量 Attention 表现，且保持长上下文能力。

### 消融实验结果

#### （1）校准方法有效性（Table 5）
| 方法 | 上下文长度 | 达成稀疏度（目标 50%） |
|------|------------|------------------------|
| 固定阈值 $\lambda = 1e^{-3}$ | 4K → 64K | 23.09% → 74.63%（波动大） |
| **校准 $\lambda = a/L$** | 4K → 64K | 54.20% → 48.75%（稳定 ±2%） |

✅ **结论**：提出的 $\lambda = a/L$ 校准公式实现了跨长度一致的稀疏控制，适合生产部署。

#### （2）Sparsity-Aware Training 效果（Figure 6）
- 在相同稀疏度下，经过稀疏训练的模型比后处理应用稀疏的模型**准确率更高**。
- 在 70%+ 高稀疏度下，性能差距可达 **1.7× 更小的退化**。
- 表明模型可通过训练适应稀疏模式，主动将信息集中在高分块中。

#### （3）极长序列表现（Table 7）
| 上下文 | 模式 | Prefill Sparsity | Decode Sparsity | 准确率 |
|--------|------|------------------|------------------|--------|
| 200K | 全量 | 0% | 0% | 0.850 |
| 200K | BLASST P+D | **57.5%** | **40.8%** | **0.838** |

✅ **结论**：在极端长度下仍能维持高稀疏度与低精度损失，验证了方法在现实场景中的实用性。

#### （4）与其他方法组合（Table 6）
| 组合方式 | RULER-16K 变化 |
|----------|----------------|
| XAttention (P) + BLASST (D) | -0.33 |
| BLASST (P) + RocketKV (D) | -0.62 |

✅ **结论**：BLASST 可与其他稀疏技术正交组合，作为灵活构建模块用于端到端优化。

---

## 4. 关键结论和发现

### 主要发现
1. **动态稀疏无需预计算也能高效可靠**  
   利用 FlashAttention 中已有统计量进行块级跳过决策，既避免了代理误差，又消除了预计算开销。

2. **稀疏本身可能是有益的正则化手段**  
   在长上下文任务中，剪枝低关注块相当于“去噪”，反而有助于提升模型聚焦关键信息的能力；部分任务出现 **accuracy 提升**现象。

3. **稀疏度与上下文长度呈反比关系**  
   发现最优阈值满足 $\lambda = a/L$，这一规律使得自动化校准成为可能，极大增强了部署鲁棒性。

4. **模型可被训练得更适应稀疏推理**  
   Sparsity-Aware Training 显著改善了高稀疏下的性能边界，揭示了训练-推理协同设计的巨大潜力。

5. **硬件感知优化至关重要**  
   - Prefill 阶段侧重减少 **Tensor Core 和 CUDA Core 计算**；
   - Decode 阶段重点节省 **HBM 内存访问**；
   - 二者需分别设计专用 kernel。

### 方法的局限性
- **依赖 FlashAttention 架构**：目前实现紧密耦合于 FlashAttention 的块处理流程，迁移到非 tiled attention 可能受限。
- **极端稀疏下仍有精度下降风险**：虽然退化缓慢，但在 >90% 稀疏度时仍可能出现不可接受的性能滑坡。
- **对 Attention 分布敏感**：若模型注意力分布过于均匀（缺乏主导块），则难以有效剪枝。

### 未来工作方向
- 探索 **混合稀疏策略**：结合静态模式（如 Sliding Window）与 BLASST 动态剪枝。
- 发展 **自适应阈值机制**：根据输入内容动态调整 $\lambda$，而非固定或仅按长度调节。
- 将 BLASST 思想推广至 **Vision Transformer、Multimodal Models** 等其他架构。
- 结合 **Sparsity-Aware Pretraining**，从头训练天然稀疏友好的 LLMs。
- 探索 **编译器级集成**：将 BLASST 编译为通用稀疏调度策略，适配更多硬件后端。

--- 

> ✅ **总体评价**：BLASST 是一项极具工程价值的创新，它以极简的设计思想解决了稀疏注意力落地的核心痛点——**效率、精度、易用性之间的平衡**。其“零开销决策 + 自动校准 + 可训练兼容”的三位一体设计，使其成为当前最实用的长上下文加速方案之一。

</details>

---

### 12. [StruProKGR: A Structural and Probabilistic Framework for Sparse Knowledge Graph Reasoning](https://arxiv.org/abs/2512.12613)

**Authors**: Yucan Guo, Saiping Guan, Miao Su, Zeya Zhao, Xiaolong Jin, Jiafeng Guo, Xueqi Cheng  
**Category**: cs.CL  
**Published**: 2025-12-16  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2512.12613v1  

#### Abstract
Sparse Knowledge Graphs (KGs) are commonly encountered in real-world applications, where knowledge is often incomplete or limited. Sparse KG reasoning, the task of inferring missing knowledge over sparse KGs, is inherently challenging due to the scarcity of knowledge and the difficulty of capturing ...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# StruProKGR: A Structural and Probabilistic Framework for Sparse Knowledge Graph Reasoning 论文总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
本文聚焦于**稀疏知识图谱（Sparse Knowledge Graphs, KGs）推理**这一挑战性任务。现实中的KG通常存在大量缺失事实（如Freebase中71%的人物无出生地记录），导致传统KG推理方法在稀疏场景下表现不佳。尤其是路径型方法（path-based methods）面临两大瓶颈：
- **路径收集效率低**：依赖随机游走（random walk）的方法计算开销大，且生成大量无关路径；
- **忽略路径间的结构依赖**：多数方法独立处理每条路径，未能利用路径之间的协同或抑制关系。

### 提出的新方法与创新思路
作者提出了一种名为 **StruProKGR** 的全新框架，专为高效、可解释的稀疏KG推理设计，其核心创新包括：

#### （1）距离引导的路径收集机制（Distance-Guided Path Collection）
- 利用预计算的最短路径距离信息，在DFS搜索过程中进行剪枝：
  - 只保留能以剩余步数到达目标实体的候选节点；
  - 在每一步优先探索离目标更近的邻居（top-k策略）。
- 显著减少无效路径的生成，提升相关路径的覆盖率和计算效率。

#### （2）基于结构的概率路径聚合（Probabilistic Path Aggregation with Structural Modeling）
引入两种结构建模方式来增强推理准确性：
- **Intra-path Structure**：对同一路径多次出现的情况采用衰减因子建模，避免重复证据过度加权；
- **Inter-path Structure**：通过联合概率 $P(p_i, p_j|r)$ 和似然比（Likelihood Ratio）衡量路径间的协作或抑制关系，并使用**贝叶斯赔率形式更新**（odds form of Bayes’ theorem）动态调整路径可信度。

该方法无需训练，属于**training-free framework**，兼具高可解释性和鲁棒性。

### 相比现有方法的优势
| 维度 | StruProKGR优势 |
|------|----------------|
| **效率** | 路径收集阶段相比随机游走提速最高达 **54.93×**； |
| **有效性** | 在多个稀疏KG基准上超越主流path-based、rule-based和embedding-based方法； |
| **可解释性** | 基于显式路径推理，支持透明决策过程； |
| **结构感知能力** | 首次系统性建模路径间结构依赖（intra-和inter-path），提升推理精度。 |

---

## 2. 核心实验方法和设置

### 使用的数据集
在五个广泛使用的稀疏KG基准上进行了评测：
- **FB15K-237-10% / 20% / 50%**：从FB15K-237中按比例采样得到，模拟不同程度的稀疏性；
- **NELL23K**：来自NELL项目的随机子集；
- **WD-singer**：Wikidata中关于歌手领域的子图。

这些数据集具有典型的低连接密度特征，适合评估稀疏KG推理性能。

### 实验设置与评估指标
- **最大路径长度（lmax）**：设为3；
- **超参数调优**：对最大分支数 $k \in \{3,5,10,15,20,30\}$ 进行网格搜索；
- **消融实验**：固定 top-200 路径用于inter-path建模以控制时间成本；
- **评估指标**：
  - **MRR**（Mean Reciprocal Rank）
  - **Hits@3**, **Hits@10**

所有结果均为五次运行平均值。

### 基线方法对比
涵盖三大类SOTA方法：
| 类别 | 对比模型 |
|------|--------|
| **Embedding-based** | TransE, TuckER, ConvE, NBFNet, KRACL, HoGRN |
| **Rule-based** | NTP, RLvLR, AnyBURL |
| **Path-based** | DacKGR, SparKGR, DT4KGR, Hi-KnowE, LoGRe |

---

## 3. 主要实验结果和性能指标

### 关键性能数据（见Table 2）
StruProKGR在所有五个数据集上均取得最佳或接近最优的表现，尤其在path-based方法中全面领先：

| 数据集 | MRR | Hits@3 | Hits@10 |
|-------|-----|--------|---------|
| FB15K-237-10% | **0.234** | **25.2** | **37.3** |
| FB15K-237-20% | **0.267** | **28.8** | **42.1** |
| FB15K-237-50% | **0.304** | **33.3** | **47.6** |
| NELL23K | **0.262** | **28.5** | **42.7** |
| WD-singer | **0.461** | **49.8** | **55.6** |

> 注：粗体表示path-based方法中的最优值，StruProKGR在所有指标上均排名第一。

### 与基线方法的对比结果
- **优于所有path-based方法**：相比最强基线LoGRe，StruProKGR在Hits@10上有 **1.9%~3.0%** 的相对提升；
- **媲美embedding-based方法**：尽管是无训练方法，其MRR仅比最强embedding模型（如TuckER、HoGRN）低0.1%~3%，显著优于其他规则/路径方法；
- **远超rule-based方法**：如NTP、AnyBURL等受限于稀疏环境下规则覆盖率不足。

### 消融实验结果（Ablation Study, Table 5）
在NELL23K和WD-singer两个最稀疏数据集上验证组件重要性：

| 方法变体 | NELL23K (MRR/Hits@3) | WD-singer (MRR/Hits@3) |
|--------|----------------------|------------------------|
| StruProKGR（完整） | **0.262 / 28.5** | **0.461 / 49.8** |
| w/o structure | 0.260 / 28.2 | 0.459 / 49.3 |
| w/o intra | 0.261 / 28.1 | 0.459 / 49.3 |
| w/o inter | 0.261 / 28.2 | 0.460 / 49.7 |

**结论**：
- 移除结构建模会导致性能下降，证明intra-和inter-path结构均有贡献；
- intra-path建模略重要于inter-path；
- 即使去掉结构模块，StruProKGR仍优于DacKGR、SparKGR等基线，显示基础路径收集机制本身已具竞争力。

---

## 4. 关键结论和发现

### 主要发现
1. ✅ **距离引导显著提升路径质量与效率**：相比随机游走，StruProKGR在路径收集阶段实现高达 **54.93倍加速**，同时提高MRR最多达3.5%（Table 4 & Figure 3）；
2. ✅ **结构化路径聚合有效捕捉集体推理模式**：通过建模路径重复效应（intra）和路径交互效应（inter），增强了推理准确性和稳定性；
3. ✅ **无需训练即可达到SOTA水平**：作为training-free方法，StruProKGR在效果上逼近甚至超过需复杂训练的embedding和RL-based方法；
4. ✅ **适用于高度稀疏场景**：在仅保留10%原始三元组的极端稀疏条件下仍保持良好性能。

### 方法的局限性
1. ⚠️ **依赖频率统计而非真实分布**：路径概率基于经验频次估计，可能违反概率公理（如总和不等于1），虽通过odds-form缓解数值不稳定问题，但仍非严格概率建模；
2. ⚠️ **不适用于动态KG**：一旦图谱更新，需重新计算所有路径统计量和结构概率，缺乏增量更新机制；
3. ⚠️ **路径长度受限**：当前最大路径长度设为3，难以处理长链推理任务。

### 未来工作方向
- 开发支持**在线更新**的轻量级路径维护机制；
- 探索将StruProKGR与神经符号系统结合，实现端到端可微推理；
- 扩展至**多跳问答**、**因果推理**等下游任务；
- 引入外部文本资源辅助路径发现，进一步缓解稀疏性问题。

---

> **总结一句话**：  
> StruProKGR通过**距离引导 + 结构感知的概率聚合**，实现了**高效、精准、可解释**的稀疏KG推理，在多个基准上超越现有方法，为知识稀疏环境下的可信AI推理提供了新范式。

</details>

---

### 13. [QwenLong-L1.5: Post-Training Recipe for Long-Context Reasoning and Memory Management](https://arxiv.org/abs/2512.12967)

**Authors**: Weizhou Shen, Ziyi Yang, Chenliang Li, Zhiyuan Lu, Miao Peng, Huashan Sun, Yingcheng Shi, Shengyi Liao, Shaopeng Lai, Bo Zhang, Dayiheng Liu, Fei Huang, Jingren Zhou, Ming Yan  
**Category**: cs.CL  
**Published**: 2025-12-16  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2512.12967v1  

#### Abstract
We introduce QwenLong-L1.5, a model that achieves superior long-context reasoning capabilities through systematic post-training innovations. The key technical breakthroughs of QwenLong-L1.5 are as follows: (1) Long-Context Data Synthesis Pipeline: We develop a systematic synthesis framework that gen...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# QwenLong-L1.5: Post-Training Recipe for Long-Context Reasoning and Memory Management 论文总结

---

## 1. 论文的主要贡献和创新点

### 解决的问题
本文旨在解决现代大语言模型（LLMs）在**长上下文推理（long-context reasoning）**能力上的瓶颈，尤其是在后训练（post-training）阶段缺乏系统化、端到端的解决方案。具体挑战包括：
- 高质量、复杂长上下文推理数据的稀缺；
- 在长序列上进行强化学习（RL）时的训练不稳定性；
- 当任务超出模型物理上下文窗口（如超过256K tokens）时的处理能力不足。

### 提出的新方法与创新
作者提出了 **QwenLong-L1.5**，一个基于 Qwen3-30B-A3B-Thinking 的长上下文推理模型，并贡献了一套完整的“后训练配方”（post-training recipe），包含三大核心技术突破：

#### （1）长上下文数据合成管道（Long-Context Data Synthesis Pipeline）
- **方法**：通过将文档分解为原子事实及其关系，构建知识图谱（Knowledge Graph），并在此基础上程序化生成需要多跳推理（multi-hop reasoning）、数值计算、假设分析等复杂任务的问答对。
- **优势**：超越简单的“针在 haystack 中”检索任务，生成真正需要全局信息整合的高价值训练样本。支持多种题型（因果分析、时间推理、观点分析等），显著提升数据多样性与难度。

#### （2）稳定的长上下文强化学习（Stabilized Reinforcement Learning）
- **方法**：
  - **任务均衡采样（Task-balanced sampling）**：确保每个训练批次中各类任务均匀分布，缓解多任务训练中的奖励偏差。
  - **自适应熵控制策略优化（AEPO, Adaptive Entropy-Controlled Policy Optimization）**：动态调节探索-利用权衡，通过监控策略熵来决定是否屏蔽负梯度，防止训练崩溃。
- **优势**：有效解决了长上下文 RL 中因高相似性错误路径导致的信用分配难题（credit assignment problem），实现稳定训练并扩展至更长序列。

#### （3）增强记忆架构用于超长上下文（Memory-Augmented Architecture）
- **方法**：引入 **Memory Agent** 范式，结合单次全上下文推理与迭代式内存更新机制。采用多阶段融合 RL 训练，先分别训练“全上下文专家”和“内存代理专家”，再通过 SCE 算法合并。
- **优势**：使模型能够处理长达 4M tokens 的输入，突破传统注意力机制的计算限制，同时保持高效的信息压缩与规划能力。

---

## 2. 核心实验方法和设置

### 数据集
- **训练数据**：从 42.7K 合成样本中筛选出 14.1K 高质量 RL 训练样本，涵盖五大领域：
  - Code repositories
  - Academic literature
  - Professional documents
  - General knowledge and literature
  - Dialogue data
- **评估基准**：
  - **LongBench-V1/V2**：多跳问答、长对话理解、代码库理解等。
  - **MRCR (Michelangelo Retrieval Challenge)**：测试在超长合成对话中定位多个“针”的能力。
  - **Frames**：维基百科主题上的多跳推理。
  - **DocMath**：财务报告中的数学推理。
  - **CorpusQA**：跨大规模文档集合的全局推理。
  - **BFCL-V4** 和 **LongMemEval**：用于评估通用能力、工具使用与长对话记忆。

### 实验设置
- **基础模型**：Qwen3-30B-A3B-Thinking-2507
- **训练框架**：VeRL，采用 GRPO（Group Relative Policy Optimization）作为 RL 算法。
- **训练策略**：渐进式长度扩展（progressive length extension），分四阶段逐步增加最大输入/输出长度（最高达 120K / 50K tokens）。
- **评估配置**：统一使用 128K 输入长度、50K 输出长度；温度 0.7，top-p 0.95。
- **评分方式**：
  - 多选题：Accuracy
  - MRCR：SequenceMatcher ratio
  - 多跳 QA：CEM（Cover Exact Match）或 LLM-as-a-judge（使用 DeepSeek-V3 判断语义等价）

### 基线方法对比
- **旗舰模型**：GPT-5、Gemini-2.5-Pro
- **轻量级推理模型**：Gemini-2.5-Flash-Thinking、DeepSeek-R1-0528、Qwen3-Max-Thinking
- **基线模型**：Qwen3-30B-A3B-Thinking-2507（直接比较对象）
- **其他代理方法**：MemAgent-14B、Qwen-Flash-Thinking-1M

---

## 3. 主要实验结果和性能指标

### 关键性能数据
| 模型 | 平均得分 | MRCR | CorpusQA | LongBench-V2 |
|------|----------|-------|-----------|---------------|
| Qwen3-30B-A3B-Thinking-2507 | 61.92 | 51.27 | 71.56 | 49.11 |
| **QwenLong-L1.5-30B-A3B** | **71.82** | **82.99** | **81.25** | **55.27** |

- 在六项主流长上下文推理基准上，QwenLong-L1.5 相比其基线平均提升 **+9.90 分**。
- 在 MRCR 上达到 **82.99**，接近 GPT-5（77.29）和 Gemini-2.5-Pro（79.92）。
- 在 CorpusQA 上得分为 **81.25**，媲美 GPT-5（81.56）。

### 与基线方法的对比结果
- 性能优于所有轻量级模型（如 Gemini-2.5-Flash-Thinking、DeepSeek-R1-0528）。
- 整体表现可与 GPT-5 和 Gemini-2.5-Pro 等顶级闭源模型相媲美。
- 在 **超长上下文任务（1M~4M tokens）** 上，其 Memory-Agent 架构相比基线代理配置提升 **+9.48 分**。

### 消融实验结果
| 方法 | 平均得分 | 相比 GRPO 提升 |
|------|----------|----------------|
| GRPO baseline | 56.07 | — |
| + Task-balanced sampling | 56.86 | +0.79 |
| + Task-balanced + Task-specific advantage | 58.62 | **+2.55** |
| + AEPO | **59.36** | **+3.29** |

- **AEPO** 显著提升训练稳定性，在 Qwen3-4B-Thinking 上带来 +3.29 分增益。
- **任务特定优势估计** 对密集奖励任务（如 MRCR）尤其有效。

---

## 4. 关键结论和发现

### 主要发现
1. **合成高质量长上下文数据是提升推理能力的关键**：通过结构化知识提取与多跳路径构造，生成的数据显著增强了模型的多跳推理与信息聚合能力。
2. **稳定 RL 是长上下文训练的前提**：AEPO 和任务均衡采样有效缓解了长序列 RL 的不稳定性，使得模型能在更长输入上持续学习。
3. **Memory Agent 可扩展至极端长度**：在 4M tokens 规模下仍能保持有效推理，验证了该架构的可扩展性。
4. **长上下文能力具有泛化效应**：不仅在长上下文任务上表现优异，还在科学推理（AIME25 +3.65）、工具使用（Memory-KV +5.80）和长对话记忆（LongMemEval +15.60）上取得显著提升。

### 局限性
- 当前数据合成依赖外部 LLM 或 API，存在成本与资源瓶颈。
- 奖励机制仍以规则+LLM-as-a-judge为主，难以应对开放性任务。
- 缺乏对长输入+长输出场景的支持（如报告生成、章节修订）。
- 尚未支持多模态长序列推理。

### 未来工作方向
1. **构建闭环数据飞轮（Closed-Loop Data Flywheel）**：利用已训练的强模型自身生成新的训练数据与思维轨迹，降低对外部资源依赖。
2. **发展细粒度信用分配机制**：从序列级奖励转向 token-level 或 step-level credit assignment，提高学习效率。
3. **研发更复杂的奖励模型**：探索基于 rubric 的评分系统，支持主观、多维度的任务评价。
4. **扩展任务类型与模态**：覆盖长输入-长输出任务，并向图像、音频等多模态长序列推理延伸。

--- 

> ✅ **总结**：QwenLong-L1.5 提供了一个系统性的长上下文推理增强方案，证明了通过高质量数据合成、稳定 RL 训练和记忆增强架构，开源模型可以达到甚至逼近顶级闭源模型的长上下文推理水平，且具备良好的泛化能力。

</details>

---

### 14. [Accelerating Sparse Matrix-Matrix Multiplication on GPUs with Processing Near HBMs](https://arxiv.org/abs/2512.12036)

**Authors**: Shiju Li, Younghoon Min, Hane Yie, Hoshik Kim, Soohong Ahn, Joonseop Sim, Chul-Ho Lee, Jongryool Kim  
**Category**: cs.DC  
**Published**: 2025-12-16  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2512.12036v1  

#### Abstract
Sparse General Matrix-Matrix Multiplication (SpGEMM) is a fundamental operation in numerous scientific computing and data analytics applications, often bottlenecked by irregular memory access patterns. This paper presents Hash based Multi-phase SpGEMM on GPU and the Acceleration of Indirect Memory A...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：Accelerating Sparse Matrix-Matrix Multiplication on GPUs with Processing Near HBMs

---

## 1. 论文的主要贡献和创新点

### **解决的问题**
稀疏矩阵-矩阵乘法（SpGEMM）是科学计算、图分析和图神经网络（GNN）中的核心操作，但在现代GPU上执行时面临以下挑战：
- **不规则内存访问模式**导致缓存命中率低、空间局部性差；
- 输出矩阵非零元数量未知，难以高效分配资源；
- 并行插入操作开销大，负载均衡困难；
- 元数据访问频繁，压缩存储格式带来额外开销。

这些问题使得传统GPU实现难以充分发挥其高带宽和并行能力。

---

### **提出的新方法与创新思路**

本文提出了一个**软硬件协同设计框架**，通过在GPU的高带宽内存（HBM）附近引入专用处理单元来优化SpGEMM性能。主要创新包括：

#### （1）**Hash-based Multi-phase SpGEMM 算法（软件层）**
- 采用三阶段策略：**Row-grouping → Allocation → Accumulation**。
- 引入基于对数分桶的**行分组机制（Row-grouping）**，实现动态负载均衡。
- 设计两种线程分配策略：
  - **PWPR（Partial Warp Per Row）**：轻负载使用4线程/行；
  - **TBPR（Thread Block Per Row）**：重负载使用完整Block处理单行。
- 利用共享内存哈希表进行高效累积，并支持回退到全局内存以应对大规模行。

#### （2）**Processing-Near-HBM 加速技术：AIA（Acceleration of Indirect memory Access）**
- 在HBM控制器中集成**AIA引擎**，位于GPU核心与HBM堆栈之间。
- 支持**Ranged Indirect Access**功能，将SpGEMM中的两级间接访问（如 `x[a[i]]`, `x[a[i]+1]...`）转换为批量连续流式访问。
- 将原本需要多次往返主存的操作合并为一次请求-响应交互，显著减少延迟和内存流量。

#### （3）**软硬协同架构设计**
- 软件算法适配硬件特性（如分组映射到AIA请求），硬件反馈提升缓存利用率。
- AIA直接作用于HBM层级，避免中间缓存污染，提升L1/L2缓存效率。

---

### **相比现有方法的优势**

| 方面 | 优势 |
|------|------|
| **性能** | 显著优于cuSPARSE，在多种应用中实现高达76.5%的时间减少 |
| **能效与带宽利用** | 减少无效数据移动，提高HBM利用率 |
| **可扩展性** | 对大规模稀疏图表现更优，尤其适用于GNN等迭代型任务 |
| **通用性** | 可应用于图聚类、图收缩、GNN训练等多种场景 |

---

## 2. 核心实验方法和设置

### **使用的数据集**

#### （1）**通用SpGEMM测试矩阵**
来自University of Florida Sparse Matrix Collection：
- `RoadTX`, `p2p-Gnutella04`, `amazon0601`, `web-Google`, `scircuit`, `cit-Patents`, `Economics`, `webbase-1M`, `wb-edu`, `cage15`, `Wind Tunnel`, `Protein`

涵盖不同规模、密度和NNZ分布的矩阵。

#### （2）**图分析应用数据集**
用于评估Markov Clustering (MCL) 和 Graph Contraction：
- 同上部分矩阵，重点关注`RoadNet-TX`, `Wind Tunnel`, `Protein`, `Economics`

#### （3）**GNN训练基准数据集**
共6个，覆盖社交、生物、电商等领域：
| Dataset | Nodes | Edges | Avg Degree |
|--------|-------|-------|------------|
| Flickr | 89K | 0.99M | 22.16 |
| ogbn-proteins | 132K | 79M | 1,193.92 |
| ogbn-arxiv | 169K | 1.3M | 15.77 |
| Reddit | 233K | 115M | 985.99 |
| Yelp | 717K | 14M | 38.93 |
| ogbn-products | 2.45M | 126M | 103.05 |

模型：GCN, GIN, GraphSAGE

---

### **实验设置与评估指标**

- **硬件平台**：
  - CPU: AMD EPYC 9555 64-core @ 3.2GHz
  - GPU: NVIDIA H200（集成AIA模块）
  - 内存: 1.5TB RAM
- **软件环境**：Linux系统，CUDA支持
- **评估指标**：
  - 执行时间（Runtime）
  - GFLOPS（基于中间产物数量计算）
  - L1 Cache Hit Ratio
  - 训练时间加速比（针对GNN）
  - Speedup / Time Reduction 相对于基线

---

### **基线方法对比**

| 基线方法 | 描述 |
|--------|------|
| **cuSPARSE** | NVIDIA官方稀疏库，作为主要对比基准 |
| **Software-only Implementation** | 本文提出的Hash-based多相算法但无AIA硬件加速，用于消融研究 |

---

## 3. 主要实验结果和性能指标

### **关键性能数据汇总**

| 应用类别 | 指标 | 结果 |
|--------|------|------|
| **基础SpGEMM（自乘）** | 平均运行时间降低 | **80.5%** vs cuSPARSE |
| | 平均吞吐量提升 | **6.87×** GFLOPS提升 |
| | 最佳案例（Wind Tunnel） | 达到 **9.4×** 吞吐量提升 |
| **L1 Cache命中率** | Accumulation阶段 | 从64.41% → **75.14%** |
| | Allocation阶段 | 从64.66% → **88.15%** |
| **Graph Contraction** | 平均时间减少 | **76.5%** vs cuSPARSE |
| | 最高提升（Protein） | **91.1%** 时间减少 |
| **Markov Clustering (MCL)** | 平均时间减少 | **58.4%** vs cuSPARSE |
| | 最高提升（Economics） | **88.7%** 时间减少 |
| **GNN训练（含结构化剪枝）** | 平均训练时间减少（vs 软件版） | **30.3%** |
| | 平均训练时间减少（vs cuSPARSE） | **48.6%** |
| | 最大加速（Products数据集） | **76.1%** 时间减少（即约4.18× speedup） |
| | 平均速度提升（vs cuSPARSE） | **1.95×** |

---

### **与基线方法的对比结果**

- **vs cuSPARSE**：
  - 在所有测试场景下均大幅领先；
  - 特别是在大规模、高稀疏度图上优势明显（如`ogbn-products`）；
  - 对复杂迭代算法（如MCL）效果尤为突出。

- **vs 软件-only实现**：
  - AIA带来额外 **10–27%** 的运行时间缩减；
  - 表明硬件加速确实有效缓解了内存墙问题。

---

### **消融实验结果**

- **AIA对Cache的影响**：
  - L1 Cache Hit Ratio在Allocation阶段提升超过23个百分点；
  - 说明AIA成功将随机访问转化为顺序流，极大改善缓存行为。

- **图规模与性能增益的关系**：
  - Pearson相关系数达 **r = 0.94**，显示AIA收益随图节点数增长而上升；
  - 小图（如Flickr）仅获~15%改进；
  - 大图（如Products）达到 **89.16%** 时间减少（vs 软件版）；
  - 超线性扩展趋势出现在 >500K节点图中。

- **不同GNN架构一致性好**：
  - GCN、GIN、GraphSAGE三者性能提升方差小于3%，表明方法具有**架构无关性**。

---

## 4. 关键结论和发现

### **主要发现**

1. **Processing-Near-HBM 是解决SpGEMM内存瓶颈的有效路径**  
   AIA通过在HBM层级处理间接访问，从根本上改变了数据流动模式，显著提升了缓存效率和带宽利用率。

2. **软硬件协同设计至关重要**  
   单纯软件优化已达瓶颈；结合定制化近内存处理单元（AIA）才能突破“内存墙”。

3. **AIA在真实应用场景中表现出色**  
   不仅在基础SpGEMM上表现优异，在**图收缩、MCL聚类、GNN训练**等复杂应用中也取得显著加速。

4. **性能增益随图规模扩大而增强**  
   验证了该方案特别适合**大规模图处理和生产级GNN训练**。

5. **结构化剪枝 + AIA 实现乘法效应**  
   算法级稀疏性与架构级优化相结合，带来叠加收益。

---

### **方法的局限性**

1. **依赖特定HBM架构改造**  
   AIA需嵌入HBM控制器，目前尚属定制化设计，尚未标准化，部署成本较高。

2. **编程模型复杂度增加**  
   开发者需理解AIA接口（如`AIA_range`调用），增加了编程负担。

3. **面积与功耗开销未详细量化**  
   文中未提供AIA引擎的具体面积/功耗影响，可能限制其在移动端或低功耗设备的应用。

4. **仅验证于H200 GPU**  
   缺乏跨平台泛化能力验证（如Ampere、Ada等其他架构）。

---

### **未来工作方向**

1. **推广AIA至更多稀疏操作**  
   如SpMV、稀疏Attention、稀疏卷积等，构建统一的PNM稀疏加速生态。

2. **自动化编译器支持**  
   开发自动识别SpGEMM模式并生成AIA调用代码的编译器工具链。

3. **标准化AIA指令集**  
   推动行业标准制定，使AIA成为下一代GPU的标准功能模块。

4. **探索更多近内存功能单元组合**  
   结合PIM（Processing-in-Memory）与其他专用单元，打造更强大的近内存计算范式。

5. **扩展至分布式训练场景**  
   利用AIA减少跨节点通信开销，支持更大规模图的分布式SpGEMM。

---

> ✅ **总结一句话**：  
> 本论文提出了一种基于**Processing-Near-HBM**的新型SpGEMM加速架构AIA，通过软硬件协同设计，将不规则内存访问转化为高效流式访问，在各类图应用中实现了最高达**76.5%的运行时间减少**和**6.87×吞吐量提升**，为解决GPU上的稀疏计算“内存墙”问题提供了极具前景的技术路线。

</details>

---

### 15. [Link-Aware Energy-Frugal Continual Learning for Fault Detection in IoT Networks](https://arxiv.org/abs/2512.13340)

**Authors**: Henrik C. M. Frederiksen, Junya Shiraishi, Cedomir Stefanovic, Hei Victor Cheng, Shashi Raj Pandey  
**Category**: cs.LG  
**Published**: 2025-12-16  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2512.13340v1  

#### Abstract
The use of lightweight machine learning (ML) models in internet of things (IoT) networks enables resource constrained IoT devices to perform on-device inference for several critical applications. However, the inference accuracy deteriorates due to the non-stationarity in the IoT environment and limi...

<details>
<summary><strong>🤖 AI Summary (by qwen-long)</strong> - Click to expand</summary>

# 论文总结：*Link-Aware Energy-Frugal Continual Learning for Fault Detection in IoT Networks*

---

## 1. 论文的主要贡献和创新点

### 解决的问题
在资源受限的 IoT 设备上部署轻量级 Machine Learning（ML）模型进行 **on-device inference**（如故障检测）时，面临以下挑战：
- 初始训练数据不足，难以覆盖所有潜在故障；
- 环境非平稳性导致模型性能随时间下降；
- 频繁更新模型会带来高昂的通信能耗，而 IoT 设备通常能量受限；
- 固定周期的数据上传和模型更新策略无法适应动态变化的无线链路条件和能量预算。

因此，如何在 **有限的能量和带宽约束下**，持续提升模型的推理准确率（尤其是对罕见故障的检测能力），是一个亟待解决的关键问题。

---

### 提出的新方法：ACORD
本文提出了一种名为 **ACORD**（Adaptive Compression Online Resource-aware fault Detection）的 **事件驱动型、节能的持续学习框架**，其核心创新包括：

#### ✅ 创新点 1：事件驱动通信机制
- IoT 设备仅在检测到潜在异常事件（即可能为故障）时才触发数据上传，避免无意义的周期性采样，显著降低通信开销。
- 引入“context window”机制，上传当前样本及其前后 $2W+1$ 个相关时序样本，增强边缘服务器（ES）的训练数据上下文信息。

#### ✅ 创新点 2：链路感知的自适应模型压缩
- 在 ES 更新模型后，根据当前估计的下行链路速率 $R_{DL}$ 和可用能量预算 $E_{\text{th}}$，动态调整模型压缩参数：
  - **Pruning Level (PL)**：剪枝比例
  - **Quantization Level (QL)**：量化位数（如 8-bit 或 32-bit）
- 压缩策略基于线性回归建模传输时间与模型大小的关系，并结合目标延迟 $t_{DL}$ 进行优化，确保在能耗限制内完成模型下发。

#### ✅ 创新点 3：联合优化通信与推理参数
- 同时优化三个关键参数：
  1. 上行数据压缩中的 **context window 大小 $W$**
  2. 下行模型压缩中的 **PL 和 QL**
  3. 故障判断阈值 **$T_{\text{th}}$**
- 所有参数均依据实时链路状态和能量消耗动态调整，实现能效与精度的平衡。

---

### 相比现有方法的优势
| 方面 | ACORD | 基线方法（Hawk / Periodic Sampling） |
|------|-------|-------------------------------|
| 能量效率 | ⭐ 高：按需上传 + 自适应压缩 | ❌ 低：固定高开销或盲目上传 |
| 模型更新质量 | ⭐ 高：只传有价值数据，提升训练有效性 | ❌ 中/低：可能包含冗余或无关数据 |
| 链路适应性 | ⭐ 支持：根据 $R_{UL}/R_{DL}$ 动态调节 | ❌ 不支持：静态配置 |
| 推理准确性（recall） | ⭐ 显著更高，尤其在低能量场景 | ❌ 受限于早期耗尽能量 |

---

## 2. 核心实验方法和设置

### 使用的数据集
- **pump_sensor_data**（来自 Kaggle）
  - 包含约 220,000 条记录，每条有 50 维传感器特征
  - 标签类别：`NORMAL`, `RECOVERING`, `BROKEN`
  - 将 `BROKEN` 视为故障状态（正类），其余为正常
  - 数据集中仅有 **7 个故障事件**，体现“罕见故障”的实际挑战
  - 初始训练集占 10%，测试集占 90%，且所有故障保留在测试集中

---

### 实验设置
- **硬件平台**：Ubuntu 24.04 主机模拟 IoT 设备与 Edge Server（ES），通过 Wi-Fi 路由器连接
- **通信协议**：TCP 协议保障可靠传输
- **模型类型**：
  - Autoencoder (AE)：用于重构误差-based 异常检测
  - Binary Classifier (BC)：直接分类
- **模型部署工具**：TensorFlow Lite Micro
- **能耗模型**：
  - 发射功率 $\xi_{tx} = 0.79\,\text{W}$，接收功率 $\xi_{rx} = 0.33\,\text{W}$
  - 单次推理能耗 $E_{\text{inf}}$：QL=8 时为 1.4 μJ；QL=32 时为 6.6 μJ
- **参考能量 $E_{\text{ref}} = 60\,\text{J}$**

---

### 评估指标
| 指标 | 定义 |
|------|------|
| **Recall（召回率）** | 正确识别出的实际故障占比，即 $\frac{\text{TP}}{\text{TP} + \text{FN}}$ |
| **Energy Consumption ($E_{\text{total}}$)** | 每轮通信（上行+下行）与计算（推理）总能耗 |
| **ROC Curve & TPR/FPR** | 衡量不同 $T_{\text{th}}$ 下的真阳性率与假阳性率权衡 |

---

### 基线方法对比
1. **Hawk [4]**：
   - 非自适应 CL 方法
   - 固定参数：$PL=0$, $QL=32$, $W=200$
   - 若某轮超出能量预算，则停止更新甚至终止任务
2. **Periodic Sampling**：
   - 定期上传数据，不考虑数据重要性
   - 上下文窗口 $W=200$，周期根据 $E_{\text{th}}$ 调整以满足能耗约束

---

## 3. 主要实验结果和性能指标

### 关键性能数据（见 Fig. 1b 和 1c）

#### 🔹 在不同能量约束下的 Recall 表现（Fig. 1b）
- 当 $E_{\text{th}} = 60\,\text{J}$ 时：
  - **ACORD** 的 recall 达到 **~0.75**
  - **Hawk** 约为 **0.55**
  - **Periodic Sampling** 约为 **0.45**
- 在低能量条件下（如 $E_{\text{th}} < 30\,\text{J}$），ACORD 显著优于两者，**最高提升达 42.8%**

> 💡 原因：ACORD 更高效地利用有限能量进行“有意义”的数据交换和模型更新，延长了有效运行时间。

#### 🔹 在不同网络带宽下的 Recall 表现（Fig. 1c）
- 当带宽从 1 Mbps 增加到 10 Mbps：
  - 所有方法 recall 均上升（更多通信机会）
  - 但在 **低带宽区域（< 3 Mbps）**，ACORD 明显领先
- 在 1 Mbps 下：
  - ACORD recall ≈ 0.75
  - Hawk ≈ 0.5
  - Periodic ≈ 0.35

> 💡 原因：ACORD 利用链路感知压缩，减少传输时间，在低速链路上仍可完成完整更新流程。

#### 🔹 分类器比较（Fig. 1a）
- **Autoencoder (AE)** 明显优于 Binary Classifier (BC)
  - AE 在保持低 FPR 的同时获得更高 TPR
  - 因其损失函数设计允许从 FP 和 TP 样本中共同学习（通过负权重 $\lambda_1 = -0.1$ 抑制故障样本重建）
- 最终选择 AE 作为默认 FD 模型

---

### 消融分析（隐含在参数优化过程）
虽然未明确列出消融实验表格，但从参数优化流程可见：
- **自适应压缩（PL/QL）** 对满足能量约束至关重要；
- **context window 大小 $W$** 影响训练数据丰富度，过大增加能耗，过小影响模型改进；
- **决策阈值 $T_{\text{th}}$** 通过 ROC 曲线优化，找到最佳 FPR-TPR 平衡点；
- 联合优化三者实现了端到端性能最大化。

---

## 4. 关键结论和发现

### 主要发现
1. **事件驱动 + 自适应压缩 = 高效持续学习**
   - 仅在必要时通信，配合链路感知压缩，可在严苛能量限制下维持长期模型更新。
2. **ACORD 显著优于固定策略**
   - 在低能量（<60 J）和低带宽（<3 Mbps）环境下，recall 提升高达 **42.8%**
   - 特别适合工业 IoT 中罕见故障检测等长尾应用场景。
3. **AE 比 BC 更适合小样本异常检测**
   - 利用 reconstruction error 和定制化 loss 函数，能在缺乏正例情况下有效学习。
4. **系统级联合优化是关键**
   - 单独优化通信或模型无法达到最优，必须协同设计数据上传、模型压缩、推理阈值等模块。

---

### 方法的局限性
1. **单设备假设**：
   - 当前框架基于单一 IoT 设备与 ES 交互，未考虑多节点竞争信道的情况。
2. **依赖可靠标签反馈**：
   - 模型训练依赖系统所有者提供的故障标签，在无人工干预场景下难以扩展。
3. **线性回归近似传输时间**：
   - 使用线性模型拟合 $b(PL,QL)$ 与传输时间关系，可能在复杂网络环境中不够精确。
4. **未考虑本地计算延迟**：
   - 忽略 MCU 上推理以外的操作延迟（如调度、内存访问等）。

---

### 未来工作方向
1. 扩展至 **多设备并发场景**，设计公平高效的信道接入与模型更新协调机制；
2. 探索 **半监督或无监督标签生成机制**，减少对外部标注的依赖；
3. 引入更精细的 **链路预测模型**（如 LSTM）替代线性回归，提升压缩决策准确性；
4. 结合 **semantic communication** 思想，进一步压缩语义上有价值的信息而非原始数据。

---

> ✅ **总结一句话**：  
> ACORD 通过**事件驱动通信 + 链路感知模型压缩 + 联合参数优化**，实现了在能量与带宽受限的 IoT 网络中高效、可持续的故障检测，显著提升了 recall，特别是在资源极度紧张的真实场景中表现出强大优势。

</details>

---

### 16. [DP-EMAR: A Differentially Private Framework for Autonomous Model Weight Repair in Federated IoT Systems](https://arxiv.org/abs/2512.13460)

**Authors**: Chethana Prasad Kabgere, Shylaja S S  
**Category**: cs.LG  
**Published**: 2025-12-16  
**Score**: 8.5  
**Type**: new  
**ArXiv ID**: 2512.13460v1  

#### Abstract
Federated Learning (FL) enables decentralized model training without sharing raw data, but model weight distortion remains a major challenge in resource constrained IoT networks. In multi tier Federated IoT (Fed-IoT) systems, unstable connectivity and adversarial interference can silently alter tran...

---

### 17. [Log Anomaly Detection with Large Language Models via Knowledge-Enriched Fusion](https://arxiv.org/abs/2512.11997)

**Authors**: Anfeng Peng, Ajesh Koyatan Chathoth, Stephen Lee  
**Category**: cs.AI  
**Published**: 2025-12-16  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2512.11997v1  

#### Abstract
System logs are a critical resource for monitoring and managing distributed systems, providing insights into failures and anomalous behavior. Traditional log analysis techniques, including template-based and sequence-driven approaches, often lose important semantic information or struggle with ambig...

---

### 18. [Personalized QoE Prediction: A Demographic-Augmented Machine Learning Framework for 5G Video Streaming Networks](https://arxiv.org/abs/2512.12736)

**Authors**: Syeda Zunaira Ahmed, Hejab Tahira Beg, Maryam Khalid  
**Category**: cs.AI  
**Published**: 2025-12-16  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2512.12736v1  

#### Abstract
Quality of Experience (QoE) prediction is a critical component of modern multimedia systems, particularly for adaptive video streaming in 5G networks. Accurate QoE estimation enables intelligent resource management and supports user centric service delivery. Existing QoE prediction approaches primar...

---

### 19. [AutoTool: Dynamic Tool Selection and Integration for Agentic Reasoning](https://arxiv.org/abs/2512.13278)

**Authors**: Jiaru Zou, Ling Yang, Yunzhe Qi, Sirui Chen, Mengting Ai, Ke Shen, Jingrui He, Mengdi Wang  
**Category**: cs.CL  
**Published**: 2025-12-16  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2512.13278v1  

#### Abstract
Agentic reinforcement learning has advanced large language models (LLMs) to reason through long chain-of-thought trajectories while interleaving external tool use. Existing approaches assume a fixed inventory of tools, limiting LLM agents' adaptability to new or evolving toolsets. We present AutoToo...

---

### 20. [AIR: Post-training Data Selection for Reasoning via Attention Head Influence](https://arxiv.org/abs/2512.13279)

**Authors**: Jinrui Liu, Jeff Wu, Xuanguang Pan, Gavin Cheung, Shuai Ma, Chongyang Tao  
**Category**: cs.CL  
**Published**: 2025-12-16  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2512.13279v1  

#### Abstract
LLMs achieve remarkable multi-step reasoning capabilities, yet effectively transferring these skills via post-training distillation remains challenging. Existing data selection methods, ranging from manual curation to heuristics based on length, entropy, or overall loss, fail to capture the causal i...

---

### 21. [Anchoring Values in Temporal and Group Dimensions for Flow Matching Model Alignment](https://arxiv.org/abs/2512.12387)

**Authors**: Yawen Shao, Jie Xiao, Kai Zhu, Yu Liu, Wei Zhai, Yang Cao, Zheng-Jun Zha  
**Category**: cs.LG  
**Published**: 2025-12-16  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2512.12387v1  

#### Abstract
Group Relative Policy Optimization (GRPO) has proven highly effective in enhancing the alignment capabilities of Large Language Models (LLMs). However, current adaptations of GRPO for the flow matching-based image generation neglect a foundational conflict between its core principles and the distinc...

---

### 22. [On Approaches to Building Surrogate ODE Models for Diffusion Bridges](https://arxiv.org/abs/2512.12671)

**Authors**: Maria Khilchuk, Vladimir Latypov, Pavel Kleshchev, Alexander Hvatov  
**Category**: cs.LG  
**Published**: 2025-12-16  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2512.12671v1  

#### Abstract
Diffusion and Schr\"{o}dinger Bridge models have established state-of-the-art performance in generative modeling but are often hampered by significant computational costs and complex training procedures. While continuous-time bridges promise faster sampling, overparameterized neural networks describ...

---

### 23. [Noise-Resilient Quantum Aggregation on NISQ for Federated ADAS Learning](https://arxiv.org/abs/2512.13196)

**Authors**: Chethana Prasad Kabgere, Sudarshan T S B  
**Category**: cs.LG  
**Published**: 2025-12-16  
**Score**: 8.0  
**Type**: new  
**ArXiv ID**: 2512.13196v1  

#### Abstract
Advanced Driver Assistance Systems (ADAS) increasingly employ Federated Learning (FL) to collaboratively train models across distributed vehicular nodes while preserving data privacy. Yet, conventional FL aggregation remains susceptible to noise, latency, and security constraints inherent to real-ti...

---

### 24. [Reflective Preference Optimization (RPO): Enhancing On-Policy Alignment via Hint-Guided Reflection](https://arxiv.org/abs/2512.13240)

**Authors**: Zihui Zhao, Zechang Li  
**Category**: cs.AI  
**Published**: 2025-12-16  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2512.13240v1  

#### Abstract
Direct Preference Optimization (DPO) has emerged as a lightweight and effective alternative to Reinforcement Learning from Human Feedback (RLHF) and Reinforcement Learning with AI Feedback (RLAIF) for aligning large language and vision-language models. However, the standard DPO formulation, in which...

---

### 25. [LightTopoGAT: Enhancing Graph Attention Networks with Topological Features for Efficient Graph Classification](https://arxiv.org/abs/2512.13617)

**Authors**: Ankit Sharma, Sayan Roy Gupta  
**Category**: cs.LG  
**Published**: 2025-12-16  
**Score**: 7.5  
**Type**: new  
**ArXiv ID**: 2512.13617v1  

#### Abstract
Graph Neural Networks have demonstrated significant success in graph classification tasks, yet they often require substantial computational resources and struggle to capture global graph properties effectively. We introduce LightTopoGAT, a lightweight graph attention network that enhances node featu...

---

### 26. [AGAPI-Agents: An Open-Access Agentic AI Platform for Accelerated Materials Design on AtomGPT.org](https://arxiv.org/abs/2512.11935)

**Authors**: Jaehyung Lee, Justin Ely, Kent Zhang, Akshaya Ajith, Charles Rhys Campbell, Kamal Choudhary  
**Category**: cs.AI  
**Published**: 2025-12-16  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.11935v1  

#### Abstract
Artificial intelligence is reshaping scientific discovery, yet its use in materials research remains limited by fragmented computational ecosystems, reproducibility challenges, and dependence on commercial large language models (LLMs). Here we introduce AGAPI (AtomGPT.org API), an open-access agenti...

---

### 27. [MAC: A Multi-Agent Framework for Interactive User Clarification in Multi-turn Conversations](https://arxiv.org/abs/2512.13154)

**Authors**: Emre Can Acikgoz, Jinoh Oh, Joo Hyuk Jeon, Jie Hao, Heng Ji, Dilek Hakkani-T\"ur, Gokhan Tur, Xiang Li, Chengyuan Ma, Xing Fan  
**Category**: cs.AI  
**Published**: 2025-12-16  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.13154v1  

#### Abstract
Conversational agents often encounter ambiguous user requests, requiring an effective clarification to successfully complete tasks. While recent advancements in real-world applications favor multi-agent architectures to manage complex conversational scenarios efficiently, ambiguity resolution remain...

---

### 28. [SPARS: A Reinforcement Learning-Enabled Simulator for Power Management in HPC Job Scheduling](https://arxiv.org/abs/2512.13268)

**Authors**: Muhammad Alfian Amrizal, Raka Satya Prasasta, Santana Yuda Pradata, Kadek Gemilang Santiyuda, Reza Pulungan, Hiroyuki Takizawa  
**Category**: cs.DC  
**Published**: 2025-12-16  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.13268v1  

#### Abstract
High-performance computing (HPC) clusters consume enormous amounts of energy, with idle nodes as a major source of waste. Powering down unused nodes can mitigate this problem, but poorly timed transitions introduce long delays and reduce overall performance. To address this trade-off, we present SPA...

---

### 29. [Meta-Continual Mobility Forecasting for Proactive Handover Prediction](https://arxiv.org/abs/2512.11841)

**Authors**: Sasi Vardhan Reddy Mandapati  
**Category**: cs.LG  
**Published**: 2025-12-16  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.11841v1  

#### Abstract
Short-term mobility forecasting is a core requirement for proactive handover (HO) in cellular networks. Real-world mobility is highly non-stationary: abrupt turns, rapid speed changes, and unpredictable user behavior cause conventional predictors to drift, leading to mistimed or failed handovers. We...

---

### 30. [Tiny Recursive Models on ARC-AGI-1: Inductive Biases, Identity Conditioning, and Test-Time Compute](https://arxiv.org/abs/2512.11847)

**Authors**: Antonio Roye-Azar, Santiago Vargas-Naranjo, Dhruv Ghai, Nithin Balamurugan, Rayan Amir  
**Category**: cs.LG  
**Published**: 2025-12-16  
**Score**: 7.0  
**Type**: new  
**ArXiv ID**: 2512.11847v1  

#### Abstract
Tiny Recursive Models (TRM) were proposed as a parameter-efficient alternative to large language models for solving Abstraction and Reasoning Corpus (ARC) style tasks. The original work reports strong performance and suggests that recursive latent updates enable non-trivial reasoning, but it remains...

---

## 🔧 Configuration

This bot is configured to look for papers containing the following keywords:
- framework, System, Generation, Video, Linear, LLM, RL, RLHF, Inference, Training, Attention, Pipeline, MOE, Sparse, Quantization, Speculative, Efficient, Efficiency, Framework, Parallel, Distributed, Kernel, Decode, Decoding, Prefill, Throughput, Fast, Network, Hardware, Cluster, FP8, FP4, Optimization, Scalable, Communication

## 📅 Schedule

The bot runs daily at 12:00 UTC via GitHub Actions to fetch the latest papers.

## 🚀 How to Use

1. **Fork this repository** to your GitHub account
2. **Customize the configuration** by editing `config.json`:
   - Add/remove arXiv categories (e.g., `cs.AI`, `cs.LG`, `cs.CL`)
   - Modify keywords to match your research interests
   - Adjust `max_papers` and `days_back` settings
3. **Enable GitHub Actions** in your repository settings
4. **The bot will automatically run daily** and update the README.md

## 📝 Customization

### arXiv Categories
Common categories include:
- `cs.AI` - Artificial Intelligence
- `cs.LG` - Machine Learning
- `cs.CL` - Computation and Language
- `cs.CV` - Computer Vision
- `cs.NE` - Neural and Evolutionary Computing
- `stat.ML` - Machine Learning (Statistics)

### Keywords
Add keywords that match your research interests. The bot will search for these terms in paper titles and abstracts.

### Exclude Keywords
Add terms to exclude certain types of papers (e.g., "survey", "review", "tutorial").

## 🔍 Manual Trigger

You can manually trigger the bot by:
1. Going to the "Actions" tab in your repository
2. Selecting "arXiv Bot Daily Update"
3. Clicking "Run workflow"

---
*Generated automatically by arXiv Bot* 
